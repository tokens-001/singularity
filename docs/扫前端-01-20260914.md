# 扫前端（ZCode · GLM-5.3-Flash）+ 我的核实

> 下面是**报告原文一字未改**。原始件 `~/Desktop/ZCode审阅/扫前端-01.md`。
> 任务：扫 `src/singularity/web/frontend/src/`（**22 个源文件 / 4259 行**）——
> 重点那一类是「**把"没有数据 / 读不到 / 不认识"渲染成"好消息"**」，
> 每个可疑判据都要对照后端 handler 核过**字段的真实取值集合**。
> ✅ **它自报的方法**：逐行读完、逐判据对照 `app.py` → `_api_handler` facade 的返回形状；
> 且**自纠过一次误报**（`approval` / `tool:*` 它先判"死订阅"，在多行 append 里找到发出处后**撤回了**）
> —— 这一步值得记：**它对自己第一遍的 grep 结论不轻信**。

## 🔴 它抓到 7 条同类，其中 3 条是新真洞 —— 我核了前两条、都已修（`b16b2aa`）

### ① `TaskCard` 只认 3 个状态，后端有 **12 个**

`done` / `failed` / `cancelled` —— 其余**一律渲染成蓝色「执行中」+ 永远转下去的圈**：
  · **`rolled_back`（终态）** ⇒ 圈永不停
  · `conflict_held` / `paused` / `blocked`（都是"停住等人"）
  · 而 `cancelled` **后端根本没有这个值**（取消走 `rolled_back` / `failed`）
✅ **我核了**：`tracker.py:23-36` 的枚举确实是 12 个。**已修**：按"跑着 / 停了等人 / 终态"分档，
**不认识的新状态一律落"停"** —— 把停住的东西画成还在转，比画错颜色坏得多。

🔴 **而且不止这一处**：`Chat.tsx` 的计数**自己又写了一遍同样的判据**
（`failed || cancelled`）⇒ `rolled_back` 被算进"执行中"，**任务已回滚、顶上还显示在跑**。
⇒ 判据**收成一份**（`TaskCard` 导出 `taskStateKind`，Chat 用同一个）。
**这条是我自己该发现的**：`TaskCard.tsx` 我上一轮刚改过（`verdict` 那行），
**却没看隔壁七行的 `status` 渲染** —— 同文件、同类病、挨着放。

### ② `Chat.tsx`：全失败时亮绿勾

判据是 `active > 0` 的取反，而**全失败时 `active` 也是 0** ⇒
**绿✓ + 绿进度条 + 红字「2 失败」同框**。✅ **我核了**（就在同一段代码里）。**已修**。

## ⬜ 它报的另外几条 —— **我一条都没核**（当候选）

- **`SkillsTab.tsx:39-52`：假"未绑定"会毁数据**（它判中危）—— 拉技能失败时静默落 `[]`
  显示全未勾；用户这时勾任意一个，PUT 会把后端原有绑定**整体覆盖成 1 个**。
  同页还有个**加载时的写副作用**：单 agent 且技能为空就自动写入全部技能，用户清空一刷新全回来。
- **`Projects.tsx:79-84`：`run()` 的返回值被当成响应解构** —— 后端在 200 里给了
  `suggested_flow`，而 `run()` 只返回布尔 ⇒ 那个字段**永远 undefined**，
  §47 的"看着像小活走轻量"人审建议是**死功能**。（它说 `Chat.tsx:175` 直接 `await` 就是对的
  —— 同仓两个写法一对比就露馅。）
- **`useSSE.ts:41`：CLOSED 后没有重连定时器，且 Chat 页没有任何轮询兜底**
  ⇒ SSE 死了以后任务卡片**全冻结且无提示**。
- **`api.ts` 的 `(d?.tasks||d)` / `Object.values(d||{})` 归一化家族**是潜伏雷：
  今天后端 200 形状恒正确，哪天哪个 handler 学 `files/diff` 回 200+`{error}` 就崩或渲染垃圾。
- **GATE 横幅在对话页是绿色、项目页是琥珀🛑**，同一阻塞状态两个页面矛盾（低）。
- **`Chat.tsx:63-68` traceability 请求失败被渲染成「QA：无报告（本次没生成）」**（灰，不绿）。
- **死代码 16+4 个 `api.ts` 方法零调用**，其中 `approveTask` / `decideApproval`
  打同一端点但不带 decision body —— 它说"留着是个钩子"。

## ✅ 它确认没问题的地方（照抄，**我没复核**）

`money.ts` 金额出口已全量收口 · 两处已修事故的修复在位 · `FilePanel` 是全前端唯一把
200+`{error}` 占位符当一等公民处理的地方 · SSE 订阅的每个 kind 后端都有发出点 ·
usage-history 的 range 两端一致 · 无 XSS sink · 定时器全清理。

---

# 扫前端-01：web/frontend/src 全量审阅（4171 行）

日期：2026-09-14。范围：`src/singularity/web/frontend/src/` 全部 22 个非测试 .ts/.tsx（4259 行含测试），逐行通读；对照 `web/app.py` 及其委托的 handler（`scheduler/_api_tasks.py` / `_api_projects.py` / `_api_admin.py` / `_api_memory.py` / `_api_monitor.py`、`tracker.py`、`permission.py`、`witness.py`、`_token_budget.py`、`validator.py`）核对了每个被前端消费的字段的**真实取值集合**，以及「200 + `{error}` 占位符」在哪些端点真实存在。判据形状对照的是任务书里给的那两例（GatePanel QA / TaskCard verdict）。

严重度标尺：**高** = 会让人做出错误决定或破坏数据；**中** = 功能静默失效 / 冻结 / 竞态；**低** = 边角、潜伏、体验。

---

## 🔴 渲染判据表（"由后端字段决定 UI 好/坏"）

| 文件:行号 | 字段 | 缺失时渲染 | 占位符时渲染 | 不认识时渲染 | 会不会撒谎 |
|---|---|---|---|---|---|
| `pages/Chat.tsx:237-246` | `tasks[].status` 聚合（active/completed/failed，本地算） | tasks 空则整块不渲染（中性 ✓） | —（本地聚合，无占位符路径） | `blocked`/`paused`/`conflict_held`/`routed`/`dispatched`/`validating`/`decomposed` 全计入 active →「N 个执行中」+转圈；**全失败时 active=0 → 绿色✓图标 + 绿色进度条 + 「进度 0/N N 失败」** | **会（高）**。判据落在 `active===0` 上而不是 `failed===0` 上——红字"2 失败"旁边立着绿勾。验：建 2 个必失败任务（如填不存在的模型）跑完，看对话页进度区。 |
| `components/chat/TaskCard.tsx:33-49` | `t.status` | undefined → 落 else 分支「执行中」蓝转圈 | — | 后端 `TaskStatus` 有 **12 个值**（tracker.py:23-35），卡片只认 `done`/`failed`/`cancelled`（`cancelled` 后端根本不存在）；`paused`（手动暂停）、`blocked`（依赖未满足）、`conflict_held`（冲突等人）、**`rolled_back`（终态！）**、`decomposed`、`routed`、`dispatched`、`validating` → 全部渲染「执行中」+无限转圈动画 | **会（高）**。`rolled_back` 是终态，转圈永不停止；`paused`/`conflict_held` 明明停着却显示在跑。验：任务页点「暂停」→ 回对话页，该卡显示蓝色「执行中」转圈。 |
| `pages/Chat.tsx:63-70` + `components/chat/GatePanel.tsx:189-216` | `acceptance`（GET /api/projects/:id/traceability 的**整体返回**） | **请求失败**（catch，Chat.tsx:68）→ `setAcceptance(null)` → 灰「QA：无报告（本次没生成 QA 报告，只按下面的明细判）」，且**无任何 toast** | `qa_report={"error":…}`（app.py:1455-1458 真实存在）→ 琥珀「核不了」✓（已修版本判据 `!qa.summary` 在位） | — | **半会（中）**。网络/500 失败被渲染成"没跑过 QA"——灰不是绿，比修掉那条轻，但同样坐在人审门上：后端明明有 QA 报告，门上写着"本次没生成"。验：给 /traceability 挂掉（或断网刷新停在 gate3）→ 面板显示「无报告」，网络面板里是请求错误。 |
| `components/chat/GatePanel.tsx:202-203` | `qa.summary.verdict` | summary 缺失 → 琥珀「核不了」✓（同上已修） | 占位符无 summary → 琥珀 ✓ | 绿灯判据是 `failed===0 && sum.verdict !== 'no_go'`——**唯一认识的失败档是 `no_go`**，后端将来新增任何失败档位（failed===0 时）都会落绿「通过」 | 目前不会（validator.py:677-692 `build_qa_report` 恒填 verdict），**新增档位会**。低概率残余。验：mock 返回 `{summary:{failed:0,verdict:"rejected"}}` → 渲染绿「通过」。 |
| `components/chat/GatePanel.tsx:320-327` + `pages/Chat.tsx:271` | `info.phase`（`gate1/2/3`） | 非 gate 不渲染 / 灰 ✓ | — | gate → **绿色横幅**（#eaf6ec 底、#16a34a 字和边框）+ GATE 徽标绿字；Chat 底部 phase 名也在 gate 期变绿（#16a34a） | **半会（低-中）**。gate = 阻塞等人审，被刷成"成功色"；同项目在 `pages/Projects.tsx:190-195` 渲染的是琥珀🛑「等待审批」——同一个状态两个页面一绿一琥珀。验：进 gate1 项目，对比对话页横幅颜色与项目页展开区颜色。 |
| `components/SkillsTab.tsx:39-41`（及 45-47） | `GET /api/agents/any/:m/skills` 的 `skill_names` | 请求失败 → catch → `[]` → 该模型显示「0 技能」全部未勾 | 后端 200 形状恒正确（app.py:2021 路由），占位符路径目前不存在 | — | **会，且破坏数据（中）**。假"未绑定"状态下用户勾任意一个 → `PUT skill_names:[这一个]` 把后端原有绑定**整体覆盖**——api.ts:84-88 注释里写过的那类事故，从 GET 侧再进来一次。验：对单个模型的 skills GET 注入 500 → 勾 1 个 → 查后端该模型技能列表只剩 1 个。 |
| `pages/Projects.tsx:79-84` | `createProject` 响应里的 `suggested_flow` | — | — | `run()`（toast.ts:47 返回 `Promise<boolean>`）把响应**吞成 `true`**，`res?.suggested_flow` 永远 undefined → 「💡 看着像小活，走轻量？」提示条**永不出现** | **会（中）**。后端确实在 200 里给了 `suggested_flow`（_api_projects.py:57-61），防御模式 §47 设计的"人点头才生效"在 UI 上整条断了：建议静默丢弃，项目照走重流程。验：用一句话描述建个小工具项目（应触发 suggest_flow）→ 无提示条；断点看 `res === true`。 |
| `pages/Projects.tsx:180` | `flow_decision.weight` | 缺失/不认识 → 「重量」 | — | 只认 `'light'`，其余全「重量」 | 不会（坏消息方向，保守）；副作用是轻量项目被标成重量，虚惊。低。 |
| `pages/Usage.tsx:97,111-124,214` | `m.used` / `m.tokens` / `totals.cost` | used 缺 → 「—」✓；**used 真 而 tokens 缺 → 「0」**（`String(n||0)`） | — | — | 边缘会（低）：用量行 0 不算大谎。合计行 `anyPriced`（130 行）+ `fmtCost(null)` 已把"全未配价 → $0.0000"的谎兜死 ✓。 |
| `pages/Tasks.tsx:10-11,126,136` | `t.status` 映射表 | 不认识 → 原样英文字符串 + 灰点 ✓（诚实） | — | `blocked:'已暂停'`——后端 blocked=**依赖未满足**（tracker.py:31 注释），不是暂停；而 `conflict_held`/`rolled_back`/`validating`/`dispatched`/`decomposed`/`routed` 无中文档 | 不撒谎（灰+原文），但 `blocked` 档语义错位，且缺 6 档中文——真机上这些状态全显示英文。低。验：造一个 conflict_held 任务看任务页文案。 |
| `components/AppLayout.tsx:140-141` | `loopStatus.running` | 缺失 → false → 灰「已停」 | — | — | 不会（坏消息方向）。请求失败保持旧值不闪 ✓。 |
| `components/AppLayout.tsx:64-65` | `conflicts` | 缺失 → `[]` → ⚠ 消失 | `conflict_list` 恒 200 + `{conflicts,total}`（_api_memory.py:56-63），占位符路径不存在 | — | 不会。 |
| `components/AppLayout.tsx:74-78` | `approvals` | 缺失 → `[]` | `approvals_list` 恒 200（_api_admin.py:346-349） | — | 不会；失败时保持旧列表（注释点名"别把横幅闪没"）✓。`decideApproval` 的 `r.ok===false` →「该审批请求已失效」✓（AppLayout.tsx:90，对照 permission.py:261-264 的语义）。 |
| `components/McpTab.tsx:52,59` | `s.connected` / `s.tool_count` | connected 缺 → 灰「未连接」✓；tool_count 恒有（_api_admin.py:389-391） | — | — | 不会。 |
| `components/ModelsTab.tsx:158,224` | `a.status` / `activeModels` | 缺 → 灰○/灰点 ✓ | — | 非 `'active'` 一律灰 | 不会。 |

**api.ts 归一化家族（潜伏雷，当前契约下不触发）**：`api.ts:21` `(d?.tasks||d)`、`:62` `Object.values(d||{})`、`:73` 同、`:80` `(d?.skills||d||[])`、`:120-121` 同。这些端点今天 200 时形状恒正确（task_list _api_tasks.py:87 恒 `{tasks,total}`；model_list 恒字典），但一旦后端某个 handler 学了 files/diff 那样在 `except` 里回 200+`{error}`：`tasks/skills/mcp` 会把 `{error}` 对象本体塞进 state → 消费端 `.map/.filter` 抛错 → ErrorBoundary 全页「页面出错了」；`models/apiStore` 的 `Object.values({error:...})` 得到 `["..."]` → 模型目录渲染一行垃圾字符串。**中（潜伏）**。验（证伪法）：mock /api/models 回 200 `{error:"x"}` → 目录页出现一行以 error 文本当 id 的行。

---

## 其它类别

| 文件:行号 | 类别 | 一句话症状 | 怎么验 | 严重度 |
|---|---|---|---|---|
| `lib/useSSE.ts:41-44` + `pages/Chat.tsx`（全页无轮询兜底） | 时序 | SSE 撞上致命错误（readyState=**CLOSED**，如 503 连接上限/服务重启后的首连失败）后单例连接死掉：`onerror` 只置 `_connected=false` 等"自动重连"，但 CLOSED 不会自愈，也没有任何定时器再调 `_ensureEs`（它只在组件挂载时跑）；Tasks/AppLayout 有轮询兜底静默退化，**Chat 页什么兜底都没有** → 任务卡片、审批横幅全部冻结，无任何提示 | 开着 Chat 重启后端（或手动把 EventSource 指到 503）→ 继续跑任务 → 卡片永远不动，页面上没有任何"连接断了"的迹象 | 中 |
| `components/FilePanel.tsx:24-46` | 时序 | activePid 切换的两个 fetch 无竞态守卫——同族的 `Projects.toggle`（detailSeq）和 `Chat.fetchTasks`（fetchSeq）都修了，这里漏了：慢的旧响应会把新项目的文件树盖掉 | 给 A 项目的 /files 加 2s 延迟，快速 A→B → 树先显示 B、随后被 A 的文件覆盖 | 中 |
| `pages/Chat.tsx:127,143` | 时序/状态 | `_default` 会话下**任意** task/system SSE 事件都会 `setActiveProject(td.project_id)`——别的项目在跑任务时，用户正打字的通用会话被反复切走上下文（输入框内容还在，但任务卡、会话、GatePanel 全换了） | _default 下同时跑两个项目的任务，观察右侧/任务区随事件来回跳 | 中 |
| `pages/Chat.tsx:154-157` | 时序 | 回答文本 `includes(p.name)` 命中就自动切项目——回答**提到**项目名（对比、引用）即误触发，取第一个命中 | 问「A 和 B 两个项目对比一下」→ 被切到列表里先匹配的那个 | 低 |
| `components/SkillsTab.tsx:49-52` | 逻辑 | 页面**加载时的写副作用**：恰好只有 1 个 agent 且其技能为空 → 自动 `updateAgentSkills(全部技能)`。"用户刚清空全部绑定"和"从没配过"无法区分 → 清空一刷新全回来，catch{} 还静默 | 单 agent 下取消全部勾选 → 刷新配置页 → 全部自动恢复（后端已被写入） | 中 |
| `components/AgentsTab.tsx:43-48`、`components/ModelsTab.tsx:50-60`、`components/RolesTab.tsx:25-32`、`components/SkillsTab.tsx:29-53` | 逻辑 | 四个 Tab 的 `fetch()` 里 `Promise.all` **整体无 catch**（SkillsTab 只包住了子请求）→ 任一请求 500/断网 = unhandled rejection，页面静默渲染成「暂无激活的智能体」「模型目录 (0)」「角色 (0)」「技能 (0)」，零报错——"读不到"渲染成"没有"，且 AgentsTab:115 还会顺势指导用户"先去扫描导入" | 停掉后端开配置页 → 四个 Tab 全显示"暂无/(0)"，console 里有 rejection、界面无任何提示 | 中 |
| `pages/Chat.tsx:185` | 信息丢失 | observer/chat 失败时 catch 把后端中文原因（如 400「问题不能为空」、500 的具体错误）整个丢掉，一律渲染「请求失败，请确认后端服务在运行。」——正是 toast.ts:15-23 注释点名要消灭的写法 | 发空问题/触发 500 → 对比网络面板响应体与气泡文案 | 低 |
| `pages/Usage.tsx:63-64`、`components/ModelsTab.tsx:127`、`pages/Chat.tsx:185` | 信息丢失 | 同族：catch{} 只弹固定文案，丢 `errText`（usage-history 400 会带「range 只支持…」这类人话） | 同上 | 低 |
| `components/ModelsTab.tsx:202-203` | 状态 | `api.setObserverModel(v)` 不 await、无 run 包裹——本地先改，失败后 UI 显示已设置、后端没改，无提示 | 断网换观察者 → 无报错，刷新后跳回旧值 | 低 |
| `components/RolesTab.tsx:34-38` | 状态 | `setPhaseRole` 乐观写 `phaseMap`，PUT 失败不回滚 | 断网改阶段→角色映射 → toast 报错但下拉停留新值 | 低 |
| `pages/Chat.tsx:191-196`、`components/AppLayout.tsx:87-93` | 并发点击 | `gateConfirm`/`decide` 按钮无 loading/禁用——双击 = 双请求（gate-confirm 连发两次审批动作） | 门禁页双击「✅ 通过」→ 后端 audit 日志两条 gate-confirm | 低 |
| `pages/Chat.tsx:183-185` | 边界 | observer/chat 200 但既无 `client_id` 又无 `answer` → loading 永远转圈（只能手点停止） | mock 200 `{ok:true}` → 转圈不停 | 低 |
| `components/FilePanel.tsx:94` | 边界 | 文件内容路径未 `encodeURIComponent`（api.ts 里 `fsList` 都记得编码，这里裸拼）——文件名含 `#` `?` `%` 必坏 | 仓库里造 `a#b.py` → 文件面板点开 → 404/截断 | 低 |
| `components/FilePanel.tsx:81-88,102` | 边界 | 空文件显示「(空文件)」后仍可下载——下载得到的文件内容是字面 `"​(空文件)"` 五个字 | 打开一个 0 字节文件 → 点下载 → cat 内容 | 低 |
| `pages/Alerts.tsx:97-109` | 状态 | 首次加载失败 `groups` 保持 null → 永远「加载中…」；且刷新按钮（123 行）在 early return **之后**，失败态下根本渲染不出来——唯一出口是整页刷新 | 断 /api/status → 告警页永远"加载中…"（有一条 toast，但没有任何重试入口） | 中 |
| `lib/pinned.ts:8-11` | 边界 | `togglePin` 的 `localStorage.setItem` 无 try/catch（隐私模式/配额满直接抛）→ 项目页点置顶整页进 ErrorBoundary | Safari 隐私窗口点「置顶」 | 低 |
| `lib/api.ts:2-6` | 潜伏 | `{ headers: {...默认}, ...opts }` 展开顺序反了——opts 自带 `headers` 时默认 Content-Type 整个被丢（现在没人传，新调用方一踩就中） | `request('/x',{headers:{'X-A':'1'}})` → 请求头里没有 Content-Type | 低 |
| `stores/app.ts:47-50` | 边界 | persist 无 `version/migrate`——ChatMsg 结构将来一变，旧 localStorage 会话直接进渲染 | 手工改 localStorage 里 `qidian-chat-v2` 的消息结构 → Chat 渲染 undefined | 低 |
| `lib/api.ts` 死代码 | 死代码 | 零调用（仅 api.test.ts 摸过）：`task`、`setTaskMode`、`updateTask`、`applyTask`、`approveTask`、`rollbackTask`、`runPhase`、`projectsRoot`、`fsList`、`fsMkdir`、`updateModel`、`fusionConfig`、`updateFusionConfig`、`tokenUsage`、`updateTokenBudget`、`addMcpServer`；`holdTask`/`releaseTask`/`addModel`/`deleteSkill` 仅被 api.test.ts:21-34 引用。其中 `approveTask` 与 `decideApproval` 打**同一个端点**但 body 里不带 `decision`——哪天被误用就是发一个空决策的审批 | `grep -rn "api\.<name>" --exclude=api.ts`（已跑，计数 0） | 低 |
| `lib/toast.ts:51` + `pages/Projects.tsx:93` | 状态 | `useRun` 有 warning 时返回 true（设计如此），但 `adoptLight` 拿到 true 后**又**无条件弹「已改为轻量…」success——后端 warning 和成功文案同屏两连发 | adoptLight 场景让后端回 warning → info+success 两条 toast | 低 |

**资源泄漏：未发现**。全部 setInterval/setTimeout 都有清理（AppLayout:49/67/83、Tasks:47、useSSE:76-78）；订阅退订齐全；无 AbortController 使用但上述 fetch 都短平快，唯一竞态点已单列（FilePanel）。AppLayout:152 的 `setTimeout(refreshLoop,400)` 未清理，组件卸载后多跑一次 setState，React 18 下无害，不计。

**安全：未发现**。全前端 grep 无 `dangerouslySetInnerHTML`/`innerHTML`/`document.write`/动态 `href`/`window.open`；`@ant-design/x` Bubble 按文本渲染后端字符串；API Key 只经 `Input.Password` 提交、不回显不落 store；文件内容读取有后端敏感文件黑名单（app.py:1372-1389）与真实路径校验兜着。

**无障碍**：
| 文件:行号 | 症状 | 严重度 |
|---|---|---|
| `components/FilePanel.tsx:114-120,125-131` | 目录/文件行是带 onClick 的 div：无 role、无 tabIndex、无键盘路径；选中态只靠颜色变化 | 中（本地面板，影响可控） |

其余页面普遍有 `aria-label`（Tasks 的行内按钮、AppLayout 折叠钮、Projects 置顶/删除、Usage 的 role="button"），状态普遍"色点+文字"双通道——这两块明显是修过的，不列问题。

---

## 我确认没问题的点

1. **金额谎（$0.00）已系统性堵死**：`lib/money.ts` 是唯一出口，全前端 grep 无第二处 `$` 拼接；Usage 合计行的 `anyPriced`（Usage.tsx:130,214）、ModelsTab 的 `isUnpriced` 警示色（ModelsTab.tsx:229-236）、后端 `price_per_m: null` 语义（_api_admin.py:186-187 与 types.ts:7-8 注释互证）三层都在位。
2. **两处已知事故的修复确实在位，且我按后端真实占位符逐分支走过**：TaskCard `verdictChip`（TaskCard.tsx:24-30，不认识→琥珀，与 tracker/validator 的 verdict 取值对过）；GatePanel `qaUnverifiable = !!qa && !qa.summary`（GatePanel.tsx:202-203，对照 app.py:1455-1458 的 `{"error":…}` 占位符——它没有 summary → 琥珀，不再落绿）。
3. **FilePanel 对 200+占位符的处理是对的**：后端 files/content/diff 三个端点的 `except` 分支**就是** 200+`{error}`（app.py:1360-1361、1377-1391、1425-1427），前端三个 fetch 都先判 `d.error` 再渲染（FilePanel.tsx:28-45,98-102）——这是全前端唯一把"占位符"当一等公民处理的地方。
4. **SSE 订阅的每个 kind 后端都有发出点**：前端用到的 `task/system/project/workflow/approval/tool:start/tool:done/gen/observer_answer` 逐一 grep 过 `_pending_sse_events`/`_push_event` 的发出处（tracker.py:252、_dispatch_skills.py:160-166、executors/openai_agent.py:569/591/1002、app.py:502-640/1251/1312/1506 等）。我一度以为 `approval`/`tool:*` 是死订阅，后来在多行 append 里找到了——这条**没有**当成发现写进来。
5. **usage-history 的 range 参数两端一致**：前端 `all/month/week/today`（Usage.tsx:30-35）= 后端 `_RANGES`（_token_budget.py:483）；_api_monitor.py 的 docstring 写的 `7d|30d|all` 是**过期注释**（后端文档问题，不是前端问题）。
6. **缺项映射全部"落到原文+灰"，不落绿**：`PHASE_CN[p.phase]||p.phase`（AppLayout:132、Projects:162）、`STATUS_CN[t.status]||t.status`（Tasks:136）、`STATUS_CN[m.cost||'']||m.cost`（ModelsTab:226）、verdictChip 的兜底琥珀——撒谎的只有上面表格里列的那些。
7. **定时器/订阅清理、CSRF/越权这类由后端 before_request 挡的（app.py:166-410）不重复造轮子**，前端没有自造 token 逻辑（少一层可错的东西）。
8. **`fetchTasks`/`toggle` 的请求序号守卫**（Chat.tsx:88-92、Projects.tsx:69-72）在位且正确——正因如此 FilePanel 的缺失才显眼（同族三处只有它没修）。

## 我没读到的部分

- `index.css` 及所有样式文件（只读了 .ts/.tsx；theme.ts 与 CSS 变量的"一字不差"约定没有核对）。
- 测试文件只用于确认死代码引用关系（api.test.ts），没有评估测试本身的有效性；GatePanel.test/TaskCard.test/Alerts.test/AgentsTab.test 等未逐个读。
- 后端只读了与前端消费字段直接相关的段落：app.py 的路由壳+files/diff/traceability/observer/gate-confirm/loop 段、_api_* 五个子模块的约 15 个 handler、tracker.py 的 TaskStatus/dag_metrics/task 事件、permission.py:249-300、witness.py:109-140、_token_budget.py:483-570、validator.py:677-720。`_loop_worker` 全文、orchestrator 全文、executors/ 其它执行器、project.py 的 phase 流转、ws_bridge 均未读。
- `@ant-design/x` 的 Bubble/Sender 内部实现按官方语义假设为纯文本渲染，未读其源码。
- 构建配置（vite/ tsconfig / package.json）未读，依赖版本未核对。
