# 要发的指令（**只装「还没发」的**）

**怎么用**：复制一整块 → 粘到 ZCode → 回车。**一个窗口一个任务**，别一次粘好几条
（中途不落盘，卡住就整块白干）。

> 🆕 **待发 0 个 · 在跑 0 个**（2026-09-15 更新）。
> ✅ 全部收口：⑦⑧⑨⑩ + ⑪ 预检工具（`docs/预检工具-01-20260914.md`）·
>   **⑫ 外派脚手架**（`docs/外派脚手架-01-20260914.md`）·
>   **⑬ 审测试判据**（60/60 过完，`docs/审测试判据-01/02-20260914.md`）。
> 📦 **发过的原文 + 归档账全在 `要发的指令-归档.md`** —— 这份**不装**发过的，
>   要重发哪一条就去那边复制。
> ⏰ **ZCode 额度 09-14 已过期** ⇒ 再派之前先确认额度/换个窗口。
> 🚦 并发上限 3 个窗口 —— ⚠️ **但算力是按"重活"算的**：两个**写代码**的窗口同时跑，
>   会把一个**只读**的挤停（09-14 实测，⑬ 就是这么停的）。**下次别让两个写代码的同时跑。**
> 🔴 **允许往仓库写文件的窗口**：`scripts/preflight.py`（⑪，已交）· `scripts/dispatch.py`（⑫，已交）。

---

**下一条要发什么**：（空 —— ⑬ 也在 2026-09-15 收口了，见上）