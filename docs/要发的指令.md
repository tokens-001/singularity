# 要发的指令（**只装「还没发」的**）

**怎么用**：复制一整块 → 粘到 ZCode → 回车。**一个窗口一个任务**，别一次粘好几条
（中途不落盘，卡住就整块白干）。

> 🆕 **待发 2 个**：**⑬ 审「测试判据本身写没写错」**（只读）· **⑫ 外派闭环脚手架**（写脚本）。
> 🏃 **在跑 1 个**：**⑪ 预检工具**。已核完归档：⑦⑧⑨⑩。
> 📦 **发过的原文 + 归档账全在 `要发的指令-归档.md`** —— 这份**不装**发过的，
>   要重发哪一条就去那边复制。
> 🚦 并发上限 3 个窗口 · ⏰ **ZCode 额度 09-14 过期**（就是今天）。
> 🔴 **⑪ 是唯一允许往仓库写文件的窗口**（新建 `scripts/preflight.py`，不许改现有文件）。

---

**下一条要发什么**：⑬（审测试判据，只读）→ ⑫（外派脚手架，写脚本）。两条可以一起发。
## ⑬ 审「测试判据本身写没写错」（**只读**，零风险）

```
【硬约束·先读】**不要读**：/Users/jingzhe/OPEN.md · /Users/jingzhe/Desktop/要发的指令.md ·
/Users/jingzhe/Desktop/静默except分类/ · /Users/jingzhe/Desktop/ZCode审阅/（**全部**）·
/Users/jingzhe/projects/singularity/docs/*20260914.md（**全部归档**）。

**可以读**：`tests/` 全部 · `src/` 全部 · `docs/防御模式.md`。

仓库根 `/Users/jingzhe/projects/singularity`。**基线提交 `9d0d389`**（先用 `git rev-parse HEAD` 确认，
不是就按你实际拿到的那个提交做，并在报告里写明）。

────────────────────────────────────────
**背景**：这个仓库今天有一批 bug，**测试全绿却没抓住**。事后查出来的原因很具体：

`tests/test_scheduler/test_io_quarantine.py` 里**每一条**"写侧拒写"用例，
都**先手动读一次**那个坏文件（注释里写着"置上损坏标记"）—— 而**那一行正是缺陷的前提**。
测试在**替被测代码把洞填上**，再断言"洞不存在"。四条真洞全被这一格盖住了。

⇒ **"测试判据本身写没写错"这一类，从来没有被系统地审过。** 这是它唯一一次被撞见。
（`docs/防御模式.md` 的 §69 就是这条形状，写在那儿了。）

**这是评审任务：你不许改任何文件。** 只交一份报告。

────────────────────────────────────────
【要判什么】逐份测试文件过，**但不是"通读"—— 是逐条问一个问题**：

  🔴 **"这条用例，删掉被测代码里的哪一行会红？"**
     **答不上来的，就是假绿候选。** 这是唯一的硬判据。

要抓的五种形状：
 1. **前置步骤替被测代码把洞填上了**（§69 那类：先构造好缺陷本该缺失的前提）
 2. **断言恒真**：断言一个无论实现对错都成立的东西；或断言的是"实现快照"而非行为
 3. **锁住了错误行为**：把 bug 当约定钉住（"兼容层"是合法的，但要**标注出来**）
 4. **只测函数不测接线**：把被测的调用/接线那一行删掉，测试照样绿
 5. **测了但永远不会失败**：mock 掉了被测逻辑本身

⚠️ **只报判据层面的错**，别把"测试写得丑/命名不好/没参数化"当战果 —— 那不是这一类。
⚠️ 每条给：**文件 · 用例名 · 判词（真绿 / 假绿 / 锁住错误行为）· 证据**（"删掉哪一行它会红/不会红"）。
⚠️ **不确定就说不确定**，别猜。

────────────────────────────────────────
【允许你跑，但要只读】
`.venv/bin/python -m pytest tests/ -q` 和前端 `npx vitest run` 都能跑（在
`src/singularity/web/frontend/` 下）。**前端的默认收集不含样板目录**，别去动它。
⚠️ **不许改任何文件**。要验证"删掉某行会不会红"，用 **`git worktree` 或 `/tmp` 里的副本** ——
⚠️ **这条特别重要**：这个仓库有过一次事故，是**测试往生产 `.qidian/` 写数据**；
另一次是**临时脚本写进了生产账本**。**跑之前先确认你动的是副本。**
⚠️ 而且别 `checkout` 当前工作区。

【落盘】报告写到 `~/Desktop/ZCode审阅/审测试判据-01.md`，**边做边追加落盘**。
报告里必须有：**逐条清单** + 单列一节「**我判不了/没读的**」。
```

---

## ⑫ 外派闭环脚手架（⚠️ 要写代码；服务对象是"另一个 AI 的工作流"）

```
【硬约束·先读】**不要读**：/Users/jingzhe/OPEN.md · /Users/jingzhe/Desktop/静默except分类/ ·
/Users/jingzhe/Desktop/ZCode审阅/（**全部**）· /Users/jingzhe/projects/singularity/docs/*20260914.md。

**可以读**（它们是**规格的实例集**，不是结论）：
    /Users/jingzhe/projects/singularity/docs/要发的指令.md        （**待发**那份）
    /Users/jingzhe/projects/singularity/docs/要发的指令-归档.md   （**发过**那份，20+ 份实例）
    /Users/jingzhe/Desktop/要发的指令.md 是上面前者的符号链接。

⚠️ **本条的例外**：这一条要你写代码 —— 可以**新建** `scripts/dispatch.py`（+ 一个测试）；
**不许改任何现有文件**，一行都不行。

仓库根 `/Users/jingzhe/projects/singularity`。**基线提交 `9d0d389`**。

────────────────────────────────────────
**背景**：我（另一个 AI）今天往外派了 20+ 轮任务给对话模型。每一次都要**手工做三件事**，
每件都出过事。额度还够，想把它们做成脚本。

**要它解决的，就是这三件手工活**（判据：**我拿到它能不能少打字、少犯错**）：

**一、禁读清单自动生成** ← **今晚真出过一次漏洞**
  外派材料开头有一段「【硬约束·先读】不要读这些位置」，列的是"另一个窗口的产物"。
  **这份清单要跟着归档一起长** —— 今天我把一份报告归档进 `docs/`，而**已发出去的指令没挡它**。
  ⇒ 让它扫 `~/Desktop/ZCode审阅/` 和 `docs/` 的**实际内容**，生成"当前该挡哪些"。
  ⚠️ 格式照 `要发的指令-归档.md` 里那 20+ 份实例，**别自己发明格式**。
  ⚠️ 清单要能**区分**"挡整个目录"和"只挡某几份"（有的任务是**要读**某一份的，指令里会开例外）。

**二、归档抬头模板**
  `docs/*-2026*.md` 每份开头都有一段我手写的抬头（原始件路径 / 任务 / 基线提交 /
  硬约束 / **我的核实**）。**生成骨架**就行 —— 内容留空给我填，
  **别替我下结论、别写"已核实"这种我还没做的事**。

**三、发过的自动挪进归档**
  `要发的指令.md`（只装待发）↔ `要发的指令-归档.md`（发过的原文 + 归档账）。
  这条现在纯手工 —— 今晚出过一次「某条已发了，但正文还躺在待发那份里」。

────────────────────────────────────────
🔴 **验收：拿现成的实例当靶子，贴实际输出**

 · **一**：生成的禁读清单，**和 `归档` 里那 20+ 份实例的格式对得上**（贴一份对照）
 · **三**：跑一遍"把某条从待发挪到归档" ⇒ **待发那份真的少一条、归档那份真的多一条**
   （贴 `wc -l` 前后 + 两份的抬头）
 · **二**：贴一份生成出来的抬头骨架

⚠️ **别应付**：如果某件事你觉得脚本化不划算（比如"抬头内容本来就该人写"），
**明说不做，并说清为什么** —— 那比做一个半成品有用。
⚠️ 这个工具的服务对象是**另一个 AI 的工作流**，所以"能少打多少字 / 少犯多少错"就是判据。

【要求】零新依赖（标准库够用）· **只读 + 只写这两个指令文件**（改它们算正常操作，
但**改之前先打印要改成什么**）· 不确定就说不确定。

【落盘】报告写到 `~/Desktop/ZCode审阅/外派脚手架-01.md`，边做边追加。
```

---

