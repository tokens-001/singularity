# 反向审阅-01（E / ZCode · GLM-5.3-Flash）+ 我的核实

> 下面是**报告原文一字未改**。原始件 `~/Desktop/ZCode审阅/反审-01.md`（2026-09-14 02:24，15524 字节）。
> 它是待发六条里的 **E**：任务 = 反向审**我们自己**的修复，判三问（①真解决了没有 ②有没有新洞 ③有没有更该改的）。
> 审时 `HEAD = 0147b07`（01:42），但它自己注明"引用**当前树**（含同晚后续提交 `4281960`、`972e79f`、`547e742`）"
> —— **它发现树在它跑的中途动了，并跟上了**。这一点比 F 那份做得好（F 只钉了开工时的基线）。

## 🏆 最要紧的一条：**它抓到一个真洞，而且是我（`a33bf79`）引入的**

**`qa_report` 读坏后的占位符，在 GATE3 人审门上渲染成绿色「QA：通过」。**

我自己独立追了一遍全部代码，**属实，链路一步不差**：

| 环节 | 事实 |
|---|---|
| 后端 | `web/app.py:1469` 读不出 QA 报告时给占位符 `{"error": "报告读不出来（…）—— 不代表没跑过 QA…"}`（这是 `a33bf79` 从 `None` 改过来的） |
| 前端取值 | `GatePanel.tsx:190` `qa = acceptance?.qa_report` ⇒ **占位符是"真值"，走进"有报告"那个分支** |
| 算分 | `:192` `sum = qa.summary \|\| {}` → `:193` `issues = []` → `:194` `failed = sum.failed ?? 0` = **0** |
| 判定 | `:195` `qaOk = !!qa && failed === 0 && sum.verdict !== 'no_go'` ⇒ `true && true && true` = **true** |
| 渲染 | `:204-205` ⇒ **`#16a34a`（绿）、文案「QA：通过」** |

**改之前**（`qa_report = None`）渲染的是**灰色「QA：无报告」+ 一行说明**。⇒ **我那次"修"把"读坏了"从灰色爬成了绿色通过**，
比它要修的那个病更糟 —— 而且它就坐在**人审门**上，那个面板的注释自己写着
「摘要行刻意放在折叠框外：**不展开也能判断该不该过**」。**后端 `witness.warn` 是唯一知道真相的通道，而坐在 GATE3 前的人看不到。**

⚠️ **判据就在同一个组件里**：隔壁 `conformance` 那条早就处理过同一件事 ——
`const unverifiable = conf?.evidence?.unverifiable === true` ⇒ 琥珀色「无法核验」（`:197-199`）。
**同一种"我核不了"在左边给了琥珀，在右边给了绿灯。** 修法照抄这一处即可（或前端判 `qa.error`）。

## ✅ 我核过的（4 条结论性声称，逐条翻代码/翻 git，全真）

| 它说 | 我核到 |
|---|---|
| 🏆 `qa_report` 占位符渲染成绿"通过" | ✅ **属实**（上表逐环节追过） |
| 🟠 worker 异常分支的抢救段**裸奔**，而同款超时分支包了兜底 | ✅ `orchestrator.py:442-445` 三连（`_salvage_timed_out`/`_save_trace`/`_account_salvaged`）**外面没有 try**；对照 `:513-518` 同款三连**包了** `try/except → _strand_guard(t, _e, "salvage_timeout")`。**两条兄弟路径一件穿、一件不穿** |
| 🟡 `roles.py` 读侧坏文件只 `logging.warning` 就静默用出厂角色 | ✅ `roles.py:161-163` `except Exception as e: logging...warning(...); return` |
| ⚪ `_io.load_toml` 的 docstring 说"解析失败返回空 dict"、代码其实抛 | ✅ **它独立找到了和我同一条**（我是核 C 的草案时抓到的，两边没通气）⇒ 两人独立撞上同一处，可信度更高 |
| 它引的 10 个提交号（`0147b07/972e79f/547e742/1f0fa50/a33bf79/fc8665b/e3a835d/70478ca/1775f5d/6d11375`） | ✅ **全部存在，且提交信息与它描述的一一对应** |
| `GatePanel.tsx` 最后一次改动早于 `a33bf79`、其后无人补 `error` 处理 | ✅ 最后一次改是 `b5be385`（**2026-09-12**），而 `a33bf79` 是 09-13 |

## ⚠️ 一条它判"存疑"、但**当前树已经修掉了**的（它没注意到）

E 把 `1f0fa50` 的 **secret_key** 判成"存疑"，并建议「改成**不覆盖+出声+内存随机**」——
**那个改法我昨晚（`4281960`）已经落了**：现在 `app.py` 是 `except FileNotFoundError`（生成并持久化）／
`except OSError as _e`（`witness.warn` + 进程内随机，**不覆盖**）／空文件（`witness.warn` + 进程内随机，
**不 raise**），**已经没有 `raise`**。

⚠️ **所以那条是"对着 `1f0fa50` 的 diff 判的"，不是当前树** —— 它头部那句"引用当前树"在这一条上不成立
（它引的 `app.py:78-81` 现在是注释块，不是 raise）。

**这值得记一笔**：**它独立复核后"站同一判"的那个建议，我早就采纳了** —— 这是一次**独立确认**（好），
但它没发现"已经修了"（报告里那条建议是空转的）。⇒ **核它的报告时要按内容对、并回当前树确认，
别照着它的建议去改已经改过的东西。**

## 📌 它的纪律

- 声称**未读** `静默except分类/` 与 `ZCode审阅/`，并在文末专门写了一节「越界说明」说明只写了输出文件本身。
- **实际跑了测试**取证据（`test_no_silent_except` / `test_no_undefined_names` / `test_memory_heavy_due` /
  `test_exec_internals` / `test_task_archive` / `test_exec_run`，共 126 条全绿）—— 不是纯读。
- **它自己认了 1775f5d 当时引入两个洞、并核到 `4281960` 已闭合**（a: fallback 链每轮重置预算；
  b: 起表在 worker 开头≠submit 时刻）—— **这两条正是我第二批修的东西，它独立复核后确认闭合**。

---

# 反审-01：对 09-13~14 自家修复的独立反向审阅

- 审阅人：ZCode（独立窗口；**未读** `静默except分类/`、`ZCode审阅/` 下任何文件——同 D 的硬约束）
- 仓库：`/Users/jingzhe/projects/singularity`（审时 HEAD = `0147b07`）
- 方法：对每个提交 `git show` 读 diff + 读改后完整函数 + 读调用方；判三问（①是否真解决声称的问题 ②有无新洞 ③有无更该改没改的相邻位）。守卫/相关测试实际跑过：`test_no_silent_except` / `test_no_undefined_names` / `test_memory_heavy_due` / `test_exec_internals` / `test_task_archive` / `test_exec_run` 共 126 条全绿。
- 行号说明：引用**当前树**（含同晚后续提交 `4281960`、`972e79f`、`547e742`）的行号；diff 原始位置另注。

## 总判

**23 处改动：21 处对（其中 4 处"对但收益弱"）、1 处存疑、1 处错（真新洞）。**
另有 1 处漏网（两个提交交界处）、若干"相邻更该改"。1775f5d 当时引入 2 个新洞，**同晚 `4281960`（09-14 02:09）已全部闭合**——以当前树看那一族是自洽的。

| 提交 | 处 | 判 | 一句话理由 | 证据（当前树） |
|---|---|---|---|---|
| 1f0fa50 | secret_key 只捕 FileNotFound + 空文件 raise | **存疑** | 方向对（绝不覆盖），但 import 期 raise 把"会话失效"升级成"整个后端起不来"，且有更优解（内存随机+出声） | `app.py:64-81` |
| 1f0fa50 | fusion GET 读坏→500+warn | 对 | 后端覆盖链真断了：`load_toml` 对损坏文件抛、PUT 同捕 500 | `app.py:916-929`、`_io.py:35-39` |
| 1f0fa50 | reconcile / consolidate / run_phase / result 四处 warn | 对 | 纯加出声，`witness` 顶层已导入、warn 自吞 OSError，不产生新错误源 | `app.py:548,580,615,624` |
| a33bf79 | `_write_role_override` 读不动拒绝写 | 对 | 防"一次解析失败=其他角色全丢"，三个调用方全在 Flask 路由里、抛了会 500 出声 | `app.py:1856-1862,1878,1894,1985` |
| a33bf79 | qa_report 占位符 `{"error":…}` | **错** | 唯一消费方把它渲染成**绿色"QA：通过"**——比改前灰色"无报告"更糟；"明说不代表没跑过"没有任何代码渲染 | `app.py:1469` vs `GatePanel.tsx:190-205` |
| a33bf79 | test_plan / traceability 占位符 | 对（弱） | 出声成立，但前端无人渲染这两个字段，"人审页分得开"只落在 payload | `app.py:1437,1457`；前端 grep 无消费方 |
| fc8665b | 3 处安全网自身失败出声 | 对 | 纯加 warn 不动逻辑；`_orphans_warned`/探测族口径一致 | `orchestrator.py:429-435,505-511,694-698` |
| fc8665b | 守卫判据不下钻嵌套 handler | 对 | 修掉"虚减=没修也记功"；残留不精确偏严侧（安全） | `test_no_silent_except.py:58-88` |
| e3a835d | 派发失败转 FAILED | 对 | 真机孤儿案的根因路径闭合；现走 `_strand_guard`（5 个调用点实证） | `orchestrator.py:136` |
| e3a835d | loop 兜底进告警 + 孤儿探测 | 对 | 判据前提在调用点上下文成立；只报不改；真机验过 | `orchestrator.py:550-586,712-717` |
| 70478ca | `_strand_guard` + 4 处兜底 | 对 | 先重读盘上状态、只改 RUNNING；"future/batch 已消费"4 个点全包 | `orchestrator.py:364-388,462-471,513-518,645-658` |
| 1775f5d | 预算"一把尺" | 对（主根因真解决）+ 当时引入 2 洞（后经 4281960 闭合） | 每-dispatch 归零这个根因真修了；fallback 链预算重置、起表在 worker 开头两洞当晚补掉 | `openai_agent.py:391-406`、`_dispatch_exec.py:195,223`、`orchestrator.py:123` |
| 6d11375 | 重活判据落盘+时间兜底 | 对 | 重启不清零、真机后续真跑出产出；边缘自愈 | `_memory_consolidator.py:28-64` |
| 972e79f（§64） | 三处补 import + F821 守卫补盲区 | 对 | 名字核实存在；守卫实测绿；真机轮5真出产出 | `_memory_consolidator.py:84,107-109` |
| 547e742（§66） | from_dict 去 level= + stored_tool_batches | 对 | 唯一调用点核实、真机复验 200；空值回落现算 | `neijinglu.py:90-97,145-146`、`_api_tasks.py:143` |

---

## 逐处详审（三问）

### 1f0fa50 —— part-01 五处

**1. secret_key（存疑）**
- ① 声称"文件在但读不动时会静默换钥并覆盖"——真。改后只捕 `FileNotFoundError`、绝不覆盖，原问题解决。
- ② **新边界**：空文件 → import 期 `raise RuntimeError`（`app.py:78-81`）= 整个后端起不来。空文件是**可达状态**：`write_text` 截断后写、中途崩就是空文件（本仓 S1 家族的经典死法），而这行写侧**不是原子写**。而且 raise 保护的对象只是会话签名——为它陪葬的是调度循环/Observer/API 全部。"宁可起不来"是有意的，但改动审阅当年给的"不覆盖+出声+内存随机"确实更优：同样不毁数据、可用性还在、照样看得见。我独立复核后站同一判。
- ③ 相邻：该写入未走 `_io` 的原子写原语。
- 证据：`app.py:64-81`（读：69-73；raise：78-81）；全仓 `secret_key` 仅 `app.py` 一个消费者（grep 核实）。

**2. fusion 配置 GET（对）**
- ① "读坏了 ≠ 空配置"：改后 FileNotFoundError→{}、其余 500+warn。核实 `load_toml` 对损坏 TOML **真抛**（`_io.py:35-39`，docstring 写"解析失败返回空 dict"是**假/过期的**——代码不捕，顺带记一笔文档债）；PUT 侧自己的 try 也接得住 → **假空覆盖链在后端两层都断了**。
- ② 无新洞。
- ③ 前端 `fusionConfig`/`updateFusionConfig` 目前**零调用方**（只有 `api.ts:107-108` 死绑定）——提交里"界面显示空→PUT 覆盖"的场景今天不活跃，修的是 API 层防线。不算白修，但声称的场景无活体。
- 证据：`app.py:916-929`、`_io.py:35-39`、前端 grep 仅 `api.ts`。

**3-6. reconcile / consolidate / run_phase / result 四处 warn（对）**
- ① 四处都是"仪器坏了没人知道"→ 出声（带固定 key 进聚合），逻辑一字未动。
- ② 无新洞：`witness` 在 `app.py:51` 顶层导入；`witness.warn` 内部吞 OSError（`witness.py:52-73`）不会成为新错误源；就算 warn 抛非 OSError，也会落进外层 loop 兜底（loop_error 有持久通道）。
- ③ 无漏：同函数里剩下的静默 handler 只有 osascript 桌面通知失败那条，真可以吞。
- 证据：`app.py:548,580,615,624`；基线棘轮 7→3（`silent_except_baseline.json` diff 属实）。

### a33bf79 —— part-02 四条

**1. `_write_role_override`（对）**
- ① 原病是"读坏当空→`atomic_write_json` 整文件覆写=其他角色全丢"，fail-closed（拒绝写+出声+抛）是正解。
- ② 抛出有承接：三个调用方全是 Flask 路由（PATCH/POST/DELETE，`app.py:1878,1894,1985`）→ 500 出声，不是新静默。
- ③ 相邻没改：**读侧** `roles.py` `_apply_overrides` 对坏文件只 `logging.warning` 然后**静默用出厂角色**（`roles.py:163-166`）——同文件同病，写侧已拒、读侧还在吞，只有日志没有告警。建议下一批一并收。
- 证据：`app.py:1856-1862`；`roles.py:163-166`。

**2. qa_report 占位符（错——本批唯一的真新洞）**
- ① 后端把"读坏"从 None 改成 `{"error": …}`（`app.py:1469-1470`），提交声称"明说不代表没跑过"。**但没有一行代码渲染这个 error 字段。**
- ② **新洞**：唯一消费方 `GatePanel.tsx` 的 `AcceptancePanel`——`qa = acceptance?.qa_report`（:190，占位符为真值）→ `sum = {}`（:192）→ `failed = 0`（:194）→ `qaOk = !!qa && failed===0 && sum.verdict !== 'no_go'` = **true**（:195）→ 渲染**绿色"QA：通过"**（:204-205）。改前（None）是灰色"无报告"+一行说明。即：**修复后人审门上"QA 报告读坏了"显示成"通过"——比它要修的那个病更糟的 fail-open**。后端 `witness.warn` 是唯一能看见真相的通道，但坐在 GATE3 前的人看不到。
- ③ 修法很便宜：后端占位符里带 `"summary": {"verdict": "no_go"}` 之类（或前端判 `qa.error`），并让 :205 渲染 error。
- 证据：`app.py:1469` vs `GatePanel.tsx:190-205`；`git log` 确认 GatePanel.tsx 最后一次改动早于本提交、其后无人补 error 处理。

**3-4. test_plan / traceability 占位符（对，弱）**
- ① 出声成立（`app.py:1437,1457`）。② 无害。③ 前端**没有任何组件**消费这两个字段（全前端 grep 仅 `api.ts` 定义）——"人审页上跟没有长得一样"的声称只落在 payload 层，UI 层今天无所谓；若将来接上 UI，`[{"error":…}]` 塞进列表型字段、`"[读不出来: …]"` 塞进文本字段，渲染前要再对一次形状。

### fc8665b —— part-03 三处 + 守卫判据

**1-3. 三处"安全网自身失败"出声（对）**：worker 异常转 FAILED 失败（`orchestrator.py:429-435`）、超时转 FAILED 失败（:505-511）、孤儿探测外层（:694-698）。都是纯加 warn；与"收割已 pop、下一轮够不着"的孤儿语义自洽。无新洞。

**③ 漏网（fc8665b/70478ca 交界）**：worker 异常分支的抢救段 `_salvage_timed_out/_save_trace/_account_salvaged` **没包兜底**（`orchestrator.py:442-445` 裸奔），而超时分支同款三连包了 `_strand_guard("salvage_timeout")`（:513-518）。三被调方内部各有 try（`_save_trace` 两大块自包、`read_partial_*` 自吞、`_account_salvaged` 自包），实际可抛面窄；但**一旦真抛**，异常穿出 `_reap_futures` → 整个 `run_queue` 中止 → **所有在跑 future 被放弃**（新循环看不见 RUNNING 任务）＝§65 孤儿**整批**重现。低概率、高后果、且同形异命本身就是要消掉的账。

**4. 守卫判据洞（对）**：`_is_noisy` 不再下钻嵌套 handler，"内层出声算外层"的虚减消除；钉了正反测试。残留不精确（嵌套 try **体**里的 raise 仍算给外层，而实际会被内层 handler 接走）——偏差方向是**少记功**，棘轮偏严侧，安全。证据：`test_no_silent_except.py:58-88`（实测绿）。

### e3a835d / 70478ca —— "标 running 没人管"族 5 处

- ① 根因（transition(RUNNING) 与 future 登记之间断掉）的修法成立；`_strand_guard` 先重读盘上状态、只在 RUNNING 时改（`orchestrator.py:364-388`），"自动改不违反只报不改"的辨析成立（不猜：没有 future 就没在跑）。4 个消费点（mq.submit / finalize / drain 主体 / salvage 段）全包；`_dispatch_ready` 现也走它（:136），**5 个调用点数过，docstring 的"统一"现在是真的**。
- ② 孤儿探测 `_warn_orphan_running`（:550-586）：判据前提"此刻无 future 无 pending 无就绪"在调用点成立（:712-717 先查 `not running_futures and not pending_batches`）；只报不改；真机验过（09-13 造孤儿→15 秒内 1 条、幂等）。小账：`_orphans_warned` 集合无界（进程级、量小）；空转 tick 每 ~3s 全量扫 tasks_dir/*.json（量小可忽略，任务数上几千再管）。
- ③ 见上条漏网（worker 异常分支抢救段）。

### 1775f5d —— 900s"一把尺"

- ① **主根因真解决**：`RunContext.deadline_at`（`_types.py:33-39`）→ `_dispatch_budget_s = deadline_at − now − 90s 余量`（`_exec.py:168-187`）→ `dispatch(budget_s=)` → `executor.budget_s`（构造后挂，`_dispatch_exec.py:276`）→ `min(_EXEC_BUDGET, budget_s)`（`openai_agent.py:399`）。逐点核过：轮间看表在**第一轮调用之前**（:406）、单次调用封顶按剩余预算（:962-963）、`None` 语义保住 goal_loop/阶段级（`goal_loop.py:66` 不设 deadline_at→0→None，不是 0）、`min` 保住 `QIDIAN_EXEC_BUDGET` 验证开关（:130-131）。
- ② **本提交当时引入/带病两个洞**（都是我独立复核确认、然后发现同晚 `4281960` 已修）：
  a. **fallback 链每次尝试塞同一份 budget_s**＝每轮重置预算，最坏 3× 超时（原 `_dispatch_exec.py:223` 直传不变量）→ 现已折成绝对时刻、每轮重算（`_dispatch_exec.py:191-195,223`）。
  b. **起表在 worker 开头 ≠ submit 时刻**：并发 1、排队几分钟是常态，排队 >90s 余量时自收尾照样赶不上收割——commit 里"离派发只有线程调度那点差"在排队场景为假 → 现已由 orchestrator 在 submit 前算好传入（`orchestrator.py:123-136`、`_task_runner.py:200-204`）。
- ③ 当时更该改没改：claude-cli / anthropic-api / zhipu-api 不消费 budget_s（registry 共 4 执行器，`dispatcher.py:40-45`）→ `4281960` 已补（各自 `max(1.0, min(原上限, budget_s))`）。
- 判：**主修对；单看该提交"存疑"，以当前树看三洞全闭合、判对。**

### 6d11375 —— 经验分层判据

- ① 原判据挂在模块级 `_consolidate_calls % 10`（重启归零）→"从没跑过"；改后计数落盘 + 1h 时间兜底（`_memory_consolidator.py:28-64`），首次进新目录只记起点不烧重活。真机后续验证：轮 5 真出 3 条轨迹。
- ② 边缘：状态文件**读、写持续同时失败**（目录不可写）时 heavy 永不跑且无痕——低概率（不可写会先炸别的）；`_heavy_due` 是无锁 RMW，今天唯一调用方是 `_loop_worker` 单线程（`app.py:580` 一带），合并风险理论。读坏单次自愈（当 {} → 重记起点 → 最多延迟 10 次）。
- ③ 文档侧顺手纠了 `_llm_judge_causal` 不挂 %10 的错，属实（它在主循环、闸门是 0.55≤sim<0.85）。

### §64（972e79f）与 §66（547e742）

- §64：三个名字核实存在（`_memory_lifecycle.py:167,191`、`_memory_graph.py:659`）；import 放在函数内、不引入循环导入问题；守卫补盲区（星号摘掉再跑 ruff + `hasattr` 滤星号供名）实测绿。真机轮 5 真出 insight/抽象产出。无新洞。
- §66：`RouteResult` 现字段确无 level（`router.py:63-68`）；`from_dict` 唯一调用点核实为 `_api_tasks.py:143`（?format=md）；真机复验 200。`stored_tool_batches` 空值回落现算，round-trip 保真。无新洞。

---

## 汇总：该动手的清单（按值排）

1. 🔴 **qa_report 占位符（a33bf79）**：让 GATE3 人审门别把"读坏了"渲染成绿"通过"。改 `app.py:1469` 占位符带 `"summary":{"verdict":"no_go"}`（一处）或前端补 `qa.error` 渲染（两处）。**这是本批唯一"错"判。**
2. 🟠 **worker 异常分支抢救段**（`orchestrator.py:442-445`）包 `_strand_guard`，与超时分支对齐——低概率但一旦发生是**整批孤儿**。
3. 🟡 **roles.py 读侧**（:163-166）坏文件只 logging——建议进 witness（同文件同病，写侧已修）。
4. 🟡 **secret_key**：改"不覆盖+出声+内存随机"或至少把那行写换原子写——免得一次截断写把后端钉死在起不来的状态。
5. ⚪ 文档债一行：`_io.load_toml` docstring"解析失败返回空 dict"与代码相反（代码抛）。
6. ⚪ 小账：`_orphans_warned` 无界、孤儿扫描频率、`fusionConfig` 前端死绑定。

## 越界说明

- 按约束未读 `~/Desktop/静默except分类/` 与 `~/Desktop/ZCode审阅/` 下任何文件（本文件除外——它是输出目标）。
- OPEN.md 属于工作目录指针文件，任务里点名引用了它的 commit 清单，故读过；未读 docs/ 下三份扫描/评审归档（避免被它们的结论喂判据——本报告所有"对/错"都是直接翻代码得来的）。
