# PRD: Singularity Stabilize Phase (2026-06-24)

## Status: IN PROGRESS

## Completed

### 1. Architecture Cleanup ✅
- Single data source: agents_custom.json
- Model rating system: SSS+ ~ A with frontend colored badges
- Current lineup: E=2(kimi-k2.7, qwen3-coder-plus), E+=4(deepseek-v4pro, kimi-k2.7, qwen3.7-plus, glm-5.2), D=3(qwen3.7-max, glm-5.2, kimi-k2.7)

### 2. Sync Infrastructure ✅
- remove_agent dual-lock pattern
- SSE agent_change events with debounced frontend refresh
- Type-safe model CRUD
- SW removal for cache elimination

### 3. Bug Fixes (6) ✅
- Graceful shutdown crash
- Skills import path
- Batch file read support
- Token estimation push
- API startup detection
- Agent sync reload
- Ghost agent cleanup

### 4. 3-Gate + D Review Inner Loop ✅
- GATE1 (research) → GATE2 (planning) → EXECUTING → [inner: D review → fix → test] → GATE3 (delivery)
- fix_round tracking with MAX_FIX_ROUNDS=3 hard cap
- Project phase: GATE3 → DONE (was GATE4)

### 5. Model Management UX ✅
- Scan API models → preview with ratings/cost → batch import with tier assignment
- Auto-assign agents on import
- Rating badges (color-coded) in model cards
- Layer switch: ON/OFF/disabled toggle per model per tier

### 6. Observer Agent Research ✅
- Requirements defined
- E-layer research complete (7.5/10 quality)
- Needs D-layer redo for production quality

## Pending

### High Priority
- **Test coverage**: 40+ modules have zero unit tests. Highest-ROI targets: `_exec.run()`, `_finalize_result()`, `router.decide_cascade()`, `dispatcher.pick_agent()`
- **ReadOnlyTracker interface**: 9+ modules call `tracker._read()` directly — define a proper seam
- **merge 6 API sub-modules** back to single `_api.py` (984 lines, under limit)

### Medium Priority
- **Architecture friction** (see `/tmp/architecture-review-20260624.html`): 12 findings, 8 Strong
- **Observer agent implementation**: D-layer redo → implement
- **GATE3 approval UI**: Frontend completion for 3-gate flow

### Low Priority
- Layer Switch drag-and-drop UX
- Fusion config UI improvements
- Skill matrix auto-population

## Verification

```bash
pytest tests/test_scheduler/ -q  # 75 tests
python3 tests/test_exec_run.py    # 21 tests
# All 96 pass
```
