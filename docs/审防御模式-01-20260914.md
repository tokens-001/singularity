# 审 防御模式.md（J / ZCode · GLM-5.3-Flash）+ 我的核实

> 下面是**报告原文一字未改**。原始件 `~/Desktop/ZCode审阅/审防御模式-01.md`（2026-09-14 03:55，34664 字节）。
> 任务：逐条对照源码审 `docs/防御模式.md`（判"同根因该合并 / 是空话 / 今天还成不成立"）。
> ✅ **约束遵守**：自报"每条都对照 `src/singularity/` 源码核了锚点，二三两节给了文件:行号"。
> 🏆 **它抓到的头号东西（§46 那条）我核实了、已修** —— 见下，这是本轮唯一的代码改动来源。

## ✅ 我核过的：**条数、头号发现、两条独立结论 —— 全真**

| 它说 | 我怎么核的 |
|---|---|
| 文档里 **67 条**，编号连续无缺号（52b/9.1/9.2 是带编号的子条，不算） | ✅ 我脚本数了：`^### N.` 共 **69** 个 = **[1..67 无缺号无重号] + 9.1 + 9.2**；另有 `### 52b.`、`### §67 补记` 两个非数字标题。**跟它的说明逐字对上** |
| 🏆 **§46 的修法只落在 `project.py:479`**，`_io.atomic_write_json` 的 tmp 仍是确定性命名、无 pid ⇒ 跨进程竞态在**共享层原样活着** | ✅ **确认，而且比它说的更值得修**（见下），**已修 `88eef70`** |
| §7 今天**已不成立**（merge 现在直接拒绝脏工作区，不再"静默 stash 卷走"） | ✅ `_git_worktree.py:170-176` `_do_merge`：`status --porcelain -uno` 非空即 `return MergeResult(ok=False, ...)`，注释原话"防呆: 不静默 stash 卷走/丢失用户改动" |
| 9.1 的 `_consolidate_calls % 10` **已修**（改成时间节流） | ✅ `_memory_consolidator.py:73` 现在是 `now - _last_run < 300` 的 300s 节流，注释里留着当年那条"单进程生命周期内 <10 次 ⇒ 永远不跑"的记录 |

### 🏆 §46 那条我为什么说"比它说的更值得修"

它说的是"§46 描述的那个竞态在 `_io` 层原样活着"。我往下多核了一层，发现**这不止是漏改**：

- `_io.atomic_write_json` 是 **api_store / tracker / _memory_core / _token_budget** 的共用入口，
  而且是**被别的模块明确托付过**的 —— `route_learner.py:156` 的注释原话是
  **"atomic_write_json 解决 ②，这把锁解决 ①"**，即它把自己那份的**跨进程**那半
  交给了 `_io`。**而在 `_io` 没 pid 的这段时间里，那个承诺是空的**（注释承诺了、实现没做到）。
- `project.py:474-478` 那条注释把后果写得很全：A replace 成功后 tmp 就没了、
  B 的 replace 撞 ENOENT、或交错写入 → 正式文件多出半个 `}` → 解析失败 →
  **`list_all()` 静默跳过 → 项目从界面上凭空消失**。
- 两样缺一不可（**同进程多线程拿到的是同一个 pid，pid 后缀挡不住它们**）：
  已按 `project.py:479` 同形补上 **pid + 模块级锁**，2 条新测试、**2 条变异全红**
  （去掉 pid / 去掉锁各红一条），全量 1315 绿。

## ⬜ 我**没核**的（别当结论）

- **第一节 7 组合并建议**（§32+§33+§54 · §34+§41+§56 · §48+§64 · §63+§67 ·
  §14+§15 · §38+§53 · §40+§66）、建议拆 §21、§7/§19 规则行重复 —— **是它的判断主张，
  我一条都没核**。要动文档时再逐条看。
- **第二节"空话只有 §11 和 §19 两条整条成立"**、另 11 条混注意点 —— 同上，**没核**。
- **它说 §29/§23/§17 三条"半成立"**、§9/§11/§18/§19/§22/§62"判不了" —— **没核**。
- 两处小发现（§64 的"10 个星号 import"现在是 11 个 · §54 一处注释自称"剩余缺口"
  但记账其实已在 `execution_judge.py:111` 接上）—— **没核**。

## 📌 它这份里唯一要立刻办的已经办完了

其余都是**"怎么改文档"的建议**（合并、拆分、删空话），属于 H 说的"写作时的活"，
按 H 的建议**不该为它单开窗口** —— 等下次真去改 `防御模式.md` 时顺手做。
⚠️ 所以这份的归档价值，主要在**它逼出来的那一条代码修复**，不在那 7 组建议。

---

# 审《防御模式》- 01（2026-09-14）

**范围**：`docs/防御模式.md` 里 `### 数字.` 编号那批。**我数到 67 条**（`### 1.` ～ `### 67.`，编号连续无缺号）。
另有三个带编号但非纯数字的子条（`### 52b.`、`### 9.1`、`### 9.2`），不算进 67，判完主批后附在文末一并判了；
末尾 `### §67 补记` 按约定不算。

**依据**：`防御模式.md` + 仓库源码（`src/singularity/`、`tests/`、`src/singularity/web/frontend/src/`）。
未读：`Desktop/静默except分类/`、`Desktop/ZCode审阅/`（除本文件）、`docs/*20260914.md`。

**判定口径**：
- ① 同根因该合并 / 建议互引 / 建议拆分。
- ② 空话 = 规则写成"注意点"，落不成可查的判据（grep 模式 / 测试 / 结构约束）。给 `防御模式.md:行号`。
  取值：**是**（全条够不上判据）/ **半**（主体可执行、混了注意点，或本仓无锚点）/ 否。
- ③ 今天已不成立 = 锚点消失或机制已换。给代码 `文件:行号`。
  取值：**是** / **半** / 否（含"已修，规则即修法，仍有效"）/ **判不了**（借条无锚点、或修在源码之外）/ 不适用（流程条无代码可判）。

---

## ① 合并组（结论先行）

**建议合并（7 组）：**

| 组 | 条目 | 同一个根因 |
|---|---|---|
| A | §32+§33+§54 | 金额链（记录→计价→显示）上三处"缺失被良性解释"：查无价兜底 $0.50 / 前端 `\|\| 0` / 合成键查无价。§44 自认总纲，这三个是同一条链的上下游 |
| B | §34+§41+§56 | 导入期冻结派生状态（模块级路径常量 / 单例 / `__init__` 冻路径）。§56 文中自认"§34 复发"，§41 是同一机制的单例变体 |
| C | §48+§64 | 未定义名被 `except` 吞成默认值。§64 开头自认"同一个形状又咬了三次"，增量只是"守卫在星号 import 文件里是瞎的" |
| D | §63+§67 | 同一把刀（外部 900s 收割）对执行器预算的层错配。§67 开头即"§63 那一整套上完之后照样撞"，§67 补记又是它的续篇 |
| E | §14+§15 | 同一锚点（tools 与 tool_choice 的配对）的镜像两坑：给错了工具面 / 关了 tools 忘了 choice。`openai_agent.py:438` 一行就是两条的合流修法 |
| F | §38+§53 | 记账覆盖面：同一根因（新增调用路径没接账）两次发现。§53 的"顺调用点数"更锋利，作主规则；§38 的 `by_level` 是工具 |
| G | §40+§66 | 删字段 vs 消费点失配：§40 是 `**r` 反序列化路径，§66 是 `from_dict` 的 kwargs。都是"删一方、没扫另一方"，静态检查都够不着 |

**互引不合并（锚点不同层）：** §6↔§49（检查侧拿错基准 vs 派发侧漏传 cwd）；§13↔§46（46 是 13 的补集："锁+原子写≠进程安全"）；§29↔§62（§62 的 `_V2_EXTRACT_FALLBACKS` 就是 §29 后半"写死精确版本号"的活实例）；§25/§47/§52 已挂在 §44 总纲下；§55↔§59（链条已互引）；§28 例2↔§60（同一函数线，病不同）。

**建议拆分：** §21 一条装两个独立坑（子进程环境清洗——已落实；临时文件 0700/随机名——本仓无对应实现）。

**编辑残留（重复行）：** §7 规则行整段重复（防御模式.md:64 与 :66 一字不差）；§19 规则行重复（:139 与 :141）。

## ② 空话（总结）

- 全条够不上判据的只有两条：**§11**、**§19**。
- 半空话（混注意点或无本仓锚点）：§4、§9、§12、§18、§21 后半、§22、§24 后半、§31、§42、§43 前半、§48 规则2。
- 其余 54 条的规则都有可查锚点或结构判据（明细见逐条）。

## ③ 已不成立 / 需改写（总结）

- **是**：§7（merge 已改为拒绝脏工作区，"卷走"机制没了）。
- **半**：§17（240s 已落）、§23（"禁 shell=True"与执行器 run_command 的有意 shell=True 冲突）、§29（registry 已生效，精确版本号白名单仍活）。
- **判不了**：§9、§11、§18、§19、§22（借自 DeepSeek Harness，本仓无锚点；§22 连 API 名都是 JS 的）；§62（修在运行时配置，不在源码）。
- 其余：坑仍在（规则有效）或已修（规则即修法），都成立。

## 审计顺带的新发现（非三问，但要报）

1. **§46 没修全**：pid 后缀只落在 `project.py:479`；`_io.py:21` 的 `atomic_write_json`（tracker/api_store/_memory_core/_token_budget 共用）tmp 仍是确定性命名、无 pid——§46 描述的跨进程竞态在 `_io` 层今天原样活着。
2. **§64 数字漂移**：文中"src 下 10 个文件带星号 import"，实测现在 **11 个**。
3. **§54 内部矛盾**：`_dispatch_exec.py:490-493` 注释自认"v2 融合那几步仍没回传，是已知剩余缺口"，但 `execution_judge.py:110-111` 已按 `level="fusion"` 记账——两处需对账（疑注释过期）。
4. §7 / §19 规则行各重复一遍（编辑残留，见上）。

---

# 逐条判定（1–20）

### §1 终态可以被改判
- ①：独立（状态机基座，§65 的孤儿判断也依赖它）。
- ②：否——判据可执行（终态白名单 + 显式 `force=True`）。防御模式.md:26-27。
- ③：否，已实现且在岗：`src/singularity/scheduler/tracker.py:52`（_TERMINAL_EXIT）、`tracker.py:214-223`（transition 的 force 闸）。

### §2 QA 门禁排在 DONE 之后
- ①：独立。
- ②：否——"先看 DONE 在哪一行"是可操作步骤。防御模式.md:32。
- ③：否（已修，规则即修法）：DONE 已延后到 QA 之后——`src/singularity/scheduler/_task_runner.py:321`（"QA gate: 必须在标 DONE 之前"）与 `:359`（QA 通过才 transition DONE）。

### §3 写入的字段 ≠ 决策读的字段
- ①：独立（§44 族的远亲）。
- ②：否。防御模式.md:37-38。
- ③：否（已修）：接线已落——`src/singularity/scheduler/_exec.py:674-679`（`min(旧,新)`，注释把两条数据流的由来写明了）。

### §4 审查路径上多个 bug 叠加
- ①：独立（方法论条）。
- ②：半——"跑一遍真实路径"是可执行动作，但无判据无锚点（防的"叠加 bug"本就只能靠跑看见）。防御模式.md:43。
- ③：不适用（流程规则，无代码锚点可判）。

### §5 同一动作有多条入口
- ①：独立。
- ②：否——判据可数（入口数量收敛）。
- ③：否，落实：`project.py:176`（唯一实现 confirm_gate），其余入口全转发——`_api_projects.py:189/214`、`_cli_projects.py:147/236`、`_observer_answer.py:171-203`。

### §6 worktree 任务里的路径基准用错
- ①：与 §49 同族不合并（互引）：6 是检查侧拿错基准，49 是派发侧漏传 cwd——锚点不同层。
- ②：否——判据具体（基准=cwd；"没找到文件"不得当通过）。防御模式.md:58。
- ③：否，已落实且仍成立：`validator.py:63-67`（"cwd 必须是任务执行的 worktree 路径，不是主仓库根"）、`:239`（root 取 cwd）。

### §7 跑任务前工作区是脏的
- ①：独立。
- ②：半——规则是习惯性注意点（先 commit/stash、先 status）；代码已把它变成结构保证后，作为"规则"独立价值不大。另：规则行与下一行一字不差重复（防御模式.md:64 与 :66）。
- ③：**是**——根因机制已不存在：merge 现在直接拒绝脏工作区，`src/singularity/scheduler/_git_worktree.py:170-176`（"防呆: 不静默 stash 卷走/丢失用户改动"）。注意 `snapshot.py:68-70` 仍用 `git stash create`，那是临时 commit、不碰工作区，另一回事。

### §8 SSE 断线重连不清理队列
- ①：独立（与 §10 同在 SSE 一处，病不同：配对 vs 隔离）。
- ②：否——判据可执行（注册注销成对 + try/finally；队列有界、满丢旧）。
- ③：否，已落实：`web/app.py:2088-2091`（上限 503 + 有界队列 maxsize=10）、`:2149-2152`（GeneratorExit finally 清理）、`:711-721`（广播时判死清理）。

### §9 清理只发信号、不等停稳
- ①：借条（DeepSeek Harness），独立。
- ②：半——原则正确但无可查判据，且通篇事件回调语境。防御模式.md:80-81。
- ③：判不了——本仓无 dispose 锚点（grep `dispose` 全 src 0 命中）。

### §10 分发器里一个回调抛异常，饿死后面的
- ①：独立。
- ②：否——判据可执行（逐个 try/except + 记日志）。
- ③：否，落实（本仓对应物是 SSE 广播循环）：`web/app.py:711-721`（逐个 try、死连接清理，一个客户端满/炸不连累别人）。

### §11 把异步状态当同步结果
- ①：借条。
- ②：**是**——"自己定义区间""显式处理无需等待分支"是原则句，无锚点、判不出照没照做。防御模式.md:91-92。
- ③：判不了——本仓无 idle/running 状态机对应锚点（仅 `_cli_tasks.py:134-147` 的 idle_ticks 是另一含义）。

### §12 worktree / 临时资源用完没清
- ①：独立。
- ②：半——通用纪律（创建即登记/try/finally），无本条专属判据；本仓已按此落实。防御模式.md:97。
- ③：否：`_worktree.py:121-152`（创建带上限检查 + 失败 warn，连"降级后无沙箱"都出声）、`_git_worktree.py:137`（finally 清理）、`_worktree.py:64`（cleanup_task_artifacts）。

### §13 跨线程改共享状态没守锁约定
- ①：与 §46 同族不合并（互引）：13 是"必须走锁+原子写"，46 是"锁+原子写≠进程安全"——46 是 13 的补集。
- ②：否——有具体锚点（_LOCK、临时文件+rename）。防御模式.md:102-103。
- ③：否：`tracker.py:18-20`（RLock + 注释）、`tracker.py:127-129`（_write 走 atomic_write_json）。

### §14 架构任务给模型开了工具
- ①：与 §15 同锚点，**建议合并**（组E）；§50 同向不同层，保留独立、互引。
- ②：否——判据可执行（纯思考类禁工具 `tools=[]`）。
- ③：否（已修）：`openai_agent.py:457-459`（thinking 模型 required→auto 降级重试）、no_tools 机制 `:356-358`。

### §15 空 tools + tool_choice=required
- ①：组E（与 §14 合并）。
- ②：否——判据就是一行代码形状。
- ③：否（已修，规则原文已落成代码）：`openai_agent.py:438`（`"required" if tools else "auto"`）。

### §16 让模型自己决定用 python 还是 python3
- ①：独立。
- ②：否——判据可 grep（prompt 写没写死解释器）。
- ③：否（已落实）：`openai_agent.py:249`（prompt 写死 `python3 -m pytest -q`，连"只在测试已存在时才跑"都写了）。

### §17 模型调用超时设太短
- ①：独立。
- ②：否——后半句（models_custom.json 的 speed/reasoning 覆盖）是可查判据。
- ③：**半**——前半已修：`openai_agent.py:1051`（httpx.Timeout(240.0)）；后半仍有效：`model_registry.py:65`（models_custom.json 仍在）、`:25`（speed 字段默认 medium）。

### §18 正交结果独立上报
- ①：借条。
- ②：半——"独立事实单独上报"是原则；timedOut/signal/exitCode 是 JS 语境命名，本仓无对应。防御模式.md:134。
- ③：判不了——grep `timedOut`/`exitCode` 全 src 0 命中，无锚点。

### §19 公共约定两侧都要遵守
- ①：借条。
- ②：**是**——"统一规范化错误出口并在类型定义处写明"是原则句，无可查判据；且规则行在防御模式.md:139 与 :141 一字不差重复两遍（编辑残留）。
- ③：判不了——本仓无 provider/wrapper 分层错误出口的对应锚点。

### §20 setdefault 不覆盖已存在的环境变量
- ①：独立。
- ②：否——判据可执行（显式赋值；401 先 `env | grep KEY`）。
- ③：否：剩余的 setdefault 只有 `web/app.py:27`（.env 装载，"环境已有则不覆盖"正是 .env 的语义，非坑）；规则作为改配置时的检查仍成立。

# 逐条判定（21–40）

### §21 别把环境变量 / 可预测路径暴露给不可信输出
- ①：一条装两个独立坑，**建议拆分**：子进程环境清洗（本仓已做）与临时文件 0700/随机名（本仓无对应实现）。
- ②：半——前半判据可 grep（`*KEY*` 过滤），后半无本仓锚点。防御模式.md:155-156。
- ③：**半**——前半已落实：`openai_agent.py:42`（_is_sensitive_env）、`:798`（safe_env 过滤后传给子进程）；后半判不了：grep `mkstemp`/`0o600`/`0o700` 全 src 0 命中，本仓没有"平台自建临时文件"的对应实现。

### §22 用 unlink 删链接形态的路径
- ①：借条。
- ②：否——判据本身可执行（先判链接再删），但表述是 JS API（`lstat().isSymbolicLink()`）。防御模式.md:161。
- ③：判不了——本仓无"递归删用户可控目录"的路径（唯一 rmtree 是 `_api_monitor.py:48` 清自己的旧目录）；Python 的 rmtree 顶层本就拒绝符号链接，坑的形状不同。

### §23 外部输入拼进 shell
- ①：独立。
- ②：否——判据可 grep（shell=True）。
- ③：**半**（规则需限定范围）：执行器的 run_command 是**有意** shell=True（`openai_agent.py:800-802`，注释写明要支持 `&&`/`|`/source，且已过 safe_env）——"禁 shell=True"对它不成立；平台自用子进程已是 shell=False（`openai_agent.py:1148`）。该改写成"平台内部子进程禁 shell=True；执行器工具单列"。

### §24 凭证进仓库
- ①：独立。
- ②：半——前半可执行（凭证只走 env/.env），后半"提交前 git diff --cached 扫一遍"是人肉注意点。防御模式.md:171。
- ③：不适用——历史事故条 + 流程规则，无锚点可判"坑没了"。

### §25 fail-open 漏报
- ①：与 §47 规则3 同向（fail-closed 两面），互引即可；§44 是更大的伞。
- ②：否——判据可执行（检查自身异常 = 拦下并报错）。
- ③：否，落实例：`_review.py:510-522`（QA 判 needs_fix 却给不出条目 → 降级"结论不可用"，不 retry）。

### §26 前端异常直接透成 500
- ①：独立。
- ②：否——两半都可验收（handler 结构化返回；前端非 200 兜底）。
- ③：否，大体落实：handler 全走 `_api_handler` 返回 `(data, code)`（如 `_api_projects.py:229-257`）；前端有 errText 出口（`frontend/src/lib/toast.ts` 等）。⚠️ 全局 `@app.errorhandler` 不存在（grep 0 命中）——"统一 try/except"是散装的，这半不算全落实。

### §27 循环 import 用延迟导入破
- ①：独立。
- ②：否——判据可执行（别把延迟 import 提到顶层；报错先查导入入口）。
- ③：否——延迟导入仍是既有设计：`dispatcher.py:70/112/134/170/207` 全是函数内 import。

### §28 API handler 返回 200 却没接真实动作
- ①：独立；例2 与 §60 同在 gate-confirm→阶段推进一条线上，但病不同（没人启动 vs 清单没人写），互引即可。
- ②：否——判据锋利（"返回的每个表示成功的字段都要能追到一个副作用"）。防御模式.md:211。
- ③：否（两例修法都在）：例1 `_api_projects.py:243-254`（template/gate 档直接 409 + 指路）；例2 `:198-207`（approved 分支起 planning，注释写明为什么）。

### §29 硬编码常量覆盖配置
- ①：与 §62 同族不合并（互引）：62 的 `_V2_EXTRACT_FALLBACKS` 就是本条后半的活实例。建议 29 加一句指向 62。
- ②：否——判据可执行（只从 registry/config 读；通配别写死版本）。
- ③：**半**——前半已修：`dispatcher.py:134-141`（registry 的 max_turns 生效，含注释）；后半仍活：`execution_judge.py:443`（`("glm-5.2", "deepseek-v4-pro")` 写死精确版本号）。

### §30 字段语义复用当缓存键
- ①：独立。
- ②：否——判据具体（显式 `cached_at`）。
- ③：否（已修，规则=修法）：`router.py:21`（字段）、`:95`（过期判断）、`:165`（写入时打点）。

### §31 静态扫描「0 引用」≠「没人用」
- ①：独立。
- ②：半——"删除前逐条复验、删后复扫"是流程注意点；有配套清单（`docs/死代码清单-20260909.md` 存在）但无判据。防御模式.md:226-227。
- ③：否——规则是防复发用的，谈不上过期。

### §32 静默兜底 = 编造数字
- ①：**组A**（32+33+54，金额链静默兜底），建议合并。
- ②：否——判据锋利（查不到返回 None；适用所有估算类数值）。
- ③：否（已修）：`model_prices.py:15`（"rates.get(model,0.50) 的坟"——修法注释就钉在原坑上）、`_token_budget.py:292`（unpriced_models）。

### §33 缺失值在前端被 `|| 0` 渲染成 0
- ①：组A。
- ②：否——判据具体（金额走唯一出口；缺失渲染"未配置"）。
- ③：否（已修）：`frontend/src/lib/money.ts` 存在且自述"金额显示的唯一出口"（UNPRICED='未配置价格'）；前端残留的 `|| 0` 只剩非金额用途（如 `GatePanel.tsx:104` 的字段计数）。

### §34 写盘时算 derived 值 → 历史被污染
- ①：**组B**（34+41+56，导入期冻结），34 为主体。
- ②：否——判据可执行（读时现算；模块级派生路径禁令）。
- ③：否（已修）：`_memory_core.py:52`（_events_path() 函数）、`route_learner.py:147`（_learner_path()）；守卫 `tests/test_scheduler/test_isolation.py:43`（test_derived_paths_are_computed_at_call_time）。文中说的例外（web/app 的 `_ad`/`_key_file` 导入期一次性初始化）仍在：`web/app.py:66/:115`——与文档一致。

### §35 折叠按天聚合时，只重算"今天"会重复计
- ①：独立。
- ②：否——判据具体（按天归位；max 的单调性前提）。
- ③：否：`_token_budget.py:191-200`（"按天各自归位 + 分量取 max"的推导注释就在实现上）。

### §36 "先剪枝后归位"会静默丢数据
- ①：独立。
- ②：否——"顺序写进注释"本身就是可验收动作。
- ③：否：`_token_budget.py:261-263`（①折叠→②剪枝→③写盘 的顺序注释与实现同在）。

### §37 流式响应默认不带用量
- ①：独立。
- ②：否——判据是具体参数（`stream_options.include_usage`）。
- ③：否（已落实）：`openai_agent.py:955`、`execution_judge.py:71-77`。

### §38 只统计了一部分调用路径
- ①：**组F**（38+53，记账覆盖面）——同根因两次发现；建议合并，53 的"顺调用点数"作主规则，38 的 by_level 是工具。
- ②：半——前半"先 grep 一遍再动手"是流程；后半（level/by_level 按用途拆）有锚点。防御模式.md:290-291。
- ③：否（已落实）：record_tokens 现有 4 处调用点（`_task_runner.py:124`、`workflow.py:260/275`、`execution_judge.py:111`、`orchestrator.py:296`），`by_level` 在 `_token_budget.py:387-407`。

### §39 把内部记录塞进会被"整行重建"的地方
- ①：独立。
- ②：否——判据是结构性的（人维数据与机器重写记录分文件）。
- ③：否（已落实）：`model_prices.py` 独立成文件，`:15` 注释自述来由。

### §40 删字段前先看有没有 `Cls(**row)` 的载入路径
- ①：**组G**（40+66）——同一根因两次咬人：40 是 `**r` 载入路径，66 是 `from_dict` 的 kwargs。建议合并（40 为主体，66 补"HTTP 面 + 静态检查够不着 kwargs"的增量）。
- ②：否——判据可 grep（`**r`/`**row`/`from_dict`）。
- ③：否（已落实）：`_token_budget.py:33-37`（留字段+注释写明为什么不能删）、`:173`（UsageRecord(**r) 仍在）。

# 逐条判定（41–60）

### §41 测试里的模块级单例不吃 conftest 的隔离
- ①：组B（与 §34/§56 合并）。
- ②：半——"monkeypatch.setattr(mod, '_singleton')" 是具体动作，"API handler 函数内 import 单例"是可执行约束；但主坑已用别的路子堵死（见③）。防御模式.md:311-312。
- ③：否（以另一方式修掉）：单例仍在（`_token_budget.py:411`），但 `_path` 改只读属性 + 写前 `_sync_dir` 重同步（`_token_budget.py:153-160`、`:258`）——测试隔离不再依赖"导入时机"。

### §42 翻告警日志不看时间 = 把已修的问题当现存 bug
- ①：独立。
- ②：半——"先看时间戳"是观测纪律，无可查判据；后半（file:line 会撒谎、当前源码不会）是原则。防御模式.md:317-318。
- ③：否——alerts.jsonl 仍是累积文件（witness 的告警通道），规则长期有效。

### §43 加"轮换/分摊"类机制前先问清列表的语义
- ①：独立。
- ②：半——前半"先确认语义"是注意点；后半（列表全展示、不靠制造使用）可验收。防御模式.md:324-325。
- ③：否：撤轮换的决定以注释形式钉在代码里——`dispatcher.py:343-350`（"这里曾经加过轮换…已撤掉"，写明列表语义是"主力 + 备用"）。

### §44 承诺类字段必须三态可分（总纲）
- ①：总纲条——§47/§52 已自引，§32/§33/§54 是它的实例。不合并，反而是组A的合并目标位置。
- ②：否——判据可验收（三态可分；过滤留落选账）。
- ③：否，四件事都在：`project.py:106-134`（to_dict 派生 fields() + unknown_fields_dropped 告警）、`api_store.py:213-215` 与 `:512-527`（dropped 落选账）、前端 errText（`lib/toast.ts`、`FilePanel.tsx`、`McpTab.tsx`）、witness.warn 通道。

### §45 计数器只增不减 = 单向棘轮
- ①：独立。
- ②：否——判据锋利（"它什么时候归零？答不出来就是没写完"）。
- ③：否（已修）：`project.py:193-197`（批 GATE2 时 review_failures/integrate_failures 清零，注释把棘轮病复述了一遍）。

### §46 确定性 tmp 路径 + threading 锁 ≠ 进程安全
- ①：与 §13 互补（见 §13）。
- ②：否——判据具体（tmp 带 pid + 锁保留）。
- ③：否（project.py 已修）＋ ⚠️ **同形状仍在别处活着**：`project.py:479` 有了 pid 后缀；但 `_io.py:21` 的 `atomic_write_json`（tracker.py:129、api_store、_memory_core、_token_budget 共用）tmp 仍是确定性命名、无 pid——两个进程并发写同一文件时本条的竞态原样存在。不是过期，是"修了一处、族没扫完"（§9.2 说的"修复半径小于形状分布"的现场版）。

### §47 代理对象漏字段 = 下游判据恒假
- ①：§44 族（自引）。
- ②：否——三条子规则都有判据（真身测试 / 往下算一步 / fail-closed 默认值）。
- ③：否（已修）：`_types.py:66-72`（_SnapProxy 补了 method，注释写明 validator._diff_base 靠它）、`validator.py:212/:231`（取不到基准 = diff 类检查不做，fail-closed）。

### §48 用了没 import 的名字 → NameError → 被 except 吞
- ①：**组C**（48+64）。
- ②：半——规则1（降级留痕）、规则3（F821 守卫）有锚点；规则2（"必须真跑一次"）是流程注意点。防御模式.md:402-410。
- ③：否（已修）：`_memory_core.py:177`（SentenceTransformer 已 import）、`execution_judge.py:624`（同族注释）；守卫 `tests/test_scheduler/test_no_undefined_names.py`（6 条测试，`:83` 起处理星号盲区）。

### §49 阶段调用的 cwd 漏传 → 在没有隔离的地方跑
- ①：与 §6 同族互引（不合并）。
- ②：否——规则1（"能不能省 = 看默认值兜底到哪"）和规则3（绝不退回 PROJECT_ROOT）都可验收。
- ③：否（已修）：`workflow.py:155`（_phase_cwd）、`:193`（"漏传 = 在奇点仓库自己里跑"）、`:208`（cwd=_phase_cwd(project)）。

### §50 产出契约是"一段文本"的阶段，别给工具
- ①：独立（与 §14 同向不同层：14/15 是 tools 与 choice 配对，50 是契约与工具面匹配）。
- ②：否——判据锋利（"看工具产物去哪"，不是"会不会用到"）。
- ③：否（已修）：`_workflow_phases.py:222-227`（调研 no_tools）、`:300-314`（架构 no_tools）、`_dispatch_exec.py:129/:205-208`（no_tools 透传 + honors_no_tools 不匹配时拦下并告警）。

### §51 "强制输出"那一轮落在循环边界外
- ①：独立。
- ②：否——判据是公式（阈值与循环上界对账）。
- ③：否（已修）：`openai_agent.py:270`（force_output_at）、`:390`；守卫 `tests/test_scheduler/test_force_output_turn.py:19-38`（含 `force_output_at(5,3)==4`）。

### §52 拦截类判据必须能说出"拦的是什么"
- ①：§44 族（自引）。
- ②：否——判据可执行（needs_fix + 空清单 = 降级 uncertain，禁 retry）。
- ③：否（已修）：`_review.py:510-522`（"结论不可用" + needs_fix_unspecified + setdefault 防覆盖）。

### §53 记账的调用点，等于记账的覆盖面
- ①：组F（与 §38 合并）。
- ②：否——判据锋利（顺调用点数，不顺函数名 grep；新路径必须回答"进哪本账"）。
- ③：否（已修）：除 `_task_runner.py:124` 外，`workflow.py:260/275`（阶段调用）、`execution_judge.py:111`（fusion）、`orchestrator.py:296`（超时抢救）都接上了。

### §54 记账的名字必须是能查到价的
- ①：组A。
- ②：否——判据可执行（键必须是计价表键；拆行或显式条目）。
- ③：否（已修，带一个尾巴）：`_dispatch_exec.py:374`（注释）与 `:498-503` 区域（member_usage 按成员记账）；⚠️ `:490-493` 注释仍自认"v2 融合自己那几步仍没回传，是已知的剩余缺口"，而 `execution_judge.py:110-111` 又以 level="fusion" 记账——两处说法需对账（疑注释过期）。

### §55 超时被杀的任务：痕迹和账一起消失
- ①：与 §59 链条互引（59 是"线程没死"机制，55 是"账/痕迹"），不合并。
- ②：否——判据可执行（超时路径单独写已知事实；降级必须出声）。
- ③：否（已修）：`_exec.py:105/:228/:573`（partial usage 落盘/读回/接线）；`orchestrator.py:476-520`（_flag_killed_without_wrapup + salvage + strand guard）；三处执行器基线降级告警（`openai_agent.py:853-854`、`claude_cli.py:141-147`、`anthropic_api.py:217`）。

### §56 单例在构造时冻住路径 → 测试写进生产（§34 复发）
- ①：组B（自认 §34 复发）。
- ②：否——判据可执行（读时现算或写前比对重载；_path 改属性后测试红是好事）。
- ③：否（已修）：`_token_budget.py:153-160`（_path property + _sync_dir）、`:258`（写前重同步）。

### §57 挂起不是异常 —— except 拦不住它
- ①：独立。
- ②：半——规则1（except 拦不住挂起，必须给超时）是可执行判据；规则2 的"必须显式 HF_HUB_OFFLINE=1"与实际修法不符（见③）；规则3/4 是排查姿势。防御模式.md:584-593。
- ③：否（已修，**修法不同**）：`_memory_core.py:178-184`（`local_files_only=True`，"只用本地缓存绝不联网"）+ `_workflow_phases.py:69-90`（preflight_external 探测并明说）。文档里"必须设 HF_HUB_OFFLINE=1"这句应改写——后人照文档找 env 变量会找不到，实际方案是 local_files_only。

### §58 "模型没按协议来"被当成了"模型没干活"
- ①：独立。
- ②：否——判据可执行（N 种格式都认 + 认出来告警）。
- ③：否（已修）：`openai_agent.py:68-92`（DSML 解析器，全角竖线 U+FF5C）、`:532`（recovered/unparsed 告警分档）。

### §59 超时 ≠ 停了 —— 执行器线程还活着
- ①：与 §55/§63 链条互引，不合并。
- ②：否——判据可执行（超时先问"它还在跑吗"；收尾后再查一次取消）。
- ③：否（已修）：`_exec.py:583-587`（收尾前再查取消，注释复述了"8 分钟后还在报错"的实测）、`:551-552`（_mark_dispatch_started，"没发起"与"没落账"分开）、`orchestrator.py:144-171`（_salvage_timed_out 返回残对象、用量先读在 try 外）。

### §60 机械检查永远不跑 —— constraints_checklist 一直是空的
- ①：独立（与 §28 例2 同在 gate-confirm 线上，病不同）。
- ②：否——判据可执行（根因链 + 守卫测试 + 真机双信号"清单有值且告警不响"）。
- ③：否（已修）：`orchestrator.py:855-906`（decompose 补写 constraints_checklist）、`project.py:245-262`（effective_constraints 兜底 + 告警语义改写）、`workflow.py:566-576`（读点换兜底读法）；守卫 `tests/test_scheduler/test_project_phase_advance.py:105`。

# 逐条判定（61–67）

### §61 同一份"架构 schema"存在两份拷贝，字段漂了
- ①：独立。
- ②：否——判据可执行（契约单一定义点；判 prompt 生效要挑"原稿没有、只有 schema 有"的字段）。
- ③：否（已修）：`execution_judge.py:205-213`（_ARCH_SCHEMA 带 covers + 结构化 check）、`:388`（规则6）、`workflow.py:120/:133`（另一份本就带）；漂移守卫 `tests/test_scheduler/test_machine_checks.py:171`（TestArchSchemaCoversContract）。

### §62 模型池只剩 2 个时，裁判类角色的告警全部常亮
- ①：与 §29 同族互引（写死精确版本号的备选表）。
- ②：否——规则1/2 可验收（备选表必须与可用池有交集；常亮告警 = 判据与配置脱节）。
- ③：判不了——修在运行时配置（扩池 + phase-models 指向，数据在 `.qidian/`，非仓库源码）；代码侧 `_V2_EXTRACT_FALLBACKS` 原样（`execution_judge.py:443`），联动查询仍在（`:639/:657/:696`）。

### §63 执行器一圈表都不看 —— 唯一的"总时限"是外面那次无声收割
- ①：**组D**（63+67 合并）。
- ②：否——判据锋利（循环必须知道外部超时；上报口径 ≠ 事实）。
- ③：否（已修）：`config.py:52/:56`（TASK_DEADLINE_S/WRAPUP 单一来源）、`openai_agent.py:127-131`（_EXEC_BUDGET）、`:391-406`（轮间看表 + min 保开关）、`:962-966`（单次调用封顶取剩余预算）、`_exec.py:588-599`（deadline_wrapup 不换模型不重试）。

### §64 星号 import 让 F821 守卫变成瞎子
- ①：组C（并入 §48）。
- ②：否——规则1/3 有判据（星号 = 无 F821 保护；守卫判据真跑 + hasattr 滤正常供给）。
- ③：否（已修）：`_memory_consolidator.py:86`（auto_maintain/system2_extract 补 import）、`:110`（add_inferred_causal_edge 补 import）、`test_no_undefined_names.py:83`（星号盲区守卫）。⚠️ 小漂移：文中"src 下 10 个文件带星号 import"，实测现在 **11 个**（dispatcher/_api/memory/_memory_graph/_memory_lifecycle/workflow/_memory_experience/_memory_consolidator/observer_agent/__main__/_cli_tasks）。

### §65 先改状态、后登记 —— 孤儿 RUNNING 任务
- ①：独立。
- ②：否——判据可执行（状态与登记原子/可回滚；兜底进持久通道；不变量配"此刻不该出现"检查且只报不改）。
- ③：否（已修）：`orchestrator.py:364`（_strand_guard）+ 5 个调用点（`:136/:464/:471/:514/:658`，与文档"全仓 5 处"对得上）、`:665/:724`（_warn_orphan_running）。

### §66 字段删了、调用点没扫 —— from_dict 恒抛，接口恒 500
- ①：组G（并入 §40）。
- ②：否——判据可执行（删字段 = 全仓扫调用点；静态检查查不出 kwargs 拼错）。
- ③：否（已修）：`neijinglu.py:79/:87/:136/:145`（stored_tool_batches 优先 + level 移除的注释钉在原坑上）。

### §67 预算的"时钟原点"和"消费者"不在同一层
- ①：组D（与 §63 合并；§67 补记跟走）。
- ②：否——判据锋利（时钟原点与消费者同层；生产值不生效先怀疑装错层；None ≠ 0）。
- ③：否（已修，文档自认未真机验）：`_dispatch_exec.py:191-223`（_budget_deadline 折成绝对时刻 + None 保持 None）、`_types.py:34-38`（RunContext.deadline_at 与收割同一原点的注释）、`_task_runner.py:156-202`（submit 前算好传入）、`_exec.py:168-185`（_dispatch_budget_s）、`openai_agent.py:391-406`（min 保 QIDIAN_EXEC_BUDGET 开关）、`claude_cli.py:81-83`、`anthropic_api.py:69-75`、`zhipu_api.py:49-57`（三个执行器都消费预算）。

---

# 附录：三个带编号的子条（不占 67 之数）

### §52b 告警文案会被误读成结论
- ①：独立（记的是"我误读告警"，不是代码缺陷）。
- ②：半——规则1（告警名不是行为描述，回判据链核）可执行；规则2 与 §51 同族互引。防御模式.md:494-498。
- ③：不适用（无代码锚点；条目内容本身就是"不是 bug"）。

### §9.1 缺陷的不可见性分三层
- ①：独立（方法论章，自我声明非个案条）。
- ②：半——三层表是分类工具，"动手前先答哪一层"可操作；两个实例有锚点。防御模式.md:936-953。
- ③：**实例一已修**：`_consolidate_calls % 10` 的判据已换成时间节流（`_memory_consolidator.py:31` 注释 + `:73` 的 `now - _last_run < 300`）——9.1 引它当"现存形状"，实际已修，应标注"已修，见 §64 守卫"；实例二（add_inferred_causal_edge 未导入）即 §64，已修。规则本身长期有效。

### §9.2 修复交付标准
- ①：独立。
- ②：否——三选一交付标准可执行（守卫[变异验证]/埋点/真机观测写明看什么）。
- ③：否——F821 盲区实例与 §64 一致且已补（`test_no_undefined_names.py:83`）；"修复半径小于形状分布"的实例（§55 一族）见上面 §46 的新发现——_io 层还有一只活的。

---

## 一句话总结

- **该合并 7 组**：A(32+33+54)、B(34+41+56)、C(48+64)、D(63+67)、E(14+15)、F(38+53)、G(40+66)；§21 建议拆分；§7/§19 有重复行。
- **空话只有 2 条整条成立**（§11、§19，都是借条），另有 11 条混着注意点。
- **已不成立只有 1 条**（§7，机制被"拒绝 merge"取代），3 条半（§17/§23/§29），5 条借条判不了（§9/11/18/19/22），§62 修在源码外判不了。
- **最值钱的新发现**：§46 的 pid 修法没扫到 `_io.atomic_write_json`（_io.py:21），tracker/api_store/_memory_core/_token_budget 的跨进程竞态今天原样活着——建议按 §65 的"按形状全仓扫"把 §46 自己再扫一遍。
