# 未完成清单

> ✅🔴 **240 秒那把尺 —— 09-21 已拍板 ② 并落地 `60a81e16`**（去掉硬顶：单次调用只封在
> **任务剩余预算**内，停滞那把 90 秒的尺兜底）。**总预算一分没多**（`_cap ≤ _EXEC_BUDGET`）。
> **还差两件**：① **真机验**（等止血解除；判据 = 下一轮里"想满 240 秒零产出"那种死法不再出现）；
> ② **取数定 ③ 的阈值** —— 看正常调用到底想多久（`llm_spin_no_output` 告警 + 分诊账）。
> ⚠️ **代价没量**：打转的调用现在能烧到预算见底（原来 240s 就止损），别当它只有好处。

> 🔴🔴 **止血（2026-09-21 20:0x，用户原话：「这几天太烧钱了，我顶不住了」）**
> **真机轮暂停** —— 不开新一轮、**不起后端**（起后端 = 调度循环＋观察者一起起，立刻开烧）。
> **要恢复真机，先跟用户确认。**
> ⚠️ **万一要起后端，先处理 c 轮残留**：项目 `1789965398548` 还挂在 `executing`，
> 里面 **2 个任务状态是 `running`（进程早死了 = 僵尸）**，而 `cancels/` 里**没有**它们
> ⇒ **一重启就被回收成 PENDING、重新派下去**。要么先给这两个写取消标记，要么先把项目停掉。
> 🔵 **账（只读量的，不是估的）**：奇点十天合计 **≈ $11.36**（`.qidian/model_prices.json` 有单价，
> 按天算）。**烧钱的大头不在奇点，在我的会话** —— 十天里我这边 500~3300 次调用/天，
> 每次重读整个上下文，缓存读 4 亿~11 亿 token/天。今天（09-21）是这十天最低的一天（127M）。
> ⚠️ **$11.36 是低估**（09-21 发现）：**审查那四路调用从来没记账**，已补（见下面那条）
> —— 但**补之前的历史花掉的那部分永远查不回来了**。
> 🔴🔴 **09-21 用户更正：钱的大头是「开发」，不是审查** —— 我先前把审查当靶子，**摆错了**。
> **开发那部分按「任务结局」切**（侧车口径，27 个任务，只读）：
> **failed 46.3%** · done 19.4% · blocked 13.0% · running（僵尸）12.0% · 任务已删 9.4%
> ⇒ **约七成花在没交付的任务上**。⚠️ 边界：只覆盖有派发记录的 27 个；"done"也≠"交付出有用的东西"。
> ⇒ **要砍就砍"白做的开发"**：240 秒零产出 · 排队等死 · 自动返工全量重跑 —— 三条已动（见下）。
> ⏳ **待用户给账单实际扣费数**才能倒推砍哪里。
> 📌 **09-21 23:4x 用户：「明天吧」** ⇒ **下一轮排在 09-22**。它就是验下面「接手指针」里
> **09-21 新落地那一批**（240 秒 · 并发 4 · 审查记账 · 自动返工默认关）的那一轮。
> 🔴 **开跑前先问一句再动手**（起后端 = 开烧）。

> 每条一行。**做完就删掉整行**（别留 ✅）。
> **详细分析不写这儿** —— 归仓库 `docs/`（踩坑 → `防御模式.md`；没验过 → `待验证清单-20260912.md`）。
> 📦 **瘦身史**：这是**第六次** —— 2026-09-20 12:5x，**65.3KB → 49.2KB**，**57 条 → 41 条**
> （删的全是"已完成 / 已作废 / 内容已过期"的账；逐条判据搬进快照，**一字未丢**）。
> 历次快照全在 `~/projects/singularity/docs/OPEN快照-*.md`。
> ⚠️ **前五次全长回来过**（09-16 起：13KB → 22 → 15 → 24KB），**每次都是我写的** ——
> 规矩在，人没照做。**新条目只写一行** —— 这个文件被 `UserPromptSubmit` hook **每轮 `cat` 一次**，
> 多 1KB 就是每个会话每轮多背 1KB。不然第七次还会有。
> 🔒 **2026-09-20 拆成两份了**：这个文件只留**要动手的活**。三类"不是活"的东西
> （**已拍板不做 / 没坐实 / 指针**）搬去了 `~/OPEN-别动.md` —— 它**不进 hook**（不每轮念）。
> ⚠️ 所以 **提任何"该做"的事之前，先 `grep` 那个文件** —— 不然就是**重复提**，
> 而那正是它们当初被记下来的原因。
> 🔵 **别写行号** —— 三次归档连着逮到行号漂（代码一改就漂，而它看着跟对的一样）。
> 要指位置就写**文件 + 一个能 grep 到的唯一字符串**（函数名 / 注释原话）；已有的行号已全部改掉。

> 🔜 **2026-09-20 23:5x 改向**：用户说「**先做奇点** —— 既然没办法做小程序，判例助手就先放着」。
> ⇒ **开工先做奇点**（`~/projects/singularity`）。
> ⚠️ **判例助手降级、但没废** —— 它剩下的 4 条还在下面（判决书模式没入口 / 受众矛盾 / README / 审计员vs顾问），
> 只是**排在奇点后面**。小程序的阻塞已搬去 `~/OPEN-别动.md`（**别再重复查一遍类目**）。
> ⚠️ 两个项目**撞 5050**，不能同时跑。

- [判例] 🔴 **判决书模式界面上没有入口 —— 但 8 个技能 + 前端分支 + 测试全在跑，用户碰不到**（09-20 核实）
  `index.html` 的 `切换模式()` 只被 `mode-contract`/`mode-case` 两个按钮调用，`当前模式` 初值就是 `'contract'`，
  **没有任何路径能把 mode 设成 judgment**（`grep judgment` 只命中死分支）。
  来历：`8c01217`「合同审查成默认，判决书分析摘出模式栏」—— **有意摘的**，后续 `abe208e` 大标题换成「合同风险检查」。
  ⚠️ 但后端 8 技能、`submode`（读懂它/挑它的问题）、`test_app_config.py` + `test_report_presentation.py`
  里的 judgment 用例**全绿着** ⇒ **测试绿着、用户碰不到**（同族：`test-wiring-not-just-functions` 第十五次）。
  ⏳ **待拍板**：**接回入口**（普通人收到判决书/仲裁裁决，"读懂它"是真需求）还是**删掉**（连测试一起删）。
- [判例] 🟡 **案情模式受众自相矛盾：入口 3 个对普通人说人话，另 3 个输出律师工作底稿**（09-20 核实）
  `identify_relationship`/`suggest_actions` = 「你是一名法律顾问」；`summarize_case` = 「口语化、无法律背景也能懂」。
  但 `assess_facts_evidence`/`project_risks`/`analyze_opposing_paths` = 「你是XX系统」，
  输出「胜诉前景分项评估」表（法律依据充分性/对方抗辩威胁）+「程序风险：诉讼时效/管辖/举证责任/保全可行性/反诉风险」。
  ⇒ 用户说完"公司拖欠我工资"，拿到的是一份诉讼评估备忘。**既然不面向专业群体，这 3 个要改**。
- [判例] 🔵 **README 数字过期**：写「21 技能 / 10 部法 / 16 文本」，现数 **20 技能模块 / 13 部法 / 19 份文本**
  （民法典按编拆 7 份）。README 也没提库里已有的公司法、合伙企业法、刑法、反不正当竞争法。
- [判例] 🟠 **「审计员 vs 顾问」那一轮：① 做完了，②③ 还没做**（09-20）
  ✅ **① 退路已落地**：`contract_skills.py` 的 `总结` 从「能不能签+先解决什么」扩成**三部分**，
     第三部分是「对方就是不让步时你的退路」，要求具体到「你下一步做什么」、点名禁掉
     「建议协商」这类空话。`总结` **不在 `锚点范围`、也不进 `法条统计`**（`validate_analysis_quality`
     只统计前四段），所以这轮**一个字没碰校验判据**。
  🔴 **顺带挖出的根因（已修）**：**`result-hero` 写死 `display:none`，全文件 JS 里 0 次引用**
     ⇒ 藏在它里面的**「分析可信度」仪表盘**和**「总结」**从来没对用户显示过。
     JS 里那句 `trust-dashboard.style.display="flex"` 设得一丝不苟，**父节点是 none，设了也白设**。
     已补接线（unhide + 用 `总结` 填 `quick-summary`），并加了一条不变量测试：
     **HTML 里写死 `display:none` 的 id，JS 里必须有人碰过它**（当时 9 个元素里只有它是 0 次）。
     ⚠️ 静态 HTML 测试查不出这类洞 —— 它不问"显不显示"（`test-wiring-not-just-functions` 第十六次）。
  ⏳ **还剩两条**：②「改法建议」给「＿＿」占位符（不编数字是对的，但用户就是不知道填多少）
     → 可改成给**常见区间 + 法定上限**（如违约金《民法典》第585条），仍不编具体数；
     ③「缺失条款」让用户"自己排条号"（`建议新增第X条`）—— 排条号是编辑合同的技术活。
  🔵 另两处小账（未动）：**可信度**"低/中/高" 与 **风险量级**"低/中/高"**两套同名三档**；
  `score_analysis.py` 的 note 文案把仓库内路径 `data/laws/missing_laws.txt` **泄漏给终端用户**。
  🔵 **免责只有"去找执业律师"一条出口** —— 对一份租房合同是"正确但不可执行"。
  ⚠️ **真机没验**：全量测试 149 绿 + JS 语法通过 + 变异验证过，但**没跑过一次真分析看页面**。


- [奇点] 🔴🔴 **c 轮的交付「跑一次就崩」—— 而 QA 的判词是「代码质量高」**（09-20 核到底）
  **接手指针那条说「失败全砸在写测试那一半」，但漏了三点**（都已实测）：
  ① **那批测试没丢，还在盘上** —— `refs/qidian/pending/*`：`test_parser.py` 624 行 ·
     `test_stats.py` 592 行 · `test_output.py` 464 行 · `golden/output_table_cjk.txt` 65 行
     （**1,680 行，任务判 failed ⇒ 不合并 ⇒ 留在这儿**；b 轮同类 refs 还有 7 条）。
  ② **捞回来一跑就抓到主线自己的 bug**：31 过 / 38 红。**红的不是一个 bug，是一簇**：
     · **19 条 = 那一个字段 bug** —— `stats.py` 的 `Accumulator.result()` 给 `StatResult`
       传了 **3 个它没有的字段**（`warnings` · `source_cardinality` · `source_cardinality_limit`）；
     · 🔴 **另 19 条已逐条判完（09-20 外派深挖，两边代码都读过）：1 条真 bug + 18 条测试半成品**：
       · **实现 bug ×1** —— `parser.py` 的 `_resolve_spec()`：`strict` 形参默认 `False`
         覆盖掉 `spec.strict=True` ⇒ **`--strict` 在"传 spec"这条路静默失效**。
         （CLI 那条路恰好单独传值，**侥幸没坏** —— 是**静默降级**，会咬人。）
       · **测试半成品 ×18** —— 12 条**同一根因**：`test_parser.py` 的 `_check_tally_invariants`
         把 `LineTally`（**只记坏行原因**）当成 `Accumulator.reasons`（**覆盖全部行**）来断言
         ⇒ **修那一行，12 条全绿**；4 条是 output 表格细节写死；2 条与环境/版本相关。
     ⇒ ⚠️ **更正两次**：我先写「红的根因全是同一个」（只看了第一条），又写「十几个洞」
       （只看了错误类型分布）。**两次都是没查到底就下全称结论。** 真数是 **1 + 18**。
       **教训：把失败按错误信息分堆，分出来的是「形状」；谁对谁错，得两边都读过。**
  ③ 🔴 **干净 HEAD 上实测：这工具跑一次就崩** ——
     `python3 -m jsonlstats x.jsonl` ⇒ `内部错误 TypeError: StatResult.__init__() got an unexpected keyword argument 'warnings'`
     ⇒ **QA 那句「代码质量高」是纯静态判词，它没跑过**。
  ⚠️ **所以 no_go 不是「没法验」，是「真的坏」** —— 和清单里那条说的不是一回事。
  📍 复现：`git clone ~/项目/项目仓库/验证-日志统计-20260920c /tmp/x && cd /tmp/x && python3 -m jsonlstats <任意.jsonl>`
  ✅ **09-20 在 `/tmp` 克隆里试修过（不碰项目仓，那是证据）**：给 `types.StatResult`
     补那 3 个字段 ⇒ **38 红 → 19 红（精确消失，其它一条没动）· 31 过 → 50 过 ·
     CLI 从「跑一次就崩」到「出完整报表」**。
  🔴 **真根因（第三版，前两版都错）：架构预言了病、开了药、药没吃 —— 而「药吃了没」没人检查。**
     · 架构 risk 段原文就写了「**契约漂移：并行任务各自定义 Record/StatsResult 字段**」，
       并开药：**T1 必须产 `CONTRACTS.md` 逐字段冻结三个契约**（acceptance 白纸黑字写着）。
     · 实测：**`CONTRACTS.md` 全历史从没存在过**（`pyproject.toml` 也不在主线），
       而 **8 条机器检查里 0 条查它** ⇒ **T1 照样判 `done`**。
     · ⇒ 后面 T2–T6 **全都以为契约冻结过了**；T4 按架构描述里那个 **`StatsResult`（带 s）**
       实现，加了基数那套；主线上的类叫 **`StatResult`（不带 s）** ⇒ 崩。
     ⚠️ **更正我先前写进 §88 的那句**：不是"T1 和 T4 从没一起对过"——
       `git merge-base --is-ancestor` 证实 **T1 的提交是 T4 的祖先，T4 看得到契约**。
       **不是"看不到契约"，是"没有一份契约值得看"。**
     ✅ **09-21 已落地 `bf571c57`**（用户拍板 A′）：`acceptance` 必须**表态** ——
       `check` 是 `{argv,expect_exit}`（兑现）或 `{text_only_reason}`（诚实，放行）；
       **两样都没有 = 漏 ⇒ 归入致命档**，架构校验当场拦下。
       ⚠️ 用户的**原版 A**（写不出命令就不许建任务）我核出会**杀掉一个在量的数** +
       **逼出假命令**，改成 A′（**拒收"没表态"，不拒收"判不出来"**），用户拍板「按你的」。
       13 条测试 + **变异两次各咬中一条** · `make check` 28/0。
     ✅ **09-21 真机验过（`round-20260921b`）：模型照新 schema 写。**
       7 个任务 14 条验收 = **13 条给了能跑的命令 + 1 条诚实说"验不了" + 0 条没表态**；
       `blockers: 0`、13 条约束**全部机器可跑**、需求覆盖 6/6。
       ⇒ **"模型不照写 ⇒ 每个项目卡 GATE2"这个担心不成立。**
     🔴 **但同一轮当场揭穿：那道门本身是装饰** —— 实测「散文验收」走完那条路
       `blockers = []`、`fatal = []`，GATE2 照过。根因是 `_run_planning` 里一行
       **内联子串匹配**（`"缺少"/"无效"/"应为"`）把新规则那句「acceptance 没表态」滤掉了
       —— **13 条测试全绿，因为中间那一行没人测**（函数对 ≠ 接线通）。
       ✅ **已修 `2e15f049`**：抽 `split_arch_issues()`，判据是**唯一一个前缀常量**，
         新规则**默认落进"要审"那一边**（漏配代价是"多审一条"，不是"静默失效"）。
         补了**端到端接线测试**（串起三个函数）+ 变异验过。
       ✅ **09-21 验掉了（`round-20260921c` 到 GATE2 那一步）：那道门是真的。**
        **真代码 + 真历史数据**：拿 c 轮那份真架构（12 条 acceptance 是**字符串** = 散文验收）
        喂进现在这条路 ⇒ **12 条全报「acceptance 没表态」⇒ 12 条全进致命档**
        ⇒ `confirm_gate(GATE2,"approved")` 返回 `None` ⇒ 放行请求拿 **HTTP 409**
        「架构校验未通过，不能放行」。
        **对照组**：同样这份数据喂**修之前那行内联过滤**（`"缺少"/"无效"/"应为"`）
        ⇒ **0 条**进分类器 ⇒ 致命 0 —— 门是装饰，数字对得上。
        ⚠️ **只验到"会拦"**：走的是真代码+真数据，**没有真点一次那个按钮**
        （"点不动"那一下由 `test_silent_failure_invariants` 那两条覆盖）。
        🔵 **本轮的另一个发现：这道门在"模型照写"时永远考不到** ——
        本轮 26/26 验收全给了能跑的命令、上轮 14 条里 13 条给了 ⇒ `validation_issues: 0`。
        **所以它的验收只能靠"喂历史架构"这种办法，不能靠"再跑一轮等着它犯错"。**
  ✅ **②`max_turns` 量完了：不是病根，别动它**（09-20，只读）—— 按 dispatch 侧车数：
     实现任务 T1–T6 = **2·2·6·2·2·1 次**（全 done）；**只有 T7 一条撞了上限**，而它
     **连侧车都没有**（=零产出，属「455 秒零产出」那条族）。T9/T10 只用了 3 次就交了测试。
     ⇒ **放宽只会让零产出的任务多烧 token**（三轮 token 已在涨：26k → 710k → 1.17M）。
  ✅ **③体检脚本已落地**：`scripts/pending_refs_probe.py <project_id>`（或 `--last`）——
     **只读**，在临时克隆里把 pending ref 合并 + 跑测试。已实测跑通（复现 38 红/31 过），
     项目仓当场核过 **git status 干净**。
  ✅ **④ 见上面那条**（1 真 bug + 18 测试半成品）。
  ✅ **形状已进 `docs/防御模式.md` §88** ——「契约冻结和实现在两个 worktree 里跑，从没对过」。
     ⚠️ §88 的边界写明了：**只核了 c 轮一个项目**，b 轮那 7 条 ref 里是什么**没查**。
  🔵 诱因：**b/c 两轮都在「写测试」这半边翻车，而它是唯一能抓到实现 bug 的尺子** ——
     尺子每轮被扔 ⇒ 每轮重新发现同一个 bug ⇒ **这才是「看不到头」的另一半**。
- [奇点] 🔴 **用户 09-21 提「验证一轮要好几小时，效率太低」—— 已量出四分之三是在等人**
  **c 轮实测时间线**（项目 `1789965398548`，从 lineage 读的）：
  `12:36:38 立项` → `12:55:48 批gate1`（**等了 18 分钟**）→ `13:21:46 架构出完`
  → 🔴 **`18:06:30 批gate2`（在这里躺了 4 小时 45 分）** → `19:00:40 执行完`（54 分）
  → `19:03–19:20 自动返工`（17 分）。
  **机器干活 ≈ 1h40m**（调研 1 分 + 规划 26 分 + 执行 71 分 + 集成 3 分）·
  **等人 ≈ 5h**（18 分 + 4h45m）· 墙钟 6h44m ⇒ **75% 是等人**。
  ⇒ **"验证慢"不是机器慢，是门是同步的。** 四条可压的，按性价比排：
  · **① 不等人（5h → 接近 0）** —— 开跑前把门点完再走开 / `auto_mode`。**不花钱，收益最大。**
  · **② 并发 2 → 4**（执行层 54 分 → 约 27 分）—— ⚠️ **同时治"排队等死"**：任务的 900 秒
    有效期**从排队那刻起算**（昨晚 3 个任务一次调用都没发起就被判失败）。
    **总 token 量未必增加**（活就那么多）。🔵 **未验证是否真的等效，是推断。**
  · **③ 规划那 26 分钟** —— 同一道题跑了五轮、每轮重出架构（三方融合，b 轮实测 1500 秒）。
    **能不能复用上一轮架构直接进执行层？** 未核可行性。
  · 🔴 **④ 最根本：不是所有验证都值得跑一整轮** —— 单测/变异（秒级）· 本机小规模端到端
    （起后端 + 小项目 + 短 deadline，几分钟）· 完整真机轮（小时级，只该用在验收线 ③）。
    **现状是"几分钟级能验的东西被攒着等小时级那一轮"** ⇒ 这才是"验证跟不上修"的来源。
  ✅ **09-21 已拍板（用户）**：① 用**"门我守着点"**（开跑后人在场把 GATE1/GATE2 点完再走，
    5h → 几分钟；**不用 `auto_mode`** —— 那会让 GATE2 那道架构门自动通过，而它昨天
    刚被验过"真会拦下散文验收"）· ② **并发 2→4 已落地 `0fded75d`**（顺带治排队等死）。
  ⏳ ③「规划复用架构」**不做**（未核可行性）· ④「分级验证」**是我的习惯问题**，
    不用拍板 —— 今天这轮就是照它做的（240 秒那条 = 改码 + 单测 + 变异，**没起后端**）。

- [奇点] 🔴 **贵的审查跑在「QA 判失败」**之前** —— 用户 09-21 问「失败也审查，意义在哪」（已核代码）
  ⚠️ **09-21 用户更正了重点**：这条**不是**烧钱的大头 —— 大头是**开发**（见顶栏止血那条）。
  留着是因为"审查没记账"那个洞是真的（已补），但**别把它当省钱的主战场**。
  **核到的事实**（`_review.run_post_exec_checks` + `_exec.py` 的调用点）：
  · 整个审查外面套的是 `if changed:` ⇒ **零产出的失败任务不审查**（这条是对的，没浪费）。
  · 里面**每一道花钱的检查**（项目测试 / 多模型审查 / QA 约束验收 / 安全审计）都带
    `validation.action == "pass" and changed and not _trivial` 这道闸门
    ⇒ **门已经判 retry/abort 的，全跳过**（注释写明"这一轮的结果马上会被重试覆盖，
    再对同一版代码花 LLM 钱不划算"）。**设计意图是对的。**
  · 🔴 **但那道闸门量的是「门（validate）判了没」，不是「这一版代码最后会不会被采用」** ——
    而 QA 门禁（`supervisor`）是**在审查之后**才判的（`_exec.py` 里 "── QA 门禁" 那段
    在调用点下面）。⇒ **门判 pass、审查全跑完、然后 QA 判 fail** 这条路上，
    审查的钱已经花掉了，而那一版代码根本不会合并。
  ✅ **09-21 去量了，量出来的答案是「量不出来」—— 而顺着挖出了更重的：审查的钱压根没记账。**
    · 分诊账 346 条**没有 task_id** · 侧车只记执行器自己的调用 · 任务文件的 `changed_files`
      **失败时恒 0**（§79② 已知没治）⇒ 三本账**没有一本能把审查归到任务上**。
    · 🔴 **真因比"归不上任务"重**：`dispatch()` 自己**不记账**，记账的一直是调用方
      （执行层 `_task_runner`、规划层 `workflow._record_phase_usage`），而**审查这四路
      一个都没记** ⇒ 审查花的钱**从来没进过账**。⇒ **上面那个"十天 ≈ $11.36"是低估。**
    ✅ **已补 `db1e4352`**：`validator._review_dispatch` —— 四处审查调用全走它，
      记成 `level="review"` + 真实模型名（5 条测试 · 三次变异各咬中 · 全量 1976 绿）。
      ⚠️ **守卫当场逮到我一次**：第一版又在里面套了个 `except: pass`，被
      `test_no_silent_except` 点名 —— 这条守卫是**活的**，别想绕。
    ⏳ **还差**：**不带 project_id/task_id** ⇒ 现在只能答"审查一共花了多少"，
      **还答不了"哪一轮/哪个任务的审查浪费了"**（那才是能不能改顺序的依据）。
      要答得把这几个函数和调用方都接上 id —— **下一轮跑完先看 `level="review"` 有多大**，
      小就不值得接。**别在没数之前动执行主流程。**

- [奇点] 🟠 **「观察者 + 前端需要优化」—— 观察者那半边已动，前端没动**
  **用户 09-21 原话**：「还有需要优化观察者，前端」；我追问现象时他说
  「**我不想看，修了吧**」⇒ 我按最接近他整晚关切（烧钱/停不住）的那条动了手。
  ✅ **观察者：`/api/loop/stop` 停不住 —— 已修 `e2aad3b3`**（细节见下面「停车」那节）。
     ⚠️ **这条严格说是"修 bug"，不是"优化"** —— 如果他本来想的是别的，
     这条也不算白做（"停不住、只能 kill -9"是真缺陷，仓内文档早记着）。
  ⏳ **前端：一个字没动。** ⚠️ **09-20 用户已拍板"样式调优整体推迟"**
     （记在 `~/OPEN-别动.md`，还写着"别再提把图调好看点"）
     ⇒ **若他要动的是样式，那是推翻自己拍过的板**，得先问清；
     若是功能（有东西看不见/点不动），**还需要他给一句现象**。
     ⇒ **别自己猜着开工** —— 这条等他说。

- [奇点] 🟡 **删任务会把「成绩单」一起删掉**（09-21 当场发现）
  `delivery_facts.py --rounds` 按**任务文件**算"按类"那一栏，而 09-21 清前四轮时把
  36 个任务文件删了 ⇒ **前四轮现在读出来是 `任务 0 个`**，只能靠冻结的
  `docs/轮次成绩单-20260921.md` 看原始读数。
  ⚠️ **锚是换桩活下来了、成绩单没有** —— 两件事被当成一件做了。
  ⇒ **以后删某轮任务前，先跑一次 `--rounds` 把那行的按类结果冻进 docs/**（一行命令的事）；
  或者让 `delivery_facts` 认 `salvaged/` 里的锚反推。**未定做哪个。**

- [奇点] 🟠 **「稳定产出」的判据 —— ✅ 09-21 已拍板（终点线 = ③ 端到端跑通），具体见下**
  ⚠️ **本条的标题原来写的是"还没有判据"** —— 那是 09-20 的状态，**已过期**。
  现在只剩一件事没闭环：**验收清单还没经过用户过目**（`docs/验收-端到端-20260921.md`）。
  🔵 **历史读数留着当背景**（**注意：前四轮的任务文件 09-21 已删，`--rounds` 读不到它们了**，
  原始读数冻结在 `docs/轮次成绩单-20260921.md`）：
  **已有的尺子**：`scripts/delivery_facts.py --rounds 6`（**只摆事实、不替你选尺**，已跑通）。
  **四轮摆一起**（09-21 补第 4 点）：`0/8（0%）·26k` → `2/9（22%）·710k` → `7/12（58%）·1.17M`
  → **`4/7（57%）·814k`**。🔵 **外派要的「单轮 token 下降」到第四轮第一次成立（−30%）**，
  且「没进仓」那栏第一次是 0。⚠️ **但分母不同（7 vs 12）** —— 别把 57% 读成"和 c 轮一样好"，
  **绝对交付是 4 个任务 vs 7 个；要真比得让题目一样大。**
  🔴🔴 **09-21 已拍板：终点线 = ③「端到端跑通一个项目」**（用户选 3）。
    **定义（四条全中才算，缺一条不算）**：
    ① 项目 `phase` 走到 **`done`** —— 不是 `gate2` 兜底、**不是我手工推进的**；
    ② 里出一个 **`release/<id>-<日期>` tag**（或等价交付包）；
    ③ **按「开跑之前就钉好」的验收清单跑一遍全过** —— 清单必须**先写下来**，
       否则就是跑完再找理由（这个仓栽过：拿后果当证据）；
    ④ **除"点批准"外零人工介入** —— GATE1/2/3 允许你点**批准**（`auto_mode=False` 下
       那是设计内的动作，09-17 那轮就是这么跑成的）。**不允许**：手工建任务、手改架构、
       手动打回、手工推 phase、手工合并。⚠️ 我第一版写的是"不点 gate"—— **那样连
       09-17 都不算数**，是一句自相矛盾的话。
    ⏭️ **连几轮**：先**做到 1 次**（从 0 到 1），再把"连 3 次"当稳定线。
    🔵 **QA 判词不算在内** —— 它是被证明会骗人的那把尺（c 轮跑一次就崩、QA 说"代码质量高"）。
    🔴 **这条线不是新高度，是"回到曾经到过的地方"**：**09-17 已经走到过一次** ——
       同题目（JSONL 日志统计 CLI）、同规模（5 模块 / 11 任务）、`phase=done`、
       tag `release/1789571713224-202609170202`，用户当时从 tag 导出亲手跑过产物。
       **之后十几轮再没走到过**，而中间加了那一堆门禁/校验/QA 层
       ⇒ 真正的问题变成：**那些层是把它做稳了，还是做死了。**
  ⇒ 定了之后，"看不到头"变成"离那条线还差多少"。**没定之前，每轮结束都只是"还没完成"。**
  🔴 **09-21 按「任务类别」重排了四轮 —— 总分被最容易的那一栏稀释了**（用户提的"要区分任务体量"，我核完同意）：
    按**标题**分类（不含正文，否则"与 tests/test_x.py"会被误判成测试类）：
    | 轮 | 实现模块 | 接线/集成 | 写测试 | 验收 | 总 |
    |---|---|---|---|---|---|
    | a 09-20 | 0/4 | 0/1 | 0/2 | 0/1 | 0/8 |
    | b 09-20b | 1/6 | 0/1 | 0/1 | 0/1 | 1/9 |
    | c 09-20c | **5/5** | **1/1** | 1/5 | 0/1 | 7/12 |
    | d 09-21b | **4/4** | 0/1 | 0/1 | **1/1** | 5/7 |
    ⇒ **「实现模块」从 0% 修到 100%（这是真进展）**，而它每轮都是**最大那一栏** ⇒
      `57% / 58%` 这两个总数**基本只是"实现模块过了没"**。
    🔴 **难的那两栏（接线+写测试）四轮合计 2/12（17%），基本原地** —— 这才是"看不到头"的来源。
    🔵 **顺手否掉一个假设**：不是"活大就死" —— d 轮按**工具事件数**排，最大的 T1(26)/T3(24) **全过**，
      死的是中等的 T5(12)/T6(14)。**分界线是"自包含 vs 要读别人的产物"，不是体量。**
    ⚠️ 样本小（难任务四轮才 12 个）、且**分类是我人工按标题分的**；"要读别人产物"那句是**推测**。
  ✅ **成绩单已按类分栏**（`5d48a8dc` + `46277a47`）：`delivery_facts.py --rounds` 每轮多一行
    「按类（过=任务 done）」，表头写明**它和「进仓」不是同一把尺**（只读任务无产物、进仓恒 0）、**别相加**。
    ⚠️ **第一版的判据是空转的**：变异（分类器改回判正文）**没红** —— 喂的正文是 ASCII 的 `tests/test_filter.py`，
    而分类器看的是中文「测试」二字。换成真机那段约束行的长相后变异才红。
    🔵 顺带核：**正文判与标题判在 62 个真实任务里 33 个不一样** ⇒ "只读标题"这个选择是对的，不是随手定的。
- [奇点] 🟡 **前缀缓存：先量一个数再动**（09-20；评估全文在 `docs/天枢对照.md`）
  天枢拿缓存当架构级承诺（命中 95–99%，DeepSeek 未命中按至多 **50 倍**计费），但**它的前提奇点没有**：
  天枢是 append-only 长会话，奇点**每个 turn 重开请求**（`_build_effective_task` 在 while 循环体内
  逐轮重建、executor 也逐轮新建）⇒ 能缓存的只有 `[system][tools]` ≈ **2232 字符**；
  且 `dispatch()` fallback 链**最多换 3 个模型**、`_premium_first` retry≥2 强制切 premium
  ⇒ **多模型阵容和前缀缓存天然打架**，是"先选"不是"补一层"。
  ✅ **09-21 量完了（用户要的那个数 + 顺带的两处更正，都只读、没花钱）**：
  · **切换占比 ≈ 0** —— 但**不是从"按次落盘"量的**（那里根本没有"模型"这一列：侧车只存**首次**模型
    `cur_model=_prev.get("model") or model`、账本按天折叠、分诊账没有 `task_id`）。
    真正查得动的是**切模型唯一那个出口**会 `witness.warn("exec", "fallback: A→B")` 落 `alerts.jsonl`
    ⇒ **4207 条告警里 0 条**。⚠️ **这证据有个洞**：`executing` 只配一家时那段对实现任务**是死代码**，
    "0 条"分不清**"没切过"**和**"切不了"**；现在是 2 家了，**下一轮才有真判据**。
  · ✅ **复核了 `[system][tools]` = 2232 字符**（`SYSTEM_PROMPT` 1033 + `TOOLS` json 1199，4 个工具）——
    和本条原来那个数一字不差。
  · 🔴 **但"为什么只有 2232"那句推理要改**：不是"每个 turn 重开请求"这么笼统，
    真因是 `_exec_context._construct_context` 是个**滑动窗口**（保留最近 N 条工具事件、更早的做摘要）
    ⇒ **每轮 user 消息的开头就在变**，前缀断在 user 消息第一个字符。
  · 🔵 **量级**（分诊账 275 条）：2232 字符占**中位 prompt 的 12.2%**、占最大那个的 1.2%。
    ⚠️ 它**只记慢调用** ⇒ 偏向大 prompt，**不能当全体**；且这是"字符占比"，**折成钱还差一个**——
    命中价/未命中价的倍数**我没核**（天枢那句"至多 50 倍"是别人产品的话，别往 DeepSeek 头上安）。
  ⇒ **结论待拍板**：观测到的切换 ≈ 0 ⇒ 原句「**多模型阵容和前缀缓存天生打架**」**被数据否掉**；
    真障碍是**消息结构**（要吃到缓存得改成真·多轮累积，那是架构改动）。**不做也行**，别当它是便宜的补丁。
- [奇点] 🟡 **外派评审 09-19 的 A1–A10 + B1–B4 全改完了 —— 剩下的两条 09-21 都定了**
  ✅ 已落地清单 + 每条落在哪个函数：`docs/外派评审-20260919.md` 顶部的「处理结果」表。
  ⚠️ 本行标题原写「还剩两条没做」，**已过期**：B5 拍板不做 / B6 已改完（只差真机验）。
  🔴🔴 **09-21 补：比"不精准"更根本的问题 —— 重跑压根不带信息**（用户问「重跑的意义在哪里」时核的）。
    `_build_effective_task` 从 `task.description` 起头，打回时**只写了两处**：lineage 里一条 +
    阶段理由那行字。而任务盘上那条 `error`（"QA:fail: 无文件改动…"）**全仓没有任何代码
    把它读进提示词**（grep 过，只有执行器自己的报错用了 `error` 字段）。
    `retry_count` 也不动（T5 打回后仍是 0）。
    ⇒ **被重置的那批任务拿到的输入跟第一次一字不差** —— 唯一变量是模型的随机性。
    🔴🔴 **2026-09-21 更正：我上面这句写宽了 —— 只对"项目级回炉"，不对"任务级重试"。**
      任务级的 cascade 重试**是带反馈的**，而且真进了提示词：`_dispatch_exec.py` 三处
      （`base = task if not feedback else f"{task}\n\n---\n[上一轮校验反馈]\n{feedback}"`）。
      **两套机制我混成一套说了** —— 判据：看 `feedback` 是从哪条路传进去的，
      而不是看"是不是重跑"。同族教训见 [[test-wiring-not-just-functions]]。
    ⚠️ **本条的结论只在"项目级回炉"上成立**，别拿它去否任务级重试。
    **这不是"修"，是再摇一次骰子。** 而 impl 那条路是**全量重置**，QA 报告里
    那些标了 `task_id` 的 issue **一条也没变成"要做什么"进到重做的任务里**。
    🔵 **当场看着它在原地重演**：返工后 T1 跑了 6 次（19:03→19:12 完成）、T2 在跑，
    而 **T3/T4/T5 状态是 `running` 却返工后 0 次派发**（提交了、没轮到）、T6–T11 还 blocked
    —— 和 18:06 那轮**同一个形状**（2 个位子、前后排差 9 分钟）。
    ⚠️ **而且那个"没轮到就回队"的补丁这轮根本没生效**（19:1x 提交，跑着的后端 12:36 起的、无热重载）⇒ **跑了也是白跑**。
    ✅ **09-21 已落地 `48b8920a`：自动返工改成默认关**（要开得显式设 `QIDIAN_AUTO_REWORK=1`）。
    理由就是上面那条 —— 一次不带信息的全量重跑，代价是**一次完整执行层**，买到的是"再抽一次签"。
    ⏳ 真机没验（判据：不设这个变量时，审查触顶**不回炉**、走 GATE2 交人）。
  ① **B5「只重做有问题的 task」—— ✅ 09-21 用户拍板：现在不做**（理由见 `~/OPEN-别动.md`）。
  ② **B6：`merge.py`** —— ✅ 09-19 通读完、落地三处（`_park` 落盘失败原来静默吞 ·
  `resolve(abort)` 删盘失败裸 `pass` · `_mark_merged` 不看 `transition` 返回值）。
  三条各一个测试、变异验过。⚠️ 动到了调度推进块（`orchestrator`），**真机没验**。
- [奇点] 🟡 **架构是"整份 blob"，改一处 = 全部任务作废重建**（09-19 外派第三轮提，我逐行核过成立）
  `_workflow_phases._run_planning` 结尾**无条件** `project.task_ids = []`；`project.confirm_gate` 的
  GATE2 打回分支和 `architecture_redo` 都 `architecture = None` ⇒ 改架构里一个字段，
  连已 DONE、已合并进 git 的任务一起作废。提法：拆 `contracts`(接口+文件所有权+DAG) /
  `verification`(约束+check) / `scope`(covers) 三段各自带版本，`_run_planning` 按段幂等更新。
  ⚠️ **改前先看** `orchestrator._decompose_and_create_tasks` 那条 `if not proj.task_ids:` 守卫 ——
  **清空正是让重建触发的东西**，只把清空那句改成"变了才清"会把 09-17「10 秒冲过执行层」放回去。
  ⚠️ 大改、零真机数据，**未定做不做**。
- [奇点] 🔴 **接手指针** —— 先看这条。
  **起点**：09-17 起跑了好几轮真机（同一题目：JSONL 日志统计 CLI）。
  **逐条判据 + "怎么验的"全在 `docs/待验证清单-20260912.md`，别在这儿抄数。**
  **五轮成绩单的原始读数**（前四轮的任务文件已删，`--rounds` 读不到它们了）
  → 冻结在 `docs/轮次成绩单-20260921.md`。
  ✅ **09-21 两轮验掉一批**（`round-20260921b` + `round-20260921c`）：
    · **A 档 / B 档**（09-20 做完、挂了一天没验）—— **四条判据全中**，见下面「最小闭环」那条；
    · **只读任务**（`76dc2592`）· **机检归因**（`missing_inputs`）—— 上轮验到；
    · **验收门**（`2e15f049`）—— 拿 c 轮真架构走真代码验到（**不是**真点按钮），见上面那条；
    · **前端两处** —— 09-20 真机看界面验过。
  ⏳ **仍然没验的（这条要留给你的就是这些）**：
  · 🔴 **09-21 新落地的一批**，一处都没真机跑过（**下一轮就是验这批**）：
    · **`5ae8cdb6`**（没轮到 ⇒ 回 PENDING 重排）—— 判据：有任务被判「未轮到→PENDING」
      且**下一个窗口它真跑起来了**；反例：到上限后不再转。
    · **`f7fdf49e`**（`POST /api/projects/<id>/stop`）—— 判据：真点一次它，**看钱停没停**。
    · **`2e15f049` 的"点不动"**那一跳 —— 我用真数据验到"会拦"，**没真点一次那按钮**。
    · 🔴 **`60a81e16`**（240 硬顶去掉）—— 判据：**"想满 240 秒零产出"那种死法不再出现**；
      🔵 顺带取数：正常调用到底想多久（定 ③ 的阈值）· `llm_spin_no_output` 有多大。
      ⚠️ 反例要一起看：**打转的调用现在能烧到预算见底**（代价没量）。
    · **`0fded75d`**（并发 2→4）—— 判据：启动日志那行 `调度循环已自动启动 (concurrent=4)`；
      再看"排队等死"那族少没少（`5ae8cdb6` 的判据同向）。
    · **`db1e4352`**（审查记账）—— 判据：账上出现 `level="review"` 的行，**看它有多大**
      （小就不值得再给审查接 task_id）。
    · **`48b8920a`**（自动返工默认关）—— 判据：不设 `QIDIAN_AUTO_REWORK` 时审查触顶**不回炉**。
  · **09-20 换桩**（`task_delete` 不再剪绳，改挂 `refs/qidian/salvaged/`）
    —— 🔵 **09-21 顺带验到了**（删前四轮 36 个任务，16 条锚全部换桩、产物没丢）：
    `salvaged/` 里现在躺着 16 条、`.qidian/salvaged.jsonl` 13 行。
    判据里还剩一条没验：`delivery_facts --refs` 的 ⑦ 节把它们印出来。
  · **观察者那个覆盖** —— 四次都没撞上竞态，判据仍未验。
  · **09-18 晚那 4 条** —— `164429f` · `4e3718f` · `d07414b` · **`aecb82f` 空壳不许顶掉真结果**。
    🔴 「产物被丢」正是 `aecb82f` 的形状 —— **修过一次，没修干净**（09-20 又犯一次，§86）。
    🔵 09-21 那轮 T3 写了 4 个文件，最后一次调用零产出 ⇒ 判失败不合并 ⇒
      **锚好好地留着**（`refs/qidian/pending/1789985192392`）⇒ 这一族**没再犯**。
  · `c9e9796c`（启动回收先兑现"停"标记）—— 判据：下轮开跑前盘上若有该停的任务，
    它进 `FAILED` 而不是 `PENDING`；顺带看 `cancels/` 里有没有该被取走的留下。
  · `62897c77`（流式补 `reasoning_content: ""`）—— 09-20/09-21 又跑了四轮、流式带工具调用的
    调用**跑过很多次**，但**那个 400 形状有没有被撞上、这条补丁有没有真救到一次，没查**
    ⇒ **仍然不算验过**（"跑过很多次"≠"验过那一条"）。
  · **09-19 那 8 处** —— ✅ 只有 `68664faa` 走到了；其余 7 处**一次都没触发**
    —— ⚠️ **"没冒"是"没走到"，不是"修对了"**。
  · `3ed1b1fa`（任务页「上一轮」标签）· `ded50bd0`（告警文案三段 + 分钟数是 20）—— **没看**。
  ✅ `de6d2fa8`（认不出来的 400 落盘）**已经起作用了** —— 09-19 那次 400 的现场就是它留下的。
  ⚠️ `4e3718f` 之后 `refs/qidian/pending/*` 会**攒得比以前多**（**有意**：超时/冲突/异常都留锚）
  —— 和「清理孤儿 pending ref」那条 🔴 是**同一个结**。

- [奇点] ✅ **`round-20260921b` 跑到 GATE2 了**（09-21，验收门那条的答案见上面）
  **三方融合真跑通**：`agent: fusion(glm-5.3-flash,deepseek-flash,deepseek-v4-pro)`，
  规划耗时 **1500 秒**、约 **16 万 token**。架构：7 任务（含 1 个 `[只读]` 独立验收）·
  13 约束**全部机器可跑**（c 轮是 8/9）· 风险 6 条 · 需求覆盖 6/6。
  `committee_not_engaged` **一条没报** ⇒ `fd3d4189` 那个假红修复**真机生效**。
  🔵 对比 b/c 轮：那是**第一次**产出"验收真的能机器判"的架构（13/13 vs 8/9）。
  ✅ **GATE2 已批，执行层也跑完了**（用户拍板批的）。
  🔴 **「零产出族」原封不动地又来了**：5 done / 2 failed，死的那两条是
  **T5 cli 入口**和 **T6 端到端集成**（都是大活）—— 工具轮 12/14 次后**被掐断**、
  `改动 0 个文件`。和 c 轮的 T7/T8 同一个死法。项目最后**又停在 GATE2 兜底**
  （审查自动修触顶 2 轮），**没走到 GATE3**。
  ✅ **一处归因在起作用**：失败判词写的是「`[completeness]` 无文件改动 ——
  **这次是被我方掐断的（deadline）**，不是空手回来的偷懒」
  —— 正是「判据错位审计」要的那样（把"这次是我们断的"贯通到判据）。
  `tests_ran: true`（c 轮是 false）· `our_side: 0`（真失败）。
  📊 **四轮摆一起**（`delivery_facts.py --rounds 4`）：
  `0/8 (0%) · 26k` → `2/9 (22%) · 710k` → `7/12 (58%) · 1.17M` → **`4/7 (57%) · 814k`**
  🔵 **外派要的「单轮 token 下降」到第四轮第一次成立**（−30%），
  且**「没进仓」那栏第一次是 0**（c 轮有 3 个任务没进仓、+1281 行打水漂）。
  ⚠️ **但分母不同**（7 vs 12）—— 别把 57% 读成"和 c 轮一样好"，
  绝对交付是 4 个任务 vs 7 个。**要真比，得让题目一样大。**
  🔵 **顺带修掉一个工具**：`--rounds` 读到一个新旁挂文件（`<id>.e2e_checklist.json`）
  当场崩 —— 黑名单漏项。已改白名单 `6ad87236`（变异复现了真机那个 ValueError）。
  🔵 **顺带修掉一条"本机绿盖住的红"**：全量 `pytest tests/` 里
  `test_no_undefined_names::test_module_attributes_exist` **本来就是红的**
  （同文件 `_mc` 别名到两个模块 ⇒ 守卫张冠李戴），而 `make check` 的 `test-fast`
  **只跑 4 个文件、看不见它**。已修 `2e15f049`，全量 **1913 passed**。
- [奇点] 🔴 **`round-20260921`（第一次那轮）**：**我把它杀了**，不是它卡死 —— 三份初稿全丢
  **事实链**：三席初稿跑完（glm 31,798 · deepseek-flash 28,308 · v4-pro 27,749）→ 融合那步
  （提取/定稿）是真在跑的长调用（第 4 笔 fusion 记账 00:30:12 · glm · 30,668 token）——
  **我在 00:30:5x 用 lldb 第二次注入 `PyGILState_Ensure()` 把进程 abort 了**
  （`Fatal Python error: _PyThreadState_Attach: non-NULL old thread state`）。
  距它自己的 `_FUSION_CALL_CAP`（600 秒，从 00:21:39 起算）**还差 49 秒**。
  ⇒ **"融合卡死/90 秒停滞超时失效"这个结论我撤回**，理由见下（两条都是我自己踩的）。
  🔵 **撤回依据**（都是离线可查的，没花真机的钱）：
    ① **"没有生成中事件 ⇒ 没吐字"是无效推理** —— `生成中` 只有执行器 `openai_agent` 会发，
       `execution_judge` **压根不发**（`grep 生成中` 全仓只命中那一处）。我从不该存在的信号里读了结论。
    ② **停滞超时是好的**：本机起个"发一个 chunk 就永久静默"的 SSE 服务器，用**生产那行
       client 配置**去打 ⇒ `ReadTimeout` 5 秒准点触发（临时把 read 调成 5s 好快出结论）。
       所以"9 分钟没超时"只可能意味着**字节在来**，不是保护死了。
    ③ 账本里正常调用能跑到 **948s / 942s / 906s** ⇒ 551 秒**不异常**。
  🔴 **仍然成立的两件事**：
    · **贵产物不落盘**：三份初稿（88k token）**只在内存**，融合没跑完就没有任何产物 ——
      进程一死全没。同族是 `aecb82f`「空壳不许顶掉真结果」那条。
    · **委员会假红**（已修 `fd3d4189`）：`committee_not_engaged` 报的是 `len(lineup)` =
      **层级数（恒为 1）**，不是席位数 ⇒ **配了三家也被报成"委员会没开"**。⇒ **看到那行别信。**
  ⚠️ **教训（我这轮栽了两次，同族）**：**先给结论、再去找证据 —— 两次都是拿"看起来像"当"是"。**
    第一次读 C 层栈说"死锁"，第二次读一个根本不存在的信号说"停滞"。**两次都是撤回。**
- [奇点] 🔴🔴 **`round-20260921c` 当场揭穿一个更大的东西：后半个队列是「排队等死的」，不是"做不出来"**
  **实测（不是推测的部分）**：11 个任务**同一秒创建**（18:06:32）；T1 独占跑到 18:20:03 才失败；
  T2/T3 紧接着在 **18:20:06 同时开跑**（就是 T1 让出的那两个位子）；
  而 **T3/T4/T5/T6/T8 在 18:33:35–37 三秒内一起终结** —— **一把刀，不是各自的死线**。
  其中：T2 赶在刀前 49 秒完成（done）；T3 卡在刀口上；**T4 只抢到 22.5 秒预算**（一次调用没产出）；
  ⚠️ **更正（09-21 深夜，重新读盘）：从没进过 dispatch 的是 2 个，不是 3 个** ——
  **T5（`…394` 输出模块 render.py）** 和 **T11（`…402` [只读] 独立验收）**。
  原文写的 `T5/T6/T8` **和记忆里写的 `T5/T8/T11` 都不对**（两份错得还不一样）。
  🔴 **判据（三个信号里只有一条站得住）**：
  · ❌ **心跳** —— 8 个**确定派发过**的任务（含跑完的 T1、64.6 万 token）**都没有心跳**；
    有心跳的只有"杀进程那一刻还在跑"的 2 个 ⇒ **它量的是"当时在不在跑"，不是"派发过没"**。
  · ❌ **trace 的 `tool_batches` 回合数** —— **每个任务都是 0**，连跑完的 T1 也是
    ⇒ 原文说的「0 回合 0 工具」**量的东西根本不存在**（同族：判据测的是声明不是实际）。
  · ✅ **侧车**（`partial_usage/<id>.json`，`_mark_dispatch_started` 在派发开头写）——
    只有这一条分得开；而"重试会删侧车"那条假阴性路**不成立**：**11 个任务 `retry_count` 全是 0**。
  ⇒ **数字改了，结论不变**：那把刀确实砍在**没轮到**的任务上。
  **机制**（读出来的）：`orchestrator` 在 **submit 前**算 `deadline_at = now + 900`
  （注释写明"排队那段也算在同一把尺上"），`_reap_futures` 按 `now - submitted_at > 900` 收割。
  ⇒ **并发只有 2 + 任务互相依赖 ⇒ 排在后面的任务，有效期在排队时就被等没了**。
  🔵 **这解释了"难的那一栏全灭"的一部分**：它们可能不是难，是**没轮到就被砍了**。
  ⚠️ **反例也要记**：名单上的旧结论「排队够久就上场即死」09-17 实测"机制成立、后果不成立"——
  **这次后果成立了**。别拿旧结论当"不会发生"。
  ✅ **09-21 已修 `5ae8cdb6`（用户拍板：回 PENDING 重排 + 带上限）**：
    `_task_runner.requeue_if_never_dispatched` —— 判据用现成的 `read_partial_started_at`
    （它就是为"压根没发起过"造的，**不新加字段**），次数用现成的 `retry_count`/`max_retries`。
    **两处接线**：① `_task_runner` 的 QA 终态判定（主路，T5/T8/T11 走的就是它）；
    ② `orchestrator._reap_futures` 的收割超时支（那条**绕过了 QA**，且必须挡在
    "写 `by:timeout` 标记"之前 —— 给没起来的任务留超时标记，下轮重排上来会被
    `_check_cancelled` 读成"用户取消了"）。
    **7 条测试 + 5 次变异各咬中一条**（删调用点 / 不区分"派发过" / 去上限 /
    去收割挡板 / 探测失败反过来放行）；顺带修了 5 条老测试的夹具
    （它们的假任务从来就没派发过，而它们讲的是"跑过的任务"）。
    ⚠️ **真机没验** —— 要等下一轮（判据：有任务被判"未轮到→PENDING"且下一窗真跑起来了）。
- [奇点] 🔴🔴 **用户 09-21 深夜：**「限制模型思考从根本上就是错的」** —— 他对，代码里确实有这回事**
  **用户原话**：「把思考和真实输出分开……现在的模型通病就是思考慢……**限制模型思考从根本上
  就是错的，模型本来就是思考完成输出**」。⚠️ **他说"我之前不是说了吗" —— 我 `grep` 了
  三个清单 + 记忆 + 仓库文档，**0 条**。**是我没登记，不是他说错**。
  🔴 **核出来的事实（代码里有两把尺在量同一次调用）**：
  | 尺子 | 量什么 | 认不认"还在思考" |
  |---|---|---|
  | **停滞** `_STALL_TIMEOUT` 90s | 多久没有**新内容** | ✅ **认** —— 注释原话「思考模型的 `reasoning_content` 也是进展，只认 content 的话会把'正在想'误判成'卡住'」 |
  | **总时长** `_cap` 240s | 跑了多久 | ❌ **不认** —— `if time.time() >= _call_deadline: _over_budget = True` **和进展无关，到点就断** |
  ⇒ **"限制模型思考"指的就是第二把尺**，用户的判断成立。
  🔵 **第二把尺为什么在**（注释里写着，2026-09-15 真机坐实）：`httpx.Timeout(read=)` 只管
  "两次读之间"，服务端只要**一直吐字节**就永不触发 —— 实测**一次调用跑了 27 分钟、流不结束**。
  ⇒ 真问题是**那把尺不区分「一直在思考」和「一直在吐字节但没内容」**。
  ⚠️ **我上一条给他的答复要更正**：我说"它不是没时间说完，是把全部时间用来想" ——
  那句话**把思考当成了浪费**。按代码自己的定义，**思考就是进展**（stall 那把尺认它）。
  真机那几次（思考 2.5万~4.3万字、正文 0）**是在产出进展的，只是没进正文**，
  被**总时长**那把尺掐掉。**我的框架错了。**
  ✅ **09-21 用户拍板 ②，当天落地 `60a81e16`**（`openai_agent.py` 的 `_stream_call`）：
    `_cap = min(240.0, 剩余)` ⇒ `_cap = max(1.0, 剩余)`。**敢去掉的理由是核过代码的** ——
    240 是 09-15「一次调用跑 27 分钟不结束」之后加的，而那个形状（一直吐字节、
    拿不到可用 token）**09-16 起已由停滞那把尺兜住**（`_last_progress` 只在
    content/reasoning/tool_calls **变长**时归零）⇒ 原理由不成立。
    新增一条测试钉"剩余 800s ⇒ 传进去的 timeout 就是 800 上下"（含两个边界）+ **变异咬中**；
    `make check` 29/0 · 全量 **1969 passed**；四处说错了的注释/文档已一起更正
    （`防御模式.md` §79 表①、§79.5 新增、§87.3、§87.6）。
    🔵 **顺带解决**：`error_kind="deadline"` 原来把 (a)「任务预算用完」和 (b)「单次撞 240、
    任务还剩几百秒」混成一个标签，而调用方对 (b) 的处理是错的 —— **去掉 240 后 (b) 不存在了**。
    ⏳ **还差**：真机验（等止血）· **取数定 ③ 的阈值**（看正常调用想多久）。
    ⚠️ **代价没量**：打转的调用现在能烧到预算见底。
  ❌ ① 把 `_cap` 调大（480？）—— 用户没选：**没解决"不区分"**，只是把线挪远。
  ⏳ ③ **按"产出量"设限** —— **还没做，卡在阈值**（现有 2.5万~4.3万字全是"被 240 砍断的
    记录"，不是"正常该想多久"）⇒ **先跑一轮取数**。

- [奇点] 🔴 **「零产出族」查到底了：240 秒的单次上限不够想完，而代码**故意**不重试**（09-21 真机）
  ⚠️ **本条下面的"240 硬顶"已经是过去时** —— **09-21 已去掉 `60a81e16`**（判据见上条
  「240 秒那把尺」）。**现场描述（T5/T6 怎么死的）仍然成立**，别把整条当过期。
  **现场**（`round-20260921b` T5/T6，两条都是大活）：单次派发、工具轮 3–4 轮、
  **工具那 8 秒就干完了**，然后**最后一轮那次调用把 240 秒全用来思考**
  （思考 24,988～43,388 字、**正文 0 字、工具调用 0**）、被掐 ⇒ `agent_output` 长度 **0**。
  T5 只用了 441 秒就停（预算 810 秒没用完），正因为下面这条收尾。
  **代码侧（读出来的，不是猜的）**：`openai_agent.py`
  · `_cap = min(240.0, 任务剩余 ⇒)` —— **单次恒 ≤ 240 秒**；
  · 被掐且正文为空 ⇒ **直接收尾失败、不重试**，理由写在注释里：
    「原样重试 = 同模型同上限 ⇒ 再烧一个 240 秒」+「真机 15 个失败任务没一个败在"做错"，
      全败在"没产出"」。`error_kind="deadline"` 是**刻意**的（别赖模型、别熔断好模型）。
  ⇒ **不是 bug，是配比**：最后一轮 prompt **10.2 万字符**，模型需要的时间**正卡在 240 秒线上**。
  🔵 **我今天在这条上读数错了一次、自己抓回来的**：先拿 `elapsed` 算出"模型慢 10 倍"，
    而 §87.6 写明 **`_cap ≤ 240` ⇒ `elapsed` 里超过 240 的部分不是"流了多久"**（关流耗时）。
    尺子错了，结论就错。
  🔴🔴 **更正（用户 09-21 凌晨追问"实现任务不是单模型吗"后查到，比我先前写的准）**：
    **两种完全不同的情况共用了 `error_kind="deadline"` 这一个标签，而调用方对它们一视同仁。**
    · (a) 任务总预算真的用完 ⇒ `_exec.py` 那支写着「**不换模型**」——**是对的**；
    · (b) **单次 240 秒上限到了、任务还剩几百秒** ⇒ 走的是同一支 ⇒ **不换模型 —— 这是错的**，
      而那段注释给的理由（"换一个只会把剩下的时间再烧一遍"）**在 (b) 下不成立**。
    **T5 实测是 (b)**：810 秒的预算只用了 **441 秒**（还剩约 370 秒），
    却因为单次调用撞了 240 秒上限、正文空 ⇒ 直接收尾。
    ⚠️ **顺带答一个配置问题**：实现任务**现在是单模型**（两道锁：`executing: ["deepseek-flash"]`
    ⇒ `restrict_to_lineup=True` 不追加兜底；`_premium_first` 在 `restricted=True` 时直接返回）。
    **但就算链上有多家也没用** —— (a)/(b) 不分，切模型那一支永远走不到。
    （用户印象里"不是单模型"来自**没配 `executing` 之前**：那时回退全池 3 家，是主+兜底的链。）
  🔵 **量过了：这个数现在量不出来 —— 而"量不出来"就是答案**（09-21 凌晨，用户让量的）
    按 `token_usage.json` 把**每个任务按时间排出的模型序列**拉出来：
    **有记录的任务 28 个，换过模型的 0 个。**
    ⇒ **不是"换了都不成"，是"一次都没换过"** —— 因为 `executing` 只配了一家，
    **链里根本没有第二个人可切**。所以 ① 的第一件事**不是改代码，是放开配置**。
    🔴 **顺带挖出同一族的第三个洞**：`_exec.py` 里那段「**容灾: 切下一个 agent**」
    对实现任务是**死代码**（`fallback_chain` 过滤掉唯一一家后就是空的）——
    而**没有任何地方说这件事**。`_phase_models_warning()` 只报两件：
    提取员在委员会里 / `planning` 只配一家（委员会关闭）。**`executing` 配一家一个字都不说。**
    ⇒ 这个仓**知道**这类"配一家就让某个能力静默失效"的事要出声，**executing 这条漏了**。
    📌 **①②已做完（09-21 凌晨，用户拍板）**：
      ✅ **① 放开 `executing`** —— 经 API 配成 `[deepseek-flash, glm-5.3-flash]`。
         **已核接线**：`pick_agent_fallback_chain` 实际返回 **链长 2**（>1 那段容灾才活）。
         **为什么选 glm 当兜底**：不同厂商（deepseek 整体出问题它不受影响）+ 价目表最低
         （0.25 $/M）+ 今晚刚把思考调 low ⇒ **它是候选里最不容易撞 240 秒那堵墙的**
         （委员会那轮：glm 35.7s / 思考 136 字 vs v4-pro 181s / 思考 4 万字）。
         ⚠️ **副作用要知道**：glm 现在既可能**当写手**又在**审查员池**（`reviewing`）里；
         真轮到它当写手时，"剔除写手"后审查员池可能空 ⇒ **fail-open 回全池 + 报警告**。
         不是坏，但是个新的组合，下轮留意。
      ✅ **② 补了告警 `c30b70fa`，并顺带修掉一个"只报第一条"** `e0f102e6` ——
         原来 `_phase_models_warning()` 一条条 `return`，**同时踩两处时只看得到第一处**，
         而新加的那条**在我正需要它的配置里隐身**（真机当场撞到）。
         改成收集全部再拼（`warning` 是单数键，前端读的就是它）。
         ⚠️ **变异第一次没红，是我判据写歪了**：断言 `"兜底" in warning`，
         可**第一条自己的文案里也有"兜底"** ⇒ 假绿。换独有字串后变异 ⇒ 红。
      ⚠️ **`researching` 仍是 1 家**（告警会报，但没动）—— 调研失败的代价比实现失败小，
         且它本来就短，**等真有需要再说**。
      ⏭️ **③ (a)/(b) 分家**：留着。**下一轮跑完就能第一次量到"换模型到底成不成"** ——
         那个数现在还不存在（不是"换了都不成"，是"一次都没换过"）。
         那个数现在还不存在（不是"换了都不成"，是"一次都没换过"）。
  🔴 **09-21 更正：我说"这轮大概率还是 0 次"是错的 —— 换模型这轮真发生了 2 次。**
    我漏了一条触发路：`_exec` 里切模型有**两条**支，我只核了「容灾」那条
    （`error_kind != deadline` 才走），漏了**「低置信 cascade_skip」**那条
    （`_decide_cascade` 返回 break ⇒ 换链上的下一家）。
    盘上证据：`alerts.jsonl` 19:08:12 / 19:09:27 各一条
    `cascade_skip:deepseek-flash→glm-5.3-flash conf=0.30` —— 方向正是我昨晚配的那条 2 长链。
    ⚠️ **但"切过去之后 glm 接手没有、接手成没成"，我没能从盘上单独认出来**：
    分诊账 19:07~19:13 那几条 glm 的 prompt 只有 7千~2万字符（像是审查调用，
    而 `reviewing` 也配的是 glm）⇒ **认不出哪条是"被切过去当写手"的那次**。
    ⇒ 判据 ② 现在的答案：**这条路能触发（2 次），但效果看不清；error_kind 那条支仍然走不到。**
  ⏳ **要拍板的（三个方向）**：
    ✅ **①「把 (a)/(b) 分开」09-21 作废**：240 硬顶一去掉（`60a81e16`），
       **(b)「单次撞 240、任务还剩几百秒」这个形状压根不存在了**，不用再分家。**别重复提。**
    ② **收尾轮注入"别想了、现在写文件"的强制指令**（`已达最大工具调用轮次` 那条路已有
       注入强制工具指令的先例，同形状）—— **还没做**；
    ③ **调 `_cap` 或任务预算** —— ✅ **09-21 已动**（`_cap` 去掉了，见上面「240 秒」那条）；
       "再烧一个 240 秒"那个顾虑现在换成了"**再烧到预算见底**"，**代价仍没量**。
  ⚠️ **改之前先量**：②哪个划算，得看"被掐"的调用后来**换模型有没有成功过**（第一轮数据点已到）。
- [奇点] 🔴🔴 **「整份覆盖写」是一整族 —— 第 1、2 层已修，第 3 层不做**（09-18 外派第二轮，我已核）
  `save()` 是 `to_dict()` **全量覆盖写**，**无字段级合并、无版本号** ⇒ **谁最后写谁全赢，
  手里那份越旧抹掉越多**。同族的写入者已核实 **≥5 类**；观察者那条是最早抓到的那个。
  ✅ **第 1 层**：`_api_projects._start_background` 的四个调用点不再传对象，改成收 `project_id`、
  **进线程再 `load()`**。测试 `test_background_loads_fresh.py` + `test_background_call_sites.py`
  （后者是补"调用点没人管"那半边），**两处都变异验过**。
  ✅ **第 2 层**：`save()` 加 `_warn_if_stale_write` —— 写盘前比对盘上 mtime 与手里 `updated_at`，
  明显更新就 `witness.warn("stale_write")`。**只报不改、不新增字段**（拿已有的持久化字段当版本令牌）。
  测试 `test_stale_write_detection.py` 含"不误报"那条命门。⚠️ **漏报面**：两次写相隔 <1s 的并发写抓不到（有意取舍）。
  🔵 **观察者那条（09-17 真机抓到、最重）**：`_maybe_report_gates` 读盘 → **调模型 10~20 秒** →
  `save()` 整体写回 ⇒ 这 20 秒里人的批准、刚建的任务全被抹掉。**09-17 已修 `19096e2`**
  （写回前重新 `load`），09-19 晚又少一个写入者（`_observer_answer` 三处 GATE 批准转
  `project_gate_confirm`）。⚠️ **但那只是"修一个写入者"，机制没变**；**判据仍未真机验**
  （四次都没撞上那个竞态）。
  ⏸ **第 3 层（乐观锁 / 字段级合并）不做**：动状态机核心，真机验不过来。
  🔵 **不用为它们单跑一轮** —— 点批准时、看 `alerts.jsonl` 里 `stale_write` 刷不刷，顺带即可。
- [奇点] ✅✅ **「最小闭环」A 档 + B 档 —— 09-21 `round-20260921c` 真机双双走到，四条判据全中**
  **现场**（项目 `1789965398548`，19:03:14）：`gate3_rejected {feedback: "自动返工: 审查触顶，先回实现层", auto: true}`
  → `phase from integrating to executing`；同一条 `gate3_route` 里写着 **`reset_tasks: 3, reset_failed: 8`**。
  按方案 §五逐条对：
  · ① `lineage` 里出现 `auto: True` 的 `gate3_rejected` ✅
  · ② phase 回到 `executing` ✅
  · ③ 🔴 **回到 executing 之后任务真的被派下去了**（判据里最重的那条）✅ ——
    T1 当场变 `running`，心跳文件写在 **19:03**（就是返工那一瞬）
  · ④ 全程**没有**任何 `gate-confirm` / `owner_confirm` ✅（`owner_confirm` 里只有我早先批的 gate1/gate2）
  · **A 档顺带验到**：`reset_failed: 8` —— **8 个失败任务也进了重置集合**（改之前只重置 `DONE`，
    而 `ready_tasks` 不扫 FAILED ⇒ 失败的任务永远不被重派 ⇒ 返工一次冲回 GATE3 转圈）。
  ⏳ **反例还没验**（要等下一次触顶）：第二轮仍不过时**不再**自动回 · `QIDIAN_AUTO_REWORK=0` 时一次都不回。
  🔵 顺带：`precise_reset_would_be: []` —— ⚠️ **原文写"退化样本第 2 次"，是错的**
    （09-21 更正）：它空**不是样本问题，是现状下必然空** —— 失败总是一大片时，精准 ≈ 全量。
    也因此 B5 被拍板不做了（见 `~/OPEN-别动.md`）。
  📌 待观察：回炉之后 `delivery_facts` 的「按类」会**整轮读成 0**（任务全被重置成非 done），
    看起来像"全军覆没"—— 那是重置的正常形状，别当成绩读。
- [奇点] ⬇️ **上条「最小闭环」的落地细节**（**A/B 档 09-21 真机都验过了，见上条**；
  这里留的是实现细节与那条同族教训，**没有待办**）
  ✅ **A 档：`impl` 返工的重置集合加上了 `FAILED`**（`handle_gate3_reject` 里那支）——
  改之前只重置 `DONE`，而 `tracker.ready_tasks` 只扫 PENDING/ROUTED/BLOCKED
  ⇒ **失败的任务原地不动、永远不会被重派** ⇒ 返工一次 = 把做成的推倒重做、
  没做成的照旧缺 ⇒ 又冲回 GATE3 ⇒ 转圈。`FAILED→PENDING` 本就合法、不需要 force。
  🔴 **顺带堵了一个会踩的坑**：超时那条路是**成对**写的（先写 `cancels/<id>.json`、
  再转 FAILED），而 `docs/防御模式.md` §82.3 的泄漏面核验**正是靠"这一支只重置 DONE"成立的**
  ⇒ 加了 `_clear_cancel_marker_for_rewind`：timeout 标记删掉再重派 · **人工取消的不重派**。
  测试 10 条 · 四次变异各咬中。
  ✅ **B 档：审查触顶那条兜底路不再直接交人** —— 先问 `_auto_rework_allowed`，
  允许就回炉一次、不允许才 `set_phase(GATE2)`（不自动时把理由写进 issues，
  否则"没回炉"和"没走到"在界面上长得一样）。它原来把自动返工**整个绕过**。
  ⚠️ **同族教训（这条比修复本身值钱）**：这条路 09-17 还绕过过**整个验收层**
  （当时靠"补一句 `_run_verification`"修的）⇒ **"兜底"要穷举它绕过了什么，
  不能发现一样补一样**。
  ✅ **两处 09-21 都真机验过了**（`round-20260921c`，判据「回到 executing 之后任务真的被派下去了」四条全中）。
  ⚠️ **代价是真的**：回炉 = 再跑一轮 = 再烧一截 token，下一轮把账一起看。
  ⏳ **C 没做**：只把 docstring 里那句**错误的辩护词**（"全量重做贵但收敛"）改对
  （随 A 动了一部分措辞，没单独做）。
  ✅ **B5 那条边：09-21 用户拍板现在不做** —— 原话「任务失败后一直在修 bug，就没重试过，
  每次都是失败一大片，**没必要**」。见 `~/OPEN-别动.md`。
  📦 09-18→09-20 的推导过程与逐条判据 → `docs/最小闭环方案-20260918.md` + 瘦身前快照。
- [奇点] 🔴🔴 **判据错位审计：实 19 / 面 2 / 疑 6，几乎全是一个治疗点的分支**（09-18）
  **全文 `docs/判据错位审计-20260918.md`**（含「五、2026-09-19 复核」一节 ——
  **原单子的行号全漂了，别照它做**）。治疗点一句话：**把"这次是被我们断的"贯通到判据** ——
  它只写进没人读的旁路（告警 + 分诊账），而 `ExecutorResult.error_kind` 的枚举里**压根没有这一类**。
  ✅ **A3–A9 / B1–B6 / 疑6 都已处理**（09-19，绝大多数是"**只标归因、门的强度一分不动**"）
  —— 逐条改法、测试与变异证据见上面那份文档，以及瘦身前快照 `docs/OPEN快照-202609200208.md`。
  ⏳ **仍开着的四条**：
  · **B3 我判"不动"** —— 安全审计没跑成带 `category: review_error`、文案已诚实、fail-closed 对了；
    要动是"**要不要阻断**"的决定。
  · **C1–C6（上限类）** —— ✅ **09-20 数据到手了**（分诊账 145 条，见下面「240 秒」那条：
    **不是"不够"，是"它在原地打转"**）⇒ 卡点从"先有数据"变成"**打断要先定阈值**"。**别重复提。**
  · **疑1** —— 核到底了，但要动**先拍板「没有盘上产物时该说什么」**。
  · 疑2 / 疑3。
  ⚠️ 边界（**没审**的部分）：observer/ · `_api_admin.py` · 前端 · `zhipu_api.py` 函数体。
  ⚠️ 剩下的大多要先定"**归因算谁的**"，那是决定不是 bug 修 —— 要动**先挑一条说清改法再动**。
- [奇点] 🟡 **「打转」真打断的阈值没定 —— 多小算小**（09-20 拆出来的；结论/报警/尺子偏差都在 `docs/防御模式.md` §87）
  报警已落地 `7677f45e`（**只出声、不打断**）⇒ **这条不阻塞**。要定的是：**从哪一轮起该动手打断**（"原地打转"烧到多少秒/多少轮算过头）。
  ⚠️ **定之前先看 §87.6**：`elapsed` 这把尺子量长了（把关流耗时算进去了），**大数值要当上界读**。
- [奇点] 🔴🔴 **`glm-5.3-flash` 比 `deepseek-flash` 慢 2.5~3 倍 —— 而 cascade 的落点正是它**（09-17 两组实测）
  直连：deepseek 首字节 0.7s / **431,422 字/秒** · glm 1.2s / **139,190**。分诊账同向（728 vs 254）。
  ⇒ `cascade_skip` 是 `deepseek-flash → glm-5.3-flash` —— **failover 落在慢的那家** ⇒ 吐不完被掐。
  ✅ **分诊账这把尺子已验**（round g，按时间切开）：deepseek 0.1s / **575~805 字/秒** ·
  glm 1.1s / **271~342**（**各只 2 条，样本小**）。首字节全 0.1~2.0s ⇒ **慢在服务端吐字，不在我们这侧**。
  🔴 **09-20 更正：本条的前提被数据否掉了 —— "按模型设 cap"治的不是这个病。**
  分诊账 145 条实测：被掐的 11 条里 **7 条是最快的 `deepseek-flash`**（glm 只占 4），
  且 4 条在 21~62 秒就被掐（被**剩余预算**掐的，不是被 cap）⇒ **和模型快慢无关**。
  ✅ **GLM 慢是真的**（首字节 0.85s vs 0.20s；272 字/秒 vs 858），但那是**另一个问题**。
  真问题是"**整轮只想不动手**"（见上面「240 秒」那条）⇒ **别按本条做 cap**。
  🔵 **09-21 补：这条现在有点反讽 —— 我偏偏把 glm 放进了 `executing` 当兜底**（见「零产出族」那条）。
    **不矛盾**：本节反对的是"按模型快慢去设 cap"，而选它当兜底的理由是**它最不容易撞 240 秒那堵墙**
    （委员会那轮 glm 35.7s / 思考 136 字 vs v4-pro 181s / 思考 4 万字）——
    被掐的根因是"想太久"，不是"吐得慢"。**两件事，别混。**
  🔵 过程教训：我第一版对照脚本**把智谱的 key 发给了 DeepSeek**（三发全 401），结论是**脏的**。
- [奇点] 🔴🔴 **观察者汇报把项目状态整体写回 ⇒ 人的批准被抹掉**（09-17 真机抓到，**最重**）
  `_maybe_report_gates`（`_observer_worker.py`）：读盘拿快照 → **调模型 10~20 秒** →
  `_proj.save(p)` **整体写回**（写的是 `to_dict()` 全量快照）。锁只防"同时写"、
  **防不住"拿旧快照写"** ⇒ 这 20 秒里发生的事全被覆盖（`owner_confirm` 掉了、`task_ids` 清零，
  而刚建的任务**还在跑、成了孤儿**）。**窗口正好压在"人看见门 → 拍板"这段上，不是小概率。**
  ✅ **09-17 已修 `19096e2`**：写回前**重新 load**（`fresh = _proj.load(pid)`），
  且这期间已经过门了就**不吭声**（那段白话说的已不是现在的处境）。
  ⚠️ **四次真机都没撞上那个竞态，判据仍未验。**
  🔵 **09-19 补充（措辞更正）**：这一修是**修一个写入者、不是修机制** ——
  同形状的写入者已核实至少 5 类，见上面那条「**整份覆盖写**」。
  ⚠️ 本条原文写的是「⇒ 修法：写回前重新 load」**像还没做** —— 那是过期的措辞，已改。
- [奇点] 🔴 **「任务标成 running 却没人管」那一族** —— ❌ 09-17 验过没兜住；**09-18 round g 又现场复现**
  `…843` 状态 `running`，但 **工作树已经没了**、侧车 12 分钟没动、心跳停在 01:11 ⇒
  项目停在 `executing` **不会自己结束**；同项目 `…841` 卡 `conflict_held`，**cancel 完又被 drain 捞回去**。
  🔵 **判据更硬了**：不是"探测器没响"，是"**工作树没了 + 侧车不动 + 心跳陈旧**三个信号同时在场，
  系统照样认为它在跑"。**替代判据**（精确、非超时猜法）：`running_futures` 非空 ∧ 工人全空闲。
  → **详情 防御模式 §77.3**
  ⚠️ **09-19 补**：本条点名的 `…843`/`…841` 两个现场**已被清掉**（它们是那 10 个孤儿里的，
  归档时删了任务 JSON）。**族还在，但盘上的证据没了** —— 要再看只能跑新的一轮。
- [奇点] 🟡 **`task_retry` 提前释放锚 —— 已修 `169081e`（你拍的板：按原则改）**（09-19）
  ⚠️ **这条的经过值得记**：我先前说"没人记过"**是错的**（代码里写得清清楚楚，是有意取舍）；
  要动手时又撞到一条**专门钉住旧行为**的测试 `test_重试仍然释放锚`（09-18 建的）。
  🔑 **真正定案的是那个文件自相矛盾** —— 文件头写「**merged 是唯一**让'产物已安全进项目仓'
  这句话成立的分支」，而那条测试却允许重试松手。
  ⇒ 按文件头那条原则改：去掉 `task_retry` 里那次 `_release_ref`，测试反向重写。
  依据：「被取代」在点重试那瞬是**假设不是事实**，而 `_anchor_ref` 用 `git update-ref`
  **无条件覆盖** ⇒ 新尝试一产出自会盖掉旧锚；提前释放只在「重试没跑成」时有差别，那时纯丢。
  **变异验过**（加回去 → 那条红、其余 6 条绿）。⚠️ **真机没验。**
  🔵 **遗留**：界面仍会说"有可打捞产物"指上一版 —— **那不是误报**，要消歧义该改**标签**（排期未定）。
- [奇点] 🟡 **告警 ③ 的"哪些算关键"是我替你挑的，你可能想改**（09-20）
  ✅ 出口已建 `de8fdf38`：macOS 桌面通知，白名单 7 个 key（判据「罕见 ∧ 人现在就得动手」），
  同 key 60 秒冷却，总闸 `QIDIAN_NOTIFY=0`。**明确不收**常驻噪声（`drain_dep_blocked` 3180 条 /
  `decompose` 386 / `lazy_spoke_import_failed` 启动即报 / `designer_reviewer_is_writer` 56 次）。
  ⏳ **要你看一眼名单**：长短、有没有漏的该动手的、有没有混进去的噪声 —— 名单在
  `witness._CRITICAL_ALERT_KEYS`。
  ⚠️ 原文那个 `→ 详情 §16` 是**死指针**（`防御模式.md` §16 讲的是 python3 那件事）——
  原始提法在 `docs/OPEN快照-20260916.md`。
  ⚠️ 09-17 中午修正：原文写"`lazy_spoke` 已归零"**是错的** —— 它在**后端启动那一刻**就会报。
- [奇点] P1 三臂实验：方案在 `docs/P1-三臂实验方案-20260912.md`，**没跑（只差跑）**。→ **详情 §21**
- [奇点] 🟡 **外派评审 09-20（成熟度 / 受众）核对完并排序 —— ✅ 真机那轮已跑完，可以动了**
  全文 `docs/外派评审-20260920.md`（逐条核对表 + 六步排序 + 三条路径选第 3 条）。
  一句话：**门接电 → 先坐实「产物被丢」再改 → 造尺子（可机检的交付事实）→ 计划期臆造
  → 状态一致性 → 最后才是"以周计"的结构工程**。评审把修订成本当唯一真天花板、**我同意**，
  但**顺序不同意** —— 那件活的验收标准，恰恰要用第 2 步造的那把尺子。
  🔵 **它量错的**：「CI 红两周」是错的（CI 09-18 就绿了），真因是 **`ahead 34` 没推**
  ⇒ 它「接线类 ≈ 一到两周」里**划掉一条**。
  ✅ **第 0 步「门接电」`0bcc6802`** —— `shapes` 走 `--baseline` **棘轮**（裸挂是恒红 ⇒
  会被 `|| true` 掉，§78）；`preflight live` / `delivery_facts` **有意不接**（CI 里没那份现场状态）。
  ✅ **09-20 推了、真 CI 也绿了**（run `35463355160`）：**`audit` / `test` / `lint` 三个 job 全 success**
  —— 这扇门现在**有第三方跑过的证据**，不是"本机绿"。
  ✅ **第二批 5 笔推完同样全绿**（`lint` / `test` / `audit` 三个都是 success）—— 两批合计 14 笔都过了门。
  ✅ **第 2 步「造尺子」`757e877d` + `dbacf210`**（09-20）—— `delivery_facts.py --rounds [N]`
  把几轮摆一起，靶子（「三轮内 6/11 → 9/11 ∧ 单轮 token 下降」）现在量得出来。
  🔴 **验收是拿 git 独立复算的**（不走工具那条路）⇒ 数字全对，但撞出「**两栏会重叠**」：
  屏幕像 `2 + 7 = 9` 的干净划分，其实有一个任务两栏都在 ⇒ 已印 `两栏都在 N 个`。逐条账在 §九。
  ✅ **那轮早跑完了**（`round-20260920c`）—— 第 2 步的判据两条都达成，见上面「接手指针」那条。
  🔵 **所以现在轮到第 4 步「计划期臆造」** —— 就是上面那条真缺口
  （**检查失败时分不出「测试没过」和「产物没到」**）。
  🔵 顺带记一条：CI 那次跑了 **3 分 43 秒**（本机 `make check` 是 11 秒）——
  慢的那条 `audit-preflight` 只在 CI 跑，正是这个分工的代价与目的。
  ⚠️ 若真要拆件开源：评审点名的 `_machine_checks` / `merge.py` ——
  ✅ **`_machine_checks` 09-21 真机跑过了**：`ran:10 passed:5 missing_inputs:5 our_side:0 skipped:3`
  （**5 条真失败、不是因为"文件没到"而失败**，归因也对）。🔵 `skipped:3` = 13 条约束只跑了 10 条，
  **但它是出声的**（汇总原话「另有 3 条超出上限 10，本轮未跑」）⇒ **不是静默截断**。
  （那个上限为什么是 10、13 条里跳过哪 3 条 —— 没查。）
  🔵 `merge.py` 这轮走了成功路径（`integration_merge ok:true`），**三条错误分支没触发** ⇒ 仍未验。
- [奇点] 🔵 **Qoder CN 外派审查**（免费 Qwen3.8-Flash，**9/30 23:59 截止**）
  **只审不改** · 报告落 `docs/` 进 git · 与 `docs/*-20260914.md`（ZCode 那批）
  **配对比对**（两家都报的 ≈ 真问题）。🔴 **第一步是「标定」不是「跑一遍」**：
  拿 ZCode 已找到的缺陷当答案，看它能不能**独立复现** —— 量不出能力，就不知道该不该
  在 10/1 之后继续用它。⚠️ `.tongyiignore` 建好了但**没实测它认不认**。
  📦 方案全文 → `docs/Qoder-外派审查-方案.md`


- [求职] 🟠 **把「5 个痛点 → 奇点模块」写成一份东西**（09-20 提出）
  用户 09-20 亲口列了做判例助手时撞的 5 个痛点，**条条对得上奇点的一个模块**（已核实模块存在）：
  ① agent 偷懒/砍掉做好的功能 → 门禁 + `validator.py`；② 模型幻觉编造没做过的决定 → 审计/验证层；
  ③ 上下文断了接不上、重改已修过的 bug → **一整套记忆系统**（`_memory_*` 8 个文件）；
  ④ 不懂开发流程、想一步做一步、"**AI 还狠吹捧**" → 四阶段流水线 `_workflow_phases.py`；
  ⑤ agent 管理反反复复 → 调度器 + 任务档案。
  🔵 **汇合点（两个项目同一个信念）**：**不信模型的自我报告** ——
  判例助手不信"模型说自己引对了"（拿原文逐字比）；奇点不信"模型说自己改好了"（拿测试/验收比）。
  **这份东西 = 面试讲稿 + 奇点现在的设计文档**（后者可能压根没有）。
  ✅ **09-20 骨架已落地**：`docs/设计源流.md`（五个痛点→五个模块，留下【你来填】的空）
  + `docs/失败形状.md`（从 1816 次提交里挑出 30 次自己推翻自己 → **四种形状**：
  拿后果当证据 / 表述比事实强 / 信了被污染的读数 / 判据的前提没人验）。**两个都已本地提交。**
  ⏳ **剩两件**：① `设计源流.md` 里的【你来填】**只有当事人能填**（当时的现场）；
  ② ~~要不要公开~~ → **09-20 用户拍板：不推**（两份都留在**本地提交**，不进 GitHub）。

- [归档] 🔵 **`case-analyzer` 仍是唯一"有意没推"的**
  · `case-analyzer`（**PUBLIC**）：合同模式「顾问视角」那一轮（5 个文件）。**等你发话**。
  · ✅ 09-21 12:2x 三仓已推：`singularity`(10) · `dotfiles`(6) · `claude-memory`(1)。推前扫过敏感串：干净。
  · ✅ 已进 `archive-check.sh` 的 `REPOS=`（09-21），它的"未推"单独标"(有意，本地留)"、不触发"等网络"。
---

## 🔴 停车（09-21 实测踩了两次，先看这个）

**「暂停」和「停整轮」是两件事，别混：**
- **任务级暂停是有的**（`POST /api/tasks/<id>/pause`，界面上是任务页那个按钮）。但它
  ① **不即时** —— `_exec._check_paused` 在**每轮开头**才读，`API 返回原文就是「当前 turn 结束后生效」`，
    飞出去的那次调用拦不住（单次最长 240 秒）；
  ② **是"占着位子等"** —— 实现是**卡住那个 worker 线程**每秒轮询，并发只有 2
    ⇒ 暂停两个任务 = **整个池子停摆**（不是腾出位子）；
  ③ **逐任务的**，不拦观察者建任务、也不拦它拉循环。
- 🔴 **没有任何"项目级/整轮"的停** —— 所有 `projects/<id>/…` 路由里只有
  `gate-confirm` / `run-phase` / `start` / `files` / `diff` / `cost` / `lineage` / `snapshot` / `traceability`
  / `lineup` / `flow-weight` / `research-raw` / `history`，**没有 pause、没有 stop**。
  全局只有 `/api/loop/stop`，而**观察者会把它拉回来**（见上）。
  ⇒ **"别烧钱了"这个需求现在只能靠杀进程。** ✅ **09-21 已修 `f7fdf49e`**（用户选 A）：
    `POST /api/projects/<id>/stop` —— 给该项目所有非终态任务发取消。
    **自己只有一句实质代码**（逐条喂给已有的 `task_cancel`），连"哪些算终态"都不自己判
    —— 第一版抄了份终态清单，而那正是我在同一个函数的注释里刚警告过的「两份逻辑迟早会漂」。
    5 条测试（含「`_check_cancelled` 真的拦得下」+ HTTP 路由两条接线）· **三次变异各咬中**
    （不喂 task_cancel / 两栏记反 / 路由空转）· 全量 1929 绿 · `make check` 过。
    ⚠️ **没真机验**（判据：真点一次它，看钱停没停）。
  🔵 **09-21 想清了最小解法，而且比我一开始想的更小**：**关键事实 —— 一个任务一旦进了
  `_exec.run()` 的回合循环，它就不再经过调度循环了**（`_exec` 自己一轮一轮地调 `dispatch()`，
  直到预算用完）。⇒ **"停循环"只能拦住"还没派下去的"**，拦不住正在跑的。
  而 `_check_cancelled` 是**每轮开头**读 `CANCEL_DIR` 的 ⇒ **写取消标记才拦得住在跑的**。
  ✅ **最小方案：加一个 `POST /api/projects/<id>/stop`，只做一件事 —— 给该项目所有非终态任务写取消标记。**
    为什么这就够：在飞的下一轮开头退出（最多再烧完当前这一次调用）· 排队没轮到的 worker 一起来
    就看到标记、立刻返回 · 还没派的 `ready_tasks` 不返回 RUNNING ⇒ 不会再被派 ·
    **调度循环照常跑也无害**（这项目没活干，它自己空转）⇒ **观察者那句"确保循环在跑"就不用去动它了**。
  ⚠️ **两条边界要你选**（这决定做哪一个）：
    · **「停」= 取消标记**（最小，上面那个）：语义是"人工取消" ⇒ 以后返工重置时这批任务
      **不会被重新拉起**（`held_back_user_cancelled`）。要接着跑得单独清标记。
    · **「暂停」= 项目级 pause 标记 + 派发前读一眼**：语义是"待会接着跑"，任务状态保留、
      标记删掉就续上。多一处读、但保住"可恢复"。
    ✅ **未定已定：用户选「停」**（09-21）。"暂停到一半接着跑"那套**没做**，理由照旧：
      为想不出使用场景的灵活性多写一套状态不划算；真出现需求时，(A) 的标记加个字段就能升上去。


- ✅ **`POST /api/loop/stop` 停不住 —— 09-21 已修 `e2aad3b3`**。
  根因：观察者建/路由任务那句末尾的「确保调度循环在跑」
  （`if not loop_status().running: start_loop(concurrent=2)`）**只问"在不在跑"、
  不问"谁停的"** ⇒ 人一停它下一句就拉回来（实测 19:15 停、19:17 被拉起）。
  改法：`_hooks` 里记一笔「是人停的」，守卫问一句再决定。
  ⚠️ **没有把守卫关掉** —— 循环**意外死了照旧拉**，加的是"谁停的"这份信息。
  `/api/loop/status` 多一个 `stopped_by_human`，"它为什么没自己起来"当场可查。
  7 条测试（含**反方向对照**：没人停 ⇒ 必须照旧拉）+ 4 次变异各咬中。全量 1968 绿。
  🔴 **真机没验** —— 判据：按一次 `/api/loop/stop`，等观察者建下一个任务，**看它起不起来**。
  ⚠️ 顺带更正一处旧措辞：原文说"`_observer_tools.py` 里**那两处**" ——
  实际**只有一处**（另一处是观察者自己的 `control_loop` 动作，不是守卫）。
- 🔴 **别用 `lsof -ti:5050 | head -1` 拿 pid** —— 它给的是**连上来的客户端**，
  不是监听者。09-21 我就这么杀错了一个 `com.apple.` 进程（真后端还活着，白杀）。
  用 **`lsof -nP -iTCP:5050 -sTCP:LISTEN -t`**。
- ⚠️ **SIGTERM 杀不动它**（实测 5 分钟没退，期间还起了新调用）⇒ 要 `kill -9`。
- ⚠️ **起后端 = 循环＋观察者一起起** ⇒ **杀了就别随手重启**，重启=接着烧。

## 怎么跑真机（09-17 核过，跑之前照这个来）

- **起后端**：`cd ~/projects/singularity && nohup .venv/bin/python -m singularity.web.app > /tmp/qidian.log 2>&1 &`
- 🔴 **改完代码必须先重启后端再跑真机**（`app.py` 是 `debug=False`，**没有热重载**）。
  判据：`ps -o lstart= -p <pid>` 要**晚于** `git log -1 --format=%ad`。
  09-17 中午实测踩到：跑着的进程是 01:40 起的，而修复是 02:12–02:28 提交的 ——
  **不重启就开跑 = 整轮跑在旧代码上，然后得出"修复没用"的假结论。**
- 🔴 **起后端 = 调度循环也跟着起**（`web/app.py` 启动时那句 `start_loop(concurrent=2)`，
  观察者智能体同理）—— **没有"后端跑着但循环没开"这个状态**。09-19 我据"没监听端口"
  推断"循环没开、不会烧钱"，**是错的**（那次恰好真没在花，因为没有项目可推 —— 结论对、
  理由是编的）。**要判它在不在烧钱，看 `usage_daily.json` 的 mtime 和 `scheduler.log` 有没有 dispatch，
  别看端口。** 停止用 `POST /api/loop/stop`。
  ✅ **并发：09-21 用户拍板 2 → 4，已落地 `0fded75d`**（原来是**三套值**：HTTP 默认 1 ·
  观察者那两处硬编码 2 · 启动那次 2 ⇒ 改一处不改另一处 = "改了没生效"，而 `/api/loop/status`
  只报一个数、看不出源）。现在**单一来源** `config.DEFAULT_CONCURRENT`（环境变量
  `QIDIAN_CONCURRENT` 可覆盖，上界仍是 `_MAX_CONCURRENT`=8）。
  ⏳ **真机没验** —— 起后端时看启动日志那行 `调度循环已自动启动 (concurrent=4)`。
- ⚠️ **`auto_mode=False` ⇒ GATE1/2/3 都要人点**：
  `POST /api/projects/<id>/gate-confirm` body `{"gate":"gate1|2|3","decision":"approved"}`；续跑 `POST /api/projects/<id>/start`
- ⚠️ **验证轮**才加 `QIDIAN_TASK_DEADLINE_S=180`（否则挂住要耗满 900s）。**真实产出别调小。**
  ⚠️ 但注意：**单次模型调用另有 240s 硬顶**，跟这个无关 —— 调小它只会让**单次调用**更碎。
- ⚠️ **curl 一律 `--noproxy '*'`**（连本机 127.0.0.1 也会被系统代理劫走）· 端口 **5050**
- **看现场常用**：`.qidian/llm_400_unknown.jsonl`（**认不出来的 400 的请求体原文**，
  一次一条；09-19 夜 `de6d2fa8` 起的）· `sample <pid> 2`（免 root 的 py-spy 替代）· `.qidian/partial_usage/<task>.json`（侧车：
  token/派发/工具事件）· `.qidian/heartbeats/<task>_any.json` · `.qidian/alerts.jsonl` · `.qidian/llm_calls_slow.jsonl`（分诊账）
- 🔴 **任务的工作树在项目仓的隐藏兄弟目录**，不在 `.qidian/worktrees/`（那个目录恒空）：
  `~/项目/项目仓库/.<项目名>-worktrees/<task_id>_<level>/`。判据：`ls ~/项目/项目仓库/` 里**以 `.` 开头**的。
  任务在那里写文件、提交（`agent changes in <task_id>_<level>`），**合并成功后才进项目仓** ——
  所以"项目仓里什么都没有"**不等于**"任务没干活"。
  ⚠️ 侧车 `partial_usage` 里那个 `(27字符)` 是**消息长度不是结果**，别拿它判断写入成败。要看 `result_preview`。
- 🔴 **每轮开局先打 tag**（09-19 定的规矩）：`git tag -a round-<日期>` 打在**开跑那一刻的 HEAD**。
  用途：跑完 `git describe --tags` ⇒ `round-20260920-3-gabcdef1`，**比它新的 N 个提交
  = 这轮带着但没验过的修复** —— 这是"修得比验得快"唯一在 git 层面可查的答案。
  上一个锚是 `round-20260919`（09-19 晚基线）。
- 🔴 **打 tag 的同一刻顺手存一份清单快照**（09-19 定）：清单在 `~` 仓、代码在这仓，
  **两个仓的提交对不上号** —— 跑完回头查"那一轮带着哪些待办"就得靠这份快照。
  `cp ~/OPEN.md docs/OPEN快照-$(date +%Y%m%d%H%M).md && git add docs/ && git commit -m "轮次快照"`。
  ⚠️ **别挂进 hook 每轮写**：那会让工作区**常脏**，而 `archive-check.sh` 第①项的
  "未提交"红存在的唯一理由就是"我忘了提交" —— 用一个常亮的假红换掉一个真红。
  **按轮存，不按秒；实时没有需求。**
- ⚠️ **重启会把 RUNNING 任务回收成 PENDING 重派** —— 见 `~/OPEN-别动.md`「重派撞上 worktree 已经没了」。
- ⚠️ **删项目 = 停任务 + 报一声，但两样仍不动**（`4316fb5c` 起）：
  `DELETE /api/projects/<id>` 会**先取消**它 `task_ids` 里的非终态任务，再把留下的条数
  写进 `alerts.jsonl`（key `project_deleted_left_tasks`）。
  **项目仓不删**（那是成品）· **任务文件不删**（那是锚的唯一线索，删了产物变孤儿 ref）。
  ⚠️ 别再用裸 `proj_mod.delete()` 绕过它。
