# Q002 — "共享记忆层 > Agent 直接对话"是否成立？

## 问题

T002 的核心假设：让 GPT 和 Claude 通过共享文件通信，比让它们直接互发消息更稳定。这个假设是否正确？

## 当前状态

Agent Bus v0.1 已运行。用户仍需手动桥接一次（上传文件给 GPT → 复制回来）。但已经从"每轮对话中转"降低到"每个任务一次"。效果待评估。

## 验证方式

跑 5-10 个任务后，统计：用户介入次数、信息丢失次数、决策链路可追溯性。对比之前纯人肉中转的效率。

## 关联

- agent_bus/T002 → 已归档
- research/insights/P005_双AI协作.md
