# 「端到端跑通一个项目」的验收清单 —— 2026-09-21 开跑**之前**钉死

> **为什么现在写**：终点线（用户 09-21 拍板选 ③）的第③条要求"按**开跑之前就钉好**的
> 验收清单跑一遍全过"。**跑完再写清单 = 跑完再找理由** —— 这个仓栽过（拿后果当证据）。
>
> ⚠️ **这份清单必须在下一轮真机开跑之前定稿并由用户确认**。跑的时候照它逐条核，
> **不靠印象、不临时加条、不临时免条**。
>
> 🔴 **物证已经没了一次**：09-17 那次（唯一一次达成 ③）的项目仓
> `验证-日志统计-20260917` **已被删除**，它的 `release/1789571713224-202609170202`
> 标签随之消失 —— **没法再拿它当基准比**。所以这份清单是**重新钉**的，不是照抄。

## 一、题目（与 09-17 / 其后各轮一致，别改）

JSONL 日志统计 CLI，拆成 **解析 / 过滤 / 统计 / 输出 / 入口** 五个模块。

## 二、开跑**前**要准备好的东西（不依赖模型，我自己做）

1. **一份已知答案的样本日志** `~/qidian-accept/sample.jsonl`（手工构造，含：
   3 种 level、2 个 source、**带时区的 ts**、**2 行坏 JSON**、**1 行空行**）。
2. **答案表**——每种计数的期望值，写在同目录 `expected.json`（跑的时候**对表**，不对印象）。

## 三、跑完逐条核这些（全过才算 ③ 达成）

| # | 核什么 | 判据（照这个核，不靠印象） |
|---|---|---|
| 1 | 项目真走到终态 | `phase == done`，且**不是我手工推的**（lineage 里没有我写的推进记录） |
| 2 | 交付包真出得来 | 里有一个 `release/<id>-<日期>` tag；`git archive <tag> \| tar -t` **能导出非空** |
| 3 | **包干净** | 导出清单里**没有** `.pyc` / `__pycache__`（09-17 那次漏了 7 个 `.pyc` 到收件人手里） |
| 4 | 装得上、跑得起来 | 解包后 `python -m <包名> --help` **退 0**（不是 traceback） |
| 5 | **核心功能对** | 拿 `sample.jsonl` 跑主统计命令 ⇒ 输出**与 `expected.json` 逐项一致**（含时区归一化那条） |
| 6 | **边界不崩** | ①空文件 ②全是坏行 ③文件不存在 —— 三种都**给出人话**，不是 traceback |
| 7 | 包里的测试真能跑 | 解包后 `pytest` **退 0**；若包里没有测试文件 ⇒ 第 7 条**记为"不适用"并写进报告**（不许静默跳过） |
| 8 | 报告齐 | 交付清单 / QA 报告**都在**，且 QA 报告里**没有**"没跑却写通过" |

## 四、**不算数**的几种情况（写下来，免得跑完自我说服）

- phase 停在 `gate2` 兜底 → **不算**（那是"卡住"，不是"做完"）
- 我手工建过任务 / 手改过架构 / 手动打回过 → **不算**
- 产物只在 worktree 里、**没进项目仓** → **不算**（`git archive` 导不出来就是没交付）
- 第 5 条"大致对得上" → **不算**，必须逐项一致；对不上就是**没成**
- **QA 判词说"通过"不能替代上面任何一条** —— QA 是被证明会骗人的那把尺
  （c 轮交付跑一次就崩、QA 写的"代码质量高"）

## 五、确认

✅ **2026-09-23 用户拍板：就这 8 条，定稿，不改。**

过目前给过的选项是「就这 8 条定稿」/「加一条本轮总花费上限」—— 用户选了前者。
⇒ **成本判据不进这份清单**（预算是另一件事，奇点自己有预算闸门）。
⇒ 下一轮真机开跑**照这 8 条逐条核**，**不临时加条、不临时免条**。

⚠️ **一条边界提醒（09-23 核出）**：第 2 条要把 `release/<id>-<日期>` tag 导出来才算交付，
而**09-17 那次唯一达成的产物连同它的 tag 已经不在盘上了**（见开头那句"物证已经没了一次"）。
⇒ 别拿 `release/1789571713224-202609170202` 去比 —— 它**现在 `git archive` 不出来**。

---

## 六、`round-20260926` 的「冻结记录」（09-26 21:5x，**开跑前**）

§二 要求的两样东西在 `~/qidian-accept/`（**home 仓是白名单式忽略，它本身不进 git**）
⇒ 把**指纹**记在这儿：**跑完之后谁动了样本或答案表，哈希对不上，当场看得出来**
（这份清单自己第一节写着"跑完再写清单 = 跑完再找理由"，同一句话对样本也成立）。

| 文件 | sha256 | 行数 |
|---|---|---|
| `sample.jsonl` | `ac26c717aac607a6078f229b50059b346787b3d4890e08de0971989ca09bbc6f` | 10 |
| `expected.json` | `5625798eb06663e23c5911fc610ab39c627f9e7fc6b87aaae8d6bcffe2e595ab` | — |
| `empty.jsonl` | `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` | 0 |
| `all_bad.jsonl` | `4bb61e10ec1992ef7d902a9823895a32e53c43b7b5eecd32fb7af59690b46ecc` | 3 |
| `README.md`（怎么对表） | `083d0bc88e91b6e8a4611d77e911c6232621f31eb550ec5008580c7fcf0f5334` | — |

🔵 **这份样本验过有判别力**：正确实现跑那个时间窗得 **2** 条，
一个**不解析时区、只比字符串**的实现在同一个窗口得 **0** 条。

📌 同一刻还做了：tag `round-20260926` · 清单快照 `docs/OPEN快照-202609262125.md` ·
非终态项目扫过（空）。
