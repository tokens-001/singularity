# 外派评审（2026-09-20）——「距离成熟还有多远 / 受众与前景」

> **来历**：用户 09-20 转来的一份外部评估（A 成熟度三条判据 + B 受众与三条路径）。
> 本文件是**核对记录** —— 按仓里的规矩「评审自带的前提要拿本仓核一遍」，逐条量过再给排序。
> ⚠️ **原文只有要点转录，不是逐字全文**；下面左列的措辞尽量照原文，但别当引文用。

---

## 一、逐条核对（09-20 当场量的）

| 评审说 | 本仓量出来 | 判 |
|---|---|---|
| **CI 红两周** | **09-18 18:40 就绿了**；最近 5 次 3 绿 2 红，红的两笔正是它自己拆 lint/test 那两次 | ❌ **过期** |
| 门没接电：`preflight` / `delivery_facts` 等审计脚本不在任何门上 | 三个脚本在 `.github/workflows/test.yml` 和 `Makefile` 里**各 0 次命中**（`Makefile` 的门只有 `check: lint test-fast`） | ✅ **属实** |
| —（**它没说的**） | **`git status -sb` = `ahead 34`** ⇒ CI 最后一次真跑是 09-19 19:19，之后 34 笔**一次都没进过门** | 🔴 **新发现，比它说的更简单也更严重** |
| `escalate()` 恒 None | `_dispatch_crud.py` docstring 原话：「**当前恒返回 None** —— 这是设计事实，不是坏了」 | ✅ 属实 |
| `complexity` 字段被丢弃 | `workflow.py` 原话：「**只作记录** —— 两档已合并成单档」 | ✅ 属实 |
| `route_level` 一律 `any` | 只搜到 **3 处**硬编码 `"any"`，量不出"一律" | ◐ **存疑，没核到** |
| 建项目无鉴权、无多租户 | 无 `login_required` / `@auth` / session 校验；**且 `api_key` 是走 HTTP 写进 `.env` 的** ⇒ 比它说的多一层 | ✅ 属实 |
| `ready_tasks` / `list_all` 每个 tick 全表读盘 | **它漏了一个 2 秒 TTL 缓存**（`tracker.ready_tasks` 里那句 `ponytail: 2s TTL 缓存, 命中时跳过 glob+json.loads`）；缓存管不管用取决于 tick 间隔，**没量** | ◐ **部分对** |
| docs 是代码量的 71% | 看分母：只算 py = **78%**，加上前端 TS = **64%**。71% 落在中间，**没说分母** | ◐ 不可判 |
| 43 处自我作废 | 换一组关键词量出 **193** —— 差太远，**说明两边数的不是一回事**，复现不出它那个数 | ◐ 不可判 |
| 26 次「更正上一版的根因」 | 按 `更正|纠正|推翻|上一版的根因|我错` 数出 **17** 次 | ◐ 存疑 |
| 84 篇文档 | 实际 **87** 篇 | ◐ 小错 |

**数字口径**（复现用）：py 35319 行 · 前端 ts/tsx 7895 行 · `docs/*.md` 27485 行。

---

## 二、结论：大方向对，**一处归因错**

**同意的**：这项目缺的不是功能，是**两层** —— 一层几行（接线），一层要动数据结构（以周计）。
前 1700 次提交修的是「这一处怎么对」，接下来要修的是「**这一层改一处付多少钱**」。

**要反驳的一处**：它把「门没接电」归因到「**CI 红**」是错的 —— CI 早就绿了。
真因是**提交压根没推**：

- 按它说的，这是个**工程债**（要花时间搭）；
- 按实际，这是**漏做了一步**，几分钟能修；
- 而它**恰好就是「别人能接手」那条判据的唯一硬否定项** —— 别人 clone 下来拿到的是两周前的代码。

⇒ 它那句「接线类那 5 件 ≈ 一到两周」，**有一条要从里面划掉**。

**它标了「不确定」的三处**（模型方差与调度浪费分不开 / 单机 10 并发会先撞 `ready_tasks` / 用户数是 0），
**没有能反驳或坐实的依据** —— 认可它标了。

---

## 三、排序决定（09-20 记录，**等这轮真机跑完再动**）

**原则：先做「能让后面每一步变便宜」的，别先做「最正确」的。**

| 顺序 | 做什么 | 为什么排这儿 |
|---|---|---|
| **0** | **门接电**（三个审计脚本进 CI / Makefile） | **已写好、带自测、只是没插电**。它把「我验过了」变成「机器验过了」—— 其它判断的地基 |
| **1** | **先坐实**「产物产出被判『无文件改动』整份丢」的机制，**再改** | 唯一一类**负收益**故障：不是慢，是**白干**。09-20 那一轮 T3/T4/T6 三个中招。⚠️ **机制现在是假说**，拿假说去改就是又一次盲改 |
| **2** | **可机检的交付事实**（这轮到底交付了什么） | **没有它，后面所有改动都说不清有没有变好**。它同时是「门」和「斜率靶子」的输入 |
| **3** | 计划期臆造（`check.argv` 里写死的测试文件名） | 改**契约**、不改架构，消掉「唯一的不可伪造证据持续产假失败 / 假通过」 |
| **4** | 状态一致性（整对象覆盖写） | 值得做，但要**在有尺子之后** —— 散布 20+ 调用点 |
| **最后** | 修订成本凸性（架构整份替换） | **唯一的结构工程，也最不可逆** |

### 与评审的一处分歧

它把「修订成本凸性」当**唯一的真天花板**，暗示该先做。**天花板同意，顺序不同意**：

> **那件活的验收标准，恰恰要用第 2 步造的那把尺子。**

改数据结构之前得先能回答「改完是变快了还是变慢了」，而现在**答不出来**
（这正是它自己列的第 5 条缺口）。**没尺子就开最大的刀，是这个仓最贵的一课**
（清单上「度量先于改动」那条是拿血换的）。

---

## 四、三条路径：选第 3 条

**继续当自己的工厂，先把斜率量出来。**

- 它是**唯一能验证「这条路到底成不成」**的；
- 另外两条（拆件开源 / 转「AI 产物的验收台」）本质是**在没验证之前先改叙事**
  —— 一个缩小故事、一个放弃最有野心的那半句；
- 它给的靶子（「三轮内 6/11 → 9/11，且单轮 token 下降」）**必须先有第 2 步才量得出来**
  ⇒ 路径 3 和一个具体动作绑死，不是口号。

**建议：先跑三轮，拿数据决定要不要转。现在不决定路径 1 / 2。**

⚠️ 若要拆件开源，注意评审点名的三块里，`_machine_checks` / `merge.py` 恰恰是**真机还没验过**的
（见 `待验证清单-20260912.md`）—— **拆出去之前先让它们绿过一次**，否则是把没验的东西包装成产品。

---

## 五、我量不出来的（别当结论用）

- `route_level` 一律 `any` · docs 占比 71% · 「43 处自我作废」·「26 次更正根因」——
  换口径差很远，**这几条我没能复现**，所以没进第三节的排序判断。
- 受众规模、竞品判断：原文自己也标了是**公开横评摘要**、不是逐个试过 —— 没有独立依据。

---

## 六、原文最后一句（我部分同意）

> 「这个项目最稀缺的价值不在代码，在那份失败形状的完整记录。」

**方向同意**（「更正自己根因」的比例确实罕见）。但要补一句：
**记录的价值取决于它可不可传播** —— 而现在的形态**恰好不可传播**：
87 篇 md、散在两个仓（代码在奇点仓、待办在家目录仓）、待办靠一个 hook 每轮念给我听。
**这些设计全为「一个人 + 一个 AI」优化，换成第二个人一条都用不了。**
