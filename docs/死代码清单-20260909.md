# 死代码 / 僵尸代码清单（2026-09-09 扫描）

> 扫描方法：AST 解析 `src/` 全部 .py + `tests/` 作引用语料，对每个模块级/类级定义统计 Name-load 与 `.attr` 引用。
> 排除：测试文件、`@app.route`/装饰器注册、`import as` 别名、`__all__`/`import *` facade、仅字符串/注册表调用的。
> **删除前必须**：加一次 CI + `grep` 复验（尤其「中」置信度和 fusion-tool 批次）。

## 高置信度可删（全仓 0 真实调用）

| 符号 | 文件:行号 | 类型 | 说明 |
|---|---|---|---|
| `_run_committee` | _planner.py:69 | 方法组组长 | 0 调用；唯一线索是 _task_runner.py:35 未用 import；活跃委员会是 `_dispatch_exec._dispatch_committee` |
| `_synthesize_plans` | _planner.py:195 | 函数 | 仅被死节点 `_run_committee` 内部引用 |
| `_llm_synthesize` | _planner.py:237 | 函数 | 仅被死节点内部引用；_task_runner 的 `as _llm_synth` 别名未用 |
| `create_handoff` | handoff.py:38 | 函数 | 0 引用；交接实写 project.handoffs dict |
| `append_to_project` | handoff.py:130 | 函数 | 0 引用 |
| `record_perf` | _profiler.py:98 | 函数 | _task_runner 只调 `get_perf_stats` |
| `get_profiler` | _profiler.py:104 | 函数 | 0 引用 |
| `get_budget` | _token_budget.py:148 | 函数 | 0 引用（record_tokens/get_usage_stats 活） |
| `list_roles` | roles.py:267 | 函数 | 0 引用（get_role 活） |
| `list_personas` | roles.py:270 | 函数 | 0 引用 |
| `get_persona` | roles.py:273 | 函数 | 0 引用 |
| `select_topology` | router.py:127 | 函数 | 0 引用（route() 活） |
| `apply_time_decay` | model_profile.py:354 | 函数 | 0 引用 |
| `incremental_review` | validator.py:470 | 函数 | 0 引用 |
| `security_review` | validator.py:511 | 函数 | 0 引用 |
| `build_routing_hint` | task_templates.py:187 | 函数 | 0 引用 |
| `audit_log` | web/app.py:84 | 函数 | 0 引用 |

## 中高置信度（0 引用，建议 grep 复验）

| 符号 | 文件:行号 | 说明 |
|---|---|---|
| `set_trace_id` | log.py:26 | 0 引用（log_event/get_trace_id 活） |
| bridge 桥接批 `start_all/stop_all/emit_task_event/emit_system_event/emit_metrics_event/get_observer_server` | bridge.py:328/364/386/405/421/296 | 代码只用 start_ws/start_observer/stop_x/broadcast_json |
| `get_executor` | bridge.py:101 | 0 引用 |
| `list_skills` | skill_loader.py:215 | 0 引用（get_tool_definitions/get_agent_skills 活） |

## 中置信度

| 符号 | 文件:行号 | 说明 |
|---|---|---|
| `register_client` / `unregister_client` / `_send_to_client` | _observer_client.py:34/40/46 | 被 star 进 facade 但无人调；submit_question/_check_anomalies 活 |
| `is_running` | _observer_worker.py:96 | 0 引用；start/stop_observer 活 |
| `list_pending` / `next_ready` | tracker.py:272/279 | v3 用 ready/collect |
| `shutdown_all` | bridge.py:369 | 0 引用 |
| `get_server` / `init_server` | observer/server.py:272/277 | ObserverServer 类经 bridge 直接用 |
| `init_global_sampler` | observer/state_sampler.py:346 | 0 引用 |
| `guess_template` | task_templates.py:165 | 仅 tests 用 |

## 中低置信度（fusion-tool 整条链，仅内部互指）

| 符号 | 文件:行号 | 说明 |
|---|---|---|
| `get_fusion_tool_def` | execution_judge.py:211 | 顶部注释「AI裁判已移除…仅规划期 fusion」 |
| `execute_fusion_tool` | execution_judge.py:216 | 仅内部互调，无外部调用 |
| `fuse_outputs` | execution_judge.py:224 | 同上，整条链仅内部互指 |

## 需人工确认（不宜当普通死代码）

- **`_memory_experience` / `_memory_lifecycle` 统计函数**：`get_experience_stats`(_memory_experience.py:160)、`analyze_failures`(:106)、`find_similar_across_projects`(:127)、`get_insights`(_memory_lifecycle.py:246)。列在各自 `__all__` 里、被 `memory.py` 用 `import *` 转发，属 public facade，删除需业务确认。
- **`blog/` 子包整体**：`.flaskenv` 声明 `FLASK_APP=singularity.blog:create_app`，但 `python -m singularity.web.app` 才是真实入口；且 `blog/__init__.py:31` import 不存在的 `blog.models`。疑似废弃，需确认谁真正提供 Flask 应用。
- **`observer/mcp_server.py`**（`run_stdio`、`_build_mcp_tools`…）：仅被自身 `__main__` 调，疑似供外部 MCP 客户端手动拉起的独立进程。
- **`tracker.py` 的 `ExecutionMode`(:39)**：public，全仓无人引用，可能过期枚举，查 git 历史。

## 疑似但可能动态引用（勿删）

- `_api*.py` 大量 handler（经 `_api.py` `import *` 汇聚成 facade → web/app.py 调用），单个名字无直接引用属正常。
- `_cli*.py` 的 `_cmd_*`（`__main__.py` 用 argv 匹配 + `import *`，注册表驱动）。
- `_observer_tools` 的 `_tool_*`（SSE 事件类型名、注册表 dict、JSON 模型字符串）。
- `dispatcher._dispatch_committee` 等已在 `__all__` 的。
