# 死代码清理记录 + 恢复手册（2026-09-09）

> ## ⚠️ 定位声明
>
> **本清单是清理记录 + 恢复手册，下次删除前必须重新 grep 复验，不得照单删除。**
>
> 清单会过期，复验不会。9/8 初版清单就有 5 条误报（下面「初版误报」一节），
> 照单执行会删掉正在跑的代码；这次（`c15914e`）能安全删，靠的是**逐条复验 + 删除后复扫**，
> 不是靠这份清单本身。

---

## 一、恢复手册

被删的 41 个定义全部可从 git 历史恢复（`c15914e^` = `d182736`）。

```bash
# ① 全部恢复
git revert --no-commit c15914e && git commit -m "revert: 恢复死代码删除"

# ② 只恢复某个文件（例：roles.py）
git checkout c15914e^ -- src/singularity/scheduler/roles.py

# ③ 只恢复某个符号（例：get_profile）—— 从旧版本捞出来，贴回当前文件
git show c15914e^:src/singularity/scheduler/permission.py | grep -A 3 "def get_profile"

# ④ 查看这次删了什么
git show --stat c15914e
```

> 注意：`c15914e` 已 push，**不要用 `reset`** 改写历史，只能 revert 或 cherry-pick 回补。

---

## 二、本次删除（`c15914e`，41 个定义）

36 个复验候选 + 删除后连带暴露的 5 个孤儿（`get_active_agent` / `switch` /
`run_parallel_models` / `shutdown_all` / `available_apis`）。18 个文件，+2 / −487 行。

| 符号 | 定义位置（删除前） |
|---|---|
| `ExecutionMode` | `tracker.py:39` |
| `_tool_read` | `executors/openai_agent.py:453` |
| `advance_to` / `is_at_gate` / `spend_tokens` / `over_budget` | `project.py:149/181/192/204` |
| `assign_agent` / `get_agent` / `get_agent_for_task` / `get_assignment` / `list_agents` / `list_assignments` / `switch_agent` / `get_active_agent` / `switch` | `roles.py:236/212/253/230/207/233/242/222/152` |
| `build_routing_hint` | `task_templates.py:187` |
| `emit_task_event` / `emit_system_event` / `emit_metrics_event` / `get_executor` / `get_observer_server` / `stop_all` / `shutdown_all` | `bridge.py:386/405/421/101/296/364/351` |
| `get_fusion_tool_def` / `execute_fusion_tool` / `run_parallel_models` | `execution_judge.py:211/216/211` |
| `get_metrics_for_monitoring` / `init_global_sampler` | `observer/state_sampler.py:318/346` |
| `get_parked` / `parked_ids` | `merge.py:107/104` |
| `get_profile` | `permission.py:120` |
| `get_profiler` | `_profiler.py:104` |
| `get_server` / `init_server` | `observer/server.py:272/277` |
| `incremental_review` | `validator.py:470` |
| `list_pending` / `next_ready` | `tracker.py:293/300` |
| `list_skills` | `skills/skill_loader.py:215` |
| `rank_by_pattern` | `model_profile.py:207` |
| `select_topology` | `router.py:127` |
| `available_apis` | `api_store.py:243` |

### 执行记录

1. **逐条复验**：36 个全部 0 引用、无 `__all__` 残留 ✅
2. **删除后复扫**：连带暴露 5 个新孤儿（原先只被被删函数引用）→ 一并删除，
   第四轮复扫收敛到 **0**
3. **清理残留注释**：`_cli_tasks.py` 中引用已删 `list_pending()` 的注释
4. **验证**：`pytest tests/test_scheduler/ -q` 306 passed；`pytest tests/ -q` 315 passed；
   `tests/test_exec_run.py` 28/28；端到端 (a) 语法错误被拦截 (b) 合法文件正常合并；
   6 个边缘脚本全过；`import singularity.web.app` 正常
5. **全仓补扫**（含前端/配置/文档/脚本，见下「盲区」）：41 个名字 **0 残留**

---

## 三、删除被删掉的"未使用的公共 API 面"

这些不是"坏代码"，是**当前没人用、但语义上属于对外接口**的东西。按 YAGNI 删了。
以后要这些能力，按「一、恢复手册」捞回来即可（不是重写，是恢复）：

| 能力 | 符号 |
|---|---|
| 查单个权限 profile | `PermissionStore.get_profile` |
| 角色管理 | `roles.py` 的 9 个（list/get/assign/switch 一组） |
| 项目状态机手动推进 | `ProjectState.advance_to` / `is_at_gate` / `spend_tokens` / `over_budget` |
| 队列自省 | `tracker.list_pending` / `next_ready`、`merge.get_parked` / `parked_ids` |
| bridge 生命周期 / 事件 | `stop_all` / `shutdown_all` / `get_executor` / `get_observer_server` / `emit_*` |

---

## 四、盲区（静态 grep 扫不到的）

删除依据是"仓库内 0 引用"。这个依据有边界，必须知道：

| 已扫 | 结果 |
|---|---|
| 全仓文本（含 `web/frontend/`、`.qidian/*.json`、`*.toml`、`docs/`、`data/`、`skills/*/SKILL.md`、脚本） | 41 个名字 **0 命中** |
| `getattr(` 全部用法 | 10 处，全是已知对象取属性，**无按名字动态派发** |
| `def __getattr__` | 全仓 **0 个** |
| `globals()[...]` / 注册表字符串派发 | **0 处** |

**剩余盲区（扫不到，客观存在）**：
- 仓库**外**的消费者（自己的脚本、别的仓库 `import singularity`）——本次就有一例：
  验证脚本 `/tmp/qidian_fix_check.py` 用了被删的 `get_profile`，删完才暴露。
- 未来新增的代码若按名字动态调用。
- 判断标准是"仓库内 0 引用"，**不等于"没人用"**。

---

## 五、复核方法（v2）

AST 收集 `src/` 全部顶层 def/class 与类方法 → 对每个名字统计全仓（`src/` + `tests/`）
非定义行引用数。**排除四类已知误报**：

1. **带装饰器的定义** —— Flask 路由（`@app.route`）、工具注册等，靠注册表调用
2. **`visit_*`** —— `ast.NodeVisitor` 反射调用
3. **facade 文件** —— `_api_*.py` / `_cli_*.py` / `_dispatch_*.py` / `_memory_*.py` /
   `_observer_*.py` / `web/app.py` / `blog/`，名字经 `import *` 汇聚
4. **`__*__`** 与注释行

> 首轮扫描没排除这四类时，报了 124 个"零引用"，其中 Flask 路由（`spa`、`api_task_submit`）
> 和 AST visitor（`visit_Call`）都是误报。**收窄后 36 个，逐条复验后 0 误报。**

---

## 六、初版误报（已接线，**勿删**）

初版清单写于 9/9 扫描时，之后 `76a650f`（"删死代码 + 接线 5 个残留函数"）
**把这 5 条接线成了活代码**，但清单没同步：

| 符号 | 定义 | 真实调用点 |
|---|---|---|
| `record_perf` | `_profiler.py:98` | `orchestrator.py:150` |
| `apply_time_decay` | `model_profile.py:356` | `model_profile.py:303` |
| `security_review` | `validator.py:511` | `_review.py:103` |
| `audit_log` | `web/app.py:84` | `web/app.py:878/1081/1347/1379/1512` |
| `set_trace_id` | `log.py:26` | `_task_runner.py:94` |

---

## 七、未删除：仅测试引用（9 个）

`_reorder_agents_by_rank`(`_task_runner.py:65`) / `architecture_redo`(`project.py:171`) /
`fallback_for_tier`(`model_registry.py:233`) / `get_stats`(`route_learner.py:91`) /
`get_success_rate`(`route_learner.py:76`) / `guess_template`(`task_templates.py:165`) /
`models_for_provider`(`model_registry.py:228`) / `rank_candidates`(`route_learner.py:83`) /
`sample_current_state`(`observer/state_sampler.py:359`)

它们**有调用者**（测试），删了要连带删测试、净减覆盖。属"未使用的 API 面"，
是产品取舍不是死代码 —— 保留待定。

---

## 八、下次删除前置（必做）

1. 逐条 `grep -rn "\b<符号>\b" src/ tests/` **重新复验**（不得照单删）
2. 补扫全仓非 Python 语料（前端/配置/文档/脚本）与动态调用形态
3. 删完**复扫一轮**抓连带孤儿
4. 跑 `pytest tests/test_scheduler/ -q` + `pytest tests/ -q` + `python tests/test_exec_run.py`
   + `import singularity.web.app`
5. 单独一个 commit，便于 revert

> **循环导入说明**：`_workflow_phases` / `_dispatch_exec` 若作为首个 scheduler 模块
> 被导入会报 partially-initialized —— 已用 `git stash` 在删除前代码上复现，
> **属既有设计（延迟导入破循环），与本次删除无关**；真实入口 `web.app` 导入正常。

---

## 九、仍需人工确认（不宜当普通死代码）

- ~~**`blog/` 子包整体**~~ → **已删（2026-09-11）**：import 双断裂已实跑确认（缺 `flask_sqlalchemy` + `models.py` 不存在），`.flaskenv` 已改指真实入口 `singularity.web.app`。以下为原始记录：
`.flaskenv` 声明 `FLASK_APP=singularity.blog:create_app`，
  但 `python -m singularity.web.app` 才是真实入口；`blog/__init__.py:31` import 不存在的
  `blog.models`。疑似废弃。
- **`observer/mcp_server.py`**：仅被自身 `__main__` 调，疑似供外部 MCP 客户端手动拉起。
- **`_memory_experience` / `_memory_lifecycle` 统计函数**：列在各自 `__all__`，
  经 `memory.py` `import *` 转发，属 public facade，删除需业务确认。
