# 未完成清单

> 每条一行。**做完就删掉这一行**（别留 ✅ —— 这份文件只装"还没做的"，留了会越长越没人看）。
> 规矩：我提任何"该做"的事，**先在这里写一行才准提**。
> **详细分析不写这儿** —— 归仓库 `docs/`：踩坑 → `docs/防御模式.md`；没验过的 →
> `docs/待验证清单-20260912.md`。（2026-09-12 夜定的：这里只留指针，别当文档用。）
> **2026-09-13 清过两遍**：① 删掉已闭环的 §60；"三轮合计状态"压成指针（那份分解本来就在
> `待验证清单` 里，抄一份必漂）；数字逐行重数 → 全文存 `OPEN.20260913-0145.bak.md`。
> ② 当晚 23:2x 又清一遍：删掉三条**已经做完、只是留着当设计说明**的（外派包答卷 / 净增量 /
> 轮 5 越界）—— **删前逐条核过留底**（前两条分别在 `docs/架构级静默失败-外部答卷-20260913.md`
> 和代码/脚本里；越界那条在 `_flag_file_overlap` 的 docstring + `待验证清单` 两行里）。
> 顺手把「告警按 key 聚合」也删了（两半都真机验过，且设计说明在 `witness` 的 docstring 里）。

- [奇点] 🔴 **接手指针（2026-09-16 凌晨更新）** —— **先看这条**：

  **⚠️ 环境已清干净（09-16 13:4x，用户拍板清的）**：三个真机项目**全部删除**，
  证据在 `~/项目/_归档-20260916-真机验证证据/`（**`README.md` 写清了来历** ＋ 三份 `git bundle`
  ＋ `产物/` 里那份 **40 个测试全过**的源码 ＋ 告警摘录）。
  ⚠️ **账本没清**：`usage_daily.json` / `token_usage.json` / `memory/{edges,events}.json`
  里还留着这三个项目 id（那 ~46.9 万 token 是**真花的**，删了等于改账）—— 见到"孤儿项目 id"别当故障。
  · **后端现在是停着的**，要跑就起：
    `nohup .venv/bin/python -m singularity.web.app > /tmp/qidian.log 2>&1 &`
  · ⚠️ **`auto_mode=False` ⇒ GATE1/GATE2/GATE3 都要人点**（`_workflow_phases.py:450`）
  · 点门：`POST /api/projects/<id>/gate-confirm` body `{"gate":"gate1/2/3","decision":"approved"}`
  · 续跑：`POST /api/projects/<id>/start`（**从当前阶段接着跑**，不用重跑调研）
  · 看阶段：`GET /api/projects/<id>` 的 `phase`；curl 记得 `--noproxy '*'`

  🎯 **根因已坐实（09-16 晚，翻存档翻出来的）—— 原先记的「baseline_ref 空」是错的，
  那条探针不用放了，省一晚。**
  🔴 **`baseline_ref` 那天全程非空。** 铁证在 trace 自己身上：`snapshot_ref: 9311d2b…`，
  而它就是喂给执行器的那个值（`snap.ref` → `ctx.snapshot_ref` → `_exec.py:555`）——
  **同一次 run 里不可能既非空又空** ⇒ 任务执行器**不可能**是 `no_baseline_ref` 的来源。
  🔴 **真出处在同一份 trace 里，一直没人看**（`unverified` 那段）：
  `"执行器到达总预算主动收尾: 到达执行预算 810s，主动收尾（工具轮 0 次，改动 0 个文件）"`
  配 `elapsed: 0.039` —— **这个执行器只活了 39 毫秒**：`budget_s ≤ 0`
  ⇒ `_deadline_at` 落在过去 ⇒ `openai_agent.py:437` 第 1 轮开头 `_wrapped=True` 立刻收尾。
  侧车对上：最后一次 dispatch 时刻 **13:03:42.7**，而任务死线 **13:03:37.8**
  （`started_at 12:48:37.8` + 900s）—— **它是在死线之后 5 秒发起的，注定什么都拿不到**。
  ⇒ **真链条**：
  ```
  已过死线还发起了一次 dispatch
    → 新建执行器预算 ≤0 → 0.039s 空收尾（0 轮 / 0 文件 / 0 token）
    → 这具空壳成了 BatchOutput.dispatch_result，**盖掉前几轮真干出来的那份**
    → _task_runner: changed=[] → QA「无文件改动」→ failed
    → 且收尾那条 return 没带 merge_request ⇒ 已 commit+锚定的产物**进不了合并队列**
  ```
  （`refs/qidian/pending/1789534116538 → 1974746` 还在，正是前几轮 `commit_wt`+`_anchor_ref` 留下的。）
  🔴 **`no_baseline_ref` 那 122 对告警是另一码事、且是虚惊**：来自**审查/QA 的 dispatch
  从不传 `baseline_ref`**（`validator.py:447/571/805/867` 只传 `cwd`），跟任务执行器无关。
  出现次数恒为 2（`oa_exec` + `claude_cli` 是**同一次调用**的两条），跨 09-13→09-16 共 122 对。
  ✅ **开工前先看一眼**：存档 `产物/` 里那份拼起来 `pytest 40 passed` ——
  **"产物能看懂"这条北星判据那天是满足的**，是系统没认出来。别从"模型不行"起手查。

  🔴 **剩下没做的只有一件（③）**：
  · **③ 那 122 对假告警怎么消** —— ⚠️ **不是"顺手加个参数"就能了**，别照我上一版说的做。
    查下来：`multi_model_review` **本来就收到了正确的 `base_ref`**（`_review.py` 传的
    `_diff_base(snap)`），它自己拿去做 git diff 了，**只是没转手给 `_dispatch.dispatch`** ——
    而那几个路口执行器的 `changed_files` **没有任何消费者**（调用方读的是 `raw_output` 里的 JSON）。
    所以"补参数"只是让一句本来就无意义的告警闭嘴，还会顺带在**那个 `cwd` 指着的仓库**里
    跑一次真 `git diff`。而 `validator.py:447` 那处 `cwd` 可能是**空串**（`cwd or ""`）⇒
    `_git_changed_files` 会退到 `config.PROJECT_ROOT`（**奇点自己的仓库**）……
    **那正是本仓栽过三次的"读错仓库"**（见下面那条 pending ref）。
    ⇒ 动手前先定：**这几个路口到底该不该"追改动"**（它们只产一段 JSON，不碰磁盘）。
    候选：走 `no_tools` 那条路（同 `_run_no_tools` 的理由），或给执行器一条
    "别追改动"的显式开关 —— **两条都会动到评审路径的行为**（评审正是质量门禁），
    所以不猜，留在这儿等人拍板。

  ✅ **09-16 晚修掉的（自检 + 变异验证 + 全量 1503 绿；⚠️ 真机没验）**：
  · **死线后不再发起 dispatch** —— `_exec.run` 的 turn 循环 + `_run_with_retry` 的重试循环
    各加一道 `_budget_exhausted`（与 `_dispatch_budget_s` 共用同一判据，不写第二份算法）；
    `_run_with_retry` 在到点且有上一轮结果时**交回上一轮那份**，而不是开一个注定空的 run。
  · **收尾那条 `return` 补上 `merge_request`**（原来漏了，前面几轮 build 出来的合并请求
    到这儿整份丢掉，`orchestrator.py:494` 只看 `batch.merge_request` ⇒ 永远不合并）。
  · 三条自检在 `tests/test_exec_run.py` 路径 14/15/16；**变异验证**：拆掉任一根对应那条就红，
    其中**拆掉 turn 循环那道 guard 复现出的正是真机症状**（`changed_files: []` + `merge_request: None`）。

  ✅ **09-16 修掉的（都真机坐实 + 变异验证 + 全量 1503 绿）**：
  · `5d4f271` **流式总时长判据永远轮不到** —— 判据写在 `iter_lines()` 的**循环体里**，
    而"服务端一直发字节、却凑不满一行"时循环体**一次都不进**；`read=` 那把尺又被
    "有字节进来"**重置** ⇒ **两条保护同时失效**。真机两轮各卡死 30+ 分钟、
    `stream_over_budget` **0 次**。改用 `iter_text()`（每批字节都过判据）。
    **同形第二处** `execution_judge._stream_once`（融合那条，**连判据都没有**）一起改。
    复盘 → `docs/防御模式.md` **§73**（含三种"服务端不吐东西"的实测对照）。
  · `3a73b30` **MPS 并发段错误** —— 同一个 `SentenceTransformer` 被多线程同时 encode
    ⇒ **整个后端进程无声消失**（没有 traceback）。委员会多模型并行正好踩中。收进一把 RLock。
    复盘 → **§74**。⚠️ 手法记牢：**"进程消失且没有 traceback" 要去
    `~/Library/Logs/DiagnosticReports/` 找崩溃报告**，日志里一个字都没有；
    `sample <pid> 3` 是 `py-spy` 的**免 root** 替代（py-spy 在 macOS 要 sudo，本会话给不了密码）。
  · `93376a7` **F2：判据盯错信号** —— `_STALL_TIMEOUT` 的语义本来是「多久没**新 token**」，
    实现却在量「多久没**字节**」⇒ 服务端一直发字节（保活 / 半截分片 / 完整行但 delta 全空）
    它就永不触发，一次调用可以烧满 240s 而**零产出**。改成比 `content/reasoning/tool_calls`
    的**长度**，**变长才算进展**；超时**抛错换 agent**（不能"出声后返回空"——
    那会被上层读成"这次调用成功了、只是模型没说话"）。复盘 → **§75**。
    ⚠️ **真机没验到**：那一轮**没有**出现停滞错误 ⇒ 它的前提"没有任何可用进展"当时不成立。

  ✅ **修完当场闭环了（这是今晚最值钱的一段）**：
  `01:15` 用新代码续跑 → `~01:51` **融合那条路又卡住**（和昨晚同一个形状）
  → **`02:01:48` `stream_over_budget:fusion:600s` 判据响了、切断**
  → **`02:04:27` `planning → gate2 架构完成`**。
  **昨天它会永远挂在那儿；今天 600 秒切断、两分半走完规划。**
  ⚠️ **这也把"卡的是哪条路"坐实了 —— 是 `execution_judge`（融合），
  不是 `openai_agent`。我原话是"没坐实"，只改 `openai_agent` 的话今晚照样过不去。**
  （细节与三种长相的实测对照 → `docs/防御模式.md` §73；MPS 那条 → §74。）

  🔵 **新增（09-16 凌晨，我提的）**：**拿一个真实规模的项目跑一遍** —— 不是 fizzbuzz。
  事实：十几次真机跑**全是同一个 FizzBuzz**（或 txtstat/wordcount 这种单文件小工具），
  **真实规模一次没跑过**。"它是不是花架子"只能靠这个实验回答，猜不出来。
  判据不是"跑通"，是三样：① 崩在哪一步（同族还是新类）② 崩几个 ③ 中途产物能不能用。
  ⚠️ 大概率崩，而且比 fizzbuzz 崩得多 —— 但成本是一晚上，换来"天花板在哪"。

  🔵 **新增 ②（09-16 凌晨，我提的）**：**拿 `docs/防御模式.md` + 今晚那次定位过程给 AETRIX 看。**
  这是"奇点有没有竞争力"唯一能一天验完的办法。判读：
  · 他反应是"这不就是个 bug 吗" → **没竞争力**；
  · 他反应是"这我也踩过、但我没记下来" → **竞争力在这儿**。
  ⚠️ 我的判断（n 很小，别当结论）：奇点的竞争力**不在"替你写代码"**，在
  **"它知道模型会怎么坏、并且留下痕迹"** —— 今晚我靠 alerts/心跳/账本/trace 四样
  在 30 分钟内定位了一个 36 分钟的静默挂起，多数 agent 框架卡住时你什么都看不到。

  🎯 **「至今没有一次完全交付」—— 用户 2026-09-14 晚原话**：
  > 到现在为止奇点还没完全交付一个项目呢，
  ⚠️ **归属更正（09-16 晚，用户问"北极星是啥我怎么不记得我说过"时查的）**：
  **用户说的是这句事实/担忧；「北极星」这个名字和下面那五条判据是我拟的**，
  而我当时在 `docs/演化史.md` 和这里都标成了「用户提的」——**标错了**。
  ⇒ **这五条从没经用户批准**，随时可改。
  **五条判据（⚠️ 我拟的，未经批准）**：
  任务无 failed · QA 有结论 · 交付清单三项非空 · 产物能看懂 · 账本记一行。
  🔴 **它本身有病（09-16 晚自查）**：五条里**三条在量账簿、不在量交付**
  （任务终态 / 交付清单 / 账本），而唯一碰产物的那条「产物能看懂」**是主观的、要人看**。
  后果实证：真机上**任务全 failed、manifest 三项全空，产物却是完整且 40 个测试全过的**
  —— 按这五条判"零交付"，按产物判"已交付"。**两把尺给出相反答案。**
  ⇒ **要动这份判据**（怎么给"产物"那条更大的权重），**得用户拍板**，别我再自拟一版。
  ⚠️ 09-15 补一条：**「任务无 failed」不等于「有产物」** —— 真机上三个任务全 `done`、
  产物却是 0 字节。**「产物非空」已作为硬判据进了验收层**（§0.16 第三层）。

  **⚠️ 那些"没验到的"（别记成验过了）**：
  · **① 流式总时长 —— ✅ 09-16 凌晨验到了，结论是判据不工作**（`5d4f271` 修了）。
    这正好印证原来那句 **"判据没触发 ≠ 修好了"** —— 触发之后发现它是坏的。
  · **② QA 契约 —— 间歇性**（同一个后端进程里一次成一次败，单列在下面那条）。
  · **③ 只读豁免 —— 半验**。
  · **当晚 6 条修复**（`2e8537f` / `0e62d96` / `a355940` / `1849b1c` + 白天三条）**都没上真机。**
  · 🔴 **今天修的三条（`2d6d6ab` 交付报告栏 + `5d4f271` 流式 + `3a73b30` MPS）
    也只有自检（变异 + 全量绿）—— 真机还没跑过完整流程。** 别记成验过了。

  **④ 两个数**：静默 except 棘轮 = **401** · `pytest tests/` **1497 绿**（0 skipped）。
    ⚠️ 尺子有已知盲区：handler 的出声写在**本地 helper** 里时它看不见 ⇒ 会平白涨基线。
    ⚠️ 09-15 加「产物非空」那条时**被它当场抓到一次**（静默 except）—— **它是管用的**。

  **⑤ 三条不能忘的**：
    · `stash@{0}`（`rollback-protect:b`）和 `stash@{1}`（`b1`）**我没删**
    · 外派⑥写的样板在 `src/pages/__rewrite_sample__/`，**已从默认收集里排除**；
      单跑用 `npx vitest run --config src/pages/__rewrite_sample__/vitest.sample.config.ts
      __rewrite_sample__`（**config 路径要写全**，只写文件名会报 resolve 失败）
    · **`ruff` 现在是装着的**（dev 依赖）—— 不装的话 `test_no_undefined_names.py` 那三条
      F821 闸门会**静默 SKIP**。**清上下文/换机器后先确认它还在**

  **⑥ 别的条目各是什么**：P1 三臂实验（观测点已补，**只差跑**）· 真机验证后半
  §0.5/§0.6/§0.8–§0.11（**人做的**）· 以及下面那些"还没修的洞"。
  ✅ 已收口：结构性那条（水位触发）· ⑬ 审测试判据 60 份 · 外派窗口 ⑪⑫⑬（账在 `docs/演化史.md`）。
- [奇点] 🔴 **委员会席位放 `claude-cli` 类执行器 ⇒ 禁工具拦不住**（09-16 晚查出来的，**现在是睡着的**）：
  **事实（读码确认）**：委员会席位是**按 agent 类型**派发的（`_run_no_tools` 走 `_EXECUTOR_BY_TYPE[etype]`），
  所以"委员会里用不同 agent"**不是新能力，它本来就有**。但 `honors_no_tools` 只有
  `openai_agent` / `anthropic_api` / `zhipu_api` 三家是 `True`，**`claude_cli` 没有**（基类默认 False），
  而 `_run_no_tools` **不过滤**——只在 `_dispatch_exec.py:333` 出条告警就往下跑。
  ⇒ 席位一旦是 claude-cli，**它会带着工具进委员会**，而"禁工具"是委员会的前提
  （原话：纯文本出方案，别改磁盘）。同形的真事：2026-09-11 调研员在**项目仓里把整个项目实现完了**。
  ⚠️ **现在没炸是因为阵容里一个 claude-cli 都没有**（`.qidian/agents_custom.json` 三个全是 `openai-agent`，
  `agents.toml` 是空占位）—— **加一个进去就醒**。
  **没动手**：修法有取舍（把链过滤成 `honors_no_tools` 的 / 直接拒 / 给 claude-cli 补只读工具面），
  且跟下面那条"要不要给委员会换/加席位"是同一个决定。
- [奇点] 🟡 **"委员会换/加席位"这个念头 —— 先别做，基础问题还没验**（09-16 晚，用户提的）：
  用户的想法是"委员会可以有适配 agent 用"。我的判断（**没动手**）：
  · **地基没验**：委员会到底比"一个模型自己改 N 轮"强不强？**P1 三臂实验就是回答这个的**，
    方案在 `docs/P1-三臂实验方案-20260912.md`、观测点也补了（`f7fda40`）、**只差跑**。
    在没验过的地基上加一层，是给未知量乘系数。
  · **本仓已经证伪过一次"给委员会加料"**：席位视角（同模型装不同人）A/B 盲评 **31 vs 32，输**，
    已默认关闭（`QIDIAN_COMMITTEE_PERSPECTIVE`）。"加一层让它更丰富"这个直觉在这系统上被打过一次脸。
  · ✅ **唯一值得记的新轴**：如果席位的价值不是"多一个模型的意见"，而是**"它能读仓库，带回来的是事实不是意见"**——
    那和已证伪的"视角"**不是一回事**。要做就得是**只读**席位（能读不能写），
    且得先有只读工具面（"只读豁免"目前**半验**）。⚠️ 成本：委员会是 N 路并行全量调用，
    带工具循环的席位会把架构阶段的 token 再翻几倍（实测三个 fizzbuzz 已烧 46.9 万）。
  ⇒ **顺序**：先跑 P1。委员会赢不了单模型多轮自改 ⇒ 加什么席位都白搭；赢了 ⇒ 才知道下一个轴该量什么。
- [奇点] 🔴 **「符不符合预期」没有一条能跑的命令 —— 这是「bug 修不完」的因，不是果**（09-16 晚，用户说没信心时查的）：
  **事实**：北极星那五条判据**全是盘上现成的数据** —— 任务终态（`tracker`）· QA 结论（`qa_report.json`）·
  交付清单（`.qidian/deliverables/<pid>/delivery_manifest.json` 的 `artifacts/docs/reports`）·
  产物（项目仓 + `refs/qidian/pending/*`）· 账本（`token_usage.json`）。
  **但没有任何一处把它们汇成一句「这次算不算交付」。** 实测最后一个真机项目的 manifest 是
  `artifacts: [] / docs: [] / reports: []` —— **数据在，判断没有。**
  **后果**：每次回答「成没成」都得人肉翻 trace ＋ 侧车 ＋ 告警 ＋ bundle（今晚我干了一整轮才敢下结论）；
  而**修 bug 看不出「还差几个」** ⇒ 每修一个又长出下一个 —— 「修不完」的体感就是从这儿来的。
  ⇒ **该做**：一条能跑的命令，吃一个 `project_id`，逐条打五行 ✅/❌ ＋ 一句为什么。
  **数据全现成，估 ~40 行，不新增机制、不碰调度器。** 这是把「有没有信心」变成可验证问题的唯一办法。
  🔴 **还有第二把尺（09-16 晚补，用户说「这都修了好长时间了」时想起来的）**：
  上面那把**量不出"在不在收敛"** —— 「任务无 failed」在 09-11（三个任务全撞 900s、事后查不出干过什么）
  和 09-16（产物完整、40 测试全过、最后一环没接上）**都是 ❌，量不出这两者天差地别**。
  真该量的是：**最近 N 次真机跑，「崩在哪一步」是不是在往后挪？**
  ✅ **09-16 晚数完了（用 `.qidian/process_ledger.json`，不是读文档）—— 结论：在收敛。**
  ```
  时间          项目                        任务    review失败
  09-12 13:54  探路3-基线-txtstat          0/3         5
  09-12 14:03  AB-B-wordcount             3/3         0
  09-12 23:26  验收轮2-txtstat             1/2         0
  09-15 19:50  测试01                      2/3         0
  09-15 22:41  第三次-只读与QA              3/3         0
  09-16 00:19  验证-三样-20260915-2219     3/3         1
  ```
  · **任务层已收敛**：09-14 时是「4 个项目、任务全绿的只有 1 个」，现在是「6 个项目、
    全绿 3 个」，而且**最近两次连着 3/3**（以前从没有过）。
  · **崩点在往后挪**：09-12 那批 100% 崩在 `executing`（900s / 任务级失败）；
    09-13 晚第一次出现「崩在验收层」；09-15 两次真走到交付。
  · 🔴 **但 `制品=0` 六个项目**全中 —— **而那正是"零交付"这个印象的来源**，
    且它已经被判成记账 bug（`2d6d6ab`，真机没验）⇒ **"没信心"里有一大块是被这个 bug 造出来的。**
  · ⚠️ **别把 `delivery: ok` 当成功**：探路3 是 `0/3` 也 `ok` —— 它只说明"交付那一步跑了"，
    不代表有东西。真正的质量信号是 `tasks_done`。
- [奇点] 🟡 **原始意图「收编 Claude/Codex 当代理」离现在多远 —— 查清了**（09-16 晚，用户提的）：
  **Claude 那半代码层面已经做完了**：`claude_cli.py`（178 行，调 `claude -p`）＋ `anthropic-api`
  两个 executor 都在 `_EXECUTOR_BY_TYPE` 里（全表就 **4** 个：claude-cli / zhipu-api /
  anthropic-api / openai-agent —— ⚠️ 我前一轮口头说的"六个"是错的，这里更正）。
  **它没跑，是因为阵容里没配**（`.qidian/agents_custom.json` 三个全是 `openai-agent`）。
  🔴 **而且连钥匙都没有**：`.qidian/api_store.json` 里只有 deepseek / kimi / zhipu，**没有 Anthropic**
  ⇒ 现在就算配一个 claude-cli agent 也调不动。**Codex 那半确实没有**（无对应类型）。
  **真门槛是三个硬伤**（都在 `claude_cli.py` 上，本次查的）：
  ① 禁工具拦不住（`honors_no_tools` 没设）② 记账靠正则猜 stdout（"未匹配返回 0"）
  ③ 权限闸门落不了地（界面显示"已绑定"，实际一条拦不住）。
  ⇒ 这三条**不是"以后再说"，是原始目标的入场券**。
  ⚠️ **但别因为怀旧把方向掰回去**：北极星那条（至今没有一次完全交付）跟执行器是谁**无关**，
  加任何 harness 都不会让它交付得更完整。真要验，最小一步 = **配 key ＋ 配一个 claude-cli agent
  跑同一个任务跟 deepseek 对比**（一次任务的钱），不是接 Codex、不是改架构。
- [奇点] 🟡 **想把 Google Stitch 挂进来当能力 —— 查完了，卡在「它可能只认 OAuth」这一条**（09-16 晚）：
  **奇点这头（读源码确认）**：`scheduler/mcp.py` 的 HTTP 传输是**一次朴素 POST**
  （`httpx.post(url, json=rpc)`，`headers` 可从 `.qidian/mcp_servers.toml` 配）—— 但
  **没有 SSE 分支**、**不处理 `Mcp-Session-Id`**、也不发 `Accept: application/json, text/event-stream`，
  `protocolVersion` 写死 `"2024-11-05"`（旧）。⇒ 对方若是标准 Streamable HTTP，可能连不上或在 initialize 就被拒。
  **Stitch 那头（⚠️ 只有二手来源，官网和仓库抓取都被证书错误挡了，没逮到一手文档）**：
  端点 `https://stitch.googleapis.com/mcp`；官方工具就 6 个（`create_project` / `list_projects` /
  `get_project` / `list_screens` / `get_screen` / `generate_screen_from_text`）——
  **没有"取代码/取图"**，那些是第三方包自己加的。认证两种：`X-Goog-Api-Key` 或 ADC/OAuth。
  🔴 **最要紧的一条**：Google 官方论坛有人报 `generate_screen_from_text` **拒 API key**
  （要 OAuth access token），而奇点只会设**静态 header**、没有 token 刷新 —— 真是这样就只能列项目、生不成图。
  **这条没验，别当结论。**
  ⇒ **下一步是 5 分钟探针，不是写代码**：拿一把 key（`stitch.withgoogle.com/settings`），
  用 `POST /api/mcp/servers` 挂上（**别手改 toml** —— 见 [[config-changes-via-api]]），
  看 `GET /api/mcp/tools` 出不出得来；再真调一次 `generate_screen_from_text`。
  **两个都过 = 能接；只过第一个 = 得补 OAuth 刷新**（那时才排得上写代码）。
  ⚠️ 另：奇点的 MCP 工具是**全局**的（`_dispatch_skills._load_mcp_for_agent()` **不收 agent 参数**）——
  挂上去**所有有工具面的 agent 都会拿到**，当前没有"只给某个 agent"的开关。
- [奇点] 🔴 **一次调用卡 240s 却一个字没拿到 ⇒ 810s 预算被烧光 ⇒ 任务判死、产物不合并**
  （09-16 上午真机，**全链已坐实**）：
  **现象**：项目 `1789491433607` 跑到 executing，T2/T3 failed；
  `test_fizzbuzz.py` **185 行**躺在项目仓的 **`refs/qidian/pending/1789516515849`**
  （= `3dec2ad`；⚠️ 我一度叫它"悬空提交"，**说错了** —— 它是系统**有意保留**的，
  见下面那条「pending ref 是地雷」）上，配上 T1 已合并的 `fizzbuzz.py`(86 行)，
  拷出来跑 **25 个测试全过** —— **活干完了，产物没进去**。
  **链条**（trace 顶层 `unverified` 里写着，一处不编）：
  ```
  provider 抽搐（流吐 0 字）
    → 每次调用烧满 240s 上限（`stream_over_budget:329s/696s/900s, chars=0`）
    → 执行器撞自己的 810s 预算 → `deadline_wrapup`
    → verdict=未知/action=abort（`_exec.py:594`）
    → break → `escalate()` 恒 None → `no_escalation_path`
    → 任务 failed → 不合并
  ```
  ⚠️ **更正**：我先前把账记成"post_hook 判死" —— **说重了**。
  `post_hook: output <80 chars` 只是**附在后面的**文本，不是主因。
  **真正两层**：
  · **外层**：provider 那阵子不吐字。⚠️ **不是我们的解析坏了** —— 事后手测
    deepseek-chat 0.9s / glm-5.3-flash 1.0s **都正常**。
  · **内层（可修）**：`_stream_call` 撞上限后**只出声、照常返回空回答**
    ⇒ 调用方以为这次调用**成功**了 ⇒ 同一个卡住的模型继续烧，直到 810s 耗尽。
    对比：真正的"停滞"是**抛 `_NetworkError`** 的（会换 agent）。
  ✅ **F2 已修（`93376a7`，用户拍板）**：每次收到字节时比一比
  `content/reasoning/tool_calls` 的**长度** —— "又收到一包字节"不算进展，**变长才算**；
  超过 `_STALL_TIMEOUT`（90s）没进展 ⇒ **抛 `_NetworkError` 换 agent**。
  ⚠️ **必须是"抛"不能是"出声后照常返回空"** —— 返回空会被上层读成
  "这次调用**成功**了、只是模型没说话"，同一个卡住的模型继续被派下一轮。
  自检：真 HTTP 服务器喂"完整、合法、能解析、就是没内容"的 SSE 行；变异验证过；
  全量 **1503 绿**。复盘 → `docs/防御模式.md` **§75**。
  ⚠️ **真机没验**：要真撞上一次 provider 抽搐才看得到它换 agent（不是每次跑都有）。
  · **F1（没做）**：撞上限且一无所获时也抛错 —— F2 已经覆盖了大部分，留着当保险。
  · **F3（没做，设计取舍）**：判失败但**产物可打捞** —— 现在靠 `refs/qidian/pending/*` 活着，
    **界面上一个字都不显示**（`_cli_tasks.py:285` 有提示，但只在 CLI 路径上）。
  ✅ 顺带证实：**流式那条修复管用**（判据现在每次都响、每次都切断）——
    但**切断 ≠ 修好上游**。
- [奇点] 🔴 **任务做完了、提交了、判「无文件改动」—— 而 trace 记账是 0（09-16 12:48 那轮）**：
  项目 `1789532258594` 的 T1（实现 fizzbuzz.py）判 `failed`：
  `QA:fail: [completeness] 无文件改动; [laziness] 检测到 1 个偷懒信号`。
  **但活是做完的**：项目仓里有 `1974746 agent changes in 1789534116538_any` ——
  `fizzbuzz.py` **75 行**，我拷出来真跑：`fizzbuzz.py 5` → `1|2|Fizz|4|Buzz`（exit 0）、
  `fizzbuzz.py 0` → exit 2 + stderr 一行用法。**完全符合需求。**
  **坐实的**：
  · trace 里 `changed_files: []`、`token_count: 0`、`tool_batches.turns: 0`、`elapsed: 0.039`；
  · 而**侧车**（`partial_usage/1789534116538.json`）写着 **`tokens: 124839`** ＋
    一串 dispatch（15.6s / 50.7s / 26.8s…）⇒ **活儿真干了、token 真烧了**；
  · 撞预算那条路径的**代码本身是对的**（`openai_agent.py:716-729` 确实返回了
    `changed_files` / `token_count` / `tool_events`）—— 所以**丢事实的不是那几行**；
  · 结论：**任务多次派发，最后那次收尾时手里是空的**，而 trace 只留了那一份。
  **三个任务全失败，两个都有真产出**（拼起来才是完整交付）：
  · T1 `1974746` 的 `fizzbuzz.py`(75 行) · T2 `16aad4c` 的 `fizzbuzz.py`(69 行)+`test_fizzbuzz.py`；
  · **两份拼起来跑：`pytest` 40 passed**；CLI 契约 `5`→`1/2/Fizz/4/Buzz`、`0`→exit 2 + 用法；
  · 三个任务侧车合计 **~46.9 万 token**（124839 + 120819 + 223354）；
  · **全部没合并**，全躺在悬挂提交里。
  ⇒ **这就是一次「完全交付」，只是系统没认出来。**
  **🔴 根因（09-16 晚坐实，更正上面那版"baseline_ref 空"的说法）**：
  · `changed=[]` 来自 `_task_runner.py:325` ← `executor_result.changed_files`；
  · 但**不是** `_track_changed_files` 退回裸 diff —— **这条 run 的 `baseline_ref` 非空**
    （trace 的 `snapshot_ref: 9311d2b…` 和 `_exec.py:555` 喂的是同一个 `snap.ref`）；
  · **真出处**：trace 的 `unverified` 自己写着 —— 那个执行器
    **`elapsed 0.039s`、第 1 轮就撞总预算收尾（0 轮 / 0 文件 / 0 token）**。
    它是一次**死线之后 5 秒才发起的、注定空的 dispatch**（13:03:42.7 vs 死线 13:03:37.8），
    而这具空壳成了本次 run 的 `dispatch_result`，**盖掉了前几轮真干出来的那份**
    （`_exec.py:768`，且那行 `return` 还没带 `merge_request`）。⚠️ 别再说"悬空提交"。
  · 🔴 `no_baseline_ref` 那 122 对是**另一码事**：来源是**审查/QA 的 dispatch 不传
    `baseline_ref`**（`validator.py:447/571/805/867` 只传 `cwd`），**跟本任务无关**，
    也不是这次失败的因。（出现次数恒为 2 = 同一次调用的 `oa_exec`+`claude_cli` 两条。）
  · 详细推演见本文件最上面那条接手指针，下一步三件事也在那儿。
  ⚠️ **F2 这一轮没救回来**：全程没有任何"停滞"错误，唯一的封顶事件是
  `stream_over_budget:347s:cap=240s:chars=0`（**一次**，上轮是三次）。
  ⇒ **F2 前提"没有任何可用进展"在这一轮不成立** —— 侧车显示模型一直在干。
  ⚠️ **顺带**：`scheduler.log` **停在 12:17（后端重启那一刻）**，这一轮 executor 的日志
  一个字没记 —— 又是"日志静默丢失"（09-15 记过同一条，**谁换的仍没查出来**）。
- [奇点] 🔴 **「清理孤儿 pending ref」是个**地雷**：它现在不工作，而**修好它会毁掉可恢复的产物**（09-16 清理时逮到）：
  **背景（更正我先前的说法）**：任务产出**不是**"悬挂提交" —— 它挂在
  **`refs/qidian/pending/<task_id>`** 上（`_worktree.py:38`），**是有意保留的**。
  真机实证：三个项目仓的 bundle 里，头部就是
  `refs/qidian/pending/1789534116538 → 1974746`（那份 40 个测试全过的产物）。
  ⚠️ **但清理那段写错了仓库**（`_git_worktree.py:315-325`）：
  ```python
  pending_refs = git for-each-ref refs/qidian/pending/
      cwd=str(config.PROJECT_ROOT)     # ← 奇点【自己】的仓库，不是项目仓
  for ref in ...: git update-ref -d ref    # ← 永远空，一条都没删过
  ```
  ⇒ **那些产物是"因为清理坏了才活下来的"。**
  🔴 **危险在后半句**：谁要是"顺手"把 `config.PROJECT_ROOT` 改成项目仓
  （**看起来是个显然的 bug fix**），这段清理就会**真的开始删** —— 把
  `refs/qidian/pending/*` 上**本来可恢复的产物**删掉。
  **修之前必须先定：什么算"孤儿"？**（现在没有任何判据 —— 它是无条件全删。）
  ⚠️ 同族第 **三次**「读错仓库」：前两次是 `orchestrator._run_integration_check`
  和 `workflow._run_verification` 的 E2E 清单（都指向 `config.PROJECT_ROOT`
  而该指向项目仓）—— 那两处修好了，**这一处没修**。
  · 附：`_cli_tasks.py:285` **有一句提示**
    （「产出分支 refs/qidian/pending/<id> 未合入 main」）—— 但那只在 **CLI 路径**上，
    界面和任务终态里**一个字都没有**。又一条"出声没出口"。
- [奇点] 🟡 **「有报告但 issues 没标路由」默认回架构（清空重规划）—— 与同函数「无依据时回实现层」的原则相反**（09-15 写修复用例时撞见）：
  `handle_gate3_reject` 有两个"没依据"的分支，挑了**相反的代价**：
  · **没有 QA 报告** → `impl`，注释写着「**无依据时回实现层: 代价最小, 且不动架构**」；
  · **有报告、但 issues 里一条 `fix_route` 都没有** → `design`，注释写着「有报告但没标路由 → 保守回架构」
    ⇒ `set_phase(PLANNING)` + **`project.architecture = None`（清空架构）**。
  ⚠️ **现在够不到**（`build_qa_report` 会给每条补 `fix_route`，今晚修完后补出来的是 `impl`），
  **所以没动它** —— 但它在那儿等着：任何手工写的 / 旧版留下的 `qa_report.json` 都会走到这条最贵的路。
  ⚠️ 「保守」在这里指的是"对代码保守"，而清空架构是这个系统里**代价最大**的动作，不是保守。
- [奇点] 🟡 **QA 契约「间歇性」那条：探针响过了，结论是「问题不在这层」（09-16 收口）**：
  09-15 的现场（同一次进程里正反两个样本）：老项目 `1789477040616` 判"未产出"、
  新项目 `1789481895784` 解析出真结论（`no_go` + 8 passed / 8 issues）。
  老项目那份「**喂进去的和落在盘上的不是同一份**」（QA 差 217 字符，而同条 SSE 里
  安全报告的数字**精确相符**）—— 两条候选（①那段跑了两趟 ②`raw_output` 被谁动了）**都没坐实**。
  ✅ **09-16 探针真机响了一次**，`saved=1819:parsed=1819`，**只打了一行**：
  · `saved ≠ parsed` 没出现 ⇒ **不是"两次访问之间被换了"**；
  · 只打一行 ⇒ **不是"这段跑了两趟"**；
  · 而且那次解析出了真结论 ⇒ 这一轮 QA 契约是好的。
  ⇒ **病灶不在这层**；那个谜**这一轮没复现**（它的定性本来就是"间歇性"）。
  ✅ 探针已按「定位完就删」从 `workflow.py` 删掉（`a052fd8`），原地留了结论注释。
  ⚠️ **没解决的是"它为什么间歇"** —— 本轮只证明"**不是这两个候选**"，**没证明是什么**。再查得换地方下手。
  （档案：`~/项目/_归档-20260916-真机验证证据/projects/1789532258594.qa-report.md`）
- [奇点] 🔴 **GATE2 的两种处境在界面上长得一模一样 —— 用户当场问「为什么任务都执行失败了，还有架构审核」**（09-15 真机）：
  GATE2 兼任两个**完全不同的角色**：① 初次审架构 ② 出事后的人工兜底点。
  真机轨迹（老项目 `1789477040616` 的 `lineage`，一条不编）：
  `planning→gate2 架构完成` → `gate2→executing 人工批准` → `executing→integrating 任务全部到终态(失败2)`
  → **`integrating→gate2 审查自动修已达上限(2轮), 升GATE2人工兜底`** ← 用户看到的"架构审核"就是这条。
  🔴 **理由一直躺在盘上**（`lineage` 那条 `reason`），**界面不读它**：
  `web/frontend/src/components/chat/GatePanel.tsx:3` 把文案按门号**写死**
  （`{'2': '架构完成·请审核方案'}`）⇒ 兜底升上来和初次审核**同一句话**，人不可能知道为什么又被问一次。
  **修法**：GATE2 面板读一眼"本门是不是已经批过"（`owner_confirm.gate2 == 'approved'`）或
  `lineage` 里最后一次进 gate2 的 reason —— 是就改口说真实来路 + 把那句 reason 摆出来。
  ⚠️ 短标签（`Projects.tsx` 的 `gate2:'G2确认'`）写死无所谓，**长文案写死才是误导**。
  ⚠️ 同族第二半（同一个按钮、两种后果）：这里点「通过」= 清掉 `review_failures` 冲 0 并进 EXECUTING，
  另一次点「通过」= 放一个从没跑过的架构下去 —— **按钮长得一样，做掉的事不一样**。

- [奇点] 🟡 **`POST /api/api-store` 收得进 `_` 开头的 id，然后它就"消失"了**（09-15 真机验证时撞见）：
  实测：`POST /api/api-store {"id": "__probe_gate__"}` → **200，写进文件了**。
  但 `api_store._load()` 是 `if not k.startswith("_")` 才当条目（`_observer` / `_quota_dead`
  这类**元数据键**就是靠这个前缀藏起来的）⇒
  · `GET /api/api-store` **列不出来**（读的时候被跳过）；
  · `DELETE /api/api-store/__probe_gate__` 回 **`{"ok": false}`**（`remove()` 里
    `if api_id not in entries: return False` —— 它压根看不见这个键）。
  ⇒ **建得进、删不掉、列表里也不显示**：三条路全断，只能手改 `.qidian/api_store.json`。
  **修法很小**：POST 那层加一条 id 格式校验，拒掉 `_` 开头的（跟"元数据键保留前缀"对齐）。
  ⚠️ **同族的坑**（我自己踩的）：探针判"删成功"时**看的是 HTTP 状态码**，
  而这个端点**恒回 200**（体里才是 `ok: false`）—— 我第一遍就是这么把失败读成成功的。
  （对照组：`/api/models` 与 `/api/agents` 同样 id 能正常增删 —— 只有 api_store 有这个前缀语义。）
- [奇点] 🔴 **`lazy_spoke_import_failed` 是慢性病，不是偶发 —— 43 次、跨三天，没人看**（09-15 真机跑 fizzbuzz 时发现）：
  **数出来的事实**：`alerts.jsonl` 里这个 key **43 次**，从 **09-12 21:08** 一直到今天 19:20，
  09-13、09-15 每天都有，跑任务时尤其密（19:19:41 / 19:20:06 / 19:20:19 连着三次）。
  每次的内容一模一样：
  `lazy_spoke_import_failed:_dispatch_exec:cannot import name '_load_skills_for_agent'
  from partially initialized module`。
  ⚠️ **最该记的不是这个 bug，是"它报了 43 次没人看"** ——
  这正是那份棘轮文档自己写明的盲区：**「它也看不出'你出声了但没人看'」**。
  出声 ✅（`dispatcher.py` 那句注释："别吞：辐条导不进来是真故障"），
  但**没人消费** ⇒ 三天没人知道。⚠️ 同类账本仓已经栽过一次（§65「事故连持久痕迹都没有」）。
  **形状（读码确定，不是猜）**：
  `_dispatch_skills.py:3` → 反向 import `dispatcher`；
  `_dispatch_exec.py:8` → `from ..._dispatch_skills import _load_skills_for_agent`，
  而那个名字在 `_dispatch_skills.py` **第 57 行**才定义；
  `dispatcher.__getattr__` 按 `_LAZY_SPOKES = ("_dispatch_skills","_dispatch_exec","_dispatch_crud")`
  **依次**惰性导入。
  ⚠️ **机制只是推断、没坐实**：我猜是"两个线程同时摸惰性名字，`import X` 从 `sys.modules`
  返回半成品"的并发导入竞态 —— 但**单线程按三种导入顺序、多线程 20 轮×8 线程都没复现出来**。
  **所以别把那张解释当结论用**（真机报错文本与它吻合，仅此而已）。
  **影响**：那次惰性解析落到下一个辐条，后续访问正常（自愈）；
  但那一刻若调用方要的正是这个辐条独有的名字，会拿到 `AttributeError`。**目前没见它造成可见故障。**
  **没动手**：作者已试过并否掉两条修法（挪 import 到文件末尾 / 拆叶子模块，见 `_lazy_spoke` docstring），
  要动先看那段。⚠️ 但**"先加个锁"这条低风险的还没试过**，也**没试过把告警接进告警面板**。

- [奇点] 🟡 **两栏永远空：交付清单的 `reports` + GATE3 的 E2E 清单 —— 已修（`2d6d6ab`），⚠️ 真机没验**：
  **同一个形状：读错了目录**，而界面上跟"正常交付 / 本来就没有"长得一模一样。
  · 交付报告：收的时候找 `_Path(项目仓库)/qa_report.json`，实际全在 `.qidian/projects/<id>.<名字>`
    —— 目录和名字**双双**对不上（`security-report.md` 连字符+.md），
    而 `test_cases.json` / `review_report.json` **全仓压根没人写**；
  · E2E 清单：读 `config.PROJECT_ROOT`（= **奇点自己的仓库**）⇒ 那一支永远走不到。
    ⚠️ 同一个错在 `orchestrator._run_integration_check` 早修过一遍，**只补了一边**。
  ✅ 顺手：报告数进了 detail 串（`报告=N`），日志里能看出空没空。
  ⚠️ **下次真交付时看两样**：清单 `reports` 非空 · 那行 detail 的 `报告=N`。
  自检 3 条、**变异验证过**（各自改回原样都变红）；全量 `pytest tests/` **1500 绿**。
  🔴 **在跑的那个后端是改之前起的**（Flask 无 reloader）⇒ 这一轮的交付仍走旧代码，**不算验过**。
- [奇点] 🔴 **告警**有出口**这件事，第一步就漏了 89%**（09-15 用户点头单列；起因是真机发现 43 次告警没人看）：
  **实测数**（`.qidian/alerts.jsonl`，09-10 14:31 → 09-15 19:25，**五天**）：
  · 总计 **2358 条**；其中 **2102 条（89%）连 `key` 都没有** ——
    而 09-13 做的那个**聚合视图是按 `key` 归并的** ⇒ **这 89% 根本归不了**，
    它们在告警页上只能一条条平铺（等于没聚合）。
  · 有 key 的 256 条里，前几名本身就说明问题：
    `collect_changes` **110** · `lazy_spoke_import_failed` **63** · `cascade_skip` 26 ·
    `xml_tool_calls_recovered` 18 · `designated_reviewer_is_writer` 14。
  ⚠️ **起因**：今天真机跑项目，发现 `lazy_spoke_import_failed` 已经报了 **43 次、跨三天**，
  **一次都没人看**（且它每次内容一模一样）。⇒ **出声这一半这仓做得很扎实
  （"别吞、要出声"写进了无数 docstring），坏的是"声音没有出口"。**
  这正好是那台棘轮尺子 docstring 自己写明的盲区：
  > 它**也看不出"你出声了但没人看"** —— 判据是"有没有出声"，不是"声音有没有出口"。
  **三个方向（用户拍板用哪个，或组合）**：
  ① **先补 `key`**：89% 没 key 是**最前置**的 —— 没有它，聚合视图、按 key 静音、
     "同一个 key 连续 N 天"这些都无从谈起。代价是要在几十个 `witness.warn` 调用点补 key。
  ② **慢性告警单独一族**：同一个 key 连续 N 天/连续 N 次还在报 → 在告警页**升级置顶**，
     而不是跟今天新出的混在一起（`lazy_spoke` 连报 63 次就是这类）。
  ③ **给关键告警一条真的出口**：邮件 / 桌面通知（本仓已有 `PushNotification` 那种能力）——
     ⚠️ 但要先解决"**哪些算关键**"，否则就是把 2358 条搬个地方，噪音照样。
  ⚠️ **别急着做 ③**：这仓为"告警筛子被糊死"栽过（`alert_summary` 当初存在的理由就是
  "25 条告警 22 条挤在两个 key 上"）—— **先做 ① 再做 ②，③ 得等前两步把信噪比拎出来**。
- [奇点] 🟡 **重启把任务回收成 PENDING 之后，重派撞上"worktree 已经没了"**（09-15 真机，**机制没坐实**）：
  **现场**：我为加载 `reasoning_content` 修复重启了后端 →
  跑到一半的任务被 `tracker.recover()` 正确回收成 `pending`（`retry_count 0→1`）→
  重新派发 ⇒ `worker 异常: [Errno 2] No such file or directory:
  '…/.验证-只读与QA-20260915-worktrees/1789476297885_any'` ⇒ 任务 `failed`（635s），
  **两个下游任务跟着进"降级运行"**（`上游依赖 … 已失败 (降级运行)`）。
  **坐实的部分**：
  · worktree 是**被正规清理**掉的（`git worktree remove` —— `git worktree list` 里也没它了）；
  · `_exec.run` 是 `try: … finally: _cleanup_wt(wt)` —— **跑完就清**（那处本身没错）；
  · `tracker.recover()` **只改状态、不碰 worktree**（读过了）。
  **没坐实的部分**：**哪条路径在"清理之后"还在用那个旧 `cwd`** ——
  我没把 `_exec.run` 那条 `while True`（fallback 链 × turn 循环）读完，
  **别当结论用**。⚠️ 但"重启一次就会踩到"这件事本身就值得查。
  **它为什么值得记**：本仓的**崩溃恢复**（`recover()` 把 INFLIGHT 打回 PENDING）
  看起来只对了**一半** —— 状态回得去，**执行现场（worktree）回不去**。
  下次"重启一下试试"之前先想到这条。
- [奇点] 📦 **历史存档：2026-09-14 中午版接手指针**（**已做完的账，别照它做**）：
  它列的那些洞**本轮全部修完了**（见最上面那条 ⓪）。外派账（哪份报告对应哪个文档、
  哪些在跑）在 `docs/要发的指令-归档.md` 的「归档账」表里；逐份报告在 `docs/*20260914.md`；
  更早的原文备份在 `~/OPEN.20260914-收工前.bak.md` 与 git 历史里。**这里不再抄。**
- [奇点] 🔴 **人审门的否定族还剩一个洞：「别继续了」判 approved**（09-14 外派⑦ 抓到、我复核属实）：
  已经修了三轮（不通过⊃通过 / 不同意⊃同意 / 不+情态动词+没未）。**剩下的是 `别`/`否` 这一族**：
  修它要把握「别/否」收进否定字，而**上一轮的误伤正是它们**（特别/是否/识别/区别/告别/个别）——
  收回来得先配一套"假朋友"反查（`(?<![特分个区识告辨])别` 之类），**那是设计取舍不是补词**。
  ⚠️ **这个函数的真正解法不是加词** —— 它是**开集枚举**，永远有下一个说法。
  真解法：门用**显式确认动作**（按钮 / 结构化回复），或把判定交给模型。
  ⇒ 现状：用一条**钉住现状**的测试把天花板变成可见的（`test_已知的漏网_别继续了_仍然是批准的`，
  谁修好了那条会红并提示改断言），**没有硬修**。
- [奇点] **验收轮状态（只留指针，别在这儿抄数）** —— 逐条的 `✅/❌/◐/—` 全在
  `docs/待验证清单-20260912.md`，**这里只留两条还成立的**：
  🔴 **轮 4（09-13 晚）全失败，根子只有一个：900s 超时** —— 实现任务被砍 →
  写测试的**降级起跑**（不级联失败是有意设计）→ 没实现可测、空手回来 →
  QA 逮住"无文件改动" → 项目**停着等人**。
  🔑 **教训：数"总数"要连组成一起数** —— 当天四次"总数一个字没变、组成全变了"，
  只数总数那次**当场漏掉**（见 [[count-criteria-hide-gaps]]）。
- [奇点] P1 三臂实验：方案在 `docs/P1-三臂实验方案-20260912.md`，**没跑**（**只差跑**）。
  ✅ **观测点已补**（2026-09-14，`f7fda40`）：原来那条判据「trace 里出现 `_soloN`」
  **落不了地** —— 那个 tag 只在内存里（执行器当 task_id 用，跑完就没了）⇒ **跑完没法验证
  这次到底是哪一臂**，而实验的全部结论都建立在"臂跑对了"上。现在 A 臂写 `solo_arm`、
  B 臂写 `committee_arm` 到 `.qidian/logs/scheduler.log`（两臂都写，否则"没看到 solo"
  同时是"跑了 B"和"没进这个分叉"，两种情况长得一样）。
  **跑之前要对上三件**（写在方案里）：tokens ≈ budget · model 是链首 · 另一臂没有对应事件。- [奇点] **§59 那个边界：兑现了（`57d2a17`）—— ⚠️ 真机没验**（要等一次真 900s 超时）。
  `token_count = None` 的**两种成因**从此分得开：① 压根没发起过调用 ② 发起了但没落账。
  以前一律报"一次都没落盘"。做法：dispatch **开头**落 `{"started_at": …}`
  （`_persist_partial_usage` 累加时要带着它写回去，否则一次正常落盘就抹了），
  收尾时按 sidecar 在不在分两种说法。
  ⚠️ **token 的下界语义没变**，这个标记只回答"进没进过"。测试 3 条、变异验证过；
  全量 **1159 绿**。
  ⚠️ **顺带踩了个测试写法的坑**：借 `test_exec_run.py` 的桩驱动真 `_exec.run` 时发现，
  桩会顺着 `_exec.X` **改到 `tracker`/`witness` 模块本身** —— 只还原 `_exec` 等于没还
  （症状：**单独跑绿、合起来红**）。教训记进 `docs/防御模式.md`。
- [奇点] 🔴 **聚合视图一上线就抓出三处"从来没跑过"的功能（已修，⚠️ 真机没验）**：
  真机 **251 条**告警按 key 归并后，`consolidate:name 'X' is not defined` 露出来了 ——
  `_memory_consolidator.py` 用了 `auto_maintain` / `system2_extract` /
  `add_inferred_causal_edge` 但**一个都没导入**，调用必抛 NameError、被
  `except` 吞成告警 ⇒ **内存维护、洞察提取、潜因果边写入——三件事从来没跑过**。
  ⚠️ 这**顺手破了**"`_llm_judge_causal` 从没成功过"那个悬案：不是判据的问题，
  是**它批的边根本没地方写**（且高置信那支一进循环就抛，整个步骤 `return 0`）。
  ✅ 已修：三处补 import ＋ **F821 守卫补盲区** —— 守卫本来是瞎的：文件里只要有
  `from X import *`，ruff 就**拒绝对整个文件报 F821**（全仓 **10 个**文件在盲区里）。
  全过程 → `docs/防御模式.md` **§64**。自检两层：静态（摘掉星号跑 ruff ＋
  `hasattr(真实模块,名字)` 把"星号本来供的"滤掉）＋ 行为（真跑一遍 `consolidate_memory()`），
  **变异验证过**；全量 `pytest tests/` **1139 绿**。
  🔍 **09-13 真机跑完查过一遍：这轮压根没触发** —— `consolidate:` 零告警 ≠ 修好了。
  实测 `_heavy_due()=False`（重活那支没跑）、25 个候选里**高置信 0 个**
  （`add_inferred_causal_edge` 那支只走 LLM 判断）。
  触发要**两个条件同时满足**（≥1 小时 + 那一轮有任务收尾），而且 `system2_extract` **会花钱**。
  ✅ **09-13 22:50 轮 5 真机：它自己触发了、而且真出了东西** —— 事件流里
  `memory 抽象 3 条轨迹`（`backfill_abstractions` 跑了）、`insight ⚠️ default×any 任务失败率
  100% (8/8)，建议升级路由`（`system2_extract` 出了洞察）、`insights.json` **3 条**、
  `consolidate_state.last_heavy` 更新。
  ⇒ **状态从"能跑了"升到"真出产出了"** —— 但 **n=1**，别当"收益拿到了"（那 3 条洞察
  本身还没被谁消费过）。
- [奇点] **异常收尾的 3 个缺口：修了 2 个 —— ⚠️ 真机没验**：
  数出来的是 3 个（全仓能穷举：`_save_trace` 只有 6 个调用点、记账入口只有 2 个），
  超时 / merge 冲突 / 全 agent 失败那三条本来就有，缺的是：
  · ✅ **worker 异常**：原来 `_save_trace(..., None, None, False)` ＋ 不记账。
    现在跟超时走**同一条路**（`_salvage_timed_out` 会读盘上的 sidecar）＋ 记账 ＋ 进记忆。
    两段共用部分抽成 `orchestrator._account_salvaged(t, salvaged, elapsed)`，免得下次补一处又漏一处。
  · ✅ **取消路径**：`DeliveryReport` 多一个 `fallback_tool_events`，`_save_trace(..., tool_events=)` 透传 ——
    **手里攥着的事件不再白攥着**。
  ⚠️ 顺带修了个**测试替身**：`test_task_archive.py` 原来用 `SimpleNamespace` 手搭 batch，
  替身不会跟着真实类型长大（加个参数就 `AttributeError`，而生产上不可能缺）。改用真 `BatchOutput`。
  自检 3 条、**变异验证过**；全量 `pytest tests/` **1142 绿**。
  ⚠️ **真机没验**：worker 异常不好主动造，**等撞上顺手看**；取消那条好造（点一次取消，
  看 trace 的 `tool_batches.turns` **不是 0**）。验法已登记 → `docs/待验证清单-20260912.md` §三·七。
- [奇点] ⚠️ **`待验证清单` 的 #33 那条过期了**（09-14 外派 G 抓到；**H 说它核过 ——
  `web/app.py:2295` 确实是 `concurrent=2`，⚠️ 但 H 那份结论我一条都没核**）：
  它写"并发 1 ⇒ 暂停任务占死队列"，而 `run_queue` 默认虽是 1，**实际部署自启动传的是 `concurrent=2`**
  （G 说 `app.py:2295`）⇒ 那条的前提要重写。**下次动"暂停占工人"那条时先核这里。**
- [奇点] **「任务标成 running 却没人管」那一族 —— 全仓扫完、5 处都兜住了（`e3a835d`/`70478ca`），⚠️ 真机没验**：
  根因（py-spy 栈定位）：`_dispatch_ready` **先 `transition(RUNNING)` 再登记 future** ——
  中间断了任务就永远挂着（没 future ⇒ 循环看不见 ⇒ `ready_tasks()` 也不返回它 ⇒
  900s 收割够不着）。按这个形状全仓扫，**共 5 处**：
  `_dispatch_ready`（真机抓到的那处）· `_reap_futures` 的 `mq.submit` / `runner.finalize` ·
  `_drain_pending` 主体 · 超时分支的抢救段（这处不出孤儿但**会中断整个 reap 循环**）。
  统一走 `_strand_guard`：**只在还停在 RUNNING 时才改** ＋ 进告警通道。
  ⚠️ 最该记的不是 bug，是**它为什么查不出来**：`_loop_worker` 的兜底只 `_push_event`（SSE），
  **不落日志、不进 alerts.jsonl** ⇒ 事故**连持久痕迹都没有**，当晚为此猜错两次方向。
  ✅ 还加了：循环兜底进 `witness.warn` ＋ **孤儿探测**（走到"没活干"那一刻不该有 RUNNING 任务）。
  测试 10 条、**变异验证过**；全量 `pytest tests/` **1155 绿**。全过程 → `docs/防御模式.md` **§65**。
  ⚠️ 变异验证顺手抓出我两个错（都记进 §65）：一条**假绿**测试（绿是状态机给的，
  跟被测判据无关）；一次**变异根本没改到文件**（差点用没发生的实验证明反向结论）。
  ✅ **孤儿探测真机验过了**（09-13 03:47）：主动造一个孤儿（`create` + `transition(RUNNING)`、
  不派发），循环空转时就报了 `orphan_running_task:<id>`；**15 秒内始终 1 条 ⇒ 幂等**。
  ⚠️ 验的是"**探测和上报这条路通**"，不是"自然发生的孤儿会被抓到"（同一段代码，触发源不同）。
  ⚠️ **另外 4 处 `_strand_guard` 仍然真机没验**（不好主动造），别把上面这条当成它们也验了。
- [奇点] **异常收尾的取消路径：真机没验到，改用"真落盘"验的**（09-13）：
  两次试都在**抢窗口**上失败 —— `_check_cancelled` 只在**两次 dispatch 之间**生效，
  而 15 秒预算下任务 42 秒就跑完了，`cancel` 回来是 400（已经是终态）。
  ⇒ 改成**走真实的 `_save_trace` 落盘、再读回来核对**（比赌时机可靠，钉的是同一个东西）。
  测试 1 条。⚠️ **严格讲这不算"真机验"**，只是"真落盘验"。
- [奇点] ⏸**搁置**（09-12 用户拍板：质量 / 成本·速度 两项先不做）→ 其前置的"分阶段算账"**一并搁置**，
  别重复提：**能按"用途"算账（`level`），不能按"阶段"算** —— 调研/架构/执行/审查都从 `any` 出去。
  ⚠️ 原来这里写着"**超时任务的花费完全不计账**" —— **09-12 夜已补**（`3453693`）。
  但补进去的是**下界**（超时那一刻正在飞的调用不计），所以成本仍**偏低、只是不再恒为 0**
  —— 要做这项时记得把这个偏差写进口径。

---

## 补记（2026-09-16 晚，瘦身之后）

- **§13 api-store 保留前缀** —— ✅ **已修 `6f640d4`**（不变量提成 `api_store.is_reserved_id`，
  HTTP 边界 + 写入口两边共用；自检 4 条、变异验证三根线；全量 1507 绿）。

- **§4 「符不符合预期」判据** —— ✅ **已做 `1aa6310`**：`scripts/delivery_facts.py <pid>`，
  **只报事实不下判词**（五样并排：任务/QA/清单/产物/账本）。自检 14 条、变异验证 2 根线。
  实跑两个真项目对照见提交信息。
  ⚠️ 顺带一次事故：测试第一版把生产 `projects_root` 覆盖掉了（恢复 + 加指纹守卫），详见提交信息。

- **§2 委员会席位 no_tools 拦不住** —— ✅ **已修 `a4f57d8`**，**四处一起修**（委员会成员 / A 臂 /
  单模型 no_tools 阶段 / 委员会合成）—— 09-11 的事故其实发生在**调研阶段**，只修委员会等于没修。
  自检 6 条、变异验证 3 根线。⚠️ 写用例时第一版有一次**假绿**（席位只有 2 个时走不到合成那步）。

- **`test_cases.json` 没人写** —— ✅ **已修 `4eb63cd`**（写入端挂架构定稿 + 读取端按声明名字拼 `-k`）。
  ⚠️ **只修写入端会让交付炸**：老代码写死 `-k test_integration`，跟声明的用例名对不上 ⇒ pytest 退 5 ⇒ 判失败。
  真机实测：按声明名字跑 8 passed/退 0，写死的跑 14 deselected/退 5。自检 7 条、变异 6 根线、全量 1520 绿。

### 补记 2（2026-09-16 深夜 —— 今晚真机跑完之后，§1 里几条**过期了**）

- ❌ **「③ 只读豁免 —— 半验」→ ✅ 已验**：`验证-两栏-20260916` 的 T4 是 `[只读]`，
  零改动判 `done`，没被"无文件改动"打死。
- ❌ **「今天修的三条…真机还没跑过完整流程」→ ✅ 跑过了**（09-16 晚全程走通 + 完全交付）：
  `2d6d6ab`（交付报告栏）**验到了**（`reports` 从 0 变 4）；`5d4f271`（流式总时长）
  这轮**没触发**（没卡）；`3a73b30`（MPS 并发）**没复现**（没崩）。
- ❌ **「⑥ 真机验证后半 §0.5/§0.6/§0.8–§0.11（人做的）」** —— 那条指的是 09-14 那份文档，
  今晚这一跑已经覆盖了它的主干，**别再照它做**。
- **数字更新**：`pytest tests/` **1520 绿**（原文 1497）；静默 except 棘轮基线**没动过**
  （今晚所有 except 都带 `witness.warn` 或上抛）。
- **⑤ 环境三条仍然有效**（`stash@{0}`/`{1}` 没删 · 外派⑥样板路径 · `ruff` 在不在）。

- **§14 `lazy_spoke_import_failed`** —— ✅ **已修 `8675358`**（`__getattr__` 的辐条导入循环加互斥锁）。
  形状是**读码读出来的**（两个线程撞出"半成品模块"），不是复现出来的 —— 测试拷的是"互斥"这件事。
  ⚠️ 还差一半：**"它报了 43 次没人看"那件事没解决**（那是 §16 告警没出口）。

### 补记 3（2026-09-16 深夜 —— §16 那条的前提是错的）

- **① 「89% 没 key」是测量误差**：`_derive_alert_key` 是 **09-13**（`ccdc009`）才加的，
  那 2104 条里 **2089 条写在规则还不存在的时候**（09-10/11/12）。按 09-13 之后算 = **2.4%（15/623）**，
  且那 15 条里 2 条是**确实没冒号**的（派不出 key 是对的）。**"补 key"不用做。**
- **② 早就建好了**：`alert_summary` 按 key 归并 + `n>=3` 标 `chronic`，告警页分「常驻/事件」两栏渲染。
- **③ 还太早**：近 7 天 1000 条 → 37 key，**前 3 名占 60%**，而**第一名就是假警**
  （`collect_changes` 338，来自评审/QA 那四路）。✅ 已修 `cb684df`；`lazy_spoke`（133）✅ `8675358`。
  **下一轮真机看这两个数掉没掉。**
- ⚠️ **教训（和第 §4 那条同族）**：**拿一个横跨修复前后的累积文件算比率，算出来的是历史，不是现状。**
