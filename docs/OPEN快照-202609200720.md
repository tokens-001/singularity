# 未完成清单

> 每条一行。**做完就删掉整行**（别留 ✅）。
> **详细分析不写这儿** —— 归仓库 `docs/`（踩坑 → `防御模式.md`；没验过 → `待验证清单-20260912.md`）。
> 📦 **2026-09-20 第四次瘦身**：61.5KB → 本文件，**一字未丢**，全文存
> `~/projects/singularity/docs/OPEN快照-202609200208.md`。
> 砍的是六条"**分析写在清单里**"的大条（判据错位审计 6.1K→1.5K · 接手指针 4.4K→2.1K ·
> 版本两套 3.1K→2.1K · 整份覆盖写 2.7K→1.8K · 外派评审 09-19 2.4K→1.4K）——
> **只留"还成不成立 + 判据 + 指哪去"，逐条账搬进快照**。**56 条一个没少。**
> ⚠️ **第四次确实是因为我写的**（09-18 那次 37.8KB → 09-20 的 61.5KB）——
> 抬头那句"**别让第四次是因为我又写了 15KB**"**没拦住我**。**规矩在，人没照做。**
> 📦 前三份快照仍在 `docs/`：`OPEN快照-20260918.md`（第三次 37.8KB 版）+ 09-16/09-17/09-17晚 三份。
> 🔴 **加新条目只写一行。** 这个文件被 `UserPromptSubmit` hook **每轮 `cat` 一次**
> —— 多 1KB，每个会话每轮就多背 1KB（09-16 一天 13KB、09-17 又 22KB、09-18 又 ~15KB、
> 09-19 到 09-20 又 ~24KB，所以瘦了**四**次）。
> 🔵 **别写行号** —— 三次归档连着逮到行号漂（代码一改就漂，而它看着跟对的一样）。
> 要指位置就写**文件 + 一个能 grep 到的唯一字符串**（函数名 / 注释原话）；已有的行号已全部改掉。

- [奇点] ✅ **`round-20260920b` 真机跑完了（09-20 01:0x–02:0x）—— 三条判据全验到，但交付是空的**
  · ✅ `0eb76048`（400）—— `reasoning_kept_for_tool_call_without_tools` 响 **14~16 次**
    ∧ `llm_400_unknown.jsonl` **一行没涨**（两句必须一起看，只看后者分不出"修好了/没走到"）
  · ✅ `8c0c20ee`（`tool_choice` 从实例挪到按模型）—— 一个进程里**只响 1 次**（旧行为 7 次）
  · ✅ `68664faa`（超时不冒充用户取消）· ✅ `_note_integration` 留痕（当场报 `tests_ran:false`）
    · ✅ B2 触顶文案 · `tasks/*.json.corrupt` 至今 **0 个**（健康态，非验证）
  🔴 **交付为空**（见下面那条 🔴🔴）：5654 行产物**一行没进 HEAD 的树**。
  ⚠️ 更正一句：先前写"**进仓 0 行**"**不准确** —— 历史里确有 3 个集成提交、+478 行，
  但那 478 行全是 README/pyproject 样板。**产物**进仓 0 行这句才是对的。
  🔴🔴 **原判据「进仓的行数不再是 0」是假的 —— 它现在就已经不是 0 了**（09-20 查出来的）。
  实测 `project_integration` 对这个项目回 `merged = 2 个任务 / 3 提交 / **478 行**`，
  一查那 478 行**全是样板**：`pyproject.toml` +36 · `README.md` +356 · `README.md` +86。
  **HEAD 的树里只有 `.gitignore / README.md / pyproject.toml` 三个文件**，
  5654 行 `jsonlstat/*.py` 一行没进 ⇒ 拿"进仓行数 ≠ 0"当验收 = **什么都没修也会绿**。
  ⏳ **改成这条**（两个方向一起看）：
  ① **`not_merged` 落下来**（该是个位数上千 → 接近 0）—— ref 只在**真有改动**时才打
     （`_anchor_ref` 的前提是 `branch_ref != snapshot_ref`），所以它量的是真产物；
  ② **`merged.files` 里出现 `jsonlstat/` 这类产物文件**，不再是 README/pyproject。
  ✅ 已给 `merged` 补 `files`（数字带上组成，`44232952` 那个面板跟着显示文件名）——
  不然"478 行"这种读数下次还会被读成好消息。
  ⚠️ 项目 **`1789835823874` 已跑废，别再点它**（停在 `gate2`）；后端循环**停着**。
- [奇点] ✅ **`tool_choice`「降级要记住」这条**（09-19 修 → 09-20 真机验完，**闭环**）
  **形状**（值得记住，它不止这一处）：`_no_required_tool_choice` 记在**实例**上，而
  `_exec.run()` 的 `for turn` 里**每一轮 `disp` 都新建一个执行器** ⇒ **换了个人，谁也没记住**
  —— `1a554c9f` 修了但**没修掉**（真机一个任务仍响 **7 次**，7 条 = 7 个实例）。
  ✅ **`8c0c20ee` 挪到模块级**（`_NO_REQUIRED_TOOL_CHOICE: set[str]`，按 `self._model`）；
  测试钉**跨实例**，变异改回实例级 → 只那条红。
  ✅ **真机判据已达成**：整个进程里 `tool_choice_required_rejected` **只响 1 次**（旧行为 7 次/任务）。
  ✅ **同形状的第二处也修了 `1223e2e4`**（09-20）：`self._rejected_think_keys` → 模块级
  `_REJECTED_THINK_KEYS`（**按模型分桶**，A 家不吃不代表 B 家不吃）。⚠️ 这一处**没有真机现场**
  —— 生产 `request_template` 没配 thinking 参数、那条路压根不走，**现在还是隐性的** ⇒
  **别当"验过"**，验的是"记在哪"。测试 3 条 · 变异把调用点退回实例级 ⇒ **只**那条跨实例的红。
- [奇点] 🔴🔴 **400 的触发条件找到了 —— 就在「撤掉工具的那一轮」**（09-19 深夜，**拿真机落下的请求体读出来的**）
  `de6d2fa8` 那条通道第一次抓到现场（`.qidian/llm_400_unknown.jsonl`，task `1789829637698`，11 条消息）。
  **凶手是第 [9] 条**：`assistant` + **带 1 个 `tool_calls`** + **`reasoning_content` 键整个不在**。
  （对照 [3]/[6]：同样带 tool_calls，但键在、有内容 ⇒ 没事。）
  **为什么偏偏是它** —— 三行代码串起来：
  ① `openai_agent.py` 里两处 `tools = []`（`测试通过即停` / `已达最大工具调用轮次`）——
     撤掉工具，但**历史里已经躺着带 tool_calls 的 assistant 消息**；
  ② 模型这轮没工具可用、**还是吐了 XML 形式的工具调用** ⇒ 被 `_parse_xml_tool_calls` 捞成
     合成 tool_call（id `xml_0`，历史里 `[8]` 那条「测试已全部通过…停止调用工具」就是证据）；
  ③ 追加历史时 `_assistant_msg_for_history(msg, tools)` 拿**这一轮的 `tools`** 判：
     `if tools:` 留、否则 **pop 掉 `reasoning_content`** —— 这一轮 `tools=[]` ⇒ **键被抹掉**。
  ⇒ 下一条请求的历史里就出现「**带 tool_calls 却没有 reasoning_content**」的消息 → 400。
  ✅ **修了 `0eb76048`，并已验到端点层**：拿落下的那份请求体回放真端点 ——
  **原样 400**；**只给 [9] 补 `reasoning_content: ""` → 200**（修法方向在真服务上坐实）。
  单测+接线 5 条（`tests/test_scheduler/test_reasoning_passthrough.py`）· **变异改回旧判据 → 5 条红** ·
  全量 1797 绿。✅ **09-20 真机验完**：`reasoning_kept_for_tool_call_without_tools` 响 14~16 次
  （那条分支真的走到了）∧ `llm_400_unknown.jsonl` **一行没涨** —— 两句一起看才算，单看后者分不出
  "修好了"和"没走到"。全文见 `docs/防御模式.md` §85。
  ⚠️ 这轮**又死在同一条上**（12/13 失败），整轮白跑 —— 现场就是它留下的。
- [奇点] 🔵 ~~**T1 死在那条 400**（09-19 夜当时只知道症状）~~ —— **已查清并修掉 `0eb76048`**，见上面那条 + `docs/防御模式.md` §85。
  🔑 当时两条线索留档：①文档说带 `tool_calls` 的轮次 reasoning 约 59% 返回空串，而**空串≠没有，整个丢掉才 400**；②流式/非流式两条路对同一轮产出**两种形状**（都已修 `62897c77`）。
- [奇点] ~~ZCode key~~ **09-19 拍板：key 不换（这条作废）**。全文存档
  `docs/ZCode-事件-20260918.md`（⚠️ 里面「查不出传没传过」是**当前状态**，不是已排除）。
- [奇点] 🟡 **外派评审 09-19 的 A1–A10 + B1–B4 全改完了 —— 还剩两条没做**
  ✅ 已落地清单 + 每条落在哪个函数：`docs/外派评审-20260919.md` 顶部的「处理结果」表。
  ⏳ **没做的两条**：
  ① **B5「只重做有问题的 task」—— 边已补上、行为照旧全量**：`build_qa_report` 给每条 issue 标
  `task_id`（平台按各任务 trace 的 `changed_files` 现填，**不是模型写**）；
  `handle_gate3_reject` 每次 impl 打回把"精准做本会重置哪几个"记进 **`gate3_route`** 的
  `precise_reset_would_be`（⚠️ **不在** `gate3_rejected` 那条上 —— 我第一次找差点看漏）。
  🔵 **09-20 第一条真机数据**：`precise_reset_would_be: []` —— **精准做会重置 0 个**
  （映射是空的：7 个任务全失败、没有 merge ⇒ trace 里没有 `changed_files`）
  ⇒ **在这里是空操作、必然转圈**。⚠️ **退化样本**（全失败才让映射为空），一条不够拍板，继续攒。
  ② **B6：`merge.py`** —— ✅ 09-19 通读完、落地三处（`_park` 落盘失败原来静默吞 ·
  `resolve(abort)` 删盘失败裸 `pass` · `_mark_merged` 不看 `transition` 返回值）。
  三条各一个测试、变异验过。⚠️ 动到了调度推进块（`orchestrator`），**真机没验**。
- [奇点] 🟡 **静默 except 那 88 条按形状复核完了：形状级修法基本都已落地**（09-19）
  全文表在 `docs/静默except待修清单-20260913.md` **§六**（六种形状逐条现状 + 证据）。
  ✅ S1（损坏当空→覆写）/ S3（坏任务文件跳过）/ S5（warn 自身失败）**已修成套**。
  ◐ S2 / S4 / S6 各剩一处或几处**没核**（表里标了哪几处）。
  ✅ **C 档剩的两条 09-20 做完了 `df1ffd29`**（`router.py` + `_observer_tools.py`）。
  **第三态定名「路由未判定」**：`RouteResult.classified=False` + 出声（`key="classify_failed"`）
  + `Task.route_gate_unknown` 真字段 + **消费侧有读者**（`validate` 在"未判定 ∧ 门没跑"时
  记进 `unverified`）。🔴 **门的行为一个字没改** —— 仍靠已有的文件级兜底
  `_gate_check_by_files`；兜底成 `gate=True` 会造出**假失败**（没 `eval.py` 的项目
  会被判 gate 失败 → retry/rollback），逐行核过之后**否掉**。
  ⚠️ **有意不标的一种**：没配分类器 / 没配 key ⇒ 那是**稳定的配置事实**，
  标上去等于每任务挂一句 = 常亮的假红。有专门的测试钉着。
- [奇点] 🟡 **架构是"整份 blob"，改一处 = 全部任务作废重建**（09-19 外派第三轮提，我逐行核过成立）
  `_workflow_phases._run_planning` 结尾**无条件** `project.task_ids = []`；`project.confirm_gate` 的
  GATE2 打回分支和 `architecture_redo` 都 `architecture = None` ⇒ 改架构里一个字段，
  连已 DONE、已合并进 git 的任务一起作废。提法：拆 `contracts`(接口+文件所有权+DAG) /
  `verification`(约束+check) / `scope`(covers) 三段各自带版本，`_run_planning` 按段幂等更新。
  ⚠️ **改前先看** `orchestrator._decompose_and_create_tasks` 那条 `if not proj.task_ids:` 守卫 ——
  **清空正是让重建触发的东西**，只把清空那句改成"变了才清"会把 09-17「10 秒冲过执行层」放回去。
  ⚠️ 大改、零真机数据，**未定做不做**。
- [奇点] 🟡 **机器检查的目标文件是计划期"臆造"的** —— 架构 schema 要求 `constraints[].check.argv`
  写出具体测试文件路径、`test_cases` 的 name 要能对上 pytest 函数名（`orchestrator` 里那句
  `-k " or ".join(names)`）⇒ 真机 `machine_checks ran=10 passed=0`，十条全是
  `file or directory not found: tests/test_contract_*.py`。提法：架构只声明"该被哪个 marker 选中"，
  **测试任务收尾登记实存的检查**，验收比对 声明数/登记数/跑数，缺登记 = 缺口、出声。
  ✅ **09-20 又见一次**：`machine_checks {ran:10, passed:0}`，8 个目标文件**一个都不存在**。
  ⚠️ **这轮还多叠了一层**：不只是"臆造"，是**产物压根没进仓**（5654 行在锚上）——
  两个病叠一起，修哪个都单独解不了。
- [奇点] 🟡 **「返工了几轮」这把尺子已造好；原文两处前提是错的**（09-19 核过，尺子已进代码）
  ❌ **前提①「上限是 `_review._REVIEW_MAX_AUTO_FIX`」错**：那个常数管的是**项目级**"审查失败"
  计数（`project.review_failures`），跟同 wt 重派那条路没关系。内层返工的真实上限是
  `config.DEFAULT_MAX_TURNS = 2` ⇒ **每个任务最多只返工 1 轮**。
  ❌ **前提②「跑完就没了」只对外层成立**：`escalate()` 恒返回 None（单档制）⇒ 一次 `run()`
  **只有一个 level**、`turn` 不会重置 ⇒ 内层轮次**已经**在 trace 里（`validation.turns_used`，
  = 1 + 返工轮数）。真只活在内存里的是**外层** `_run_with_retry` 的整轮重跑次数（局部变量）。
  ✅ **已补**：`_persist_partial_usage` 的每条 dispatch 现在带 `turn` + `attempt`
  （前者=内层第几圈，后者=`ctx.retry_count`）⇒ 读法：同一 attempt 里 `turn>1` 的条数 = 内层返工；
  `max(attempt)+1` = 整轮重跑了几次。测试钉在 `tests/test_exec_run.py` 的接线那条（变异验过）。
  🔵 **现成数据**（89 份 trace 现算）：`turns_used` 1→43 / **2→20（返工过一次）** / 0→26（被掐断，validation=None）。
  ⇒ 20 次返工里 **15 次最后判「通过」**、5 次没救回来。**但"2 轮够不够"答不了 ——
  因为压根没让它试第 2 次**（上限就是 1）。要往下走得先拍"要不要放开上限"，那和 C 组是同一个决定。
- [奇点] 🔴 **接手指针** —— 先看这条。
  **起点**：09-17 起跑了好几轮真机（round d/e/f/g，同一题目：JSONL 日志统计 CLI），
  完整交付但账难看（6 成 5 败 / 71 分钟 / **39% token 打水漂**）。
  **逐条判据 + "怎么验的"全在 `docs/待验证清单-20260912.md`，别在这儿抄数。**
  ✅ 09-18 凌晨那批已验完（`a0e8f19` · 调研重试 · 禁用模型闸门（round g））。
  ⏳ **还没验的（这条要留给你的就是这些）**：
  · **观察者那个覆盖** —— **四次都没撞上竞态**，判据仍未验。
  · **09-18 晚那 4 条** —— `164429f` 阶段产出覆盖前归档 · `4e3718f` 锚只在**真进仓**时松手 ·
    `d07414b` `lazy_spoke`（根是 importlib 探 dunder，不是竞态）· **`aecb82f` 空壳不许顶掉真结果**。
    🔴 **今天那条 🔴🔴（产物被丢）正是 `aecb82f` 的形状** —— **修过一次，没修干净**。
  · **`c9e9796c`**（启动回收先兑现"停"标记）—— 判据：**下轮开跑前盘上若有该停的任务，
    它进 `FAILED` 而不是 `PENDING`**；顺带看 `cancels/` 里有没有该被取走的留下。
  · **`62897c77`**（流式补 `reasoning_content: ""`）—— **整轮真机没跑过，别当验了**。
  · **09-19 那 8 处** —— ✅ 只有 `68664faa` 走到了（`by:timeout` + 理由写的是"超时"不是"用户取消"）；
    其余 7 处**一次都没触发**（`stale_write` 没冒、`bded9d8`/`ef74ddd`/`1db42d6d` 没被撞上）
    —— ⚠️ **"没冒"是"没走到"，不是"修对了"**。
  · `3ed1b1fa`（任务页「上一轮」标签）· `ded50bd0`（告警文案三段 + 分钟数是 20）—— **没看**。
  ✅ `de6d2fa8`（认不出来的 400 落盘）**已经起作用了** —— 09-19 那次 400 的现场就是它留下的。
  ⚠️ `4e3718f` 之后 `refs/qidian/pending/*` 会**攒得比以前多**（**有意**：超时/冲突/异常都留锚）
  —— 和「清理孤儿 pending ref」那条 🔴 是**同一个结**。
- [奇点] ✅ **版本两套那条 —— 按方案做完了 `ab2d4c2c`**（09-20）
  `config.runtime_identity()`（**不新增文件**）+ 两个出口：启动日志一行（在**起循环之前**）
  + `GET /api/status` 新增 `identity`（**不新增接口**）。两个字段：`git`/`dirty_src`（这轮是哪版）
  和 `dist_built`/`frontend_stale`（界面产物跟不跟得上，**"比时间戳"换成"读事实"**）。
  🔴 **`frontend_stale` 的判据改过一次（当天就栽）**：原来想比"前端源码最后一次提交"的时间，
  而正常顺序是"改 → build → 提交" ⇒ **提交必然晚于构建** ⇒ 刚构建完就报落后
  （加 90 秒余量还是假红）。⇒ 改成比**前端源码文件的 mtime**。CLAUDE.md 里那条旧判据已划掉。
  ⚠️ **`dirty_src` 只看 `src/`** —— `.qidian/` 天天在变，带上它这个字段**恒亮**。
  ✅ **真机验过**（不是"应该能跑"）：重启后端，日志里
  `身份: git=round-20260920b-11-g1223e2e4 | 源码有未提交改动=True | 界面产物=2026-09-20 02:00`，
  `curl /api/status` 回同一份。测试 7 条、变异两次各咬中（含**写这行的当天真踩到的那脚**：
  提交完紧接着构建，秒级对不齐 ⇒ 必须留余量，否则刚构建完就报"落后"）。
  🔵 下面这段是原始分析，留档：
  `pyproject` 的 `2.1.156` 自 06-23 起一次没动；语义 tag 停在 `v4.2.1`（07-03），**之后 700+ 个提交**。
  绕开的办法是按轮打 tag（`round-*`）—— 规矩写在下面「怎么跑真机」。
  🔵 **09-20 查清并定了方案（没动手）**：**`pyproject` 那个号根本没人读** ——
  `src/` 里没有任何 `importlib.metadata`/`__version__` 去读它、没有发布流水线（CI 只有 `test.yml`）、
  `Dockerfile` 不碰、界面不显示、启动也不报身份。⇒ **真痛点不是"号写错了"，
  是"跑着的进程不知道自己是谁"** —— 所以只能拿 `ps -o lstart=` 去跟 `git log -1` 对，
  间接、又容易看错。
  **方案（小）**：启动时算一次身份（`git describe --tags` + **源码目录**有没有未提交改动）
  ⇒ 打进启动日志 + 塞进**已有的** `GET /api/status`（`app.py` 那个，**不新增接口、不新增文件**）；
  拿不到 git（无仓 / 打包）就回 `unknown`，不报错。`pyproject` 那个号**不动**，只加一行注释。
  ⚠️ **同族的另一半（顺手一起做）**：**界面产物也不知道自己是哪版** —— `dist/` 是构建产物，
  而判据又是"**比时间戳**"（`ls dist` vs `git log -- src/`）。09-15 就栽过：dist 停在 09-13、
  之后 17 个提交没进去，症状是"**代码改了界面没变**"。⇒ 同一个面板里**带上构建时间**，
  把"比时间戳"换成"读事实"。
  ⚠️ **不做的**：给它补版本（"两套对不上"→"一套**没人读**的号"，什么也没解决）；
  也不引 `setuptools-scm`（为一个没人读的号加依赖不划算）。
  ⚠️ **更正**：原文写「活台账 `~/OPEN.md` 在**版本库外**（无 diff/无历史）」**是错的** ——
  它在 `~` 仓里。真实的问题小得多：**它和奇点仓的提交对不上号**，按轮存快照就够对齐。
- [奇点] 🟡 **`anthropic_api.py` 的 max-turns 不看有没有文件改动就给成功**（09-19 读码顺带发现）
  `openai_agent` 那条**有守卫**（`if self._changed_files:` 才 `success=True`），而 anthropic
  的 `# ── Max turns exhausted ──` 直接 `success=True`、`raw_output` 可能是 `"(max turns)"`
  ✅ **09-20 查清并改掉 `4ea34f79`**（用户拍板"先查清再定"）。**查清了：没有"本来就不该产文件"
  的任务** —— 走 `run()` 的是执行层；唯一例外的 planner **当前不可达**（没有 agent 配
  `mode: planner`）；真机 124 份 trace 里 `truncated_by=max_turns` 只有 2 条，**两条都有文件**。
  ⇒ 对齐 openai：**零产出 ⇒ `success=False` + `error_kind="exec"`**（会触发换模型重试）。
  ⚠️ 顺带查清：**`anthropic-api` 这个 type 当前不可达**（没 agent 用它）⇒ 这条**没有真机现场**。
  （同族：`truncated_by` 已经补上，这条是"判定"那一半。）
- [奇点] 🔴🔴 **「整份覆盖写」是一整族 —— 第 1、2 层已修，第 3 层不做**（09-18 外派第二轮，我已核）
  `save()` 是 `to_dict()` **全量覆盖写**，**无字段级合并、无版本号** ⇒ **谁最后写谁全赢，
  手里那份越旧抹掉越多**。同族的写入者已核实 **≥5 类**；观察者那条是最早抓到的那个。
  ✅ **第 1 层**：`_api_projects._start_background` 的四个调用点不再传对象，改成收 `project_id`、
  **进线程再 `load()`**。测试 `test_background_loads_fresh.py` + `test_background_call_sites.py`
  （后者是补"调用点没人管"那半边），**两处都变异验过**。
  ✅ **第 2 层**：`save()` 加 `_warn_if_stale_write` —— 写盘前比对盘上 mtime 与手里 `updated_at`，
  明显更新就 `witness.warn("stale_write")`。**只报不改、不新增字段**（拿已有的持久化字段当版本令牌）。
  测试 `test_stale_write_detection.py` 含"不误报"那条命门。⚠️ **漏报面**：两次写相隔 <1s 的并发写抓不到（有意取舍）。
  🔵 **观察者那条（09-17 真机抓到、最重）**：`_maybe_report_gates` 读盘 → **调模型 10~20 秒** →
  `save()` 整体写回 ⇒ 这 20 秒里人的批准、刚建的任务全被抹掉。**09-17 已修 `19096e2`**
  （写回前重新 `load`），09-19 晚又少一个写入者（`_observer_answer` 三处 GATE 批准转
  `project_gate_confirm`）。⚠️ **但那只是"修一个写入者"，机制没变**；**判据仍未真机验**
  （四次都没撞上那个竞态）。
  ⏸ **第 3 层（乐观锁 / 字段级合并）不做**：动状态机核心，真机验不过来。
  🔵 **不用为它们单跑一轮** —— 点批准时、看 `alerts.jsonl` 里 `stale_write` 刷不刷，顺带即可。
- [奇点] 🟡 **删掉的项目，它的 conflict_held 任务仍然解不开**（`repo_dir` 那条修完剩下的半边，09-19）
  ✅ mkdir 那个成因已修 `7bdeb34`（`_worktrees_dir()` 不再建目录）⇒ `repo_root_for()` 不再被喂出一个刚建出来的假路径。
  ⚠️ **残留**：`repo_dir()` 兜底仍返回一个**不存在**的路径，`merge.resolve_merge` 拿它仍会抛 `FileNotFoundError`。
  ⇒ 只对「父项目被删过、任务还卡着」的现场成立 —— 而 09-19 已清掉 10 个孤儿任务，**盘上现在没有这种现场了**。🔵 是「没证据」不是「已修」，**下次撞上再说**。
- [奇点] 🔴 **外派评审推翻我一条判断（我编了历史）—— ①②已改 `a47fc86`，③仍没改**（09-18/19）
  逐条核实全文 `docs/外派评审-执行归路-20260918.md`。**①我错**：`_run_verification` 不是 `d3c117e`(06-27 20:12) 删的，
  是 `cd05533`(**两小时后**) 新增的；被删的全是"审/判"，**没有"验"** ⇒ 我说"闭环被砍"**在历史上没有指称物**。
  **②我错**：项目层不是"没接"是**接不了** —— QA 的 issue id 是**约束 id**，而任务对象没有约束标识
  ⇒「哪条约束没过→重跑哪个任务」这条边**在数据模型里不存在**（`handle_gate3_reject` docstring 说"只重做有问题的 task"是假的）。
  **③我错**：最小闭环方案的**第一道刹车是坏的** —— `qa_saved` 判的是 `build_qa_report` 有没有抛，`{}` 不抛
  ⇒ QA dispatch 报错时报告照样落盘、`verification_skipped` 不撒但 `verification_ran` **照样撒** ⇒ 会在我**最想排除的那一轮**开火。
- [奇点] ✅ **「最小闭环」已落地 `949ed80b`**（09-18 出方案 → 09-20 拍板"先补护栏再做" → 当天做完）
  全文 `docs/最小闭环方案-20260918.md`。一句话：**不重建 conductor**，复用已有的 `handle_gate3_reject`，
  在 `run_test_fix_loop` 加一次自动回 EXECUTING（只走 `impl` · 上限 1 轮 · 验收被跳过时不自动）。
  ⚠️ **最大风险没验**：回 executing 后靠调度循环派活，**循环没开 = 扔在一个不动的地方（比停在 GATE3 更糟，人看不见）**
  —— 判据在 §五 第 3 条。
- [奇点] 🟡 **验收会因为上游少一个字段整段不发生**（09-18 讨论所得）
  `workflow.py` 里那句「验收跳过: 架构没产出约束清单」—— 标准里「没输入」该算**验收失败**，
  不是**验收没发生**。⚠️ 不是静默：它写了 `verification_skipped`，还当了 GATE3 的入门票。
  🔵 **09-19 拍板：先不动** —— 改它要动**门的准入语义**；按仓里「人负责决定」的规矩够用。
  窟窿表在 `docs/生产流现状.md` §四（`#4 审查不闭环`）。**别再当遗漏提。**
- [奇点] ✅ **「什么算孤儿」定了、也可数了 `a232af0e`（09-20）—— 但**一条都没清**
  **定义**：`refs/qidian/pending/<task_id>` 在项目仓里在 ∧ `.qidian/tasks/<task_id>.json` 没了。
  **为什么不能自动清**：产物**本体没丢**，丢的是「它是什么」（描述/状态/父项目都在那份没了的
  任务文件里）⇒ 清一条 = 永久删掉一份**还在**的产物，而判据已经没了。
  🔵 **真机此刻：pending ref 30 条 · 孤儿 14 条**。出口：`scripts/delivery_facts.py --refs`（只读）。
  ⚠️ **扫不到的盲区**：项目仓被删时里面的 ref 跟着没了，这个数不出来。
  ⏳ **清不清是另一个决定，等你拍板**（现在有数了，可以判了）。
- [奇点] ~~`glm-5.3-flashx`~~ **09-19 拍板：不启用（太贵）（这条作废）** —— 官方 09-18 发布，
  国内 2 元/输入 · 7 元/输出 = `glm-5.3-flash`(0.8/2.8) 的 **2.5 倍**，自称速度 5×。
  ⇒ 贵的那 2.5 倍在 `reviewing` 那种**主力**位置上就是常态开销，不划算。
  🔵 现状留档：`models_custom.json` 里**登记了**（评级 `?`、notes 空）· `agents_custom.json` 的
  `any` **没有它** · `model_prices.json` **没有它** ⇒ 没启用、也没配单价（`_row_cost` 有意不兜底）。
- [奇点] 🟡 **`cancels/` 里 6 个标记没人消费 —— 而"谁把任务转的 FAILED"对不上**（09-19 21:59 现场）
  6 个标记 mtime 21:59:24-25（`task_cancel` 的 body 格式），对应任务 21:59:54 转 FAILED、理由 `用户手动取消`。
  能写这句的只有两处：`_task_runner.finalize`（**会消费标记**，文件该没了）· `_api_tasks.task_cancel`
  的 else 分支（只在状态**非** RUNNING 时走）。两条都对不上"标记还躺在盘上"，`alerts.jsonl` 里
  也没有 `cancel_marker_unlink_failed` / `cancel_marker_unreadable`。⚠️ **没解释出来，别当结论。**
  🔵 已核**不是**定时炸弹：`task_retry` 开头 `cleanup_task_artifacts` 会删它
  （`_worktree.py` 里 `_rm(config.CANCEL_DIR / ...)`，`orchestrator` 610-615 那段注属实）。
  🔵 顺带：8 个标记**一个带 `by` 的都没有** ⇒ `68664faa` 那条路修完后**还没被走到**（不是没修对）。
  ✅ **09-20 更正（这句已过期）**：当天 **23:26:31** 就走到了一次 —— `cancels/1789829637704.json`
  = `{"by": "timeout", ...}`，任务终态 `failed`、理由 `执行超时(>900.0s)`。
  ⇒ **21:59 那个"一个都没有"只是当时的快照**，别当"这条路没通"用。
- [奇点] 🟡 **删项目：停任务已进代码，但两处仍是"有意不动"**（09-19）
  ✅ `4316fb5c`：`DELETE /api/projects/<id>` 现在**先取消它的非终态任务**（治的正是
  "废弃项目 ≠ 停任务 ⇒ 重启捞回来接着烧钱"那个 🔴），并把留下的任务数**报出来**
  —— ✅ **09-19 21:59 真机验到了**：删 round b 的项目 ⇒ 11 个非终态任务全转 `failed`
  （5 个 PENDING 直接转 · 6 个 RUNNING 先收标记、30 秒后在 turn 边界转），
  `alerts.jsonl` 多一条 `project_deleted_left_tasks:13`，13 个任务 JSON 一个没少。
  ✅ **任务 JSON 一个不删**（09-19 你拍的板）：它是锚的唯一线索，删了产物就变孤儿 ref。
  ✅ 项目根那 8 个 0B `.<名字>-worktrees` 空目录**已清**（`rmdir`，非空会自己拒绝）。
  ⚠️ **两处仍是有意不动**，别再当遗漏：① **项目仓不删**（那是成品本身）；
  ② **孤儿任务仍在盘上**（只是现在会被报出来，不再无声攒）。
  🔵 09-19 清的那 10 个的备份在 `~/项目/项目仓库/残留任务-备份-20260919-000002.tar.gz`（含任务↔ref 对照表）。
  ⚠️ **`conflict_held` 那条没动**：cancel 会把它转 failed，但 parked 的合并请求还在、
  `drain` 仍可能把它捞回去 —— 见下面那条。
- [奇点] 🔴🔴 **判据错位审计：实 19 / 面 2 / 疑 6，几乎全是一个治疗点的分支**（09-18）
  **全文 `docs/判据错位审计-20260918.md`**（含「五、2026-09-19 复核」一节 ——
  **原单子的行号全漂了，别照它做**）。治疗点一句话：**把"这次是被我们断的"贯通到判据** ——
  它只写进没人读的旁路（告警 + 分诊账），而 `ExecutorResult.error_kind` 的枚举里**压根没有这一类**。
  ✅ **A3–A9 / B1–B6 / 疑6 都已处理**（09-19，绝大多数是"**只标归因、门的强度一分不动**"）
  —— 逐条改法、测试与变异证据见上面那份文档，以及瘦身前快照 `docs/OPEN快照-202609200208.md`。
  ⏳ **仍开着的四条**：
  · **B3 我判"不动"** —— 安全审计没跑成带 `category: review_error`、文案已诚实、fail-closed 对了；
    要动是"**要不要阻断**"的决定。
  · **C1–C6（上限类）** —— **卡的是"先有数据"**：要动得先跑一轮真机收分诊账，
    拿 `content_chars` vs `reasoning_chars` 回答"240s 是不够、还是它在原地打转"。**别重复提。**
  · **疑1** —— 核到底了，但要动**先拍板「没有盘上产物时该说什么」**。
  · 疑2 / 疑3。
  ⚠️ 边界（**没审**的部分）：observer/ · `_api_admin.py` · 前端 · `zhipu_api.py` 函数体。
  ⚠️ 剩下的大多要先定"**归因算谁的**"，那是决定不是 bug 修 —— 要动**先挑一条说清改法再动**。
- [奇点] ✅ **240 秒那条 —— 09-20 拿分诊账答出来了：不是"不够"，是"它在原地打转"**
  🔴 **结论（数据在 `.qidian/llm_calls_slow.jsonl`，145 条带 `reasoning_chars`）**：
  被 `cut` 的 **11 条里 10 条**同时满足「**正文 0 字 ∧ 工具调用 0 个**」—— 整轮只在思考、
  一次手都没动。对照：**没被掐但正文也是 0 的 40 条，100% 都调了工具**（思考完直接动手，正常）。
  ⇒ **判据落地**：「正文 0 ∧ 工具 0 ∧ 被掐」= 打转，和"正常的正文 0"分得开。
  ✅ **"加时间"这个方向被数据否掉**：被掐的 11 条里 **7 条是 `deepseek-flash`**
  （**最快的那个**，858 字/秒），且 elapsed 分布是 `21 30 52 62 373 386 657 674 906 942 948` 秒
  —— **有 4 条在 21~62 秒就被掐了**，离 240s 差得远（那是被**任务剩余预算**掐的，不是被 cap）
  ⇒ 加 cap 对它们一点用没有。**修法该是"检测只想不产出并打断"，不是加时间。**
  ⚠️ "打断"要先定阈值（本仓「多小算小」仍没答）—— **动之前先拍板**。
  🔵 下面这段是 09-18 的原始分析，留档：
  🔵 先记一句：`d664f5a`（思考不许当正文）只让**失败理由变诚实**了 —— **刀还在**。
  🔵 另一面**已消掉**：`aecb82f` 让「小到白跑」不再丢账 ⇒ **「多小算小」仍没答，但不再有后果**（只是浪费几秒）。
  单次上限 `min(240s, 剩余预算)` 是**硬顶**，跟 deadline 无关；今天 62 次被掐里 **20 次撞这硬顶**、
  **正文全 0 字**。⚠️ 我先前"主要是我压了 deadline"**说大了**。
  🎯 两者修法**完全相反**：不够 ⇒ 加时间 / 让它**边想边写**；打转 ⇒ 得**检测"只想不产出"**并打断。
  ✅ **尺子已造好**（`ca4987d`）：分诊账原来把正文和思考**加起来记**成 `out_chars`
  （"产出 67385 字符"看不出是哪一种）⇒ 已拆成 `content_chars` / `reasoning_chars`。
  ⇒ 下次跑到掐断时，**这一比就能答**，不用再另跑一次实验。**量出来之前不动 cap。**
- [奇点] 🟡 **阵容的两个洞（⚠️ 09-18 归档时核对过，原文写错了）**
  **现状（我核的实盘）**：激活池 = `deepseek-flash` + **`glm-5.3-flash`**；
  停用 = `kimi-k3` / `deepseek-v4-pro` / `glm-5.2` / `glm-5.3`。
  阶段：`executing`=`[deepseek-flash]` · `reviewing`=`[glm-5.3-flash, deepseek-flash]` · `extract`=`[glm-5.3-flash]`。
  ⚠️ **原文写"可用的只有 `deepseek-flash`"是错的** —— 池子里有两个；
  而且 **`glm-5.3-flash` 今晚 round g 真的调成功过**（分诊账里有产出）⇒
  "GLM 账号没钱"那个前提**至少现在不成立**了（充值了？还是它在另一个 key 上？**我没查**）。
  **洞①** 提取员 = 委员会成员（`extract` 那个 `glm-5.3-flash` 也在池子里）⇒ **仍成立**，接口会警告"选手给自己出题"。
  **洞②** 原文写"审查池空、等于没有审查"—— **也不对**：写手 `deepseek-flash` 被剔后还剩
  `glm-5.3-flash`，是**只剩一家**（不是没有）。`_pick_reviewers` 会 warn `designated_reviewer_is_writer`。
- [奇点] 🔴 **`glm-5.2` 烧掉当时 61% 的花费 —— 它是被 cascade 当"执行工人"用的**（09-18）
  查账：`glm-5.2` **$0.5017 / 61.4% / 4 次 / 250,833 token**，`by_model_level` 是 **`any`**（不是 `fusion`）。
  ⇒ 我先前"它是融合提取员、影响有限"**是错的**：在**启用池**里 ⇒ **cascade 换人会挑它干执行层的活**。
  ⇒ 已处理（全走 API）：`glm-5.2` 停用 · `glm-5.3` 进 any 档 · `extract` 改 `['glm-5.3']`（同价 2.0，快 35%）。
  ⚠️ 两者仍比 `deepseek-flash`（425 字/秒、0.48）**慢 3 倍、贵 4 倍**。
- [奇点] 🔴🔴 **`glm-5.3-flash` 比 `deepseek-flash` 慢 2.5~3 倍 —— 而 cascade 的落点正是它**（09-17 两组实测）
  直连：deepseek 首字节 0.7s / **431,422 字/秒** · glm 1.2s / **139,190**。分诊账同向（728 vs 254）。
  ⇒ `cascade_skip` 是 `deepseek-flash → glm-5.3-flash` —— **failover 落在慢的那家** ⇒ 吐不完被掐。
  ✅ **分诊账这把尺子已验**（round g，按时间切开）：deepseek 0.1s / **575~805 字/秒** ·
  glm 1.1s / **271~342**（**各只 2 条，样本小**）。首字节全 0.1~2.0s ⇒ **慢在服务端吐字，不在我们这侧**。
  🔴 **09-20 更正：本条的前提被数据否掉了 —— "按模型设 cap"治的不是这个病。**
  分诊账 145 条实测：被掐的 11 条里 **7 条是最快的 `deepseek-flash`**（glm 只占 4），
  且 4 条在 21~62 秒就被掐（被**剩余预算**掐的，不是被 cap）⇒ **和模型快慢无关**。
  ✅ **GLM 慢是真的**（首字节 0.85s vs 0.20s；272 字/秒 vs 858），但那是**另一个问题**。
  真问题是"**整轮只想不动手**"（见上面「240 秒」那条）⇒ **别按本条做 cap**。
  🔵 过程教训：我第一版对照脚本**把智谱的 key 发给了 DeepSeek**（三发全 401），结论是**脏的**。
- [奇点] 🔴🔴 **观察者汇报把项目状态整体写回 ⇒ 人的批准被抹掉**（09-17 真机抓到，**最重**）
  `_maybe_report_gates`（`_observer_worker.py`）：读盘拿快照 → **调模型 10~20 秒** →
  `_proj.save(p)` **整体写回**（写的是 `to_dict()` 全量快照）。锁只防"同时写"、
  **防不住"拿旧快照写"** ⇒ 这 20 秒里发生的事全被覆盖（`owner_confirm` 掉了、`task_ids` 清零，
  而刚建的任务**还在跑、成了孤儿**）。**窗口正好压在"人看见门 → 拍板"这段上，不是小概率。**
  ✅ **09-17 已修 `19096e2`**：写回前**重新 load**（`fresh = _proj.load(pid)`），
  且这期间已经过门了就**不吭声**（那段白话说的已不是现在的处境）。
  ⚠️ **四次真机都没撞上那个竞态，判据仍未验。**
  🔵 **09-19 补充（措辞更正）**：这一修是**修一个写入者、不是修机制** ——
  同形状的写入者已核实至少 5 类，见上面那条「**整份覆盖写**」。
  ⚠️ 本条原文写的是「⇒ 修法：写回前重新 load」**像还没做** —— 那是过期的措辞，已改。
- [奇点] 🟡 **心跳那条：原判据已作废，真缺口改成「调度器从不看停滞」**（09-19 量穿）
  ❌ **原写的替代判据（`running_futures` 非空 ∧ 线程池全空闲）实测站不住，别再照它做** ——
  真的 `ThreadPoolExecutor` 上量过：未完成 future 数 > 工人数**是正常排队**；
  "全空闲 ∧ 有排队项"只存在于 `submit()` 到工人醒来之间的**几微秒**（赌竞态）；
  工人被 `sys.exit` 带走时 future 照样被标 done。全文更正已写进 `docs/防御模式.md` §77.3。
  ⚠️ **同条 item 的前提也是错的**：它写"新鲜度上界 = 单次模型调用的 240s"，
  而心跳在 `for turn` 循环里、**一个 turn = 一整次 dispatch** ⇒ 上界是 **810s**，
  而 `STALLED_AFTER_S` = **1200s** 本来就在上面 ⇒ **"心跳陈旧"这条判据本身站得住**。
  🔵 **真缺口只剩一个**：`check_stalled()` 全仓**只有观察者调**，**调度器/编排器一次都不调**
  ⇒ 观察者聋了就没人在看。🔵 **09-19 拍板：先不动** —— 观察者自己**有活性痕迹**
  （`observer_state.json` + `/api/observer/status` 能看出它聋了），再加一个调用点
  只会多一份重复告警。**等真撞上"观察者死了没人发现"再说。**
  ✅ 告警"只报事实不报怎么办"那半边 **09-19 已修 `ded50bd0`**。
- [奇点] 🟡 **审查阵容只有一家 —— ①已做（写手摘掉了），"只剩一家"是现状不是残留**（09-19）
  ✅ 走 API 改的：`reviewing` 从 `[glm-5.3-flash, deepseek-flash]` → **`[glm-5.3-flash]`**
  （写手 `deepseek-flash` 摘掉，读盘核对过；备份 `/tmp/phase_models.bak-20260919-125430.json`）。
  ⇒ 名单不再说谎，`designated_reviewer_is_writer` 那条警告也不再每审一次报一次。
  🔴 **但审查**确实**只剩一家** —— 这是事实，不是修好了：**要多人审只能启用第三个模型**。
  🔵 **别再试"扩池"补人**：那条路已死（闸门 = 激活池 = 阵容，恒返回空，见 `_review.py` docstring）。
  ⚠️ **残留洞**：写手若换成 `glm-5.3-flash`（cascade 落到它时就会），
  名单里又只剩写手自己 ⇒ 那条警告会再响、审查池退回"全池排除写手"。
- [奇点] 🟡 **「打回能说理由」验完了 —— 通道通，但管不动模型**（09-17，**实验证伪**）
  四跳全通（打回 → lineage → 拼进提示词 → 我逐字核过）—— **而三轮 `uncovered [7,8,9]` 一字未动。**
  ⇒ **"理由进了提示词" ≠ "模型会照做"**。根因：`_validate_architecture` 只把「tasks 缺失」当
  `arch_invalid`（会硬拦），其余只是 `arch_warning`（有意如此）。**09-17 用户拍板：暂时不动** ——
  系统报到人眼前就够了，**人负责决定**。🔵 副产品：**观察者主动汇报真的有用**（两次都用大白话摆到面前、两次都准）。
- [奇点] 🟡 **GATE1 打回后不清 `research_report`** —— 重跑期间界面显示的是**上一版**报告。
  没清的理由：重跑若早退（预算硬停）会变成空面板、旧报告永久丢。⇒ **是取舍不是遗漏，别再"修"。**
- [奇点] ⚠️ **待查：集成失败之后没重新集成，项目却进了 GATE3**（09-17，**没坐实**）
  已排除 `_merge_inflight` 没释放、`set_phase` 漏记。**别当结论用。** → **详情 防御模式 §77**
- [奇点] 🔴 **「任务标成 running 却没人管」那一族** —— ❌ 09-17 验过没兜住；**09-18 round g 又现场复现**
  `…843` 状态 `running`，但 **工作树已经没了**、侧车 12 分钟没动、心跳停在 01:11 ⇒
  项目停在 `executing` **不会自己结束**；同项目 `…841` 卡 `conflict_held`，**cancel 完又被 drain 捞回去**。
  🔵 **判据更硬了**：不是"探测器没响"，是"**工作树没了 + 侧车不动 + 心跳陈旧**三个信号同时在场，
  系统照样认为它在跑"。**替代判据**（精确、非超时猜法）：`running_futures` 非空 ∧ 工人全空闲。
  → **详情 防御模式 §77.3**
  ⚠️ **09-19 补**：本条点名的 `…843`/`…841` 两个现场**已被清掉**（它们是那 10 个孤儿里的，
  归档时删了任务 JSON）。**族还在，但盘上的证据没了** —— 要再看只能跑新的一轮。
- [奇点] ⚠️ **待查：「审查砍掉的文件」有没有真记进报告**（09-17，**没坐实**）
  `_review.py` 里那句 `len(changed) > 3`（有意，同时 warn + `unverified.append`），
  但 **46 个 trace 里一条「未审查」都没有**。**没能定位是哪个任务触发的**（告警不带 task_id）。
  良性解释未排除：`_save_trace` 幂等，trace 若先落盘则追加进不去。
  ✅ **09-19 已把"没法定位"这半边去掉**：那条告警现在带 `task.id`
  （聚合键不变）。⇒ **下次谁触发过，`alerts.jsonl` 里直接能查到哪个任务**，
  再拿它去对 trace 就能判"到底记没记进报告"。
  ✅ **09-20 00:13 真机第一次抓到「带 task_id」的那条**：老轮 `1789829637702`（23:42）·
  新轮 `1789834349419`（00:13）⇒ **"没法定位"这半边验完了**。
  ⚠️ **"记没记进报告"这半边仍没坐实，而且第一个样本用不上**：老轮那个 `702`
  终态 `blocked`、`changed_files=[]`（**它压根没走到交付报告**），trace 的 `unverified`
  里只有"被掐断"那一句。🔵 **新轮那个 `…419` 还在跑 —— 等它跑完直接查它有没有那句**
  （`f"{len(changed)-3} 个改动文件未审查（只审了前 3 个）"` 在 `_review.py`，
  落进 `validation.unverified`）。⚠️ 但注意：**它报的是 `6->3`，而老轮 702 是改了 0 个文件
  却报 `5->3`** —— 两边"changed"的口径可能不是同一个，查的时候别混。
- [奇点] 🔴 **任务排队等的时间算进执行预算**（09-17 量到准数）
  实测：`13:30:47` 提交 ⇒ 全部 deadline `13:45:47`；`917` 排队 **8 分 17 秒** ⇒ **上场只剩 400s（44%）**。
  ⚠️ **但不能叫"上场即死"**（我先前这么写，当场被真机打脸）：917 拿这 400s 照样干了 94k tokens、
  22 次工具、写出 6430 字符的 `output.py` ⇒ 症状是**"上场时预算被砍一截"**，砍多少 = 排了多久。
- [奇点] 🟡 **`final_status` 会把「已交付」标成 `blocked`**（09-16 查清）
  它是 `neijinglu.build_report` 算的**交付判词**，不是任务状态。**没修**：动的是报告语义，
  UI 和账本都在读，改了得真机验一轮 —— 不值得为一行字冒险。
- [奇点] 🟡 **「委员会换/加席位」先别做** —— 地基没验（P1 三臂实验只差跑）；席位视角已被 A/B 证伪。→ **详情 §3、§21**
- [奇点] 🟡 **「收编 Claude/Codex」** —— 真门槛是三个硬伤（禁工具/记账靠正则/权限拦不住）。
  🔵 09-17 查到一条路（**只读了公开文档，别当定论**）：OpenHands 用 **ACP** 当这些 CLI 的**客户端**，
  且 ACP **自带权限通道** `session/request_permission` ⇒ "权限拦不住"有标准做法；**记账它没提**。
  ⇒ **待评估，不是待做。** → **详情 §5**
- [奇点] 🟡 **Google Stitch 挂进奇点** —— 卡在「它可能只认 OAuth」。下一步是**探针不是写代码**。→ **详情 §6**
- [奇点] 🟡 **`task_retry` 提前释放锚 —— 已修 `169081e`（你拍的板：按原则改）**（09-19）
  ⚠️ **这条的经过值得记**：我先前说"没人记过"**是错的**（代码里写得清清楚楚，是有意取舍）；
  要动手时又撞到一条**专门钉住旧行为**的测试 `test_重试仍然释放锚`（09-18 建的）。
  🔑 **真正定案的是那个文件自相矛盾** —— 文件头写「**merged 是唯一**让'产物已安全进项目仓'
  这句话成立的分支」，而那条测试却允许重试松手。
  ⇒ 按文件头那条原则改：去掉 `task_retry` 里那次 `_release_ref`，测试反向重写。
  依据：「被取代」在点重试那瞬是**假设不是事实**，而 `_anchor_ref` 用 `git update-ref`
  **无条件覆盖** ⇒ 新尝试一产出自会盖掉旧锚；提前释放只在「重试没跑成」时有差别，那时纯丢。
  **变异验过**（加回去 → 那条红、其余 6 条绿）。⚠️ **真机没验。**
  🔵 **遗留**：界面仍会说"有可打捞产物"指上一版 —— **那不是误报**，要消歧义该改**标签**（排期未定）。
- [奇点] 🔴 **「清理孤儿 pending ref」是地雷** —— 它现在不工作，而**修好它会毁掉可恢复的产物**。
  ⚠️ 同族第三次「读错仓库」。**修之前先定什么算"孤儿"。** → **详情 §9**
  🔵 F3（`caca6c0`）把"哪些任务还有可打捞的产物"摆到了界面上（`salvageable_refs()`）⇒ 动手前至少能先看清。
  ✅ **09-19 摸清了「什么算孤儿」—— 但要分两条路，差别是致命的**：
  · **走 `rm` 直删任务 JSON** ⇒ ref 不动，**安全**（实测：删完 10 个，`salvageable_refs()` **仍是 14 条，一条没少**）。
    ref 打在**项目仓**上（`refs/qidian/pending/<task_id>`），`rm` 碰不到它。
  · 🔴 **走 `DELETE /api/tasks/<id>`** ⇒ **`task_delete` 里显式调 `_release_ref`**（grep `_release_ref(` 那行）**，
    **产物真丢**（代码注释原话：「释放 = 断言**产物已经安全进项目仓了**」「实测掉过 3 个」「**用户要留就先别删任务**」）。
    ✅ **同族的 `task_retry` 已修 `169081e`** —— 它原来也调 `_release_ref`（重试一个失败任务
    **会把产物释放掉**），现在不释放了。见「`task_retry` 提前释放锚」那条。
  · 孤儿 **ref**（ref 本身是产物）：**至今没清过，别清。** 那才是那条 🔴 说的地雷。
  ⚠️ 删任务**会丢「这个 ref 是什么」的线索**（描述/状态/父项目）⇒ 备份**必须带对照表**。
  ⚠️ 盘上**已有 4 条 ref 的任务文件早就不在了**（14 条 ref 对不上 10 个任务）—— 那个状态**已经存在**。
- [奇点] 🟡 **QA 契约「间歇性」** —— 探针响过、**只证明"不是那两个候选"**，没证明是什么。→ **详情 §11**
- [奇点] 🟡 **GATE2 两种处境同一句话 —— 已修 `6aa1539`**（09-20 **路真触发了**：兜底进了两次，
  `owner_confirm.gate2` 已是 `approved` + `phase` 回到 `gate2` ⇒ 按 `gateCopy` 的判据该走"兜底"那支）。
  ⚠️ **但我没看界面**，只核了它读的那几个输入 ⇒ **判据是"面"不是"实"，别当验过**。
  兜底升上来时会改口 + 把 `lineage` 里那条 reason 摆出来。→ **详情 §12**
- [奇点] 🟡 **告警 ③「给关键告警一条真出口」**（邮件/桌面通知）—— 剩下的是"**哪些算关键**"。→ **详情 §16**
  ⚠️ 09-17 中午修正：原文写"`lazy_spoke` 已归零"**是错的** —— 它在**后端启动那一刻**就会报。
- [奇点] 🟡 **重启把任务回收成 PENDING 后，重派撞上"worktree 已经没了"** —— **机制没坐实**，别当结论用。→ **详情 §17**
- [奇点] 🔴 **人审门的否定族还剩一个洞：「别继续了」判 approved**（**拍板：搁置**）
  🔵 **09-19 拍板：先不动** —— 代码注释和那条钉住天花板的测试里都写着同一句：
  **真正的解法是让门用「显式确认动作」（点按钮），不是把自然语言继续塞进正则**；
  往正则上加词只是往一个已知会漏的方向加。谁哪天真改好了，
  `test_已知的漏网_别继续了_仍然是批准的` 会红 —— 那时把断言改成 `rejected` 并删掉这条。
- [奇点] 🟡 **异常收尾：**路径**已补齐，剩下的只是"真机没验"**（09-19 重读后改写）
  ⚠️ 原文写"3 个缺口修了 2 个"是**框错了**：收尾路本来就有 3 条（超时 / merge 冲突 /
  全 agent 失败），**缺的那 2 条（worker 异常、取消路径）都补上了** —— 没有"剩下的第 3 个"。
  ①② 都已合进接手指针那条：worker 异常**不好主动造**（撞上顺手看）；
  取消那条好造：**点一次取消，看 trace 的 `tool_batches.turns` 不是 0**。
- [奇点] P1 三臂实验：方案在 `docs/P1-三臂实验方案-20260912.md`，**没跑（只差跑）**。→ **详情 §21**
- [奇点] **验收轮状态**：逐条的 ✅/❌/◐/— 全在 `docs/待验证清单-20260912.md`，**别在这儿抄数**。
  🔑 轮 4 的教训：**数"总数"要连组成一起数**。→ **详情 §20**
- [奇点] 📦 **历史存档：2026-09-14 中午版接手指针**（已做完的账，**别照它做**）。→ **详情 §18**
- [奇点] ⏸ **搁置**（09-12 用户拍板：质量 / 成本·速度 两项先不做）→ 前置的"分阶段算账"**一并搁置，别重复提**。
- [奇点] 🔴🔴 **产物真产出来了，判词说「无文件改动」⇒ 整份丢**（09-20 round-20260920b 真机，
  **T3/T4/T6 三个样本**）
  **同一个形状，提交时间离判死只差 19~105 秒**：
  · T3 `1789836336318` → `d90998c` **+1008 行**（`filters.py` 515 · `types.py` 493）· 01:01:29 → 判死 01:02:24（晚 55 秒）
  · T4 `1789836336320` → `83c228d` **+251 行**（`stats.py` 137）· 01:02:06 → 判死 01:02:25（晚 19 秒）
  · T6 `1789836336323` → `c08dbfa` **+281 行** · 01:10:47 → 判死 01:12:32（晚 105 秒）
  · 判词三条一模一样：`QA:fail: [completeness] 无文件改动 —— 被掐断（deadline）`。
  ✅ **对照组成立（重要）**：T5 `1789836336321` 同样判这句，但它**压根没有锚**
  （上场 1 秒、0 token）⇒ **锚不是无脑写的** ⇒ 上面三个的锚是"真有产物"的证据，不是噪音。
  🔵 顺带：T2 是**另一种**死法（自己的测试过不了），**不算**这一族，别混进来。
  **判据入口**：`_task_runner` 那句 `changed = disp_result.executor_result.changed_files`
  —— **只认「最后一次派发」交回的文件列表**（worker 侧 `_exec.py` 那条只在有 merge_request 时跑）。
  ✅ **假说已坐实 → 已修 `4f7971ed`**（09-20，**没重跑**，盘上读出来的）：
  trace 的 `token_count=4920 / changed_files=[] / unverified=deadline 那句` +
  `partial_usage` 最后一条 attempt3-turn1 + ref `d90998c` 上 +1008 行 —— 三样时间数字全对。
  对照组 T5 `token_count=0` **且没有 ref** ⇒ 锚不是无脑打的。
  **根因两处**（`_run_with_retry`）：① bool 判据 ⇒ 档 1（只烧钱）顶掉档 3（带 merge_request）；
  ② `if deadline_wrapup: return batch` **无条件**交空壳 ⇒ 产物到不了 `mq.submit`。
  修法：`_batch_evidence()` 分 3/2/1/0 档，**弱的不许顶掉强的**，两处都用。
  测试 4 条、**变异两次各咬中**。复盘 `docs/防御模式.md` §86。
  🔵 同族旧账：`_exec.py` 注释那句「上一轮真干出来的账整份丢掉（09-16 真机）」**就是这个形状**
  —— 修过一次、**修了一半**，这是第二次（§86.5 规则 2：修一半比不修更坏）。
  ✅ 产物**没丢**（还在 ref 上）。
  ⏳ **真机没验** —— 判据就是上面「下一轮只验一条」那句：**进仓的行数不再是 0**。
- [奇点] 🟡 **外派评审 09-20（成熟度 / 受众）核对完并排序 —— ✅ 真机那轮已跑完，可以动了**
  全文 `docs/外派评审-20260920.md`（逐条核对表 + 六步排序 + 三条路径选第 3 条）。
  一句话：**门接电 → 先坐实「产物被丢」再改 → 造尺子（可机检的交付事实）→ 计划期臆造
  → 状态一致性 → 最后才是"以周计"的结构工程**。评审把修订成本当唯一真天花板、**我同意**，
  但**顺序不同意** —— 那件活的验收标准，恰恰要用第 2 步造的那把尺子。
  🔵 **它量错的**：「CI 红两周」是错的（CI 09-18 就绿了），真因是 **`ahead 34` 没推** ——
  09-20 已推（奇点 34 笔 + dotfiles 45 笔）。⇒ 它「接线类 ≈ 一到两周」里**划掉一条**。
  ✅ **第 0 步「门接电」已做 `0bcc6802`**：Makefile `audit` + CI 新 job `audit`；
  `shapes` 走 `--baseline` **棘轮**（裸挂是恒红 ⇒ 会被 `|| true` 掉，§78）；
  `preflight live` / `delivery_facts` **有意不接**（要真机现场，CI 里没有那份状态）。
  逐条理由 + 验证方式在 `docs/外派评审-20260920.md` §七。
  ✅ **09-20 推了、真 CI 也绿了**（run `35463355160`）：**`audit` / `test` / `lint` 三个 job 全 success**
  —— 这扇门现在**有第三方跑过的证据**，不是"本机绿"。
  ✅ **第二批 5 笔推完同样全绿**（`lint` / `test` / `audit` 三个都是 success）—— 两批合计 14 笔都过了门。
  🔵 顺带记一条：CI 那次跑了 **3 分 43 秒**（本机 `make check` 是 11 秒）——
  慢的那条 `audit-preflight` 只在 CI 跑，正是这个分工的代价与目的。
  ⚠️ 若真要拆件开源：评审点名的 `_machine_checks` / `merge.py` **真机还没验过**，
  拆之前先让它们绿一次。
- [奇点] 🟡 **前端样式调优整体推迟**（09-20 用户拍板）—— 架构图的盒宽/标题截断长度这些
  **先不动**，后期统一优化前端时一起做。⚠️ **别再提"把图调好看点"**（那是被推迟过的）。
- [奇点] 🔵 **Qoder CN 外派审查**（免费 Qwen3.8-Flash，**9/30 23:59 截止**）：只审不改，报告落 `docs/` 进 git，**和 `docs/*-20260914.md`（ZCode 那批）配对比对** —— 两家都报的≈真问题。已建 `.tongyiignore` 排除 `.git/ docs/ .qidian/`；**⚠️ 没实测它认不认这个文件**。**第一步先"标定"**：拿 ZCode 已找到的缺陷当答案，看 Qwen 能不能独立复现 —— 量不出能力，就不知道该不该在 10/1 之后继续用它。

---

## 怎么跑真机（09-17 核过，跑之前照这个来）

- **起后端**：`cd ~/projects/singularity && nohup .venv/bin/python -m singularity.web.app > /tmp/qidian.log 2>&1 &`
- 🔴 **改完代码必须先重启后端再跑真机**（`app.py` 是 `debug=False`，**没有热重载**）。
  判据：`ps -o lstart= -p <pid>` 要**晚于** `git log -1 --format=%ad`。
  09-17 中午实测踩到：跑着的进程是 01:40 起的，而修复是 02:12–02:28 提交的 ——
  **不重启就开跑 = 整轮跑在旧代码上，然后得出"修复没用"的假结论。**
- 🔴 **起后端 = 调度循环也跟着起**（`web/app.py` 启动时那句 `start_loop(concurrent=2)`，
  观察者智能体同理）—— **没有"后端跑着但循环没开"这个状态**。09-19 我据"没监听端口"
  推断"循环没开、不会烧钱"，**是错的**（那次恰好真没在花，因为没有项目可推 —— 结论对、
  理由是编的）。**要判它在不在烧钱，看 `usage_daily.json` 的 mtime 和 `scheduler.log` 有没有 dispatch，
  别看端口。** 停止用 `POST /api/loop/stop`。
  🔵 **并发有两套值**：HTTP `POST /api/loop/start` 默认 **1**（不传 `concurrent` 时）、
  观察者那两条路硬编码 **2**（`_observer_tools.py` 里 `start_loop(concurrent=2)` 那两行）。启动自动起的那次是 **2**。
- ⚠️ **`auto_mode=False` ⇒ GATE1/2/3 都要人点**：
  `POST /api/projects/<id>/gate-confirm` body `{"gate":"gate1|2|3","decision":"approved"}`；续跑 `POST /api/projects/<id>/start`
- ⚠️ **验证轮**才加 `QIDIAN_TASK_DEADLINE_S=180`（否则挂住要耗满 900s）。**真实产出别调小。**
  ⚠️ 但注意：**单次模型调用另有 240s 硬顶**，跟这个无关 —— 调小它只会让**单次调用**更碎。
- ⚠️ **curl 一律 `--noproxy '*'`**（连本机 127.0.0.1 也会被系统代理劫走）· 端口 **5050**
- **看现场常用**：`.qidian/llm_400_unknown.jsonl`（**认不出来的 400 的请求体原文**，
  一次一条；09-19 夜 `de6d2fa8` 起的）· `sample <pid> 2`（免 root 的 py-spy 替代）· `.qidian/partial_usage/<task>.json`（侧车：
  token/派发/工具事件）· `.qidian/heartbeats/<task>_any.json` · `.qidian/alerts.jsonl` · `.qidian/llm_calls_slow.jsonl`（分诊账）
- 🔴 **任务的工作树在项目仓的隐藏兄弟目录**，不在 `.qidian/worktrees/`（那个目录恒空）：
  `~/项目/项目仓库/.<项目名>-worktrees/<task_id>_<level>/`。判据：`ls ~/项目/项目仓库/` 里**以 `.` 开头**的。
  任务在那里写文件、提交（`agent changes in <task_id>_<level>`），**合并成功后才进项目仓** ——
  所以"项目仓里什么都没有"**不等于**"任务没干活"。
  ⚠️ 侧车 `partial_usage` 里那个 `(27字符)` 是**消息长度不是结果**，别拿它判断写入成败。要看 `result_preview`。
- 🔴 **每轮开局先打 tag**（09-19 定的规矩）：`git tag -a round-<日期>` 打在**开跑那一刻的 HEAD**。
  用途：跑完 `git describe --tags` ⇒ `round-20260920-3-gabcdef1`，**比它新的 N 个提交
  = 这轮带着但没验过的修复** —— 这是"修得比验得快"唯一在 git 层面可查的答案。
  上一个锚是 `round-20260919`（09-19 晚基线）。
- 🔴 **打 tag 的同一刻顺手存一份清单快照**（09-19 定）：清单在 `~` 仓、代码在这仓，
  **两个仓的提交对不上号** —— 跑完回头查"那一轮带着哪些待办"就得靠这份快照。
  `cp ~/OPEN.md docs/OPEN快照-$(date +%Y%m%d%H%M).md && git add docs/ && git commit -m "轮次快照"`。
  ⚠️ **别挂进 hook 每轮写**：那会让工作区**常脏**，而 `archive-check.sh` 第①项的
  "未提交"红存在的唯一理由就是"我忘了提交" —— 用一个常亮的假红换掉一个真红。
  **按轮存，不按秒；实时没有需求。**
- ⚠️ **重启会把 RUNNING 任务回收成 PENDING 重派** —— 见「worktree 已经没了」那条。
- ⚠️ **删项目 = 停任务 + 报一声，但两样仍不动**（`4316fb5c` 起）：
  `DELETE /api/projects/<id>` 会**先取消**它 `task_ids` 里的非终态任务，再把留下的条数
  写进 `alerts.jsonl`（key `project_deleted_left_tasks`）。
  **项目仓不删**（那是成品）· **任务文件不删**（那是锚的唯一线索，删了产物变孤儿 ref）。
  ⚠️ 别再用裸 `proj_mod.delete()` 绕过它。
