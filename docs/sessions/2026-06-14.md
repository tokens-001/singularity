# 2026-06-14 会话归档

## 会话一（上午）：基础设施 + 战略验证

从判例助手线上部署出发，发现 import re 致命 bug 并修复。PythonAnywhere 配置 API Token 实现远程自动拉代码。Safari MCP 全局白名单、Playwright MCP 备选。

ljg-skills 21 个安装后，从中提炼出核心四法写入 teach-me。teach-me 重写为双轨——编程教学和知识教学，7 个概念教学法。

Opus 两次战略验证：方向确认、阶段任务确认、学习路径确认，产出模型分层策略（DeepSeek 日常 + GPT Plus 推理 + Opus API 深度）。天工造物架构深拆 I011，10 节完整分析。

社交平台首发策略确定——事故报告形式。产出 HTML + 封面 + 文案，小红书、抖音双平台发布。Twitter 注册完成。

质量标准文件建立：发现多次偷懒行为（不审 Opus 输出就念、概括代替拆解），写入 quality-standards.md + CLAUDE.md 引用。

## 会话二（傍晚）：I010 收尾 + 日程清理

### 启动冲突
新窗口启动后，奇点归档规则（用户当前说法覆盖死数据）未被执行——我原样列出归档待办，其中推广两平台已完成但没更新。触发 CLAUDE.md 新增规则：归档是参考，用户当前说法是权威，冲突时直接以用户说法为准并同步更新归档。

清除 .claude 会话记录，删除过期的 test.txt。

### I010 第2-4条修复
2. 伪校验话术：三个 LLM prompt 模块（论证链/证据缺失/相反法条）从"硬校验/检测"改为"AI辅助分析"，去掉【硬性要求】，docstring 标注"非硬校验，多次运行输出可能不同"
3. 空壳入口：HTML 删相邻法条和关联判例区块+CSS+JS，app.py 删 /related 路由、构建判例索引()、查关联判例()、find_adjacent_laws 导入
4. 可信度评分：compute_trust_score() 去掉伪精确 0-100 分，改为信号驱动高/中/低三级。前端明细从"影响 ±N"改为"方向 正面/负面/中性"

代码提交推送：main c5fff7b

### 待办清仓
- GPT Plus：查价后发现土区 6月14日起涨到 999 TL（≈145元），与美区持平，半价红利消失。决定搁置
- 找第一个真实用户：已找同学
- 认知追平纳入日程第一优先级：学判例助手代码，目标两周后脱稿讲清每一步

## 会话三（晚间）：产品迭代 + 法条库大扩容

### 判例助手功能迭代
- **案情分析模式**：页面顶部增加判决书/案情分析双模式切换。案情模式跳过溯源校验，法条库验证两模式均生效
- **日期输入优化**：input type 改为文本框，支持自由输入"2023年5月"等格式。加"记不清"checkbox。案情模式下日期可选
- **每日上限**：从 3 次改为 5 次
- **本地测试版**：run.sh 一键启动，无限分析次数
- **JS bug 修复**：中文引号 `""` 在 JS 中被当成代码引号，导致整个脚本解析失败。改为「」

### 法条库大扩容（3 次爬取迭代）
- 从 1 个文件 5 条 → 5 部法律约 2370 条
- 民法典（7 编 1257/1260）、刑法（452/452）、民事诉讼法（303/306）、公司法（262/266）、劳动合同法（97/98）
- 修复法条匹配三处 bug：_解析法名 截断问题、中文数字条文号转换、文章号查前转阿拉伯数字

### 可信度评分修正
- 法条不在库里改为中性信号（不计入危险），只有溯源越界和版本不匹配算真正危险
- 测试结果：未入库从 17 条降到 10 条，民诉法开始能匹配

### PA 部署踩坑
- git pull 冲突（missing_laws.txt）、Flask 模板缓存、sed 误删 `</style>` 导致整页白屏

## 当前状态
日常执行：①认知追平→②等同学反馈→③思考案卷。待办：认知追平。法条库差距已大幅缩小。
