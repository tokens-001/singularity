# 判清单（H / ZCode · GLM-5.3-Flash）+ 我的核实

> 下面是**报告原文一字未改**。原始件 `~/Desktop/ZCode审阅/判清单-01.md`（2026-09-14 03:50，16535 字节）。
> 任务：判我的 `OPEN.md` 里有**哪些判断是反的**。
> ✅ **约束遵守**：自报"没读 `Desktop/静默except分类/`、`Desktop/ZCode审阅/` 其余文件、
> `docs/*20260914.md` 全部" —— 也就是说**它没看过本轮任何一份外派结论**，是独立判的。
> 🏆 **它抓到一条我自己反复犯的病**（反1），见下。

## ✅ 我核过的三条：「你判断反了」**三条都成立**

| 它说 | 它说得对不对 | 我怎么核的 |
|---|---|---|
| **反1**：我把「文档事实钉测试」当 02b 的**真增量**收下，可 `tests/test_scheduler/test_doc_facts.py` 是 **`cb05cd1` 我自己建的** | ✅ **成立，而且我自己 OPEN.md 第 235 行就写着它** | `git log --diff-filter=A -- tests/.../test_doc_facts.py` → `cb05cd1`；`grep test_doc_facts ~/OPEN.md` → 第 235 行 |
| **反2**：02b 说「18 条里只有 #1 配了查法」**已过时** | ✅ 成立 | `docs/架构性质清单.md` 数出来 **20 条、19 处有"怎么查"** |
| **反4**：我驳回「unittest 对 0 测试退出码 0」**依赖运行环境 ≥3.12** | ✅ 成立 | `pyproject.toml` 写 `requires-python = ">=3.11"`，而 `Dockerfile` 是 `python:3.12-slim`、本机 3.14 ⇒ **声明的地板（3.11）上那个洞是活的** |

🔴 **反1 是这份报告最值的一条**，它点名的是**我自己的行为模式**：
我刚嘲讽完 02b"读过代码却没盘点已建好的资产"，**转头就把它的②（文档事实钉测试）
当成真增量收下了** —— 同一个动作在我自己的核实环节又演了一遍。
⇒ **已按它改**：那条改成"已存在，剩下的是纪律"，不再当待办。

## 🔴 但它在 `_exec.py:656` 上**判错了**（跟 D 犯同一个错）—— 我逐行核到根

它（和 D）说那条是"用编造的 0.5 **覆写 validate 真分**"，并给了"正解：保住 validate 原分、min 对原分做"。
**我核完：这个前提不存在。**

| 它的前提 | 代码里的实际 |
|---|---|
| 有一个"validate 真分"被覆盖 | `ValidationReport.confidence: float = 0.0`（`validator.py:33`），而 **`validate()` 从头到尾没给它赋过值** —— 构造是 `ValidationReport(turns_used=turn)`（`validator.py:39`），后面只写 verdict/action/unverified/evidence。**没有"真分"这个东西。** |
| `.get("confidence", 0.5)` 里那个 0.5 会生效 | `post_execution_hook` **三条 return 全都带 `confidence`**（`validator.py:126/141` 两条 + 正常出口）⇒ 默认值**永远不触发** |
| 别删 656，删了"惩罚传丢" | `validation.confidence` **全仓只有三处写它**（`_exec.py:656` / `:685` / `:718`）⇒ 删掉 656 会让它**恒为 0.0**，那才是真问题；但"保住原分"这个正解**没有对象** |

⇒ **它的警告方向有价值（别naive删），但它给的正解落不了地。** 这条**不动**。
⚠️ **这也是"复核我的驳回"的第一次实战**：`H` 自己在反4 里说"真正没人复核的是你的驳回类结论
—— 错杀无声" —— 结果**它的一个结论就是错杀**（把不存在的东西说成被我毁掉了）。

## ✅ 反3 成立，但**它的排序已经过时**

它把「取消后重跑」排第 1、判"别外包、直接修"，**这个判断对**（比我原来"交下轮扫 bug"对）。
但它写这份时我**已经修完了**（`ba9c0e9`，同一晚）—— 见 `docs/复核核实-01-20260914.md` 的核实头。

## ✅ 反7 成立：清单漏登记（我的规矩破了）

扫bug-02 的 ③（MCP stdio 不按 id 匹配）④（Observer WS 零鉴权+0.0.0.0）⑦（MAGMA 信号链死代码）
**三条核过为真、却没有任何"待修"行**。按我自己的规矩（"提了就先登记"）这是**漏登记**。
④ 尤其不该悬空 —— 安全项，且和"bridge 有门、observer 没门"是同一套系统的两半。**已补登记。**

## ⬜ 我**没核**的（别当结论）

- 反5（砍掉"审防御模式"任务）· 反6（收掉多动作 A/B）· 反8（文档批解锁）—— 都是**建议类**，
  对不对取决于取舍，不是事实判断
- 二、三节（合并分组 / 排序）—— 是它的方案主张，**我认大部分但没逐条核**
- 附节里它"顺手核掉的 7 条"（#33=concurrent 2 · A 臂判据落空 · E'①②⑤ · 我的两条驳回）
  —— ⚠️ **这 7 条我一条都没核**，**全部当候选**，别因为"H 核过"就当成结论。

## 📌 它自己列的"仍开放"里，有一条我认

> 「取消重跑的下游：任务状态始终 RUNNING，最后会不会被当成正常完成 —— 我只验到'会 retry'」

**这条现在有答案了**：`K` 的条1 把这一半答了（状态全程 RUNNING 直到重试耗尽才 FAILED），
我修的时候也是按"取消是终态、`finalize` 单开分支收 FAILED"定的 —— **两条独立来路对上了**。

---

# 判清单-01（ZCode 窗口 = OPEN.md 里那个 H，2026-09-14）

**读了什么**：`OPEN.md`（工作树版）、`docs/架构性质清单.md`、singularity 仓源码 + git log。
**没读**（按硬约束）：`Desktop/静默except分类/`、`Desktop/ZCode审阅/` 其余文件、`docs/*20260914.md` 全部。
**先纠一个账**：你 OPEN.md 顶部指针写「D 在跑 / G、H 没发」，但 HEAD（`5d99fb7`）显示
**D 已跑完归档（111 条 = 真坏 20 / 假警报 91）、G 已归档（`4fb9b18`）、E' 已归档（`88730bc`）**。
顶部指针比仓库落后一轮，你排序时用的"谁还没跑"已经是旧地图。

## 我翻过代码的证据点（下面引的行号全是我自己翻的，不是抄材料的）

- 取消链：`_api_tasks.py` cancel 只写标记；`_exec.py:262` `unlink()`；`_exec.py:794-803`
  早退集合只有 `ok/planner_decomposed/merge_conflict/soft_quality_gate/deadline_wrapup`，
  **没有 `cancelled_by_user`** ⇒ 落 retry、标记已删 ⇒ **真重跑。G 这条成立。**
- 人审门：`_observer_answer.py:28-50` —— `_REJECT_WORDS` 无"不同意"（`"不同意"⊃"同意"` 走
  `_APPROVE_PHRASES` 批准分支）；bare 分支 `startswith` ⇒ "行不行？/可以吗"过门。
  **E'① 成立，未修。**
- GATE3：`project.py:291-317` `_gate3_admission` 只查 issues 标记（`verification_ran/skipped`），
  **不查 qa_report.json 存在** ⇒ D+E 撞的病根在。另 `project.py:225` set_phase docstring
  仍是现在时（02b 待改②未做）。
- 0.5 覆写：`_exec.py:656` `validation.confidence = quality.get("confidence", 0.5)`；
  `:674-679` 的 min 注释自证本意是"对 validate 原分取 min"，被 `:656` 先毁 ⇒ **D 这条成立**。
- `witness.py:~74` `except OSError: pass` ⇒ **D 这条成立**（注释自认"不该再抛"，但没第二通道）。
- env：`openai_agent.py:1137-1153` 模块级 `_run_command` 的 `subprocess.run` **不传 env=**
  （对照 `:796-802` 类方法有 `env=safe_env`）⇒ **E'② 成立**。
- `web/app.py:2295` `start_loop(concurrent=2)` ⇒ **G 的 #33 前提修正成立**（部署并发是 2 不是 1）。
- `_dispatch_exec.py:553,580`：`_soloN` 只作 tag 传给 `_run_no_tools`，正常路径无落盘出口
  ⇒ **G 的"A 臂判据①可能落空"成立**（P1 的判据要先改）。
- `mcp.py::_recv_stdio`（~:240-256）：readline 回来**不比 id**、异常一律 `return None`
  ⇒ 扫bug-02③ 成立。`observer/config.py:14` `DEFAULT_HOST="0.0.0.0"` + `observer/server.py`
  全文无 token ⇒ 扫bug-02④ 成立。
- `validator.py:185` `result["failures"] = r.returncode` ⇒ 展示层编数成立。
- S1 劈分：`model_prices.py:83` / `_model_discipline.py:63` / `route_learner.py:175` 全是
  `atomic_write_json`；`_dispatch_crud.py:24` 仍 `write_text` 直写 ⇒ **D 的降级/升级都对**。
- `executors/base.py:37` `_BLOCKED_PATTERNS`：有 `.env*/*.key/*.pem`，**无 `id_rsa`/`.ssh`**
  ⇒ **E'⑤ 成立**（E' 自己没核完的那条，我核完了：与 web 侧黑名单确实分叉）。
- `pyproject.toml` `requires-python>=3.11`，但 Dockerfile `python:3.12-slim`、本机 3.14
  ⇒ **你驳回"unittest 0 测试退出码"在两个实际运行环境都成立**（rc=5 是 3.12 才有的），
  但判据悄悄依赖环境版本——见反4。

---

# 一、你判断反了 / 判错的（逐条，不顺着说）

## 反1｜「02b 真增量②：文档事实钉测试」——**已经建好了，你推荐的是你自己的资产**

`tests/test_scheduler/test_doc_facts.py` 在 `cb05cd1`（冻结期、早于 `90580a2`）就提交了，
**OPEN.md 第 199 行你自己写着它**（"冻结期间只加过测试和文档（test_doc_facts.py + …）"）。
你刚嘲讽完 02b 推荐已存在的 silent-except 守卫（它的①过期），转头把它的②当"真增量"收下——
**同一个病（核了代码、没盘已建资产）在你自己的核实环节又演了一遍**。
⇒ 该行改成："**已存在**；剩下的是纪律：可测断言往里加"。
顺带：那个文件 docstring 自述「生产流现状.md 两处过时说法**已手工改掉**」⇒
你🔴「02b 两条待改」的**第①条已完成**，只剩 project.py:225 docstring（我核了，还没改）。

## 反2｜「02b 真增量③：性质清单要么每条配查法、要么砍到 8 条」——**已过时**

今天这份 20 条**每条都有「怎么查」**（我逐条看了）。它说"18 条里只有 #1 配了查法"
对着的是旧快照。剩下的有效内核只有一句：**查法里的数会过期**——#5 的"561/420"早已被
守卫基线超越（`silent_except_baseline.json` 我数了：295 个文件；ratchet 自报 426 handler），
下一个读者照旧手工数、照旧对不上——你 9-13 正是这么摔的。⇒ 改法：#5 的查法**指向守卫**，
别写死数；"砍到 8 条"作废（文件已有 A/B/C/D 分组和用法）。

## 反3｜「取消后重跑 → 建议交下轮扫 bug 或真机造一次」——**判反，别外包，直接修**

链路你逐步核过，我刚才**独立**又核了一遍：三步全真，没有"等真机才能定"的成分。
"下轮扫 bug"= 花一个窗口重买同一份静态证据；真机造一次应该是**修复后的验收步骤**
（修完点一次取消、看它停），不是修复前的调研。这条是全清单里**频率最高**的真洞
（每次人点取消都中招，最多白烧 3 次 dispatch）× **改动最小**（早退集合加一个词）
⇒ 排第 1，见第三节。

## 反4｜「派一份『复核我的核实』」——**该派，但靶子选错了：复核你的"驳回"，不是你的"核实"**

原方案（复核 G 取消链 + E' 五条，"只给声称不给结论"）有两个问题：
(a) 那两组都是静态可判的，我本轮已把大头判完（取消链、E'①②⑤、#33、_soloN 全坐实），
再派一票不承重——"投票不承重"是你自己的规程（评审-02 那次定的）；
(b) **真正没人复核的是你的驳回类结论——错杀无声，漏放有声**。清单：
- "unittest 0 测试不成立" —— **成立，但有前提**：rc=5 要 ≥3.12。Docker 3.12-slim、本机 3.14
  都满足，可 `requires-python>=3.11`，**在 3.11 上洞复活**。钉一句（requires-python 提到 3.12
  或 validator 里断言）就闭环，不钉就是给未来埋的。
- "S1 三条降为假警报" —— 我替你核了：三个文件确实都 `atomic_write_json`，**驳回成立**。
- "#9 ≡ 扫bug-01#1 同一条"、"fail-open 族只有 `_check_permission` 真坏"、"02b 那条
  '76→76 没变'" —— 这几个我**没**逐条重核，是"复核我的驳回"窗口该接的活。
⇒ 若今晚还有窗口，派"**复核我的驳回**"；没有就把上面第三组自己翻一遍（半小时）。

## 反5｜「审 防御模式 68 条（同根因/空话）」——**砍掉，别派**

B 刚做完 90+101 条"文档 vs 代码"；"同根因合并"是**写作时**的活，不是评审的活；
产出又是一份要你核的清单——瓶颈恰在你这端，且额度今晚归零。真要做，等下一次
改防御模式时顺手做，别开窗口。

## 反6｜「多动作 A/B：要不要 ×5–10 重复」——**收掉，接受"没效果"**

原假设已被证伪（基线自己就有 57% 的轮在合并），配对内部方向还不一致，再花 5–10× 成本
大概率买回一个噪声。开关留着、别再指望它省轮。这在 OPEN.md 里还是个"要你拍"的开放问题——
它是可以关的，别让它在下一轮接手指针里再出现。

## 反7｜扫bug-02 高危 ②③④⑦ 四条核实为真，却**没有任何"待修"行**——清单漏登记

你的规矩是"提任何该做的事，先在这里写一行"。四条里：②（api_store `_load` 损坏→`_seed()`
→`_save()` 自毁链）算被 C 草案/S1 族覆盖（链路我核了，在）；**③（MCP stdio 不按 id 匹配 +
迟到响应串位）、④（Observer WS 零鉴权 + 0.0.0.0）、⑦（MAGMA 记忆信号链死代码）三条悬空**。
④ 尤其不该悬空——那是安全项，和"bridge 有门、observer 没门"是同一套系统的两半。
修法便宜：默认绑 127.0.0.1 + 把 bridge 的 token 握手抄过去。要么各立一行，要么显式写
"不修，为什么"。

## 反8｜（小）F 19 条文档过期的 ⏸「等四条都发完再动」——**解锁条件已消失**

D/G/E' 全归档、H 就是本窗口、额度今晚过期——"现在动文档，H 读到的 OPEN.md 还会变"
这个理由不再成立。文档批现在就能动（但见反1：02b 待改只剩一条；F 的 19 条照它排好的改）。
改的时候给 `test_doc_facts.py` 加对应断言，别只改散文——文档漂过一次的地方会漂第二次。

---

# 二、其实是同一件事的（该合并，别当 N 条办）

## 并1｜「同族清扫批」= E' 五条半修 + GATE3 后端门 + 扫bug-02 的 ③④

不是八件独立的事，是**同一个动作**："两批修复修了形状的本体，同族还有谁没扫"
——你自己 09-14 的总结就是这句（"修形状类要回头扫同族还有谁，今晚第三次栽"）。
一次 commit、一套验收：
1. 人审门：`不同意` 进 `_REJECT_WORDS`；bare 分支处理疑问/反问（`？`/`吗`/`不行` 结尾不批准）
   —— E'①（已核）
2. 模块级 `_run_command` 传 `env=safe_env`（或收编进类方法）—— E'②（已核）
3. anthropic `_tmo` 逐轮看表 —— E'③（纸面洞，配置一改就活，修法一行）
4. `mcp_server_add` 补 registry —— E'④（`drop_server` docstring 自己写着"免得下次再漏"）
5. `base._BLOCKED_PATTERNS` 补 `id_rsa`/`.ssh` —— E'⑤（我替你核完了，成立）
6. `_gate3_admission` 补"qa_report.json 真在" —— D+E 的病根（已核：只查标记不查文件）
7. MCP stdio 按 id 匹配 + 迟到响应丢弃 —— 扫bug-02③（已核 `_recv_stdio` 不比 id）
8. Observer WS：默认 127.0.0.1 + token 握手（抄 bridge）—— 扫bug-02④（已核）
其中 1、6 是承重墙（门在说谎），2-5、7-8 是同 commit 搭车。

## 并2｜「编造数字/静默吞」批：`_exec.py:656` + `witness.py:74` + `validator.py:185` + `_memory_consolidator:140`

四个都是"失败/缺失被换成一个体面的数"，同形状 S2，一个 commit：
- `:656` 注意修法有坑：**直接删掉会把 `run_post_exec_checks` 的惩罚传丢**
  （`:674-679` 注释里那个老病会回来）。正解是保住 validate 原分、min 对**原分**做。
- `witness.py:74`：`except OSError: pass` → 至少往 `logging`/stderr 落一条（第二通道），
  不用推翻"不再抛"的决定。
- `validator.py:185`：`failures = r.returncode` → 存结构化信息或 0，三处消费端措辞一起改。
- `_memory_consolidator:140`：失败 `return 0` 与"一条没加"分不开 → 失败时返回 `None`/抛，
  调用方区分三态（它现在有 `witness.warn` 了，只差返回值语义）。

## 并3｜「S1 统一修法」一条线（现在是三条散行）

C 草案执行（`_process_ledger` 先跑通）+ 评审待办③（`.qidian/` 单写者）+ 断更那条
（`.corrupt` 无界增长）= **同一件事**：把"损坏的 JSON 当空"一族按形状修掉。
api_store seed 链（扫bug-02②）就是这个形状，别当另一件事。顺序：`.corrupt`
上限/目录/清理归属**先定** → `_process_ledger` 跑通 → 逐形状铺开（含
`_dispatch_crud.py:24` 的两个写者问题）。

## 并4｜P1 前置线（三行并一行）

「P1 三臂实验：只差跑」+「A 臂判据①可能落空（G，⚠️没核）」+「#33 过期（G，⚠️没核）」
——后两条就是第一条的拆解，且**我两条都核了：都成立**。⇒ P1 现在不是"只差跑"，
是"**差观测点**"：`_soloN` 没有落盘出口（判据①先改写，比如让 `_run_no_tools` 把
tag+usage 落进 sidecar），#33 按 concurrent=2 重写，B 臂 covers 等下一轮真机看它填不填。
不把观测点定下来就跑 P1 = 白跑，这条和 G 的原话一致。

## 并5｜文档批（一行装下所有散文修订）

F 核出的 19 条（生产流现状 三处 + 项目速查 四处，F 已按文档位置排好）+ project.py:225
docstring（02b 待改仅剩这条）+ `_io.load_toml` docstring（方向拿准：只改文档别改行为）+
性质清单 #5 数字改指向守卫 + `待验证清单` #33 重写。一次提交。⏸ 已解除（见反8）。

## 明确**不并**的

- **取消后重跑**：单独排第 1（见三），别混进批次——它小到不值得等批次，频率大到不值得等。
- **暂停占工人 / 水位触发 / ProjectState RMW 跨进程锁 / `_norm_verdict`**：四个都是
  "先出一句话设计再动手"的设计题，互相别混。RMW 的前置（role_probe 是第二写者）
  和并3 的单写者规矩有交集，但解法一个在文件协议、一个在进程协议，分开拍。

---

# 三、排序（一条线，不给并列）

前提：额度今晚过期 ⇒ 窗口不再来，剩下全是你自己的手；你的标准是
"离线真代码 + 全量绿 + 变异红，真机后补"。按「频率 × 后果 ÷ 改动量」排：

**第 1｜取消后重跑**（反3）。半小时级：`_run_with_retry` 早退集合加 `cancelled_by_user`
（或 API 侧取消时直接 transition）；变异一条（把 term_reason 放回去必须红）；
修完真机点一次取消当验收。理由：全清单唯一"每次使用都会踩"的真洞。

**第 2｜同族清扫批**（并1）。半天级。GATE3 后端门是病根（你自己写的"我修的是展示层"），
人审门词表是"门说谎"——这两个是验收链的承重墙；其余六条同 commit 搭车。

**第 3｜编造数字批**（并2）。半天级。这是你整个项目在治的那个病的本体，
`:656` 记住保 validate 原分。

**第 4｜文档批**（并5）。纯文档半天。做完后下一个接手者的第一读物不再说谎，
`test_doc_facts.py` 顺手加断言。

**第 5｜S1 第一批**（并3）。`_process_ledger` 一处跑通 + `.corrupt` 上限设计先行。
从这开始要写新结构，比 1–4 贵，所以排后。D 判的"真坏 20 条"（`docs/核待修-01`，
我没读）按形状并进这条线。

**第 6｜P1 前置**（并4）。观测点定了 P1 才谈得上"跑"；本身还要流水线空，急不来。

**第 7｜等拍板、现在别动**：
- 暂停占工人（设计题；#33 已核 concurrent=2，严重度从"占死"降到"占一半"，
  但 900s 照砍 PAUSED 的形状没变）
- 水位触发/Reconcile —— 方向你判对了（"比补第 6 处 `_strand_guard` 更值"我同意），
  **但它在小修之后不在之前**：上面 1–4 每个半天级且证据已闭环，Reconcile 是周级。
- ProjectState RMW 跨进程锁 · `_norm_verdict`（先有测量）· MAGMA ⑦（先拍方向：
  接线还是标死，别让它占着"记忆信号链"的名分当死代码）
- 派发指令抄进 docs/ —— 顺手 5 分钟，做掉（D/G/H 原文只在 Desktop 一处）。

**第 8｜不做**：多动作 A/B 重跑（反6）· 审防御模式 68 条（反5）· 复核"成立"类
（只复核"驳回"类，见反4）。

---

# 附：这次顺手核掉的「⚠️ 我没核」+ 仍开放的

**核掉了**（原 OPEN.md 标 ⚠️ 没核 / 候选的）：
- G 的 #33（concurrent=2）—— 属实，`web/app.py:2295`
- G 的 A 臂判据落空 —— 属实，`_soloN` 无落盘出口
- E'①（人审门"不同意"/疑问句）—— 属实，未修
- E'②（模块级 `_run_command` 不传 env）—— 属实
- E'⑤（`_BLOCKED_PATTERNS` 无 id_rsa）—— 属实（E' 自己没核完的那条）
- 你的驳回"S1 三条假警报" —— 成立
- 你的驳回"unittest 0 测试不成立" —— 成立，但**依赖运行环境 ≥3.12**，钉一句就闭环

**仍开放/我没追到底的（保持候选）**：
- 取消重跑的下游："任务状态始终 RUNNING，最后会不会被当成正常完成"——我只验到
  "会 retry"，和你一样没追到终态
- "#9 ≡ 扫bug-01#1"、"fail-open 族只有一处真坏"、"02b 的 76→76" 三个驳回——没重核
- MCP stdio 迟到响应"整体串位"的实际波及面（要真机或长读 `_rpc` 调用方）
- D 的 111 条明细（`docs/核待修-01-*` 按硬约束没读）——"真坏 20"的清单内容我没验，
  只验了它逼出来的 4 条登记
