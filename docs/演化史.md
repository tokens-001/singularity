# 奇点演化史

## v0：探索期（2026-05）

| 时间 | 事件 | 方向 |
|------|------|------|
| 5/28 | 奇点体系初始化 | 学习 Python + AI Agent 概念 |
| 5/31 | 项目1完成(任务启动器) | Python 入门：while/if/break |
| 6/1 | 项目2完成(能量日记) | 列表+文件读写+for循环 |

## v1：Agent Bus（2026-06-12~13）

| 时间 | 事件 |
|------|------|
| 6/13 | Agent Bus v0.1 — 你+GPT+Claude 共享任务板 |

**设计思路**：用一个 markdown 文件做共享任务板，多个 AI 协作。

**问题**：手动协调，没有调度。

## v2：奇点调度平台（2026-06-18）

| 时间 | 提交 | 内容 |
|------|------|------|
| 6/18 | `ced5d93` | 奇点 Agent 调度平台 v3 — 多模型并行 + DAG 分解 + 合并队列 |
| 6/18 | `0cd1bfa` | openai_agent.py — 通用 Agent Runtime，不依赖 Claude Code |
| 6/18 | `2ff8965` | QA Gate 接入 + 前端奇点奏报面板 |
| 6/18 | `537593c` | API 库 + 模型注册表 + 项目级 agent 编组 |
| 6/18 | `26da7f2` | MAGMA 记忆注入 agent prompt |
| 6/18 | `645f6ee` | D层可切换 — glm-5.2 注册为D备选 |
| 6/18 | `ee4f13e` | 理论差距审计报告 |

**设计思路**：E/E+/D 三层模型分级 — 便宜模型(E)做简单任务，中等(E+)做代码，强力(D)做架构/设计。

**核心模块**：orchestrator 调度循环 + dispatcher 模型分发 + planner 任务拆解 + MAGMA 记忆。

## v2.1：架构审计+优化（2026-06-20~22）

| 时间 | 内容 |
|------|------|
| 6/20 | ARCHITECTURE.md 完成 — 30 commits, 84 tests green |
| 6/20 | 20个 bug 修复（命令注入/worktree泄漏/线程安全/前端500） |
| 6/20 | Self-Fusion + DCPM System2 + ConstructContext + HyDRA路由 落地 |
| 6/22 | Opus Phase 2 审计完成，前端框架切 React |

**关键决策**：
- 文件持久化而非数据库（零依赖、可grep）
- worktree 隔离（git 原生，类 Docker）
- 循环依赖用延迟导入破（刻意设计，不是 bug）
- 前后端 SSE 事件驱动

## v3：废弃三层，改两档+六阶段（2026-06-24~28）

| 时间 | 内容 |
|------|------|
| 6/24 | production-flow.md — 六阶段生产流基准 |
| 6/24 | 前端框架切 React 完成 |
| **6/27** | **`d3c117e` 砍 AI 互审** —— 删 `conductor.py`(AI自动推进门,303行) / `judge_monitor.py`(AI裁判监控) / `_judge_and_profile`+`_run_fusion`+`_do_review` 链；`run_test_fix_loop` 直通 GATE3。**⚠️ 原因未记录，见下** |
| 6/28 | 专家团队架构.md — 13角色 + 两档模型 + 多模型碰撞策略 |

**为什么废弃 E/E+/D**：三层太机械。人的需求不是一个层级能扣进去的。改为两档（便宜/强力），按阶段分工：
- 定义/实现/交付 → 便宜模型
- 架构/审查/验收 → 强力模型

**为什么砍 AI 互审 —— 原因不明（2026-09-11 追查后的结论）**：
提交信息只列了删什么，一个字没提为什么；本文件当时也没记；`docs/` 下没有解释。
**能还原的只有上下文**：这是 v3 六阶段重构的一部分，方向是
「**AI 自动推进 + AI 裁判**」→「**固定六阶段 + 三道人工门**」。
**推测**（无证据）：AI 当裁判判自己容易偏（代码里 `_warn_same_model` 引的
MAD/EMNLP 2024 就是这条），且"AI 决定能不能过"是黑盒、人看不见它凭什么放行。

> **留这条的意义**：三个月后问"审查为啥删了"，仓库答不上来——包括自己。
> 一个砍掉 300+ 行、改变整条流水线性质的决策，**没留下为什么**，就是代价。
> 以后再砍大件，至少写一行。

**后果（2026-09-11 评估）**：审查层从此是空的 —— AI 自审删了、替代品没装，
交付质量全靠人在 GATE3 兜。而"能自动验证到多大，才能自动推进到多大"，
所以这同时是**能力天花板真正的约束**（见 `生产流现状.md` §四）。

**为什么 13 角色**：借鉴 workbuddy 专家协作，单 AI 写代码没架构/没测试/没安全。角色按软件开发流程分工，阶段内并行。

**为什么多模型碰撞只在 4 个角色**：碰撞不滥用。只在选错代价大的地方（系统架构/AI架构/安全审计/研究员）用多模型。其余单模型照规格施工。

## v4：产品化打磨（2026-06-28~07-02）

| 时间 | 内容 |
|------|------|
| 6/28 | 智能体角色可配 — 点卡片选角色 |
| 6/28 | 模型目录/智能体分离 |
| 7/2 | Config 全中文化 |
| 7/2 | 前端评级 B+→A（错误处理/类型/组件拆分/CSS/测试） |
| 7/2 | 8项 UX 问题修复（Skills自动开/新建项目/Toast/格式化trace） |
| 7/2 | Ref：Zcode 界面风格（纯文本+底部输入+侧边栏） |

## v4.1：智能优化（2026-07-03）

| 时间 | 提交 | 内容 |
|------|------|------|
| 7/3 | `5659ffb` | Router 从 30 行正则 → LLM 分类 (`_llm_classify` + `RouteResult` 缓存) |
| 7/3 | `5659ffb` | Observer 系统提示词改"直接干活"：用户说要做东西 → 必须调 create_task，不先打招呼 |
| 7/3 | `5659ffb` | Dispatcher `pick_agent`/`pick_agent_fallback_chain` 加 route_learner Hedge 权重排序 |
| 7/3 | `5659ffb` | Memory `index_task` 加 `_infer_mem_type()` 自动分类 + `mem_type` 过滤查询 |
| 7/3 | `5659ffb` | 文档清理：18 个过期文档 + 历史会话删除，34→9 文件 |
| 7/3 | `5659ffb` | 新增 CLAUDE.md 硬规则：先读蓝图/对比标注/禁止编造 |
| 7/3 | — | Router 缓存永不过期 bug 修复：`matched_signals` 存关键词当时间戳用 → 加 `cached_at` 字段 |
| 7/3 | — | pause/resume：`_check_paused()` + `PAUSE_DIR`，GATE 人审可暂停任务等反馈 ⚠️**后续订正(9/11)**：GATE 那部分始终没接上 —— GATE 实际靠"项目停在 gate 阶段等人批"实现，从不读写 PAUSE_DIR。这套机制最后只服务「逐步确认」和手动暂停。详见 `docs/项目速查.md` |
| 7/3 | — | 嵌入模型默认启用：`_ntilc_filter` 用 cosine 相似度匹配 skill + `_get_embed_model` 默认 on |
| 7/3 | — | Observer 状态增量推送：`_build_status_context` hash 缓存，状态无变化复用 |
| 7/3 | — | MCP server：`observer/mcp_server.py`，stdio JSON-RPC 暴露 11 个工具 |
| 7/3 | — | 4项修复：Observer多轮历史注入、GATE统一confirm_gate路径、架构线程池独立、pytest发现修复 |
| 7/3 | — | 端到端实测：Observer对话→create_task→Router→Dispatch→_exec，3模型委员会碰撞验证通过 |

**已验证**：3 模型委员会碰撞（deepseek + kimi + glm 并行 → 合成器融合，155s 产出统一架构）

## v4.2：流水线串通 + 封存（2026-07-03~04）

| 时间 | 内容 |
|------|------|
| 7/3 | 6阶段全流程串通：定义→GATE1→架构→GATE2→实现→集成→审查→GATE3→交付→DONE |
| 7/3 | 3模型委员会碰撞验证通过（deepseek+kimi+glm并行+合成器融合） |
| 7/3 | Observer 实测通过（对话→create_task→执行） |
| 7/3 | 276 测试通过（QIDIAN_SKIP_EMBED=1） |
| 7/3 | 项目封存：修 Dockerfile、清垃圾文件（645文件/56万行）、个人知识移出 |
| 7/4 | 目录整理：src/todo_cli残留、.DS_Store、运行时数据清理 |

**封存状态**：`docker compose up` 或 `python3 -m singularity.web.app` 即可恢复。不再开发。

## v4.3：重启开发（2026-09-04 ~ 09-09）

> 封存两个月后重启。核心转向三条线：**砍复杂度**（16 角色→3）、**修流水线**（executor 8 bug 链、超时治理、QA 路径 bug）、**能力数据自动化**（零手写 + 一键基准）。

| 时间 | 提交 | 内容 |
|------|------|------|
| 09-04 | — | 清除泄露的 PAT，上传 GitHub `tokens-001/singularity` |
| 09-06 | 多 commit | 项目迁英文路径 `~/projects/singularity`；bug #1-#5 修复（hello.py 5s done）；能力快照与模型库分离；成品路径 `~/qidian-projects`；上线形态定自部署 |
| 09-07 | `e90aec7` | 角色 16→3（见 [角色体系重构.md](角色体系重构.md)） |
| 09-07 | `9b04c1d` 等 | flow A/B 合并、软质量硬门槛、对账升级 LLM、severity 阈值调优 |
| 09-07 | `75b0e57` | 需求核对优化：审查 prompt 加需求完整性，治「产出太简单」 |
| 09-07 | `ddcb5c1` | 审查路径 3 叠加 bug（NameError + JSON 解析坏 + 需求核对缺失），审查打回从失效到双向正确 |
| 09-07 | — | 架构验证实验：单文件 todo 奇点更朴素更慢；大项目流水线跑不通；委员会 vs 单模型略胜 |
| 09-07 | `049ec04`+`7ae49ec` | executor 写文件链 8 bug 修复（见 [executor写文件链修复.md](executor写文件链修复.md)） |
| 09-07 | `ad3d633` | 执行超时治理：max_turns 尊重 registry + max_tool_turns 3 + 测试通过即停 |
| 09-07 | `9bca7cd` | QA 硬规则路径 bug：no-delete-tests 用主仓库根查 worktree 文件，误杀 test_*.py |
| 09-08 | `d7f08bc` | scan 通配：_is_major_model 白名单精确版本号滞后，改系列+数字通配 |
| 09-08 | `f6dc646` | 一键基准：模型能力字段零手写 + 跑基准自动定档（见 [执行超时与模型能力自动化.md](执行超时与模型能力自动化.md)） |
| 09-08 | `a301cbb` | 安全审计 + bug 修复：P0 安全 4 项 + P1 功能 8 项（QA gate 恢复）+ P2 数据完整性 5 项（见 [审计修复-20260908.md](审计修复-20260908.md)） |
| 09-09 | `28276bd`→`a036f3e` | **flash 全量扫描 + 修复**（16 commit，DeepSeek v4.1-flash 完成）：3 P0（权限 profile 增删必500 / 回滚必500 / transition 无终态保护）+ 12 P1 + 死代码 41 定义（见 [死代码清单-20260909.md](死代码清单-20260909.md)） |
| 09-09 | — | **QA 门禁整条接线**（本轮核心）：原 finalize 先标 DONE 再跑 QA → 门禁形同虚设。改：QA 前移 merge 前（硬证据 fail 清 merge_request）、DONE 挪 QA 后、supervise 接 repo_root、审查置信度接回决策。端到端跑出 worktree repo_root bug 并修 |

**核心结论**：多视角碰撞是唯一证实的核心价值；角色约束增量小；能力评级纯展示（调度不看），改成一键基准自动定档，告别手写维护。

**09-09 取舍定案**（flash 扫描修复的遗留，已拍板）：
- **QA 门禁接线**已打桩（拦）+ 真跑（不误拦）双重验证。supervise 是机械检查（非 LLM），真 LLM 在 check_requirement_conformance；checklist 走 _review_requirements → multi_model_review。
- **`_check_completeness` 降级为「只记录」**：接受。机械子串匹配不可靠，checklist 有 LLM 兜底。
- **偷懒软信号（文件数/无测试）不拦合并**：接受，避免误伤「单文件精准修复」「不需改测试的任务」。
- **9 个「仅测试引用」API 保留**：有测试覆盖，删了净减覆盖，保留。
- **死代码清单定位**：清理记录 + 恢复手册，下次删前必须重新 grep 复验，不得照单删。

## v4.4：真机验证 + 审计 + 用量计价（2026-09-10 ~ 09-11）

三天里从"跑通"走向"跑对"，主线是**把静默失败一个个挖出来**。

### 09-10：融合机制重设计 + 真机验证
- **融合 A/B 三轮反复，最终定案「融合有效」** —— 前两次的"+7.8"/"−3.3"都是假象，
  根因是**输出被 `max_tokens` 腰斩**。分数线与截断位置完美单调。
  详见 `docs/融合实验-20260910.md`、`docs/融合机制重设计.md`。
- **教训入档**：max_tokens 是保险丝不是油门；对照被截断会把结论推向反面（两个方向都踩过）；
  数字异常时先读原始产物别只看分数；测试输入必须和生产同形。
- 修：v3 合并路径漏做三件归档（经验/用量/路由学习）；提取员候选被委员占满；
  兜底合成没禁工具（往奇点自己的仓库写文件）；拆不出任务导致项目无声永久卡死。
- **四条子系统首次真正写盘** —— 奇点第一次记下自己的用量（8797 tokens）。

### 09-11 上半场：全量审计
- 30 个提交 / 71 文件 / 测试 479 → 557。**门禁整层失效**（审查取 diff 用裸 `git diff`，
  而改动在 `validate()` 之前就被 `commit_wt` 提交了 → 恒空 → 五道门禁全静默放行）。
- 死代码：`_collect_ready_pending` / `start_all` / `probe` / `blog/` 子包 / `_cache.py`。
- 详见 `docs/审计报告-20260911.md`。

### 09-11 下半场：用量统计与计价
- **诊断**：用量早就按模型统计了，**钱是编的**（8 模型写死表 + 静默 $0.50 兜底）。
  全仓无真实价格数据。顺带清掉 5 处同类编造。
- **两个决定**：单价存独立文件（模型表会被"跑基准/扫描导入"整行重建）；
  费用读时现算（不需要迁移，补价后历史一起变对）。
- **补全流程记账**：6 条调用大模型的路原来只记 1 条。观察者/任务分类/架构融合/
  记忆整合/目标循环全部补上。⚠️ 流式要申请 `stream_options.include_usage` 才拿得到用量。
- **按天历史**：`usage_daily.json`，折叠算法必须**按天各自归位**（只重算今天会每过午夜重复计）。
- **模型轮换加了又撤**：那个列表本质是"主力 + 备用"，不是 bug。
- 详见 `docs/用量统计与计价-20260911.md`。

### 09-11 晚场：静默失败治理（外派答卷 + 收口）

**一天之内挖出六个缺陷，没有一个报错。** 共同形状：系统没做或做错了，
但从外面看一切正常。

- **诊断**（外派给外部模型做的设计题，材料见 `docs/静默失败治理-外派材料.md`）：
  > 事实从发生到被看见要经过若干次交接（执行→状态、状态→序列化、序列化→界面、
  > 过滤前→过滤后）；**每次交接都靠约定维持、没有任何对账**，而所有交接点的默认行为
  > 都是把"拿不到"解释成"没有/正常"。**静默失败 = 一次断掉的交接 × 一个良性的默认解释。**
- **修的六个**：验收跳过不留痕 / 计数器单向棘轮（GATE2 永久死锁，"点通过永远弹回"）/
  GATE3 看不到 QA 报告 / 项目状态文件被写坏后凭空消失 / 点文件定位失败 /
  扫描静默少一个模型。
- **另外三处**：provider 级熔断降到**模型级**（一个模型欠费不该连坐同厂商其他模型）；
  **别名对账**（厂商把旧模型名路由到新模型时，委员会不能拿它占两个席位）；
  融合提取员走了**死默认值**（621 秒里有 170 秒白烧）。
- **架构收口**：`ProjectState.set_phase()` 成为阶段流转唯一入口，**21 处散点赋值**全部改过去。
  以前两套驱动各写各的 —— `run_phase`（人手）不认识 `INTEGRATING`/`DELIVERING`，
  而那正是 `orchestrator`（自动）在驱动的两个；出事时落盘里只有最终 phase，没有轨迹。
- **立法**：防御模式 43 → 46（#44 承诺类字段三态可分 / #45 计数器棘轮 / #46 确定性 tmp + 线程锁）。
- **图纸**：新增 `docs/生产流现状.md` —— 对着源码写代码实际在干什么，
  并把 `production-flow.md`（6/24 的 v3）标记为"落后三个大版本，只当设计意图读"。

⚠️ **仍有六个窟窿开着**，见 `docs/生产流现状.md` §四。

### 09-12：探路2 复跑 + 论文/分析收口

**一夜 17 笔提交，测试 862 → 1030。** 起因是重跑一轮真实的探路轮
（项目 `探路2-文本统计CLI`，5 个任务）。结果 **1 成 4 败 —— 但真正"不会做"的：零。**
四种死法全是机制：

| 死法 | 任务 | 真相 |
|---|---|---|
| **超时吞掉已完成的工作** | T2（+T3） | 干完了、提交了；抢救逻辑只跑 `git status` → **看不见已提交的改动** |
| **格式认不出** | T3 首次 | DeepSeek 吐自家 DSML，平台只认 OpenAI 的 `tool_calls` → 整段被当普通回答 |
| **只说不做** | T5 | 写了份"实现完成"的交付报告，**一个工具都没调**（平台拦住了 —— 这个兜底有效） |
| **报错说不清** | T4 | "无可用 runner" 的真相是"**没找到测试文件**" |

**产物没丢**：T2 那份是能跑的完整实现 + 37 个测试，实测与 `wc -lwmc` 分毫不差
（存 `~/项目/项目仓库/探路2-salvage/`）。**失败的不是能力，是机制。**

**当夜修完**（详见 `docs/防御模式.md` §57/§58 与各提交信息）：

- **§57 挂起不报** —— `SentenceTransformer` 加载**本地**模型时仍联网查 metadata，
  断网就退避重试**挂死**；而**挂起不是异常，`except` 拦不住**
- **DSML 解析** —— 半角/全角、1 根/2 根竖线都认（prompt 里早有禁令，但**知道 ≠ 处理**）
- **§55 三处** —— trace 侧 / **记忆侧** / `crossover_review`（**fail-open**：看不见改动还判"通过"）
- **三条"说不清"** —— runner / 安全审计 / 目录不存在
- **论文四步：各有落脚点，但都是简版** —— 存全过程 / 按阶段切 / 分层抽象 / **检索后改写**
  ⚠️ 2026-09-12 复核（对着 arXiv 2607.29658 **原文**逐条核过）：原先这里写的是"**四步齐**"，
  **那是夸大**。四步里"分层抽象"和"按阶段切"离论文的机制差得远 ——
  论文是**迭代**分组+抽象、做到根节点只剩 2 个，节点带**四样**（意图/关键动作/适用条件/常见坑），
  且**每个阶段各建一棵树**；我们这边是**一次调用吐三层**（concrete/strategy/principle），
  **没有分组这一步**，"按阶段切"只是按工具名分三类各出一句统计
  （`_memory_graph.py` 的 `split_stages`/`stage_summary`）。
  降级原因代码里写了：样本量撑不起论文那种离线批量（479 条 ≈ $211）
  → `_memory_consolidator.py:153-158`。**效果没验证**（样本太少，验不出来）。
- **分析 P2–P6 齐** —— 验收即代码、机械可验率（含需求侧覆盖率）、预算真实化 + 周期对账、
  流程复利账本 + 纪律表自动累积、防御模式当找茬清单（P5 的变体）

⚠️ **绝大多数没在真机验过** → `docs/待验证清单-20260912.md`。

**同时清了一次记忆**：`qidian-bug-backlog` **629 → 100 行** —— 它自己头部写着
"历史进 `docs/`"，却堆了 47KB 的日期流水账。历史本来就该在这儿。

### 跨轮次的稳定结论
- **静默失败是这个代码库的主要 bug 模式** —— 查这类问题先看 `.qidian/heartbeats/` 里的 `warn:` 行。
- **报告会撒谎，当前源码不会**（审计报告的文件:行号几乎全错）。
- **告警日志要连时间一起看** —— `alerts.jsonl` 是累积的，翻旧账会误判成现存 bug。
- **厂商的模型名是别名游戏**：`/v1/models` 只列主推名，旧名会被静默路由到新模型
  （DeepSeek 2026-09-14 起把 `deepseek-v4-pro` 全部路由到 V4.1-Flash）——
  **只有响应体的 `model` 字段说实话**。
- **配置里的死键比没配更糟**：你以为配上了，实际走的是代码里的硬编码默认值，
  而且没有任何提示。（`fusion.toml` 的 `judge_model` + `call_model` 都是 v1 残留，
  v2 唯一读的 `extract_model` 反倒不存在。）
- **计数器必须同时定义清零事件**，否则它就是个棘轮 —— 到阈值就动作、只增不减，
  能把人永久锁死（人工介入即兜底，配额随之恢复）。
- **改状态的入口超过一个，轨迹就一定会丢**。散点赋值时出事只能看到最终态；
  收成单一入口 + 自动留痕，死循环才读得出来。
- **同一个形状会重复出现，而且不止一次**。"只看得见未提交的改动"这个坑，在
  `validator`（09-11）、`orchestrator` 抢救（09-12）、`crossover_review`（09-12）
  **各出现一次** —— 修一处时必须问"**同形状还有几处**"，判据在**调用点**不在实现点。
- **空返回比报错更危险**。"思考吃光 `max_tokens` → `content` 为空"一夜咬了三次
  （分层抽象 700 / 检索后改写 1200 / 因果判断 200），每次的样子都是"**模型没答**"
  而不是"配置错"。**症状是空串的那种失败，必须留痕。**
- **"登记"不等于"做掉"**。把问题写进待办那一刻会产生"已处理"的错觉 —— 09-12 一夜
  提了几十件事、丢了大半。根因不是任务多，是**按"最新刺激"排序、且没有一个外部清单**。
  解法是机制的（`~/OPEN.md` + `UserPromptSubmit` hook 每轮把清单念出来），
  不是"我会注意" —— 那一夜"我会注意"失效了两次。


---

## 2026-09-16 OPEN.md 瘦身：搬出来的「已做完的账」

> 来历：`OPEN.md` 每轮都被 hook 从头念一遍，涨到 41 KB。这些条目记的都是**做完的事**，
> 按那份文件自己的抬头「详细分析不写这儿 —— 归仓库 docs」，本来就该在这儿。
> **内容一字未改，整段搬来。**

  **⓪ 这一整天做了什么（`944cad4..dcbe4a4`，**29 个提交**、都已推送）**：
    三块，按来路：
    · **把 ⑨/⑩ 已确认的洞逐条修完**（权限工具门收进 `BaseExecutor` · 敏感名单三张表
      收成一份 · `task_timeline` 不再编造终点 · gate 兜底表补 `config.py` · 词表 `fusion`
      收口 · origin 两个门收成一份 · worker 异常抢救段补兜底 · unittest 0 测试 ·
      `level_filter`/roles/`project_lineage` 三条静默错）—— **每条都做了变异自检**；
    · **按外派⑬ 的清单修"假绿"**（详见下面单列那条）：三条真洞（mcp/agents/自定义模型的
      写侧护栏"查在读之前"）+ 十余条假绿（恒真断言 / 只测函数不测接线 / 原子性本身无测…）；
    · **自己做的几件**：`.corrupt` 备份加上限 · F 的 19 条文档过期 · `run_command` 两种语义
      收敛 · Python 地板提 3.12（你拍的）· **P1 观测点**（`solo_arm`/`committee_arm`）·
      Observer WS 逐连接鉴权 · **结构性那条的第一个落点**（孤儿探测改成水位触发）。
    **验收**：全量 `pytest tests/ → 1445 绿`（**0 skipped**，见下）· 前端 `tsc 0` /
    `vitest 76` / 样板 `24`。⚠️ **一处没上真机** —— 清单已补 **§0.6**
    （`docs/真机验证-20260914.md`），那步是人做的。
  **⓪-b（09-15 中午补齐）**：**三条拍板项全部修完**（`4b14951`）—— pass 守卫改成
    `action == "pass" or verdict in ("通过","pass")`（真机形状是中文「通过」，原来两道
    守卫都不触发 ⇒ **成功任务的 trace 被系统性截断**）· `_reorder_agents_by_rank` 死代码
    删掉 + 5 条用例 · 启动日志只给 `startup` 挂 handler（不碰 `basicConfig`），
    落盘坏了**退 stderr 不吞**。另：审计 §8.5 那批"未核"清完（`11a58f4`，坐实 2 条假绿）。
    全量仍 **1458 绿**、棘轮仍 **401**。
    ⚠️ **三条都还没上真机**，但 pass 守卫那条**已经拿真数据验穿**（09-15 下午，只读副本）：
    · 真机 24 条 trace 逐条跑 `_is_redundant_failure` —— **旧守卫判 16 条"重复"（其中 9 条是
      `通过/pass`）· 新守卫 7 条（全是 `阻断/abort`）**，差的正好是那 9 条通过，真失败一条没误伤；
    · 再走最后一跳（**真类型** `DeliveryReport` + 真落盘 `save_trace`，现场铺那 24 条当邻居）：
      旧守卫把 `agent_output` 从 800 字裁到 214 字、`changed_files` 20→5 个、打上 `_dedup`；
      新守卫**原文全留**。**症状原样复现 ⇒ 修的是它。**
    ⇒ 剩下要真机做的只有"现场再撞一次"（同类成功任务连续几次），**不是"还没验"**。
    ⚠️ 启动日志那条仍是真机没验：重启一次就能看见（`.qidian/logs/scheduler.log` 里出现「模型: N 注册」）。
  **⓪-c（09-15 傍晚：完整跑通一条项目）**：用户建了 `测试01`（fizzbuzz 小活），
    **从建项目到交付 DONE 全走完** —— 详细账见 `docs/真机验证-20260914.md` **§0.13**。
    链路：`flow_decision{research:false, committee:true, heavy}` → 架构出 **`committee_arm`**
    （316s）→ **GATE2 停住等人**（用户点通过）→ 拆 3 任务（依赖链对）→ `#1 done 208s` ·
    `#2 done 323s` · **`#3 failed`** → 合并回主仓 → **GATE3 停住等人**（用户点通过）→
    `delivering` → **`done`** · `release/1789470376084-202609151950` · `process_ledger 3→4`。
    产出：`fizzbuzz.py` 59 行 + `test_fizzbuzz.py` 8 个测试全过 · QA 散文 7/7 约束 pass。
    **新验到**：人审门该停停该放放 · 依赖链 · **worktree 隔离**（任务在自己目录干活、
    完了才合并）· **§1 process_ledger 新增行** · **§4 记忆信号 narrative 非空** · P1 观测点活着。
    🔴 **挖出 6 条（都已单列在上）**：`lazy_spoke` 43 次慢性告警没人看 · **planner「只跑不改」
    撞 QA 判据（412s 就死，与超时无关）** · **QA 格式对不上（永远"未产出"）** ·
    **点「对话」丢待审批项目（✅ 已修 `f92e340`）** · **交付清单 reports 永远空** ·
    `fn_fail` 不记原因。
    ⚠️ **还没做的**：§3 取消（一直没点，任务自己跑完）· §0.6 权限闸门 · §0.10/§0.11 ·
    §0.8 A1（**试过，结论是"验不了"**）。
  **⓪-d（09-15 晚上：真机跑 + 修了 6 条）** —— 详细账见 `docs/真机验证-20260914.md` **§0.14**。
    **这一晚干了什么**：用户建项目真跑，我从真机抓出并修了 6 条：
    · **`reasoning_content` 被一刀切剥掉**（DeepSeek 带 tools 时要求回传，否则 400）
      —— `f8b326b`；**真机验过**（修完 #1 干净跑完 354 秒）。
    · **「只跑不改」的任务被 QA 判死**（planner 会拆出这种；`supervisor.py` 那条硬规则
      分不开"真偷懒"和"本就不该改文件"）—— `65240b3`；改成**认上游的 `[只读]` 声明**。
    · **QA 契约没建立**（提示词只说"输出 JSON"、不给字段，下游却读 `verdict/issues/passed`）
      —— `4cc5c1b`；**主修复在提示词**，解析端只是配套。
    · **`fn_fail` 不记失败原因** —— `6bb6f7c`；**这条一上线就抓到了上面第一条**。
    · **点侧边栏「对话」把待审批的项目弄丢**（审批面板凭空消失）—— `f92e340`。
    · **流式调用没有总时长上限**（"900s 自收尾没生效"的**新根因**）—— `0e8bc69`。
    · 前端「出错给出路」（chunk 过期时"重试"永远好不了）—— `84871cf`；
      对话页**随时能回看架构/调研** —— `0fec0a1`。
    ✅ **真机验到的**：`reasoning_content`（#1 跑通）· `[只读]` 标记稳定出现 ·
      `fn_fail` 记原因有用 · 任务回收 `running→pending` · 启动日志。

- [奇点] ✅ **「900s 自收尾没生效」已收口 —— 两个根因都找到并修了**（09-13 查一个 · 09-15 查一个）：
  · **根因①（09-13）**：执行器的 810s 预算是**每 dispatch 归零**的 ⇒ 跑过 ≥2 次 dispatch
    就永远不收尾。**已修**（`0d64ff9` 一族：`RunContext.deadline_at` 全程一把尺）。
  · **根因②（09-15 真机坐实）**：**单次流式调用没有总时长上限** —— `httpx` 的 `read=`
    封的是"两次读之间"、**不是总时长**，而"看表"那句只在**轮间** ⇒ 服务端持续吐 token
    就能跑任意久（量尺：一次 dispatch **1615.7 秒**、`tokens=0`）。**已修**（`0e8bc69`）。
  ⚠️ **两条是两个病，修一条不等于修另一条** —— 这正是它悬了三天的原因。
  ⬜ **唯一还没解决的**：**暂停占着工人**（`while pause_path.exists(): sleep(1)` 把工人堵住，
  `run_queue` 默认并发 1 ⇒ 一个暂停的任务能把队列停摆）—— 已单列进 `待验证清单`。


- [奇点] **规程：派发指令**（09-14 定）——
  ① **不在版本控制里**（原来只在 `~/Desktop/`）**已解决**：指令文件收进了 `docs/`，
  Desktop 上那两个路径改成**指向仓里的符号链接** ⇒ **一份内容、两个路径、进版本控制**
  （实测"通过 Desktop 路径改，`git status` 看得到"）。**没用"抄一份进 docs"那种做法** ——
  那是两份副本，按本仓自己的规矩"**抄一份必漂**"。
  ② **「硬约束」列表要跟着归档一起长**：**每把一份外派产物归档进 `docs/`，它立刻变成
  "下一个窗口的污染源"**。今晚现演过一次 —— 我把 E 的报告归档成 `docs/反审-01-20260914.md`，
  而 E' 的指令是在那之前写的、**没挡它**。⚠️ **已发出去、还在跑的窗口改不了** ⇒ 只能"发之前就想全"。

- [奇点] ⚠️ **规程：外派指令的「硬约束」列表要跟着归档一起长**（09-14 定的，成本为负）：
  **每把一份外派产物归档进 `docs/`，它立刻变成"下一个窗口的污染源"。** 今晚就现演了一次 ——
  我把 E 的报告归档成 `docs/反审-01-20260914.md`，而 E' 的指令是在那之前写的、**没挡它**。
  ⇒ **归档的同一刻，回来给所有还没发的指令加一条**（`~/Desktop/要发的指令.md` 里已加，含
  `S1统一修法草案` / `反审-01` / `文档核对-02` 三条 + "这个列表要跟着归档一起长"那句）。
  ⚠️ 而且**已发出去、还在跑的窗口改不了** —— 只能靠"发之前就想全"。

- [奇点] **规程：外派材料里别写 `文件:行号`**（09-13 定，成本为负的一条）：
  这次回收的答卷**引的坐标全对不上** —— `orchestrator.py:260` 那个 `while True` 实际在
  `:685`、"两条路"的 `:569`/`:783` 实际在 `:578`/`:875`。**不是它编的，是我们材料发出后
  代码又改了一天**（同族的坑我们吃过一次：`防御模式.md` 里的行号同样会漂）。
  ⇒ **以后外派材料写符号名（函数名 / 常量名 / 类名），不写行号** —— 符号比行号稳得多，
  而且**改名会立刻炸出来、行号漂了没人知道**。**没改（材料是脚本拼的，下次生成时一起改）。**

- [奇点] 🔴 **`_rollback_git` 那条路（09-14 修完，留个记录）**：
  `repo_root_for(task)` 对**没有 project 的任务**返回**引擎本仓**（"独立任务"的设计，不是 bug），
  而 `_rollback_git` 会 `stash push -u` + `checkout -- .` + `clean -fd`。
  **两次我都从 `stash` 里把自己的工作区捞回来了** —— 那是运气，不是保证。
  ✅ **两条都已修**：① stash 成功时**出声**（原来零痕迹，`4ac3d29`）
  ② **没有 ref 就不许销毁工作区**（销毁了也恢复不回来，`d21495f`）。
  ✅ 那条会走进这条路的**测试也加了护栏**（把 `rollback` 打桩）。
  ⚠️ `stash@{0}` / `stash@{1}` **我没删**（内容都在里面）。

- [奇点] 🟡 **我的记录漂过号，改掉**（09-14 外派⑨ 查出来的）：
  我在派单里写「`#9` ≡ `扫bug-01` 的 `#1`（超时误杀）」—— **七份报告的第 9 条里没有一条是超时误杀**。
  真身是 `扫bug-03` 核实头那句「#1 和 `扫bug-02` 的 `#9` 是同一条」⇒ 两条都是
  **ProjectState 读-改-写无锁丢更新**，且 `扫bug-01` 的 `#7` 也是同一条（**三轮独立撞上**）。
  ⇒ 去重本身对，漂的是号。**教训：我引编号时是凭印象写的 —— 引编号也要核。**

- [奇点] ⚠️ **外派指令有处没写清（09-14）**：禁读清单只给了**位置**、没给"清单本身从哪拿"，
  于是 ⑤ 那个窗口**为了找回清单定义，去读了 git 历史里的 `OPEN.md`**（与禁读的同源），
  它**主动自报了**。⇒ **下次派活要把"清单本身"直接贴进指令体**，别让人去找。

- [奇点] 🔴 **棘轮的尺子今晚修了两次、撤了一次（09-14）—— 数都不可比了，记清楚**：
  ① **别名虚增**：守卫只认字面 `witness`/`logging`/`log`，而本仓大量用
     `from ...log import warn as _log_warn` ⇒ **虚增 12 处**（`mcp.py` 8 · `web/app.py` 4 ·
     `_api_projects.py` 1）。**已修**（守卫加 import 别名解析）：**真实存量 412**，
     而当晚早些时候报的 426/424/423 **都偏高**。
  ② **本地 helper：试了、撤了**。想让尺子跟"出声收在本地 helper 里"的 handler，
     实测**造出假阴性** —— `orchestrator._reap_futures` 那个外层 handler 的出声其实在
     **嵌套** handler 里（守卫 docstring 专门记过这条），它只是因为调了 `_save_trace` 才算数。
     **"调了个会出声的函数" ≠ "报了自己的失败"** ⇒ 撤掉，把这次尝试和失败原因写进了
     `_call_is_noisy` 的 docstring。
  ⇒ 现在的数 **415**（含 S1 试点新增的 4 个"出声在 helper 里、尺子够不到"的 handler）。
  **往后的数才可比。**
  🔁 **09-14 上午又撞上同一个盲区两次**（`_role_write_error` / `_strand_guard` 那两处，
  都是"handler 的出声写在本地 helper 里"）⇒ 处理是**把出声挪回调用点**，所以基线没涨，
  现在是 **401**。⚠️ **这说明盲区是结构性的、会反复咬**：要么定一条**命名约定**
  （比如"`_warn_*` / `_deny_*` 开头的函数算出声通道" —— 比"跟任意 helper"窄得多，
  上次那次的假阴性来自跟了 `_save_trace` 这种**做事**的 helper），要么每次手工挪回来。
  **没定，记着。**


---

# 逐日补记（2026-09-16 ~ 09-20）

> 来历：这份文件原来停在 **09-15 深夜**（下面那份记忆快照里），四天没写，这一段是补的。
> 依据：**提交历史** + `docs/防御模式.md` §73–§85 + `docs/待验证清单-20260912.md` +
> `~/OPEN.md`，四天分别 **29 / 32 / 17 / 61 / 8 个提交**（按提交日期数）。
> 🔴 **三种状态分开写**：**真机验过** / **修了没验** / **只是提出来没做** ——
> 它们在历史上长得一样，后果完全不同，别把它们混成"都做了"。

## 2026-09-16（29 个提交 —— 两个"判据永远轮不到"的真机卡死 + 一晚真机验证）

**凌晨那两笔是真的现场**（各自卡死 **30+ 分钟**，而 `stream_over_budget` **全库 0 次**）：

- **§73（`5d4f2710`）判据的检查点挂在「事件到达」上 —— 而故障恰恰是「事件不到达」**：
  判据写在 `for line in resp.iter_lines()` 的**循环体里**，而服务端只要**一直发字节、却凑不满一行**
  （`httpx` 的 `LineDecoder` 会一直缓冲），循环体**一次都不进** ⇒ 判据永远轮不到；
  同时 `read=` 那把尺量的是"**两次读之间**"、每来一个字节就**重置** ⇒ **两条保护各自独立地被绕过**。
  改用 `iter_text()`（**每批字节都过判据**）。**同形的第二处** `execution_judge._stream_once`
  （融合那条，**连判据都没有**）一起改。
  · 定位手法：`py-spy dump` 要 root、这个会话给不了密码 ⇒ 换 **macOS 自带的 `sample <pid> 3`**（免 root）
    拿到全线程栈；再拿一个**独立探针**（本地 server + 裸 httpx）实测三种"服务端不吐东西"的长相，
    **真机死的是第三种**（吐字节凑不满一行）。
- **§74（`3a73b300`）共享的 MPS 模型被并发使用 ⇒ 整个后端进程无声消失**：
  委员会多模型并行，每个分支都去查记忆/技能 ⇒ 一起进同一个 `SentenceTransformer`。
  **Python 层一个字都没有**（无 traceback、`curl` 回 `HTTP 000`）；真机 `POST /start` 后 **8 秒进程消失，
  重试一次又消失**。崩溃报告在 `~/Library/Logs/DiagnosticReports/`：`SIGSEGV / at::native::mps::copy_cast_kernel_mps`。
  实测：单线程 `encode()` 正常，**8 线程同时 `encode()` 同一个模型 = 必崩（退出码 139）**。修法：加载＋推理都收进一把 RLock。
- **当场闭环（当晚最值钱的一段）**：`01:15` 用新代码续跑 → `02:01:48` **`stream_over_budget:fusion:600s` 判据响了、切断**
  → `02:04:27` `planning → gate2 架构完成`。**昨天它会永远挂在那儿。**
  ⚠️ 这也**坐实了卡的是融合那条路（`execution_judge`），不是 `openai_agent`** —— 只改 `openai_agent` 的话当晚照样过不去。

**上午 `12:17` §75：同一个形状的第三、第四次** —— `_STALL_TIMEOUT` 的注释本来写着「多久**没新 token**」，
实现却在量「多久没**字节**」⇒ 一次调用可以**烧满 240s 而零产出**，几轮就把 810s 预算烧光。
改成比 `content/reasoning/tool_calls` 的**长度**（**变长才算进展**），超时**抛错换 agent**
（不能"出声后返回空" —— 那会被上层读成"这次调用**成功**了、只是模型没说话"）。
⚠️ **真机没验到**：那一轮**没有**出现停滞错误 ⇒ 它的前提当时不成立。真机现场那会儿是：
`test_fizzbuzz.py` **185 行**躺在项目仓一条**悬空提交**里，配上已合并的 `fizzbuzz.py` 拷出来跑 —— **25 个测试全过**。

**白天的几笔零碎**：`00:18` 交付栏读错了地方（报告栏和 E2E 清单两栏永远空）·
`18:28` **死线已过还发起一次 dispatch** ⇒ 空壳盖掉前几轮真干出来的那份 ·
`13:47` **删掉临时探针 `qa_raw_probe`**（它响过一次、答完了，留着不该留）。
**`13:4x` 环境清干净（用户拍板）**：三个真机项目**全部删除**，证据归档到
`~/项目/_归档-20260916-真机验证证据/`（README 写了来历 + 3 份 `git bundle` + 产物那份 **40 个测试全过**的源码 + 告警摘录）；
⚠️ **账本没清** —— `usage_daily.json` / `token_usage.json` 里那 **~46.9 万 token 是真花的**，见到"孤儿项目 id"别当故障。

**`19:47` 更正「北极星」的归属（`2a8e324b`）** —— 用户说的是那句担忧，**「北极星」这名字和那五条判据是我拟的**，
原标注写成"用户提的"**是错的**。同一晚自查出它**本身有病**：五条里**三条在量账簿、不在量交付**，
唯一碰产物的那条「产物能看懂」**是主观的、要人看**。后果实证：真机上**任务全 failed、manifest 三项全空，
产物却是完整且 40 个测试全过的**。

**晚上跑真机验两条**（`验证-两栏-20260916` / 项目 `1789563770837`）：

- **③ 交付清单两栏空 —— `reports` 那半 ✅ 真机验过**：`delivery_manifest.json` 的 `reports` **非空**
  （`qa_report.json` / `qa-report.md` / `security-report.md` / `machine-checks.json` 四个；**之前六个项目全是 0**）。
  **E2E 那半 ❌ 还是空** —— 根因不是路径，是**没人写 `test_cases.json`**。
- **④ 死线空壳 ✅ 真机验过**：四个任务 `changed_files` 都非空、trace 的 `elapsed` 是 **19.2 / 18.3 / 27.4 / 12.6 秒**
  （**不是 0.039**）、**一个 <1 秒的 dispatch 都没有**。顺带把 **`[只读]` 豁免**从"半验"变成真验到
  （T4 零改动判 `done`，没被"无文件改动"打死）。

**同一晚 §76：三个"只做了一半"的形状**（一晚连撞三个，说明不是运气）：

- **`6f640d4d` 不变量只在读的一侧执行** —— `api_store` 的 `_` 前缀是元数据键的保留前缀，
  `_load` 认、`_save` 也认，**只有 `add()` 不认** ⇒ `POST {"id":"__probe"}` **200、写进文件了**，
  然后 **建得进、看不见、删不掉**。修法：提成**一个函数给两边共用**（这个 bug 本身就是"判据只在一侧"造成的）。
- **`a4f57d81` 「禁工具」拦不住时只出个声** —— `no_tools` 是**四条路的共同前提**（委员会 / A 臂自修订 /
  单模型"只出 JSON"的阶段 / 委员会合成），而自带工具面的执行器（`claude-cli`）**直接忽略它**。
  代价是**实测过的**：**调研员在项目仓里把整个项目实现完了**、合成 agent 把目标项目的架构写进了**奇点自己的 `docs/`**。
  规则：**"出声"不等于"拦住了"** —— 是前提就 fail-closed。
- **`4eb63cdb` 一个文件只有读的人、没有写的人** —— `<项目仓>/test_cases.json` 有**三个读者**、
  全仓**没有一处写它** ⇒ `if tc_path.exists()` **恒假** ⇒ **集成测试从来没跑过**，而且**静默**
  （跟"本来就没有集成用例"长得一模一样）。⚠️ **补上写入端之后才炸出第二个 bug**：那段检查跑的是**写死的**
  `pytest -k test_integration`，而清单声明的名字**一个都不含它** ⇒ 一条都选不中 ⇒ pytest 退 **5** ⇒ 老代码 `!= 0`
  ⇒ **整轮交付判失败**（**只补写入端会让下一次交付直接炸**）。⇒ 推论：**一条"从来没被激活过"的检查，
  激活它的那一刻要当成新代码来验。**
- ⚠️ 同一晚验证时我**自己踩了三个坑**（记进 §76）：变异**打偏**（同一句话在文件里出现两次）·
  把 **pytest 退出码 1 误读成"挂了"**（那是"有测试失败 = 变异红对了"）· 变异**挂住整个测试进程**。

**其它**：`532ae897` 撞上限且零产出 ⇒ **抛**（F1，不能返回"看起来正常的空回答"）·
`6aa15391` GATE2 两种处境长得一模一样（兜底升上来和初次审架构同一句话，**真机没验**）·
`8675358e` 惰性辐条的导入循环加互斥（**当时以为根因是并发竞态** —— 见两天后 §81 的翻案）·
`cb684df0` 评审/QA 那几路的 `collect_changes` 假警**一周 338 次**、霸占告警页第一名 ·
`a0eda811` 一处"89% 告警没 key"是**累积文件的测量误差**。

**⚠️ 09-16 晚修的那几条（自检 + 变异验证 + 全量 1503 绿）真机没验**：死线后不再发起 dispatch
（`_exec.run` 的 turn 循环 + `_run_with_retry` 的重试循环各加一道 `_budget_exhausted`）·
**收尾那条 `return` 补上 `merge_request`**（原来漏了，前面几轮 build 出来的合并请求整份丢掉 ⇒ **永远不合并**）。
`23:15`（`a2bc687f`）把 09-17 凌晨那轮要验的判据**逐条写死**。

**🔵 也是这天定的**：**拿一个真实规模的项目跑一遍** —— 十几次真机跑**全是同一个 FizzBuzz**，
"它是不是花架子"只能靠这个实验回答。判据不是"跑通"，是三样：**崩在哪一步 / 崩几个 / 中途产物能不能用**。


## 2026-09-17（32 个提交 —— 第一次拿真实规模跑：11 个任务 6 成 5 败 / 71 分钟 / 39% token 打水漂）

**凌晨那轮换了题目**：不再是单文件小工具，是 **JSONL 日志统计 CLI**（解析/过滤/统计/输出/入口 五个模块），
项目 `1789571713224`。**11 个任务 / `concurrent=2` / 71 分钟 / 6 成 5 败 / 39% 的 token 打了水漂。**
**开跑前先把环境摆正**（否则整轮不算数）：后端**重启过**（旧进程 21:02 起，而待验的修复都在 21:48–22:57 落地，
Flask **无 reloader**）· 前端 `dist` mtime 与最后一次前端源码提交**同刻** · 告警基线 **2714 行**。

**§77 一夜挖出七个形状**，收口时发现是**一个病在七层上复发** ——
**每一层的判据都在量「声明」（状态字段 / 登记表 / 文件名 / 消息数），而故障永远发生在「声明与实际之间」**：

| 谁在说 | 它说的 | 实际 |
|---|---|---|
| 任务 | 我在跑 | 没人管它 |
| 调度循环 | 我在转 | 没有进展 |
| 合并队列 | 我在等依赖 | 那个依赖永远不会来 |
| 告警 | 我出声了 | 没人听 |
| 观察者 | 我盯着呢 | 喇叭是空的 |
| 代码仓 | 这是我的源码 | 一半是字节码 |
| 判据 | 我检查过了 | 检查的东西没人被要求造 |

逐条细节见 §77.1–§77.10，这里只记**那天到底发生了什么**：

- **🔴 依赖失败 ⇒ 合并队列永久 defer ⇒ 调度永久自旋、任务永久卡 RUNNING** ——
  `_deps_satisfied` 要求依赖**全部 DONE**，而 **FAILED 是终态、永远到不了 DONE** ⇒ drain 永远 defer
  ⇒ 任务永远留在 pending ⇒ 循环的睡觉条件恒 False ⇒ **全速空转**。实测 **1731 条 `drain_dep_blocked` / 2 分钟、
  44 分钟 CPU**；一晚复现两次、**重启后仍复现**。修完 **CPU 44 分钟 → 17 秒、空转告警 → 0**。
  ⚠️ 这是**「修了一半」的第四个**：注释早写着"1 个依赖未满足就能让 drain 永不返回"，**他们只修了内层**（死循环换成了外层空转）。
- **🔴 热循环没有退避** —— **不是卡死独有的，是常态**：重启后 **3 分钟又刷 2292 条**。判据要是"有没有进展"，不是"表里有没有东西"。
- **🔴 观察者的喇叭是空的 —— 谁来监督守护者？** 卡了 **50 分钟**、早该响。告警广播给 `_pending_replies`，
  而唯一正经的注册入口 `register_client` **全仓零调用者** ⇒ **没人聊天时，广播给一个空字典、无声消失，而且一个字都不落盘**。
- **🔴 `.pyc` 一个根因打三枪**（项目仓没有 `.gitignore`）：合并二进制冲突（T3/T5）· 集成检查判**"工作区不干净"**
  ⇒ 在第①步就返回 · **交付包里躺着 7 个 `.pyc`**（坑漏到收件人手里）。前十几轮没撞见只因为 FizzBuzz 单文件、pyc 不交叉。
- **🔴 判据要的输入，没有传到产出它的人手里**（**这一夜撞见两次**）：`test_cases.json` 三个读者**没人写**；
  架构声明 10 条约束、每条指定一个测试文件，而 **10 个全没被创建**
  （构造执行器上下文时**只传 `rule` 的文字、不传 `check`**）⇒ 机器检查 `ran=10 passed=0`。
- **🔴 `resolve` 解完冲突后任务永远卡 `conflict_held`** —— `_mark_merged` **只改内存不 `tracker.transition`**
  ⇒ **进得去、出不来**；而且**没有任何 API 能把任务从 `conflict_held` 挪出来**。
- 另两条：**排队等的时间算进执行预算**（排队实测 4s…**1732s**，**972s 那个开始出问题**）·
  **调研报告解析失败 ⇒ 界面是个空框、一个字不提示**（兜底对象字段全缺，而前端**压根不认识 `parse_error`**）。

**当晚修 7 条**（`4eae901` 依赖/退避 · `5c61755` `conflict_held` · `6ca1d92` 观察者告警出口 ·
`0803540` 建仓写 `.gitignore` · `b1cfacd` 约束进执行器 · `6de62c4` 调研兜底**说清自己是截断的** ·
`c390ade` 集成检查选不中名字要**真跑第二条命令**）。⚠️ **一条都没真机验过**（全量 1556 绿 ≠ 真机验过）。
**没修的还剩 3 条**：§77.3 孤儿判据（⚠️ **原写的替代判据 09-19 实测站不住、已划掉**）·
§77.4 告警轮转**把诊断证据冲掉**（开跑前 2714 行 → 事后 2175 行）· §77.8 排队算预算（**要人拍板**）。

**凌晨那轮带着的「已修未验」六条**（判据写在 `待验证清单` 里）：

| # | 结果 |
|---|---|
| B 告警两个数掉没掉 | ✅ **两个都是 0**（`collect_changes` 修复前近 24h 是 98、`lazy_spoke_import_failed` 45）—— **修复生效** |
| D 聚合视图三处 | ✅ **触发了、而且真出产出了**：`memory 抽象 2 条轨迹` + 一条 `insight`（`consolidate:` 告警 0 条） |
| G 停滞/上限判据 F1+F2 | ✅ **三种形状全跑出来**（有产出→不抛 / 零产出→抛 / 只剩 6s 的小壳子→抛），与 `_exec` 注释写的行为**逐条一致** |
| A 交付清单 E2E 那半 | **①④✅、②③没验到** —— 集成检查在**第①步"工作区不干净"就返回了**，没走到跑 pytest 那步 |
| C / E / F | **没触发**（C 要"审查自动修达上限"、E 要 worker 在两步之间断掉，都难触发）—— **没触发 ≠ 没问题** |

**下午那一段是用户一句话引出来的**：他在 GATE1 上问「**怎么不能看报告**」「有修改途径吗」——
查出来是**三件事叠在一起**，当天下午全修完并**在真机上验完**（`f055c37c`）：
GATE1 上**一处都看不到调研报告**（`GatePanel` 和 `Chat` 两道门一起挡死）· 摘要条把"解析失败"说成
「⚠ 调研没给推荐方案」（**把锅甩给调研**，判据量的是"字段在不在"、故障在"报告坏没坏"）· 报告 **8 段只露 3 段**。
验的方式是**真组件 + 真数据**：8 段全出现，而且 `note` / `verification_commands` 这两个
**我写代码时不知道的键自己冒出来了** —— 这比"8 段都在"更能证明"模型多吐一段也能露出来"。
**打回能说理由**这条也是真机验完的：接口回 `started`、`phase` 真变了（`gate1 → researching`）· 理由落 lineage ·
**用真机状态倒回"刚打回那一刻"重拼提示词，末尾真出现了那句理由、且只生效一次** · 模型真照做（竞品 **6 → 12 家**）。
⚠️ **但同一件事的另一面当天就被证伪**：四跳全通，**而三轮 `uncovered [7,8,9]` 一字未动** ⇒
**"理由进了提示词" ≠ "模型会照做"**。（09-17 用户拍板：暂时不动，系统报到人眼前就够了。）

**下午·晚那轮专门验那 7 条**（项目 `1789618855407`，**没跑完**）：**6 条验完 ✅、1 条没被走到 ❌**

- ✅ 项目仓 `.gitignore`：`git ls-files` 里 **0 个 `.pyc`**（而且是**跑过 pytest 之后**的结果）
- ✅ F3 产物可打捞：失败的 `…3920` 带出 `salvage_ref`（18k tokens / 10 次工具），而 `…3921/922/923` **没有**
  ⇒ **"没得捞"和"没捞出来"分得开**
- ✅ 依赖失败锁死调度：`…3925` 的 5 个依赖里 4 个 `failed`，**它照样被派发并真跑起来**；
  **合并路径也走通了**（项目仓里累计 **9 个 `merge … via queue` 提交**）
- ✅ 观察者的告警出口：`alerts.jsonl` 里**有** `observer_stalled_task`（14:14:54 / 15:20:00）——
  **此前统计是 0 条**（"观察者是 40 个写手里唯一不产出告警的那个"，那句作废）
- ❌ `conflict_held`：**场景到了但没验完** —— `…3925` 真进了 `conflict_held`、parked 记录也在，
  **但一直等人解、没解** ⇒ 项目停在 `executing` **等了一个小时+**
- ⏸ 另外 3 条（约束进执行器 / 集成检查 / 交付包里没 pyc）**没跑到**
- 这轮**新发现的形状**：`stream_over_budget` **全是 0 字符、而且越来越长**（240s/6064字 → 321s/0字 → 576s/0字）·
  **心跳分辨不出「模型慢」和「工人死了」**（17 分钟无心跳仍不能断定）· 观察者的停滞告警**会误报 `conflict_held`**
  （判据分不出"在等你"和"没人管"）· **排队时间扣预算量到准数**：`917` 排队 **8 分 17 秒** ⇒
  上场只剩 **400s（44%）**，但它照样干了 **94k tokens**、22 次工具、写出 6430 字符的 `output.py`
  ⇒ **不是"上场即死"，是"剩余预算 < 一次调用所需"才死**（我先前那句被真机打脸）。

**夜里连着跑了好几轮（round d/e/f，同一个项目）**：

- ✅ **打回重做真的重建了任务**（`7d24625`）—— 打回 GATE2 后重规划 `task_ids` 清成 **0**，
  批准后建出 **8 个全新任务**（**与旧 9 个重合 0**），"拿旧任务 10 秒冲过执行层"**没复现**
- ✅ **`a0e8f19`（兜底升 GATE2 之前先把验收跑掉）验到** —— round e 走的正是兜底那条路，
  `issues` 里出现了埋的标记；对比修复前**人站到 GATE2 面前手里什么都没有**
- ✅ **调研解析失败重试（`9bc4c3b`）验到** —— lineage 里 `research_parse_retry` → `research_complete`。
  🔵 而这是**当天第 4 次**调研解析失败 ⇒ **它本来就常发生**。
  **根因当晚还更正过一次（`14e5c21b`）：不是"被截断"，是字符串里没转义的双引号。**
- 🔵 round e **只失败 2/7**（round d 是 7~8），快跑 deadline 没怎么伤质量

**夜里插了三把尺子**：**分诊账**（`a1ea7689` + `3dcb85c8` 补 `model` 字段 ——
不记是哪一家，就分不出"这家慢"和"这家卡了"）· `stream_over_budget` **多带 `loops`/`idle` 两个数**（`736f8792`）·
`TASK_DEADLINE_S` 支持 env 覆盖（`15098b07`，**一轮 90 分钟压到 20 分钟**）。

**⚠️ 我自己那晚踩的**：`22:57` 杀掉 round d 的项目 + 重启后端，以为完事了 ——
结果它的任务**被回收成 PENDING 又派下去**，`23:06` 还在跑、**烧了 9 分钟**，还和 round e 抢模型调用。
⇒ **「废弃一个项目」不等于「停掉它的任务」**（同族的坑清单里早就写着"重启会把 RUNNING 回收成 PENDING 重派"
—— **写着 ≠ 我会照做**）。同批：round d 重规划后同一个项目上有**两批共 17 个任务平铺**，
用户当场问"都到十七个了"（**数据层没问题，是显示层把它们混在一起**）。


## 2026-09-18（17 个提交 —— 闸门装错层：测试全绿、变异也过，钱照烧；15 个失败任务没一个"做错"）

**起因是用户一句话**：「每次怎么都失败这么多」。核下来 ——
**15 个失败任务没有一个败在"做错"，全败在"没产出"**（12 个 `无文件改动 + 偷懒信号`、3 个 `空输出`）。

**§79 一条链，每一跳都读过源码**：

```
① 240s 硬顶掐断（openai_agent.py，跟任务 deadline 无关）
② content = content or reasoning_content —— 把半截思考当成了任务的产出
③ 拿这段思考去判它「偷懒」（changed_files 为空 ⇒ 两条软信号必然同时亮）
```

**最荒唐的一跳是③**：判"偷懒"时扫的 `agent_output`，**扫的其实是模型自己的思考过程**。
接口**本来就**把 `content` / `reasoning_content` **分开发** —— **是我们自己在②合起来的**；
兜底本身有理（有的模型把答案写在思考里，不合就白跑），**错在它没有条件**。

✅ **修了 `d664f5a`（三处缺一不可）**：`_stream_call` 出口带 `_cut` · 掐断时不拿 reasoning 兜底 ·
**掐断且正文空 ⇒ 直接收尾**、标 `error_kind="deadline"`（`_dispatch_exec` 认这档**不记熔断**）。
⚠️ **只改前两处会「更烧钱」**：正文变空 ⇒ 轮循环**再调一次** ⇒ 同一个模型同一个上限**再烧 240 秒**。
⚠️ **真机没验**。**没治的两条**：① **刀还在**（240s 照样掐，只是失败理由诚实了）；
② **失败就丢改动** —— 前几轮明明写过文件，**一失败就报"改了 0 个"**（三条失败 `return` 都不带 `changed_files=`）。
**② 比①更狠：它冤枉的是已经干过活的任务。**

**同一夜出了 `docs/判据错位审计-20260918.md`**：**实 19 条 · 面 2 条 · 疑 6 条**，
且它们**几乎是同一个治疗点的分支** —— 把「**这次是被我们断的**」从 `_stream_call` 一路贯通到判据
（现在它**只写进告警 + 分诊账两条没人读的旁路**，而 `error_kind` 的枚举里**压根没有这一类**）。
⚠️ 边界：没审 `observer/`、`_api_admin.py`、前端、`zhipu_api.py` 函数体。

**§78 闸门装在错的层**（真机）：用户把 `glm-5.2` / `deepseek-v4-pro` **停用**了，
可分诊账里这两个**每 20 来秒还在花钱**，prompt 6 千来字符、全被 cap 掐断。

- **第一次修错了层**（`eef16fb`，装在融合侧 `execution_judge._call_model`）：**全量测试绿、变异也验过**
  —— **`00:35:28` 重启后 `00:37` / `00:38` 分诊账还在进**。告警给出了真路：`review_pool_expanded:glm-5.2+deepseek-v4-pro`。
- **真根**：审查池里只剩一个审查员 ⇒ `_expand_review_pool` 从**模型注册表**补人，
  而它只问 `agent_api_available`（**只看 provider / key，完全不看激活池**）。
- **修在所有选路的"下游汇合点"**。**真机判据拿到（round g `01:05:17`）**：同样的触发、前三行只变第三行 ——
  `review_pool_expanded:glm-5.2+deepseek-v4-pro` → ✅ `single_reviewer:deepseek-flash:pool=2`，
  分诊账里禁用模型 **0 次**。**代价（有意）**：审查退化成单 reviewer。
- ⚠️ **验证这条路时踩的两个坑**：**(a)** 在"任务全终态"的项目上批准 GATE2 = **执行层 1 秒冲过、
  逐任务那条路压根不走** —— **告警三条全空，看着像"修好了"，其实是"没跑到"**；
  **(b)** **触发条件比代码更早决定"验得到验不到"** —— 那次泄漏在我的修复（00:51）之前，
  **先被配置（00:40:41 把 `reviewing` 改成两个模型）改掉了** ⇒ 复现这类 bug 必须**先把配置恢复到触发它的那份**。

**§80「没动过」被当成「有产物」** —— `commit_wt` 在**没有改动**时返回的是工作树的 HEAD
（工作树是从 `ctx.snapshot_ref` 建的 ⇒ **那就是基线本身、是别人的提交**），
而调用方**无条件**把它锚成"这个任务的产物"。修在**调用方**（`branch_ref != ctx.snapshot_ref`），**不是改原语**。
⚠️ **别用 `_has_changes_in_wt` 当判据**：它看的是"**未提交**改动"，而 agent 自己 `git commit` 过之后工作区是干净的
⇒ 用它会把**真的产出当成没产出、连合并都不做** —— **比原 bug 更坏**。

**`88629bb` 一条脏数据毁掉整条建任务流程**（`00:1x` **正在刷屏时抓的**）：架构第 11 条约束的 `check` 写成**散文**
（⚠️ **散文写法是设计允许的**），而 `execution_judge` 假设它一定是 `{"argv":[...]}` ⇒ **`decompose_architecture` 整条崩**
⇒ `alerts.jsonl` **每 3 秒一条、刷了 331+ 条**、项目 `phase=executing` 而 **`task_ids` 恒为 0**（一个任务都建不出来）。
改成**复用** `_machine_checks.parse_check`。

**`glm-5.2` 烧掉当天 61% 的花费 —— 而它是被 cascade 当"执行工人"用的**（用户问"烧了 50 块"时查的）：
`$0.5017 / 61.4% / 4 次 / 250,833 token`，`by_model_level` 是 **`any`**（不是 `fusion`）。
⇒ 我先前"它是融合提取员、每轮只跑 1~2 次、影响有限"**是错的**：它在**启用池**里 ⇒
**cascade 换人会挑它去干执行层的活**。全走 API 改：`glm-5.2` 停用 · `glm-5.3` 进 any 档 · `extract` 改 `['glm-5.3']`。
⚠️ 两者仍比 `deepseek-flash`（425 字/秒）**慢 3 倍、贵 4 倍**。
🔴 **同族的另一条（09-17 两组独立实测）**：`glm-5.3-flash` 比 `deepseek-flash` **慢 2.5~3 倍**，
而 `cascade_skip` 的落点**正是它** ⇒ failover 落在慢的那家、吐不完被掐。
⚠️ **过程教训**：我第一版对照脚本**把智谱的 key 发给了 DeepSeek**（三发全 401），那条"glm 慢"的结论**当时是脏的**。

**🔴 ZCode 事件曝光（09-18）** —— 智谱的桌面 AI 编程工具默认开「代码库索引 / Repo Wiki」，
**UI 开关关不掉、隐私政策没披露**，包里含**完整 git 历史 / LFS / reflog / 全局配置**（`.git` 占 86.6%）。
本机核实：它的工具调用里有 `Edit`×60 / `Write`×18，路径是 `/Users/jingzhe/projects/singularity/...`
⇒ **它真在这个仓库里读写过**（核实过，不是猜的）。核过的一面：**git 历史是干净的**
（全 ref 搜 `DEEPSEEK_API_KEY=` / `sk-` **零命中**、`.env` 从未被提交过）。
全文 `docs/ZCode-事件-20260918.md`（09-19 卸载收尾 + 拍板 key 不换）。

**外派评审第二轮（找 bug，逐条核实）**：**S1 成立而且最重** —— `_start_background` 把**内存对象**
传进分钟级线程（四个调用点**全传 `proj` 对象**，隔壁异步那条收的是 `project_id`）⇒ 整份覆盖写 ·
**S2 成立** —— `repo_dir` 注释"绝不能 mkdir"，下游 `_worktrees_dir` 照样 `mkdir`（盘上坐实）·
**S4 成立** —— `heartbeat` 裸 `write_text`，读侧把读坏的**删掉且一声不吭** ·
**S6 成立但按现部署打不到**（`start.sh`/Dockerfile 都是单进程）⇒ **不改** · **S5 未核**。

**`01:49` 清验证轮项目前先备份（`9c90da7d`）**：3 个 release 标签 + **14 个可打捞 ref** + 3 个被清项目，
存 `docs/清理前备份-20260918.md`（`~/项目/项目仓库/` 那层东西**仍在磁盘上**）。

**晚上 `21:00–21:27` 又修 4 条 + §81**（⚠️ **真机全没验**）：

- `164429f3` **阶段产出覆盖写之前把上一版归档**（打回重做不再只剩一版）
- `4e3718f8` **只有"产物真进了项目仓"才许松手**（超时 / 冲突 / 清残留都不许）
  ⚠️ 副作用：`refs/qidian/pending/*` 会**攒得比以前多**（**这是有意的** —— 那正是"产物在、只是没进仓"的地方）
- `d07414b1` **`lazy_spoke_import_failed` 的真根是 importlib 在探 dunder，不是竞态** ——
  跨三天 **80+ 次**；09-16 加的那把锁**从原理上**就挡不住（**同一个线程自己绕回来**，RLock 同线程放行）。
  手法：**挂探针记录每次 `__getattr__` 的入参 + 调用栈，跑全量测试实测**（**不是推断**）；
  量到一次全量测试**惰性循环被白跑 103 次**（`__path__` 64 + `__test__` 26 + `__bases__` 13）。
  ⚠️ 教训：**"加了锁"和"治好了"是两回事**；判对症状、判错**主体** ⇒ **修在不生效的地方**。
- `aecb82fe` **空壳 `BatchOutput` 不许顶掉真结果**（`prev_batch` 原来是无条件赋值的）
- 🔵 副产品：**告警账本会被测试污染** —— pytest 导入测试模块（收集）发生在**任何 fixture 生效之前**，
  跑一次 `pytest tests/` 就往**真** `.qidian/alerts.jsonl` 写 1 条；我当天**差点拿它算出"后端空转时反复失败"**。
  修法：`tests/conftest.py` 在**导入期**就把 `QIDIAN_DIR` 指向临时目录。
  （**同族的假隔离 09-19 又抓到一处**：`test_exec_run.py` 一直在往生产 `alerts.jsonl` 里写。）
- 🔵 同一晚**改掉 3 条错的旧结论**（"并发竞态" / "先导出 `salvageable_refs`" / "cap=6s 注定空"）—— **别再照旧话做**。


## 2026-09-19（**61 个提交，四天里最大的一天** —— CI 第一次能绿 / 外派评审 A1–A10 全落地 / 静默失败那一族收口）

**① 凌晨：把那一族静默失败按「删掉了自己唯一的判据」收口（§82，四条）**

- `1db42d6d` **`tracker` 读路径静默吞错 ⇒ RUNNING 幽灵** —— `read_task` / `ready_tasks` / `recover` /
  `_load_all_tasks` **四处**一律 `except (JSONDecodeError, TypeError, ValueError): return None / continue`、
  **零留痕**（实测 tmp 里 `alerts.jsonl` 从头到尾**没被创建**）。最毒的一环：`Task.from_dict` 结尾是裸 `cls(**d)`、
  **不丢未知键** ⇒ 一条 RUNNING 任务只要**多一个键**就读成 `None` ⇒ **它永远是 RUNNING 的幽灵**。
  四处合成 `_read_task_file()`，走 `_io.load_json_or_quarantine`。
- `6ca7e8aa` **`witness` 删掉自己唯一的输入，且一声不吭** —— 写侧 `heartbeat()` 用的是**裸 `write_text`**
  （全 `.qidian` 唯一不走 `_io.atomic_write_json` 的状态写入）⇒ **能撕**；读侧三处 `except: p.unlink()`
  **都没有 warn**。一次撕裂 = **一个真卡死的任务从告警系统里消失**。写侧改原子写，读侧删之前**先报一条**
  （**走 `alerts.jsonl`，不走日志** —— 换成日志等于没修）。
- `68664faa` **超时写的"停"标记冒充了用户取消** —— 调度器超时写的标记和**人工取消**共用同一个文件、
  同一个消费者 `_check_cancelled` ⇒ **我们自己的超时被记成"用户取消了"**（原来写的是 `"{}"`，全仓唯一一个**空 body** 的取消标记）。
  body 带 `{"by":"timeout"}`，两种分开报。⚠️ **修之前先把"泄漏面"核完**：三条重排队路**都够不着**
  ⇒ **要修的是"冒充用户"这件事本身，不是"标记该不该写"**。
- `7bdeb34` **「绝不能 mkdir」的兜底路径，被只读的调用点 mk 出来了** —— 盘上坐实
  （`.qidian/projects/<id>/` 下**只剩一个 `.repo-worktrees`**）。改成**只算路径**；四个调用点里**只有一个真要建**，
  而它调的是 `git worktree add` —— **git 自己会建父目录**（2026-09-19 实测，别猜）。
- 同批：`ef74ddd` REVIEWING 分支**补防重入守卫**（验收会同时跑两遍）· `bded9d8` `_start_background` 收 `project_id` ·
  `d998c38` `save()` 写盘前检「盘上是不是比我新」（**只报不改**）· `169081e` **重试不再提前释放锚**（**你拍的板：按原则改**）。
  🔵 `169081e` 那条的**经过值得记**：我先前说"没人记过"**是错的**（代码里写得清清楚楚，是有意取舍）；
  要动手时又撞到一条**专门钉住旧行为**的测试；**真正定案的是那个文件自相矛盾** ——
  文件头写「**merged 是唯一**让'产物已安全进项目仓'这句话成立的分支」，而那条测试却允许重试松手。

**② 凌晨到早上：CI / lint 那一役（这一天最硬的一块）**

外派给了一份「CI 从来没绿过」的审计。**我逐条自己跑过**（`docs/CI与发布审计-20260919.md`）：

- **它说 113 次全 failure，我跑到 117 条**（`2026-09-04 → 09-18`）—— 结论不变、量级对。
  `.github/workflows/test.yml` 里 **`ruff check` 排在第二位**，它一红，**后面四步
  （`test_imports` / `pytest` / `test_exec_run` / `mypy`）全 skip** ⇒
  **"1645 条测试全绿"是只在本机成立、第三方通过仓库自己的自动化复核不了的声明。**
- ⚠️ **它有两处说错**：① 它建议加的 `--cov-fail-under=20` **一直在 workflow 里**（照抄会做出一个
  "新增了一个早就有的东西"的空动作）；② CI 那条 `--select=E,F` **会重开项目自己 ignore 的规则**（`E501` 75 条）。
- 🔵 **我核出来的**：`Makefile` 走 pyproject **1021 条**、CI 那条命令 **757 条** ——
  **同一个 lint 有两个互不预测的定义**（路径 / 规则集 / ignore 生效性三样都不同）⇒
  `make check`（= 仓库自己定义的"完成判据"）**必然失败** ⇒ **它不指导任何日常动作**。
  另：`mypy ... || true` **结构上不可能失败**；dev 依赖声明了 `pre-commit` 而根目录**没有配置文件**。
- **顺序我改了它一处**：**先把 CI 拆成 `lint` / `test` 两个 job**（`bf618137`，约 10 行 yaml）——
  立刻让"测试那一半**是真绿的**"变可见，lint 债之后慢慢还。**不要**改成 `ruff ... || true`
  （**§78 自己写着"假门比没门坏"**；**重排/拆 job 和放行是两回事**）。
- **当晚清了 1021 条 lint 债**（`0eb2ed1e` / `5e67d972` / `47623a5c` / `a59308bd` / `8560673d` / `c9eb17d7` / `98578022`），
  **踩了三次同一个形状（§83：批量改代码时，定位必须来自"工具给出的精确坐标"，不能来自"这一行看起来是要改的那个"）**：
  · **F401 自动修把「再导出」当死 import 删了 ⇒ 53 个测试文件 ImportError** —— 而那句
    「名字保持不变：全仓有别的模块 `from ...executors.base import _BLOCKED_PATTERNS`」的注释
    **就长在它底下**：**注释不是判据**，工具不读，**我也没读**。
  · **B007 是按列号点名的，我按"位置"猜** ⇒ 改错了变量、**`task_file_overlap` 整条失效**。
  · **E741 改推导式变量 = 改整个推导式**，不是那一行 ⇒ F821 3 条。⚠️ 同晚还有一次**假修复**：
    验证脚本里写 `assert "\bl\b" in line` —— **非 raw 字符串里的 `\b` 是退格符**不是词边界
    ⇒ 断言恒假、修复没落盘，而我以为改好了。**量尺写错时，先怀疑尺子。**
- **§84 最值钱的一笔：债里藏着真 bug** —— `F823` 让 `openai_agent` 里那条**最该留痕的告警**
  （「模型不吃思考参数 → 摘掉重试」）**一跑到就抛 `UnboundLocalError`**；`B023` 闭包捕获了循环里会被重绑的 `draft`
  （**"现在恰好对"不是"对"**）。🔑 这两条能躺这么久，是因为**门从来没绿过** ——
  **清债不是保洁，是把没人看的那份报告重新摊开。**
- 顺带两条：**别让测试结果取决于"这台机器上有没有 `.env`"**（`79c8717c`）—— `app.py` 在 **import 期**读 `.env`
  ⇒ 同一份码**单跑红 / 全量绿 / CI 红**，靠"CI 第一次真跑测试"才露出来；**四条 ruff 豁免逐条写了理由**（`98578022`）。
- ✅ **CI 的门 2026-09-19 第一次全绿**（此前 **120 次运行全是 failure**）。

**③ 白天：外派评审（09-19）逐条落地 —— A1–A10 + B1–B4**

处理结果表在 `docs/外派评审-20260919.md` 顶部。**每条都配了会红的用例（变异验过）**，
`make check` 绿 · `pytest tests/` **1740 passed**。最重的两条：

- **A1 两个审查模型判 abort，任务照样 `ok=True` 合并** —— `verdicts` 分支只设 `action="retry"`、
  **不扣 confidence、不写 warnings、不写 unverified**，而 `_decide_cascade` 的第一判据是 `conf >= 0.75`；
  算术上「长输出 +0.1、含 passed +0.15、测试通过再 +0.1」正好越过去 ⇒
  **审查层最明确的否定结论被"省钱分支"吃掉**，任务进 DONE 并合并。
- **A5 观察者的 GATE 批准丢了 `confirm_gate` 的返回值 = 把没放行说成放行** ——
  聊天里硬编码"✅ GATE2 已通过。进入实现阶段…"，而实际 `owner_confirm` 落盘、**phase 还在 GATE2**；
  **且外层 `except Exception: pass`** —— 连抛异常都照样回成功。
- **A4 不是两套驱动，是三套**，第三套在降级时**绕开 INTEGRATING** ⇒ 那条路上的项目
  **跳过了"工作区必须干净 + 集成测试"整道门**，而人看到的是**完整的 GATE3**。
- **A2 审查层是短路链**（越重要的检查越跑不到）· **A6 委员会席位两端都没管**（上限被并发/时限偷偷截、
  下限静默关闭 —— 本机 `planning` 只剩 1 个模型时委员会**静默不开、全仓没有任何告警**）·
  **A7 融合 v2 是"朝定稿人的单调过滤器"**（列分歧越多的模型越容易既当执笔人又当默认胜者）·
  **A9 验收证据是唯一一类非原子写的状态文件**，而"读坏了"被当成"没跑过"。
- 🔵 **我抽验了 5 条**（A1 / A2 / A5 / A6 / A10）—— **都成立，A5 还更糟**；其余照原样保留，**别当已核**。
- ⏭ **当时没做的两条**：**B5「只重做有问题的 task」**（只修了 docstring 那句假话 ——
  真做要按 QA issue ↔ `estimated_files` ↔ 任务标题**模糊匹配**，而**任务不记录自己改过哪些文件**，
  匹配偏了的失败模式是**转圈**）· **B6（读 `merge.py`）** —— 当天晚些时候补上了（见下）。

**④ 同一天白天的另一批（`12:47–13:54`）**：`4316fb5c` **删项目先把它的任务停掉**
（治的正是"废弃项目 ≠ 停任务 ⇒ 重启捞回来接着烧钱"）· `ded50bd0` **异常告警改成讲人话**（事实 + 为什么 + 怎么办，
并修掉说谎的那句"10 分钟"）· `f60ab5b3` **`drain_dep_blocked` 从 3180 条降到"状态变化才报"**
（它在把真事故挤出观测窗口）· `d04022e6` `PUT /api/agents/<层>/<模型>` 对不存在的模型回 **404**
（别再回 500+HTML）· `18ea5c96` 修外派评审 ③ 那道**坏刹车**（"报告落盘了" ≠ "QA 产出了结论"）·
`273409b0` 两条"没依据"的分支不再挑相反的代价 · `3f9c1514` 架构的「历史版本」有个入口了
（**数据 09-17 就在盘上，界面一直没门**）· `3ed1b1fa` 任务页把「上一轮」的任务和当前批次分开 ·
`2097c74a` **外发提示词落盘 + 守卫**（它以前只活在对话里，所以不漂给任何人看）·
`40fe0e30` **§77.3 那条替代判据实测作废**，连同它自己的前提一起更正 ·
`76505317` 修掉待验证清单里那句过期的并发假设（**而且并发其实有两套值**）。

**⑤ ZCode 卸载收尾（09-19）**：**8 项 / 约 1.5G 卸干净**（`/Applications/ZCode.app` · `~/.zcode` **344M** ·
`~/Library` 下六个 · `~/ZCodeProject`），复查**无进程、无 LaunchAgent、shell 配置无残留**。
删前核过两条**不丢东西**（报告在 `~/Desktop/ZCode审阅/` 和仓库 `docs/*-20260914.md` 两处都有）。
⚠️ **但「查不出传没传过」那句仍然是当前状态，不是"已排除"**（关键词扫不到 ≠ 没有 —— §77.3 刚栽过同形状）。
**用户拍板：key 不换**（卸载只是止损，传出去的换不回来）。
🔵 **教训（AI 的错）**：删 `~/.zcode` 前我核了"内容是否别处也有"，**没核"它是不是某个还没结的问题的证据"**
—— 用户正在问这件事，却把材料清了，**且无 TM / 快照可捞**。

**⑥ 傍晚到夜里（`20:30–22:55`）：把「归因」这一族清了一遍** —— 这批的共同点是
**「只标归因、门的强度一分不动」**：

- `8952ff67` **系统的失败别再记成模型/任务的失败**（判据错位审计 A5/A7/A9 + B1/B2/B4）·
  `0de20d9e` **阶段归属做成表 + 把代码钉上去**（09-18 那条"只活在注释里"）·
  `4ec2578d` `supervise()` 知道"**这次是被我方掐断的**"（新入参 `our_side_stop`，只认 `deadline` 和 `truncated_by`）·
  `df68f5e3` 「成功但其实被截断」**补结构化出口**（+ **A8 翻案：不动** —— `if result.raw_output` 看着松、
  实际是**严的一侧**）· `d0743949` **流停滞有自己的 `error_kind`**（判定一个字没动）·
  `bf66035d` **「降级运行」那句 error 只写内存没落盘** ⇒ **对一类任务人审页永远看不见**（疑6 核到底：真缺口在**写侧**）·
  `c366d139` merge 失败的终态**把真因带上**（原来是光秃秃一句 `merge failed`）
- `10ba8d37` **QA issue 带 `task_id`**：谁改的这条文件，**平台填、不让模型写** ·
  `786f8abe` 删 `project.fix_round`（06-27 砍内循环时留下的**孤儿**）·
  `9bc3fd21` 给「返工了几轮」**造尺子**（⚠️ **原文两处前提也是错的**：内层返工的真实上限是
  `config.DEFAULT_MAX_TURNS = 2` ⇒ **每个任务最多只返工 1 轮**；"跑完就没了"只对外层成立）·
  `c2b75850` 审查截断那条告警**带上 `task_id`**（09-17 那个悬案卡的就是它**没有主体**）
- `3d089b1b` **`merge.py` 三处「承诺了却没发生」要出声**（**B6 通读落地**）—— `_park` 落盘失败原来**静默吞**
  （`静默except待修清单` 里 09-13 就分好类、**一直没做**的那条）· `resolve(abort)` 删盘失败也是裸 `pass` ·
  `_mark_merged` **不看 `transition` 返回值**（任务被取消后合并仍会落地 ⇒ git 合了、状态留在 FAILED，
  **交付报告说失败而代码在仓里**；**没 force 硬改，只出声**）
- `310f2fb8` **静默 except 那 88 条按形状复核完**（表在 `静默except待修清单` **§六**）——
  **S1 / S3 / S5 已修成套**；⚠️ **C 档（安全）里剩下 2 条不能照原文的建议改**：
  "兜底 `gate=True`"会让没 `eval.py` 的项目把"分类调用失败"变成 **gate 失败 → retry/rollback**，
  **比放行更坏，而且是假失败** ⇒ 要动先定**第三态**。
- `15368374` **`test_exec_run.py` 的隔离是假的** —— 它一直在往**生产** `alerts.jsonl` 里写 ·
  `8f5d4cf8` `.tongyiignore` 收进版本库（Qoder CN 的索引排除清单）· `7f98acae` ZCode 事件存档

**⑦ 深夜（`21:45–22:55`）：新一轮真机前的准备 + 又修几条**

- `1a554c9f` **`tool_choice` 的降级要记住，别每轮重撞一次 400**（当晚修，**第二天真机验完发现没记住** —— 见 09-20）
- `de6d2fa8` **认不出来的 400 把请求体落一份**到 `.qidian/llm_400_unknown.jsonl`（"别再让下一次变成无头案"）
  —— **这条通道当晚就响了，正是它把那条悬了几天的 400 查清的**
- `c9e9796c` **启动回收时先兑现"停"标记**，别把取消过的任务重派回去 ·
  `2417c13a` `app.py` `-m` 起的进程里**登记包名别名**（治「同一个模块诞生两个副本」）·
  `62897c77` `openai_agent` **流式/非流式对同一轮产出两种形状** + 摘参数判据的**子串地雷**
  （⚠️ **真端点复跑流式验过，整轮真机没跑；也不声称修好了那个 400**）
- **规矩落地**：打了 tag **`round-20260919`**（09-19 晚基线），并把
  **「清单快照跟 tag 同一刻存」写进「怎么跑真机」** —— 清单在 `~` 仓、代码在这仓，**两个仓的提交对不上号**，
  跑完回头查"那一轮带着哪些待办"就得靠这份快照。轮次快照：`round-20260919b`（`4f63fc1e`）·
  `round-20260919c`（`4079334f`）。

**⑧ 真机 `round-20260919c`（`22:47` 起，项目 `1789829245876`）—— 边跑边记**

- ✅ **① `2417c13a` 验到**（`22:53:57` 点批准）：后端回
  `{"ok":true,"driven_by":"scheduler_loop","loop_running":true}`，与**同刻**的 `GET /api/loop/status`
  （`{"concurrent":2,"running":true}`）一致 ⇒ 那句假警告**不该出现**。
  ⚠️ **边界**：我看到的是**后端响应**，前端把它渲染成那句话的那一跳**没亲眼看到** —— 严格说还差一次界面确认。
- ✅ **② `4316fb5c` 验到**（`21:59` 删 round b 的项目）：`alerts.jsonl` 多一条 `project_deleted_left_tasks:13`；
  13 个任务**全 `failed`**（5 个 PENDING **当场转**· 6 个 RUNNING **先收取消标记、30 秒后在 turn 边界转**）；
  **13 个任务 JSON 一个没少**。
- ⏳ **顺带扫过、没被本轮走到**（是"**没触发**"，不是"没问题"）：`cancels/*.json` 里 **一个带 `by` 的都没有**
  （`68664faa` 那条路修完**还没被走到**）· `tasks/*.json.corrupt` **没冒出来** · `alerts.jsonl` 里 `stale_write` **0 条**
  · `review_files_truncated` 6 条**全是 09-17 的**（早于加 `task_id` 那次修复）。
- 🔴 **现场撞见、没解释出来的一条**：6 个取消标记（`21:59:24-25` 写下）**没人取走**，
  而对应任务 `21:59:54` 已是 FAILED、理由「用户手动取消」—— 能写这句话的只有两条路
  （`_task_runner.finalize` 会消费标记 / `task_cancel` 的 else 分支只在非 RUNNING 时走），
  **两条都对不上"标记还躺在盘上"**。🔵 已核**不是**定时炸弹（`task_retry` 开头会删它）。**别当结论用。**
- 🔴 **那一夜两轮真机各被同一条 400 打死一次**（第二轮 **13 个任务里 12 个失败**），
  **整轮基本死在这一条上** —— 现场就是它留下的（见下）。


## 2026-09-20 凌晨（到 `00:52`，8 个提交 —— 那条 400 查清了；`tool_choice` 的"记住"真机验完是**没记住**）

**① `0eb76048` 那条悬了几天的 400 —— 判据卡错了维度（§85）**
症状：`worker 异常: … deepseek-flash: exec 未产出 [HTTP 400: The reasoning_content in the thinking mode must be passed back]`，
随后下游全 `blocked`（`上游依赖 … 已失败(降级运行)`）。

- **现场是 `de6d2fa8` 那条通道留下的**：`.qidian/llm_400_unknown.jsonl` 里一条请求体原文
  （task `1789829637698`，**11 条消息、107KB**）。**凶手是第 [9] 条** ——
  `assistant` + **带 1 个 `tool_calls`** + **`reasoning_content` 键整个不在**
  （对照 [3]/[6]：同样带 `tool_calls`，但键在、有内容 ⇒ 没事）。
  **不是"reasoning 内容不对"，是"这个键整个不在"**（**空串 ≠ 没有；整个丢掉才 400**）。
- **来路三步，缺一不可**：① 两处 `tools = []` 中途撤掉工具（**「测试通过即停」** / **「已达最大工具调用轮次」**），
  而**历史里已经躺着带 `tool_calls` 的 assistant 消息**；② 模型这一轮没工具可用、**还是吐了 XML 形式的工具调用**
  ⇒ 被 `_parse_xml_tool_calls` 捞成合成 tool_call；③ 追加历史时 `_assistant_msg_for_history(msg, tools)`
  **拿"这一轮的 `tools`"判** ⇒ 这一轮 `tools=[]` ⇒ **把 `reasoning_content` 整个 pop 掉**。
- **修法**：判据改成**按消息** —— 带 `tool_calls` 的消息**永远保留** reasoning，模型没给就补 `""`；
  只有**不带 `tool_calls` 的纯文本消息**才照旧剥。**已验到端点层**：拿落下的那份请求体**回放真端点** ——
  **原样 400**，**只给 [9] 补 `reasoning_content: ""` → 200**；单测 + 接线 5 条（`test_reasoning_passthrough.py`）·
  **变异改回旧判据 → 5 条红** · 全量 `pytest tests/` **1797 绿**。
- ⚠️ **在此之前试的六个探针全部 200**（4 个热发 + 2 个冷却 13 分钟后的冷回放），当时的结论是
  "触发条件没找到，可能要等现场"。🔑 **那六个都没有判别力** ——
  **"一次没红的实验，先问它有没有发力"**，六个都不发力，"六次都没红"什么都证明不了。
- ⏳ **还差真机**（判据写死了）：`llm_400_unknown.jsonl` **不再增长** ∧ 终态**不再出现**那句 400
  （这条路**几乎每轮都会走到** —— "测试通过即停"一触发就撤工具 ⇒ **一轮就够**）。
  ⚠️ **只看"没再冒 400"分不出"修好了"和"没走到"** ⇒ 所以 `00:29`（`b78b9cd5`）专门给
  "修复兜住那个形状时"加了一条**出声**；`00:30`（`c845c38b`）去掉裹着的 `try/except` ——
  **仓库有「禁止新增静默 except」闸门，它当场点了名**。
- 🔵 **顺带纠正两条旧判断**：`test_thinking_params.py` 里那条 `test_不带工具时照旧剥掉_对别家零回归`
  **拿带 `tool_calls` 的消息断言"剥掉"** —— 它钉的**正是这个 bug**（已反向重写）；
  老注释里"不带 tools 就不必回传"**是对文档的过度外推** —— **"不必" ≠ "必须没有"**。

**② `8c0c20ee` `tool_choice` 的"记住"真机验完 —— ❌ 没记住**（`00:12–00:26`）

- **症状**：一个任务里 `tool_choice_required_rejected:deepseek-flash` 响了 **7 次**，
  而那条告警的设计是"**只在第一次学到时出声**" ⇒ **7 条 = 7 个实例**；
  侧车 `partial_usage/1789834349419.json` 的 `dispatches` **正好也是 7 条** ✓。
- **根因**：`_no_required_tool_choice` 记在**实例**上，而 `_exec.run()` 的 `for turn` 里
  **每一轮 `dispatch()` 都新建一个执行器** ⇒ **换了个人，谁也没记住**。
  ⇒ **代价：一个 13 分钟的任务白打 7 发 400**（一个来回 + 时间）。
- **修法**：记到**模块级** `_NO_REQUIRED_TOOL_CHOICE`（按 `self._model`，**进程级够用、不落盘** ——
  重启后多打一发换一个模型的探测，可忽略）。测试新增一条钉**跨实例**的用例
  （`test_换了实例也不重撞_跨轮次记忆`）+ autouse 夹具清那个集合（**必须清**，否则用例串味、
  红的是测试脏不是代码坏）；**变异：判据改回实例级 → 只这条红**。
- ⚠️ **同一个形状还剩一处没修**：`self._rejected_think_keys`（`_apply_think_params` 那个）**也是实例级**，
  同样每轮重建 ⇒ 同样白撞。**现在隐性**（生产 `request_template` 没配 thinking 参数，那条路压根不走），
  **哪天配了就会发作**。同一句话能修（照模块级那个挪）。

**③ 其它（都还没上真机）**：`00:04` 轮次快照 `round-20260920` 开跑时的待办全文（`0244d2a9`）·
`705554e6` §85 落文档 + 待验证清单记「到哪一步了、真机还差什么」·
`00:36` 轮次快照 `round-20260920b` 基线（`6cf72cfe`）· **`00:52`（`c78caa54`）GATE2 上多了架构图**
（模块依赖图 + 任务依赖图）。

**④ 真机那两轮的账（★ 别当验过）**

- **`round-20260920`**（项目 `1789833908112`，**已废**）：T1 **13.5 分钟 / 332k token / 零文件产出**；
  T3 `exec 未产出 [达到最大轮次]`；**3 次 XML 捞回、400 一次没跟着来**（**未定性，别当验过**）。
  两处失败**都不是 400**。⚠️ **这一轮跑的是旧代码**（`0eb76048` 之后那两处修复，**进程里一行都没有**）。
- ⚠️ **后端已停**（`pgrep -fl singularity.web.app` 应为空）。停的时候 T2 / T6 **还在飞**，
  而 `POST /api/loop/stop` **只拦新派活、拦不住在飞的任务** ⇒ 只能 `kill`（**在飞的任务拦不住**）；
  重启会把 RUNNING **回收成 PENDING** —— **别以为它们还在跑**。
- 🔴 **`round-20260920b` 现场（`01:01–01:02`，还没提交）：产物真产出来了，判词说「无文件改动」⇒ 整份丢** ——
  T3 `refs/qidian/pending/1789836336318` 上是 `d90998c`、**相对父提交 +1008 行**
  （`filters.py` 515 · `types.py` 493），**提交 `01:01:29`、判失败 `01:02:24`（晚 55 秒）**；
  T4 同形（`83c228d` +251 行）。判据入口是 `_task_runner` 那句
  `changed = disp_result.executor_result.changed_files` —— **只认「最后一次派发」交回的文件列表**。
  ⏳ **机制是假说不是结论**（侧车最后一条 `dispatch` 的 `at` = **与死亡同一秒**），**这轮先记账、不动手**。
  🔵 同族旧账：`_exec.py` 注释那句「上一轮真干出来的账整份丢掉（09-16 真机：判『无文件改动』，
  产物在 pending ref 上）」**就是这个形状 —— 修过一次，没修干净**。

**⑤ 你拍板 / 核对出来的两件**：**前端样式调优整体推迟**（架构图的盒宽、标题截断长度这些**先不动**，
后期统一优化前端时一起做）· 核对"记录覆盖"时发现 **`防御模式.md` 已到 §85，而两处的数字还停在旧值** ——
它**自己的抬头**写「把这 **66 条**反过来读」（66 是 09-13 的数；现有 **87 个 `### NN.`**），
`docs/架构性质清单.md` **09-13 之后再没提交过**、正文写死「**20 条**」⇒
**§67–§85 这十九个坑没回流**，而那份正是"**换架构时要逐条对着问**"的那份。
症状正是归档规矩里那句「**格式对、内容旧，比缺失更坏**」。

**⑥ 另一件起头的**：`🔵 Qoder CN 外派审查`（免费 Qwen3.8-Flash，**9/30 23:59 截止**）——
只审不改，报告落 `docs/` 进 git，**和 ZCode 那批（`docs/*-20260914.md`）配对比对**（两家都报的≈真问题）。
已建 `.tongyiignore` 排除 `.git/ docs/ .qidian/`，**⚠️ 没实测它认不认这个文件**。
**第一步先"标定"**：拿 ZCode 已找到的缺陷当答案，看它能不能独立复现 —— 量不出能力，就不知道该不该继续用它。

📌 **下一步**（判据都写死了，别靠"没冒 400"当结论）：**重启后端 + 再开一轮**，验三条 ——
① `0eb76048`（400 修复）：`reasoning_kept_for_tool_call_without_tools` 告警**出现过**
  ∧ `llm_400_unknown.jsonl` **没跟着长**；
② `8c0c20ee`（`tool_choice` 记住改按模型）：**同一个任务里 `tool_choice_required_rejected` 不该响第二次**
  （旧行为：一个任务响 7 次）；
③ 顺带看 `alerts.jsonl` 里 `by:timeout` / `stale_write` / `tasks/*.json.corrupt`。
🔵 **活的待办清单在 `~/OPEN.md`**，别在这儿抄。跑真机前先按「怎么跑真机」那节核对环境
（**开局先打 tag `round-<日期>` + 同一刻存清单快照 + 重启后端**）。


---

# qidian-bug-backlog 的历史快照（09-13 ~ 09-15）

> 来历：那份记忆文件涨到 **68.5 KB**，而它**每次被召回就整份进来**。里面绝大部分是
> 逐日的工作快照 —— 按记忆的规矩该留在仓库文档里。**内容一字未改，整段搬来。**
> 原文备份：`~/memory-备份-20260916-0055.tar.gz`。

⚠️ 其中一节标题叫「**现在怎么样（2026-09-13 凌晨）**」—— 写的时候是「现在」，
搬到这里时已经**过期两天**了。**标题会骗人，看内容的时间戳。**

## 2026-09-15 **深夜**（重启后端 + 又跑两条：三样只验到一半；查清 T2；顺手修掉 6 条）

📌 **下一步**：**再跑一条到 GATE3** —— 解析点那行**临时探针**（`qa_raw_probe`）
**到今晚为止一次没响过**。它响一次，那个 217 字符的谜就能定位；
   ⚠️ 当夜那 6 条修复（含新加的「产物非空」硬判据）**都还没上真机** —— 这一跑也得验它们。
⚠️ **响过一次就该把那行删掉**，别让它留在代码里。

**三样的真实结果（⚠️ 别读成"都验过了"）**：
- **① 流式总时长 —— 没验到**：`stream_over_budget` **全库 0 次**，整晚 dispatch 都在 25 秒上下
  ⇒ **判据压根没被触发**。**判据没触发 ≠ 修好了。**
- **② QA 契约 —— 间歇性**：同一个后端进程（22:17 起）里，老项目 22:29 判"未产出"、
  新项目 23:00 判出真结论（`no_go` + 8 passed / 8 issues）。**我一度说"真机判死"是说重了。**
  老项目那次"喂进去的和落在盘上的不是同一份"（QA 那份差 **217 字符**，而同条 SSE 里
  安全报告的数字**精确相符**）**仍未解开**（两条候选都没坐实）。
- **③ 只读豁免 —— 半验**：`[只读]` 那条 `done` 了，但它上次是**超时**失败、
  不是"零文件改动" ⇒ **豁免逻辑有没有真的放行它，没看见**。

**两条跑**：老项目 `1789477040616` 走到 `done`（两个失败任务重试后全过：T2 4 分钟 /
T3 `[只读]` 76 秒）—— **目前最接近"完全交付"的一次**；新项目 `1789481895784` 三个任务
全 `done`，但 T2 的产物 `test_fizzbuzz.py` **是 0 字节** —— 而
**QA 判 `no_go` + 机械检查 0/8 各自独立抓到了**。
⚠️ 它暴露北极星那条判据不够：**"任务无 failed" ≠ "有产物"**。

**当夜修掉 6 条**（各有独立提交、各做了变异自检；详细账在 `docs/真机验证-20260914.md` §0.15 / §0.16）：
- 重试成功的任务 **trace 停在失败那一版**（幂等守卫对着 task id 判，而重试复用 id
  ⇒ 后半截记忆索引也整块跳过 ⇒ 记忆里那条任务永远以为自己是失败的）→ `2e8537f`
- **短 id 全仓取前 8 位**（任务号是毫秒时间戳，前 8 位**每 100 秒才进一位** ⇒ 同批任务
  全撞车，告警里读成"自己改了自己"）→ `0e62d96`
- **GATE3 打回把 `critical` 当成架构级**（会 `architecture = None` 清空重规划）→ `a355940`
- **交付物被自己清空、三层防线全漏**（这就是「T2 为什么写出 0 字节」的答案）→ `1849b1c`：
  真机是 `+311 行 → -311 行`（同一个任务自己删光的）；三层各漏一处 ——
  「写文件不挡空内容」·「收尾指令不说要落盘」·「验收只看改没改、不看有没有东西」。
  ⇒ **「产物非空」已作为硬判据进验收层**；北极星那条因此补了半句：
  **「任务无 failed」不等于「有产物」**。
  ⚠️ 到底哪一次调用清的**没坐实**（`partial_usage` 只留最后一次 dispatch 的事件）。
  ⚠️ 加这个检查时**被棘轮当场抓到**（静默 except）—— 那条尺子是管用的。

**其他**：全量 `pytest tests/` **1497 绿** · 棘轮仍 **401**（加新检查时被它抓过一次，已改为出声）·
前端 `tsc 0` / `vitest 94` 且 **`static/dist/` 已重建**。


## 2026-09-15 **晚**（真机连跑三跑 → 修了 6 条）

📌 **下一步**：**重启后端 + 再跑一轮**，验三样 —— ①流式总时长 ②QA 契约 ③只读豁免。
⚠️ **这两条后端修复还没生效**（进程跑的是旧代码）：`reasoning_content` · 流式总时长。

**这一晚干了什么**（用户建项目真跑，我从真机抓洞 —— 详细账在 `docs/真机验证-20260914.md` §0.14）：
1. **`reasoning_content` 被一刀切剥掉**（`f8b326b`）—— DeepSeek 官方规则：**带 `tools` 时
   必须原样回传**（不回传 400），不带 tools 时传了也忽略。原代码是旧经验的一刀切，
   而**流式那条特意把它拼回来了**、下一行又扔 —— 两条路自相矛盾。
   ✅ **真机验过**：修完 #1 干净跑完 354 秒。
2. **「只跑不改」的任务被 QA 判死**（`65240b3`）—— planner 会拆出这种任务，而
   `supervisor._check_completeness` 的硬规则分不开"真偷懒"和"本就不该改文件"。
   改成认上游的 **`[只读]` 声明**（架构 schema 约定写进标题）；**刻意不做自然语言推断**
   （开集枚举，`_observer_answer` 栽过）。✅ 真机两次都看到标记了。
3. **QA 契约没建立**（`4cc5c1b`）—— 提示词只说"输出 JSON"、**不给字段**，下游却读
   `verdict`/`issues`/`passed`。⚠️ **我第一版建议"剥 markdown 代码块"是错的**（剥完照样没
   `verdict`）—— 主修复在提示词给 schema。
4. **`fn_fail` 不记失败原因**（`6bb6f7c`）—— ✅ **一上线就抓到了第 1 条**。
5. **点侧边栏「对话」把待审批的项目弄丢**（`f92e340`）—— 审批面板凭空消失（用户原话"审核消失了"）。
6. **流式调用没有总时长上限**（`0e8bc69`）—— **"900s 自收尾没生效"的新根因**：
   `httpx` 的 `read=` 封的是**两次读之间**、**不是总时长** ⇒ 服务端持续吐 token 就跑任意久；
   而"看表"那句只在**轮间**。量尺给的数：**一次 dispatch 1615.7 秒、`tokens=0`**。
   ⚠️ 与 09-13 那条「每 dispatch 归零」是**两个病**，修一条不等于修另一条。
7. 前端两条：出错给出路（chunk 过期时"重试"永远好不了）· 对话页随时回看架构/调研。

🎯 **"至今没有一次完全交付"（用户 2026-09-14 晚原话：「到现在为止奇点还没完全交付一个项目呢，」）**
⚠️ **归属更正**：「北极星」这名字和下面五条判据**是 Claude 拟的，不是用户提的**（原标注写错了）。
**判据（未获用户批准，随时可改）**：
任务无 failed · QA 有结论 · 交付清单三项非空 · 产物能看懂 · 账本记一行。
（`process_ledger` 里跑完的 4 个项目只有 1 个任务全绿；9 次交付的 `reports` **全是 0**。）

⚠️ **两条没坐实的**（别当结论用）：重启后重派撞上"worktree 已没"（哪条路径用旧 `cwd` **没读完**）·
两个日志文件 inode 被换掉、**日志静默丢失**（**谁换的没查出来**）。

**现在的数**：全量 `pytest tests/` **1475 绿** · 静默 except 棘轮 **400** · 前端 16 文件/92 用例。


## 2026-09-15 白天（三条拍板项收口 + 审计清尾）

📌 那半天的**下一步**已过时（见上）。

1. **三条拍板项全部修完**（`4b14951`，用户 09-15 拍的方向）：
   · **`_is_redundant_failure` 的 pass 守卫** —— 改成 `action == "pass" or verdict in ("通过","pass")`。
     真机实测 24 条：**旧守卫判 16 条"重复"（9 条是成功的）· 新守卫 7 条（全是真失败）**；
     真类型跑 `save_trace` 末跳复现了症状（800 字→214 字、20→5 个文件、打 `_dedup`），
     新守卫原文全留。**拿真数据验穿的，不是"等真机"**。
   · **`_reorder_agents_by_rank` 死代码** —— 删函数 + 5 条用例。
   · **启动自检 INFO 静默** —— 新增 `_setup_startup_logger()`，只给 `startup` logger 挂 handler，
     **不碰 `basicConfig`**；落盘坏了**退 stderr 不吞**（`except: pass` 会被棘轮当场逮到）。
2. **审计 §8.5 那批"未核"清完**（`11a58f4`）—— 逐条真变异：**坐实 2 条假绿**
   （`test_empty_changed_files` 传 `[]` 而非默认 `None`；`test_missing_body...` 的输入
   **到不了被测那行**，因为守卫在前一个 early-out 就返回了），其余 9 条真绿。
3. **文档"内容过期"过了一遍**（脚本查不出的那类）：`审测试判据-01` 抬头还写着"后半还没跑"
   （**会叫人重头扫**）· `要发的指令.md` 里 ⑬ 还躺在"待发" · 预检工具归档缺"来历"段。
   `git add -A` 那条教训从 OPEN.md **落进 `防御模式.md` §72**。

⚠️ **新踩的坑**（记进 `test-wiring-not-just-functions` 第七次）：**变异"掐不断"时先怀疑变异
没生效** —— 3.13+ 的 `Logger.addHandler` 对**同一实例**会自己去重，拿同一个 handler 复用去测
幂等，删掉守卫照样绿。

**现在的数**：全量 `pytest tests/` **1458 绿**（0 skipped）· 静默 except 棘轮 **401**。


## 2026-09-14 **深夜续**（当天总共 **125 个提交**）

📌 **下一步**：`docs/真机验证-20260914.md` 的 **§0.5 / §0.6 / §0.8–§0.11** —— 那步**是人做的**。
**代码这边能做的都做完了**，剩下三类：① **三条等用户拍板**（`_is_redundant_failure` 的 pass 守卫 ·
`_reorder_agents_by_rank` 死代码 · 启动自检 INFO 静默）；② **要真机的**；③ 历史存档。
**细节全在 `~/OPEN.md` 第一条。**

**这一段（上面那段之后）做了什么**：

1. **⑬「审测试判据」60 份全部过完**（A1/A2/A4/A6/A7 → `docs/审测试判据-02-20260914.md`）。
   方法：**代理逐份读码出判词 + 我逐条变异复核**。抓到的真东西：
   · **循环导入**让 `test_thinking_params.py` **单独跑必红** —— 根在 `dispatcher` 末尾那三句星号导出，
     改成 **PEP 562 惰性转发**解决，**顺带把登记很久的「`_dispatch_exec` 单独导入会炸」一起修好**；
   · `test_router` 三条**把整条分类逻辑删掉都绿**（只比默认值）· 一批**恒真断言**
     （`len(x) >= 0` / `code in (200,403)` / `f(x) == f(x)` / `from_dict(p.to_dict()).to_dict() == p.to_dict()`）；
   · **`_is_redundant_failure` 的两道 pass 守卫认不出真机的形状**（真机是 `verdict="通过"` /
     `action="pass"`）⇒ **成功任务的 trace 被系统性截断** ——
     ⚠️ **当时没修，09-15 已按真机形状修完**（见上面那节）。
2. **结构性那条（边沿触发→水位触发）全部收口**；另做了 B1（三个写入点 + `rollback_create`）·
   B2（重活记账拆"尝试/成功"）· #5（`merge_ref` 抛点实测 + 回滚）· #7（交付没 code_ref 要出声）。
3. **110 份测试逐个单独跑**：**没有第二份会红** —— 那条循环导入是**独苗**，不是一类普遍问题。

⚠️ **今天我自己踩的两个坑**（都记进 `docs/防御模式.md` **§71**）：
· **变异实验的脏 `.pyc`**：`git status` 干净 **≠** 跑的是干净的代码 ——
  **变异一律带 `PYTHONDONTWRITEBYTECODE=1`**（审计外派件的方法节里早就写着，我读了没照做）；
· **`cmd | wc -l` 会把"命令失败"读成 `0`** —— 我据此说过两次"都推完了"，其实有未推的。
  正确姿势：看 `git status -sb` 的 ahead **和退出码**（今晚归档时正是这么抓出来的）。

**现在的数**：全量 `pytest tests/` **1458 绿** · 静默 except 棘轮 **401**。


## 2026-09-14 一整天（晚间版）

> ⚠️ 这一段是**当天早些时候**写的快照，**下一步那句已过期**（后来又做了上面那一整段）。

**这一整天（`944cad4..dcbe4a4`，29 个提交，每条都做了变异自检）分三块**：

1. **把 ⑨/⑩ 已确认的洞逐条修完**：权限工具门收进 `BaseExecutor`（原来**只有 openai
   执行器真的拦**，换个 `type` 就绕过；`claude-cli`/`zhipu` 拦不了 ⇒ 改成**出声**而不是
   假装拦住了）· 敏感名单三张表收成一份 · `task_timeline` 不再给没跑完的任务画终点 ·
   gate 兜底表补 `config.py` · `route_type` 的 `fusion` 收口 · origin 两个门收成一份 ·
   worker 异常抢救段补兜底 · unittest 0 测试 · `level_filter`/roles/`project_lineage` 三条静默错。
2. **按外派⑬ 的清单修"假绿"**（**今天最值的一条来路**）：它报的**三条真洞**是
   `load_for_rewrite` 那一族**只修了 3 处**、mcp/agents/自定义模型**漏了**
   （判据是模块级/路径标记 ⇒ **第一次触碰时闸门恒假、整份重建照写**）；
   而**那三条用例本身是假绿** —— 每条都先手动读一次再调写（**替被测代码把洞填上了**）。
   另外十余条假绿：恒真断言 / 只测函数不测接线（XML 工具调用的解析器钉得干净、
   `run()` 里那句接线删掉 14 条全绿）/ **原子性本身全仓无测**。
3. **自己做的**：`.corrupt` 备份加上限（留最近 5 份）· F 的 19 条文档过期 ·
   `run_command` 两种语义收敛（同一个工具名换个执行器两种行为）· Python 地板提 3.12 ·
   **P1 观测点**（`solo_arm`/`committee_arm` 落 `scheduler.log`，实验只剩"跑"）·
   Observer WS 逐连接鉴权（顺带纠正两条：bridge 本来就有、前端根本不用 WS）·
   **结构性那条的第一个落点**（孤儿探测从"只在队列空了那一刻"改成**每轮对账**）。

**验收**：`pytest tests/ → 1445 绿（0 skipped）` · `tsc 0` · `vitest 76` · 样板 `24`。
**数**：静默 except 棘轮 **402**。⚠️ **一处没上真机**。

三条值得记的：
- 🔴 **我自己犯的错**：用 `git add -A` 提交，把**外部 ⑪ 窗口正在写的** `scripts/preflight.py`
  扫进了 5 个不相关提交（来历丢了）。⇒ **和外部窗口共用一棵树时，提交必须写明确路径**；
  `git add` 前先 `git status`（用户 CLAUDE.md 里早写着，我没做）。
- 🔁 **棘轮对"出声写在本地 helper 里"是瞎的，今天撞两次** ⇒ 处理是把出声挪回调用点。
- 🟡 **三条守卫"从未运行过"**：`test_no_undefined_names.py` 的 F821 闸门要 ruff，
  而 venv 里没装 ⇒ 一直静默 SKIP（实测：没装 → `1438 passed, 3 skipped`；
  装上 → `1441 passed`）。**已装 + 写进 CLAUDE.md 验证节**。
- ⚠️ **操作坑**：`git commit -m "…"` 里带**反引号**会被 shell 当命令替换（我吃过一次）。


## 2026-09-14 收工（**这一夜那一批**；⚠️ 已经被下面"早上→中午"那段接上了，别只读这段）

> ⚠️ **先说清楚**：这一段写的是**收工那一刻**的状态（34 个提交、6 份外派）。
> **之后又发生了两段**（见下面的"早上→中午"）。**只看这段会拿到过期状态。**
>
> ⚠️ 下面这段里写着"一处没上过真机" —— **现在是更多**（又多了 7 个提交）。
> 清单已补 `docs/真机验证-20260914.md` 的 §0.5。

**那一夜/早上：34 个提交、68 个文件。全部只有离线验证（后端 1371 绿 + 前端 71 绿 +
每条改动都做了变异测试），⚠️ 真机一处没验。**

- 🔴 **醒来第一件**：`docs/真机验证-20260914.md` —— 按风险排好了步骤。
  第一优先看「**S1 那族闸门有没有误伤正常路径**」（它们全是"坏了拒绝服务"，
  误伤就是把能用的堵死；而我的对照测试也是我写的）。
- **做了三大块**：① 外派 ④（扫前端）报的 6 条全处理完 ② **S1「损坏的 JSON 不许当空」
  铺完 9 处**（`_io.load_json_or_quarantine` + `is_quarantine`，读降级写拒写）
  ③ `_rollback_git` 的 no-ref 守卫（**没有 ref 就不许销毁工作区**）。
- **一条最贵的教训**（同一个病换个字符串，**一晚撞了五次**）：
  `cancelled_by_user`/`cancelled_during_pause` · `escalation_exhausted`/`no_escalation_path` ·
  `mcp` 的 add/delete · 类方法/模块级 `_run_command` · web/agent 两张黑名单。
  ⇒ **改判据/改词表时，必须同时搜消费者** —— 只改生产者 = 功能静默不触发。
- **一条关于我自己的**：`TaskCard.tsx` 我上一轮刚改过（`verdict` 那条），
  **却没看隔壁七行的 `status` 渲染**（同文件、同类病、挨着放）。
  ⇒ 见 [[fix-at-root-not-minimal-diff]] 的"改完先问同族还有谁"。
- **棘轮的尺子本身那晚修了两次、撤了一次**（别名虚增 12 处 / 试过跟本地 helper 造出假阴性后撤回）
  ⇒ **当晚早些时候报的 426/424/423 都不可比**；现在的数是 **401**（09-14 深夜从 402 降到 401：`_recover_parked` 那处不再静默）。
- **待办/指针看 `~/OPEN.md`**（一条接手指针 + 29 条）。
  **外派指令**在 `docs/要发的指令.md`（Desktop 上是它的符号链接；**只装待发的**，
  发过的在 `docs/要发的指令-归档.md`）。

**09-14 早上→中午（清上下文后的这一个会话，一路做到上下文满）**

📌 **下一步在哪**：`~/OPEN.md` 的接手指针 —— **看它的 ⑥「下一段要做什么」**（用户睡前交代：
**所有待办做完 → 再验证**，全权）。`docs/真机验证-20260914.md` 是**人做的**那半。
**外派账**：⑨⑩ 已核完归档；**⑪ 没做完**（只交判据，工具和成绩单都缺，要重派）；**⑫⑬ 待发**。
⚠️ **现在没有在跑的窗口。**

**这一段最值的三件**（都验过）：
· **⑨ 复核驳回**：没有一例"真洞被杀"的错杀；**翻案一条**（`unittest` 0 测试的洞在 **<3.12 上活着**：
  3.9→rc=0 / 3.14→rc=5，我实测）。它还**纠正了我给错的坐标**（我引 `#9` 时凭印象写，漂了号）。
· **⑩ 按形状扫**：8 对确认、**0 假警报**；**和 ⑨ 独立撞上同一处**（profile 工具门只在 openai
  执行器上执行）⇒ 可当实锤。另有 SANDBOXED 第三张表 / task_timeline 重写状态机等。
· **`docs/防御模式.md` 71→74**：§68 写侧护栏查在读之前 / §69 测试把缺陷前提固化成前置步骤 /
  §70 失败的原因在端点层被丢掉。

- 外派 ⑥（前端重写方案）**已核完归档** → `docs/前端重写方案-01-20260914.md`。
  我抓到它两处文档撒谎（`2af723e`）；修了一条真·偶发（`5a0fe9c`，见
  [[test-wiring-not-just-functions]] 的"第六次：绿得不稳"）。
- 🔴🔴 **外派 ⑦（逆向审-02）是这一夜最值的一份 —— 它抓到的是我自己造的洞**：
  四个"读改写"接入点的**闸门查在读之前** ⇒ 进程刚起、坏文件还没人读时形同虚设，
  **整份重建照写**（实测：模型纪律表的历史只剩新记的一条、`settings.json` 的
  `user_theme` 全没）。已修（`8e46dee`，`_io.load_for_rewrite`）。
  ⚠️ 它还指出**测试全体漏掉这一格**：每条写侧测试都**先手动读一次**才调写 ——
  那是把**缺陷前提固化成了前置步骤**。两条都进了 `docs/防御模式.md`（§68/§69/§70）。
- 外派 ⑧（反审样板）**证伪了我两条**（#2 记错原版行为、#15 的对照想当然），
  并抓到铺开方案**自己内部 4 处矛盾** ⇒ 已写进方案的「⑧ 反审后的勘误」一节。
  它最值的：`useResource` 缺序号**不只是洞照旧**，慢的**失败**后到会往新数据头上贴
  「刷新失败」，**那条横幅的文案本身就在撒谎** ⇒ 铺开会**新增一种谎**（已修 `5b117db`）。
- **我这轮修了 7 个提交**（全部变异自检过、全部已推送）：S1 第一次触碰 · 人审门否定族
  第三轮 · 技能两个端点 · SkillsTab 两处 · 原子写 9 处 + 静态闸门 · useResource 序号。
  ⚠️ **一处没上过真机的提交又多了 7 个** ⇒ 真机验证清单已补 §0.5。
- 🔴 **我自己的一次事故**（教训另存 [[repro-scripts-need-real-isolation]]）：复现脚本
  用环境变量做隔离，**那个变量没人看** ⇒ 真写进了生产 `.qidian/model_discipline.json`。
  已删掉伪造记录、重做隔离复现、如实上报。


> **历史与教训都在仓库 `docs/` 里，这里只留"现在怎么样 + 待办 + 指针"。**
> ⚠️ 别再往这里堆日期流水账 —— 放 `docs/`。
> （2026-09-12 清过一次：**629 行 → 这个大小**。删掉的段落在 `docs/防御模式.md` /
> `审计报告-20260911.md` / `工作流详解-外派评审.md` / `演化史.md` 里都有，不是丢了。）


## 🔴 09-13 晚（清上下文前一刻的状态）

**当晚最后一轮（`1789303369052`）全失败了** —— 根子只有一个：**900s 超时**。
链条：实现任务 900s 被砍 → 写测试的**降级起跑**（不级联失败是有意设计）→
没有实现可测、空手回来 → **QA 逮住"无文件改动 + 2 个偷懒信号"** → 项目**停着等人**。
⚠️ **但这轮失败得"诚实"**：上一轮写测试的**自己把实现写了**、机械检查 9/9 全过；
这轮**没有**那种"绿得可疑"。⇒ 跟当晚两处修复方向一致，但 **n=1，不归因**。

**⚠️ 最要紧的一条：900s 超时当晚撞了 4 次。**
~~原来推断：被砍那个任务 5 轮 / 900 秒 ⇒ 每轮约 180 秒，而余量只有 90 秒 ⇒
"安全窗口比一个检查间隔还窄"~~ —— **09-13 晚推翻，主因不是这个**：
**执行器的 810s 预算每 dispatch 归零，而 900s 是任务级的**（详见下面那一节 + §67）。
那条旧推断**降级为"可能同时成立"，不再当主因**；量尺和修法都已落（未真机验）。

**当晚修的 4 件，全部真机没验**（都在 `docs/待验证清单-20260912.md` §三·八）：
QA 门禁 fail-closed · 架构零依赖出声 · 抽象待补新旧兼顾 · 降级运行/被砍出声。

**残留已清**（15 个任务 + 43 个孤儿快照 + 1 个过期取消标记），**两份备份都验过能读**：
`~/项目/项目仓库/残留任务-备份-20260913.tar.gz`（71 条：tasks/traces/projects）
＋ `…-备份-20260913-补.tar.gz`（46 条：snapshots/cancels）。
⚠️ **两个测试项目留着没删**（`1789300044340` / `1789303369052`）—— 那是当晚两轮的记录。
⚠️ **删任务不动记忆**（events 里还在）—— 已知行为。

**外派包 v2 的答卷已归档 + 已核完 + 追问过一轮** →
`docs/架构级静默失败-外部答卷-20260913.md`（**追加同一份，不新开文件**）。
⚠️ **核出来的四类**：① 它引的坐标**全过期**（材料发出后代码又改了一天 ⇒ **以后外派材料写符号名不写行号**）；
② ✅ 三条真缺口；③ ❌ 它批评了一个我们**已经解决**的问题（性质 #2）；
④ ⚠️ **它继承了我们材料里一处假前提**（`_strand_guard` "5 处统一"实际只有 4 个调用点，
而出事那处恰恰没走它 —— **已还账**）。
🔑 **追问那轮的结论**：它**对的比我们多** —— 我们拿来支援看门狗的 §57/§65 两个案例
**恰恰在对账循环的独占射程、看门狗的盲区里**（§65 文档原文就写着"空转分支"）。
双方排序已消解：**看门狗先做**。它保留一条：若永远只能选一件仍选对账循环。


## 现在怎么样（2026-09-13 凌晨 · 900s 那条真机验完之后）

**那一夜 + 09-12 晚那两批改动，已经在真机上验过一轮了** → `docs/待验证清单-20260912.md`
（标记口径：`✅` 验过 / `❌` 没过 / `◐` 半验 / `—` 未触发）。
**逐行重数的数（09-13 22:5x · 轮 5 回填后）**：`✅` 23 / `❌` 0 / `◐` 8 / `—` 10 / `⬜` 9（合计 **51**）。
⚠️ **轮 5 这次合计一个字没变（还是 48）、组成变了**（`✅`22→23 / `◐`7→8 / `⬜`9→7）——
**"总数不变"那一类当天第一次当场抓到**，靠的是连组成一起数。
（21:3x 那次是 45 —— 之后一条 `✅`→`◐`、新增三条：`一把尺` 修复 / `暂停算不算预算` / `循环心跳`。
它当天漂过**十一次**；其中**四次是"总数不变"那种**：`—`→`◐` → `◐`→`✅`（合计一直 40）、
告警页 `⬜`→`✅`、架构守卫 `⬜`→`◐`（后三次合计一直 42）。
⚠️ **只对总数完全看不出来，必须逐项对组成** —— 这条当天被咬**四次**。）
**数法（以 `待验证清单` 头部为准）**：原始 **59 行**（`✅`29 / `—`10 / `◐`8 / `⬜`10 / `➕`2），
**跳过第三节那 6 行**（全 `✅`）＋ **`➕` 那 2 行** ⇒ **59 − 6 − 2 = 51**。
⚠️ **减的是"行数"不是"组成"** —— `⬜`/`—` 那些行都要算进去；
⚠️ **`⬜` 在文件里写作 `[ ]`**（grep `⬜` 一条也数不到）；**三·七/三·八 夹着三张续表**
（按"一张表"数会漏 5 行）。历次旧值 40 / 37 / 45 都是当时的快照。
⚠️ `~/OPEN.md` 里那份分解是跟它**同一时刻逐行核的**，**再改那个文件要回来一起重数**。

**三轮的口径**（别混）：
- **轮 1** = 探路3 基线轮（`1789160560999`）：3 个任务**全没交付**。
- **轮 2**（`1789223754637`）：**1 成 1 败**（8002 交付、机械检查 7/7 全过；8003 超时）。
- **轮 3**（`1789226826934`）：**2 成 3 败**。**只为验 §60 根因修复而跑**；项目退回 GATE2 后**停在那儿不批**。

### §59–§62 的现状（**主体都真机验过了**，别再按"结构级"读）

⚠️ **一处例外**：§59 的**边界**（`57d2a17` 那种"两种没账分得开"）**只兑现了、真机没验**
—— 要等一次真 900s 超时才看得到（09-13 19:40 才补登记进 `待验证清单` §三·七）。

| 条 | 一句话 | 现状 |
|---|---|---|
| **§60** | `constraints_checklist` 为空 ⇒ 机械检查这条链是断的 | **🎯 根因定位 + 修复 + 真机闭环**。根因**不是被覆盖**，是"**两条建任务的路，跑的那条从来不写清单**"（批准 GATE2 不启 `run_phase` ⇒ `_run_execution` 压根不跑）。修法一行（`9d38212`）。轮 3 验：清单 **0 → 9**、`constraints_checklist_fallback` 告警 **零新增**。⚠️ **告警语义已改**：出现 ≠ "覆盖源还在"，而是"又有条建任务的路没写" |
| **§59** | 超时 ≠ 停了 | **四半都验完了**。收尾 ✅、tool_events ✅、**账 ✅ 但有边界**（"干了活才超时"有账／"没干活"是 `null`；做成本口径按"**可能整条为 0**"算）。✅ **边界已兑现（`57d2a17`，09-13）**：`token_count=None` 的**两种成因**（压根没进过 dispatch vs 进了但没落账）**从此分得开** —— dispatch 开头落 `started_at`，收尾按 sidecar 在不在分两种说法。⚠️ token 的**下界语义没变** |
| **§61** | 架构 schema 两份、字段漂了 ⇒ `covers` 必丢 | **验过**：`covers` **9/9**（轮 1 是 0/8）。⚠️ `check` 7/9 结构化，另 2 条模型自觉写了散文 |
| **§62** | 提取员每轮真自审 | **验过（间接证据）**：委员会名单里没有 `glm-5.2`；`extractor_swapped` / `empty_content` / `fusion_v2_empty_extract` 两轮均 **0 次**。⚠️ 没直接抓到"提取步跑的就是 glm-5.2" |

### 🔴 900s 超时 —— 改是改了，但 **09-13 发现改错了层（生产值上基本不触发）**

**09-13 复查翻案**：**没有"两种形状"**（那次的分法是错的，别再按它解释）。

- 8003 那个"第一个调用就卡死"的**唯一依据是 `turns=0`**，而那是**上报假象**
  （`_persist_partial_usage` 只在 dispatch 返回后落盘 ⇒ 被砍的恒为 0）
  ⇒ **拿上报口径的缺失当事实，会得出相反的根因**。
- 引擎本身有界（流停滞 90s / 单次尝试 240s / 单次调用重试预算 600s）⇒ 一次卡死凑不满 900s。

⇒ **只有一种：执行器一直干到被杀。** 根因是**执行器的 turn 循环一圈表都不看**，
唯一上限是 orchestrator 那句硬编码的 `deadline = 900`。

✅ **第一版改法（`debde43`/`0bea891`）**：`config.TASK_DEADLINE_S` 单一来源；执行器提前自己收尾
（轮间看表 **＋** 单次调用封顶，**两处缺一不可**）；`_dispatch` 里**收尾结果是终态**，
不许被"空输出"那条判据吞掉（吞过一次，白烧 423 秒）。
✅ **真机验过**（`QIDIAN_EXEC_BUDGET=15`，任务 `1789241960646`）：自己收尾、
**`executor.run` 只出现一次（不重试）**、`token_count=13098`、`changed_files` 两个真文件。
→ `docs/防御模式.md` **§63**。

🔴 **但 09-13 晚轮 4 照样全灭在 900s ⇒ 09-13 读代码翻案（未真机验）**：
**两级预算的时钟原点不在同一层。**
- orchestrator 的 900s 从 **future submit** 算，**任务级**、跨 dispatch 一直跑；
- 执行器的 810s 从**它自己 `run()` 里的 `start`** 算，而 `_run_executor` **每次 dispatch
  新建 executor** ⇒ **每 dispatch 归零一次**；
- `_exec.run` 外层 `while True`（fallback_chain）× 内层 `for turn in range(1, 2)`
  **自己一圈表都不看**（全函数 `time.time()` 只 4 处，全是打时间戳）。
⇒ 任务只要跑过 **≥2 次 dispatch、每次都没到 810s**（模型每轮几十秒时**常态**），
**自收尾永远不触发**，人却早被无声收割。**收尾逻辑是对的，装在走不到的层上。**
⚠️ **§63 用 15 秒验的，验不出这个**：15s 档下每次 dispatch 都超预算、收尾必然触发，
"每 dispatch 归零"这个形状被**完全掩盖**。**用小值验，验不出"层错了"。**
⚠️ "预算是按每次 dispatch 算的"**早写在 §63 补记里**了 —— 但当时是当**测试口径的
注意事项**记的（"别拿任务墙钟推收尾"）。**记了事实 ≠ 记了它的后果。**
✅ **量尺已落（未真机验）**：`_persist_partial_usage` 多收 `elapsed`，每次 dispatch 往
sidecar 追加 `{"at":…, "elapsed":…}`（超时任务走不到收尾 ⇒ 只能落盘）。→ **§67**
✅ **修法也落了（`1775f5d`，⚠️ 真机没验）**：走**全程一把尺**（用户拍"按最合理的来，
别走捷径"）—— `RunContext.deadline_at`（任务死亡时刻绝对值）→ `_dispatch_budget_s(ctx)`
**每次 dispatch 重算** → `dispatch(budget_s=)` → `_run_executor` 挂到执行器上 →
`min(_EXEC_BUDGET, budget_s)`。**取 min 是保住 `QIDIAN_EXEC_BUDGET` 那个真机验证开关**
（写成 max，§63 那套验法会悄悄失效）。`deadline_at==0`（goal_loop/阶段级）⇒ `None`
⇒ 退回老行为；**`None` 不是 0**（当成 0 会让那两条路一个调用都不发就判死）。
验证：单测 6 条（含**三处接线**＋开关优先级）· **6 处变异逐条掐断全红** · 全量 **1200 绿**
· `test_exec_run.py` **41/41** · 手工边缘路径（真 `_run_executor`＋真执行器、每次调用
0.6s、任务尺给 3s）实测 **3.1s 自己被叫停**、账全带回来。
⚠️ **全是离线真代码，真机没验。**
⚠️ **用户提的"按已花掉的 dispatch 时间之和扣"查完否掉**：`elapsed` 只覆盖 dispatch
在跑那段，而校验/门禁/worktree 操作**以及暂停**，外面那 900 秒**全都算**
（`orchestrator.py:131` submit 起表、`:453` 到点砍，**全文 0 处提 PAUSED**）。
照它改 = **把两个时钟重新装回去、偏在危险一侧**。
⚠️ **暂停那条单独登记、没动，而且现在知道它不能随便解**：暂停时工人线程**是被堵住的**
（`while pause_path.exists(): sleep(1)`），`run_queue` 默认并发 **1** ⇒ 一旦"暂停不计入
预算"，**一个暂停的任务能把唯一的工人占死、整个队列停摆**。今天的 900s 恰是这个安全阀。
要动得先解决"暂停的任务不该占着工人"。**等用户拍。**

### 🔴「任务标成 running 却没人管」那一族（09-13，py-spy 栈定位）

- **症状**：任务被标 RUNNING、快照也拍了，之后**一个字节没跑**（0 日志 / 无 sidecar /
  无心跳 / 无取消标记 / 无 trace），过了 1100 秒**收割也没触发**。
- **根因**：`_dispatch_ready` **先 `transition(RUNNING)` 再登记 future** ——
  中间断了任务就永远挂着（没 future ⇒ 循环看不见 ⇒ `ready_tasks()` 也不返回它）。
- **全仓 5 处同一形状**，统一走 `_strand_guard`（只在还停在 RUNNING 时才改）。
- ⚠️ **最该记的不是 bug，是它为什么查不出来**：`_loop_worker` 的兜底只 `_push_event`（SSE），
  **不落日志、不进 alerts.jsonl** ⇒ 事故**连持久痕迹都没有**。
  **反过来说：有视图不等于有数据 —— 聚合视图救不了没被记下的东西。**
- ✅ 孤儿探测**真机验过**（主动造一个孤儿，循环空转时报出来，且幂等）。
  全文 → `docs/防御模式.md` **§65**。

### 其它（09-13 一晚）

- **三处没导入 ⇒ 内存维护/洞察提取/潜因果边写入从来没跑过**（`972e79f`）。
  发现它的是**新造的告警聚合视图**。⚠️ **状态仍是"从'永远不跑'变成'能跑了'"**，
  不是"收益拿到了"（触发要等 1h + 有任务收尾）。→ §64
- **F821 守卫在带星号 import 的文件里是瞎的**（全仓 10 个文件）→ 补了盲区。→ §64
- **`?format=md` 恒 500**：`RouteResult` 删了 `level`，`from_dict` 还在传 →
  **删字段要全仓扫调用点**。→ §66
- **告警按 key 聚合**（`alert_summary`）：真机验过，`collect_changes` 跨两个 scope 归并。
  ⚠️ 前端那两栏没做。
- **§59 的边界兑现**：`token_count=None` 的两种成因（没进过 dispatch / 进了没落账）
  从此分得开。→ §59 补记

**轮 1 另外两条死法**（探路3）：模型吐的 JSON 被截断（`71e0e2d` 已修 SSE 坏帧）、QA 挂 1 个测试。
**三轮下来，死法没有一个是"模型不会做"。**

**同夜还按"形状"扫了 §55**（裸 `git status` / 裸 `git diff` 判"有没有改动"）——
**一次抓出 3 处**（`2af70d4`）。此前这个形状**被修过两次**，每次只修眼前那一处。
⚠️ 另两处只是**降级出声**（拿不到基线仍会退化成跟 HEAD 比），**不是修好**。
**另**：重活判据从"会归零的进程内计数器"改成**落盘**（`6d11375`）——
「经验分层抽象」因此**从"永远不跑"变成"能跑"**（但收益还没拿到）。

**「一次多动作」A/B 跑完了 —— 结论「没效果」** → `docs/多动作A-B-20260912.md`
（配对比较：合并率 57% vs 60%，差异在噪声内）。**附带证伪了原假设**：
**不开开关时基线自己就有 57% 的轮在合并** —— 原先"基线从不合并"只建立在一个样本上。

**核心价值那条仍未证实**：「多视角碰撞 > 同档单模型」**没被证明** ——
要回答正是 P1 三臂实验（方案已写，没跑）。
⚠️ **前置 §62 已解**（09-12 夜扩池 + 提取员指到委员会外）—— P1 现在只差「流水线要空」+「A 臂要新写代码」。

**探路2**（更早那轮）：5 个任务全到终态、真正"不会做"的零；产物在
`~/项目/项目仓库/探路2-salvage/`。**探路3 那轮的项目今天已清场**，
产物+8 条原始 trace 抽到了 `~/项目/项目仓库/多动作AB-salvage/`。


## 09-13 深夜 → 09-14 凌晨：静默 except 的存量治理（**分类已盖全，修复没开始**）

**背景**：全仓 565 个 `except` 里 **433 处（77%）既不出声也不上抛**，66 个文件。
> 不是"挨个加告警"（那会糊死告警筛子）—— 是**先拦住新的 + 拿一份可排序的清单**。

**① 守卫**（`tests/test_scheduler/test_no_silent_except.py`）—— `{(文件,函数): 处数}` 的
**计数棘轮**，任何增减都红。判据：handler 体内含 `raise` 或调 `witness.*/logging.*/log.*`
才算出声（**调别的函数不算**，含 `print`）。
⚠️ **判据的洞（已修）**：原来 `ast.walk` **下钻嵌套** ⇒ 内层出声替外层销账 ⇒ **虚减**。
**真账 431 → 428**（错判据报的是 429→425）。基线 `silent_except_baseline.json`（428 处 / 297 函数）。

**② ZCode 外派分类**（glm-5.3-flash，300M 额度 09-14 过期）—— **已完成，`out-01…16` 全落盘**。
命中：**≈91 条 B/C/D 该修**，全在 `~/projects/singularity/docs/静默except待修清单-20260913.md`。
- 🔴 **对账翻过一次车**：老材料**漏发 `openai_agent.py` 7 处**，而**总数 428 = 428 正负相抵**
  ⇒ **光看总数永远发现不了**（见 [[count-criteria-hide-gaps]]）。生成器重写在
  `~/Desktop/静默except分类/gen.py`，**带硬自检**（缺口不补完拒绝写文件），
  判据**直接 import 守卫的 `scan`，不另写一份**。
- 🔑 **别逐条办 —— 只有六种形状**（S1 损坏的 JSON 当空→再存就覆写 / S2 失败写 0=编造数字 /
  S3 坏任务文件 continue 跳过 / S4 门禁静默跳过=假干净 / S5 warn 自身失败吞掉=观测单点 /
  S6 账和证据落盘失败无痕）。清单里另有 3 条单独标红：`_api_tasks:398` **回滚会毁自家仓**、
  `:340` 回退错仓库、**三条 fail-open**（`_dispatch_skills:176` / `router:159` / `_observer_tools:135`）。
- ✅ **fail-open 那一族已人看完**（写了把"吞掉后返默认值"的扫描器，**精确率 1/25 且漏了 2 处已知的**
  —— 赋值型它不认）。逐个读 17 个函数后：**只有 `openai_agent._check_permission:668` 一处真坏**
  （同一函数叠两个 fail-open：checker 为 None 走判空、checker 抛异常走 `except: pass`，
  **两条路都 `return True` 全放行**）。结论：**别为这一族造判据**，人工过完就完了。
- ⏸️ **代码冻结中**（等外包的扫描落盘，保住行号）—— **改动时机 = 那批回来之后**。

**已修完的**：part-01/02/03 筛出的 13 条（`1f0fa50` / `a33bf79` / `fc8665b`）。

**改动审阅**（8 笔 diff 判"成不成立"）—— 8 条全"成立"，**揪出三条**：
① 其他执行器**没消费 `budget_s`**（只有 `openai_agent`）② 🔴 **执行器起表在 submit 之后**
（池排队会吃穿 90s 余量 ⇒ 自收尾赶不上收割）③ **`secret_key` 改过头**。**三条都还没修。**

**③ 外派评审（架构）+ 扫 bug** —— 都归档进 `docs/`，**没修的发现登记在 `~/OPEN.md`**：
- `docs/架构评审-01-20260914.md`：**抽 8 条最要紧的核过，8 条全对**；揪出**我们材料里一条假前提**
  （`run_phase` 报"未知 phase"这条 09-11 就修了 —— 材料照抄了旧 docstring）。
- `docs/架构评审-02-20260914.md`：⚠️ **被污染**（同窗口接着 01 写、读过 01），
  **而且它第 8 行自称"与评审-01 相互独立"是假的** ⇒ 见 [[outsourcing-to-a-chat-model]]。
- `docs/扫bug-01-20260914.md`：12 条，**高危 5 条我翻代码核过全是真的**（超时收割不查 `fut.done()` /
  `dispatched` 只增不减 / PAUSED 在 `_SCHEDULABLE` 但两步 CAS 都拒 ⇒ **`run_queue` 0.5s 空转永不退出** /
  `_check_paused` 无上限等而 900s 照走 / `budget_s` 只有 openai_agent 消费）。
- `docs/扫bug-02-20260914.md`：28 条（第二轮补第一轮没读的那片），**7 条高危我逐条翻代码，全是真的**：
  ① **GATE 关键词倒挂** —— 批准分支的 `"通过"` 先于打回分支的 `"不通过"` 匹配（`_observer_answer.py`
  三处同型）⇒ **人审门里输"不通过"结果是批准** ② **`api_store` 整库自毁链** —— 读到损坏即 `_seed()`，
  而 `_seed()` 末尾就是 `_save()` ⇒ 一次撕裂写让下次读取把欠费标记/别名**全盖成种子数据**
  ③ **MCP stdio 响应不按 id 匹配** + 30s 超时后迟到响应喂给下次调用 ⇒ 串位
  ④ **Observer WS 零鉴权 + `0.0.0.0`** 而 `bridge.py` 那侧有 token 握手 ⑤ **`/api/memory/chain` 恒 500**
  （调的 `get_task_chain` 全仓无定义）⑥ **`validator` npm 失败被显式豁免** ⇒ JS 项目全红也报 passed=True
  ⑦ **MAGMA depth-1 无 `narrative` 键** ⇒ 整条记忆信号链是死代码。
  ⚠️ 它这轮**并行代理全挂**，是单线程直读的。**中危 11 条没逐条核（当候选）。**
- `docs/评审-02b-20260914.md`：**干净窗口重发的 02**（02 被污染后重跑，硬约束"别读 ZCode审阅 目录"）。
  它揪出的假前提**和 01 是同一条 ⇒ 不算新证据**；真增量是 ① `docs/生产流现状.md` **自相矛盾**
  （`:71` 说"不认识"、`:186` 把修复列为待办）② 材料 8.2 表格列着 `FIXING`，而它 **6/27 就删了**
  ③ 8.1"实测并发 2"与代码相反（默认 **1**，`max_workers=2` 是**集成合并专用池**）。
  **我核了它 10 条声称：9 条一字不差，1 条是假的**（"76→82 个 .py" —— **拿递归的今天比顶层的材料**）。
  ⚠️ **它两个头号推荐我们都已经有了**（推荐建 `test_silent_except.py`，而那台守卫 09-13 就建好了；
  推荐给 `alert_summary` 加 `first_seen/days_chronic`，而那两列已经有了）——
  **"核了代码但没盘查已建好的资产"**，和它自己诊断的病同族。
- `docs/扫bug-03-20260914.md`：19 条（6 中 13 低），范围 = **前两轮都没碰的 5 个文件**（≈4477 行）。
  **我核了 6 条关键项全真**：`/api/auth/status` **在免认证白名单里 ⇒ 未认证可拿全量用户清单** ·
  敏感文件黑名单是 basename 精确匹配 ⇒ **`.env.local`/`id_rsa` 读全文** · `_is_local_origin` 写的是
  **带方括号的 `"[::1]"`** 而实际返回 `::1` ⇒ **永远匹配不上** · **SSE 回放迭代 deque 被线程 append
  ⇒ RuntimeError**（外层 try 只有 finally，异常直接穿出去）· bootstrap 只给 `token[:8]` **自锁死门** ·
  `_VALID_DECISIONS` **零引用**。**它还主动排除**了自己最大的疑点（`_pending_sse_events` 是同一对象）。
- `docs/文档核对-20260914.md`（B）：**防御模式 70 + 性质清单 20 = 90 条逐条对源码 —— 0 条「完全不符」**，
  8 条部分过期、2 条核不了。**它自己找到了 `test_no_silent_except.py`**（02b 没找到的那台）。
  ⚠️ **它的表头跟自己的正文对不上**（表头写 7 条、正文标 8 条，多列 §26 漏了性质 17/20）—— 又一个总数掩盖分布。
  **它核出的三条该做**：`openai_agent` 子进程 **env 过滤不含裸 `KEY`**（`MY_KEY` 漏网，**安全缺口**）·
  `validator._norm_verdict` 默认 `accepted`（**第二条 fail-open，但注释自认是已知残留**）·
  `_token_budget.record` 三步顺序**只在注释里**（§36 自己定的规矩是"顺序要机器保证"）。

**✅ 第一批修复已落地（`90580a2`，2026-09-14 03:0x）** —— 从上面四轮里挑改动最小、最有价值的 6 处：
① **人审门回复判定**（**"不通过"子串倒挂 ⇒ 打回永远不生效** + "这方案行不行"的"行"就把门放行）
② `[::1]`→`::1` ③ 敏感文件黑名单兜住 `.env.local`/`id_rsa` ④ 子进程 env 脱敏改**分段匹配**
（裸子串 `KEY` 会误伤 `MONKEY`/`KEYBOARD`）⑤ **权限闸门两处 fail-open 改 fail-closed**
⑥ `/api/memory/chain` 改 `find_causal_chain`。
**测试 14 条、8 个变异全红、全量 1230 绿。** ⚠️ **静默 except 棘轮主动报"清干净了 2 处"**
（基线 428→426 / 297→295，与我改的两处对上）—— **不看代码也能确认修复真落地**。
⚠️ **写测试时抓到我自己一个 bug**：`except` 块里的闭包引用 `e`，Python 在 except 末尾 `del e` ⇒ NameError。

**✅ 第二批也修完了（`4281960`，2026-09-14 03:3x）—— 8 处。** 头 5 处是安全/门禁/崩溃：
`validator.run_project_tests` **npm 非零退出不再豁免**（原来两个分支都不进 ⇒ `passed` 停在初值 True、
`runner` 停在 `""` ⇒ **纯 JS 项目 npm 全红报 passed=True**，理由是第三种谎"三个 runner 都启动不了"；同时补
`Missing script`/ENOENT = "没找到测试"，免得把"没配 test 脚本"判成失败）· `/api/auth/status`
**默认 `include_users=False`**（fail-closed；它在公开白名单里，靠"每次记得传"迟早漏）·
`/api/auth/bootstrap` 回**完整 token**（盘上只存哈希 ⇒ 那是唯一一次机会，打 `[:8]` 等于没给）·
`mcp_server_delete` 现在调 **`MCPRegistry.drop_server()`**（原来只删配置文件、注册表里客户端和工具原样还在
⇒ 界面删了模型还能调；delete 与 reconnect 共用这段）· **SSE 回放读写同一把 `_SSE_BUF_LOCK`**。
**后 3 处是"§67 那个病又下了一层"**：fallback 链每轮塞同一份 `budget_s`（**最坏 3× 超时**，改成折成绝对时刻每轮重算）·
**claude-cli（默认类型）/ anthropic-api / zhipu-api 根本不消费 `budget_s`**（各用固定 300s 上限 ⇒ 照样
越过任务死线被 900s 无声收割；三处取 `max(1.0, min(原上限, budget_s))`，zhipu 的限流重试也看预算）·
🔴 **起表位置错**：`deadline_at` 原来在 worker 线程开头算，而任务是**先 submit 进池、排到之后才起来**
⇒ 两把尺差一个**排队时间**（并发 1、单任务可跑 810s ⇒ 排队几分钟是常态）；改成 orchestrator 在
**submit 之前**算好传进去 ⇒ **"同一把尺"不只是同一个常量，还包括同一个起点**。
**测试 18 条、11 个变异逐条掐断全红、全量 1241 绿。** ⚠️ **真机没验。**
✅ **还清掉一句旧账**：**"unittest 对 0 测试退出码 0" 不成立** —— 3.14 实测打印 `NO TESTS RAN`、**退出码 5**，
本来就被 `"no tests ran"` 兜住，**差点为一个不存在的洞写代码**。
⚠️ **顺带一条方法论**（已进 `docs/防御模式.md` §67 补记）：这次我**分析对了却没写下来** —— §67 原文写的是
"离派发只有线程调度那点差"，**把"差不多"当成了"就是同一刻"**。
⇒ **凡是要写"只差一点点"，先问：这一点在什么条件下会变大？**
**✅ C（S1 统一修法草案）已到并核完**（02:14，34KB）⇒ `docs/S1统一修法草案-20260914.md`：
清单 S1 行点的 12 处**它自己核出 11 真 1 部分错**（`mcp:434` 其实出声了、且是 TOML，真链在下游
`_api_admin.mcp_server_add/delete` 的读→整份写回），**另找到 3 处两张表都漏的同形兄弟**。
**它 14 条结论性声称我逐条翻当前源码，全真**；**最值的一条**是 `dispatcher.load_agents` 走同一个
`_load_custom_agents`（`dispatcher.py:383` 星号导）⇒ **tri-state 化不加降级会炸出新的 AttributeError
——"修 A 文件不能砸 B 读法"**。修法：`_io` 新增 `load_json_or_quarantine` / `load_toml_or_quarantine`，
三态（缺文件=空 / 正常 / 损坏=`None`+备份`.corrupt`+双通道出声），**只读路径降级、读改写路径必须 raise**；
**必须在函数体内 lazy import**（`witness→tracker→_io` 是现成的环）。**它自己纠了草稿里两处结构错。**
⚠️ **我判的风险是执行方式**：**16 个接入点一次上就是"别一晚堆 50 处改动"那种** ⇒ 按形状分批、
**先拿 `_process_ledger` 一处跑通**（链路最短：500 行→1 行）。
⚠️ **它漏了一条**：`.corrupt` "时间戳轮转" = **无界增长**，**得先定上限再写函数**。
🆕 **我另抓一处它没提的**：`_io.load_toml` 的 **docstring 撒谎**（说"解析失败返回空 dict"，代码是**抛**的）
—— 而整个草案的方向就建立在"`load_toml` 会 raise"上，**它是从代码读出来的、照文档读会读反**。
**✅ F（核 生产流现状/项目速查 vs 代码）已到并核完**（02:30，**它直接写进仓库**）⇒
`docs/文档核对-02-20260914.md`：**101 条 = 成立 77 / 部分过期 19 / 完全不符 0 / 核不了 5**，
**我抽 9 条结论性的逐条翻码全真**（速查写 v4.4 实际 `pyproject` **2.1.156** · 「集成失败 ≤2 次回
EXECUTING」**偏宽**，判据是 `>=2` ⇒ **第 2 次就升 GATE2** · 「issues 在 `_run_execution` 开头清空」
**位置错**，真在 `workflow.py:430` · 辩论轮数**默认上限 5、env 可调** · `/ws` 是**独立 WS 服务器** ·
`/api/tasks/<id>` **无 DELETE** · 测试收集 1241/1248 · 防御模式 71 条 · `test_flow_weight` 22 条）。
⭐ **它自报的基线也是准的**（`HEAD = 90580a2`，提交时间正是 01:54:15）—— **没谎报**。
⚠️ **但第二批 `4281960`（02:09）正落在它跑的中途** ⇒ **它是"对 90580a2 的结论"，不是对 4281960 的**
⇒ **教训：派任务要把"基线提交"钉进指令体、要求开工时钉一次。**
✅ **它自己的纪律值得记**：只跑 `grep -c '^### '` 取防御模式条数、**没读正文** —— 那正是 B 被污染的方式，它躲开了。
⏸ **它核出的 19 条「部分过期」是现成的改文档清单**（末尾按文档位置排好了），**等四条都发完再改**。
⚠️ 「防御模式 **71** 条」这个数是**跟着编辑漂的**：我今天加了一条 `### §67 补记`，`grep -c '^### '` 就从 70 变 71
⇒ **别把这个数写死进文档正文**。
**✅ E（反向审我们自己的修复）已到并核完**（02:24）⇒ `docs/反审-01-20260914.md`：
**23 处改动（21 对 / 1 存疑 / 1 错）**。我 4 条结论性声称逐条翻码全真，**它引的 10 个提交号全部存在且描述对得上**，
而且**它发现树在它跑的中途动了（`4281960`）并跟上了**（比 F 那份只钉开工基线做得好）。
🏆🔴🔴 **它抓到一个真洞，是我自己（`a33bf79`）引入的 —— 今晚最值的一条产出**：
**GATE3 人审门把"QA 报告读坏了"渲染成绿色「QA：通过」**。链路（我自己逐环节复核过，属实）：
后端 `app.py:1469` 读不出报告时给占位符 `{"error": …}`（`a33bf79` 从 `None` 改来的）
→ 前端 `GatePanel.tsx:190` **拿它当真值** → `sum={}` → `failed=0` → `qaOk=true` → **绿"通过"**。
**改之前是灰"无报告"** ⇒ **我那次"修"把它从灰爬成了绿**，而且就在人审门上（那面板注释自己写着
"不展开也能判断该不该过"）。⚠️ **判据就在同一个组件里**：隔壁 `conformance` 的
`unverifiable === true` **早就给了琥珀色"无法核验"**（`:197-199`）—— **同一个"我核不了"，
左边给琥珀、右边给绿灯**。✅ **已修（`d55d1c1`，用户"那就修"）**：判据从「是不是真值」改成
「**像不像一份报告**」（真报告一定带 `summary`，见 `validator.build_qa_report`），琥珀色 + 文案「核不了」+
**把后端那句解释露在摘要行里**。测试 3 条、**变异 5/6 红**（漏网那条是"放宽判据"、也是 fail-closed，
不是洞；⚠️ **第一轮"只改颜色"那刀漏网了** ⇒ 补了取 DOM 颜色的断言）；前端 47 绿 / tsc 干净 / 后端 1241 绿。
🟠 它另抓两条我核过：**worker 异常分支的抢救段裸奔**（`orchestrator.py:442-445` 三连外面没 try，
而同款超时分支 `:513-518` 包了 `_strand_guard`；低概率但一旦抛 = **§65 孤儿整批重现**）·
**`roles.py` 读侧只 `logging.warning`**（写侧已 fail-closed，读侧还在吞）。
⚠️ **一条核报告的纪律**：E 把 secret_key 判"存疑"并建议"不覆盖+出声+内存随机"——
**那个改法 `4281960` 早落了**（它是对着 `1f0fa50` 的 diff 判的，不是当前树）
⇒ **核外派报告要回当前树确认，别照着建议改已经改过的东西。**
**✅ E'（只审 `90580a2`+`4281960`）已到并核完**（04:0x）⇒ `docs/反审-02-20260914.md`：
**14 处 = 9 对 / 5 存疑 / 0 错**。**5 条存疑全是同一形状：「本体修对了，但它声称要治的那个病，
在别处原样还活着」** —— **我逐条实测，全部属实**：
① 🔴 **人审门只修了一半**（实测真调 `_is_gate_reply`）：`'行不行？'`→approved（疑问句放行）、
`'不同意'`→approved（"同意"是它的子串，`_REJECT_WORDS` 里根本没有"不同意"）、`'可以吗'`→approved。
⇒ **我修了 `不通过 ⊃ 通过`，没修 `不同意 ⊃ 同意`；短词那条注释写"整句就是那个词"、实现是 `startswith`**。
② 🔴 **env 脱敏只修了类方法**：anthropic 执行器走**模块级** `_run_command`（`openai_agent.py:1137`），
`subprocess.run(...)` **根本没传 `env=`** ⇒ 全量 `os.environ` 照漏。
③ 🔴 **anthropic 的 `_tmo` 循环外算一次**（**我自己 `4281960` 写的**）、max_turns 轮共用、**无逐轮看表**；
对照 `openai_agent` 有 `time.time() >= self._deadline_at: break` ⇒ **900s 无声收割经 anthropic 仍可达**。
⚠️ **②③ 当前是"纸面上的洞"**：`anthropic-api` 是**注册了的**执行器类型，但 `agents.toml` 与运行时
`.qidian/agents_custom.json` **都没用它** ⇒ 改一行配置就活。
④ **`mcp_server_add` 也只写配置不动注册表** —— 与 delete 同一面镜子，**这次只修了 delete**
（而 `drop_server` 的 docstring 自己写着"免得下次再漏"）⇒ **这入口今天就在漏**。
⑤ **`base._BLOCKED_PATTERNS`（agent 侧 read_file 闸）里 `id_rsa` 那类原样还在**，与 web 侧那张表分叉。
⚠️ **⑤ 我没核完（上下文用尽）—— 当候选不当结论，下次接手先读它。**
🔴 **流程账**：`4281960` 的 diff **实际有 9 处改动**，我的 commit message **只列了 8 处**
（**secret_key 那处改了却没登记** —— 违反"提交要写为什么"）。
🔑 **共性（第三次了）**：**修"形状"类问题要回头扫"同族还有谁"** —— §67 下两层 · `dispatcher` 读路径 ·
这次的 add/delete。**这个动作我做得不稳，得当成一个固定步骤。**
**✅ G（判 `待验证清单` 哪些读码就能定）已到并核完**（04:2x）⇒ `docs/核待验证-01-20260914.md`：
51 条 = **31 条读码就能定（全"成立"）· 19 条"接线静态成立+某一观测半必须真机" · 1 条纯环境**。
🔴🔴 **它顺带抓到一条真洞，我逐步核过、链路闭合：「手动取消 = 删标记 + 重跑」** ——
① `_api_tasks.py:283-290` 取消只写 `CANCEL_DIR` 标记、**任务状态一字未改**；
② `_exec.py:262` `_check_cancelled` 命中后 **`unlink()` 把标记删掉**；
③ `_exec.py:796-801` 早退集合**不含 `cancelled_by_user`** ⇒ 落进 `retry += 1`
⇒ **标记已删、重试时查不到 ⇒ 真的重跑**，最多再烧 3 次 dispatch。
⚠️ **我只核到"会重跑"**（状态始终没离开 RUNNING、"会不会被当成正常完成"我没核，当候选）；
**全程静态证据、没真机复现**；**900s 那档无害，手动取消/暂停中取消那档是真的**。
⚠️ 另两条**我没核、当候选**：`待验证清单` **#33 的"并发 1"前提过期**（实际部署传 `concurrent=2`，
`app.py:2295`）· **A 臂判据①（trace 里 `_soloN`）静态看不到落盘出口** ⇒ 真机跑前必须先把观测点定下来，
否则**整个 P1 三臂实验白做**。✅ 清单 **#45 的 secret_key"还没改"属实过期**（那正是 `4281960` 修的），可销账。
**✅ D（补调用点那层，判 111 条哪些真坏）已到并核完**（04:3x）⇒ `docs/核待修-01-20260914.md`：
**111 条 = 真坏 20 / 假警报 91 / 判不了 0**（⚠️ **实数 111 不是清单首页的 88/91** —— 又一个总数对不上）。
🏆 **它最有价值的一节**：**清单标得最红的 fail-open 四条，调用层全部落空** —— 两条**已经修好了**
（`_dispatch_skills:176` / `openai_agent:668`，正是 `90580a2` 修的）、两条**有下游硬兜底**。
它一句话点到位：**清单判「except 后落在宽松默认=坏」（形状+方向），它判「这个默认值在调用层被谁吃掉」**
—— **分类器看不出的那一层被补上了**。
🔴🔴 **最要紧：GATE3 的门有两层，我只修了上层** —— 我修的是**前端渲染**（读坏了别显示绿"通过"），
**后端 `_gate3_admission` 只认 `verification_ran` 标记、不查 `qa_report.json` 是否存在 ⇒ 报告丢了门照开**。
⚠️ **D 和 E 两轮独立撞上这一条**。⇒ **下一步在 `_gate3_admission` 补"报告得真在"**。
另三条**反升级（原判低估）我抽核全真**：🔴 **`_exec.py:657` 用编造的 `0.5` 覆写 validate 的真分**
（真分 0.2→0.5 把"立即升级"错成"同模型重试"）· 🔴 **`witness.py:74` 记告警失败静默吞**（全仓唯一汇聚点
⇒ 观测整体失明而外表健康）· ✅ **S1 族按写路径劈开**（`model_prices`/`route_learner`/`_model_discipline`
**都已 `atomic_write_json`** ⇒ 降为假警报；**剩 `write_text` 直写的 8 处才是真坏**，其中
`_dispatch_crud:17` **升级**：`agents_custom.json` 有**两个独立写者**）。
✅ **`_memory_core` 的 `.corrupt` 留底 + warn 已在代码里** —— **正是那 8 条的现成修法范本**。
⏭️ **还剩两条没动（都不是小活，等拍）**：`project.py` **ProjectState 读-改-写无锁**
（`_api_tasks.py:504-506` 最典型；`save()` 自己的 `_LOCK` **盖不住 RMW 窗口**；`role_probe` 是**独立进程**
⇒ 线程锁按定义不够，**得先定"跨进程怎么写同一个项目文件"**）· `validator._norm_verdict` 默认 `accepted`
（`:761`/`:832`，**注释自认是已知残留，要改先有测量**）。



## 待办

> **⚠️ 活的待办清单是 `~/OPEN.md`**（`UserPromptSubmit` hook 每轮自动念）。
> 下面这些是**奇点仓专属、跨会话常驻**的观察项，不在 OPEN.md 里，别混。

1. **9/14 12:00 之后** DeepSeek 把 `deepseek-v4-pro` 全路由到 V4.1-Flash。
   已加别名对账会自动处理，**那天值得看一眼链是不是变成 2 席**。
2. ~~**提取员是委员会成员**~~ **2026-09-12 结案 → §62，且当天就改掉了**：
   不是偶发，是**2 模型池下的结构性必然**（池就两个、`planning` 用满 ⇒ 委员会 = 整个池；
   兜底表那两个**全被停用** ⇒ 换不动 ⇒ 每轮必报）。同族的 `designated_reviewer_is_writer`
   反而兜住了 ⇒ **一个兜得住一个兜不住，别混**。
   **改法**：扩池（`any` 加 `glm-5.2`）+ `extract` 阶段改指它 ⇒ 提取员不在委员会里。
   ✅ **真机验过（轮 2 / 轮 3）**：委员会名单 = `[glm-5.3-flash, deepseek-flash]`（**没有 glm-5.2**）；
   `extractor_swapped` / `empty_content` / `fusion_v2_empty_extract` 两轮均 **0 次**。
   ⚠️ 是**间接证据** —— 没直接抓到"这一轮提取步跑的就是 glm-5.2"。
3. **观察者角色提示词的 A/B 只跑了 A 臂**。kimi-k3 基准砍了，先放着。
4. ~~**提取员"必须是非思考模型"这条注释与实测冲突**~~ **2026-09-12 处理完**：
   真实约束**不是"非思考"这个标签，是"别把 max_tokens 烧在 reasoning 上"** ——
   而那句纪律**没有任何地方执行它**（兜底表两个都是思考模型、`glm-5.2` 也是）。
   已把注释改准，并写明真护栏是两件东西：`_V2_EXTRACT_MAX_TOKENS`（131072 够用）+
   提取空产出会**出声**（`fusion_v2_extract_failed_all` / `empty_content:*:length`）。
   ⚠️ 刻意**不加**"注册表标了 reasoning 就告警"的检查 —— 那个标注本身就不可靠。
5. **P1 三臂实验** —— 方案在 `docs/P1-三臂实验方案-20260912.md`，**没跑**（三个前置全齐了）。
   **A 臂 09-13 写完**：`_dispatch_solo`（单模型 + 等预算多轮自修订），开关 `QIDIAN_SOLO_TOKENS`
   （token 预算，`0`/不设 = 关，关时逐字走委员会那条）。口径**按 token 等**、
   预算从 B 臂实测值填；模型 = 链首（= `deepseek-flash`）。单测 10 条、变异验证过、全量 1169 绿。
   ⚠️ **一次真机都没跑过**，登记在 `docs/待验证清单-20260912.md` **§三·八**。
   （~~③ §62 污染结论~~ —— **已解**，09-12 夜扩池 + 提取员指到委员会外。）
6. ~~**`%10` 判据要改**~~ **2026-09-12 改完**（`6d11375`）：判据**落盘 + 时间兜底（1h）**，
   重启不再清零。「经验分层抽象」从"永远不跑"变成"能跑"（⚠️ 收益还没拿到，真机没验）。
   ⚠️ **同时更正一处归因错误**：`_llm_judge_causal` **不**挂在 `%10` 上 ——
   它在 `_memory_consolidator.py` 主循环里，闸门是**候选条件**（`0.55 ≤ sim < 0.85`
   且能定因果方向）。"它从没成功过"可能仍成立，但**原因不是 %10**，别再按 %10 解释。
7. ~~**§55 那个形状的扫尾**~~ **2026-09-12 扫完**（`2af70d4`）：按形状全仓扫，**又抓出 3 处**。
   ⚠️ 其中两处只是**降级出声**，不是修好 —— 拿不到基线时仍会退化成跟 HEAD 比。
8. ~~**外派材料待发**~~ **2026-09-12 已发，答卷已回**：
   材料 = 那些文档，用 `docs/生成外派包.sh` 拼（**拼出来的那份 09-13 已删** ——
   它被 v2 取代，留着只会让人拿过期的去问；git 历史里还能找回来）。
   **答卷** → `docs/静默失败治理-外部答卷-20260912.md`（**归档，不是补丁 —— 别照着直接改代码**）。
   核过它最吃重的两条：① 它推的"写点探针"**位置猜错了**（后来真机证明答案在别处）；② 它说
   "schema 漂移守卫已落地"**核实为真** ⇒ 它的增量 4「契约单源」价值低、**建议不做**。
   ⚠️ **六条增量 09-13 已全部核完**（结论在答卷 `## 六条增量的核实结论` 一节，别再说"还没核"）：
   ① 写点探针 / ④ 契约单源 —— **不做**（① 位置猜错：真机证明答案不在它猜的那里；④ 价值低）；
   ③ 告警 key 聚合、⑤ 异常收尾清单 —— **已做**（`ccdc009` / `d513fcd`，3 个缺口修了 2 个）；
   ⑥ 方法论 —— **已做**（①② 都落进 `docs/防御模式.md` **第九章**）；
   ② 字段活性账 —— **没做**（结论：**先**手写短清单 + 一个"从未活过"的查询，
   **别先造埋点** —— 不带注册表的埋点没处挂，"首次非默认"也抓不到"从来没跑过"那种形状）。

关联 [[qidian-audit-20260911]] [[qidian-model-pricing]] [[qidian-usage-history]]
[[qidian-token-accounting-coverage]] [[qidian-auto-vs-gate]] [[qidian-debug-first-look]]
[[commit-messages-explain-why]] [[qidian-thinking-params]] [[user-config-is-intentional]]


---

## OPEN.md 接手指针里搬出来的「外派窗口状态」（§②）

> ⑪⑫⑬ 三个外派窗口的账，全做完了，09-16 凌晨搬出。

  **② 🚦 窗口状态**：**⑪（预检工具）已写完 → 已核完归档**（`e301e9a`；它后来自己又改了
    一轮，我复跑过 5/5 成绩单）· **⑫（外派脚手架）已核完归档**（`6e215f0`）·
    **⑬（审测试判据）60/60 全部过完** —— 前半在 `docs/审测试判据-01-20260914.md`
    （它自己写到 A5 停笔）、续跑的批次 + 我的逐条变异复核在 `-02`；最后剩的"未核候选"
    那批也已在 09-15 清完（`11a58f4`）。**它报的洞条条核过、修过，别照旧抬头重头扫。**
    ⚠️ `docs/要发的指令.md` 里那条 ⑬ 的原文 09-15 已挪进归档（它原来还躺在"待发"里）。
    ⚠️ **教训已落 `docs/防御模式.md` §72**：和外部窗口共用一棵树时，**提交写明确路径、
      不许 `-A`**；`git add` 前先 `git status`。（09-14 那次把外派⑪的半成品扫进了 5 条提交。）

---

## 2026-09-21 归档：从记忆正文里搬出来的逐日账（09-19 ~ 09-21）

> **为什么在这儿**：`memory/qidian-bug-backlog.md` 的「现在怎么样」那一节**每次整段重写**，
> 09-21 第十五次重写时把下面这 370 行挤掉了。而它是**逐日历史**，
> 按那个文件自己的分工就该待在 `docs/演化史.md`（术语：「历史进演化史.md」）——
> 只是这个文件停在 09-14，没跟上。**原样搬过来，一个字没改。**
> ⚠️ 里面有些"现在怎么样"的措辞是**当时**的（比如某项还标着"未验"），别当现状读；
> **现状只看 `~/OPEN.md`**。

## 现在怎么样（2026-09-21 04:1x 第十四次复核 —— 这一夜真机跑了两轮）

**权威只有 `~/OPEN.md`（30 条），这里只写"现在怎么样"。**
⚠️ 看到本节日期比 `~/OPEN.md` 旧，就是**又过期了**。

### 🆕 09-21 01:00–04:10（真机两轮 + 一处翻案）

**真机结果**：`round-20260921`（被我 lldb 杀了、无产出）· `round-20260921b` **跑到 GATE2 兜底**。
四轮成绩单 `0/8(0%)·26k → 2/9(22%)·710k → 7/12(58%)·1.17M → 4/7(57%)·814k`
—— **单产物 token 第一次下降（−30%）**，「没进仓」那栏第一次是 0。⚠️ 分母不同（7 vs 12），
**别把 57% 读成"和 c 轮一样好"**，绝对交付是 4 个任务 vs 7 个。

· ✅ **验收表态那条门「验了」—— 但结论是两条，方向相反**：
  · **模型照新 schema 写**（13 条 argv + 1 条诚实 + **0 条没表态**）⇒ 原先那个"不照写就卡死"的担心**不成立**；
  · 🔴 **可那道门本身是装饰** —— 实测「散文验收」走完那条路 `blockers=[]`、`fatal=[]`、GATE2 照过。
    根因：`_run_planning` 里一行**内联子串匹配**（`"缺少"/"无效"/"应为"`）把新规则那句
    「acceptance **没表态**」滤掉了。**13 条测试全绿，因为中间那一行没人测**（函数对 ≠ 接线通）。
    ✅ 已修 `2e15f049`（抽 `split_arch_issues`，**新规则默认落进"要审"那一边**）+ 端到端接线测试。
  · ✅ **验掉两条挂了几天的待验项**：`76dc2592` **只读任务不再被判死**（T7 `changed_files=[]`
    而 `verdict="通过"`）· **机检归因 `missing_inputs`**（点名报出缺哪个文件、`our_side:0`）。

· 🔴 **「零产出族」查到底了 —— 是这一夜最重的一条**（全文 `~/OPEN.md`）：
  最后一轮 prompt 10.2 万字符 ⇒ 模型把**整个 240 秒用来思考**（正文 0、工具调用 0）⇒ 被掐
  ⇒ **代码故意判"无可用产出"、不重试** ⇒ 任务 `blocked`、改动 0 文件、输出长度 0。
  🔴 **缺口不是"240 太小"，是两种 deadline 共用 `error_kind="deadline"` 一个标签**：
  (a) 任务总预算真用完 ⇒ 「不换模型」**对**；(b) **单次 240 秒到了、任务还剩几百秒** ⇒ 同一支 ⇒ **错**。
  T5 实测是 (b)：810 秒预算**只用了 441 秒**。
  · ✅ **已做**：`executing` 放开成 `[deepseek-flash, glm-5.3-flash]`（**核过链长 2**，那段容灾不再是死代码）
    · 补了「配一家 ⇒ 没兜底可切」告警 `c30b70fa` · 顺带修「**同时踩两处只看得到第一条**」`e0f102e6`。
  · ⏭️ **未做**：把 (a)/(b) 分家（新 `error_kind`）—— **要先有"换模型成不成"那个数，而它现在不存在**
    （账上 28 个任务**换过模型的 0 个**，不是"换了都不成"，是"一次都没换过"）。

· 🔴 **前端白屏**（用户点「📁 文件」报的）：`acceptance` 新形状的**第 6 个消费者**在前端
  —— `ArchitectureDetails` 里 `{t.acceptance}` 直接把对象塞进 JSX ⇒ **React error #31**、整面板白。
  已修 `085d9796`（`acceptanceText()` 拍平，旧形态原样透传）+ 渲染 `ProjectMaterials`（页面真挂的那个）的测试。
  📌 **教训入 `docs/防御模式.md` §89**：改值的形状要扫**全部**消费者，而我**扫了两遍**才被用户点出来。

· 🔧 **工具修了两处**：`delivery_facts --rounds` 被新旁挂文件打崩（黑名单 → 白名单 `6ad87236`）·
  全量测试里一条**本来就在红、而 `make check` 看不见**的守卫（`2e15f049` 同笔）。

### 🆕 09-21 00:0x（验收表态那道光）

· ✅ **`bf571c57` `acceptance` 必须【表态】** —— 补上 §88 真根因的那道门。
  三态（复用 `constraints[].check` 的 `parse_check`）：**兑现**（`{argv,expect_exit}`）/
  **诚实**（`{text_only_reason}`，**放行**）/ **漏**（都没有 ⇒ **致命档，架构校验当场拦**）。
  🔴 **"诚实"必须放行是命门** —— 逼模型"必须给出命令"它就会**编一条假命令**（假门比没门坏）。
  用户原版拍板是 A（写不出命令就不许建任务），**我核出它会杀掉一个在量的数**（`parse_check`
  写着"两种写法是故意的……**两者的比例就是那个数**"）+ 逼出假命令 ⇒ 改成 **A′**，
  用户拍板「按你的」。⚠️ **上面那节写了它落地后的真机结果与翻案**，此处不重复。

· 🔵 **同轮还改了 `docs/防御模式.md` §88**（第三版）：规则 5 加了「写了不判的验收比没写更坏」，
  规则 6 记「我在同一个判断上栽三回」，规则 2 改成「别急着把锅推给 worktree 隔离」。

### 🆕 20:0x–23:5x（天枢对照 + 契约冻结 + 体检脚本）

· 🆕 **读了开源同类「天枢 harness」，对照在 `docs/天枢对照.md`**（**只读它的 docs，没读 src**）。
  结论：**不是 7 倍强，是 7 倍量**（它 51 万行 vs 奇点 3.6 万+3.3 万；**测试:源码 0.99 vs 0.91**，
  两家独立撞出同一个比例）。🔴 **它自曝的假绿和 `docs/失败形状.md` 是同一个物种**。
  ⚠️ **别信它的**：`decisions/`、`specs/` **是空目录**；README 说"星域切工具白名单"而它自己 docs 写
  "全工具白名单、星域是认知姿态"（**README 与 docs 打架**）；CVM 实证 **n=5** 却标"显著性极高"；
  **招牌功能 `review.skipAuto` 开箱默认关**。
  ✅ **可抄**：`绿声明 fail-closed` 这个说法 · `OBJECTIVE_REVIEW_STANCE` 第 4 条
  「**复核"定义"是否真接入调用边界**」（这条早写进清单，`result-hero` 会早一天被抓）· 它的探针测量法。
  ❌ **抄不了**：**前缀缓存整套** —— 它的前提是 append-only 长会话，而奇点**每 turn 重开请求**。

· 🔴🔴 **c 轮交付「跑一次就崩」，而 QA 判词是「代码质量高」**（全文 `docs/防御模式.md` §88）。
  🔴 **真根因（09-21 第三版才核对，前两版都错）：架构预言了病、开了药、药没吃 ——
  而「药吃了没」没人检查。** 架构 risk 段原文写了「**契约漂移**」并开药：
  **T1 必须产 `CONTRACTS.md` 逐字段冻结三个契约**；实测**该文件全历史从没存在过**、
  **8 条机器检查里 0 条查它** ⇒ **T1 照样判 `done`** ⇒ T2–T6 全以为契约冻结过了。
  ⚠️ **更正**：不是"两个 worktree 从没对过"—— `git merge-base --is-ancestor` 证实
  **T1 的提交是 T4 的祖先，T4 看得到契约**。**不是看不到契约，是没有一份契约值得看。**
  🔴 **更狠的一层：测试被扔掉 = 把 bug 洗白** —— T9/T10 死法是"写了测试、跑红了、判 failed ⇒ 不合并"，
  **而它们红的原因就是主线这个 bug** ⇒ 闭环：实现 bug 让测试红 → 测试任务 failed → 产物扔 → bug 没人发现。
  ✅ **那 1,680 行测试没丢**，在 `refs/qidian/pending/*` 上（**b 轮同类 refs 还有 7 条**）。
  ✅ **新脚本** `scripts/pending_refs_probe.py <project_id>`（**只读**，tempfile 克隆）—— 捞出来跑一遍。
  ⚠️ 38 条红查清了：**1 条真实现 bug**（`parser.py` 的 `_resolve_spec()`，`strict` 形参默认值
  盖掉 `spec.strict=True` ⇒ **静默降级**）+ **18 条测试半成品**（**12 条同一根因，修一行全绿**）。
  🔴 **我在这件事上两次下早了全称结论**（先说"红的全是一个 bug"、再说"十几个洞"）——
  真数 1+18。**已写进 §88 规则 6：我在同一个判断上栽了三回，三次都是从"部分证据"跳到"全称结论"。**

· 🆕 **「稳定产出」还没有判据**（用户 09-20 说「感觉看不到头」）。现成的尺子是
  `scripts/delivery_facts.py --rounds`（**只摆事实、不替你选尺**）。三轮：**0/8 → 2/9 → 7/12**，
  单产物 token **355k → 167k**。⏳ **要用户拍一个数**（到多少、连几轮、QA 算不算）。

· ⚠️ **`max_turns` 不是病根**：按 dispatch 侧车数，实现任务 T1–T6 = 2·2·**6**·2·2·1（全 done），
  **只有 T7 一条撞上限**，而它连侧车都没有（=**零产出**，属「455 秒零产出」那条族）。
  ⇒ **别放宽**（只会让零产出的多烧 token）。

· 🆕 **240 秒那条拆了**（`§87` 补 87.5 报警已落地 / 87.6 尺子 `elapsed` 量长了），清单只剩"真打断的阈值"。

· ⚠️ **今晚两次改口**：20:1x 说「不推」→ 23:4x 用户改口**推了**
  （singularity 4 颗 + dotfiles + claude-memory）。⚠️ 推代码踩到 **zsh 不做无引号分词**的坑
  （存变量再用 `$VAR` ⇒ git 报 usage，**不像网络问题**），已补进 [[network-proxy-arxiv-mirror]]。

### 🆕 19:0x–19:5x 的五笔（都推了、CI 待跑）

· ✅ **删任务的产物不再丢**（`a914a18b` + `92d7be18`）—— 那条 🔴「清理孤儿 pending ref」的地雷**拆了**。
  改法 = **换桩**：`pending/<id>` → `refs/qidian/salvaged/<id>`（**先建新桩再拆旧桩**；
  建不出来 ⇒ 一个字都不动），线索写 `.qidian/salvaged.jsonl`，`delivery_facts.py --refs` 的 ⑦ 节印出来。
  ⚠️ **真机没验**。⇒ **上面旧结论"代码里的自动清理路一寸没动、仍是地雷"作废。**
· ✅ **「⚠ N 条项目问题」「为什么又问你一次」原来挂在死组件上**（`00072a18` + `c23b0f54`）——
  09-20 **真机看界面**抓到的：这两块只被 `GateBody` 渲染，而 `GateBody` ← `GatePanel`
  **没有任何调用方**（09-17 侧滑面板重构把 `<GateBody/>` 换成了 `<ProjectMaterials/>`）
  ⇒ 加进去的东西**落地即隐身**，而单测直接渲染 `<GatePanel>`（把两半拼起来）所以照绿。
  **两个组合件已删**，测试改成渲染页面真正挂的那三件 + 一条**页面级**接线测试钉着。
  ⇒ **旧结论"把 `GateSummary` 那个尸体参数接上了"是错的**：接上了，但没人渲染。
· ✅ **只读任务不再被判死**（`76dc2592`）—— `[只读]`（`config.READONLY_TAG`）这份协议标记
  **只被 `supervisor` 认**，而**决定 retry/abort 的 `validator` 不认** ⇒ 零文件改动 →
  `信息不足` → 逼一轮返工 → 还是零改动 → `abort`。真机 6 条「返工了还是没过」**全是这个形状**
  ⇒ **返工那一轮就是这条逼出来的**。＋收尾话术按任务分岔（只读任务说"别写文件"）。
  ⚠️ **真机没验**。
· ✅ **「整轮只想不产出」出声**（`7677f45e`，240 秒那条的落地）—— `key="llm_spin_no_output"`，
  **只报警、不打断**（打断阈值仍然没定）。真账 248 条里这个形状 **30 条（12.1%）**。
· ✅ **关键告警推 macOS 桌面通知**（`de8fdf38`）—— 白名单 7 个 key（判据「罕见 ∧ 人得动手」），
  同 key 60 秒冷却，总闸 `QIDIAN_NOTIFY=0`。⚠️ **名单是我替用户挑的**，等他看一眼。
· 🔧 **阵容**：激活池加了 `deepseek-v4-pro`（1.848/M）当**非委员提取员**。
  🔵 **先纠正旧结论**：洞① 是"**静的病**" —— 真账里 `fusion_self_judge` **一次都没发生过**
  （委员会 = 这一轮真产出方案的那几个，而 `planning` 只配了 1 家 ⇒ 提取员压根不是委员）。
  **今天的代价 0**；**开始花钱的条件**：谁给某个项目设了 `agent_lineup`。

### ✅ 两条被数据结掉的

📌 **读 `turns_used` 的姿势**：它记的是 **validate 跑了第 i 圈**，所以 `2` = 返工完那一圈
（不是"两轮都失败还标通过"）；`0` = 被掐断（`validation=None`）。

· **「返工上限放不放开」—— 不放开**。31 次返工里 25 次通过（81%）；把没过的那 6 条逐条挖开，
  **没有一条是"同一个错重复"**（那才是"多给一轮就有用"的形状），全是被别的东西挡死
  —— 一半就是上面那条只读任务。⇒ **多给一轮救不了它们，只是多烧一轮 impl。**
· **240 秒那条**：不是"不够"，是"**它在原地打转**"（`docs/防御模式.md` §87）。判据 =
  **被掐 ∧ 正文 0 ∧ 工具 0**（正常的"正文 0"绝大多数调了工具）。"加时间"被数据否掉。

### 🔴 上面 13:2x 那批**仍然成立**的部分

🔴🔴 **09-20 早上把三个结构性坑修了**：
· ✅ **`impl` 返工不再只重置 `DONE`** —— 加上 `FAILED`（A 档，用户拍板）。
  改前 `tracker.ready_tasks` 只扫 PENDING/ROUTED/BLOCKED ⇒ 失败的任务**永远不会被重派** ⇒
  返工 = 把做成的推倒重做、没做成的照旧缺 ⇒ 转圈。
  🔴 **顺带堵了一个会踩的坑**：超时那条路是**成对**写的（先写 `cancels/<id>.json`、再转 FAILED），
  加了 `_clear_cancel_marker_for_rewind`（timeout 标记删掉再重派 · **人工取消的不重派**）。
· ✅ **审查触顶那条兜底路不再直接交人** —— 先问 `_auto_rework_allowed`，允许就回炉一次（B 档）。
  ⚠️ **同族第二次**（09-17 它绕过过整个验收层）⇒ **"兜底"要穷举它绕过了什么**。
· ✅ **机器检查的失败归因**（`missing_inputs`）：分不出「测试挂了」和「**产物压根没交付**」。

⏳ **09-20 落地但没真机验的**（判据全在 `~/OPEN.md` 的「接手指针」）：
A 档 · B 档 · 机检归因 · **只读任务** · **换桩** · **转圈告警** · 观察者覆盖写。
✅ 已经验到的：**GATE2 兜底那支真机上确实改口了** · 📦 交付抽屉不再是空盒子（09-20 晚上看界面）。

🔴 **代价一起读：token 三轮全涨** 26,392 → 709,734 → **1,168,802**（c 轮比 b 还 +65%）；
「单轮 token 下降」**到现在没出现过**。⚠️ 混淆没排除（c 轮任务构成和 b 不同，12 vs 9）。
⚠️ **B 档让"再烧一轮"变成默认动作**（回炉）—— 下一轮的账要把这笔算进去。

🔵 **一条"最重的待办"核出来不是 bug**：「任务排队等的时间算进执行预算」——真因是**并发 1**。
已移进 `~/OPEN-别动.md`。⚠️ 教训：**清单里 🔴 的严重度是当年判断的，不是今天核过的。**

⚠️ **两条尺子自己也有缺陷**：跨轮那把"两栏会重叠"（已印「两栏都在 N 个」）；分诊账的 `elapsed`
**是在关流之后量的**（28 条 `cut=true` 里 8 条 >240s、最高 947s，而 `_cap` 恒 ≤240）
—— **大数值当上界读，别拿它算"烧了多久"**。

🔴🔴 **ZCode 已卸载（09-19）** —— 它 09-18 被曝光"登录状态下静默打包整个工作区上传"，
本机核实它真在这个仓库读写过（`Edit`×60/`Write`×18 指向 `~/projects/singularity/...`）。
详见 [[outsourcing-to-a-chat-model]] 顶部那段 + `docs/ZCode-事件-20260918.md`。
✅ **09-19 拍板：`.env` 那 5 个 key 不换**（这条作废，别再问）。
⚠️ 但**日志证据我删掉了、再也查不了**（卸载前只核了"内容别处也有"，
没核"它是某个没结问题的证据"）；"到底传没传过"**至今没有答案**。

⚠️ **本文件往下那一大段（"09-19 白天"起）是逐日历史流水** ——
**别拿它当"现在还没验"用**（里面好几处写着"真机没验"，而其中多数后来都验到了）。
逐日账归 `singularity/docs/演化史.md`，这里只留"现在怎么样"。

· 🆕🆕 **09-20 07:2x（接着清待办，7 笔；两批都推了、CI 三个 job 全绿）**
  ✅ **最小闭环落地 `949ed80b`** —— 五条刹车，第⑤条是**用户拍板加的护栏**：
  **调度循环没在跑就不自动返工**（回 EXECUTING 只是改 phase，真正派活得靠循环；
  循环没开 = 把项目扔在一个**人看不见**的地方，比停在 GATE3 更糟）。
  ⏳ **真机没验**，且方案自己写着"单测证明不了这套"—— 最关键那条是
  「回到 executing 之后**任务真的被派下去了**」。
  ✅ **路由未判定 `df1ffd29`**（静默 except C 档最后两条）：给"判不出来"一个名字。
  🔴 **门的行为一个字没改** —— 核过代码后**否掉了外派评审"兜底 gate=True"的建议**
  （会让没 `eval.py` 的项目判 gate 失败 → retry/rollback = **假失败**）。
  ✅ **anthropic max-turns `4ea34f79`**：零产出不再报成功。**查清了"没有本来就不该产文件的任务"**；
  ⚠️ 顺带查清 **`anthropic-api` 这个 type 当前不可达** ⇒ 这条**没有真机现场**。
  ✅ **孤儿 ref `a232af0e`**：「什么算孤儿」定了（ref 在 ∧ 任务文件没了），
  `delivery_facts.py --refs` 可数 —— **真机此刻 19 条，一条没清**（清 = 永久删掉还在的产物）。
  🔴 **一条待办被数据否掉**：「GLM 按模型设 cap」治错了病 —— 被掐的 11 条里 **7 条是最快的
  `deepseek-flash`**，且 4 条在 21~62 秒就被掐（被**剩余预算**掐的）⇒ 与模型快慢无关。
  真问题是「**整轮只想不动手**」：审判据 = 正文 0 ∧ 工具 0 ∧ 被掐；而正常的"正文 0"
  **100% 都调了工具**（见 `docs/防御模式.md` §87）。
  ✅ **CI 第一次真跑通了 `audit` 门**（两批 14 笔都绿）—— 门接电这件事从此有第三方证据。

· 🆕🆕 **09-20 02:0x–03:0x（清"不依赖真机"的待办，4 笔）** —— 逐条账在 `~/OPEN.md`。
  ✅ **"产物被丢"坐实了、也修了**（`4f7971ed`）—— **没重跑一轮**，机制全在真机落下的
  四样东西里对出来的（`traces/` · `partial_usage/` · `tasks/` · 项目仓的 pending ref 时间与数字全对得上）。
  根因两处：`_batch_has_facts` 是个 **bool**，而事实分三档（`merge_request` > `changed_files` >
  `token_count`）⇒ **"只花过钱"那档顶掉了"带合并请求"那档**；且 `if deadline_wrapup: return batch`
  **无条件交空壳** ⇒ 产物到不了 `mq.submit`。复盘 `docs/防御模式.md` §86。
  ✅ **`_rejected_think_keys` 也挪到模块级**（`1223e2e4`，同 `tool_choice` 那个形状的第二处）。
  ⚠️ 这处**没有真机现场**（生产没配 thinking 参数，那条路不走）—— 验的是"记在哪"，别当"验过"。
  ✅ **进程身份**（`ab2d4c2c`）：启动日志 + `/api/status` 各报一行 `git` / `dirty_src` /
  `dist_built` / `frontend_stale`（**真机验过**，不是"应该能跑"）。
  🔴🔴 **一条判据被证伪**：「下一轮验**进仓的行数不再是 0**」—— 它**当时就已经不是 0**：
  `merged = 2 任务 / 3 提交 / +478 行`，而那 478 行**全是 README/pyproject 样板**，
  HEAD 的树里**一个产物文件都没有** ⇒ **什么都没修也会绿**。
  **改用**：`not_merged` 落下来 + `merged.files` 出现产物文件（`d211ef5a` 给 `merged` 补了 `files`）。
· 🆕🆕 **09-20 凌晨·真机 `round-20260920b` 跑完** —— 逐条判据在 `~/OPEN.md`。
  ✅ **三条判据全验到**：
  · `0eb76048`（400 修复）—— `reasoning_kept_for_tool_call_without_tools` 响了 **14~16 次**，
    而 `llm_400_unknown.jsonl` **一行没涨**。⚠️ 只看"没 400"分不出"修好了"和"没走到"，
    所以**必须两条一起看**。
  · `8c0c20ee`（`tool_choice` 的"记住"从实例挪到按模型）—— 一个进程里**只响 1 次**（旧行为 7 次）。
  · `68664faa`（超时不再冒充用户取消）—— `cancels/1789829637704.json` = `{"by":"timeout"}`，
    任务终态理由写的是 `执行超时(>900.0s)`，**不是"用户手动取消"**。
  ✅ **09-19 晚那两条也顺带验到**：`_note_integration` 留痕（`lineage` 里有
  `integration_merge`，且它当场报 `{"ok": true, "tests_ran": false}` —— **集成"通过"但没跑测试**）·
  B2 触顶文案（`…（这个数含审查侧自身的超时/异常，不全是模型没改好）`）。
  · `tasks/*.json.corrupt` 至今 **0 个**（健康态，不是"验证过"）。
  🔴🔴 **但这轮交付是空的 —— 这是目前最重的发现**：7 个任务的产物（**5654 行**）
  全躺在 `refs/qidian/pending/` 上，**HEAD 的树里一个产物文件都没有**
  （只有 `.gitignore` / `README.md` / `pyproject.toml`）。
  ⚠️ 更正一句：先前写"合并进项目仓的代码是 0 行"不准确 —— 历史里确有 3 个集成提交、
  +478 行，但**全是样板**（正是这一点让上面那条判据变成了假的）。
  三个任务 + T1 重跑**判词都是「无文件改动 —— 被掐断（deadline）」而锚里有真代码**
  （提交离判死只差 19~105 秒）。✅ **机制已坐实并修掉**（见本节最上面那条 09-20 02:0x）。
  ✅ 对照组成立：T5 真没产出的那次**没有锚** ⇒ 锚不是无脑写的。
  🔵 另拿到 **B5 的第一条真机数据**：`gate3_route` 里 `precise_reset_would_be: []`
  —— 精准重置会重置 **0 个**（全失败 ⇒ 映射为空）⇒ **换成精准做在这里是空操作、必然转圈**。
  ⚠️ 退化样本，一条不够拍板。
· 🆕 **09-20 新增功能**：项目详情多了「**集成实况**」块 —— 进仓几个任务 / **没进仓几个** +
  行数 + 跑没跑测试 + 机械检查（`_api_projects.project_integration`，挂在已有的详情接口上）。
  上一个洞的教训就是"**5654 行没进仓，而页面上一个字都看不出来**"。
· ⚠️ **后端进程的现状要现看，别信这里**（09-20 02:5x 我为了验身份重启过一次，
  跑完把循环又停回去了）：**进程在跑、调度循环是停的**。判它在不在烧钱看
  `usage_daily.json` 的 mtime 和 `scheduler.log` 有没有 dispatch —— **别看端口**
  （09-20 那次重启实测：两次重启期间 0 次 dispatch、`usage_daily.json` 的 mtime 没动）。

· 🆕 **09-19 白天做了 4 笔（都不依赖真机；除注明外都做了变异验证）**：
  `f60ab5b3` `drain_dep_blocked` 改"状态变化才报"（它一条占了 `alerts.jsonl` 的 **80%**，
  3180 条、间隔中位 0.0s —— 而 witness 轮转是"只留最后 1000 行"，**真事故会被挤出去**）·
  `d04022e6` `PUT /api/agents/<层>/<模型>` 对不存在的模型 **500+HTML → 404**，
  同时堵掉"失败却把 `_disabled` 标记写进盘"那个**半动作** ·
  `4316fb5c` `DELETE /api/projects/<id>` **先取消它的任务**（治"废弃项目≠停任务⇒重启捞回来
  接着烧钱"），**任务 JSON 一个不删**（它是锚的唯一线索）· `2097c74a` 外发提示词落盘
  （`docs/外派提示词-20260919.md`）+ 与位置说明的 §0/§7 **逐字同步守卫**。
  另：清了项目根 8 个 0B `.<名字>-worktrees` 空目录。
  ⚠️ 除阵容外**全部真机没验**（**这是 09-19 当时的快照** —— 其中 `68664faa` / `4316fb5c`
  后来在 round b / 09-19 21:59 真机验到了；**别拿这一行当"现在还没验"用**，
  当前状态看本节顶部第九次复核 + `~/OPEN.md` 接手指针）。
· 🆕 **09-19 白天第二批（同日，接着清"不依赖真机的待办"）**：
  `3ed1b1fa` 任务页把「上一轮」的任务和当前批次分开（判据：挂在项目下却不在
  `task_ids` 里；**已 `npm run build`**）· `ded50bd0` 观察者告警改成
  「事实｜为什么｜怎么办」+ 修掉说谎的那句"停滞 10 分钟"（实际阈值 1200s=20 分钟）·
  `76505317` `待验证清单` #33 那句过期的并发假设 ·
  `40fe0e30` **防御模式 §77.3 那条替代判据作废**（实测站不住，见 [[measurement-before-change]]）。
  🔵 **更正**：`check_stalled()` 只有观察者调、**调度器一次都不调** —— 这是 §77.3 的真缺口；
  **09-19 已拍板：先不动**（观察者自己有活性痕迹，加了只是多一份重复告警）。
  ⚠️ 后端 09-19 12:54 起**一直开着**（14:0x 核实：循环在动、`usage_daily.json` 仍停在
  09-18 23:58 ⇒ **没花钱**）。**起后端 = 调度循环自动跟着起**（`web/app.py` 的
  `start_loop(concurrent=2)`）—— **没有"后端跑着但循环没开"这个状态**。
· 🆕 **09-19 白天第三批**（用户说"修完"之后接着做）：
  `18ea5c96` **验收门槛标记**：原来判据是 `qa_saved`（"报告落盘没抛"），而
  `build_qa_report([], [], …)` 空输入照抛不误 ⇒ **QA 什么都没产出时
  `verification_ran` 照样撒**（外派 ③ 说的那道坏刹车）。收紧成"必须有 verdict"。
  `273409b0` **GATE3 打回路由**：两条"没依据"的分支原来挑**相反的代价**
  （无报告→impl 不动架构 / 有报告没标路由→清空架构重规划），统一成 impl。
  `3f9c1514` **架构「历史版本」入口**：数据 09-17 就在
  `.qidian/projects/<id>/history/<文件名>.<n>`，界面上一直没门 —— 新端点
  `GET /api/projects/<id>/history/<文件名>`（纯文本，新→旧）+ 架构抽屉里的链接。
· 🆕 **09-19 晚：外派评审（`docs/外派评审-20260919.md`）的 A1–A10 + B1–B4 全改完**
  （`0f32b692`，22 个文件 / +1055−272）。**每条都配了会红的用例，变异逐个验过。**
  改动横跨审查层（`_review` / `_exec`）、推进块（`orchestrator` / `web/app.py`）、
  融合（`execution_judge`）、观察者批准（`_observer_answer`）、原子写（`_io`）。
  逐条账在 `docs/外派评审-20260919.md` 顶部，**别在这儿抄**。
  🔴 **动的是调度推进块，真机没验** —— `_advance_project` 是搬家（拿 HEAD 逐行对过），
  但 `_auto_trigger_test_fix` 的 per-project 隔离、验收改异步这两处**每 tick 都在跑**，
  下一轮真机重点看这里。
  ⚠️ **顺手作废的**：`_first_speaker` 删了（它和"分歧默认判给发言方"同源，
  构成自我强化闭环）—— 它的两条用例一并删；`auto_mode` 最后那个消费点也没了
  （全仓只剩自己两行，**从来没生效过**）。`make check` 绿 · `pytest tests/` **1740**。
· 🆕 **09-19 晚·第二批（B6 + 静默 except 复核 + 尺子）**：
  `3d089b1b` **`merge.py` 三处"承诺了却没发生"** —— `_park` 落盘失败原来**静默吞**
  （静默 except 清单 09-13 就分好类、一直没做的那条）· `resolve(abort)` 删盘失败裸 `pass` ·
  `_mark_merged` **不看 `transition` 返回值**（任务被取消后合并照样落地 ⇒ git 合了、
  状态留在 FAILED；**没 force 硬改**，只出声）。`c2b75850` `review_files_truncated`
  告警**带上 task_id**（09-17 那个"46 个 trace 里一条都没有、查不出是谁触发的"悬案卡在这）。
  `9bc3fd21` **「返工了几轮」的尺子**：sidecar 每条 dispatch 带 `turn`/`attempt`
  ⇒ 同一 attempt 里 `turn>1` 的条数 = 内层返工；`max(attempt)+1` = 整轮重跑几次。
  ⚠️ **纠正两条错前提**（原文挂在 `~/OPEN.md`）：内层返工上限是
  `config.DEFAULT_MAX_TURNS=2`（**每个任务最多返工 1 轮**），**不是** `_REVIEW_MAX_AUTO_FIX`；
  且 `escalate()` 恒 None（单档制）⇒ 一次 `run()` 只有一个 level ⇒ 内层轮次**本来就在
  trace 的 `validation.turns_used` 里**。
  ✅ **静默 except 那 88 条按形状复核完**（S1/S3/S5 已修成套；C 档 6 条里 4 条已修，
  剩下 2 条**不能照原文建议改** —— 会造出假失败），表在 `docs/静默except待修清单-20260913.md` §六。
  ⚠️ 全部**真机没验**；`pytest tests/` 现在 **1774 绿**。
· ⚠️ **改完代码/前端记得**：后端 `debug=False` **没有热重载**，前端要
  `cd src/singularity/web/frontend && npm run build`（`static/dist` 是 gitignore 的）。
  ⚠️ `npx` 在**仓库根**跑会建一个空的 `node_modules/`（那次报 ERR_MODULE_NOT_FOUND）
  —— 前端命令一律在 `frontend/` 里跑。

· 🔴 **09-19 凌晨修了 8 处，真机全没验**：`ef74ddd` REVIEWING 重入守卫 ·
  `bded9d8` `_start_background` 收 id 不收对象 · `d998c38` `save()` 加"盘上是不是比我新"报警 ·
  `169081e` 重试不再提前释放锚 · `7bdeb34` `_worktrees_dir()` 不再建目录 ·
  `6ca7e8aa` 心跳改原子写 + 损坏不再静默删 · `68664faa` 超时不再冒充用户取消 ·
  `1db42d6d` tracker 读路径留痕 + 未知键不再让整条任务消失。
  ⚠️ **不用为它们单跑一轮** —— 都"顺便就能看到"（点批准时 / 看 `alerts.jsonl` 刷不刷屏 /
  撞上并发）；**后 4 处尤其**跑一轮也不一定看得见，顺带看
  `cancels/*.json` 有没有 `by:timeout`、`tasks/*.json.corrupt` 有没有冒出来即可。
· 🔴 **「整份覆盖写」是一整族**（和下面的观察者那条**同一个根**，已核实 ≥5 类写入者）。
  分三层：第 1、2 层已修（就是上面那两条）、第 3 层（乐观锁 / 字段级合并）**不做**。
  ✅ 观察者那条 **09-17 就修过**（`19096e2`：写回前 `fresh = _proj.load(pid)`）——
  但那只是**修一个写入者**；**它本身仍未真机验**（四次都没撞上那个竞态）。
· ⚠️ **我这轮同一个毛病犯了四次：没核前提就开口。**
  ① 说"闭环被砍" —— 其实 `_run_verification` 是**后加的**（`cd05533`），我**编了段历史**；
  ② 说 `task_retry` 那条"没人记过" —— 代码里写得清清楚楚；
  ③ 要改 `task_retry` 时撞到一条**专门钉住旧行为**的测试（改之前没搜有没有东西钉着）；
  ④ 归档时把观察者那条写成"仍未修" —— 它 09-17 就修了。
  ⇒ **定案靠的是那个测试文件自己前后矛盾**（头说 merged 是唯一，测试却允许重试松手），
  **不是我的论证** —— 这点比结论更该记住。
· 🆕 **09-19 晚：外派那几条拍板并做完了**（原来的"6 条"至此全部收口）——
  `fix_round` 当时只把注释改成实情（"无实现"）—— **09-19 深夜整条删掉了**（它是 06-27
  砍内循环时留下的孤儿：全仓只有 `= 0`、没有 `+= 1`，账本那句"返工 N 轮"永不触发）。
  ⚠️ **别把"删字段"读成"这个数有了"** —— 09-19 晚**已核并补上**（见下面那条
  「返工了几轮」），原话"没落盘"**只对外层成立**，别照旧话做。
  · `_ESCALATION` 经核**是有意设计不是缺陷**
  （两档已合并成单档），真缺口在 `complexity`：提示词说"low→廉价层"，而建任务处硬写
  `route_level="any"` ⇒ **只改了那句骗模型的说明** · `_expand_review_pool` 被闸门打死、
  恒返回 `[]`，docstring 改准（**要多人审只能启用第三个模型**）。
  ⚠️ **这三条是"改说明"，没有可验的行为。**
· 🆕 **09-19 晚另修一条真缺口**：「集成通过」的两条来路分不清（外派 ③）——
  `orchestrator._note_integration` 记 `{action:"integration_merge", tests_ran}` 留痕，
  前端 `gateCopy('3')` 读它，没跑到测试时门上改口。**真机没验。**
  ⚠️ 核的时候发现外派那句"**在 GATE3 面板上长得一样**"不准 —— 实情是这条 detail
  **哪儿都不显示**（面板没"集成"这块 / `issues` 会被 `run_test_fix_loop` 清掉 /
  lineage 里虽有但界面只对 gate2 读）。
· ⚠️ 另有一条**修了半边**：`repo_dir()` 兜底仍返回不存在的路径（`merge.resolve_merge`
  会抛 `FileNotFoundError`）—— 只对"父项目被删过、任务还卡着"的现场成立，
  09-19 已清 10 个孤儿任务 ⇒ **盘上现在没这种现场**，是"没证据"不是"已修"。
· 🔴 **清任务有两条路，差别致命**：走 `rm` 直删任务 JSON ⇒ ref 不动、安全；
  走 `DELETE /api/tasks/<id>` ⇒ `task_delete` **显式 `_release_ref`**、**产物真丢**。
  （09-19 清了 10 个孤儿，走的是 `rm`，实测 14 条 `pending` ref 一条没少。）

· 🔴 **09-18 晚修了 4 条，真机全没验**（判据在 `docs/待验证清单-20260912.md`）：
  `164429f` **阶段产出覆盖前先归档上一版**（挂在那唯一的覆盖点上 ⇒ 架构/调研/测试计划全覆盖）·
  `4e3718f` **锚定 ref 只在"真合进项目仓"时松手** —— 原来 6 处释放、**4 处东西根本没进仓**
  （超时/worker异常/冲突/合并失败）；判据是系统自己写的："ref 还在 = 有可打捞的产物" ⇒
  **释放 = 断言"产物已进仓"**·
  `d07414b` **`lazy_spoke_import_failed` 的根是 importlib 在探 dunder**（不是竞态）·
  `aecb82f` **空壳 BatchOutput 不许顶掉真结果**（用户拍板走"修伤害面"，不猜阈值）。
· ⚠️ **同一晚改掉三条错的旧结论**（`OPEN.md` 里都更正了，**别再照旧话做**）：
  ① `lazy_spoke` 判"并发竞态"❌（实为**同一线程重入**，那把 RLock 从原理上挡不住）·
  ② "清任务前先导出 `salvageable_refs()`"❌（SHA 抄进文件**不防 gc**，锚一删就没人引用）·
  ③ "cap=6s 注定空"❌（推断、没数据；真伤害是**丢账**，不是那 6 秒）。
· 🔴 **240 秒是「单次调用」的硬顶**（`openai_agent.py:1070`，`min(240, 剩余预算)`）——
  **跟任务 deadline 无关**。今天 62 次被掐里 **20 次撞在这硬顶上、正文全 0 字**。
  ⇒ **"不够"还是"打转"还没答**（尺子 09-18 凌晨才造好：分诊账拆了正文/思考）。**量出来之前不动 cap。**
· 🔴 **`d664f5a`（被掐断时思考不许当正文）真机没验**；观察者那个"汇报写回覆盖人的批准"
  **修了、但四次都没撞上竞态**。两条判据都在 `docs/待验证清单-20260912.md`。
· 🔴 **`4e3718f` 的副作用**：`refs/qidian/pending/*` 会**比以前攒得多**（超时/冲突/异常都留锚）。
  这是**有意的**，但它和「清理孤儿 pending ref」那条 🔴 是**同一个结** —— 先定"什么算孤儿"。
· 🆕 `docs/防御模式.md` 补到 **§81**（§79 掐断被算到模型头上 · §80 空输入返回了别人的东西 ·
  **§81 惰性 `__getattr__` 把 importlib 的 dunder 探测当成了请求**）。
· 🆕 **告警账本的读数要先问"这行是谁写的"**：跑测试会往真 `.qidian/alerts.jsonl` 写
  （收集期在任何 fixture 之前）—— 已修（conftest 导入期隔离）。⚠️ 当天那 16 行是我自己跑的，
  已剔掉，备份 `/tmp/alerts.jsonl.bak-212034`；**别再拿它当后端行为**。

**阵容现状（09-19 白天经 API 改过、读盘复核）**：激活池 = `deepseek-flash` + **`glm-5.3-flash`**；
停用 = `kimi-k3` / `deepseek-v4-pro` / `glm-5.2` / `glm-5.3`。
阶段：`executing`=`[deepseek-flash]` · **`reviewing`=`[glm-5.3-flash]`**（09-19 白天把写手
`deepseek-flash` 摘掉了 —— 它每次审都要被剔一次、还报一条警告；名单以前在说谎）·
`extract`=`[glm-5.3-flash]`。
🔴 **但审查实际就是只剩一家** —— 这不是修好了，是**写实**了。要多人审只能启用第三个模型
（"扩池"那条路已死，见 `_review.py` docstring）。
⚠️ **"GLM 账号没钱"那个前提至少现在不成立**（`glm-5.3-flash` 09-18 凌晨真调成功过）。
⚠️ **`glm-5.3-flash` 比 `deepseek-flash` 慢 2.5~3 倍，而 cascade 的落点正是它** —— 待拍板。

---

