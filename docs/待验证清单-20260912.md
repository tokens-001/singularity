# 待验证清单（2026-09-12 一夜的改动）

> **这份文件的存在理由**：2026-09-12 那一夜改了 16 笔提交、测试 862 → 1030，
> 但**绝大多数改动只在桩测试里绿过，没在真实运行里跑过**。
> 而那一夜最大的教训恰恰是：**测试绿 ≠ 对** —— 探路2 五个任务代码全绿、
> 模型也把活干完了，照样一个都没交付。
>
> **每一条都写清"怎么算验过了"。** 验完就把 `[ ]` 划掉并写上日期。
>
> **标记口径**（别混）：`✅` 验过了 · `❌` 验了、**没过** · `◐` **只验了一半**（写明是哪半） ·
> `—` **本轮未触发**（写明为什么，**不等于验过**）。
>
> **数法（2026-09-13 补 —— 不写下来就会重蹈覆辙）**：合计 = **跳过两处**之后逐行数。
> ① **跳过第三节**（"已经在真实数据上验过的，不用重复验"那 6 行）；
> ② **跳过 `➕` 行**（自标"不是清单项"）。
> ⇒ 45 行 − 6 − 2 = **37**。⚠️ **不按这个数法会得到 43/45，看上去像又漂了** ——
> 09-12 那晚为此数错过四次。另外**第三节那 6 行的写法跟别的行不一样**（说明写进了标记格），
> 机械数也会漏掉它们。
>
> **验收轮 1：2026-09-12 夜 · 探路3 基线轮 · 项目 `1789160560999`** ——
> 结果见各表。⚠️ 它**不是一次干净的复跑**：3 个任务全没交付，**但三种死法都不是"模型不会做"** ——
> T1 `执行超时(>900s)`（产物在：改了 2 文件 + 提交 `c8bca33`，只是没及输出总结）、
> T2 `worker 异常: JSONDecodeError: Unterminated string`（模型吐的 JSON 被截断 ⇒ 两 agent 全挂 ⇒ 0 产物）、
> T3 `QA:fail: tests failed: 1 failures`（活干完了：101 秒 / 4 轮 / 10 万 token、约 100 个用例，**只挂 1 个**）。
> 所以标 `—` 的条目**别当成通过**。
>
> **验收轮 2：2026-09-12 夜 · 项目 `1789223754637`（"验收轮2-txtstat"）** ——
> 同一份 `txtstat` 需求、`flow_weight=auto` ⇒ 同样重流程。**后端在开跑前重启过**
> （22:27 用 `.venv` 起，带上截至 19:52 的全部改动）。
> 结果比轮 1 好得多：**8002 交付**（`txtstat.py` + `tests/test_txtstat.py`，机械检查 **7/7 全过**）、
> 8003 撞 900s 超时。**注意**：8003 的超时性质跟轮 1 的 T1 **不同** ——
> 轮 1 是"活干完了没及收尾"，这次是"第一次模型调用就没返回"。
> **标记口径不变**：`✅` 验过 · `❌` 验了没过 · `◐` 半验 · `—` 未触发。
>
> **验收轮 3（09-13 凌晨 · 项目 `1789226826934`）—— 只为验 §60 根因修复而跑**：
> 同需求、同重流程，**唯一变量是后端重启后带上了修复**。结果：**2 成 3 败**
> （8382/8387 done；8384/8386 撞 900s；8385 `阻断: abort`），项目退回 GATE2，**用户拍板停在这儿不批**。
> ⚠️ **它达成目的了**：§60 根因修复**真机验过**（`constraints_checklist` 0 → **9**，
> 兜底告警零新增），见第三节那条。
> 🔴 **但这一轮抖出一个更大的问题**：**900s 超时不是偶发** ——
> 今夜 2 轮 7 个任务**撞了 3 个（43%）**。⚠️ 当时写成"**两种完全不同的形状**
> （第一个调用卡死 vs 活儿比 900s 多）"—— **这个分法是错的，09-13 复查推翻了**：
> 只有一种，**执行器一直干到被杀**。根因与规则见 `docs/防御模式.md` **§63**。
>
> **🔍 "900s 花在哪"（轮 1 T1 遗留）—— 09-13 复查结案，⚠️ 但结论跟当时写下的相反。**
> 当时据 8003 的 `turns: 0` 推断"**900s 卡在第一次模型调用上**"。**这个推断是错的**：
> `turns: 0` 在超时任务上**分不出**"真没调"和"没落盘"（两件事数据上长得一样）——
> 而当时那句"能确定"恰恰**把上报口径的缺失当了事实**。
> 复查（逐条从盘上读的）：
> · 8003 `depends_on=[8002]`，快照 `1789225670` → 判死 `1789226581` ⇒ **实打实占了 worker 911 秒**；
> · 引擎**本身有界**（流停滞 90s / 单次尝试 240s / 单次调用重试总预算 600s）
>   ⇒ **一次卡死凑不满 900s**；
> · 真正的结构事实：**执行器的 turn 循环没有总时限检查**，唯一上限就是外面那次无声收割。
> ⇒ 所以 `8003`（0 轮无账）和 `8384/8386`（4-5 轮、改了 2 文件）**不是两种毛病，是同一件事的两端**：
> 都是执行器一直干到被杀，差别只在杀之前落了多少盘。
> **全过程与规则** → `docs/防御模式.md` **§63**；**改法与待验** → 下面 **§三·七**。

---

## 一、一次干净复跑就能验的（优先级最高）

跑一轮新的探路轮（参考 `docs/P1-三臂实验方案-20260912.md` 里的任务选型：
单文件 CLI、有约束、规模小）。

> **验收轮：探路3 基线轮** —— 项目 `1789160560999`，2026-09-12 05:02 立项 → 05:51 进 GATE3。
> 复用探路2 的 `txtstat` 需求；`flow_weight=auto` ⇒ 走了**重流程**（调研 + 委员会，2 席）。
> 结果：**3 个任务全未交付**（T1 撞 900s 超时、T2 无产出、T3 `delivered_unverified`）。

| | 改了什么 | 结果（核于 2026-09-12 夜 · 探路3 基线轮 · `1789160560999`） |
|---|---|---|
| ✅ | **架构 prompt 要求 `check` 出 argv 结构 + `covers` 声明** | **轮 1 ◐（`check` 8/8、`covers` 0/8）→ 轮 2 ✅（`covers` 9/9）**。<br>轮 1 的根因诊断（**不在 prompt**，在委员会定稿那份 schema `execution_judge.py:205 _ARCH_SCHEMA` 少了 `covers`）**被后续证实是对的** —— 修的就是那份 schema。<br>⚠️ **跟"三·六"那条是同一件事的两面**：这条问"prompt 要求了没有"，那条问"定稿 schema 兑现了没有"。**修的是后者**（只改 prompt 没用，证实了轮 1 的判断）。<br>⚠️ 轮 2 的 `check` **7/9 结构化**、另 2 条模型自觉写散文（详见三·六那条）。 |
| ✅ | **机械检查在验收时真跑**（`_run_verification` → `machine_checks.json`） | **轮 1 ❌（没跑）→ 轮 2 ✅（跑了、全过）**。<br>轮 1：`1789160560999.machine-checks.json` 不存在，issues 只有 `verification_skipped`。<br>**轮 2（`1789223754637`）：文件存在，7 条可机器跑的约束 `ran:true/passed:true/exit:0` 全过**。<br>⚠️ **但根因当时还没修** —— 轮 2 能跑是**兜底**（`effective_constraints()`）挡住的。根因（"建任务那条路不写清单"）在轮 2 之后才定位并修复（`9d38212`），**它的验证信号是 `constraints_checklist_fallback` 告警消失**，不是"机械检查跑起来了"。详见 `docs/防御模式.md` §60 |
| ✅ | **需求侧覆盖率**（`requirement_coverage`） | lineage 有 `action:"requirement_coverage"`（05:14:36，`{total:8, covered:0, uncovered:[0..7]}`），issues 有 `requirement_uncovered`。**机制是通的**；`covered:0` 是上面 `covers` 缺字段造成，不是机制坏 |
| — | **检索后改写**（论文那步） | **本轮未触发**：`fix_round=0` / `review_failures=0`，deep 条件不满足（首轮本来就不会触发） |
| ✅ | **`run-phase` 不再谎报** | 对 `executing` 项目分别请求 `template` / `gate1` → **两条都回 `409` + `ok:false, started:false`**（原先是 `{"ok":true,"started":true}`） |
| ✅ | **批准 GATE1 后自动踢起 planning** | 走 API 批准 GATE1（05:04:37）→ 回 `started_phase:"planning"`，20 秒后 phase **确实**变 `planning` —— 不是"嘴上说动了手" |
| ◐ | **防御模式找茬清单进审查 prompt** | **静态确认接线在**：`validator.py:370`（`crossover_review`）与 `:504`（`multi_model_review`）都调 `_defense_checklist`。**运行时未证** —— `checklist()` 挑不中返回 `""`（设计如此），从产物里看不出那次到底注入没有 |

## 二、要靠特定场景才触发的

| | 改了什么 | 触发条件 | 结果（核于 2026-09-12 夜 · 探路3 基线轮） |
|---|---|---|---|
| ✅ | **DSML 解析** | 模型吐 `<\|DSML\|` 格式时 | **判据两半都满足**了：<br>① 告警出现，且**接的就是 `write_file`**（`xml_tool_calls_recovered:1:write_file`，14:01:52）—— 这正是清单里说"从没验过"的那半边<br>② 那次所在的**任务 trace 可用且 `changed_files` 非空**（窗口落在 AB-B 的 `1789192758034` 或 `…035`；两者分别是 `['tests/']` / `['sample.txt']`，**都非空**）<br>⚠️ 只能说"落在它俩之一的窗口里"，**没细到具体是哪一个** —— 告警本身不带任务号。<br>**这是 5 次触发里第一次能归因成功的**（前 4 次分别落在：被取消的任务 ×2、已删的僵尸项目 ×1、被重启杀掉的进程 ×1）。 |
| ✅ | **§55 trace 侧**（看见已提交的改动） | 任务撞 900s 超时 | T1（`1789161332044`）trace：`changed_files` = **`txtstat/output.py` + `txtstat/stats.py`**（不是 `__pycache__`）；`agent_output` 含 **"另有 1 个提交未合并（c8bca33）"** —— **两半都满足** |
| ✅ | **§55 记忆侧**（超时任务进记忆） | 同上 | `events.json["1789161332044"].trajectory` = 那句超时说明（57 字，**非空**，原先恒 0）；`attrs.files` 也带两个真文件 |
| — | **`_llm_judge_causal` 修复** | 整合器跑到第 10 次（`%10` 节流） | **本轮未触发**（没跑够 10 次）—— 仍**从来没成功过一次**，还是最该盯的一条 |
| ◐ | **经验分层抽象** | 同上（`%10` 节流） | **轮 1 判 ❌（判据本身有问题）→ 已修 → 轮 2 仍未触发。**<br>轮 1 的证据与根因：三个失败任务 `attrs.abstraction` 全 `None`；`backfill_abstractions` 挂在 `_consolidate_calls % 10 == 0`，而那是**模块级变量（`_memory_consolidator.py:15`）、每次重启归零** ⇒ 进程内任务数 < 10 就**永远不跑**。<br>**修法**（`6d11375`）：判据**落盘** + 1h 时间兜底。<br>**轮 2 实测**：`attrs.abstraction` **仍然全 `None`**，但这次**原因不同** —— `.qidian/memory/consolidate_state.json` = `{"calls":1,"last_heavy":22:39}`，`calls` 没到 10、1h 兜底（23:39）又晚于 GATE3（23:24）⇒ **没轮到**，不是判据坏。<br>⚠️ **仍没观测到"判据真跑起来"那一次** —— 要再跑一轮，或者让后端再挂 1 小时。 |
| — | **预算门** | 项目花费到预算 80% / 100% | **本轮未触发**（本项目花费远低于 5.0 的预算） |
| — | **模型纪律表自动累积** | 任务收尾且架构给了 `estimated_files` | **本轮未触发** —— 本轮的架构**没给** `estimated_files`，所以量不出来不记是**对的**，不是 bug |

## 三、已经在真实数据上验过的（不用重复验）

> ⚠️ **本节的"验过"一律是"函数/单元级"** —— 拿真实数据**离线调那个函数**，证明它算得对。
> **它不证明链路上会调用它**。两者是不同的命题，别把本节当成"这条路已经通了"。
> 反例就在眼前：`abstract_trajectory` 在这里是 ✅，但在线自动触发**一次都没跑过**（见第二节）。

| | 验的方式 |
|---|---|
| ✅ **§55 trace 侧**（"看得见已提交的改动"） | 拿 T2 那个真 worktree 跑：旧逻辑只捞到 `__pycache__`（1 个）→ 新逻辑捞到 4 个含 `txtstat.py` + `new_commits` |
| ✅ **经验分层抽象**（`abstract_trajectory`）<br>⚠️ **只验了函数，没验接线** | 拿探路2 T3 真轨迹**离线**跑：9599 → 328 字（3.4%），三层各司其职。<br>**但这不等于链路上会跑它** —— 见第二节那条：在线自动触发**从没发生过**（`%10` 计数器重启归零）。<br>**"函数对"和"接线通"是两件事，本节全是前者。** |
| ✅ **检索后改写**（`adapt_experience`） | 拿 T4 那条真经验改写 → 产出是针对任务的计划（3/3 成功） |
| ✅ **需求侧覆盖率**（函数本身） | 拿探路2 真架构 + 真需求算：5 条需求 / 覆盖 4 / 可机器跑覆盖 0 / `uncovered=[4]` |
| ✅ **埋点：`trajectory` + `tool_seq` 存储** | T4/T5 真机存进去了（2610 / 16000 字，16000 = 上限截断） |
| ✅ **删任务不动记忆** | 清掉探路2 的 5 个任务后，`events.json` 里那 5 条**还在** |

---

## 三·五、09-12 **晚**又改的一批（同一次探路轮一起验）

> 上面那份是"那一夜"的 17 笔。晚上又落了 5 笔（`45c2680`…`9a0b59d`），
> **同样只有桩测试绿过**。下一轮探路轮顺手就能验，别另开一轮。

| | 改了什么 | 结果（核于 2026-09-12 夜 · 探路3 基线轮） |
|---|---|---|
| ✅ | **流程重量分级**（`project.resolve_flow`） | **两半都验了**：<br>· heavy（`1789160560999`，`auto`）：`flow_weight = {weight:"heavy", source:"auto", research:true, committee:true, reason:"自动: product_dev 模板"}`，轨迹走了 `template→researching→gate1`<br>· **light（`1789192127267`，用户指定）：`flow_weight = {weight:"light", source:"user", research:false, committee:false, reason:"用户指定轻量"}`，轨迹 `template→planning` —— 直接跳过 `researching`**<br>**两半都带 `reason` ⇒ 没犯 §44（跳过必须是有理由的显式事实）** |
| ✅ | **轻量项目架构不开委员会** | **两边凑成完整对照**：<br>· heavy ⇒ `committee_fusion` **非空**（2 席 `glm-5.3-flash` + `deepseek-flash`）⇒ "不是从来没开过"这条前提成立<br>· **light ⇒ `committee_fusion` = `None` ✅**<br>⇒ 关掉委员会这件事**真的生效了**，不是"因为从来没开过所以是 None" |
| ➕ | **顺带证实了 `docs/防御模式.md` §61**（不是清单项，但这是它的可证伪预言） | §61 说"`covers` 丢是因为**委员会定稿用的是另一份没有 covers 的 schema**"，预言：**轻流程免委员会、走席位 prompt ⇒ `covers` 应该正常**。<br>实测：heavy 架构 `covers` **0/8**；**light 架构 `covers` `4/5`** ⇒ **预言成立，根因坐实**。<br>⚠️ 但要如实说：轻流程也只有 4/5 —— **席位 prompt 也不是 100% 兑现**，只是从"全丢"变成"基本有"。 |
| ➕ | **§60 在轻流程下同样复现** | `constraints_checklist` **长度仍为 0**（架构里 5 条约束），跟 heavy 那轮一样 ⇒ 印证 §60 的判断：**这个 bug 跟流程轻重无关，在执行层**。 |
| — | **建议器**（创建时的提示条） | **本轮未触发** —— 触发条件要"**很短、不含调研/架构/方案字眼**"的描述，而 `txtstat` 那条又长又含"实现/工具"，返回 `suggested_flow: null` 是**符合预期**的，不算失败 |
| — | **observer 的 GATE3 验收汇总** | **本轮未验** —— 它要"**在对话页问一句**"才会生成 `observer_rollup.json`，本轮全程走 API、没在界面对话，所以没触发 |
| ✅ | **一次多动作埋点** | T3（`1789161332049`）trace：`tool_batches = {turns:4, per_turn:[1,1,1,1], total_calls:4, batched_turns:0}` —— **字段在、`turns > 0`，埋点落地了**。<br>⚠️ 附带一个**基线事实**：不开开关时**每轮只发 1 个工具调用、从不合并**（4 轮全 1）。<br>⚠️ 另：T1/T2 的 `turns` 是 0，**不是埋点坏**，是超时路径拿不到 `tool_events`（见 `docs/防御模式.md` §59） |
| ✅ | **`start.sh` 真能起** | `pkill -f 'python.*singularity.web.app'` 后 `sh start.sh -d` → **一次起在 5050**，`/api/projects` 回 `200 application/json`（不再 `ModuleNotFoundError`） |
| ◐ | **模板下拉 7 个都能建** | **后端 7/7 全 HTTP 200**（`product_dev` / `bug_fix` / `refactor` / `agent_dev` / `feature` / `test` / `review`，建完即删、已清场）。**前端下拉本身没验**（要开浏览器点） |

### 三·六、09-12 晚**新改的**（§61 修复 + §62 扩池 + §60 探针）

> 这几条是轮 1 之后新落的。**下面已按验收轮 2（`1789223754637`）的真机结果逐行更新** ——
> 标 `✅`/`◐` 的都写明了依据（哪个文件、哪条 trace、哪条告警）；仍然 `—` 的写明了为什么没轮到。

| | 改了什么 | 结果（核于 2026-09-12 夜 · **只做了静态/单元级核对**） |
|---|---|---|
| ✅ | **§61 定稿 schema 补 `covers`**（`6110b13`） | **轮 2 真机验过（`1789223754637`）**：架构 9 条约束，`covers` **9/9 全有**（轮 1 是 **0/8**）；`check` **7/9 结构化** `{"argv":[...],"expect_exit":0}`。<br>⚠️ **没到 9/9 的那 2 条要如实说**：模型自觉写成了散文（"流式 O(1) 内存"、"零第三方依赖"），理由是"argv 命令断言不了"。诚实，但其中**"零第三方依赖"其实可机械验**（`ast` 扫 import 块）—— 属于模型**没尽力**，不是机制不允许。<br>⚠️ `covers` 的**语义**也对，不是敷衍填满：约束 0（pytest 全绿）覆盖需求 0–7 全部，约束 1（文件存在）只覆盖 6、7。 |
| ✅ | **§62 扩池 + 换提取员** | **轮 2 真机验过（`1789223754637`）**，三条间接证据都对上了：<br>① `committee_fusion.models = ["glm-5.3-flash","deepseek-flash"]` —— **委员会里没有 glm-5.2**，"提取员不在委员会里"成立<br>② 今晚 `alerts.jsonl` 里 `extractor_stays_member` **0 次**（轮 2 全程无）<br>③ **`extractor_swapped` 今晚 0 次**（最近一次 09-11 00:43）—— 不再需要换人了<br>④ **`empty_content` / `fusion_v2_empty_extract` 今晚 0 次**（最近一次 09-10 / 09-11）⇒ **那个被点名的风险没发生**<br>⚠️ **间接证据的边界**：没有直接抓到"这一轮提取步跑的就是 glm-5.2"那条记录 —— 上面是"没有出坏结果 + 委员会名单里没有它"，**不等于**"提取步确实用 glm-5.2 跑过"。要坐实得再抓一次提取步的模型名。 |
| — | **§60 探针**（`55efa14`） | **轮 2 仍然没落**（lineage 里 0 条 `probe_constraints_checklist`）—— 但这次**知道为什么了**：探针挂在 `_run_execution` 里，而这条路径**根本不执行 `_run_execution`**（根因见下条）。<br>⇒ 语义变了：**探针不落 ≠ 没数据，是探针所在的那个函数没跑**。留在那儿仍然有用（走 `run-phase` 手动那条路时才会落）。 |
| ✅ | **§60 本体：约束清单兜底**（`effective_constraints()`） | **轮 2 真机验过（`1789223754637`）—— 症状端通了**：<br>`.qidian/projects/1789223754637.machine-checks.json` **存在**（轮 1 是不存在）；<br>**7 条可机器跑的约束 `ran:true / passed:true / exit:0` 全过**，其中 pytest 那条真跑了 **33 passed in 0.59s**。<br>⚠️ **这不是根因修复的功劳** —— 是**兜底**把症状挡住了（清单空但架构里有 9 条 ⇒ 用架构那份）。兜底工作正常、且**按设计出声**（`constraints_checklist_fallback`）。<br>🔻 **根因另见 `docs/防御模式.md` §60 的"根因已定位"段**：不是被覆盖，是**建任务的那条路从来没写过清单**。<br>**✅ 修复已在轮 3 真机验过**（`1789226826934`）：`constraints_checklist` = **9**（架构里 9 条），且 `constraints_checklist_fallback` 告警**零新增**（最晚一条早于轮 3 开跑）⇒ **写入生效，不是兜底现算**。 |
| ✅ | **§59 超时路径三半修了**（账 / 收尾 / tool_events） | 🔻 **2026-09-13 更正**：本行原先写"**两次形状不同 / 两种超时**"—— **那个分法是错的**（复查推翻，见文件头那段和 `docs/防御模式.md` §63）：只有**一种**，都是执行器一直干到被杀，差别只在杀之前落了多少盘。下面三半的结论**不受影响**。<br><br>**轮 2 + 轮 3 各撞一次超时，合起来把三半说清楚**：<br><br>**① 收尾 ✅**：`collect_changes: [Errno 2]` 两轮均 **0 次**（最后一次是 09-12 13:59）。现有的 `collect_changes` 全是 §55 那批**故意加的出声告警** `no_baseline_ref`。<br><br>**② tool_events ✅**：普通任务 `per_turn:[2,1,1,1]`；**超时任务也有值**了 —— 轮 3 的 8384 是 `{turns:4, per_turn:[4,5,2,1], total_calls:12, batched_turns:3}`（原先超时任务恒 0 ⇒ 那次修复要的正是"在最该看的任务上能算"）。<br><br>**③ 账 ✅（但边界要写清）**：轮 3 的 8384 **记上了** —— `token_count = 156219`，`agent_output` 写明"已知花费 156219 token（**下界，最后那次调用未计**）"。<br>⚠️ **边界**：轮 2 的 8003 `token_count: null`（没 sidecar）。⚠️ **这里原先说"是两种超时"—— 该说法已于 09-13 更正**：两者**不是两种毛病**，是同一件事的两端（都干到被杀，差别只在杀之前落了多少盘）。**看账这件事本身**的差别仍然成立：<br>· **落过盘的**（8384：4 轮 / 12 次工具调用）⇒ **有账**（修复要的就是这种）<br>· **一圈都没落过的**（8003：`turns:0`，而 `turns:0` 分不出"真没调"和"没落盘"）⇒ `null`，**别拿它当"没调用过"的证据**<br>⚠️ 所以做成本口径时按"**可能整条为 0**"算，别按"至少有下界"算。 |
| ✅ | **§55 同形状又 3 处**（`2af70d4`，executor 采集改动文件） | **轮 2 真机验过（`1789223754637`）**：8002 交付任务的 trace 里 `changed_files = ['txtstat.py','tests/test_txtstat.py']`（**非空、是真文件**，不是 `__pycache__`），`final_status: delivered`，`validation.verdict = 通过`。<br>⇒ **没有出现"改了文件却报审查/QA 被跳过"** 那种反常。<br>⚠️ 另两处**降级出声**也如实生效了：今晚 20 条 `collect_changes:no_baseline_ref（判据不完整）` + `（跟 HEAD 比，已提交的看不见）` —— 这正是"拿不到基线时出声、不再静默"的设计。<br>⚠️ 但**别把告警当通过**：那两处**仍然**退化成跟 HEAD 比，只是不再沉默。 |
| — | **「经验分层抽象」判据**（`6d11375`） | **轮 2 未触发**（`attrs.abstraction` 三个任务全是 `None`）—— 但这次**不是判据坏**：`consolidate_state.json` 里 `calls:1`（没到 10），1h 时间兜底要等 `last_heavy + 1h`，而本轮结束时还没到（22:39 + 1h = 23:39，GATE3 在 23:24）。<br>⇒ 判据**确实换成落盘的了**（见下条），只是**这一轮没轮到**。 |
| ◐ | **重活判据落盘**（同 `6d11375`） | **一半验到了**：`.qidian/memory/consolidate_state.json` **存在且被写**（`{"calls":1,"last_heavy":22:39}`，晚于 22:27 那次重启）⇒ **确实落在盘上、不在进程内**了。<br>⚠️ **还差的另一半**：要"重启一次再看它还在不在"才算坐实 —— 本轮期间没有第二次重启，所以**"跨重启不清零"仍未直接观测到**。 |
| ✅ | **web/app.py 不在 import 期建真目录**（同 `2af70d4`） | 验过了：清掉真 `.qidian/partial_usage` → 跑全量 → **不再出现**；真 `audit.log` 的 mtime/大小没变（137389 @14:09）。另单独验过懒初始化**真会写**（`QIDIAN_DIR` 指 tmp 后调 `audit_log`，日志落在 tmp）。<br>⚠️ 同形状还剩一个没动：读 `secret_key` 也在 import 期（生产上那文件早就在、只读不写，无实际危害）—— 如实记着，不一刀切。 |

---

### 三·七、09-13 改的（执行器自查总预算 —— 治 900s 那句"跑到被砍"）

> **它为什么存在**：轮 2 + 轮 3 七个任务撞了三个 900s，死法全都是"一直干到被杀"。
> 复查（09-13）发现根因不是哪个模型卡住，是**执行器的 turn 循环一圈表都不看**
> —— 唯一上限是 orchestrator 那句硬编码的 `deadline = 900`。全过程见
> `docs/防御模式.md` **§63**。
>
> ⚠️ **本轮（09-13）改动的性质**：单测绿、变异验证过，但**一次真机都没跑**。
> 别按"已解决"算。

| | 改了什么 | 怎么算验过了 |
|---|---|---|
| ✅ | **执行器提前自己收尾**（`config.TASK_DEADLINE_S` 单一来源；`QIDIAN_EXEC_BUDGET` 可覆盖） | **验证开关就是为这条留的**：`QIDIAN_EXEC_BUDGET=60` 起后端，跑一个**会超过 60s 的小任务**（设计得让它多转几轮），然后看四样：<br>① trace 里 `final_status` 是失败但**不是** `执行超时(>900s)`（说明是自己收的，不是被砍的）；<br>② `agent_output` / `error` 里出现"到达执行预算 …s，主动收尾"；<br>③ **`token_count` 不是 `null`**、`tool_batches.turns > 0`（**这是改动的全部意义** —— 以前被砍 = 什么都没留下）；<br>④ `changed_files` 和实际改动对得上。<br>⚠️ 另要确认**没退化成新毛病**：正常任务（预算没到）**行为一字不变**，`turns`/`final_status` 跟改之前一样。 |
| ✅ | **收尾不被升级/重试**（`_exec` 见 `error_kind="deadline"` 不换模型；`_run_with_retry` 见 `deadline_wrapup` 不重试） | 同一轮里看 `perf_samples.json` / trace：**这个任务只跑了一次**（`attempts` 不 ≥2），不是"换了个模型再烧一遍"。<br>⚠️ 这条**离线测过**（`test_wrapup_is_not_retried`，变异验证过），真机要验的是"在真实那条链上确实没重试"。 |
| ◐ | **单次调用封顶在剩余预算内**（`_stream_call` 的 httpx timeout 取 `min(240, 剩余)`） | **这条最难单独验**，因为它的作用是"兜住一轮跑太久那种" —— 要有一次**单轮就跨过预算**的调用才看得到。判据：撞预算那个任务里，最后一次调用的耗时**不超过剩余预算**（`perf_samples` 的 execute 耗时可对）。<br>⚠️ **拿不到就说"没触发"，别记成通过。** |
| [ ] | **§59 那个边界：两种"没账"从此分得开**（`57d2a17`，2026-09-13） | **以前**：`token_count=None` 一律报"一次都没落盘"，分不出"没进过 dispatch"和"进了没落账"。**现在**：dispatch **开头**落 `{"started_at": …}`（`_persist_partial_usage` 累加时要带着它写回去，否则一次正常落盘就抹了），收尾按 sidecar 在不在分两种说法。<br>**怎么算验过了**：要**等一次真 900s 超时**（不好主动造），看那个超时任务的 trace —— ① 有 sidecar ⇒ 报"进了 dispatch 但没落账" ② 没 sidecar ⇒ 报"压根没进过 dispatch"。<br>⚠️ **token 的下界语义没变**，这个标记只回答"进没进过"。 |
| [ ] | **异常收尾留"已知事实"**（worker 异常 ＋ 取消路径，2026-09-13） | 数出来的三个缺口修了两个。验法：**等一次真的 worker 异常**（不好主动造），看 trace 里有没有 `changed_files`/`token_count`/`tool_batches`；取消那条更好造 —— **点一次取消**，看 trace 的 `tool_batches.turns` **不是 0**（修复前恒 0，因为 `dispatch_result=None` 时 `tool_events` 进不去）。<br>⚠️ 修复前现象：worker 异常那条 `_save_trace(..., None, None, False)`，**盘上明明有 sidecar 也不读**。 |
| — | **内存三处补 import**（`auto_maintain`/`system2_extract`/`add_inferred_causal_edge`，2026-09-13） | **09-13 本轮未触发**（写在文末"❌ 但有一条查完确认…"那段）：`_heavy_due()=False`、25 个候选里高置信 0 个 ⇒ 重活那支没跑，`consolidate:` 零告警**不能当验过**。<br>**怎么算验过了**：跑一轮，看 ① `alerts.jsonl` 里还有没有 `consolidate:` ② **`_memory_graph` 的因果边有没有真的多起来**（后者才是"功能活了"的证据，前者只证明"没炸"）。触发要 **≥1h ＋ 那一轮有任务收尾**。 |
| ✅ | **告警按 key 聚合**（`alert_summary`，2026-09-13） | **09-13 端到端真机验过**：`/api/status` 真的返回 `alert_summary`，**15 组**、`chronic` 标记齐全。关键那条 —— `collect_changes` **同时带 `scopes=['claude_cli','oa_exec']`** ⇒ **按 key 跨 scope 归并**在真机上生效（带 scope 会被劈成两行、每行都不够"常驻"）。<br>⚠️ **验的是后端数据**；**前端那两栏还没做**（界面没分栏）。 |
| — | **`claude_cli` 执行器** | **这次没动它** —— 撞 900s 的全是 `oa_exec`（`openai_agent`）。它有没有同样"不看表"的毛病，**没查**。验的时候如果任务走了 `claude-cli` 这条路，别把结果套到这条改动上。 |

**✅ 2026-09-13 凌晨真机验过（`QIDIAN_EXEC_BUDGET=15`，任务 `1789241960646`）：**

- **收尾生效**：`executor.run` **只出现一次**（03:39:29 → 03:40:11，42.5s）。
  判断"不重试"的判据就是"只跑一次" —— ⚠️ **前一次（预算 60）判不出来**：
  那次收尾正好落在 `max_retries=3` 允许的**最后一次**尝试上，"之后没再起"
  两种解释都成立（守卫生效 / 次数用完）。**预算压到 15 秒才逼得出"第一次就收尾"。**
- **账和事实都落了**（trace `1789241960646.json`）：
  `token_count=13098`（不是 null）· `changed_files=["txtstat.py","test_txtstat.py"]`
  · `tool_batches={turns:2, per_turn:[2,1], total_calls:3}`
  · `unverified="执行器到达总预算主动收尾: 到达执行预算 15s，主动收尾（工具轮 6 次，改动 2 个文件）"`
- **单次调用封顶 ◐**：只能间接看出（那次 dispatch 用了 42.2s 而预算 15s ——
  是"每次调用被剩余预算截断 + `_api_call` 自己重试"的合成结果），
  **没单独抓到"某一次调用被截断"的那一下**。别记成 ✅。

⚠️ **顺带纠一个我自己的误判**：`QIDIAN_EXEC_BUDGET=60` **那次没触发过收尾** ——
每次 dispatch 只花 29~47 秒（模型比 60 秒快），执行器每次都是正常答完的。
**预算按"每次 dispatch 新建一个执行器实例"算**，所以"任务跑了多久"和预算无关。

**✅ 同日又真机验了两条（09-13 03:47）：**

- **孤儿探测**（`_warn_orphan_running`）：**主动造了一个孤儿**（`tracker.create` +
  `transition(RUNNING)`，不派发），循环走到"没活干"那一刻就报了：
  `orphan_running_task:1789242460645:没有 future，也没人收割它`。
  而且**幂等** —— 15 秒内始终 1 条（不去重的话每次空转都会刷）。
  现场已删。⚠️ 这是**主动造的**，验的是"探测和上报这条路通"，
  **不是**"真机上自然发生的孤儿会被抓到"（那是同一段代码，但触发源不同）。
- **`?format=md`**（`547e742`）：真机从 **500 → 200 + 正常 markdown**。

**❌ 但有一条查完确认"这轮没被触发"** —— 内存那三处（`auto_maintain` /
`system2_extract` / `add_inferred_causal_edge`）：

- `consolidate:` **零告警** ≠ 修好了。实测 `_heavy_due()=False`（重活那支压根没跑），
  25 个候选里**高置信 0 个**（`add_inferred_causal_edge` 那支只走 LLM 判断）。
- 触发它要 `_heavy_due()`（上次重活 + 1h，或 10 次调用）**且**那一轮有任务收尾
  （`consolidate_memory()` 只在 `results` 非空时被调）—— 而且 `system2_extract` 会**花钱**。
- ⇒ **状态仍是"从'永远不跑'变成'能跑了'"，不是"收益拿到了"。**
  下次真跑（≥1 小时后 + 有任务收尾）它自己会触发，不用造。

---

### 三·八、09-13 新写的（A 臂 ＋ 告警页）

> **A 臂**：P1 要问"同样一笔钱，花在**一个强模型多改几轮**上，会不会比
> **两家各出初稿再融合**更好"。B 臂（委员会）早就有，A 臂**没有** —— 这段是新写的。
> 方案见 `docs/P1-三臂实验方案-20260912.md`。
> **告警页**：后端 `alert_summary` 早就有且真机验过，前端**一个字都没有** —— 从零加的。

| | 改了什么 | 怎么算验过了 |
|---|---|---|
| [ ] | **告警页**（`/alerts`，`pages/Alerts.tsx`，2026-09-13） | 后端数据已验过（§"告警按 key 聚合"那条）；**缺的是"人拿浏览器打开看一眼"** —— 目前只验到：`tsc` 过、build 出 chunk、`/static/dist/assets/Alerts-*.js` 真能取到（200 + `text/javascript`）、单测 4 条（含**变异验证**）。<br>**怎么算验过了**：开着服务打开 `/alerts`，看四样 —— ① 侧边栏出现「告警」且点得进去；② **常驻**那栏在上、里面是 `collect_changes` / `constraints_checklist_fallback` 这类反复出现的 key；③ **事件**那栏在下；④ 点「刷新」数字会变、"数据截至"的时间戳跟着走。<br>⚠️ 写这页时被单测逮到一个**真 bug**：`useToast()` 每次渲染返回**新函数**，放进 `useCallback` 依赖 ⇒ `useEffect` 死循环拉接口。已修，教训写进页内注释。 |
| [ ] | **A 臂：单模型 + 等预算多轮自修订**（`_dispatch_solo`，开关 `QIDIAN_SOLO_TOKENS`，2026-09-13） | **默认关** —— 不设 env 时走的是原来委员会那条，逐字未变（有守卫测试钉着）。<br>**怎么算验过了**：设 `QIDIAN_SOLO_TOKENS=<B 臂实测的 token 数>` 跑一次架构阶段，看四样：<br>① trace 里出现 `<task>_solo1 / _solo2 / …` 的调用（**且全程没有委员会那几家**）；<br>② 第 2 轮起提示词里带**它自己上一轮的稿**（不是重跑，是自修订）；<br>③ 累计 token 到预算就停（不是跑满轮数）；<br>④ **账上按真实模型名逐轮记** —— 记成 `solo(a,b)` 这类合成名的话计价表查不到，整段算不出钱。<br>⚠️ **口径 = 按 token 等**（09-13 用户拍板，三选一）；预算**从 B 臂实测值填**，别自己估。<br>⚠️ **到目前一次真机都没跑过** —— 只是单测绿 + 变异验证过。 |

---

## 四、验的方式（怎么跑）

**主路径**：重跑一轮探路轮（新项目，不沿用旧的）。

**验之前记得**：
- 后端**重启过**才带上这些改动（2026-09-12 03:00 那次已重启，之后又改了 6 笔 → **要再重启一次**）
- `docs/防御模式.md` 里 §1「先看症状」那套流程照走
- 出问题**先翻 `alerts.jsonl` 看时间**（§42：不看时间 = 把已修的问题当现存 bug，那一夜我犯了两次）

**看完一轮之后**：把这份清单里对应的 `[ ]` 划掉，写上"核于 <日期 / 哪个项目 / 哪条 trace>"。
**标记不带锚点，读的人分不出它漂没漂**（这条规矩来自 `docs/审计报告-20260911.md` 头部）。
