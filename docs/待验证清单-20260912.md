# 待验证清单（2026-09-12 一夜的改动）

> **这份文件的存在理由**：2026-09-12 那一夜改了 16 笔提交、测试 862 → 1030，
> 但**绝大多数改动只在桩测试里绿过，没在真实运行里跑过**。
> 而那一夜最大的教训恰恰是：**测试绿 ≠ 对** —— 探路2 五个任务代码全绿、
> 模型也把活干完了，照样一个都没交付。
>
> **每一条都写清"怎么算验过了"。** 验完就把 `[ ]` 划掉并写上日期。

---

## 一、一次干净复跑就能验的（优先级最高）

跑一轮新的探路轮（参考 `docs/P1-三臂实验方案-20260912.md` 里的任务选型：
单文件 CLI、有约束、规模小）。

| | 改了什么 | 怎么算验过了 |
|---|---|---|
| [ ] | **架构 prompt 要求 `check` 出 argv 结构 + `covers` 声明** | 跑完看架构产出：`constraints[].check` 里出现 `{"argv": [...], "expect_exit": N}`（不是散文）；`covers` 非空。**若还是全散文 → prompt 没起作用** |
| [ ] | **机械检查在验收时真跑**（`_run_verification` → `machine_checks.json`） | 项目跑完后 `.qidian/projects/<id>.machine-checks.json` **存在**，且里面有 `ran/passed/exit` |
| [ ] | **需求侧覆盖率**（`requirement_coverage`） | 项目 lineage 里出现 `action: "requirement_coverage"`；有 `uncovered` 时 issues 里有 `requirement_uncovered` |
| [ ] | **检索后改写**（论文那步） | ⚠️ 只在 `deep=True` 时触发，而 deep 的条件是 `fix_round>0` 或 `review_failures>0` → **首轮不会触发**。要验得造一个"栽过跟头"的项目 |
| [ ] | **`run-phase` 不再谎报** | 对 `template` 项目 POST `/api/projects/<id>/run-phase` → 应回 **409 + `started:false`**（不再是 `{"ok":true,"started":true}`） |
| [ ] | **批准 GATE1 后自动踢起 planning** | 走 **API**（不是界面）批准 GATE1 → 项目应自己进 planning，不再空等 |
| [ ] | **防御模式找茬清单进审查 prompt** | 审查那一刻的 prompt 里有「【历史上出过的这类毛病…】」段（从 trace / `alerts` 看不出来，得临时打日志或看调用） |

## 二、要靠特定场景才触发的

| | 改了什么 | 触发条件 | 怎么算验过了 |
|---|---|---|---|
| [ ] | **DSML 解析** | 模型吐 `<\|DSML\|` 格式时 | `alerts.jsonl` 出现 `xml_tool_calls_recovered:<n>:<工具名>`，**且那次的 `changed_files` 非空**（2026-09-12 只触发过 1 次，接的是 `run_command`，**没验过它救回一个 write_file**） |
| [ ] | **§55 trace 侧**（看见已提交的改动） | 任务撞 900s 超时 | trace 里 `changed_files` **不止 `__pycache__`**；`raw_output` 里有"另有 N 个提交未合并" |
| [ ] | **§55 记忆侧**（超时任务进记忆） | 同上 | `events.json` 里那个超时任务的 `trajectory` **非空**（原来恒 0） |
| [ ] | **`_llm_judge_causal` 修复** | 整合器跑到第 10 次（`%10` 节流） | `edges.json` 的 `causal` 边里出现 `reason` 以 `llm:` 开头 —— **从来没成功过一次**，这是最该盯的一条 |
| [ ] | **经验分层抽象** | 同上（`%10` 节流） | `events.json` 里有事件的 `attrs.abstraction` 非空 |
| [ ] | **预算门** | 项目花费到预算 80% / 100% | 80%：issues 里出现 `budget`；100%：调研/架构被拦，phase 被推到门 |
| [ ] | **模型纪律表自动累积** | 任务收尾且架构给了 `estimated_files` | `.qidian/model_discipline.json` 里 `audits` 涨了 —— ⚠️ **探路2 的架构全没给这个字段**，大概率**量不出来一直不记**（那是对的，不是 bug，见模块头铁律） |

## 三、已经在真实数据上验过的（不用重复验）

| | 验的方式 |
|---|---|
| ✅ **§55 trace 侧**（"看得见已提交的改动"） | 拿 T2 那个真 worktree 跑：旧逻辑只捞到 `__pycache__`（1 个）→ 新逻辑捞到 4 个含 `txtstat.py` + `new_commits` |
| ✅ **经验分层抽象**（`abstract_trajectory`） | 拿 T3 真轨迹：9599 → 328 字（3.4%），三层各司其职 |
| ✅ **检索后改写**（`adapt_experience`） | 拿 T4 那条真经验改写 → 产出是针对任务的计划（3/3 成功） |
| ✅ **需求侧覆盖率**（函数本身） | 拿探路2 真架构 + 真需求算：5 条需求 / 覆盖 4 / 可机器跑覆盖 0 / `uncovered=[4]` |
| ✅ **埋点：`trajectory` + `tool_seq` 存储** | T4/T5 真机存进去了（2610 / 16000 字，16000 = 上限截断） |
| ✅ **删任务不动记忆** | 清掉探路2 的 5 个任务后，`events.json` 里那 5 条**还在** |

---

## 四、验的方式（怎么跑）

**主路径**：重跑一轮探路轮（新项目，不沿用旧的）。

**验之前记得**：
- 后端**重启过**才带上这些改动（2026-09-12 03:00 那次已重启，之后又改了 6 笔 → **要再重启一次**）
- `docs/防御模式.md` 里 §1「先看症状」那套流程照走
- 出问题**先翻 `alerts.jsonl` 看时间**（§42：不看时间 = 把已修的问题当现存 bug，那一夜我犯了两次）

**看完一轮之后**：把这份清单里对应的 `[ ]` 划掉，写上"核于 <日期 / 哪个项目 / 哪条 trace>"。
**标记不带锚点，读的人分不出它漂没漂**（这条规矩来自 `docs/审计报告-20260911.md` 头部）。
