# 真机验证清单 —— 2026-09-14 那一夜/早上的 34 个提交 **+ 之后的 17 个**

> **为什么要这份**：那一轮改了 **68 个文件 / +5337 行**（34 个提交），
> **全部只有离线验证**（真代码 + 全量 1371 绿 + 前端 71 绿 + 每条改动都做了变异测试），
> **一处没上过真机**。而变异测试只能证明"判据被钉住了"，**证明不了"真机上它按预期工作"**。
>
> ⚠️ **这批改动的共性风险**：S1 那一族全是"**坏了就拒绝服务**"。
> 我把"正常路径不受影响"也写了对照测试，**但对照测试也是我写的** ——
> 真机上最需要看的就是：**配置都好的时候，一切照旧**。

---

## 🆕 0.5 **后加的**：清单写完之后又落了 17 个提交（7 个含代码）

> ⚠️ **这一节是补的，不是原稿。** 原清单写于 `814e1be`，之后代码又动了 7 次。
> **不补进来就是"格式对、内容旧"** —— 你会照旧清单验、以为验完了，而新那批一处没看。
> （这条是这仓 `CLAUDE.md` 里明文记着的纪律：**脚本查得出"漏没漏提交"，
> 查不出"内容过没过期"**；今晚刚在另一处踩过一次。）

**A. 这三件先验 —— 都是"改了判断逻辑"，判反了两头都疼：**

- [ ] 🔴 **人审门里回「不能通过」→ 该打回。**
      **原来判成「通过」**（`b9bc612`）。顺手把这几个也走一遍：
      「没通过」→ 打回 · 「不想继续」→ 打回 · 「**特别同意**」→ **通过**（别误伤） ·
      「好的」→ 通过。**这是门禁，判反了比什么都严重。**
- [ ] 🔴 **四个写侧闸门改成"读在前、判在后"了**（`8e46dee`）——
      验的就是**正常路径没被新闸门误伤**：模型纪律表照常累加 · 设项目根目录时
      **别的设置没被吃掉** · agent 技能能勾能存。
      ⚠️ 这批正是上面 §1 那个洞的修复；**新闸门会不会拦错，只有真机能看**。
- [ ] 🔴 **技能页正常时仍是 200**（`fe4267e` 把"坏文件"那条改成回 503）——
      正常打开技能页、勾一个技能 → **成功、不报错**。（别把正常路径也一起堵了。）

**B. 再验这几件（改动小，但都可能悄悄坏）：**

- [ ] **角色页**：`roles_custom.json` 那条动过（`e97d25d`）—— 增删改角色照常；
      真坏的话**告警面板**上能看到（不再只写进调度器日志）
- [ ] **失败用例数是不是真数**：跑一次会失败的校验，界面上的"失败数"（`e97d25d`：
      原来把 npm 的**退出码**直接存进了"失败数"字段 —— `9 failures` 是编出来的）
- [ ] **`.qidian` 里没有 `.tmp` 残留**（`11b34c2` 全换原子写 + 自动清扫；
      跑完一轮 `ls .qidian/*.tmp` 该是空的）
- [ ] **账本 `process_ledger.json` 照常新增一行**，缩进仍是 1 空格（别被原子写顺手改了）
- [ ] **技能页**：某个阶段的绑定读不到时，模型那行该说"**判不了**"，
      而不是装作"没被覆盖"（`11a52e8`）

**C. 不占你真机时间的两条**（我已经离线跑过，列出来免得你以为漏了）：
`5a0fe9c`（前端测试偶发红）· `2af723e`（样板文档撒谎）—— 都是**测试/文档**，不动真机行为。

---

## 🆕 0.6 **再后加的**：又是 9 个提交（`944cad4..b9e76ff`，全部已提交推送）

> 和 §0.5 同一条纪律：**清单不补就过期**。这一批是"把已确认的洞修掉"，
> 判据都是**安全 / 门禁**，改的又是"判据层" ⇒ 真机上最该看的是**正常路径没被误伤**。
> 离线账：`pytest tests/ → 1417 绿` · `tsc --noEmit → 0` · `vitest run → 12 文件/76 用例`
> · 样板 `--config src/pages/__rewrite_sample__/vitest.sample.config.ts __rewrite_sample__
> → 24 绿`（四条退出码逐个看过，走的文件不是管道）。

- [ ] 🔴 **权限闸门现在真的会拦了**（`82ef130`）。给某个 agent 绑 `read-only`
      或 `sandboxed`，跑一个要写文件的任务：
      **该被拒**（tool 结果里出现"操作被拒绝"）。
      ⚠️ 反向更要看：**没绑 profile 的 agent 照常干活**（默认 profile 是
      `full-access` = 不拦）。改之前这条闸门**只有 openai 类型的 agent 生效**，
      所以"以前能跑、现在被拦"如果发生，先查那个 agent 的 profile 绑定。
- [ ] 🔴 **`claude-cli` / `zhipu` 类型的 agent 若绑了限制性 profile**：
      `alerts.jsonl` 里该出现一条 `profile_not_enforceable:`。
      那是**故意的出声**（这两种执行器的工具在外部进程里、本进程拦不了）——
      看到它说明"拦不了"这件事不再被藏着，而**不是**新故障。
- [x] ✅ **已验**（见 §0.7）**Observer WS 的来源校验**（`f3bdeef`）：本机 UI 连 WS **照常**；
      从**外部页面**（`file://` 打开一个自己的 html 去连 8765）**该连不上**。
      ⚠️ 这条顺手把 `0.0.0.0` 补进了 WS 侧（和 HTTP 门对齐）——
      如果你以前靠"WS 拒 0.0.0.0"当过滤，现在它放行了。
- [x] ✅ **已验**（见 §0.7）**建任务带 `route_type=fusion` 现在会 400**（`88daeb3`）：
      前端不发这个字，所以正常点界面看不到；**任何脚本/收藏的 curl 发过它，会从 200 变 400**。
- [ ] **角色页**：增 / 改 / 删照常（`b9e76ff` 只动了失败分支）。
      要验失败分支：把 `.qidian/roles_custom.json` 手工写坏（写一行 `{`），
      再去改角色 → 该看到 **503 + 中文原因**（"拒绝写入：…读不动…"），
      而不是一个 generic 报错。⚠️ **验完把它改回来**（坏文件会被隔离成 `.corrupt`）。
- [ ] **任务时间线**（`9341505`）：找一个 `decomposed` 或 `conflict_held` 的任务，
      看 `/api/tasks/<id>/timeline`（或以后接上的前端）—— 最后一条该带
      `"terminal": false`，**不该**出现 done/failed 那种终点节点。
- [x] ✅ **已验**（见 §0.7）**`project_lineage`**（`b9e76ff`）：`GET /api/projects/<id>/lineage` 现在
      返回**项目自己的血缘**（phase 流转记录）。前端**没有**任何地方调它，所以
      界面上看不出变化；真要验就 curl 一下。
- ⬜ **不用真机验的三条**（说清楚边界）：`validator` 的 unittest 0 测试那条
      **只在 Python <3.12 上才是活的**（本机 3.14 走的是另一句，改了也看不出）；
      `worker 异常抢救段` / `orchestrator` 那两条**不好主动造**；`is_model_available`
      的出声要**把模型库弄坏**才触发（不值得为验它去弄坏）。

---

## 🆕 0.7 **我（Claude）已经替你验过的**（2026-09-14 19:2x，用当前代码 `dcbe4a4` 的真后端）

> 这些不是"离线",是**真机 + 真后端**。每条都有命令和结果，你可以只读结论、不用重跑。
> 剩下的（要动界面 / 要跑任务的）我没法替你做，还挂在 §0.5 / §0.6 / §1 里。

**⚠️ 先说一个前提**：接手的那个后端是**昨天 22:39 起的**（`scheduler.log` 里
`scheduler loop started` 停在那），**跑的是今天 34 个提交之前的代码** —— 对着它验等于白验。
已按 `start.sh` 的方式重启（`.venv/bin/python -m singularity.web.app` → 日志 `/tmp/qidian.log`），
重启后 `scheduler loop started` 是 `19:27:50`，三个端口 5050 / 5051 / 8765 都绑在 `127.0.0.1`。

| 项 | 结果 | 证据 |
|---|---|---|
| 起得来、无 import 错 / NameError | ✅ | Flask 起了、`scheduler loop started` 落了、三个端口全在 |
| 日志里没有 `损坏`/`record_skip`/`save_skipped`/`rollback_skipped` | ✅ | `grep -cE` 在两个日志上都是 **0** |
| `.qidian/` 里没有 `.corrupt` / `.tmp` | ✅ | 两个 glob 都无匹配（起服务前后各看一次） |
| 界面能列出任务 / 项目 / 模型 | ✅ | `/api/tasks` `/api/projects` `/api/models` 全 **200**，`/` 也 200，内容非空 |
| **§0.6 建任务带 `route_type=fusion` → 400** | ✅ | `{"error":"route_type 无效，允许: bugfix,default,docs,feature,refactor"}` **HTTP 400**；前后任务数都是 **2** ⇒ 没落脏数据 |
| **§0.6 `project_lineage` 返回项目自己的血缘** | ✅ | 两个项目各自 200，**首条 ts 不同**（`1789303372` vs `1789300047`）⇒ 不是同一份全局数据 |
| **§0.6 / §4 Observer WS 来源校验（8765）** | ✅ | `http://localhost:5050` · `http://127.0.0.1:5050` · **无 Origin** → 连上；`http://evil.com` · `http://127.0.0.1.evil.com` · `null`(file://) → **HTTP 403** |
| 顺手同验了 bridge WS（5051） | ✅ | 同上六条**结果一模一样**（两扇门没分叉） |

**这一轮没验成的（不是失败，是造不出条件）**：
- `任务时间线` 那条要一个 `decomposed` / `conflict_held` 的任务 —— 现在盘上就 2 个任务、
  一个是 `done` 一个是 `failed`，**没有非终态的**。（读代码确认了分支写在
  `_api_tasks.task_timeline` 的 `_NOT_TERMINAL_ENDS` 上，但**没真机跑过那条分支**。）
- `profile_not_enforceable` / 权限闸门 / 角色页 503 —— 都要跑任务或改配置，没动。

**探针留档**：`/tmp/ws_origin_probe.py`（只连不断、不发任何 action、不碰生产数据）。
⚠️ 它踩了个坑值得记：本机 Clash 的**系统级 SOCKS 代理会被 `websockets` 读走**，
连 `127.0.0.1` 也报 `connecting through a SOCKS proxy requires python-socks` ——
**那是探针的毛病，不是被验的门**。要写 `proxy=None`。

---

## 🆕 0.8 **刚落的两个改动**（结构性清单里的 A1 / A2）—— ⚠️ 真机没验

> **离线账**：全量 `pytest tests/ → 1447 绿（0 skipped）`；
> 两条新用例**都做了变异验证** —— A1 把判据改成 `if False:` → 红；
> A2 删掉 `discard` 那行 → 红在 `'proj1' not in {'proj1'}`。
> 说明与取舍见 `docs/结构性-水位触发-清单-20260914.md`。

- [ ] **A1 周期对账改成水位触发**：`_loop_worker` 里那句从 `if count == 0:` 里**挪出来**，
      改成"每轮 + 按时间节流 300s"。原来只在"空转满 100 轮"时跑，而**忙的时候
      `idle_ticks` 每轮被清零** ⇒ 计数永远够不到 100 ⇒ 一直有活就一次都不对账。
      ⚠️ **真机上怎么看出它跑了：看不出来。** 对账在没有漂移时**什么都不推**
      （只在有 drift 时推一条 SSE）⇒ "没看到 reconcile 事件"**不等于**"它没跑"。
      要判它真跑过，只能起服务后**跑一个真任务**（让队列忙一轮）、再等 ≥300s，
      看有没有 `reconcile` 事件或 `reconcile_failed` 告警。
      **这条的可见性问题本身值得单独记住**（仪器自己没心跳）。
- [ ] **A2 合并提交失败会回滚**：`submit` 抛了会把 `_merge_inflight` 的 id **撤掉**并出声
      （`merge_submit_failed:`，进 `alerts.jsonl`）。真机触发要"恰好在 executor 关闭时提交"，
      **不好主动造** —— 撞上时看到这条告警的意思是"这个项目本来会永远不再被合并，
      现在下一轮会重试"。

---

## 🆕 0.9 **又落了三条**（清单里核出来的 🅰：#8 / #6 / #4）—— ⚠️ 真机没验

> **离线账**：全量 `pytest tests/ → 1449 绿（0 skipped）`；三条各一条新用例、
> **各自变异验证过**。⚠️ 棘轮基线跟着从 **402 → 401**（`_recover_parked` 那处不再静默）。

- [ ] **#8 冲突任务不再"凭空蒸发"**：`MergeQueue._recover_parked` 原来两条路都静默 ——
      parked 文件解析失败、或 `branch` 为空，都被**无声跳过** ⇒ 那个任务还挂着
      `CONFLICT_HELD`，但 `conflicts()` 里已经查不到它了。
      **真机怎么验**：把 `.qidian/parked/` 下随便一个 json 写坏（写一行 `{`）再重启后端，
      **该看到 `parked_recover_failed:` 的告警**；验完把文件改回来。
      （⚠️ 别留坏文件在那儿 —— 那会让一个真冲突任务一直不在列表里。）
- [ ] **#6 收尾三件各自 try**：`_drain_pending` 里原来一个 `try` 包四件 ⇒
      `_release_ref` 一抛，**trace / 记账 / 经验 / 路由学习全跳过**，而任务状态还是 DONE。
      **真机不好造**（要让 `_release_ref` 抛）—— 撞上时看有没有 `release_ref_failed:` / `save_trace_failed:` 告警。
- [ ] **#4 隔离登记提前**：`_QUARANTINED.add` 从善后之后挪到了之前。
      **真机验不了**（要备份/剪枝抛非 OSError —— 小概率），这一条靠用例钉住就够了。
- [ ] **#5 冲突解决失败不再丢记录**：`MergeQueue.resolve` 里 `merge_ref` 抛了
      （**实测抛点**：`repo_root` 目录不存在 ⇒ `FileNotFoundError`），现在会把 parked
      记录**补回去**并出声（`resolve_merge_ref_failed:`），不再往上抛（返回值里带失败原因）。
      **真机怎么验**：`/api/conflicts/<id>/resolve` 打在一个项目目录已被删掉的冲突任务上 ——
      该看到告警，而且**任务还在 `conflicts()` 列表里**（以前会凭空消失）。
      ⚠️ 不好造（要先有一个 parked 任务 + 把项目目录删掉），撞上时再看也行。

---

## 🆕 0.10 **回滚三处写入点**（B1）—— ⚠️ 真机没验

> **离线账**：全量 `pytest tests/ → 1454 绿（0 skipped）`；新增 `tests/test_scheduler/
> test_task_attach_rollback.py` 四条用例 —— **三条接线 + 一条原语**。
> **变异验证**：三处的回滚同时去掉 ⇒ **正好那三条接线用例红**，第 4 条（测原语本身）不红。

- [ ] **建任务时登记进项目失败 → 任务被撤回**。三条路都要能观察到：
      · 界面建任务挂在项目上（`task_submit`）→ 失败**该返回 500 且任务不在列表里**
      · 拆解落任务（`_run_execution`）· 重规划兜底（`_decompose_and_create_tasks`）
      **真机怎么验**：都不好主动造（要让 `project.save` 抛，比如磁盘满 / 权限）。
      **看到告警就是它在干活**：`task_attach_failed:`（原因）+ `created_task_rolled_back:`
      （动作，带撤了几条）。⚠️ 这两个 key 是**分开的** —— 一个是"为什么"，一个是"做了什么"。
      ⚠️ **没看到告警 ≠ 没漏**：真出现"任务建了但项目里没有"，先看这两个 key 在不在。
- [ ] **有一条"退化成显式失败"的分支**：连删文件都失败（权限/占用）时，
      任务会被标成 **FAILED** 并把原因写进 `error`，而不是留着一条没人管的 pending。
      **真机不好造**，知道有这回事就行。

---

## 🆕 0.11 **两条"纸面成立、真机零次"的**（B2 / #7）—— ⚠️ 真机没验

> 两条都是 **2026-09-14 用户拍板后做的**，依据是**真机证据**（见下），不是纸面判断。
> **离线账**：全量 `pytest tests/ → 1457 绿（0 skipped）`；各带用例、**各自变异验证过**。

- [ ] **B2 重活记账拆成"尝试 / 成功"**（`_heavy_due` + 新的 `_mark_heavy_done`）：
      现在 **`_heavy_due()` 返回 True 只说明"该试了"**，清计数归 `_mark_heavy_done(ok)` ——
      **只有成功才归零**；失败时 `calls`/`last_success` 原样留着，节流一过就会再试。
      **真机怎么验**：等一次重活（`memory/consolidate_state.json`）——
      **成功**时 `calls` 归 0、`last_heavy` 推进；**失败**时 `calls` 不清零、`last_heavy` 不动、
      只推进 `last_attempt`。⚠️ `last_heavy` 仍照写（它是你排查时看的那个数）。
- [ ] **#7 交付没有 code_ref 时出声**（`delivery_no_code_ref:`）：真机 **8/8 都是真 tag**、
      从没触发过；一旦触发，`code_ref=unknown` 原来**只存在于 detail 串里**。
      **仍然算交付成功**（判失败会把好项目卡死在 delivering）。
      ⚠️ 没看到这条告警才是正常的。

---

## 🆕 0.12 **2026-09-15 下午这一轮**（真后端 + 真 HTTP，我跑的；每条都有证据）

> 前置动作：**把 09-14 20:33 起的那个后端停了、用当前代码重启**（它跑的是今天全部提交
> **之前**的代码，对着它验等于白验）。重启走 `./start.sh -d`，日志 `/tmp/qidian.log`。

| 项 | 结果 | 证据 |
|---|---|---|
| **🔴 今天的修复：启动自检 INFO 现在真落盘了** | ✅ **真机验到了** | `.qidian/logs/scheduler.log` 里新出现 6 行 `startup`：`Singularity Dispatch面板 → …` · **`模型: 21 注册, 13 可用`** · `WebSocket 服务器已启动` · `Observer WebSocket 已启动` · `调度循环已自动启动 (concurrent=2)` · `观察者智能体已自动启动`。改之前这一串**一个字都没有**（09-14 撞见的那个症状）。 |
| 起得来、三个端口都在 | ✅ | `lsof` 显示 **5050 / 5051 / 8765 全绑 `127.0.0.1`**；`/api/tasks` `/api/projects` `/api/models` `/` 全 **200** |
| `MCP: N 服务器` 那行为什么没打 | ✅ **不是漏** | 它在 `if mcp_configs:` 里，而 `load_mcp_configs()` 实测 **0 个** ⇒ 本来就不该打 |
| **§1 四个写侧闸门逐条走**（加→读回来→删→对哈希） | ✅ **4/4** | `api_store.json` · `agents_custom.json` · `models_custom.json` · `settings.json` —— 加得进、读得回、**删完 sha256 逐字节回到原值**；且**原有条目一条没被吃掉**（哈希相等就是硬判据） |
| **§1 的误伤症状面** | ✅ 零 | `.corrupt` / `.tmp` **一个都没有**；日志里 `损坏\|record_skip\|save_skipped\|rollback_skipped` 计数 **0** |
| **§0.6 角色页失败分支**（按清单把 `roles_custom.json` 写坏） | ✅ | PATCH → **HTTP 503** + 中文原因：`拒绝写入：…roles_custom.json 读不动（Expecting property name…）—— 拒绝覆盖写：那会把已有角色覆盖全丢掉。先修好它再改。`<br>坏文件**被隔离成 `.corrupt`**（设计行为）；三条告警如实进了 `alerts.jsonl`（`json_corrupt` / `role_override_unreadable` / `role_write_rejected`）。**已还原 + 删掉测试产生的 `.corrupt` + 复验 PATCH 回 200。** |

**⚠️ 这一轮顺手挖出一个洞（不是测试的错，是真行为）**：`POST /api/api-store` **收得进
`_` 开头的 id**，而 `api_store._load()` 把 `_` 前缀当**元数据键**跳过 ⇒ 那个条目
**列表里看不见、`DELETE` 回 `{"ok": false}` 删不掉**（建得进、删不掉、也不显示）。
已登记进 `~/OPEN.md`。对照组：`/api/models`、`/api/agents` 同款 id 增删正常 ——
只有 api_store 有这个前缀语义。
> ⚠️ 探针自身的教训（同族）：我第一版判"删成功"**看的是 HTTP 状态码**，
> 而这个端点**恒回 200**（成功与否在 body 的 `ok` 里）—— **把失败读成了成功**。

**这一轮没做的（还在清单上）**：§0.6 的权限闸门 / `profile_not_enforceable` ·
§0.8 的 A1（要跑任务 + 等 ≥300s）· §0.10/§0.11 · §1 的模型纪律表与 `process_ledger` ·
§2 前端那 6 条（要眼睛看）· §3 取消/回滚 · §4 MCP 重连与记忆信号 ——
**这些都要跑一个真任务**（花 token、要几分钟），等你发话。

---

## 0. 先起后端，看日志（5 分钟）

```bash
cd ~/projects/singularity
.venv/bin/python -m singularity.web.app
```

**看什么**：
- [x] ✅ **已验**（见 §0.7）起得来、没有 import 错误 / NameError
- [x] ✅ **已验**（见 §0.7）日志里没有 `损坏` / `record_skip` / `save_skipped` / `rollback_skipped`
- [x] ✅ **已验**（见 §0.7）界面能打开、能正常列出任务/项目/模型

---

## 1. 🔴 最高优先：S1 那套"读降级写拒写"有没有误伤正常路径

> 这是本批最大的风险面 —— 全是"不给服务"的闸门，误伤了就是把能用的功能堵死。

- [ ] **模型/API 库页**：能列出、能加一个 API、能删一个（`api_store` 两侧都改过）
- [ ] **agent 页**：能增删 agent、能改技能绑定（`agents_custom.json` 有两个写者）
- [ ] **技能页**：能勾技能（`skill_loader` 写路径改过）、**读不到时显示的是"读不到绑定"而不是"0/N"**
- [ ] **项目成品根目录**：换个目录设置一下，**别的设置没被吃掉**（`settings.json` 读改写）
- [ ] **模型纪律表**：跑一次带范围检查的融合审查，看 `model_discipline.json` 有没有正常累加
- [ ] 跑完一轮交付后 `process_ledger.json` 有新增一行

**如果哪一项被堵了**：八成是我把"未损坏"也判成损坏了 —— 去 `.qidian/` 看有没有多出 `*.corrupt` 文件。

---

## 2. 前端那 6 条

- [ ] **任务卡状态**：找一个 `paused` / `conflict_held` / `rolled_back` 的任务，
      **不该显示成蓝色「执行中」+转圈**；「rolled_back」该是红的「失败」
- [ ] **卡片上的验收结论**：有条任务 verdict 是「阻断」时，**不该是绿色 ✓**
- [ ] **全失败时**：对话页顶上**不该是绿勾**（该是琥珀⚠）
- [ ] **技能页**：断网（或让后端 500）时打开，**该显示"读不到绑定"且勾不动**；
      恢复后再勾，**后端的原有绑定没被覆盖**
- [ ] **建项目**：描述写个小活（比如"改个文案"），**该弹"看着像小活，要不要走轻量流程"** ——
      这条以前是死功能，现在该活了
- [ ] **SSE 断了**：把后端停几秒再起，对话页**该显示"实时连接断开"并且自己恢复**、卡片继续更新

---

## 3. 取消 / 回滚（这两条是从真实事故里长出来的）

- [ ] **点一次取消**：任务**该立刻停**，不再跑满 3 轮
- [ ] `git stash list` **不该**因为我这边的操作多出新条目（`rollback_skipped:no_snapshot_ref` 出现才是对的）

---

## 4. MCP / WS / 记忆

- [ ] **MCP**：加一个服务器 → **不用手动 refresh 模型就能调它的工具**（这条是 add 那半）
- [ ] **MCP 重连一个服务器**：**其他服务器不该从列表里消失**（这条以前会全抹掉）
- [ ] **Observer WS 绑定**：`lsof -i :8765` 该是 `127.0.0.1`；从一个**外部页面**连它
      **该连不上**（Origin 校验）
- [ ] **记忆信号**：跑一个 `retry_count == 0` 的首次任务，trace 里的 `pre_search_memory`
      **`narrative` 不该是空的**（深度 1 那条以前恒空）

---

## 5. 我**没法自己验**的三件（说清楚边界）

1. **前端所有视觉改动**：我只有 jsdom，**没有浏览器** —— 颜色/布局对不对得你眼睛看
2. **SSE 重连**：假 `EventSource` 只能验"我发了重建命令"，验不了"浏览器真的恢复了"
3. **`atomic_write_json` 的 pid+锁**：竞态要真并发才撞得出来

---

## 附：这批改动里"最可能悄悄坏掉"的四处（我会先看这几个）

| 处 | 万一坏了的现象 |
|---|---|
| `_io.is_quarantined` 把正常文件判成损坏 | 某个配置再也存不进去 + `.qidian/` 里冒出 `.corrupt` |
| 🆕 **`load_for_rewrite` 把正常文件判成"读不出来"** | 同上，而且**四个写侧**（模型纪律 / 设置 / 技能绑定 / 自定义模型）会一起写不进去 —— 这是 `8e46dee` 新加的闸门 |
| `_scan` / `test_no_silent_except` 基线 | 测试报"多 N 处"（那说明我新写的 handler 又在静默） |
| `Chat.tsx` 的轮询兜底 | SSE 正常时**不该**还有 10 秒一次的 `/api/tasks` 请求（用 DevTools 看） |

> 🆕 **最后补一句**：真机验出问题时，**先别急着改代码** ——
> 把这仓的 `docs/postmortem-模板.md` 走一遍（症状 → 根因 → 规则），
> 因为这一夜的教训全都是"**修一处、同一形状在别处又长出来**"。
