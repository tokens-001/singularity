# 自动归档规则

## 写入案卷/追踪文件的触发条件
（满足任一条即写入，不等用户开口）

- 新体验验证了已有知识概念（如今天物理隔离→自控力）
- 追踪文件有进展（阅读计划、日常执行、复习追踪等）
- 产生新决策或新规则（影响未来行为的）
- 不写入：日常对话流程、已有知识的换说法重述

## 阅读笔记归档格式
- 表格放提炼结论（方便闪卡复习），末尾放原始笔记全文保留
- 用户聊天窗口打的原始思考→逐字改错别字→存原始笔记区。不动结构和内容，只纠错字

## 笔记与案卷的关联规则
- 发现阅读笔记和案卷有内容关联时，先标明各自出处（书/案卷/对话），再审查关联逻辑
- 审查中发现定义模糊、逻辑漏洞→列出问题让用户补充→用户补充后再归档
- 不跳过审查直接引用关联。模糊处宁可列为待验证，不当作已确认结论写入

## 会话结束归档
- "结束"定义：用户说"今天就到这""先这样""下次继续""再见""拜拜""先下了"或类似结束语
- 触发后：将推进事项、关键决策和下一步写入 sessions/YYYY-MM-DD.md
- **归档格式**：不是概念列表，是对话推动的轨迹。每条推进写清楚——从哪句话/什么冲突触发的、中间有没有张力或拦截、结论怎么落地的。不写"核心推进：X→Y"这种跳跃概括
- **会话结束前强制检查启动方式**：确认 `claude` alias 指向的二进制文件存在且可执行（`ls -la` 验证），alias 路径与实际安装路径一致。不等用户开口，归档前自动执行
- 用户连续两轮只有确认/收尾型回复时（如"好""行""嗯""ok"），主动问"今天还有什么要讨论的吗？"
