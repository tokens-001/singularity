# Handoff: Singularity 2026-06-24 Stabilize Session

**Date:** 2026-06-24
**Session:** Stabilize — 架构重整 + 评级升级 + 同步修复 + 观察者智能体调研
**Git:** `969b9d5` on main (96 tests green / 75 scheduler + 21 exec_run)

## What was done

### Architecture
- `agents.toml` cleared, `agents_custom.json` becomes single data source
- Model ratings upgraded: SSS+ to A tier, colored badges in frontend
- Current lineup: E=2, E+=4, D=3 models

### Sync fixes
- `remove_agent` dual-lock: delete from custom + add to disabled
- SSE `agent_change` event push → frontend 2s debounce refresh
- `save_custom_model` / `ModelEntry.from_dict` type safety
- Architecture switch disabled count only counts eligible models in that tier
- Service Worker deleted to eliminate cache issues

### Bug fixes (6 bugs)
- `_graceful_shutdown` calling non-existent `orchestrator.stop_loop()`
- Skills import path `singularity.skills.skill_loader`
- Batch file read `read_file` supports `paths` parameter
- Token estimation pushed after planner decomposition
- API startup detection at `_loop_worker` entry
- Agent sync `_loop_worker` reloads every round
- Ghost agent cleanup (gpt-5.5 / kimi-k2.6)

### Model management UX
- Backend: `POST /api/api-store/<id>/scan`, `POST /api/models/import`
- Frontend: scan → preview → select tiers → batch import flow
- API store add returns hint to call `/scan`

### Model registry merge fix
- Custom model empty fields no longer overwrite TOML canonical values
- Fixes: rating, provider, display, strengths, notes preserved from TOML when custom JSON has empty values

### Observer agent
- Requirements doc: `tasks/observer-agent-requirements.md`
- Research report: `tasks/observer-agent-research.md` (7.5/10, needs D-layer redo)

### mattpocock/skills installed
- 34 skills installed to `~/.agents/skills/`
- Need session restart to register
- Key ones: `/grill-me`, `/diagnosing-bugs`, `/improve-codebase-architecture`, `/review`, `/handoff`, `/to-prd`, `/to-issues`, `/implement`, `/tdd`

## What was NOT done

- 3-gate + D review inner loop frontend (GATE3 approval UI) — partially done
- Observer agent implementation — research only
- 40+ modules still have zero unit tests
- Architecture friction items identified in `/tmp/architecture-review-20260624.html` (12 findings, 8 Strong)
- Layer Switch drag-and-drop UI redesign
- `/setup-matt-pocock-skills` initialization not run yet

## Active code changes

- `model_registry.py`: merge logic for custom model overlay
- `agents_custom.json`: model name ordering swap

## Running the project

```bash
cd /Users/jingzhe/奇点
pytest tests/test_scheduler/ -q    # 75 tests
python3 tests/test_exec_run.py     # 21 tests
python3 -m singularity.web.app     # web UI at 127.0.0.1:5050
```

## Suggested skills for next session

- `/setup-matt-pocock-skills` — initialize issue tracker integration
- `/grill-me` — before any new feature
- `/diagnosing-bugs` — if bugs found
- `/improve-codebase-architecture` — explore friction items from the HTML report
- `/to-issues` — break roadmap items into executable issues
- `/implement` — execute PRD/spec implementations
