# 外派包 —— 静默失败治理（拼接文件）

> ⚠️ **本文件是拼装的**，不是手写的。源文件改了要重新生成（命令见文末）。
> 单独看请改 `docs/静默失败治理-外派材料.md`。

**给谁看**：外部模型（设计题，不是补丁题）。**整份贴/上传这一个文件就够**，不用再找别的。

---

## 本包包含什么（按阅读顺序）

| # | 内容 | 为什么在里面 |
|---|---|---|
| 一 | **静默失败治理 —— 外派材料** | **正题**。12 个已证案例 + 方法学 + 我们要你答的 4 个问题 |
| 二 | **防御模式（62 条）** | **必读**。症状→根因→规则的沉淀，材料里多次引用 |
| 三 | **演化史** | 历轮同类教训，材料让它自己说"静默失败是本仓库主要 bug 模式"就出自这里 |
| 四 | **审计报告（09-11 那轮）** | 上一轮全量审计，看得到"修过什么、当时怎么判断的" |
| 五 | **CLAUDE.md** | 模块地图 + 验证命令，回答问题时定位用 |

**建议的读法**：先读第一部分（它是问题本身），需要细节时再翻二~五。
**不要**把第二部分当成"要修 62 条"——那是资料库，不是任务清单。

---

# 一、静默失败治理 —— 外派材料

# 静默失败治理 —— 外派材料

> 用途：拿给外部模型做**设计题**（不是补丁题）。
> 材料里所有「位置」都已核对过当前源码；「已修」是 2026-09-11 那轮做的，**已提交**。
>
> **2026-09-12 更新**：补了 6 个新案例（§三 的 7–12，含探路轮的真实死因）——
> 其中 5 个是从**真机**跑出来的、同一形状；另加了一节「方法学：为什么跑了几轮也定位不到根因」（§四·五），
> **那一节对回答第五节的问题 2（为什么补丁式修复反复失效）是直接输入，别跳过。**
>
> **怎么用这份材料**：**整份贴过去**（它已经自包含，一~六节就是全部）。
> 如果那个平台能上传文件，把 `docs/防御模式.md`（62 条）一起给它 —— 第六节列的那几份
> 也可以带，但**别只发防御模式**：这份材料的作用是把"设计题"和"约束"讲清楚，
> 光给那 62 条会变成"修 62 个 bug"。
> 拿到答复后的去处：`docs/` 新建一份结果文档，**别直接照着改代码** ——
> 它的产出是**设计**，落地前得过一遍本仓库的 `CLAUDE.md` 和防御模式。

---

## 一、背景

**奇点**：单体 Python 的 AI Agent 调度平台，多模型按角色协作完成软件开发任务。
约 28,600 行 Python + 3,900 行前端 + 16,700 行测试（核于 2026-09-12）。零数据库，状态全在 `.qidian/` 下的 JSON 文件里。

流程：立项 → 调研 → GATE1(人审) → 架构 → GATE2(人审) → 执行 → 集成合并 → GATE3(人审) → 交付。

**项目的核心资产不是代码，是 `docs/防御模式.md` 里 62 条「症状 → 根因 → 规则」** ——
那都是真金白银踩出来的。改这套代码之前必须先扫它。

---

## 二、问题陈述（**这是重点，别当成"修 12 个 bug"**）

用一天时间在这套系统里挖出 6 个缺陷，09-12 又补了 6 个（见第七至十二条），**没有一个报错**。它们的共同形状是：

> **系统"没做"或"做错了"，但从外面看一切正常。**

这套代码库自己的总结（写在演化史里）：

> **静默失败是这个代码库的主要 bug 模式**

**要回答的问题不是"怎么修这 12 个"，是：**

1. **这套系统的哪一层，本该负责"自证它真的做了"？** 现在这一层是空的，还是分散在十几个地方各管一段？
2. **为什么补丁式修复会反复失效？**（下面案例 1 和案例 2 修的是同一个模式的两个不同位置）
3. 在**单体 Python + 零数据库 + 文件持久化 + 明确反对过度设计**的约束下，
   最小可用的方案是什么？

---

## 三、十二个已证案例

> 前 6 个是 2026-09-11 一轮里挖的；**7–12 是 2026-09-12 从真机跑出来的** ——
> 同一形状、不同位置（11 是读代码挖的，12 是探路轮的死因）。
> **案例 8 至今没定位到根因**，如实标着；末尾另列了两件**至今无法解释**的现象。

### 案例 1：验收被跳过，只活在一条聊天消息里

- **症状**：到了 GATE3（交付审核），项目状态里 `issues` 永远是空数组；没有 QA 报告，没有任何地方说明"验收到底跑没跑"。
- **根因**：两处叠加。
  - `run_test_fix_loop()` 里 `project.issues = []` **写在验收调用之后**，把 `_run_verification()` 刚记进去的东西一并擦掉。
  - `_run_verification()` 在无约束清单时只 `return ["验收跳过 (无约束清单)"]` —— **一个字符串，不落盘**。
- **位置**：`src/singularity/scheduler/workflow.py`（`run_test_fix_loop` / `_run_verification`）
- **已修**：清空移到验收之前；跳过改为写入 `project.issues`。
- **注意**：「跳过」这件事本身可能合理（架构没产出约束清单时确实没得验），
  **不合理的是它不留痕**。

### 案例 2：计数器是单向棘轮 → 用户点一百次都出不去

- **症状**：用户在 GATE2 点「通过」，界面 2 秒后弹回同一个 GATE2，再点、再弹，永远出不去。
- **根因**：
  - `review_failures` 达到上限（2）后，`_run_integration_merge_async()` **在集成合并已成功的情况下**仍然把 phase 打回 GATE2。
  - 而 `review_failures` / `integrate_failures` **全仓没有任何地方重置** —— 只增不减。
  - 于是：批准 → 执行 → 合并成功 → 检查计数器仍超限 → 回 GATE2 → 批准 → …
- **位置**：`src/singularity/scheduler/orchestrator.py`(`_run_integration_merge_async`)、
  `_review.py`(`check_review_fail_limit`)、`project.py`(`confirm_gate`)
- **已修**：人工批准 GATE2 时重置这两个计数器（人工到场兜底 = 自动重试配额恢复）。
- **注意**：这个 bug 的可怕之处是**界面上一切正常**。没有报错、没有红色、
  用户只会以为自己点错了，然后反复点。

### 案例 3：后端花钱生成了报告，前端一个字都不显示

- **症状**：GATE3 的标签写着"请审核交付物"，屏幕上显示的是**几周前的**调研报告和架构方案。
  交付物本身（QA 报告、安全审计报告）看不见。
- **根因**：
  - 后端确实生成了：`_run_verification()` 花两次 LLM 调用产出 `qa-report.md` / `security-report.md`，
    还存了结构化的 `qa_report.json`，连 GATE3 打回的路由逻辑都读它。
  - 但 `ProjectState.to_dict()` 里**没有 qa_report 这个字段**，前端拿不到。
  - 而且 `GET /api/projects/<id>/traceability` 是一个**专门为 GATE3 写的端点**
    （docstring 明写 "GATE3: 返回需求追溯表 + 符合性检查结果"）—— **前端从来没有调用过它**。
- **位置**：`project.py`(`to_dict`)、`web/app.py`(traceability 端点)、`web/frontend/src/components/chat/GatePanel.tsx`
- **已修**：端点带出 `qa_report`；前端在 GATE3 拉取并渲染验收面板。

### 案例 4：状态文件被写坏 → 项目凭空消失

- **症状**：一个项目从界面上消失，没有任何提示。文件还在磁盘上。
- **根因**：
  - `save()` 用原子写：写 `<id>.tmp` → `os.replace()`。
  - 但 tmp 是**确定性路径**，而 `_LOCK` 是 `threading.RLock()` —— **只挡同进程的线程**。
  - 后端调度循环和 `tests/integration/role_probe.py`（独立进程）会同时 save 同一个项目：
    A 的 `replace` 成功后 tmp 就没了，B 的 `replace` 撞 ENOENT；或两边写入交错。
  - 结果是项目 JSON 末尾多出一个 `}` → `json.loads` 失败 → `list_all()` 静默跳过 → 项目消失。
- **位置**：`src/singularity/scheduler/project.py`(`save`)
- **已修**：tmp 路径带 pid。
- **注意**：`save()` 的注释显示，作者**专门加过锁**来防"两个线程共用同一 tmp"——
  **防住了线程，漏了进程**。

### 案例 5：扫描静默少一个模型

- **症状**：用户问「deepseek 怎么只能扫描出一个模型」。
- **根因**：模型扫描有两道过滤门：
  1. 在能力快照里吗？
  2. `_is_major_model(id)` 认得吗？
  
  白名单里 DeepSeek 那条是 `r'^deepseek-v\d+'` —— **要求 `v` 后跟数字**。
  而 `deepseek-flash` 既没有 `v` 也没有数字，两道门都没过 → 被丢掉。
  （同一份白名单里，Anthropic 那行**专门写了** `r'^claude-opus'...` 处理"名字不带代际"的情况。）
- **位置**：`src/singularity/scheduler/api_store.py`(`_is_major_model`、扫描主循环的过滤条件)
- **已修**：补 `r'^deepseek-(flash|pro|chat|reasoner)'`。
- **注意**：这个白名单**已经被补过两次**（一次是精确版本号滞后，一次是这次）。
  每次都只是补一条 pattern。**它会不会一直漏下去？** 这是设计问题的一部分。

### 案例 6：错误信息把原因吞了

- **症状**：点任务卡片上的「在文件夹中显示」，弹一句「定位失败」，没了。
- **根因**：
  - 前端 `.catch(() => toast('定位失败'))` —— **把后端返回的具体原因整个丢掉**。
  - 而后端确实有话说：接口把路径限制在**奇点自己的仓库**（`PROJECT_ROOT`），
    但项目产出的文件在**项目仓库**（`repo_dir(project_id)`）里，两个完全不同的目录 → 404。
- **位置**：`web/frontend/src/pages/Chat.tsx`、`web/app.py`(`api_files_reveal`)
- **已修**：前端带 `project_id`，后端按来源选根目录；toast 改带原因。

### 案例 7：任务超时被杀 —— 钱、痕迹、动作记录三样一起消失

- **症状**：任务撞 900s 超时被"杀"。trace 里 `changed_files: [] / elapsed: 0 / tokens: 0 /
  agent_output: ''` —— **看上去什么都没干**，而磁盘上 worktree 里文件是写全的。
  更怪的是：超时**8 分钟后**告警里还在刷 `collect_changes: [Errno 2]`（那个 worktree 早被删了）。
- **根因**：三层叠加，每一层都是"某条路没接上"，没有一处报错。
  - `concurrent.futures` 的 timeout **只让等的人放弃，杀不掉已经在跑的线程** ⇒
    "超时"不等于"停了"，线程还在改磁盘、还在调 API（**=还在花钱**）。
  - 记账的唯一入口在 `finalize` 那条路上，而超时分支**直接判失败、根本不进那条路**
    ⇒ 花掉的钱一条不落。执行器那个累计值在它线程里，超时方 `fut` 已被 pop，**本来就取不到**。
  - `tool_events` 平时只在内存和 SSE 里过一遍、**从来不落盘** ⇒ 超时任务的
    `tool_batches.turns` 恒为 0，"这一轮到底调了几次工具"根本没法算。
- **位置**：`orchestrator.py`(`_reap_futures` / `_salvage_timed_out`)、
  `_task_runner.py`(`_archive_task_outcome`)、`_exec.py`
- **已修**：执行器**边跑边把累计用量和工具事件落盘**，超时方读回来。
  ⚠️ **补出来的是下界**（超时那一刻正在飞的那次调用不在里面）—— 这点写进了产物里。
- **注意**：这三样**不是一个 bug**，是同一个形状（"正常收尾那条路没走到"）的三种表现。

### 案例 8：一份值写进去了、又没了 —— **至今没定位到是谁覆盖的**

- **症状**：项目跑完，机械检查的产物 `.qidian/projects/<id>.machine-checks.json` **不存在**。
  而**同一份 json 里** `architecture.constraints` 完好有 8 条，`constraints_checklist` 却是 `[]`。
- **根因**：`_run_execution` 里明明 `constraints_checklist = architecture["constraints"]`
  紧跟着 `save()`，**两个端点都验证过**（"应该写进去"和"盘上是空的"），
  **中间那一步没抓到**。全仓只有两处写它，第二处是**死代码**，所以覆盖只可能发生在
  save 那条线上 —— 而那是**时序竞态**。
- **位置**：`_workflow_phases.py:567`（唯一的活写入点）、读取点 `workflow.py`(`_run_verification`)
- **状态**：⚠️ **只修了症状，根因仍未定位**。读取时加了兜底（清单空而架构里有就用架构那份），
  **并且兜底会出声告警** —— 那条告警就是"根因还在"的信号。同时留了探针。
- **注意**：**读代码定不了这个案**。这是本材料里"为什么需要自证层"最直接的证据。

### 案例 9：一个功能"在代码里、有测试、但从来没执行过"

- **症状**：没有任何症状。功能真的没跑过，但**没有报错、没有告警、没有日志**。
- **根因**：判据挂在 `_consolidate_calls % 10 == 0` 上，而 `_consolidate_calls` 是
  **模块级变量、后端每次重启归零** ⇒ 只要单个进程生命周期内的调用次数 < 10，
  这一步**永远不触发**。实测那晚重启 4 次，没有一个进程跑到 10。
  确认方式：查它的产物字段，**全是 `None`**（有个任务有 3414 字真轨迹也没被处理）。
- **位置**：`_memory_consolidator.py`
- **已修**：判据改成**落盘**（重启不清零）+ 时间兜底。
- **注意**：**这一类的检测方式不是"看报错"，是"看产物里那个字段是不是永远是默认值"。**
  这一条对第五节的"自证层该管什么"是直接输入。

### 案例 10：告警常亮 ⇒ 真问题时被淹在噪声里

- **症状**：一次正常跑动里，三条"自己评自己"的告警**每轮都报**。
- **根因**：模型池只剩 2 个，而 `planning` 恰好用满这两个 ⇒ 委员会 = 整个池 ⇒
  裁判类角色必然撞上"自己评自己"。其中一条能兜住（剔掉写手换人），
  另一条**换不动**（兜底表里两个模型都被停用 ⇒ 表跟池**零交集**）。
- **位置**：`execution_judge.py`(`_V2_EXTRACT_FALLBACKS` 等)
- **已修**：扩池 + 把提取员指到委员会之外。
- **注意**：**"每轮都报"和"从不报"一样坏**。判据跟配置脱节时，告警会变成背景噪声，
  真出问题时没人会注意到。这也是自证层要处理的：**它得能区分"持续异常"和"配置使然"**。

### 案例 11：同一份契约有两个定义点 ⇒ 字段静默消失

- **症状**：重流程（开委员会）产出的架构里，8 条约束的 `check` **全是**结构化的
  `{"argv":[...],"expect_exit":0}`，但 `covers` **一条都没有**（**字段名都不存在**）。
  于是覆盖率只能报"8/8 条需求没人验"。
- **根因**：系统里**有两份架构 schema，内容不一致** ——
  - 架构师**席位**的 prompt（`workflow.py` `_ARCHITECT_CONTEXT`）：有 `covers`，`check` 是对象
  - 委员会**定稿**用的 schema（`execution_judge.py` `_ARCH_SCHEMA`）：**没有 `covers`**，`check` 是字符串

  而**最终架构是融合稿** ⇒ 定稿模型**根本没被告知有 `covers` 这个字段**。
- **为什么 `check` 却对了**（这是最容易误判的地方）：定稿 prompt 里带**各成员原稿全文**，
  原稿里是结构化的 ⇒ 它**照抄了形状**。**不是 schema 起了作用。**
  ⇒ 判"prompt 有没有生效"，要挑一个**原稿里没有、只有 schema 里有的字段**看。
- **位置**：`workflow.py:41`(`_ARCHITECT_CONTEXT`)、`execution_judge.py:205`(`_ARCH_SCHEMA`)
- **已修**：补齐字段 + 说清语义；并留了一条**漂移检查**
  （两份 schema 的字段集必须一致，变异验证过会红 —— 光修一次不解决复发）。
- **注意**：**同一份契约有两个定义点就一定会漂，而且漂的是其中一份** ——
  出问题时你盯着 prompt 找原因，**根因在另一个文件里**。

### 案例 12：错误信息**指向了错的方向**

- **症状**：任务报 `worker 异常: any 层所有 agent 均失败: <模型>: JSONDecodeError:
  Unterminated string`。读起来像"模型吐了个坏 JSON"，于是去查模型、查 prompt、查 max_tokens。
- **根因**：**不是模型的问题** —— 是一个**被截断的 SSE 流式分片**废掉了整条 fallback 链。
  报出来的错指向 JSON 解析，跟真正的原因（传输层帧被切断）**不在一个地方**。
- **位置**：`executors/openai_agent.py`
- **已修**：坏帧不再打死整轮。
- **注意**：**错误信息指向错的方向，比"没有错误信息"更费时间** ——
  它让你在一个错误的地方找很久，而且中间每一步看起来都合理。
  （跟案例 6 不同：案例 6 是把原因**丢掉**，这条是把原因**指错**。）

---

### 还有两件**至今无法解释**的现象（一并列出，它们是自证层的需求证据）

- **案例 8 的覆盖源**：两个端点都验过，中间那步**没有记录**，事后翻不出来。
- **一条调试期的测试间歇性失败**：重复 6+ 次复现，**加两行诊断字段之后就再没复现过**，
  反复跑全量（1124 条）全绿。**至今不确定**是竞态还是调试残留态。
  ⇒ 系统的记录能力，不足以回答"**这一次和上一次有什么不同**"。

## 四、已经存在的观测机制（**别重复提议这些**）

请不要提议下面这些，它们已经在跑了：

| 机制 | 位置 | 说明 |
|---|---|---|
| `witness.warn(scope, msg)` | `alerts.jsonl` | append-only 告警流，不受心跳清理影响 |
| `witness.heartbeat(task_id, agent_level, status, detail)` | `.qidian/heartbeats/` | 任务心跳 |
| `docs/防御模式.md` | 62 条 | 症状→根因→规则，人读的 |
| `project.issues` / `lineage` / `supervision_log` | 项目状态 JSON | 结构化留痕（**但案例 1 显示它会被静默清空**） |
| SSE 系统消息 | 前端聊天流 | 一闪而过，不落盘 |
| `docs/` 下各份报告 | — | 人工写的复盘 |
| `_review.py` 的 `quality["warnings"]` | — | 软信号，明确注释"先不设 hard gate" |
| `chancellor.py` | — | 报错分类 + 白话翻译，纯规则不调 LLM |

**已知的反模式**（这个仓库明确反对的）：

- **fail-open**：拿不到证据时默认"通过"。仓库里有注释直接写着
  "被当成软信号放行 —— 正是这个仓库修过一轮的 fail-open"。
- **过度设计**：`防御模式.md` 开头明确写了，从另一个 57 包 monorepo 借鉴时
  "**只借思路，不搬机制**" —— 那套插件 / invariant 机制搬过来就是过度设计。
  这里是单体 Python。

---

## 四·五、方法学：为什么"跑了几轮"也定位不到根因

> 这一节是 2026-09-12 实测出来的，**直接关系到第五节的第 2 问**（为什么补丁式修复反复失效）。

- **真机跑能回答"哪一步坏了"，回答不了"为什么坏" —— 只要坏在中间过程。**
  因为**一次跑只留终态**：写入点和读取点事后都能看见，但**"被谁覆盖的那一步"没记录**，
  事后翻不出来。案例 8 就是这样 —— 跑完就知道清单是空的，但**跑的时候没记那一步**。
  **这跟跑几次无关：跑 100 次也一样，只要没埋点。**
- **反过来，按"形状"静态扫一次，比跑几轮产出高。**
  同一个形状（"判据会被已提交的改动骗"）此前**被修过两次**，每次只修眼前那一处；
  2026-09-12 改成**按形状全仓扫**，**一次抓出 3 处** ——
  而那 3 处是任何一次真机跑都没找出来的（它们不会每次跑都触发，也不会自己报出来）。
- **⇒ 两类要分开打，别混**：
  - 判据撒谎 / 功能从不执行 / 静默降级 → **静态扫**能扫出来（案例 5、9 属此类）
  - 竞态 / 时序 → 静态扫**够不着**，只能**埋点后跑**（案例 8 属此类，所以至今没定位）
- **还有一条**：那些标着「已修」的，多半**只在单元测试里验过**。
  同一个系统在**桩测试全绿**的同时，案例 7 / 8 / 9 **全都活着** ——
  **桩测试测不出时序、测不出沉默**（它把外部依赖都 mock 掉了，而 bug 恰恰在接口的时序上）。

---

## 五、希望你产出什么

1. **诊断**：这 12 个案例背后是不是同一个缺陷？如果是，那个缺陷的最准确表述是什么？
2. **定层**：这套系统的哪一层该负责"自证"？给出这一层的职责边界（它管什么、不管什么）。
3. **方案**：在上述约束下的**最小**落地形态。要具体到"改哪几个地方、加什么"，
   并明确说出**哪些东西你考虑过但决定不做**（这个仓库很在意这个）。
4. **反例**：有没有哪一类静默失败，你的方案**覆盖不到**？事先说清楚。

**不要**给一个需要引入新依赖 / 新框架 / 新目录结构的方案。
**不要**把这 12 个案例当成 12 个独立 bug 来列修复清单。

---

## 六、可以补充给它的资料

- `docs/防御模式.md`（62 条，必读）
- `docs/演化史.md`（含历轮同类教训）
- `docs/审计报告-20260911.md`（上一轮全量审计）
- `CLAUDE.md`（模块地图 + 验证命令）


---

# 二、防御模式（62 条）

# 防御模式（改代码前先扫一遍）

> **什么时候读**：动状态机 / 门禁 / worktree 路径 / 子进程 / 清理 / 模型调用 / 外部输入之前。
> **怎么用**：每条是「症状 → 根因 → 规则」。先对症状，对上就按规则改。

来源：奇点自己踩过的坑（`演化史.md` + 09-09 全量扫描）+ DeepSeek Harness `docs/defensive-patterns.zh.md`
的 7 条通用防御模式（原意保留）。

**只借思路，不搬机制** —— 那套插件 / invariant 机制是 57 包 monorepo 的产物，奇点是单体 Python，
搬过来是过度设计。

标注说明：未标注 = 本项目真实发生过（有 commit / 复盘可查）；标「演化史仅记名目」= 记录太简，
规则按同类缺陷的标准做法给，未逐行核实。

---

## 一、状态机与门禁

### 1. 终态可以被改判
- **症状**：已 merge 的任务被二次执行 / 二次合并，产出重复提交。
- **根因**：`transition()` 不校验旧状态，DONE 也能改回 PENDING / FAILED。「终态」只是约定，不是约束。
- **规则**：终态只放行白名单内的流转（`_TERMINAL_EXIT`）；GATE3 打回这类显式重置必须显式写
  `force=True`，别让默认路径能改判。

### 2. QA 门禁排在 DONE 之后
- **症状**：QA 判定 fail，任务照样 DONE、照样合并 —— 门禁形同虚设。
- **根因**：`finalize()` 先标 DONE 再跑 QA，门禁结果无处生效。
- **规则**：门禁必须排在**不可逆动作之前**（merge 前、标 DONE 前）。改 finalize 先看 DONE 在哪一行。

### 3. 写入的字段 ≠ 决策读的字段
- **症状**：审查打了低置信度，cascade 仍 `accept` 放行。
- **根因**：审查写 `quality["confidence"]`，决策读 `validation.confidence`，两条数据流没接上。
- **规则**：改「打分 / 评级」类字段时 `grep` 出所有读取点；写入方与读取方对不上就当场接线
  （如 `min(旧, 新)`）。

### 4. 审查路径上多个 bug 叠加
- **症状**：修完「审查打回失效」的一个原因，打回还是不对。
- **根因**：同一条路径叠了 3 个独立 bug（NameError + JSON 解析坏 + 需求核对缺失），修一个不够。
- **规则**：修「整条路径失效」的 bug 后，**跑一遍真实路径**确认真的通了，别假设「修了那个就好」。

### 5. 同一动作有多条入口
- **症状**：GATE 确认在一处生效，另一处绕过。
- **根因**：confirm 逻辑分叉成多条路径。
- **规则**：同一语义动作收敛到单一入口（如 `confirm_gate`），其他入口转发而非复制。

---

## 二、路径与仓库边界

### 6. worktree 任务里的路径基准用错
- **症状**：worktree 里语法坏掉的文件被放行合并；或测试文件被误判「删除测试」而拦下。
- **根因**：检查拿「项目根」去查 worktree 里的文件（用了 `repo_root_for(task)` 而非 `cwd`）。
  py_compile / ruff 找不到文件就**静默跳过**。
- **规则**：worktree 任务里一切路径检查的基准是 **worktree 根（cwd）**；工具「没找到文件」不能当通过。
- **同类已复发两次**：`_exec` 的 QA 门禁传参（09-09）、`no-delete-tests` 硬规则（09-07 `9bca7cd`）。

### 7. 跑任务前工作区是脏的
- **症状**：任务 merge 时把没提交的改动一起卷进「agent changes」提交。
- **根因**：merge 前会 `git stash`，脏工作区被卷走。
- **规则**：跑任务 / 让 agent 改代码前先 `git commit` 或 `git stash`；改前先 `git status`。

- **规则**：跑任务 / 让 agent 改代码前先 `git commit` 或 `git stash`；改前先 `git status`。

---

## 三、子进程、并发与清理

### 8. SSE 断线重连不清理队列
- **症状**：反复断连重连后 `_sse_clients` 越积越多，到上限后新连接被 503 拒。
- **根因**：断开时没把该连接的队列从注册表摘掉。
- **规则**：注册与注销成对，用 `try/finally` 兜住；队列要有界，满了丢旧而不是拒新。

### 9. 清理只发信号、不等停稳
- **症状**：留孤儿进程；迟到的事件打到已销毁的对象上。
- **根因**：清理流程发完终止信号就返回，不等子进程真正退出。
- **规则**：dispose 要等到**完全停稳** —— 发信号后等子进程退出；先关监听器 / 通知注册表，
  再终止进程。

### 10. 分发器里一个回调抛异常，饿死后面的
- **症状**：一个监听器报错，后面的监听器全不执行，或整个 promise 被 reject。
- **根因**：分发循环没有逐个隔离回调异常。
- **规则**：分发循环逐个 `try/except` + 记日志；订阅者行为不端不得破坏核心生命周期。

### 11. 把异步状态当同步结果
- **症状**：把 idle / running 当成「某次调用完成了」—— 多条排队消息、中途引导、取消共用同一区间。
- **根因**：后台任务完成与轮次边界存在竞争，状态是共享的，不是某次调用的返回值。
- **规则**：要判定「某次运行」就自己定义区间（如从回执到下次 idle）；等待的转换可能永不发生，
  必须显式处理「无需等待」分支。

### 12. worktree / 临时资源用完没清
- **症状**：worktree 残留、磁盘堆积。（演化史仅记名目：v2.1「worktree 泄漏」）
- **根因**：异常路径没清理，正常路径没配对。
- **规则**：创建即登记，`try/finally` 配对清理；失败路径也要清。

### 13. 跨线程改共享状态没守锁约定
- **症状**：任务 JSON 写坏 / 状态读串。（演化史仅记名目：v2.1「线程安全」）
- **根因**：多线程同时读写同一份文件或全局 dict。
- **规则**：改 tracker / config 这类共享状态必须走既有锁（`_LOCK`）与原子写（临时文件 + rename）；
  别在锁外直接写文件。

---

## 四、模型调用与结果判读

### 14. 架构任务给模型开了工具
- **症状**：架构任务写着写着去改代码，卡在「达到最大轮次」。
- **根因**：thinking 模型触发 tool_choice 降级，被注入「必须调用工具」。
- **规则**：纯思考类任务（架构 / 评审）禁工具（`tools=[]`）；需要工具的任务才开。

### 15. 空 tools + tool_choice=required
- **症状**：测试通过即停的收尾轮卡住 —— 没有工具可调，却要求必须调。
- **根因**：`tool_choice` 硬编码 `required`，与动态清空的 `tools` 冲突。
- **规则**：`tool_choice = "required" if tools else "auto"`；任何「按需清空 tools」的代码旁边都要检查
  tool_choice。

### 16. 让模型自己决定用 python 还是 python3
- **症状**：pytest 没跑起来，模型以为测试过了。
- **根因**：prompt 没写死解释器，模型敲 `python`（macOS 上常不存在 / 指向别处）。
- **规则**：prompt 里写死 `python3 -m pytest`；别指望模型猜对环境。

### 17. 模型调用超时设太短
- **症状**：reasoning 模型（kimi-k2.6 / glm）输出长 JSON 慢，请求被 120s 超时掐断，任务卡住或报错。
- **根因**：httpx 请求超时设太短（120s），不够慢模型出长 JSON。
- **规则**：reasoning / 慢模型把超时放宽（已调 240s）。识别慢模型别只看 toml 的 reasoning 标志——
  models_custom.json 的 speed/reasoning 字段可能覆盖它（慢模型识别失效）。

### 18. 正交结果独立上报
- **症状**：进程已超时却 exit 0（捕获了终止信号），调用方误判为正常成功。
- **根因**：把一个标志的上报嵌在另一个标志的分支里。
- **规则**：每个独立事实（`timedOut` / `signal` / `exitCode`）单独上报，不嵌套。

### 19. 公共约定两侧都要遵守
- **症状**：消费方猜不出捕获的异常来自 provider 还是 wrapper。
- **根因**：同一结果有多种表示（异常 / error 分片），没在公共 API 出口规范化。
- **规则**：适配层统一规范化错误出口并在类型定义处写明；消费方覆盖每种来源形式。

- **规则**：适配层统一规范化错误出口并在类型定义处写明；消费方覆盖每种来源形式。

---

## 五、环境、凭证与外部输入

### 20. setdefault 不覆盖已存在的环境变量
- **症状**：换了 key，服务仍读旧值 → 401。
- **根因**：`os.environ.setdefault` 只在变量不存在时写入；shell 里残留的旧值优先。
- **规则**：改配置后要么显式 `os.environ[k] = v`，要么确认进程环境干净；排查 401 先 `env | grep KEY`。

### 21. 别把环境变量 / 可预测路径暴露给不可信输出
- **症状**：凭证经命令输出、`env` 或 spill 文件泄漏；临时文件被符号链接竞态利用。
- **根因**：启动的子进程继承了完整环境；临时文件用可预测路径 + 全局可读权限。
- **规则**：子进程环境清掉 `*KEY*` / `*SECRET*` / `*TOKEN*` / `*PASSWORD*`；临时文件放 0700
  私有目录、随机名、`'wx'` + `0o600`。

### 22. 用 unlink 删链接形态的路径
- **症状**：删「目录」时递归穿进符号链接 / junction 的目标，删掉别人的东西。
- **根因**：直接递归删会跟随链接。
- **规则**：先 `lstat().isSymbolicLink()` 判定，是链接就 `unlink`（只删链接本身）；真目录才递归删。

### 23. 外部输入拼进 shell
- **症状**：命令注入。（演化史仅记名目：v2.1）
- **根因**：命令用字符串拼接，交给 shell 解释。
- **规则**：`subprocess` 用列表参数、禁 `shell=True`；外部值永远不拼进命令串。

### 24. 凭证进仓库
- **症状**：PAT 明文提交到 Git。
- **根因**：把密钥写进代码 / 配置后提交。
- **规则**：凭证只走环境变量或未跟踪的 `.env`；提交前 `git diff --cached` 扫一遍。

---

## 六、错误处理、配置与静态扫描

### 25. fail-open 漏报
- **症状**：检查自身崩了，却被当成 pass 放行。
- **根因**：异常分支默认返回「通过」。
- **规则**：门禁 / 校验一律 fail-closed —— 检查失败 = 拦下并报错，不是放行。

### 26. 前端异常直接透成 500
- **症状**：后端异常直接透成 500 页面 / 空白。（演化史仅记名目：v2.1「前端 500」）
- **根因**：handler 未捕获异常，前端无兜底。
- **规则**：API handler 统一 `try/except` 返回结构化错误；前端对非 200 有兜底 UI。

### 27. 循环 import 用延迟导入破
- **症状**：`ImportError: partially-initialized module`，但只在「谁先被导入」时出现。
- **根因**：`dispatcher ↔ _dispatch_exec` 互相 import，导入顺序决定成败。
- **规则**：这是既有设计（延迟导入破循环），**不是 bug**。改导入时别把延迟导入提到模块顶层；
  报这个错先确认是不是从非真实入口（`web.app`）导入的。

### 28. API handler 返回 200 却没接真实动作
- **症状**：接口「成功」但什么都没发生；或者调用必 500。
- **根因**：handler 只写了返回，没调真正的 store / rollback 实现。
- **规则**：新增 / 改 API handler 后，用真实调用验一遍**副作用**（数据真的落盘 / 真的回滚），
  别只看返回码。
- **2026-09-12 实测两例**（同一个病的两个变种，都修了，但形状要记住）：
  1. **`run-phase` 对 `template` 项目是空操作** —— 回 `{"ok":true,"started":true}`，
     实际后台线程只 append 一句"等待 Owner 填写需求"就 `break`，而且那句 `msgs`
     被 `_worker` 丢了 → **外面看着像启动了**。真正入口是 `POST /start`。
     修法：对"自己不会推进"的档（template / gate1-3）**直接回 409 + 说清该用哪个接口**。
  2. **批准 GATE 之后没有人启动下一阶段** —— `gate-confirm` 的 `approved` 分支
     只 `confirm_gate` + `save`，而 `planning` **只有 `run_phase` 能推**
     （调度循环只管 EXECUTING 之后，前端又没有 run-phase 调用者）→
     **从界面批准 GATE1 后项目永久卡在 planning**。实测空等 14 分钟。
     ⚠️ 这条**当时被我误判成"前端会做第二步、改 API 会跑两次"** ——
     实际前端只调一次（`api.runPhase` 定义了但没人用）。**"我以为前端会做"本身就是没核。**
  - **共同点**：返回体里有"看着像成功"的字段（`ok` / `started` / `next_phase`），
    而外面**没有任何东西能分辨"真启动了"和"只挪了指针"**。
    判据：**返回的每个表示成功的字段，都要能追到一个具体的副作用。**

### 29. 硬编码常量覆盖配置
- **症状**：registry 里配的 max_turns 不生效；模型白名单因写死精确版本号而滞后。
- **根因**：代码里写死的值优先于配置 / 注册表；白名单写死精确版本号。
- **规则**：可配置项只从 registry / config 读；必须硬编码的用「系列 + 数字」通配，别写死精确版本。

### 30. 字段语义复用当缓存键
- **症状**：Router 缓存永不过期，路由结果一直用旧的。
- **根因**：把关键词列表塞进本该存时间戳的字段，比较永远不相等。
- **规则**：缓存条目显式带 `cached_at`；别复用语义不同的字段存别的东西。

### 31. 静态扫描「0 引用」≠「没人用」
- **症状**：删完死代码，仓库外的脚本 / 装饰器注册 / 反射调用当场坏掉。
- **根因**：grep 扫不到注册表调用、`visit_*` 反射、`import *` facade、仓库外消费者。
- **规则**：删除前逐条复验并排除已知误报类；删后复扫一轮抓连带孤儿；清单只当记录，
  不得照单删（见 `docs/死代码清单-20260909.md`）。

---

## 七、数据统计与持久化（09-11 用量统计轮新增）

### 32. 静默兜底 = 编造数字
- **症状**：界面上显示的金额是假的，且**没有任何提示**说明它是猜的。
- **根因**：`rates.get(model, 0.50)` —— 查不到就用一个默认值兜底。用户实际在用的模型
  不在那张写死的 8 模型表里，于是所有金额都按 $0.50/百万 算。验证方法很直接：
  `375188 ÷ 100万 × 0.50 = 0.187594`，与界面显示分毫不差。
- **规则**：**查不到就返回 `None`，绝不兜底**。让调用方如实显示"未配置"。
  这条适用于所有"估算类"数值：宁可没有数字，也不要一个看着可信的错数字。
  同类还有 `_planner` 的"混合均价 ~$0.5/M"、`_cli_projects` 的门禁估算、
  `proj.token_spent`（全仓无赋值处，恒 0，连带"预算将超支"是死代码）。

### 33. 缺失值在前端被 `|| 0` 渲染成 0
- **症状**：后端返回 `null`（未配置），前端显示 `$0.00` —— 用户以为真的没花钱。
- **根因**：`(usage?.daily_cost || 0).toFixed(2)`。
- **规则**：金额一律走统一出口（`lib/money.ts`），缺失显式渲染成"未配置价格"。
  **"绝不把缺失值渲染成 0"这条规则只能住在一个地方**，散在各处就一定会漏。

### 34. 写盘时算 derived 值 → 历史被污染
- **症状**：改算法后，存量数据的值还是旧的错的。
- **根因**：`cost_est` 在写入时算好存盘，所以算法一改就得迁移/回填。
- **规则**：**能在读时算的就别写时算**。改成读时现算后：零迁移、旧假值随上线消失，
  而且**修正输入（如补上单价）时历史数据自动变对**。
- **推广到"派生路径"**（2026-09-11）：同一条规则也管**路径常量** ——
  `_MEMORY_DIR = config.QIDIAN_DIR / "memory"` 这种在模块级算好的派生路径，
  导入后就锁死了。生产没影响（`QIDIAN_DIR` 启动定死），**但测试隔离会失效**：
  `monkeypatch QIDIAN_DIR` 对它无效 → 测试往**真实**的 `.qidian/` 写。
  实测代价：一条测试把假洞察写进了生产的 `.qidian/memory/insights.json`。
  **一天内同一个形状出现三次**（`skill_loader._QIDIAN_DIR`、`_INSIGHTS_PATH`、
  `route_learner._LEARNER_PATH`）—— 说明"能读时算就别写时算"当时只当成了一句
  **排错经验**，没当成**写代码的规矩**。
  现在：内存模块 + `route_learner` 全改成函数（`_events_path()` / `_learner_path()`…），
  `tests/conftest.py` 里那几刀多余的补丁跟着撤了，
  并由 `tests/test_scheduler/test_isolation.py::test_derived_paths_are_computed_at_call_time` 钉住。
  **只剩 `config` 自己的 `*_DIR`**（它是根，按定义就该在那儿）和
  `web/app` 的 `_ad`/`_key_file`（导入时一次性初始化，用完就消耗，本来就不该跟着变）。

### 35. 折叠按天聚合时，只重算"今天"会重复计
- **症状**：每天过午夜，昨天的量被重复算进今天。
- **根因**：原始记录窗口**不按天过滤**（`_daily` 存的是最近两天的行）。只重算
  `days[今天]` 就会把昨天的行一起算进今天。
- **规则**：**每条记录归到它自己那天**，再按分量合并。合并用 **max** 而非相加的前提是
  「桶里每个字段对行集合单调不减」—— 谁加"平均""最大间隔"这类非单调字段，max 立刻就错。

### 36. "先剪枝后归位"会静默丢数据
- **症状**：过去日期的记录还没进历史就被删掉（我第一版就这么写，4 条测试当场红）。
- **规则**：`record()` 里**先折叠归位 → 再剪枝 → 最后写盘**。顺序写进注释，
  这类"三步顺序不能换"的地方必须显式标注，否则后人重构时必踩。

### 37. 流式响应默认不带用量
- **症状**：走流式的调用（架构融合）在统计里完全没有用量，**连想补都补不了**。
- **根因**：OpenAI 兼容 API 的流式响应默认不返回 `usage`，要显式申请
  `stream_options={"include_usage": True}`。
- **规则**：任何走流式且需要计量的调用都要带上这个参数。先例见 `openai_agent.py`。

### 38. 只统计了一部分调用路径
- **症状**：统计里的钱比实际花得少，用户以为"统计坏了"。
- **根因**：全仓 6 条调用大模型的路（任务执行 / 观察者对话 / 任务分类 / 架构融合 /
  记忆整合 / 目标循环），**只有 1 条记账**。
- **规则**：加计量时，**先把所有调用大模型的入口 grep 一遍**再动手，
  别只修用户报的那一条。用 `level` 区分用途，`by_level` 就能按用途拆开看。

### 39. 把内部记录塞进会被"整行重建"的地方
- **症状**：用户手填的配置，跑一次某个功能后**静默归零**。
- **根因**：价格曾打算存在模型条目里，而"跑基准""扫描导入"会把该条目
  **每个字段重新传一遍** —— 没被显式带上的字段就没了。
- **规则**：**人类手工维护的数据，不要和机器会整体重写的记录放一起**。
  独立的文件/表让这种失败在结构上不可能（而非"靠测试兜住"）。

### 40. 删字段前先看有没有 `Cls(**row)` 的载入路径
- **症状**：删掉一个"没人用"的字段后，**整份历史静默消失**（被 `except` 吞掉）。
- **根因**：`_load()` 走 `UsageRecord(**r)` —— 删了字段，存量行就抛 `TypeError`，
  被 `except Exception` 吞掉后 `self._daily` 变空。
- **规则**：删字段前 grep `**r` / `**row` / `from_dict` 这类反序列化入口。
  确认只能"保留但不再读写"，就**留字段 + 注释写明为什么不能删**。

### 41. 测试里的模块级单例不吃 conftest 的隔离
- **症状**：测试往**生产数据目录**里写假记录。
- **根因**：单例（如 `_budget = TokenBudget()`）在 import 时就绑定了真实路径；
  conftest 改 `config.QIDIAN_DIR` **改不到已经构造好的单例**。
- **规则**：测这类模块必须 `monkeypatch.setattr(mod, "_singleton", fresh)`。
  所以 API handler 要**函数内 import** 单例（这样才 patch 得到）。

### 42. 翻告警日志不看时间 = 把已修的问题当现存 bug
- **症状**：差点报一个"已经修好"的 bug（5 条告警，看着触目惊心）。
- **根因**：`alerts.jsonl` 是**累积**的，翻旧账会误判。
- **规则**：看告警先看时间戳，对照修复提交的时间。同样地，**报告里的 file:line 会撒谎**
  （审计报告的文件行号几乎全错），**当前源码不会**。

### 43. 加"轮换/分摊"类机制前先问清列表的语义
- **症状**：给"主力 + 备用"的清单加了轮换，反而让归因变难、成本忽高忽低。
- **根因**：那个模型列表的语义是**优先级**（永远用第一个、挂了才用下一个），
  不是"负载均衡池"。我把"配了多个却只用一个"当成了 bug，其实那是设计。
- **规则**：改选择逻辑前先确认那份配置**语义是什么**。想让用户"看见"配了哪些，
  靠**列表全展示**（含未使用/不可用状态），而不是靠制造使用。

### 44. 承诺类字段必须三态可分（这是一整类的总纲）
- **症状**：一天之内挖出六个缺陷，**没有一个报错**。GATE3 看不到 QA 报告、
  验收被跳过不留痕、`deepseek-flash` 从扫描列表里凭空少一个、点文件只弹「定位失败」……
  共同形状都是「系统没做或做错了，但从外面看一切正常」。
- **根因**：事实从发生到被看见要经过若干次交接（执行→状态、状态→序列化、序列化→界面、
  过滤前→过滤后），**每次交接都靠约定维持、没有任何对账**，而且所有交接点的默认行为
  都是把"拿不到"解释成"没有/正常"。静默失败 = 一次断掉的交接 × 一个良性的默认解释。
- **规则**：**承诺类字段必须三态可分 —— 未运行 / 运行了·无发现 / 跳过(原因)。**
  消费方不得把缺失解释为良性（"没有报告"不等于"没问题"）。
  **过滤类操作必须留落选账**（滤掉了谁、为什么）—— 白名单对"厂商的命名"是开放集合，
  永远补不全，补丁只能当第一道防线，落选账才是那个能被发现的。
  已落到代码：`to_dict()` 派生于 `dataclasses.fields()`（省得手抄清单漏字段）、
  `list_all`/`load` 读不出时写 `witness.warn`、扫描把 `dropped` 聚合进 `alerts.jsonl`、
  前端 `errText(e, fallback)` 不再吞后端原因。

### 45. 计数器只增不减 = 单向棘轮，能把人永久锁死
- **症状**：用户在 GATE2 点「通过」，界面 2 秒后弹回同一个 GATE2，再点、再弹，
  **永远出不去**。而界面一切正常：没有报错、没有红色，用户只会以为自己点错了。
- **根因**：`review_failures` 触顶后，`_run_integration_merge_async` **在集成合并已经成功
  的情况下**仍打回 GATE2；而该计数全仓**没有任何地方重置**。人工批准这个"兜底动作"
  没有复位计数器 —— 于是批准 → 执行 → 合并成功 → 检查计数仍超限 → 回 GATE2 → …
- **规则**：任何"到阈值就采取动作"的计数器，**必须同时定义清零事件**，否则它就是个棘轮。
  本仓库的定案：**人工介入（批 GATE2）= 人到场兜底，自动重试配额随之恢复**。
  写这类计数器时先问一句「它什么时候归零」，答不出来就是还没写完。

### 46. 确定性 tmp 路径 + `threading` 锁 ≠ 进程安全
- **症状**：一个项目从界面上凭空消失，没有任何提示 —— 文件其实还在磁盘上。
- **根因**：`save()` 用原子写（写 `<id>.tmp` → `os.replace`），但 tmp 是**确定性路径**，
  而 `_LOCK` 是 `threading.RLock()`，**只挡同进程的线程**。后端调度循环和
  `tests/integration/role_probe.py`（独立进程）会同时 save 同一个项目：A 的 replace
  成功后 tmp 就没了，B 的 replace 撞 ENOENT（`alerts.jsonl` 里实见）；或两边写入交错
  → 项目 JSON 末尾多出一个 `}` → 解析失败 → `list_all()` 静默跳过。
  `save()` 的注释显示作者**专门加过锁**防"两个线程共用同一 tmp"—— 防住了线程，漏了进程。
- **规则**：进程内锁只解决进程内问题。tmp 路径带上 pid
  （`p.with_name(f"{p.name}.{os.getpid()}.tmp")`），锁仍保留（同进程多线程还是共用同一个 pid 的 tmp）。
  验证：`multiprocessing` 起 4 个进程 hammer 同一个项目 —— 旧代码会复现出与
  `alerts.jsonl` 一字不差的 `FileNotFoundError`。

### 47. 代理对象漏字段 = 下游判据恒假，而且查不出来
- **症状**：单文件任务**永远跳过审查**（项目测试 / 多模型审查 / QA 验收 / 需求对账 /
  安全审计五道一起跳），多文件任务的审计也对着**空 diff** 跑。而披露文案写的是
  "改动被判为小改动(单文件, diff<50行)" —— 看起来像是自己改得少，完全不像 bug。
- **根因**：`_exec.run` 构造的快照是 `_SnapProxy(ctx.snapshot_ref)`，而这个类
  **只设了 `ref` 和 `worktree_path`，没有 `method`**。`validator._diff_base` 的判据是
  `getattr(snap, "method", "") == "git"` —— 对 proxy 恒为假 → **基准恒为空串**。
  基准一空就退回裸 `git diff`，而 worktree 里改动在审查前已被 `commit_wt` 提交，
  裸 diff 恒 0 行 → 恒判 trivial。
  **上一轮审计其实"修过"这条**（P0-1：改用执行前快照的 ref 当基准），但它只核到
  `_exec.py` 写了 `base_ref=val_mod._diff_base(snap)` 就收工了，**没往下算这个表达式
  求值成什么**。而那个测试文件里原有的用例用的是**鸭子类型假对象**，恰好有 `method`
  —— 真身漏字段，假对象测不出来。
- **规则**：
  1. **测"接线"要用真身，别用假对象**。鸭子类型假对象只验证"名字对得上"，
     不验证"真实现有这个属性"。凡是被测代码对对象做 `getattr(obj, x, 默认值)` 的，
     默认值就是一条隐藏的失败路径 —— 测它必须用真实类型。
  2. **核"修好了没"要往下算一步**：`base_ref=<表达式>` 不等于基准非空。
     改完贴一行运行输出，别停在"代码看起来接上了"。
  3. **门禁类判据的默认值一律 fail-closed**：取不到基准就**不判**"小改动"，
     宁可多跑一遍贵的审查。`getattr(..., 默认)` 让"拿不到"和"拿到了但不等"同路，
     这正是 §44「承诺类字段必须三态可分」在判据层的又一例。

### 48. 用了没 import 的名字 → NameError → 被 `except` 吞成"正常的默认值"
- **症状**：功能**一直在跑、结果看着也正常**，但它其实一直走降级路径 ——
  因为 `except Exception` 把"我写错了"和"环境不可用"一起吞了。
  2026-09-11 一天出现**两次**，两次的症状都是"看不出来坏了"：
  · `_memory_core._get_embed_model` 用 `SentenceTransformer` 但**没 import**
    → 每次抛 NameError → 被"下载失败就降级"那句 except 吞掉 → **嵌入路径
    从来没生效过**（技能过滤退化成"取前 2 个"、记忆语义检索永远返回空）
  · `execution_judge._usable` 用 `model_registry` 但**只 import 了 `api_store`**
    → 恒抛 NameError → 被"查不了就别拦"那句 except 吞掉 → **提取员可用性检查
    从来没生效过**（每次都白撞，还会换到用户已停用的模型上真发调用）
- **根因**：`except Exception: <用一个安全的默认值>` 是重灾区。默认值通常**刻意的
  等于旧行为**（"保持原行为"），于是"新代码没生效"和"新代码生效了但没触发"
  在外部完全同形。**代码看着完整** —— 缺的那行 import 在同一个文件里，
  上一行就有（`_resolve_api` 里 import 了 `model_registry`，`_usable` 里没抄）。
- **规则**：
  1. **降级路径必须留痕**（`witness.warn`）。"降级"和"没写"分不开，就永远查不出来。
  2. **判断"检查类代码修好没"，必须让它真的跑一次并观察输出** ——
     `grep -c is_available` 数到 8 处也可能一行都不执行。
     这条是 2026-09-11 我自己犯的：复验审计报告时数到引用就标了 ✅。
     **grep 命中数 ≠ 行为。**
  3. **别靠读码找这一类**：`ruff check --select F821` 一次全扫出来。
     已在 `tests/test_scheduler/test_no_undefined_names.py` 挂成守卫
     （F821 ≠ 0 就红，豁免要写理由，且带"豁免过期"检查）。

---

## 八、阶段调用与记账（09-12 探路轮新增）

> 来源：跑一个真实项目（"写个字数统计工具"，4 个任务）走完整条流水线。
> 四个阶段调用漏了三样东西（cwd / 工具面 / 记账），一环扣一环。
> 详细复盘见记忆里的探路轮记录。
>
> **状态（2026-09-12 收尾）**：§49～§55 全部已修并带反证测试（全量 857 通过）。
> §52b 记的是我自己的一个误判 —— 核实后**不是 bug**，留着提醒别再犯同一个推理错误。

### 49. 阶段调用的 cwd 漏传 → 在没有隔离的地方跑
- **症状**：跑一个"字数统计工具"的项目，奇点**自己仓库的根目录**里冒出 `wc_lite.py`
  和 `examples/`。项目仓库却压根没被创建。
- **根因**：`_safe_dispatch()` 调 `disp_mod.dispatch()` 时**没传 `cwd`**，而 `dispatch`
  的默认值是空串，执行器再兜底成 `cwd or config.PROJECT_ROOT` —— **PROJECT_ROOT 就是
  奇点仓库自己**。走这条路的四个阶段（调研 / 架构 / QA 验收 / 安全审计）**全都在主仓
  根目录里跑，且没有 worktree 隔离**（worktree 只给执行阶段用，见 `_exec.py:326/336`）。
  **同一形状此前已经踩过一次** —— `_dispatch_exec.py` 里委员会合成那段注释写着
  "把目标项目的架构直接写进了奇点仓库的 docs/"，当时**只修了那一处支路，根因没动**。
- **规则**：
  1. **"这个参数能不能省"的答案 = "看它的默认值兜底到哪"**。凡是被执行器 `or` 兜底的
     参数（cwd / 模型 / 路径），漏传 = 静默走一个**没人看过**的分支。
  2. **修一处支路时必须问"同形状还有几处"**。这类 bug 的判据在**调用点**（谁没传），
     不在**实现点**（谁兜底错了）—— 只改后者就漏掉所有兄弟调用点。
  3. 非执行阶段**没有 worktree**，cwd 就是唯一那道边界。取不到项目目录时**绝不退回
     PROJECT_ROOT**，宁可自己 mkdir 一个空目录（`workflow._phase_cwd()`）。

### 50. 产出契约是"一段文本"的阶段，别给工具
- **症状**：调研员**不写调研报告，直接去写代码** —— 在项目仓库里把整个项目实现完
  （主程序 + 测试 + 跑了一遍），工具轮次耗尽，报告只剩一句
  `(达到最大工具轮次, 已产出文件)` → JSON 解析失败 → GATE1 无物可审，这轮白烧。
- **根因**：调研阶段的产出契约是"吐一段 JSON"，但平台给它的是**带 `write_file` /
  `run_command` 的干活执行器**。模型看到"做一个字数统计工具"，很自然就去做了。
  `openai_agent` 早就备好了 `SYSTEM_PROMPT_NO_TOOLS`（委员会那条路在用），
  调研/架构这条**单模型**路径没接上。
- **规则**：**工具面要跟产出契约一致**。产出是"一段结构化文本"的调用一律 `no_tools=True`
  （现在：调研 + 架构）。判断标准不是"它会不会用到工具"，而是**"它用了工具之后产物去哪"**
  —— 产出的文本会被丢进 `try_parse_json`，那工具就是纯干扰。

### 51. "强制输出"那一轮落在了循环边界外 → 模型永远没机会答话
- **症状**：任务**活干完了**（`changed_files: ['wc_lite.py', 'tests/test_wc_lite.py']`），
  交付报告里却一个字总结都没有，`raw_output` 是一句占位串
  `"(达到最大工具轮次, 已产出文件)"`。
- **根因**：执行器的工具循环是 `for turn in range(1, max_turns + 1)`。判"该撤工具、
  逼模型出终答"用的是 `tool_turns >= max_tool_turns + 2`，而 `max_turns=5` /
  `max_tool_turns=3` 时 **`3+2 = 5` 恰好等于最后一轮** —— 注入那条
  "现在必须直接输出最终答案"的系统消息后紧接 `continue`，**循环当场结束，
  那次模型调用从未发生**。占位串就是这么来的。
  连带：agent 拿不到收尾那一轮 → 没法自我总结；post-hook 还会按
  `output <80 chars`（`validator.py:129`）扣一次信心分。
- **⚠️ 一度把因果写反过（教训）**：第一版把"任务失败"也算在这条头上，理由是
  `term_reason` 里带着 `post_hook: output <80 chars`。**那句只是拼进字符串，
  不决定成败**（`_exec.py:574`）—— 真正的失败判据是 `batch.ok == False`，
  而它是 §52 的 `action=retry` 驱动的。**日志里出现的字样 ≠ 判决依据。**
- **规则**：
  1. **凡是"注入一条系统消息然后 continue"的机制，都要问一句"下一轮还存在吗"**。
     阈值型判据（`>= n+2`）必须跟循环上界对账，否则最想触发的那次恰恰触发不了。
  2. **判失败原因要顺着判据链走，别拿 `term_reason` 里的字样当结论** ——
     那是个拼接串，什么都会往里塞。§52 也是这么误判出来的（见其症状描述）。
  3. 测试用真实配置对账：`tests/test_scheduler/test_force_output_turn.py`
     直接钉住 `force_output_at(5, 3) < 5`。

### 52. 拦截类判据必须能说出"拦的是什么"
- **症状**：`unverified` 里出现 `'QA 约束验收 0 条未满足: '`（冒号后面空的），
  同时 `action: retry` —— **拦住了，但说不出为什么**；下一轮必然同样结果，循环收敛不了。
- **根因**：`_review.py:492` 只在 `qa.verdict == "needs_fix"` 时写这条消息，主体是
  `fails` 列表；而 `fails` 是从 `verifications` 里筛出来的。模型回了 `needs_fix`
  却没给任何一条具体 verifications → `fails` 为空 → 消息成了 `0 条未满足`。
- **规则**：**`needs_fix` + 空清单 = 不可执行的判据**。凡是"判定为坏"的分支，
  要么带上具体条目，要么**降级成"结论不可用"**（记 `uncertain` 而不是 `retry`）。
  拦了不说是**比不拦更坏**的状态：它把任务拖进一个没人能修的重试循环。
  （同族见 §44「承诺类字段必须三态可分」—— 这里是"拒绝"这个态没带载荷。）

### 52b. 告警文案会被误读成结论（2026-09-12，我自己犯的）
- **经过**：告警里出现 `designated_reviewer_is_writer:deepseek-flash`，我据此判定
  "**审查员就是写代码那个模型，自己审自己，平台没拦**"，还写进了当天的结论里。
  回源码核对（`_review.py:88-104`）才发现：平台**会把这个写手从指定名单里剔掉**，
  那条告警说的是"你配的名单含写手，我去掉它了"—— 而且当场另一个模型正常审了。
  **不是 bug。**
- **真正该记的是**：`reviewing` 名单里放了实现模型，会让审查席位**从 2 席缩成 1 席**
  （名字里的 writer 被剔走）。这是**配置观察**，不是代码缺陷。
- **规则**：
  1. **告警名不是行为描述。** `xxx_is_writer` 这种名字读起来像"发生了自审"，
     实际可能是"检测到并把这种情况处理掉了"。**看告警名字下结论，和看文档下结论是同一个错。**
  2. 同一天在 §51 上犯的是同族错误：拿 `term_reason` 里出现的字样当失败依据。
     **凡是"字符串里出现了 X"就断言"X 是原因"的推理，都要回判据链核一遍。**

### 53. 记账的调用点，等于记账的覆盖面
- **症状**：项目详情页花费**恒为 `$0.0000`**；用量页最大的一桶是 `_unknown`。
  实测三次完整调研（各 ~77 秒、上万字报告）在用量表上**贡献 0 条记录** ——
  那一小时只有各一发 router 被记下（432×3=1296，正好对得上）。
- **根因**：`record_tokens` 全仓**只有一个调用方** `_task_runner.py:124`
  （调度循环派任务那条路）。而**调研 / 架构 / QA / 安全**四类阶段调用走
  `_safe_dispatch → dispatch`，**一处都不记**。
- **规则**：**问"这条链路记账了吗"，要顺着调用点数，不是顺着函数名 grep。**
  `record_tokens` 存在 ≠ 所有路都调它。新增一条会花钱的调用路径时，
  **必须同时回答"它进哪本账"**；答不上来就是 `_unknown` 桶的新住户。
  连带后果值得记住：`token_budget_total` 拦不住，根子不只是"没接判断逻辑"，
  而是**连"这个项目花了多少"都算不出来** —— 分母是 0，预算拿什么比。

### 54. 记账的名字必须是能查到价的
- **症状**：架构阶段（整条流水线最贵的一段）记了账，**但算不出钱** —— 项目 cost 仍为 0。
- **根因**：委员会那条记录的 `agent_cfg["model"]` 是**合成串**
  `fusion(glm-5.3-flash,deepseek-flash)`（`_dispatch_exec.py:377`），
  计价表里当然没有这个名字 → `unpriced_models` 命中 → 费用按 `None` 跳过。
- **规则**：**记账写的"模型名"这个键，必须是计价表里的键**。多模型聚合体想记账，
  要么**按成员拆成多行**（各自能查到价），要么在计价表里给它一个显式条目。
  写个"看起来像模型名"的合成串，结果是**账面上有这笔、算钱时当它不存在**
  —— 比不记还难发现。

### 55. 超时被杀的任务：痕迹和账一起消失
- **症状**：3 个任务撞 900s 兜底超时被杀，trace 里是
  `changed_files: [] / elapsed: 0 / tokens: 0 / agent_output: ''`
  —— **看上去什么都没干**。而磁盘上 worktree 里文件是写全的。
- **根因**：执行 worker 被 deadline 砍掉时正常返回路径没走到，`_exec` 的收尾
  （追踪改动 / 回填用量）全部跳过；trace 只剩一句 `未执行验证（worker 异常 / 超时）`。
- **规则**：**超时路径必须单独写一套"已知事实"**（改了哪些文件、烧了多少 token），
  别复用"正常运行结束"的收尾。否则最需要排查的场景恰恰**什么都没留下** ——
  而且**最贵的场景不记账**（3 任务 × 900s × 2 路并发，一条不进）。
  同族：`_dispatch_exec.py` 里"回填真实用量"那段注释（委员会成员初稿曾恒记 0）。
- **2026-09-12 全仓扫了一遍同形状，又抓出 3 处（已修，`2af70d4`）**：
  这个坑**不止超时那一条路**，凡是拿 `git status` / 裸 `git diff` 判"有没有改动"的都中招。
  坏法统一：**agent 自己 `git commit`** —— 这命令**不在** `base.py._BLOCKED_COMMANDS`
  里（拦不住），提交后工作区干净 ⇒ "干完了并提交了"和"什么都没干"长得一模一样
  ⇒ `_exec` 那句 `if changed:` 为假 ⇒ **审查 / QA / 安全审计整条被跳过**。

  | 处 | 症状 |
  |---|---|
  | `executors/openai_agent._track_changed_files` | **最要紧**：`baseline_ref` 存了却**全文没引用**，纯跑 `git status --porcelain`。改法直接复用 `claude_cli._git_changed_files` |
  | `executors/anthropic_api._get_changed_files` | `self.baseline_ref or "HEAD"` —— 显式退化到裸 diff，且**静默** |
  | `executors/claude_cli._git_changed_files` | 正路是对的，但基线为空时静默退回裸 diff |

  后两处**降级仍然发生**（没基线确实没得比），改为**出声告警** ——
  静默降级正是这个 bug 的一半：**判据不完整**和**没改东西**长得一样。
- **规则（补一条）**：**降级路径必须出声。** 判据退化可以接受，静默退化不行。
  一条"取不到基准"的告警，比事后从 trace 里反推"为什么这次没有审查"便宜得多。
  ⚠️ 审计同时确认了 9 处"看着像其实没问题"的（方向相反的干净检查、带 ref 的 diff、
  取 ref 本身、纯展示用途）—— **别看到 `git status` 就改**，先问它想回答什么问题。

### 56. 单例在构造时冻住路径 → 测试写进生产（§34 复发）
- **症状**：给阶段调用接上记账后，全量测试一跑，生产 `.qidian/token_usage.json` 里
  出现 `model="test" / project="test-wf" / tokens=1` —— 测试桩数据写进了真实账本。
- **根因**：`TokenBudget.__init__` 里 `self._path = config.QIDIAN_DIR / "..."`，
  路径**构造时冻住**；而 `_budget` 是模块级单例，**导入时**就建好了，
  早于 conftest 隔离 `config.QIDIAN_DIR`。
  **这条写路径一直通着**，只是以前没有测试会走到它（阶段调用压根不记账）
  —— **新接的一条记账线，会把所有"以前不会写这里"的老路径一起点亮**。
- **规则**：
  1. §34 那条规矩对**持有状态的对象**同样成立：路径要么读时现算，要么在写盘前
     比对目录并重新加载（`_sync_dir()`）。冻在 `__init__` 里的单例是同一个坑。
  2. **加一条写路径时，先跑全量测试再看生产文件** —— 你新接的线会点亮别人家的旧灯泡。
  3. `_path` 改成只读属性后，原来靠 `setattr(obj, "_path", ...)` 做隔离的测试会红
     —— 那是**好事**：它在告诉你隔离方式该换了（改成隔离目录，别硬塞路径）。

### 57. 挂起不是异常 —— `except` 拦不住它
- **症状**：跑"字数统计工具"项目，调研阶段**无报错、无告警、无 trace、无模型调用**，
  卡满两分半不动，`phase` 停在 `researching`；外面看像"启动了没反应"。
  同一个项目重跑，**每次都卡在同一处**。
- **根因**：`_run_research` 里的 MAGMA 记忆检索（`pre_search.py:117` → `memory.query`）
  要加载本地嵌入模型，而 `sentence_transformers` **即使模型已在缓存里，也要先联网查
  `huggingface.co` 的 metadata**（看要不要更新）。那域名不通 → `huggingface_hub`
  的 `http_backoff` 退避重试 → **挂死**。
  而整段包在 `try/except Exception: pass` 里（`pre_search.py:126-132`），注释写着
  "记忆挂了不阻塞" —— **挂起不是异常，`except` 拦不住它。**
- **实测证据**（`py-spy dump`，C 帧看不出来、`sample` 也看不出来）：
  ```
  do_handshake → http_backoff → get_hf_file_metadata → hf_hub_download
    → cached_file → from_pretrained → _get_embed_model → _embed
      → find_similar → query → pre_search → _run_research
  ```
  加上 `HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1` 后，**同一份代码 50 秒跑完**。
- **规则**：
  1. **`except` 只能拦异常，拦不住挂起。** 凡是"出问题不该阻塞主流程"的调用，
     光包 `try/except` 是**假保险** —— 必须同时给**超时**。没有超时的兜底 = 装死。
  2. **本地有缓存 ≠ 不联网。** `transformers` / `sentence_transformers` 加载缓存模型时
     仍会做 metadata 检查。离线环境必须显式 `HF_HUB_OFFLINE=1`（或
     `TRANSFORMERS_OFFLINE=1`），否则**首次调用随机挂死**。
  3. **判断"有没有走网络"不能只看 `lsof`。** 失败的连接已经关掉了，`lsof` 里看不见
     —— 我据此误判过一次（"没有对外连接 = 没走到网络"）。要看的是**栈**，不是 socket 表。
  4. 排查姿势：`sample` 只看得到 C 帧，**`py-spy dump` 才看得到 Python 函数名**
     （macOS 上要 root）。这类"卡住不报错"的问题优先上 `py-spy`，别猜。

### 58. "模型没按协议来"被当成了"模型没干活"
- **症状**：任务判失败，理由 `[completeness] 无文件改动`。而它的 `agent_output`
  有 **9599 字**，开头的全文就是它"写的那个文件"（连文件头的注释都在）。
  模型**以为写成功了**。
- **根因**：模型（DeepSeek 系）不回 OpenAI 的 `tool_calls`，而是吐**自家的 DSML**：
  `<` + 两个**全角竖线 U+FF5C** + `DSML` + 同 + `invoke name="write_file">…`。
  平台只读 `msg["tool_calls"]` → 这一整段被当成"模型的普通回答" → 工具一个没跑
  → `changed_files: []` → 判"无产出"。
  **讽刺的是 prompt 里早就写着"不要输出 `<invoke>` 块"** —— 知道这个格式，
  却**只有禁令、没有解析器**。
- **规则**：
  1. **"没按协议来"和"没干活"必须分开。** 前者多数是我们该适配的（厂商协议差异），
     后者才是模型的问题。混在一起处理，就会把**干完了的活**判成没干。
  2. **prompt 里的禁令 ≠ 处理。** 写"不要输出 X"只在你面对**同一个模型**时管用；
     换个厂商、换个版本，X 就回来了 —— 而这次的代价是**整批工作蒸发且无声**。
     凡是"模型可能吐出 N 种格式"的地方，要么都认，要么**认不出就明报**。
  3. **肉眼分不出全角半角。** U+FF5C（全角竖线）和 `|` 在终端里几乎一样。
     第一版正则按 ASCII 写，**拿真实输出一跑才发现对不上** —— 又验证了那条：
     写解析器必须拿**真实样本**验，不能拿自己手搓的形状验。
  4. 现在的处置：`_parse_xml_tool_calls` 两种格式都认（半角/全角、1 根/2 根竖线），
     **认出来就告警**（`xml_tool_calls_recovered` / `_unparsed`）——
     这是"模型没按协议"，不是正常路径，得能查得到。

### 59. 超时 ≠ 停了 —— 执行器线程还活着；且 §55 的"账"那半仍未兑现

- **症状**：任务 A 在 05:30 撞 900s 被杀。**8 分钟后（05:38:47）**告警里还在报
  `collect_changes:[Errno 2] No such file or directory: '.../.探路3-基线-txtstat-worktrees/...'`
  —— 而那个 worktree **在超时那一刻就被删了**（该目录下只剩另一个还在跑的任务的 worktree）。
- **根因**：`concurrent.futures` 的 timeout **只让等的人放弃，杀不掉已经在跑的线程**。
  执行器线程继续跑，继续走 `_track_changed_files`（`executors/openai_agent.py:758`
  的 `subprocess.run(..., cwd=self._cwd)`），而 `self._cwd` 已经不存在。
- **规则**：
  1. **"超时"不等于"停了"。** 写超时处理前先问一句：被超时的那个东西**现在还在跑吗**？
     在跑的话它会继续改磁盘、继续调 API（**=继续花钱**）。
  2. **超时要主动让执行器停**（协作式取消标志、或让 `_cwd` 失效时自检退出），
     不能指望 future 帮你收拾。
  3. ⚠️ 本条的因果链里，"那个还在报错的是任务 A 的线程"是**强推测**，不是直读：
     支撑是两条 —— ① 告警时刻 worktree 目录里只剩任务 B 的；② A 超时比告警早 8 分钟。
     **"线程杀不掉"本身是机制事实**（Python 线程无法从外部终止），不依赖这条推测。

- **§55 的验收结果（2026-09-12 夜 · 探路3 基线轮 · 项目 `1789160560999`）**：
  §55 的规则要求超时路径留下"改了哪些文件、**烧了多少 token**"。实测**只兑现了一半**：

  | §55 要求 | 实测 | 证据 |
  |---|---|---|
  | 改了哪些文件 | ✅ | `changed_files` = 2 个真 `.py`；`agent_output` 写明"另有 1 个提交未合并（`c8bca33`）" |
  | 烧了多少 token | ❌ | trace 里 `token_count: null`，`token_usage.json` **一条没写** |

  同轮**跑完的**那个任务正常记了 `102727` token（05:51:16）⇒ **账本本身是好的，
  是"超时"这条没接上**。别把这条误读成"记账坏了"。

- **已修（2026-09-12 夜）**：

  | 洞 | 修法 |
  |---|---|
  | **账丢了** | 执行器每 dispatch 一次就把**累计**用量落一盘（`_exec._persist_partial_usage` → `.qidian/partial_usage/<id>.json`），超时方 `read_partial_usage` 读回来，再补一次 `record_tokens`。**为什么必须边跑边落**：执行器那个 `total_tokens` 在线程里，而超时方 `fut` 已经 pop 掉 —— 本来就取不到。 |
  | **收尾还在跑** | `exec_result` 回来之后**再查一次取消**（原来只在每轮开头查）。§59 观测到的 `collect_changes: [Errno 2]` 就是这一轮继续往下收尾、去碰已被撤掉的 worktree。 |
  | **抢救失败连账一起丢** | `_salvage_timed_out` 的 `except` 分支原来 `return None` —— 而用量是**先读**的、跟 git 没关系。改成回一个"残的 disp 对象"（能填多少填多少）。**这条改动了一个既有契约**，`test_timeout_salvage.py::test_salvage_never_raises` 的断言跟着从 `is None` 改成"拿得到对象且 token 字段在"，理由写在那个 docstring 里。 |

  ⚠️ **天花板（别当精确值）**：落盘粒度是"每次 dispatch 之后"，所以
  **超时那一刻正在飞的那次模型调用不在里面** —— 报出来的数是**下界**。
  `raw_output` 里明写了"（下界，最后那次调用未计）"，别拿它当账单。
  **一次都没落盘**（第一轮就超时）才如实留 `None` —— `None` 是"不知道"，不是"没花钱"。
  ⚠️ **仍未真机验过**：单测 + 桩测试（`test_exec_run.py` 那条接线检查做过变异验证，
  拆掉接线掉 2 条）都绿，但**要真撞一次 900s 才谈得上兑现**。

- **同族的新发现（2026-09-12 夜，本次顺手撞见的）**：
  `web/app.py:79` 在**模块层**调 `sched_config.ensure_dirs()` ⇒ **任何 import
  `singularity.web.app` 的测试都会在真 `.qidian/` 下建目录**（`tests/conftest.py` 的
  隔离够不着 import 期）。本次加 `PARTIAL_USAGE_DIR` 时实测到：跑 `test_exec_run.py`
  把 `1234567890123.json` 写进了**生产** `.qidian/partial_usage/`。
  ⇒ 加新的 `config.X_DIR` **必须同时**登记进 `conftest.py` 的派生目录元组
  **和** `test_exec_run.py:install_stubs`（它是独立脚本，不走 conftest）。两处都补了。

### 60. 机械检查永远不跑 —— `constraints_checklist` 在真机上一直是空的

- **症状**：项目跑完，`.qidian/projects/<id>.machine-checks.json` **不存在**；
  issues 里只有一句 `verification_skipped: 验收跳过: 架构没产出约束清单, QA/安全审计师都没跑`。
  而同一个项目的 `architecture.constraints` **有 8 条、且 8 条的 `check` 全是
  `{"argv":[...], "expect_exit":0}` 结构**（不是散文）。
- **实测（2026-09-12 夜 · 探路3 基线轮 · 项目 `1789160560999`）**：
  - `project.constraints_checklist` = **`[]`（空）**
  - 同一份 json 里 `architecture.constraints` = **8 条**
  - planning 阶段却报过 `check_coverage {runnable: 8, total: 8}` ⇒ **有人从 `arch['constraints']`
    读到了 8 条，但没人把它落到 `constraints_checklist` 上**
- **后果**：`_run_verification`（`workflow.py:431`）进门第一句就是
  `if not project.constraints_checklist: return 早退` ⇒
  **机械检查一条都跑不了、"信任上限 = 机械证据覆盖的验证面比例"这个分子恒为 0**。
  这不是"某次没跑"，是**这条链现在是断的**。
- **已知的写入点只有一个**：`_workflow_phases.py:567`（在 `_run_execution` 里，
  `project.constraints_checklist = constraints`）。而 `_run_execution` 那一轮**确实跑过**
  —— 证据是它建的 3 个任务 `created_at` = 05:15:32（即 phase 变 EXECUTING 之后）。
- ⚠️ **推测（未定位，别当结论）**：值写进去之后**被别的 save 覆盖**了
  （同一个项目对象被两处持有、后写的那个拿的是旧副本）。
  我把"应该写进去"和"盘上是空的"两头都验证了，**中间那步没抓到**。
- **下一步**（谁捡谁做）：在 `:567` 那句前后各打一条 lineage
  （写之前 `len(constraints)`、写之后读回来 `len(project.constraints_checklist)`），
  跑一轮就知道是"没赋上"还是"被覆盖"。

- **已修（2026-09-12 夜）—— 修的是症状，覆盖源仍未定位，探针留着**：

  先说清**为什么不是去找覆盖源**：全仓 grep 下来，写 `constraints_checklist` 的
  **只有两处** —— `_workflow_phases.py:567` 和 `project.py:212 architecture_redo()`。
  **后者是死代码**（`src/` 里零调用）。所以覆盖只可能发生在 save 那条线上，
  而那是个时序竞态，**读代码定不了案**（文档里那句"两处持有同一对象"是推测，不是直读）。

  所以不赌是谁覆盖的，改成**让症状不可能发生**：新增
  `ProjectState.effective_constraints()` —— **清单为空、而架构里有约束，就用架构里那份**。
  两者按构造应该相等（`constraints_checklist` 就是它的副本，没有任何地方做过 GATE2 过滤），
  所以兜底不会绕过什么确认。

  读取点都换成了它：`workflow._run_verification`（关键的那个，机械检查的闸门）、
  `_exec_context`、`_review`（两处）、`supervisor`。
  ⚠️ 另配一个**容错的模块级版本** `project.effective_constraints(proj)` —— 测试里大量
  `SimpleNamespace(constraints_checklist=...)` 桩没有这个方法，不值得为它把桩全改一遍。

  ⚠️ **兜底时必须出声**：走兜底会 `witness.warn("constraints_checklist_fallback:...")`。
  没有这条告警，覆盖源就**永远查不出来**了 —— 降级可见是刻意留的（§55 补的那条规则）。
  **探针也留着**，但语义变了：读取点探针现在只在"架构里也真没有"时落，
  "清单空但架构里有"由告警来报。

  ⚠️ **仍未真机验过**：单测 5 条（含变异验证：拆掉兜底红 2 条），全量 1124 绿。
  真跑一轮要看的是 —— 机械检查**真跑起来了**（`.qidian/projects/<id>.machine-checks.json`
  存在），以及有没有 `constraints_checklist_fallback` 告警（有 = 覆盖源还在）。

### 61. 同一份"架构 schema"存在两份拷贝，字段漂了 —— `covers` 按构造必丢

- **症状**：重流程（开委员会）跑出来的架构，8 条约束的 `check` **全是**结构化的
  `{"argv":[...], "expect_exit":0}`，但 `covers` **一条都没有**（字段名都不存在）。
  于是 `_machine_checks` 的覆盖率只能报 `no_covers=8` ⇒ "8/8 条需求没人验"。
- **根因**：系统里**有两份架构 schema，内容不一致**——

  | 出处 | 给谁看 | `constraints[]` 长什么样 |
  |---|---|---|
  | `workflow.py:41 _ARCHITECT_CONTEXT` | **架构师席位**的 prompt | `{"type","rule","check":{"argv":[…],"expect_exit":0},"covers":[0,2]}` ✅ 有 covers |
  | `execution_judge.py:205 _ARCH_SCHEMA` | **委员会定稿**的 prompt（`:837 schema=_ARCH_SCHEMA`） | `{"type":"","rule":"","check":""}` ❌ 没 covers，`check` 还是字符串 |

  而**最终架构是融合稿**（`_workflow_phases.py:286` 建席位 prompt → `fuse_architecture_v2`
  → `_V2_FINALIZE`）⇒ 定稿模型**根本没被告知有 `covers` 这个字段**。
- **为什么 `check` 却对了**：定稿 prompt 里带了【各成员原稿】全文，原稿里是结构化的
  ⇒ 它**照抄了形状**。**不是 schema 起了作用** —— 这一点最容易误判成"prompt 生效了"。
- **规则**：
  1. **同一份契约只能有一个定义点。** 两份拷贝必然漂，而且漂的是**其中一份**，
     出问题时你盯着 prompt 找原因，根因在另一个文件里。
  2. **判"prompt 有没有起作用"，要挑一个原稿里没有、只有 schema 里有的字段看**——
     拿两个来源都有的字段判，会把"抄来的"当成"要求生效了"。
- **已修（2026-09-12 夜，`6110b13`）**：`_ARCH_SCHEMA` 的 constraints 补上 `covers` 和结构化
  `check`，`_V2_FINALIZE` 补规则 6 说清 covers 语义。⚠️ 它是 `.format()` 模板，
  **字面花括号必须写 `{{ }}`** —— 漏了转义 18 个 fusion 测试当场全红（`KeyError: '"argv"'`）。
- **留了漂移检查**（`tests/test_scheduler/test_machine_checks.py::TestArchSchemaCoversContract`）：
  两份 schema 的 constraints 字段集必须一致。**光修一次不解决规则 1** ——
  下次谁再改其中一份，这条会红。变异验证过：从任一份删掉 `covers` 它立刻失败。
- ⚠️ **仍未真机验过**：`covers` 补进去了，但"定稿模型会不会照写"只有真跑一轮才知道。

### 62. 模型池只剩 2 个时，裁判类角色的告警会全部常亮 —— 一个兜得住，一个兜不住

- **症状**：一次正常跑动里，`extractor_stays_member` / `fusion_self_judge` /
  `designated_reviewer_is_writer` **每轮必报**，全是"自己评自己"那一族。
- **根因**：激活池只有 2 个模型（`deepseek-flash`、`glm-5.3-flash`），而：
  - `planning` 恰好用满这两个 ⇒ **委员会 = 整个池**
  - `reviewing` 名单也含这两个 ⇒ **写手必然在审查名单里**
  - 提取员的兜底表 `_V2_EXTRACT_FALLBACKS = ("glm-5.2", "deepseek-v4-pro")`
    **两个都是被停用的模型** ⇒ `_usable` 全 False ⇒ **换不动**（表跟池**零交集 = 表失效**）
- **两种结局，别混为一谈**：

  | 告警 | 机制的动作 | 结论 |
  |---|---|---|
  | `designated_reviewer_is_writer` | **剔掉写手，换另一个来审**（`_review.py:86`） | ✅ **兜住了**，告警在 2 模型池下是**常亮噪声** |
  | `extractor_stays_member` | 想换、**换不动**，留在委员会里 | ❌ **真自审**，且**每轮都发生** |

- **规则**：
  1. **写死一张"备选表"就必须保证它跟"当前可用池"有交集**，否则它是一条**永远走不到的兜底**
     —— 而告警还照报，让人以为"系统在努力换人"。
  2. **告警常亮 = 判据跟配置脱节**，不是"最近问题多"。真出问题时会被淹在噪声里。
  3. ⚠️ 影响面（要用户拍，别自己决定）：提取员真自审 ⇒ **"多视角碰撞"在提取环节已打折**
     ⇒ 而 P1 要回答的正是"碰撞比单模型强多少"，**这个折扣会污染 P1 的结论**。
     扩池 / 让兜底表跟激活池联动 / 或接受并写进文档 —— 三选一。
- ⚠️ **上面那个"让兜底表跟激活池联动"是错的 —— 它早就实现了**（2026-09-12 夜更正）：
  `execution_judge.py:627` 就是 `next((m for m in _V2_EXTRACT_FALLBACKS if m not in members
  and _usable(m)), "")`，一直在查激活池。病根不是"没联动"，是**表里那两个模型全被停用
  ⇒ 交集为空**。所以当时的"三选一"其实是**二选一**。教训：**列选项前先读代码确认哪个选项
  还没实现** —— 把一个已经存在的机制当选项列出来，会让用户以为它有得选。
- **已修（2026-09-12 夜，用户拍板扩池）**：`any` 池加 `glm-5.2`（`POST /api/agents`），
  `extract` 阶段改指 `glm-5.2`（`PUT /api/phase-models`）。这样提取员**不在 planning 委员会里**
  （委员会由 `phase_models.for_phase("planning")` 显式锁死为那两个，**扩池不会把它带进委员会**
  —— 这是这次扩池能成立的前提，改之前先确认过）。
  ⚠️ **只做了结构级核对**（解析出 `glm-5.2`、不在委员会里）；`extractor_stays_member`
  是否真的不再报，要看下一轮 `alerts.jsonl`。**没验过就别当已解决。**


---

# 三、演化史

# 奇点演化史

## v0：探索期（2026-05）

| 时间 | 事件 | 方向 |
|------|------|------|
| 5/28 | 奇点体系初始化 | 学习 Python + AI Agent 概念 |
| 5/31 | 项目1完成(任务启动器) | Python 入门：while/if/break |
| 6/1 | 项目2完成(能量日记) | 列表+文件读写+for循环 |

## v1：Agent Bus（2026-06-12~13）

| 时间 | 事件 |
|------|------|
| 6/13 | Agent Bus v0.1 — 你+GPT+Claude 共享任务板 |

**设计思路**：用一个 markdown 文件做共享任务板，多个 AI 协作。

**问题**：手动协调，没有调度。

## v2：奇点调度平台（2026-06-18）

| 时间 | 提交 | 内容 |
|------|------|------|
| 6/18 | `ced5d93` | 奇点 Agent 调度平台 v3 — 多模型并行 + DAG 分解 + 合并队列 |
| 6/18 | `0cd1bfa` | openai_agent.py — 通用 Agent Runtime，不依赖 Claude Code |
| 6/18 | `2ff8965` | QA Gate 接入 + 前端奇点奏报面板 |
| 6/18 | `537593c` | API 库 + 模型注册表 + 项目级 agent 编组 |
| 6/18 | `26da7f2` | MAGMA 记忆注入 agent prompt |
| 6/18 | `645f6ee` | D层可切换 — glm-5.2 注册为D备选 |
| 6/18 | `ee4f13e` | 理论差距审计报告 |

**设计思路**：E/E+/D 三层模型分级 — 便宜模型(E)做简单任务，中等(E+)做代码，强力(D)做架构/设计。

**核心模块**：orchestrator 调度循环 + dispatcher 模型分发 + planner 任务拆解 + MAGMA 记忆。

## v2.1：架构审计+优化（2026-06-20~22）

| 时间 | 内容 |
|------|------|
| 6/20 | ARCHITECTURE.md 完成 — 30 commits, 84 tests green |
| 6/20 | 20个 bug 修复（命令注入/worktree泄漏/线程安全/前端500） |
| 6/20 | Self-Fusion + DCPM System2 + ConstructContext + HyDRA路由 落地 |
| 6/22 | Opus Phase 2 审计完成，前端框架切 React |

**关键决策**：
- 文件持久化而非数据库（零依赖、可grep）
- worktree 隔离（git 原生，类 Docker）
- 循环依赖用延迟导入破（刻意设计，不是 bug）
- 前后端 SSE 事件驱动

## v3：废弃三层，改两档+六阶段（2026-06-24~28）

| 时间 | 内容 |
|------|------|
| 6/24 | production-flow.md — 六阶段生产流基准 |
| 6/24 | 前端框架切 React 完成 |
| **6/27** | **`d3c117e` 砍 AI 互审** —— 删 `conductor.py`(AI自动推进门,303行) / `judge_monitor.py`(AI裁判监控) / `_judge_and_profile`+`_run_fusion`+`_do_review` 链；`run_test_fix_loop` 直通 GATE3。**⚠️ 原因未记录，见下** |
| 6/28 | 专家团队架构.md — 13角色 + 两档模型 + 多模型碰撞策略 |

**为什么废弃 E/E+/D**：三层太机械。人的需求不是一个层级能扣进去的。改为两档（便宜/强力），按阶段分工：
- 定义/实现/交付 → 便宜模型
- 架构/审查/验收 → 强力模型

**为什么砍 AI 互审 —— 原因不明（2026-09-11 追查后的结论）**：
提交信息只列了删什么，一个字没提为什么；本文件当时也没记；`docs/` 下没有解释。
**能还原的只有上下文**：这是 v3 六阶段重构的一部分，方向是
「**AI 自动推进 + AI 裁判**」→「**固定六阶段 + 三道人工门**」。
**推测**（无证据）：AI 当裁判判自己容易偏（代码里 `_warn_same_model` 引的
MAD/EMNLP 2024 就是这条），且"AI 决定能不能过"是黑盒、人看不见它凭什么放行。

> **留这条的意义**：三个月后问"审查为啥删了"，仓库答不上来——包括自己。
> 一个砍掉 300+ 行、改变整条流水线性质的决策，**没留下为什么**，就是代价。
> 以后再砍大件，至少写一行。

**后果（2026-09-11 评估）**：审查层从此是空的 —— AI 自审删了、替代品没装，
交付质量全靠人在 GATE3 兜。而"能自动验证到多大，才能自动推进到多大"，
所以这同时是**能力天花板真正的约束**（见 `生产流现状.md` §四）。

**为什么 13 角色**：借鉴 workbuddy 专家协作，单 AI 写代码没架构/没测试/没安全。角色按软件开发流程分工，阶段内并行。

**为什么多模型碰撞只在 4 个角色**：碰撞不滥用。只在选错代价大的地方（系统架构/AI架构/安全审计/研究员）用多模型。其余单模型照规格施工。

## v4：产品化打磨（2026-06-28~07-02）

| 时间 | 内容 |
|------|------|
| 6/28 | 智能体角色可配 — 点卡片选角色 |
| 6/28 | 模型目录/智能体分离 |
| 7/2 | Config 全中文化 |
| 7/2 | 前端评级 B+→A（错误处理/类型/组件拆分/CSS/测试） |
| 7/2 | 8项 UX 问题修复（Skills自动开/新建项目/Toast/格式化trace） |
| 7/2 | Ref：Zcode 界面风格（纯文本+底部输入+侧边栏） |

## v4.1：智能优化（2026-07-03）

| 时间 | 提交 | 内容 |
|------|------|------|
| 7/3 | `5659ffb` | Router 从 30 行正则 → LLM 分类 (`_llm_classify` + `RouteResult` 缓存) |
| 7/3 | `5659ffb` | Observer 系统提示词改"直接干活"：用户说要做东西 → 必须调 create_task，不先打招呼 |
| 7/3 | `5659ffb` | Dispatcher `pick_agent`/`pick_agent_fallback_chain` 加 route_learner Hedge 权重排序 |
| 7/3 | `5659ffb` | Memory `index_task` 加 `_infer_mem_type()` 自动分类 + `mem_type` 过滤查询 |
| 7/3 | `5659ffb` | 文档清理：18 个过期文档 + 历史会话删除，34→9 文件 |
| 7/3 | `5659ffb` | 新增 CLAUDE.md 硬规则：先读蓝图/对比标注/禁止编造 |
| 7/3 | — | Router 缓存永不过期 bug 修复：`matched_signals` 存关键词当时间戳用 → 加 `cached_at` 字段 |
| 7/3 | — | pause/resume：`_check_paused()` + `PAUSE_DIR`，GATE 人审可暂停任务等反馈 ⚠️**后续订正(9/11)**：GATE 那部分始终没接上 —— GATE 实际靠"项目停在 gate 阶段等人批"实现，从不读写 PAUSE_DIR。这套机制最后只服务「逐步确认」和手动暂停。详见 `docs/项目速查.md` |
| 7/3 | — | 嵌入模型默认启用：`_ntilc_filter` 用 cosine 相似度匹配 skill + `_get_embed_model` 默认 on |
| 7/3 | — | Observer 状态增量推送：`_build_status_context` hash 缓存，状态无变化复用 |
| 7/3 | — | MCP server：`observer/mcp_server.py`，stdio JSON-RPC 暴露 11 个工具 |
| 7/3 | — | 4项修复：Observer多轮历史注入、GATE统一confirm_gate路径、架构线程池独立、pytest发现修复 |
| 7/3 | — | 端到端实测：Observer对话→create_task→Router→Dispatch→_exec，3模型委员会碰撞验证通过 |

**已验证**：3 模型委员会碰撞（deepseek + kimi + glm 并行 → 合成器融合，155s 产出统一架构）

## v4.2：流水线串通 + 封存（2026-07-03~04）

| 时间 | 内容 |
|------|------|
| 7/3 | 6阶段全流程串通：定义→GATE1→架构→GATE2→实现→集成→审查→GATE3→交付→DONE |
| 7/3 | 3模型委员会碰撞验证通过（deepseek+kimi+glm并行+合成器融合） |
| 7/3 | Observer 实测通过（对话→create_task→执行） |
| 7/3 | 276 测试通过（QIDIAN_SKIP_EMBED=1） |
| 7/3 | 项目封存：修 Dockerfile、清垃圾文件（645文件/56万行）、个人知识移出 |
| 7/4 | 目录整理：src/todo_cli残留、.DS_Store、运行时数据清理 |

**封存状态**：`docker compose up` 或 `python3 -m singularity.web.app` 即可恢复。不再开发。

## v4.3：重启开发（2026-09-04 ~ 09-09）

> 封存两个月后重启。核心转向三条线：**砍复杂度**（16 角色→3）、**修流水线**（executor 8 bug 链、超时治理、QA 路径 bug）、**能力数据自动化**（零手写 + 一键基准）。

| 时间 | 提交 | 内容 |
|------|------|------|
| 09-04 | — | 清除泄露的 PAT，上传 GitHub `tokens-001/singularity` |
| 09-06 | 多 commit | 项目迁英文路径 `~/projects/singularity`；bug #1-#5 修复（hello.py 5s done）；能力快照与模型库分离；成品路径 `~/qidian-projects`；上线形态定自部署 |
| 09-07 | `e90aec7` | 角色 16→3（见 [角色体系重构.md](角色体系重构.md)） |
| 09-07 | `9b04c1d` 等 | flow A/B 合并、软质量硬门槛、对账升级 LLM、severity 阈值调优 |
| 09-07 | `75b0e57` | 需求核对优化：审查 prompt 加需求完整性，治「产出太简单」 |
| 09-07 | `ddcb5c1` | 审查路径 3 叠加 bug（NameError + JSON 解析坏 + 需求核对缺失），审查打回从失效到双向正确 |
| 09-07 | — | 架构验证实验：单文件 todo 奇点更朴素更慢；大项目流水线跑不通；委员会 vs 单模型略胜 |
| 09-07 | `049ec04`+`7ae49ec` | executor 写文件链 8 bug 修复（见 [executor写文件链修复.md](executor写文件链修复.md)） |
| 09-07 | `ad3d633` | 执行超时治理：max_turns 尊重 registry + max_tool_turns 3 + 测试通过即停 |
| 09-07 | `9bca7cd` | QA 硬规则路径 bug：no-delete-tests 用主仓库根查 worktree 文件，误杀 test_*.py |
| 09-08 | `d7f08bc` | scan 通配：_is_major_model 白名单精确版本号滞后，改系列+数字通配 |
| 09-08 | `f6dc646` | 一键基准：模型能力字段零手写 + 跑基准自动定档（见 [执行超时与模型能力自动化.md](执行超时与模型能力自动化.md)） |
| 09-08 | `a301cbb` | 安全审计 + bug 修复：P0 安全 4 项 + P1 功能 8 项（QA gate 恢复）+ P2 数据完整性 5 项（见 [审计修复-20260908.md](审计修复-20260908.md)） |
| 09-09 | `28276bd`→`a036f3e` | **flash 全量扫描 + 修复**（16 commit，DeepSeek v4.1-flash 完成）：3 P0（权限 profile 增删必500 / 回滚必500 / transition 无终态保护）+ 12 P1 + 死代码 41 定义（见 [死代码清单-20260909.md](死代码清单-20260909.md)） |
| 09-09 | — | **QA 门禁整条接线**（本轮核心）：原 finalize 先标 DONE 再跑 QA → 门禁形同虚设。改：QA 前移 merge 前（硬证据 fail 清 merge_request）、DONE 挪 QA 后、supervise 接 repo_root、审查置信度接回决策。端到端跑出 worktree repo_root bug 并修 |

**核心结论**：多视角碰撞是唯一证实的核心价值；角色约束增量小；能力评级纯展示（调度不看），改成一键基准自动定档，告别手写维护。

**09-09 取舍定案**（flash 扫描修复的遗留，已拍板）：
- **QA 门禁接线**已打桩（拦）+ 真跑（不误拦）双重验证。supervise 是机械检查（非 LLM），真 LLM 在 check_requirement_conformance；checklist 走 _review_requirements → multi_model_review。
- **`_check_completeness` 降级为「只记录」**：接受。机械子串匹配不可靠，checklist 有 LLM 兜底。
- **偷懒软信号（文件数/无测试）不拦合并**：接受，避免误伤「单文件精准修复」「不需改测试的任务」。
- **9 个「仅测试引用」API 保留**：有测试覆盖，删了净减覆盖，保留。
- **死代码清单定位**：清理记录 + 恢复手册，下次删前必须重新 grep 复验，不得照单删。

## v4.4：真机验证 + 审计 + 用量计价（2026-09-10 ~ 09-11）

三天里从"跑通"走向"跑对"，主线是**把静默失败一个个挖出来**。

### 09-10：融合机制重设计 + 真机验证
- **融合 A/B 三轮反复，最终定案「融合有效」** —— 前两次的"+7.8"/"−3.3"都是假象，
  根因是**输出被 `max_tokens` 腰斩**。分数线与截断位置完美单调。
  详见 `docs/融合实验-20260910.md`、`docs/融合机制重设计.md`。
- **教训入档**：max_tokens 是保险丝不是油门；对照被截断会把结论推向反面（两个方向都踩过）；
  数字异常时先读原始产物别只看分数；测试输入必须和生产同形。
- 修：v3 合并路径漏做三件归档（经验/用量/路由学习）；提取员候选被委员占满；
  兜底合成没禁工具（往奇点自己的仓库写文件）；拆不出任务导致项目无声永久卡死。
- **四条子系统首次真正写盘** —— 奇点第一次记下自己的用量（8797 tokens）。

### 09-11 上半场：全量审计
- 30 个提交 / 71 文件 / 测试 479 → 557。**门禁整层失效**（审查取 diff 用裸 `git diff`，
  而改动在 `validate()` 之前就被 `commit_wt` 提交了 → 恒空 → 五道门禁全静默放行）。
- 死代码：`_collect_ready_pending` / `start_all` / `probe` / `blog/` 子包 / `_cache.py`。
- 详见 `docs/审计报告-20260911.md`。

### 09-11 下半场：用量统计与计价
- **诊断**：用量早就按模型统计了，**钱是编的**（8 模型写死表 + 静默 $0.50 兜底）。
  全仓无真实价格数据。顺带清掉 5 处同类编造。
- **两个决定**：单价存独立文件（模型表会被"跑基准/扫描导入"整行重建）；
  费用读时现算（不需要迁移，补价后历史一起变对）。
- **补全流程记账**：6 条调用大模型的路原来只记 1 条。观察者/任务分类/架构融合/
  记忆整合/目标循环全部补上。⚠️ 流式要申请 `stream_options.include_usage` 才拿得到用量。
- **按天历史**：`usage_daily.json`，折叠算法必须**按天各自归位**（只重算今天会每过午夜重复计）。
- **模型轮换加了又撤**：那个列表本质是"主力 + 备用"，不是 bug。
- 详见 `docs/用量统计与计价-20260911.md`。

### 09-11 晚场：静默失败治理（外派答卷 + 收口）

**一天之内挖出六个缺陷，没有一个报错。** 共同形状：系统没做或做错了，
但从外面看一切正常。

- **诊断**（外派给外部模型做的设计题，材料见 `docs/静默失败治理-外派材料.md`）：
  > 事实从发生到被看见要经过若干次交接（执行→状态、状态→序列化、序列化→界面、
  > 过滤前→过滤后）；**每次交接都靠约定维持、没有任何对账**，而所有交接点的默认行为
  > 都是把"拿不到"解释成"没有/正常"。**静默失败 = 一次断掉的交接 × 一个良性的默认解释。**
- **修的六个**：验收跳过不留痕 / 计数器单向棘轮（GATE2 永久死锁，"点通过永远弹回"）/
  GATE3 看不到 QA 报告 / 项目状态文件被写坏后凭空消失 / 点文件定位失败 /
  扫描静默少一个模型。
- **另外三处**：provider 级熔断降到**模型级**（一个模型欠费不该连坐同厂商其他模型）；
  **别名对账**（厂商把旧模型名路由到新模型时，委员会不能拿它占两个席位）；
  融合提取员走了**死默认值**（621 秒里有 170 秒白烧）。
- **架构收口**：`ProjectState.set_phase()` 成为阶段流转唯一入口，**21 处散点赋值**全部改过去。
  以前两套驱动各写各的 —— `run_phase`（人手）不认识 `INTEGRATING`/`DELIVERING`，
  而那正是 `orchestrator`（自动）在驱动的两个；出事时落盘里只有最终 phase，没有轨迹。
- **立法**：防御模式 43 → 46（#44 承诺类字段三态可分 / #45 计数器棘轮 / #46 确定性 tmp + 线程锁）。
- **图纸**：新增 `docs/生产流现状.md` —— 对着源码写代码实际在干什么，
  并把 `production-flow.md`（6/24 的 v3）标记为"落后三个大版本，只当设计意图读"。

⚠️ **仍有六个窟窿开着**，见 `docs/生产流现状.md` §四。

### 09-12：探路2 复跑 + 论文/分析收口

**一夜 17 笔提交，测试 862 → 1030。** 起因是重跑一轮真实的探路轮
（项目 `探路2-文本统计CLI`，5 个任务）。结果 **1 成 4 败 —— 但真正"不会做"的：零。**
四种死法全是机制：

| 死法 | 任务 | 真相 |
|---|---|---|
| **超时吞掉已完成的工作** | T2（+T3） | 干完了、提交了；抢救逻辑只跑 `git status` → **看不见已提交的改动** |
| **格式认不出** | T3 首次 | DeepSeek 吐自家 DSML，平台只认 OpenAI 的 `tool_calls` → 整段被当普通回答 |
| **只说不做** | T5 | 写了份"实现完成"的交付报告，**一个工具都没调**（平台拦住了 —— 这个兜底有效） |
| **报错说不清** | T4 | "无可用 runner" 的真相是"**没找到测试文件**" |

**产物没丢**：T2 那份是能跑的完整实现 + 37 个测试，实测与 `wc -lwmc` 分毫不差
（存 `~/项目/项目仓库/探路2-salvage/`）。**失败的不是能力，是机制。**

**当夜修完**（详见 `docs/防御模式.md` §57/§58 与各提交信息）：

- **§57 挂起不报** —— `SentenceTransformer` 加载**本地**模型时仍联网查 metadata，
  断网就退避重试**挂死**；而**挂起不是异常，`except` 拦不住**
- **DSML 解析** —— 半角/全角、1 根/2 根竖线都认（prompt 里早有禁令，但**知道 ≠ 处理**）
- **§55 三处** —— trace 侧 / **记忆侧** / `crossover_review`（**fail-open**：看不见改动还判"通过"）
- **三条"说不清"** —— runner / 安全审计 / 目录不存在
- **论文四步：各有落脚点，但都是简版** —— 存全过程 / 按阶段切 / 分层抽象 / **检索后改写**
  ⚠️ 2026-09-12 复核（对着 arXiv 2607.29658 **原文**逐条核过）：原先这里写的是"**四步齐**"，
  **那是夸大**。四步里"分层抽象"和"按阶段切"离论文的机制差得远 ——
  论文是**迭代**分组+抽象、做到根节点只剩 2 个，节点带**四样**（意图/关键动作/适用条件/常见坑），
  且**每个阶段各建一棵树**；我们这边是**一次调用吐三层**（concrete/strategy/principle），
  **没有分组这一步**，"按阶段切"只是按工具名分三类各出一句统计
  （`_memory_graph.py` 的 `split_stages`/`stage_summary`）。
  降级原因代码里写了：样本量撑不起论文那种离线批量（479 条 ≈ $211）
  → `_memory_consolidator.py:153-158`。**效果没验证**（样本太少，验不出来）。
- **分析 P2–P6 齐** —— 验收即代码、机械可验率（含需求侧覆盖率）、预算真实化 + 周期对账、
  流程复利账本 + 纪律表自动累积、防御模式当找茬清单（P5 的变体）

⚠️ **绝大多数没在真机验过** → `docs/待验证清单-20260912.md`。

**同时清了一次记忆**：`qidian-bug-backlog` **629 → 100 行** —— 它自己头部写着
"历史进 `docs/`"，却堆了 47KB 的日期流水账。历史本来就该在这儿。

### 跨轮次的稳定结论
- **静默失败是这个代码库的主要 bug 模式** —— 查这类问题先看 `.qidian/heartbeats/` 里的 `warn:` 行。
- **报告会撒谎，当前源码不会**（审计报告的文件:行号几乎全错）。
- **告警日志要连时间一起看** —— `alerts.jsonl` 是累积的，翻旧账会误判成现存 bug。
- **厂商的模型名是别名游戏**：`/v1/models` 只列主推名，旧名会被静默路由到新模型
  （DeepSeek 2026-09-14 起把 `deepseek-v4-pro` 全部路由到 V4.1-Flash）——
  **只有响应体的 `model` 字段说实话**。
- **配置里的死键比没配更糟**：你以为配上了，实际走的是代码里的硬编码默认值，
  而且没有任何提示。（`fusion.toml` 的 `judge_model` + `call_model` 都是 v1 残留，
  v2 唯一读的 `extract_model` 反倒不存在。）
- **计数器必须同时定义清零事件**，否则它就是个棘轮 —— 到阈值就动作、只增不减，
  能把人永久锁死（人工介入即兜底，配额随之恢复）。
- **改状态的入口超过一个，轨迹就一定会丢**。散点赋值时出事只能看到最终态；
  收成单一入口 + 自动留痕，死循环才读得出来。
- **同一个形状会重复出现，而且不止一次**。"只看得见未提交的改动"这个坑，在
  `validator`（09-11）、`orchestrator` 抢救（09-12）、`crossover_review`（09-12）
  **各出现一次** —— 修一处时必须问"**同形状还有几处**"，判据在**调用点**不在实现点。
- **空返回比报错更危险**。"思考吃光 `max_tokens` → `content` 为空"一夜咬了三次
  （分层抽象 700 / 检索后改写 1200 / 因果判断 200），每次的样子都是"**模型没答**"
  而不是"配置错"。**症状是空串的那种失败，必须留痕。**
- **"登记"不等于"做掉"**。把问题写进待办那一刻会产生"已处理"的错觉 —— 09-12 一夜
  提了几十件事、丢了大半。根因不是任务多，是**按"最新刺激"排序、且没有一个外部清单**。
  解法是机制的（`~/.claude/OPEN.md` + `UserPromptSubmit` hook 每轮把清单念出来），
  不是"我会注意" —— 那一夜"我会注意"失效了两次。


---

# 四、审计报告（2026-09-11）

# 全量审计报告（2026-09-11 凌晨）

> **口径（审计当时）**：`src/` 94 个 py 文件 / 23,873 行 + 前端 `web/frontend/src`（2,781 行）。
> **09-11 晚实测**：88 个 py 文件 / 25,538 行 + 前端 3,836 行 —— 审计当天后来又删了
> 死代码、加了测试与功能，**两个口径都对，只是时间点不同**（审计口径保留不改，它是历史记录）。
> **方法**：9 路并行只读审计（按模块簇分工）+ 1 路独立死代码扫描 + 本人对高优先项的二次复验。
> **本次未改任何代码。**
>
> **复验标记**（读法很重要）：
> - ✅ **已复验** = 我本人读过完整调用链 / 跑过，机制确定
> - ⬜ **待复验** = 子审计员报告，我**没有**独立复验，采信前需自查
>
> 子审计员自己也犯了 4 类误判（见 §6），所以 ⬜ 不等于假，只是**未经第二双眼睛**。
>
> ---
>
> **⚠️ 每条标记必须带「核于」锚点（09-11 晚立的规矩）**
>
> 立的直接原因：当天 21:00–22:30 的一批修复把本节大量 ⬜ **修掉了，但标记没翻**。
> 结果同一天坑了三次 —— 外派材料 §8#3、§8#4，以及外部评审据此断言"DCPM 仍在空转"
> （实测是好的）。**标记不带锚点，读的人分不出它漂没漂。**
>
> 所以格式定为：`✅ … + 核于 <文件:行 或 命令>`。
> 锚点是**复验当时**的行号，还会漂 —— 行号只是线索，**判据是那行代码本身**。
> 反过来也成立：读的人发现锚点对不上，就该重新核，而不是采信标记。

---

## 一、P0 —— 已复验，机制确定

### P0-1 ✅ 审查/门禁看到的是**空 diff** —— 5 道门禁形同虚设

**哪里坏了**：`_exec.py:378` 调 `_process_planner_or_merge`，该函数在 `_exec.py:182`（v3）
/ `:190`（v2）**已经把 worktree 的改动 commit 了**；而 `validate()`（`:406`）和
`run_post_exec_checks()`（`:426`）在这**之后**才跑。

**为什么是空的**：审查取 diff 用的全是**裸 `git diff`**（无基准 ref）：
`_review.py:82` / `:376` / `:425` 是 `git diff <file>`，`validator.py:229` 是
`git diff HEAD -- <file>`，`validator.py:268/272` 是 `git diff [--stat]`。
commit 之后这些**一律 0 行**。

**影响**（按 `_review.py:84` 的判据 `line_count < 50`）：
- 单文件任务 → `_is_trivial_change` 判 trivial → **项目测试 + 多模型审查 + QA 约束验收
  + 需求符合性 + 安全审计，5 道全跳**，报告照样写 `delivered`
- 多文件任务 → `multi_model_review(diff_only=True)` 拿到空 diff → **一个模型都不调**就返回
  `{"issues":[],"models_used":[]}`；QA/安全审计拿到 `(无 diff)`
- `_hard_diff_rules` 的"移除了 N 处鉴权"检测依赖 `git diff HEAD` → 永不为真

**唯一还硬的**是 `supervisor.supervise`（机械检查，不吃 diff）。

**为什么测试没抓到**：`tests/test_scheduler/test_review_gate.py:229` 把 `subprocess.run`
monkeypatch 成返回**假 diff** —— 单测结构上不可能发现这个 bug。

**未做的验证**：我没跑端到端流水线去**观察**一道门禁在空 diff 上放行。
机制由读码确定（顺序 + 命令 + commit 三件事都单独核过），端到端**未观测**。

---

### P0-2 ✅ `witness` 幽灵名 —— **错误处理路径本身崩溃**（16 处调用 / 0 处定义）

`witness` 全仓**没有任何定义**（`grep -rE "^(def|class) witness"` = 0），却被 3 个模块
调用 16 次。危害不是"少了一条告警"，而是**本该优雅降级的 except 分支自己抛异常**，
把降级变成崩溃，**并掩盖原始错误**。

逐函数核实结果（顶层无 import、是否有函数内 import、**行序**）：

| 位置 | 函数 | 局部 import | 结论 |
|---|---|---|---|
| `_worktree.py:44` | `_anchor_ref` | 有(L43) | ✅ 正常 |
| `_worktree.py:80/103/111` | `cleanup_task_artifacts` | 有(L69/110) | ✅ 正常（`_rm` L80 是嵌套闭包，外层能捕获） |
| `_worktree.py:131` | `_maybe_create_worktree` | 有(L130) | ✅ 正常 |
| **`_worktree.py:136`** | `_maybe_create_worktree` | L130 在 try **体内** | ❌ **`UnboundLocalError`** —— 异常若发生在 L122-124（早于 L130），名字从未绑定 |
| **`_worktree.py:150`** | `_cleanup_wt` | **无** | ❌ **`NameError`**。它在 `_exec.run()` 的 `finally`（`_exec.py:507`）里调用 → worktree 留在磁盘（泄漏）+ 任务被记成"worker 异常" |
| **`pre_search.py:104`** | `pre_search` | L127 才 import | ❌ **`UnboundLocalError`** —— 函数内有 import 就使名字**整个函数作用域**变局部，L104 早于 L127 |
| `pre_search.py:128/130/177/179` | `pre_search` | 有 | ✅ 正常 |
| **`_dispatch_skills.py:85/107/152`** | 三个函数 | **无** | ❌ **`NameError`**。L85/107 把"降级 `return [],"",{}`"变成抛异常；L152 让"需审批"通知永远发不出去 |

**这条的教训**：`_worktree.py` / `pre_search.py` 里那些"有局部 import"的写法是**对的**，
说明有人知道要 import —— 只是**漏抄了几处**。依赖"错误路径"才能触发的代码，
测试覆盖率天然是 0。

---

### P0-3 ✅ 集成合并与交付操作的是**奇点自己的仓库**，不是项目仓库

`orchestrator.py:502`（`_run_integration_merge`）与 `:558`（`_run_delivery`）都写
`root = str(config.PROJECT_ROOT)`。而 `config.PROJECT_ROOT = Path(__file__).parents[3]`
= **本仓库 `projects/singularity`**。同一个文件里其他地方取的都是项目仓库
（`repo_root_for(t)`，见 `:105/:144/:183…`）。

**症状**：
- 集成阶段 `git status --porcelain` 检的是奇点的工作区 → 你正在改奇点自己时，
  项目被无理由打回 `EXECUTING`，两次后升 GATE2
- 交付阶段给**奇点仓库**打 `release/<项目id>-<时间>` 标签，把奇点的 `pyproject.toml`
  当项目产物（永远报 "python-package"）

**旁证**：`_run_delivery` 唯一出口是 `return True`（`orchestrator.py:607`），失败分支
（`:360-364` "交付失败, 需人工处理"）**是死代码** → 用户永远看到"交付完成!"。

---

### P0-4 ✅ 调用**不存在的函数** —— 3 个入口必炸

| 入口 | 证据 |
|---|---|
| `POST /api/projects/<id>/auto` | `_api_projects.py:243` 调 `proj_mod.advance_phase` —— 全仓 **0 定义**（上一轮删掉的 `advance_to` 没留替代）。子审计员实跑复现 → 500 + HTML 报错页 |
| `POST /api/conflicts/<id>/resolve` | `_api_memory.py:63` `from .merge import resolve_conflict` —— 该名全仓 **0 定义**；真实现是 `MergeQueue.resolve()`（`merge.py:207`），而它自己 **src+tests 零调用** |
| `PUT /api/token-budget` | `_api_monitor.py:58` `from ._token_budget import set_budget` —— `set_budget` 只是 `TokenBudget` 的**方法**，不是模块级函数 |

三处全仓 grep 定义数均为 **0**，机制确定。

---

### P0-5 ✅ CLI 三处 `NameError`

| 命令 | 位置 | 缺的名字 |
|---|---|---|
| `scheduler rollback <id>` | `_cli_tasks.py:130/135` | `snap_mod`（在 `__main__` 命名空间，函数取不到） |
| `scheduler memory query/traverse` | `_cli_memory.py:55/72` | `_parse_concurrent`（只在 `__main__.py` 定义） |
| `python _cli_projects.py` | `_cli_projects.py:236` | `main`（本模块**没定义**） |

`rollback` 那条只在**快照真实存在**时才走到，即"最需要它的时候"炸。

---

## 二、P0/P1 —— ✅ 已复验（2026-09-11 晚逐条对源码）

> **第二次复验（09-11 晚）**：本节 38 条已逐条对**当前源码**复核 —— **36 条已修，
> 2 条部分修**（`execution_judge` 的 adopt 门槛、`model_registry` 的字段覆盖），0 条未修。
> 复验方式：每条的标记后面都跟「核于」——**它复验时锚定的那行代码**。
>
> **为什么加这个**：本节原来标的是 `⬜`（未复验），但当天晚些时候这些条目**被照着修掉了**
> 而标记没翻。结果同一个病当天坑了三次：外派材料 §8#3、§8#4、以及外部评审据此说
> "DCPM 仍在空转"。**标记不带锚点，读的人就分不出它漂没漂**——所以从此每条要带锚。
>
> ⚠️ 下面每条的「核于」是**复验当时**的行号，还会漂。行号只是线索，判据是那行代码本身。

### 门禁 fail-open 族（与 P0-1 同源）—— ✅ 7/7 已修

| # | 位置 | 症状 + 核于 |
|---|---|---|
| ✅ | `validator.py:720` / `:649` ↔ `_review.py:431` / `:382` | verdict 大小写归一化漏网 → **核于** `_review.py:32` `_norm()` + `:35-37` `_sev()`，两侧都过归一 |
| ✅ | `validator.py:148-178` | `run_project_tests` 三 runner 全失败返回 `passed=True` → **核于** 原函数**确实没改**（`result` 初值仍是 True），但调用方 `_review.py:282-291` 改成按 `runner=="none"` 判并披露"项目测试未执行" |
| ✅ | `_review.py:307-312` | `== "needs_fix"` 分支永不成立 → **核于** `_review.py:405-410`，注释直引原 bug，改判 `in ("retry","abort","needs_fix")` |
| ✅ | `_review.py:184-199` / `:263-275`、`validator.py:453-459` | "超时包装"不成立（`with` 退出时 `shutdown(wait=True)`）→ **核于** `_review.py:251-253,293,352` + `validator.py:489-490,507`，全部显式 `shutdown(wait=False)` |
| ✅ | `_review.py:374-380` / `:423-429` | diff 取不到 → `except: pass` → 模型答 clean → **核于** `_review.py:374-384`，critical 结论现在**同时进 `unverified`**（注释注明"修档 #10 立过这条规矩，这里同族却漏了"） |
| ✅ | `supervisor.py:430` / `:400-406` | `has_files` 空 `covered_by` 时恒真 → **核于** `supervisor.py:466-472`：提前 `if not covered_by and not keyword_match: unverifiable; continue`，绕开了那个恒真。`:457` 那行写法还在但已够不到 |
| ✅ | `_review.py:77-87` | trivial 跳过不写 unverified → **核于** `_review.py` 的 trivial 块（09-11 外派评审那轮补的披露 + fail-closed） |

### 静默失败 / 通道断头 —— ✅ 10/10 已修

| # | 位置 | 症状 + 核于 |
|---|---|---|
| ✅ | `_review.py` 多处 → `_exec.py:580-599` → `neijinglu.py:97-130` | 审查结论只进内存 `quality` → **核于** 结论现在进 `validation.unverified`（见上条），`neijinglu.py` 里已无 `quality` 引用 |
| ✅ | `neijinglu.py:112-113` | `executor_result` 无保护解引用 → worker 异常/超时路径无 trace → **核于** `neijinglu.py:112-113,129-132`，全改 `if self.executor_result else …`，注释直引"最该看现场的那两条路径反而没有 trace" |
| ✅ | `_observer_answer.py:97-113` | 观察者确认 GATE 后磁盘没动（忘了 save）→ **核于** `_observer_answer.py:106,125` `save_project(proj)` + 注释"必须落盘: confirm_gate 只改内存对象" |
| ✅ | `_worktree.py:137-140` | `wt_create` 抛异常静默 `return None` → 无沙箱裸跑 → **核于** `_worktree.py:138-145`，`witness.warn("worktree", "wt_create_failed:…:unsandboxed:…")` |
| ✅ | `_git_worktree.py:252-292` | 启动清理只扫奇点仓库 → 项目 worktree 永不被清 → **核于** `_git_worktree.py` 现有 5 处 `projects_root`/`WORKTREES` 引用（未逐行细读覆盖面） |
| ✅ | `snapshot.py:96-111` | 回滚不查 stash 返回码 → 用户未跟踪文件永久删除 → **核于** `snapshot.py:132-141`：`rc != 0` 直接 `return False` + 告警，注释含"已在 /tmp 仓库实跑复现" |
| ✅ | `snapshot.py:91-99` | 回滚把改动静默挪进 stash 不提示 → **核于** `snapshot.py` 现有 2 处 `witness.warn`（仅确认有留痕，未细核文案） |
| ✅ | `_memory_lifecycle.py:189-194` | DCPM System 2 产出恒 0 → **核于** `_memory_lifecycle.py:206-212`（`getattr(ev,"attrs")` 判 dict）。**实测**：9成1败→`high_success_pattern`、1成9败→`failure_hotspot`、6成6败→不报（正常区间，设计如此） |
| ✅ | `_api_projects.py:217-222` | `project_lineage` 恒走通用模板 → **核于** `_api_projects.py:290` 注释直引"DEFAULT_TEMPLATE（恒为真）→ 永远走这一支" |
| ✅ | `_api_projects.py:243` | 见 P0-4 → P0-4 已 ✅ |

### 并发 / 数据 —— ✅ 6/6 已修

| # | 位置 | 症状 + 核于 |
|---|---|---|
| ✅ | `project.py:319-329` + `:285` | `save()` 裸奔 + 确定性 tmp 路径 → **核于** `project.py:430-441`，`with _LOCK` + tmp 带 pid（注释说明"threading 锁只挡得住同进程线程"） |
| ✅ | `route_learner.py:141-156`、`_token_budget.py:31-55` | 非原子写 + 读改写无锁 → **核于** `route_learner.py:151-153,172`（`atomic_write_json` + 锁，注释拆解了"撕裂写"和"丢更新"两个问题）+ `_token_budget.py:179` |
| ✅ | `_memory_graph.py` / `_memory_core.py` / `_memory_lifecycle.py` | 记忆 JSON 全模块 `grep Lock` = 0 → **核于** 现在锁数 `_memory_core:4` / `_memory_lifecycle:1` / `_memory_graph:0`。**覆盖面未细核**，只确认不是 0 了 |
| ✅ | `_memory_core.py:49-55` | 损坏 JSON 静默当空 → 下次写覆盖全库 → **核于** `_memory_core.py:56-66`：改名 `.corrupt` 留底 + `witness.warn("corrupt_json_backed_up:…")` |
| ✅ | `mcp.py:303-319` | `load_configs` 清空重建无锁 → 撞半截注册表 → **核于** `mcp.py:305-317`：改成**先建局部结构再整体原子换入**（不是加锁，是消除中间态），docstring 写明 |
| ✅ | `api_store.py:249-270` | 欠费标记有去无回 → **核于** `api_store.py:255-258`：改成 `if entry.status != "active": if (now - marked_at) < _RECOVERY_COOLDOWN: return False` —— 过冷却期即重试 |

### ⚠️ 我核错过一条（2026-09-11 深夜发现）

上面那条「融合路径不查 `api_store`」，我复验时**数到 8 处 `is_available` 引用就标了 ✅** ——
没验它**会不会执行**。实际：`_usable()` 里**只 import 了 `api_store` 却用了
`model_registry`**，每次调用抛 `NameError` → 被 `except Exception` 吞掉 → 恒返回 True。
**这个函数从上线起就没生效过。**

**这正是我批评 5.3 的那个错**：拿"代码里写了"当"代码会跑"。
**grep 命中数 ≠ 行为**。同族的坑今天已经出现两次（今早嵌入路径 `SentenceTransformer`
没 import，同一个形状）。

**教训**：核一条"检查/门禁"类的修复，**必须让它真的跑一次并观察输出**，
不能只确认代码存在。判据是行为，不是文本。

### 其余 —— ✅ 13/15 已修，2 条部分修

| # | 位置 | 症状 + 核于 |
|---|---|---|
| ✅ | `router.py:68-77` | 路由分类完全失效（`task_type` 恒 default）→ **核于** `router.py:64-69`：先调 `agent_api_available(a)`（它会**就地**补全 `entry`/`api_key_env`），注释直引"不补的话…所有任务都被判成 default" |
| ✅ | `_dispatch_exec.py:265-280` | `_FusionResult.token_count = 0` 硬编码 → **核于** `_dispatch_exec.py:349-355`，`= member_tokens` 回填真实用量 |
| ✅ | `execution_judge.py:616-632` | 第二轮确认空返回被当"已收敛"、分歧全判给发言方 → **核于** `fusion_round3_empty` 告警 + 09-11 补的 `fusion_confirm_unresolved` |
| 🟡 | `execution_judge.py:652-658` | "全体 adopt"实际是"表过态者 adopt" → **核于** 09-11 加了**空口 adopt 降级**（不带理由不算采纳），门槛收紧了一点，但"表过态"这个语义仍在 —— **部分修** |
| ✅ | `execution_judge.py:44-56` | 融合不查 `api_store.is_available` → 代码确实写了（`_usable` 函数），**但它从来没生效过** —— 见下面「我核错过一条」。2026-09-11 深夜才真正修好 |
| 🟡 | `model_registry.py:103-117` | `models_custom.json` 覆盖时整字段替换 → **核于** `model_registry.py:105-113`：改成**逐字段回退**（`rating`/`provider`/`display`/`strengths`/`notes` 缺则取 toml）。但 audit 点名的 `speed`/`reasoning`/`max_turns`/`recommended_for`/`cost` **仍整体取自定义** —— 所以 audit 说的"`strengths` 全变成同一份标签"已解，"`recommended_for` 变 `['any']`"仍在。**部分修** |
| ✅ | `_workflow_phases.py:296` | 重规划后 `task_ids` 只增不清 → **核于** `_workflow_phases.py:296-299`：注释直引"上一版架构已废弃的 DONE 任务"，下面 `project.task_ids = []` |
| ✅ | `orchestrator.py:382-425` | 兜底建任务丢 `depends_on` → **核于** `orchestrator.py:410,422-424`：注释直引"DAG 被压平"，`dep_deps` 重建并传 `depends_on=dep_ids` |
| ✅ | `web/app.py:1144-1205` | 项目文件面板忽略 `project_id` + 中文名八进制转义 → **核于** `app.py:1198-1206`：`root = _project_repo_root(project_id)` + `-c core.quotePath=false`（注释直引两处症状） |
| ✅ | `_auth.py:18/102-149` | 鉴权开启即永久锁死 → **核于** `_auth.py:117-127`（注释直引"原来打的是 token[:8]…谁手里都没有完整 token"）+ `:142` 换发重置 TTL |
| ✅ | `__main__.py:29-68` | 打错命令兜底进 `_cmd_run` 真跑流水线 → **核于** `__main__.py:69` `if cmd.startswith("-")` 拦住选项；已知命令缺参数也在 `:33-36` return 2。（注：**纯拼错的词**仍会当任务文本跑——那是"兼容旧用法"的既定行为） |
| ✅ | `app.py:1117-1141/1257-1260` | 分钟级 workflow 同步跑在 Flask 请求线程 → **核于** `_api_projects.py:190` `_start_background(project_id, phase, wf_mod.run_phase, …)` + 重入保护（`:198` 注释"run_phase 是分钟级的…"） |
| ✅ | `_io.py:77-89` + `app.py:777-795` | `save_toml` 不转义引号/换行 → **核于** `_io.py:85-90`：转义 `\` `"` `\n` `\r` `\t`，注释直引"读侧 `except: return {}` 会把解析失败吞成空配置" |
| ✅ | `_api_projects.py:135`、`app.py:965`、`_api_tasks.py:514` | 非法参数未捕获 → 500 → **核于** `_api_projects.py:137-141`：注释直引"`Phase(gate)` 对垃圾值直接抛 ValueError → 未捕获 → 500 + HTML"，下面 `except ValueError` |
| ✅ | `execution_judge.py:506-513` | 分歧票 id 类型不一致静默丢票 → **核于** `_votes_into` docstring：id **统一转成字符串**再入表，并说明不归一的后果 |

---

## 三、死代码（✅ 已独立复验）

独立扫描器（AST + 真实引用口径，**排除**注释/字符串）在**删除前代码树**上校准：
标记 36 个 = 上一轮 36 个复验候选、**0 误报**；"仅测试引用"9 个也完全吻合。

### 真死（可删）

| 符号 | 位置 | 规模 | 复核 |
|---|---|---|---|
| `_collect_ready_pending` | `tracker.py:281` | ~14 行 | 全仓仅 2 处命中：定义行 + `tracker.py:139` **注释**。逻辑已被 `ready_tasks()` 完全覆盖 |
| `start_all` + 孤儿 `_all_started` | `bridge.py:315` / `:33` | ~35 行 | 3 处自引用全是**函数体内的日志字符串**。`web/app.py:1945/1951` 是分别直调 `start_ws_server`/`start_observer_server`，绕开了它。上一轮删掉了它的对偶 `stop_all` |
| `probe` | `api_store.py:497` | ~43 行 | Python 内 **0 引用**。裸词 grep 的 234 处命中**全是 vendored 前端资产里的 `route-probe`**（`-` 是词边界）—— 这正是"判据会撒谎"的活例 |

**边界声明**：判据是"**仓库内** 0 引用"，不等于"没人用"。`start_all`/`probe` 是公共命名，
最可能被仓外脚本按名调用。删前按 `docs/死代码清单-20260909.md` 第一节留好 revert 点。

### 仅测试引用（9 个，与上一轮**完全相同**，属"未使用的 API 面"，非死代码）
`guess_template` / `_reorder_agents_by_rank` / `fallback_for_tier` / `rank_candidates` /
`models_for_provider` / `sample_current_state` / `architecture_redo` / `get_success_rate` / `get_stats`

### 待业务确认（勿照单删）
- **`blog/` 子包整体** → 判定**废弃**：实跑 `import singularity.blog` 双断裂
  （第 7 行缺 `flask_sqlalchemy`；第 31 行 `from singularity.blog import models` 而
  `blog/` 下根本没有 `models.py`）。`.flaskenv` 声明它是 `FLASK_APP`，但真实入口是
  `python -m singularity.web.app`
- **`observer/mcp_server.py`** → **不是死代码，勿删**：外部 MCP 客户端经 stdio 拉起，
  `observer_agent.py:17` 专门为它再导出两个下划线名
- **`_memory_experience` / `_memory_lifecycle` 的统计函数** → public facade（`__all__` +
  `memory.py` 的 `import *`），`_cli_memory.py:22` 真调用

### 其他死分支
`_run_delivery` 的失败分支（`orchestrator.py:607` 唯一出口是 `True`）、
`_ESCALATION = {}` 空表（`dispatcher.py:21`）→ `escalate()` 恒返回 None，
`:365`/`:491` 的 `fallback: A→B` 告警**在撒谎**（B 从没被试过）、
`_task_runner.py:250` 的"重试耗尽→自动拆分"（`retry_count` 全仓只被 `recover()` 写）、
`_cache.py` 整块 TTLStore（0 调用）、`codegraph.py:125` 的 `call_graph`（`visit_Call` 是 `pass`）、
`router.py` 的 `_CLASSIFY_CACHE`（提前 return 跳过写缓存）。

---

## 四、明确判定"**不是**问题"的（避免下轮重复查）

- `merge_tree_probe` 的冲突解析：**已修对了** —— 实跑 git 2.50.1 确认根目录无扩展名文件
  （Makefile/Dockerfile）正确进列表
- `drain()` 死循环：逐 case 走查（含全 defer、交错 defer/成功）均终止
- `_do_merge`：脏工作区 fail-closed、冲突/非零 rc 都 `merge --abort`
- **中文路径**：实测 `git worktree list --porcelain` 在 `core.quotePath=true` 下也**不转义**
  中文 → `cleanup_orphans` 的判断对中文路径成立
- `witness.heartbeat` 参数错位：修复归档 #10 那 4 处**已清干净**
- `_model_breaker`：**没有**永久熔断/永不恢复
- **前端 XSS：无**。全量 grep `dangerouslySetInnerHTML|innerHTML|eval(|new Function` = 0 命中
- **前端硬编码地址：无**。`BASE=''` 全走同源相对路径
- `observer/` **没有采样线程**，也没有采样循环 → "线程泄漏"这一项不存在
- 鉴权绕过：静态/SPA/SSE/`_api.py` 聚合路由**都没找到绕过路径**
- 路径穿越：项目名/task id/文件名三个入口在 Flask 转换器 + 校验组合下**堵住了**

---

## 五、前端 —— ✅ 已复验（09-11 晚逐条对源码，9/9 已修）

| # | 位置 | 症状 + 核于 |
|---|---|---|
| ✅ | `lib/api.ts:77` ⟷ `web/app.py:1714` | 技能勾选把该模型技能清空（前端发 `{skills}`、后端读 `skill_names`）→ **核于** `lib/api.ts` 现有 5 处 `skill_names`，且带注释"原来发 {skills} → …覆盖成空数组" |
| ✅ | `pages/Chat.tsx:138-146` + `:37-40` | 未选项目时第一条消息**立刻从屏幕消失**；通用对话每次进页被清空 → **核于** `Chat.tsx:40-44` 注释直引"原来每次挂载都 clearConversation('_default')…没有任何必须重置的理由" + `:169` 注释"原来顺序反了：先 addChatMsg 落到 '_default'，随后 setActiveProject 切到新项目" |
| ✅ | `components/FilePanel.tsx:80-90` | 读文件失败（403/404）显示成「(空文件)」→ **核于** `FilePanel.tsx:97-104`：先判 `d.error`，注释直引"直接 `|| '(空文件)'` 会把'禁止访问/文件不存在'显示成'这是个空文件'" |
| ✅ | `components/FilePanel.tsx:26-35` | 列表/diff 出错静默显示「暂无文件」→ **核于** `FilePanel.tsx:26-29`：注释直引"后端把错误也放在合法 JSON 里…只判 `if (d.files)` 会把'读取失败'显示成'暂无文件'"，改为优先看 `error` + toast |
| ✅ | `pages/Tasks.tsx:28-40` | SSE 每次触发 `setLoading(true)` → 整表反复闪骨架屏 → **核于** `pages/Tasks.tsx:29` 注释直引原症状 + `:32,34` 改成 `if (initial)` |
| ✅ | `pages/Chat.tsx:78-88` | traceCache 写占位后失败不清理 → 终生不再拉产物 → **核于** `Chat.tsx:107-112` `catch(() => traceCache.current.delete(x.id))`，注释直引"一次瞬时失败 = 这个任务的产物终生不再拉取" |
| ✅ | `lib/useSSE.ts:14-41` | 单例连接 CLOSED 后永不重建 → 静默退化成 10s 轮询 → **核于** `lib/useSSE.ts:31` `if (_es && _es.readyState !== EventSource.CLOSED) return _es` + 注释 |
| ✅ | 多处 | 竞态：快速切项目旧响应覆盖新列表 → **核于** `Chat.tsx:36` 新增 `fetchSeq = useRef(0)  // fetchTasks 的请求序号，用于丢弃过期响应` |
| ✅ | `lib/api.ts` 15 个封装 | `startLoop`/`stopLoop` 零引用，但侧边栏显示状态却没有启停入口 → **核于** `components/AppLayout.tsx:141-149`，注释直引"没有任何启停入口"，下面接了调用 |

---

## 六、方法教训（本轮最值钱的部分）

1. **我自己的检查器第一版就错了**：用 `ast.walk` 把函数内局部绑定当成模块级，
   于是 `pre_search.py:witness`（局部 import）和 `_api_memory.py:resolve_conflict`
   （那行坏 import 本身）全被误判成 ✅。改成"只下潜模块顶层"+ 自检已知名后才发现。
   —— **"自己写的工具也要独立重算"，这个仓库第 4 次踩同一个坑。**
2. **裸词 grep 对公共命名完全不可信**：`probe` 裸词 234 处命中，
   真实引用 **0** —— 全是 vendored JS 里的 `route-probe`（`-` 是词边界）。
3. **`except` 分支里的名字最容易死**：`witness` 那 6 处全在错误路径。
   函数内 `import` 会让名字**整个作用域**变局部 —— 所以"漏抄一处 import"的后果
   不是 NameError 而是 `UnboundLocalError`，且只在异常路径暴露。
4. **单测 monkeypatch 掉的东西，正是 bug 藏身处**：`test_review_gate.py:229`
   伪造了 `subprocess.run` 的 diff 返回值 —— P0-1 在那里结构上不可能被发现。
5. **同一份文档里的事实会互相矛盾**：`修复归档-20260910.md` 未决 #4 仍写
   "reviewer 扩池没真机跑过"，而 #14 已划掉标"已验证"（同一条）；
   §"下次从这里接" item 3 说未决 9/10 没查根因，而正文 #9/#10 都标了已修。

---

## 七、建议的处理顺序（**推测，未实测效果**）

1. **P0-1 空 diff** —— 一个改动决定 5 道门禁是真的还是装饰。修法要先有测量：
   把 diff 基准改成 `snap.ref`（快照 ref）而不是裸 `git diff`
2. **P0-2 witness 幽灵名** —— 6 处改成统一顶层 import，风险低、收益立刻（错误路径不再炸）
3. **P0-3 集成/交付错仓库** —— 把 `config.PROJECT_ROOT` 换成 `repo_dir(proj.id)`
4. **P0-4 三个 500 接口** —— 要么接回实现，要么下掉入口
5. 死代码 3 个 + `blog/`（**`mcp_server.py` 别动**）
6. ⬜ 那批统一收口：verdict 归一化（照抄 09-10 severity 的修法）、`quality` 进 trace

---

# 第二轮：⬜ 待复验项的独立复验 + 修复（2026-09-11 03:00）

上一轮留了约 50 条 ⬜（子审计员报告、我未独立复验）。本轮挑了**最严重的 16 条**
派 3 个独立 agent 专门去**证伪**（指令明确写了"证伪比确认有价值"）。

## 复验结果：16 条全真，0 条证伪 —— 但有 4 处修正

| 修正 | 内容 |
|---|---|
| **行号几乎全错** | 审计报告里的 `文件:行号` 大面积对不上当前源码（V1 生产端实际在 `validator.py:743` 不是 `:720`，消费端 `_review.py:445` 不是 `:431`）。**照原报告的行号去改会改错地方** —— 这正是"判据会撒谎"的又一面 |
| `run_project_tests` 不是漏网 | 是**有测试锁定的既有设计**（`test_validator.py::test_run_tests_no_tests` 显式断言 `passed=True`）。且 `runner != "none"` 的 guard 挡住了"假装测试通过"的正向加分 —— 危害是"静默放行 + 不披露"，不是"虚报通过" |
| `_dispatch_exec` 超时包装 | **代码里已有注释承认**（`_dispatch_exec.py:22-26` 写明"实测 timeout=0.3s 仍等了 3s"），是已知且接受的行为，不是漏网 |
| `api_store` 那条半失效 | `probe` 本轮已删，"永远观测不到恢复"那半随之消失；且 `note_api_error` 对 **402 是无条件标记**（不只看 body）；**存在**手动恢复入口 `PUT /api/api-store/<id>/status`，但前端/CLI 都没接 |

## 本轮修掉的（全部复验后再改，按复验给的行号）

1. **外层 verdict 大小写 fail-open** —— 生产端只归一精确小写 `critical`/`rejected`，
   消费端又拿 `== "needs_fix"` 精确比。模型吐 `"Critical"` → `findings` 整条丢弃 →
   **真漏洞随代码合并**。加了 `_norm_verdict()`，消费端改走已有的 `_norm()`。
   同族 bug：09-10 修的是 **severity** 字段，这次漏的是 **verdict** 字段。
2. **失败任务写不出 trace** —— `neijinglu.to_dict()` 对 `executor_result` 无保护解引用，
   worker 异常 / 执行超时两条路径传 `None` → AttributeError 被吞 → **最该看现场时没有 trace**。
3. **观察者确认 GATE 假成功** —— 5 个分支都 `confirm_gate` 后不落盘（`save` 导入了却零调用），
   而 `load()` 每次重新解析 → 回"✅ 已通过"但 phase 纹丝不动。
4. **worktree 创建失败完全静默** —— 裸 `except: return None`，连一条 warn 都没有；
   调用方拿不到 wt 就用仓库根当 cwd → **无沙箱裸跑**且不进合并队列。
5. **`project.save()` 无锁** —— `tmp` 是确定性路径 `<id>.tmp`，多线程同项目 save 会共用同一 tmp。
6. **CLI 兜底把 `--help` 当任务真跑** —— `--help`/`-h`/漏参数的 `rollback`/`add` 会掉进
   "兼容旧用法"分支 → 建任务 + 立即跑完整流水线（真烧钱）。
7. **`system2_extract` 恒空转** —— `_load_events()` 返回 `EventNode` **对象**，
   而循环写 `if not isinstance(ev, dict): continue` → 条条跳过。

## 一条"修了但没修好"的，如实记

**`system2_extract` 只拆了第一道闸，依然恒空。** 复验员说"isinstance 是唯一问题"是**不完整**的。
实测 65 条真实事件里：

```
attrs 键分布 : files 65 / depends_on 65 / mem_type 65 / status 3 / route_level 3 / route_type 3
status 取值  : None 62 条，'routed' 3 条
```

`status` 只有 3/65 有值，且值是 `"routed"` —— **不在 `("done","pass","merged")` 也不在
`("failed","blocked")` 里**。所以 successes/failures 仍恒空、`groups_analyzed` 仍是 0。

**真正的根因在写入侧**：`index_task` 没把任务终态写进 `attrs.status`。
本次**没动**（要改得先确认 attrs 的写入契约，且改了会影响 memory 图谱的其他消费者）。

## 方法教训：这一轮我的"判据"骗了我 6 次

1. `ast.walk` 把函数内局部绑定当成模块级（误判 2 个真 bug 为 ✅）
2. 字符串匹配撞上**我自己写的修复注释**（`config.PROJECT_ROOT`）
3. 快照 ref 用 `git stash create` 造，漏了"干净工作区兜底记 HEAD"
4. diff fixture 的基准文件里压根没有那行 `require_auth`
5. QA 测试传了**空约束清单** → 函数在 `:659` 提前返回 accepted
6. 测试依赖真实环境（conftest 隔离了 QIDIAN_DIR → `load_agents()` 空 → 走"无可用模型"分支）

**每一次都是"手工重算/打印中间值"才抓到。** 这仓库"自己写的工具也要独立重算"
这条教训，本轮一个人身上就复现了 6 次。


---

# 五、CLAUDE.md

# 奇点（Singularity）项目

> 详细文档 → `docs/项目速查.md`

## 项目地图（冷启动速查）

**一句话**：AI Agent 调度平台，多模型按角色协作完成软件开发任务。数据都在 `.qidian/` 下（JSON 文件，零数据库，可 grep 排查）。

### 模块 → 文件

| 模块 | 文件 |
|------|------|
| 调度循环 / 阶段流转 | `scheduler/orchestrator.py` |
| 任务状态机 + 持久化 | `scheduler/tracker.py`（→ `.qidian/tasks/`） |
| 执行器核心（`run()` 最复杂） | `scheduler/_exec.py` |
| 分发到 executor | `scheduler/dispatcher.py` + `_dispatch_*.py` |
| GATE 审查 | `scheduler/validator.py` |
| worktree 生命周期 | `scheduler/_git_worktree.py` + `_worktree.py` |
| merge / 冲突 | `scheduler/merge.py` |
| 任务 API handler | `scheduler/_api_tasks.py` |
| 目录常量 | `scheduler/config.py` |
| Web 前端 | `web/app.py` (Flask) + `web/frontend/` (React) |

### 改 bug 先看哪

- 调度超时 / 轮次 / dispatch 次数 → `_exec.py` + `dispatcher.py` + `orchestrator.py`
- 任务残留 / 删除 / 状态 → `_api_tasks.py` + `config.py` + `tracker.py`
- worktree / merge 残留 → `_git_worktree.py` + `_worktree.py`

**动手前**：改状态机 / 门禁 / worktree 路径 / 子进程 / 清理 / 模型调用前，先扫一遍 `docs/防御模式.md`（症状 → 根因 → 规则）。
**踩了 P0/P1**：按 `docs/postmortem-模板.md` 复盘，防护措施要可执行，不是「注意点」。

### 验证

- `pytest tests/test_scheduler/ -q`（~11s，全绿基线；全量 `pytest tests/` 是 823）
- `.venv/bin/python tests/test_exec_run.py`（`_exec.run()` 退出路径，桩测试不碰真 API）
- `.venv/bin/python tests/test_review_gate.py`（门禁能不能看到改动 —— 真 git + 真快照，不 mock；桩测试测不出这个时序）
- `.venv/bin/python tests/smoke_test.py`（走 HTTP，40 项；需先 `.venv/bin/python -m singularity.web.app` 起后端）

> 后三条必须用 venv 解释器：系统 `python3`（homebrew 3.14）没装 singularity，直接跑会 `ModuleNotFoundError`。
> `pytest` 那条不受影响 —— `pyproject.toml` 给 pytest 配了 `pythonpath`。

---

## 重新生成

```sh
cd ~/projects/singularity && sh docs/生成外派包.sh
```

源文件：`docs/静默失败治理-外派材料.md` + 上面那四份。
⚠️ 防御模式/演化史/审计报告一直在改 —— **发之前重跑一次**，别用旧的。
