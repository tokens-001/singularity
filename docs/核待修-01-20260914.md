# 核「静默 except 待修清单」（D / ZCode · GLM-5.3-Flash）+ 我的核实

> 下面是**报告原文一字未改**。原始件 `~/Desktop/ZCode审阅/核待修-01.md`（2026-09-14 04:3x，204 行）。
> 任务：补分类器看不到的那一层 —— 对那批"只看 except 那段代码"分出来的条目，**去读调用点**。
> **总账：111 条 = 真坏 20 / 假警报 91 / 判不了 0。** ⚠️ **实数 111 不是 91**
> （清单装的是 out-04…out-16 全部；out-01/03 已修不在文内）—— **又一个"总数对不上"**。
> ✅ **约束遵守**：自报"三个禁读位置一次没碰，判定只来自清单原文和仓库代码"。

## 🏆 它最有价值的一节：**fail-open 门禁族，调用层全部落空**

**清单标得最红的四条（C/3~C/4）它全判"不成立"**，理由分两类：

| 它说 | 我核到 |
|---|---|
| 两条**已经修好了**（`_dispatch_skills.py:176` / `openai_agent.py:668`）⇒ 清单是修复前生成的 | ✅ 属实 —— 那两处正是 **`90580a2`** 修的（权限闸门两处 fail-open 改 fail-closed）。**它读到的是修复后的代码，这个判断对。** |
| 两条**有下游硬兜底**（`router.py:159` / `_observer_tools.py:135`）：`gate=False` 之后 `validator.py:50` 的文件级判据还在 | ⚠️ **我没核**（上下文用尽）—— 当候选 |

**它自己的总结一句到位**：清单判的是「except 后落在宽松默认 = 坏」（**形状 + 方向**），
它判的是「**这个默认值在调用层被谁吃掉**」—— 这正是清单自己那句"坏不坏取决于默认值在不在
决策位上"的全量验证。**分类器看不出的那一层，被它补上了。**

## ✅ 我核过的（抽 3 条"反升级"—— 原判低估的，**全真**）

| # | 它说（原判低估） | 我核到 |
|---|---|---|
| 1 | **`_exec.py:658` 编造 `conf=0.5` 覆写 validate 的真分** ⇒ `0.2→0.5` 把「立即升级」错成「同模型重试」 | ✅ `_exec.py:657` `validation.confidence = quality.get("confidence", 0.5)` —— **`validation` 是上面 `val_mod.validate()` 刚算出来的、真分被这一行盖掉**；且 `:659` 的 `except` 里 `quality = {…, "confidence": 0.5}` 兜的是同一个假数。**"编造的数字"最纯的一例。** |
| 2 | **`witness.py:73` 是全仓告警**唯一**汇聚点，死了全哑且看着健康** | ✅ `witness.py:74-75` `except OSError: pass  # 记告警失败不该再抛` ⇒ **写告警文件失败 = 静默零痕迹**，而所有告警都从这儿出去 ⇒ **观测整体失明，而外表健康**。 |
| 3 | **S1 族必须按写路径劈开**：已换 `atomic_write_json` 的 3 处（`model_discipline` / `model_prices` / `route_learner`）**降为假警报** | ✅ grep 核实：三处**都** `from ..._io import atomic_write_json` 且写点用的就是它（`model_prices.py:83` / `route_learner.py:175` / `_model_discipline.py:63`）⇒ **撕裂写这个主要损坏源已除**，"损坏"要外部才触发。**这个劈分是对的。** |

## ⚠️ 与 E 撞车的一条（两轮独立撞上 ⇒ 可信度更高）

D 的 **`workflow:690`**（GATE3 入门票只认 `verification_ran` 标记、**不查 `qa_report.json` 是否存在**
⇒ 报告丢了门照开）—— **和 E 的"顺带发现 #8"是同一条**。**两轮独立撞上同一处。**
⚠️ 而且它**和我今晚修的 GATE3 那条是同一族的上一层**：我修的是**前端渲染**（读坏了别显示绿"通过"），
**后端这道"门放不放行"没动**。⇒ **见登记。**

## 其余（我没核，当候选）

`api_store:58/71` 反升级（文件一坏，**下一次成功 API 调用的 `record_alias` 就把配置全毁**，不用等人保存）·
`_observer_answer:140` **给用户回假成功「✅ GATE1 已通过」** · `_dispatch_crud:17` 升级
（`agents_custom.json` 有 **skill_loader 和 _dispatch_crud 两个独立写者**，谁先写谁触发抹除）·
另两条的**建议修法已经在现行代码里**（`_process_ledger` 的 `cost=None` · `_memory_core` 的
`.corrupt` 留底 + warn —— **后者正好是真坏那 8 条的现成修法范本**）。

---

# 核待修-01 —— 调用方视角复核《静默except待修清单-20260913》

- 复核人：ZCode（glm-5.3-flash），2026-09-14。
- 方法：对清单每一条，去读它的**调用点与返回值去向**，判「真坏 / 假警报 / 判不了」。
- 范围：清单内 out-04…out-16 全部分批表，实数 **111 条**（清单首页说 88/91，是旧数；out-01/03 已修不在此文）。
- 行号说明：清单材料 23:32 生成，**行号已漂**。每条给「原行号」和「现行号（按内容重定位）」。
- 未读（按硬约束）：`Desktop/静默except分类/`、`Desktop/ZCode审阅/` 其他文件、`docs/S1统一修法草案-20260914.md`。本判断完全独立于那三处。

判定标尺（用户口径）：**真坏** = 吞掉的失败会真的造成后果（数据被覆盖 / 数字是编的 / 门禁被绕过 / 没人知道）；**假警报** = 失败在此处无关紧要或上层本来兜住；**判不了** = 说清缺什么。

---

## 批次1：out-04 全部 + out-05 前半（_exec.py 前半）—— 20 条

| # | 位置（原:现） | 判定 | 后果一句话 | 为什么（调用点证据） |
|---|---|---|---|---|
| 1 | openai_agent.py `_tool_search`（原:801→现:831） | 假警报 | 坏正则→每文件都抛 re.error→回"未找到匹配"，模型拿到假阴性 | 返回串直接回给 agent 工具循环（openai_agent.py:818-835），无状态、模型下轮换 pattern 即自愈；re.error 是模型自己传的 pattern 造成，非外部数据损坏 |
| 2 | openai_agent.py `_search_code` 模块级（原:1147→现:1177） | 假警报 | 同上（模块级复制） | 同上（openai_agent.py:1155-1181）；外层 except: return "搜索错误: {e}" 已兜住 ValueError 类，内层吞的只是逐文件读失败+re.error |
| 3 | openai_agent.py `_api_call_once` 别名记账（原:897→现:924-928） | 假警报 | 别名映射可能记不上→委员会去重暂时盲 | record_alias 在**每次** API 成功响应路径上重试（openai_agent.py:924-928），瞬时失败下次调用自动补；且 api_store.record_alias 内部自己已全吞（api_store.py:332-340），外层 try 实际只兜 import 失败；持续坏=api_store 文件损坏族（out-08/09 的 :58/:71），不在这处 |
| 4 | openai_agent.py `_raise_for_status` 熔断标记（原:909→现:936-940） | 假警报 | 标记失败→死 provider 下轮可能再撞、账晚记一轮 | 真正的 HTTP 错误照常上抛（openai_agent.py:941-946），每次碰撞都是响的；note_api_error 每个 4xx 响应都会再试（api_store.py:383-398）；回退方向不是"放行"，只是熔断记忆延迟 |
| 5 | workflow.py `_phase_cwd`（原:176→现:174-177） | 假警报 | mkdir 也失败→返回不存在的 cwd→后续子进程当场报错 | 外层 ensure_repo 失败已 witness.warn（workflow.py:172）；cwd 不存在时 executor 的 subprocess.run(cwd=…) 立刻 FileNotFoundError，响的；该函数存在的目的恰是**不**回退 PROJECT_ROOT（workflow.py:165），方向对 |
| 6 | workflow.py `_collect_changed_files`（原:306→现:302-307） | **真坏** | 某任务 trace 读坏→它的改动文件从验收上下文消失，QA/安全审计覆盖静默变窄，无任何标记 | 消费点 workflow.py:423（文件数展示）与 :590-599（验收 ctx 的"改动文件 (N)"列表，喂给 QA/安全审计师）；trace 坏→该任务文件根本不进审计视野，人审页只见"验收正常完成"，没有"少了谁"的痕迹——S4 假干净形状 |
| 7 | workflow.py `_run_verification` E2E 清单（原:674→现:665-675） | 假警报 | e2e_checklist.json 没落成→人审少一份参考材料 | 全仓 grep 仅 workflow.py:670 一个写点、**无代码消费方**；解析失败时"E2E用例 N 个"消息也在 try 内不会发（:668-669），不存在"说有却没文件"的错位；test_cases.json 原件仍在 |
| 8 | workflow.py `_run_verification` QA 报告（原:690→现:678-691） | **真坏** | QA 报告构建/落盘失败被吞，但 :701 仍无条件写"verification_ran: QA+安全审计已执行"→GATE3 入门票照常放行 = 假干净 | GATE3 入门票只认 issues 标记、不查 qa_report.json 是否存在：workflow.py:701（except 后照写 verification_ran）+ project.py:306（`has_verification_evidence()` 只看标记，_gate3_admission 不阻断）——证据链断了而门以为开着 |
| 9 | workflow.py `_changed_files_of`（原:722→现:716-723） | 假警报 | 单任务 trace 读坏→"越界改文件"人审提示漏报这一个 | docstring 明说这是"报"不是"防"、尽力而为、已进 silent_except 守卫基线（workflow.py:739-745）；漏报≠写假"干净"（没有 all-clear 输出）；坏 trace 本身会在 tracker 一族更响地炸 |
| 10 | workflow.py `_flag_file_overlap` import 兜底（原:749→现:747-750） | 假警报 | import 失败→整个提示函数跳过 | tracker/witness import 挂=仓级损坏，别处全响；函数 advisory-only（只 append issues + warn，workflow.py:774-788） |
| 11 | workflow.py `_flag_degraded_tasks` import 兜底（原:812→现:810-813） | 假警报 | 同上 | 同上（workflow.py:791-835，只进 issues 不改行为） |
| 12 | _exec.py `_persist_partial_usage` 读旧值失败（原:131→现:127-132） | 假警报 | 累计 token 从当前 delta 重算→超时账缩水（下界更小） | 唯一消费方 orchestrator.py:172→:234 把 0/缺一律转 None"用量未知"，且契约明写"下界不是准确数"（orchestrator.py:162-163）；不产生"花了 $0"的记录 |
| 13 | _exec.py `_persist_partial_usage` tool_events 回读失败（原:161→现:157-162） | 假警报 | 旧 tool_events 丢→超时 trace 轮次统计少报 | 消费方 orchestrator.py:177 只进 trace 度量；_exec.py:100-102 注释明说"真被截断也只会少报，是如实少报" |
| 14 | _exec.py `_persist_partial_usage` 整体失败（原:164→现:164-165） | 假警报 | 超时任务整份 sidecar 没了 | 消费方 orchestrator.py:234 `token_count = _partial_tokens or None`——0/无→None"用量未知"，**不是 0**；"None=不知道，不是没花钱"是消费方自己立的契约（orchestrator.py:163） |
| 15 | _exec.py `_mark_dispatch_started` 整体失败（原:213→现:213-214） | 假警报 | started_at 没写上→§59 分态诊断话说错 | 影响=orchestrator.py:231-233 那句"用量未知"的措辞（说成"一次 dispatch 都没进去过"）；只是超时 trace 的诊断文案，无行为分支吃这个值 |
| 16 | _exec.py `read_partial_started_at` 读坏回 None（原:224→现:224-225） | 假警报 | 文件在但读坏→同上误说"没进过 dispatch" | 消费点只有 orchestrator.py:174 / :231-233 的诊断文案，无决策分支 |
| 17 | _exec.py `read_partial_usage` 读坏回 (0,"")（原:236→现:236-237） | 假警报 | 读坏回 0→但消费方把 0 转 None"未知" | orchestrator.py:234 `or None`——"回 0 = 编造没花钱"在唯一调用点不成立；原判 S2 指控被调用层化解 |
| 18 | _exec.py `read_partial_tool_events` 读坏回 []（原:254→现:254-255） | 假警报 | 度量少报 | 同 #13（orchestrator.py:177） |
| 19 | _exec.py `_mark_dispatch_started` 读坏后覆写（原:208→现:207-209） | 假警报（sidecar 家族里最该加留底的一个） | 文件截断→d={}→**主动覆写抹掉已累计 tokens/events** | 抹的是"下界账"：消费方 orchestrator.py:234 把丢账映射为 None"未知"，不进食量/门禁；触发链=写非原子+任务被杀+同任务重试再 dispatch（_exec.py:201-212），低概率；要修 S1 时这里是 sidecar 里唯一真抹数据的点，值得写前留 .corrupt |
| 20 | _exec.py run 主循环 hook 死兜底（原:658→现:654-659） | **真坏** | hook 死→**编造 conf=0.5 覆写 validate() 真分**+failure_kind 谎报"ok"，全程无信号 | (a) _exec.py:656 用 0.5 覆写、经 :678 min 进 `_decide_cascade`：validate 给 0.2 本该"立即升级"（:392 conf<0.35）被抬成 0.5 变"同模型重试"；hook 活时的高分 cascade_accept（:384 ≥0.75）永远到不了→软伤任务烧满重试；(b) failure_kind="ok" 使 :375 的 soft_quality turn≥2 硬停永不触发；(c) 无任何 warn——质量信号层整层静默降级。原判说"假中性"方向其实说轻了：0.5 既收紧 accept 又扭曲 escalate/retry 分界 |

**批次1小结**：真坏 3（#6/#8/#20），假警报 17。out-05 的 sidecar 读回一族（#12-18）几乎全被 orchestrator.py:234 的 `or None` 消费契约化解——原判的"编造数字/整笔账丢"在调用层不成立。
---

## 批次2：out-05 后半 + out-06 + out-07 + out-08 前半 —— 20 条

| # | 位置（原:现） | 判定 | 后果一句话 | 为什么（调用点证据） |
|---|---|---|---|---|
| 21 | validator.py `run_project_tests` runner 循环（原:187→现:197-198） | 假警报 | runner 挂/超时→静默 continue→全挂时报"没找到测试/启动不了" | 调用方 _review.py:284-296 明确把 runner=="none" 当"**没跑**"处理：不加分、写"项目测试未执行"进 unverified，语义有测试锁定；supervisor.py:323 只对 passed=False 拦。丢的只是"哪个 runner 因何抛"的诊断细节 |
| 22 | validator.py 硬规则·鉴权移除 diff（原:280→现:279-292） | 假警报 | git diff 抛→该文件的"移除认证检查"检测跳过 | 检出结果只是 severity=warning 不拦合并（validator.py:294 只数 critical）；base 本就可选（:272）；单文件低频跳过，审查员证据少一条而非假"干净" |
| 23 | validator.py `_defense_checklist`（原:325→现:326-337） | 假警报 | checklist 模块死→审查 prompt 缺"找茬清单" | 函数自己的规矩就是"挑不中→''，宁可不给"（validator.py:331）——失败与"没匹配"混为一谈，损失=一段 prompt 料；审查员仍在全量审 |
| 24 | workflow.py `_read_observer_rollup`（原:859→现:850-860） | 假警报 | rollup 坏→None→调用方完全走原逻辑 | docstring 明写 fail-closed"绝不猜"（workflow.py:846-848）；消费点 :908-914 有值才用、没值标 route_source；没有编造值进决策 |
| 25 | workflow.py `handle_gate3_reject` qa_report（原:893→现:878-894） | 假警报 | 报告损坏→并入"无报告"→保守回实现层 | :900-902 兜底 fix_route="impl" 不动架构；:930-932 消息带 ⚠ 说明；:927-929 lineage 记 no_qa。混淆已知、兜底保守且有披露 |
| 26 | witness.py `warn`（原:73→现:73-74） | **真坏** | 告警通道自己死→**全仓所有告警静默消失**，面板空=看着像没病 | witness 是全仓告警唯一汇聚点（本清单所有 key= 建议都落这里）；try 内无任何跨通道兜底；read_alerts→web 面板消费，空列表呈现为健康。S5 单点本身，修法（stderr+log 兜底、不抛）成本极低 |
| 27 | witness.py `read_alerts`（原:84→现:82-85） | 假警报 | 告警文件读不动→面板空 | 读侧只是显示层；事件本身已随文件损坏丢失（病根在 #26）；无决策分支吃返回值。修时与 :73 一起收 |
| 28 | _review.py `_review_requirements`（原:150→现:147-151） | 假警报 | 载不动 project→审查 prompt 缺顶层需求/约束 | 纯 LLM prompt 料；load_project 失败=项目文件本身坏了（别处会更响）；审查仍对 diff 全量跑 |
| 29 | _review.py `run_post_exec_checks` 本地安全扫描（原:234→现:215-235） | **真坏** | 正则安全扫描炸→**前置防线整段跳过、零披露** | 同函数其他跳过都留痕：trivial 写"审查已跳过"（:241-243）、超时写"不默认通过"并降置信（:265-272）；唯独这里 except: pass 什么都不写——流水线照常往下走、可能 cascade_accept 合并。按设计它本该 failure_kind=security→retry。S4 假干净，原判 B/4 成立 |
| 30 | _workflow_phases.py 调研 pre_search（原:217→现:196-218） | 假警报 | 记忆检索死→调研 prompt 缺"已知历史任务"段 | 纯增强料，prompt 没它照样成立；无决策值；失败低频 |
| 31 | _workflow_phases.py 架构 ledger digest（原:286→现:280-287） | 假警报 | 账本摘要死→架构 prompt 缺"上一轮实际发生了什么" | 注释自陈"事实供参考，不是指令"（:278-279）；架构仍有调研/约束可用 |
| 32 | mcp.py `load_mcp_configs`（原:434→现:451-460） | **真坏** | 配置损坏→静默回默认配置→**下一次 admin 保存整文件覆写，用户 MCP 配置没了** | 覆写链坐实：_api_admin.py:427 `load_mcp_configs()` → :437/:442 `save_mcp_configs(...)` → mcp.py:500 整文件 write_text。有 _log_warn 但没有任何东西**拒绝覆写**——损坏+一次后台保存=真数据被毁 |
| 33 | _memory_consolidator.py `_should_run_heavy`（原:44→现:42-45） | 假警报 | 状态损坏→节流计数清零、heavy 推迟 | 失败方向保守（heavy 不跑=不白烧钱）；"状态"只是个计数器，下次写自愈；无数据可丢 |
| 34 | _memory_consolidator.py `_save_heavy_state`（原:66→现:60-67） | 假警报（值得捎带一行 warn） | 状态写不动→**heavy 永不触发**（机制上确认：每次读回 {} 走"首次"分支返回 False） | 但死的只是"抽象/整合"这个辅助功能的触发器，方向保守；系统性磁盘病会有别处更响的炸；不到真坏档 |
| 35 | _memory_consolidator.py `_record`（原:190→现:183-191） | 假警报 | memory API 记账死→memory 用量缺角 | 缺行≠编造 0；memory 调用便宜；账只进成本展示，无决策分支吃它 |
| 36 | _memory_consolidator.py `abstract_trajectory` stage_summary（原:234→现:230-235） | 假警报 | 阶段摘要死→抽象 prompt 缺"动作序列"段 | LLM prompt 料；"(无)"兜底调用照常；产出是参考性"经验" |
| 37 | _observer_answer.py GATE1 确认（原:140→现:167-174） | **真坏** | confirm/save 抛→except: pass→**照样回"✅ GATE1 已通过"** | :166 session phase 已设 done（不会再触发重试）；:175 无条件返回成功——项目真实状态没推进、无任何痕迹；**对用户说了假成功** |
| 38 | _observer_answer.py GATE2/GATE3 确认（原:173→现:182-207） | **真坏** | confirm/save 抛→吞掉→**人的批准/打回被当成普通聊天丢掉** | 成功消息全在 try 内（:192/:196/:201/:205），except: pass 后流程落进 Step 3 普通对话（:209-226）——门禁决定无痕消失，用户只会看到答非所问 |
| 39 | _observer_answer.py `_build_definition_prompt`（原:365→现:388-399） | 假警报 | 已完成角色文档读不出→当前角色 prompt 缺前序产出 | 纯 LLM 上下文料；保存侧已有 _log_warning（:362-363）；单文档损坏低频；无决策值 |
| 40 | api_store.py `_load_raw`（原:58→现:52-60） | **真坏** | 损坏当 {}→**下一次 record_alias 把整文件覆写成只剩 alias 键，provider 配置全毁** | 覆写链：api_store.py:57-59（坏→{}）+ :333-338（load 后直接整文件 write_text）+ openai_agent.py:924-928（**每次**成功 API 响应都 record_alias）——文件一坏，下一次成功调用即毁真配置。S1 + 覆写，最实的一条 |

**批次2小结**：真坏 6（#26/#29/#32/#37/#38/#40），假警报 14。新抓到的覆写链两条：mcp 配置（#32）和 api_store（#40）——后者是"损坏+任意一次成功调用=配置全毁"，危害比原判 B/2 高。
---

## 批次3：out-08 后半 + out-09 + out-10 —— 20 条

| # | 位置（原:现） | 判定 | 后果一句话 | 为什么（调用点证据） |
|---|---|---|---|---|
| 41 | api_store.py `_load`（原:71→现:63-73） | **真坏** | 损坏→回 `_seed()` 内置种子→后续任何 save 用种子数据**整文件覆写**用户配置 | 覆写链：:68-71（坏→seed）+ note_api_error :402-405（load 后整文件 write）+ set_observer_model :421-428 同款；与 #40 同一根因（_load_raw 坏→{}），两条修一处即可，但都是"损坏+一次写=配置全毁" |
| 42 | api_store.py `note_api_success` provider 解析（原:303→现:299-304） | 假警报 | provider 解析死→盘上 status 不翻回 active（界面挂"欠费"） | 调度真正依赖的是 `_quota_dead` 标记，且在本点**之前**已 pop（:294-297）；provider status 注释自陈"只给界面看，不作调度连坐依据"（api_store.py:400）；外层失败已有 witness.warn（:314-317） |
| 43 | api_store.py `record_alias` 内层（原:339→现:332-340） | 假警报 | 别名记不上→委员会去重盲（同 #3） | 每次 API 成功响应都重试（openai_agent.py:924-928）；持续坏=文件损坏族，归 #40/#41；这层吞的是"记不上不影响本次调用"，方向对 |
| 44 | api_store.py `is_model_available` provider 解析（原:373→现:370-374） | 假警报 | 解析死→provider=""→`return True` 放行一个没验过的模型 | 放行后真调用缺 key/欠费会**响亮地失败**并走 cascade/重试——不是静默坏；且解析死=model_registry 层的病（out-15:76 那条管它） |
| 45 | api_store.py `note_api_error` api_id 解析（原:395→现:392-396） | 假警报 | 解析死→api_id=""→provider 状态不标（显示层） | 调度真依赖的模型级 `_quota_dead` 标记照写（:402-405，且写失败有 warn :406-407）；`if api_id:` 只跳过 provider 级显示状态 |
| 46 | api_store.py `load_custom_models`（原:608→现:602-610） | **真坏** | 自定义模型表损坏当 {}→下一次 `save_custom_model` **整文件覆写**，其余自定义模型全毁 | 覆写链：:606-609（坏→{}）+ save_custom_model :617-638（load→改一条→整文件 write_text，无条件写）。与 #40/#41/#46 同一 S1+覆写形状 |
| 47 | project.py `effective_constraints` 内的 warn（原:277→现:272-278） | 假警报 | 告警通道死→§60 探针信号少一条 | 兜底值本身照常返回（:279）；吞的只是"诊断的诊断"；通道单点是 #26 的事，修 #26 全仓受益 |
| 48 | project.py `_gate3_admission` 内的 warn（原:316→现:313-317） | 假警报 | 同上 | 入门票的实体记录是 issues.append（:308-312），**在 try 外**，不受这个吞影响 |
| 49 | project.py `get_projects_root`（原:378→现:368-382） | **真坏** | settings.json 损坏→静默回默认根→**用户所有项目从界面"消失"**，且此处无任何出声 | :378-379 except 连 warn 都没有；回退根上 `mkdir` 照建（:381），后续新项目还会写到错误根上造成分裂；数据虽在盘上但用户视角"全没了"+无诊断线索。另 set_projects_root :390-397 损坏时 data={} 整写，会抹掉 settings 其他键 |
| 50 | project.py `load` 内的 warn（原:540→现:534-542） | 假警报 | 项目文件损坏→witness.warn(load_failed)+返 None | **主行为已出声**（:537-539 warn 在）：吞掉的只是 warn 自身失败，即 #26 的病；返 None 语义=调用方按"不存在"处理，有日志可查 |
| 51 | project.py `list_all` 内的 warn（原:680→现:674-682） | 假警报 | 同上：unlistable 文件 warn+跳过 | 注释明说"不能静默 continue"并已加 warn（:675-679）；吞的只是 warn 自身失败（#26） |
| 52 | _api_admin.py `agent_create` 空壳警示（原:83→现:70-84） | 假警报 | 警示生成器死→用户没被提醒"配了空壳 agent" | 损失的只是一段**创建时**的 UX 提示；空壳 agent 本来就不会被调度（这是既有的配置问题，不是这次吞出来的）；触发前提是 registry 读挂（罕见） |
| 53 | _api_admin.py `model_remove` purge（原:224→现:221-225） | 假警报 | purge 死→「阶段→模型」名单留死键 | 死键后果=界面看着像配了、实际调不动（:219-220 注释自己写的"纯噪声"）；删模型主流程照常成功；无错误执行路径 |
| 54 | _observer_tools.py `_tool_create_task` 分类兜底（原:135→现:130-136） | 假警报（与清单的 C 级相左） | 分类挂→gate=False、type=default→**可能少跑一道 gate** | 下游有硬兜底：validator.py:50 `gate_required or _gate_check_by_files(changed_files)`——文件级判据仍在；且分类挂的窗口（LLM 接口坏）里 gate 脚本大概率同挂→validate 走"未知→retry/abort"（validator.py:79-82）fail-closed；残余风险=分类器单独坏+文件模式不匹配的窄窗。方向确实宽松，加 warn 便宜，但到不了"真坏" |
| 55 | permission.py `pending_approvals`（原:269→现:266-270） | 假警报 | 坏审批文件不显示→幽灵条目不可见 | 坏文件本来就不可决策（decide_approval :296-299 读坏也只返 False）；等待方超时→**按拒绝处理**（fail-closed，permission.py 语义）；跳过只是不再显示一个已死的请求 |
| 56 | permission.py `decide_approval`（原:298→现:296-299） | 假警报 | 读坏→返 False→界面告诉用户"这条已经不在了" | docstring 明说返 False 就是为了让界面如实说（:290-291）；写路径失败还有 witness.warn（:307-308）；决定没有"丢失"——是根本无法形成 |
| 57 | tracker.py `read_task`（原:138→现:132-139） | **真坏** | 任务文件损坏→**视为不存在**→全仓几十个调用点当"没有"处理，任务凭空消失且永不恢复 | 消费面太宽：workflow._flag_*（:754/:817）、handle_gate3_reject :922、调度链……没有任何一处会知道"它坏过"；非原子 `_write`（:127）+进程被杀即可触发；S3"用户看到的是什么都没发生"本形 |
| 58 | tracker.py `ready_tasks` 扫描（原:338→现:335-339） | **真坏** | 损坏任务不在扫描集→**永远不会被调度**，无声 | 这是调度主扫描（:317-341 整段持锁）；PENDING 任务文件一坏=永不运行、无 error、无恢复；与 #57 同一损坏事件的两张脸（读单任务/扫全目录），修时一起加 key=task_file_corrupt |
| 59 | tracker.py `recover`（原:419→现:416-420） | 假警报 | 损坏的 inflight 任务不被恢复→卡死 | 但它已因损坏对 #57/#58 不可见，recover 跳过不新增伤害（文件对一切消费者都等于不存在）；修复 #57/#58 的 warn 后此处自然有迹可循 |
| 60 | tracker.py `_load_all_tasks`（原:546→现:543-547） | 假警报 | DAG 分析/展示少任务 | 分析与展示层；调度真路径是 ready_tasks（#58）；缺一个任务对指标是少报不是编造 |

**批次3小结**：真坏 5（#41/#46/#49/#57/#58），假警报 15。api_store/models_custom 的"损坏→空→整写"链与 tracker 的 task_file_corrupt 家族是本批两个实锤；#54 与清单分歧（原判 C/3 fail-open，我判假警报：validator.py:50 的文件级兜底 + 门禁同挂即 fail-closed）。
---

## 批次4：out-11 + out-12 前半 —— 20 条

| # | 位置（原:现） | 判定 | 后果一句话 | 为什么（调用点证据） |
|---|---|---|---|---|
| 61 | _api_projects.py `project_cost`（原:330→现:328-333） | **真坏** | 用量账读挂→stats={}→**界面上"今日花费 $0.00"**——把"不知道"当 0 给人看 | _api_projects.py:332-333 直接 `cost = … or 0.0` 进 HTTP 响应，无 error 字段、无"—"态；与 sidecar 家族不同，这条的 0 没有上游 None 化，是 S2 编造数字的展示端 |
| 62 | supervisor.py `_check_artifact` lint（原:316→现:305-317） | 假警报 | ruff 超时/挂→lint 静默缺席，门禁照写"通过" | 最常见的 FileNotFoundError 已披露（:314-315 "ruff not installed, skipped"）；lint 只是三道机械检查之一（py_compile+tests 仍在），且它缺席只会放宽、不会误拦；残余=超时分支无痕，建议补 evidence 标记但不至真坏 |
| 63 | supervisor.py `_check_artifact` tests（原:332→现:325-333） | 假警报 | test runner 挂→门禁无 tests 证据通过 | 主路径 tests_result 由 _review 复用（:320-324），runner 挂在 _review.py:299-300 已 append 警告披露；else 分支的 run_project_tests 全函数几乎不可能抛（逐 runner 吞、终态 return）；注释说"记在 supervisor 日志"确实没记——文档烂，不是行为坏 |
| 64 | _io.py `apply_json_patch`（原:214→现:211-215） | 假警报 | 补丁应用失败→静默返回原方案 | docstring 明文契约"宁可保留初稿也不引入坏数据"（_io.py:202-203）；后果=辩论轮的一次修订丢失、无重试——质量损失但方向安全；失败触发常是模型吐了坏补丁，非环境故障 |
| 65 | execution_judge.py `_load_fusion_config`（原:32→现:26-33） | 假警报 | fusion.toml 手改坏了→静默用默认提取员/裁判 | 只读文件、无代码写回（无覆写链）；extract 优先级是 phase_models > fusion.toml > 默认（execution_judge.py:449-450），fusion.toml 本就是兜底轴；受损人群=手改 toml 的人，行为回退到合理默认。值得加一行 warn，不到真坏 |
| 66 | execution_judge.py fusion 记账（原:116→现:101-117） | 假警报 | 记账死→**最贵单次调用**不在账上 | 缺行≠编造 0；消费方是成本展示；且该函数其他失败路径都有 witness.warn（:130/:150/:156/:168）——记账这一处是同函数里唯一没响的，值得补齐 |
| 67 | execution_judge.py `_call_model` note_api_error（原:173→现:170-174） | 假警报 | 熔断标记死→死 provider 晚一轮被记 | HTTP 失败本身**已 witness.warn**（:168-169 http{status}），失败是响的；note_api_error 每次非 200 都再试；同 #4 |
| 68 | execution_judge.py `_model_discipline` 读（原:514→现:511-515） | 假警报（家族的真坏在写侧） | 纪律表坏→{}→定稿人回退"提分歧最多者" | 只是启发式降级到合理回退（_pick_writer :524-529 有无数据分支）；**覆写丢历史**的实锤在 record_scope 写侧（out-14 的 _model_discipline.py:41，批次5 判）；此处读侧不加信号不加重后果 |
| 69 | executors/anthropic_api.py `collect_changes`（原:237→现:230-245） | 假警报（与清单 B/4 相左） | git 挂→空集→**不是"审查静默跳"，而是任务被硬判"未产出任何文件"重试/失败** | 空集进 validator.validate 的 `if not changed_files` 早退（validator.py:45-48）→retry/abort——方向 fail-closed，绝不放行；真正的害处是病因被掩成"没产出"（误诊+烧重试），docstring 已修掉更险的"跟 HEAD 比恒空"变体（:217-222 加了 warn） |
| 70 | skills/skill_loader.py `_parse_frontmatter`（原:111→现:109-112） | 假警报 | YAML 坏→元数据空→该技能"缺 name"带错误标记 | errors 附在 SkillDef.errors（:197-199），损的是单个技能（多半用户自己改坏的）；不涉数据/门禁 |
| 71 | skills/skill_loader.py `_load_one`（原:194→现:190-195） | 假警报 | 读不动→注册表少一个技能 | 单技能缺位；触发=权限/IO 故障（罕见）；无下游决策吃它 |
| 72 | skills/skill_loader.py `_build_function_def`（原:209→现:203-210） | 假警报 | function_def 构建死→agent 少一个工具 | 模型侧能力降级、静默；agent 照跑；无账/门 |
| 73 | skills/skill_loader.py `get_agent_skills`（原:303→现:293-304） | 假警报 | agents_custom.json 坏→绑定空→agent 无技能裸跑 | 技能绑定是 prompt 增强料；**该文件的覆写实锤在写侧 #74**；读侧本身只是缺料 |
| 74 | skills/skill_loader.py `set_agent_skills`（原:323→现:320-324） | **真坏** | 读坏→data={}→:334 **整文件写回**→agents_custom.json 里其余所有自定义配置被抹 | 覆写链：:322-324（坏→{}）+ :333-334（无条件 write_text）；一次"给某 agent 绑技能"操作=其他 level/model 的绑定全没。S1+覆写 |
| 75 | observer/mcp_server.py `_build_mcp_tools`（原:18→现:14-19） | 假警报 | import 死→MCP 暴露 0 工具、无日志 | import 挂=observer_agent 层坏了，同进程内处处会响；0 工具是诚实输出；值得补 logging，危害低 |
| 76 | _api_tasks.py `task_delete`（原:340→现:337-341） | 假警报（驳清单🔴"错仓库"的实害） | repo_root_for 挂→回退奇点仓→清理跑错仓库 | `cleanup_task_artifacts` 的删除**全部按 task_id 键控**（_worktree.py:83-109：patch/cancel/snapshot 都在全局目录按 id 找，worktree glob `{task_id}_*`）——错仓里不存在这些路径，删不到任何东西；真后果=项目仓的 worktree/pending ref **漏清**（垃圾残留），奇点仓本体无损 |
| 77 | _api_tasks.py `task_retry`（原:398→现:395-399） | 假警报（同上驳 C/4） | 同 #76：清理扑空→retry 带着旧 anchor_ref/脏 worktree 重跑 | 同 #76 的键控论证——"回滚毁自家仓"在调用层不成立；真后果=retry 可能撞 anchor 冲突（响的）或留垃圾；修法（拒绝回退）仍值得，但定级降到假警报 |
| 78 | _dispatch_exec.py 委员会合成（原:539→现:539-550） | **真坏** | 合成挂/抛→**静默回退第一个成员的初稿，success=True** | :539-544 与 :545-550 两条路都把 outputs[0] 当成功结果返回——"多视角碰撞→合成"这个委员会的核心价值无声消失，调用方拿到的是单人初稿却按正常结果走完全流程；无任何 warn/key。B/4 成立 |
| 79 | _observer_definition.py `_any_project_at_gate3`（原:196→现:191-197） | 假警报 | list_all 挂→False→verdict schema 不注入 | 只影响"无 project_id 旧路径"的 prompt；rollup 解析不出→handle_gate3_reject 走 qa_report 原逻辑（批次2 #25 已判保守） |
| 80 | _observer_definition.py `project_at_gate3`（原:213→现:207-214） | 假警报 | load 挂→False→该项目的 GATE3 聊天不注入 schema | 同 #79：下游兜底保守；schema 缺席只降 rollup 成功率，不伪造结论 |

**批次4小结**：真坏 3（#61/#74/#78），假警报 17。两条清单🔴红条（_api_tasks:340/398）在调用层被降级：cleanup 全按 task_id 键控，"毁奇点仓"不成立，实害是漏清垃圾；#78（合成回退单稿）是本批最实的真坏。
---

## 批次5：out-12 后半 + out-13 + out-14 前半 —— 20 条

| # | 位置（原:现） | 判定 | 后果一句话 | 为什么（调用点证据） |
|---|---|---|---|---|
| 81 | _process_ledger.py `load`（原:44→现:40-45） | **真坏** | 交付账本损坏当 []→下一次 `record()` **整文件覆写，全部交付历史没了** | 覆写链：:44（坏→[]）+ record :54 `rows=load()` + :106 整文件 write_text——每笔交付都会触发一次，损坏后第一笔新账就把历史全抹 |
| 82 | _process_ledger.py `record` 计数（原:68→现:57-69） | 假警报 | 计数死→done=0 进账 | except 只兜 import 级故障；日常路径 read_task 返 None 是静默跳（那是 #57 的病），不进这个 except |
| 83 | _process_ledger.py `record` cost（原:74→现:70-75） | 假警报（**原判的修法已经在了**） | 失败→cost=None | 现行代码 :70 先置 `cost = None`、except 只 pass——"未知写 None 不写 0"就是现状；row["cost_usd"]=None 如实 |
| 84 | _process_ledger.py `record` 写盘（原:108→现:104-109） | 假警报 | 写失败→该笔交付结局缺失 | 账本缺行≠编造；消费方 digest()→prompt 参考（#31 同判）；持续失败=磁盘级病，别处更响 |
| 85 | _cli_tasks.py `--concurrent`（原:34→现:29-35） | 假警报 | `--concurrent abc` 静默忽略、并发保持 1 | CLI 参数解析层 UX 问题；坏值落入 rest 当位置参数，不进任何数值路径；无系统状态被写错 |
| 86 | _dispatch_crud.py `_load_custom_agents`（原:17→现:12-19） | **真坏** | agents_custom.json 损坏当 {}→下一次 add/remove/purge 或绑技能就**整文件覆写** | 覆写链：:15-18（坏→{}）+ `_save_custom_agents` :21-24 整写 + **还有第二个写者** skill_loader.py:333-334（同一文件！）——两个模块谁先写谁触发抹除。与 #74 同根，修必须两处一起 |
| 87 | _dispatch_crud.py `_in_model_library`（原:108→现:102-109） | 假警报 | 注册表读挂→当作"在库里" | docstring 明写这是**故意的**："宁可多留一个标记，也不要把用户'我停用过它'丢掉"（:103-104）——方向是保用户意图；后果只是 UI 幽灵条目噪声 |
| 88 | _machine_checks.py `parse_check`（原:60→现:58-61） | 假警报 | expect_exit 写错→检查静默变"散文"、不机器跑 | 设计本就两态（argv 可跑 / 散文如实认输，:50-51）；坏配置并入散文态=少跑一道机器检查，约束在页面上仍以散文可见；无数据/门禁 |
| 89 | _observer_worker.py 循环（原:63→现:63-64） | 假警报（**原判前提已过期**） | "循环一抛线程即死"——现行代码是 log+continue，**线程不死** | :63-64 `except Exception: _log.exception(...)` 后 :67 wait 5s 继续下一轮；且带 exc_info 全栈入 scheduler.log——不是"仅一行"，也不终止 |
| 90 | _planner.py `_maybe_complete_parents`（原:58→现:55-59） | 假警报 | 坏父任务文件跳过→父卡住 | 只有**父文件本身损坏**才影响冒泡——而损坏的父任务对一切消费者已不可见（#57 的病）；本扫描跳过无关坏文件无害 |
| 91 | _worktree.py `_lock_wt`（原:178→现:171-179） | 假警报 | chmod 只读失败→个别文件仍可写 | 防护是纵深防御的一层（非安全边界）；单文件失败、触发罕见；上游 git ls-files 失败也有早退 |
| 92 | merge.py `_recover_parked`（原:95→现:86-96） | 假警报 | 坏 parked 文件→重启后该合并请求丢失→任务搁浅 | 任务本体在 tracker 里仍可见（CONFLICT_HELD 态，用户看得到卡住）；损坏文件本就不可恢复；无数据被毁，可人工重走 |
| 93 | merge.py `_park`（原:196→现:190-197） | 假警报 | 落盘失败→重启即丢→同 #92 搁浅 | 内存 _parked 仍有效（本轮会话正常）；任务进 CONFLICT_HELD 是显性的（:198-201 transition 带 error 说明）；触发=OSError 罕见。两处修时一起加 warn |
| 94 | router.py 记账（原:155→现:150-156） | 假警报 | 分类调用记账死→router 用量缺角 | 缺行≠编造；最频繁调用已在统计里（:149 注释），except 只是护栏；消费方为成本展示 |
| 95 | router.py `_llm_classify`（原:159→现:159-160） | 假警报（与清单 C 级相左） | 分类挂→RouteResult()=type default+gate False | 下游硬兜底在：validator.py:50 `gate_required or _gate_check_by_files(changed_files)` 文件级判据仍跑；分类挂窗口（LLM 不可用）里 validate 的 gate/校验脚本也挂→"未知→retry/abort" fail-closed（validator.py:79-82）；残余=分类器单独坏的窄窗里少一道 gate。加 warn 便宜，定级到假警报 |
| 96 | _dispatch_skills.py 技能匹配（原:40→现:40-41） | 假警报 | embedding 挂→降级关键词匹配，无痕 | 注释明写"embedding 不可用, 降级关键词"——** designed fallback**；技能仍会选、只是选得糙；无决策数值 |
| 97 | _dispatch_skills.py `make_permission_checker`（原:176→现:176-187） | 假警报（**原判危害已被现行代码消除**） | （原判：权限层死→None→工具全放行） | 现行 except：witness.warn(make_checker_failed) + **返回 `_deny_all` checker**（:180-187，fail-closed），注释明说"不能静默返回 None——上游见 None 就直接放行"。C/4 的实害已在先前修复中落地；剩余弱点在**上游** openai_agent._check_permission（out-16 :668，批次6） |
| 98 | _exec_context.py 项目上下文（原:163→现:163-164） | 假警报 | 上下文构建死→prompt 缺历史脉络 | 纯 LLM 上下文料（同 #28/#30/#31 族）；无决策值 |
| 99 | _model_discipline.py `load`（原:41→现:37-42） | 假警报（**批次6复核后由真坏降级**） | 纪律表损坏当 {}→下一次 record 覆写丢历史 | record 的写路径是 `atomic_write_json`（_model_discipline.py:63）——**撕裂写这一主要损坏源已被原子写消除**；覆写链只剩外部损坏（手改/磁盘）才触发，与 route_learner（#102）同构同判。丢历史后定稿人降级为"提分歧最多者"（execution_judge.py:524-529）。#68 是它的读侧脸 |
| 100 | dispatcher.py 别名去重（原:374→现:361-375） | 假警报 | canonical() 挂→跳过去重→委员会可能双席位 | 注释明写"去重失败就当没去重，不能因此让链空掉"——deliberate；触发多为 api_store 损坏（#40/#41 的病）；委员会质量降级但功能在 |

**批次5小结**：真坏 2（#81/#86），假警报 18。真坏两条（连同批次3/4 的 api_store/models_custom 族）都是"损坏→空→**write_text 直写**的 load-modify-整写"；而写路径已换 atomic_write_json 的同形（#99 纪律表、#102 route_learner）降为假警报——撕裂驱动已除，只剩外部损坏。#97 确认 _dispatch_skills:176 已修复（fail-closed+warn），原清单 C/4 不再成立，遗留短板移到 openai_agent._check_permission。
---

## 批次6：out-14 后半 + out-15 + out-16 —— 11 条

| # | 位置（原:现） | 判定 | 后果一句话 | 为什么（调用点证据） |
|---|---|---|---|---|
| 101 | goal_loop.py 判定记账（原:129→现:124-130） | 假警报 | goal 判定调用的记账死→用量缺角 | 缺行≠编造；判定失败另有 `{"met": False, "reason": judge_unavailable}` 如实回（:138-139）；消费方为成本展示 |
| 102 | route_learner.py `load_learner`（原:166→现:160-168） | 假警报（原判 B/4 降级） | 学习史损坏当空→下次 save 覆写 | 清单担心的"撕裂写→JSONDecodeError→整份覆写"**已被修**：:152-156 注释 + save_learner :175 `atomic_write_json`；覆写链只剩外部损坏（手改/磁盘）才触发 |
| 103 | _defense_patterns.py `load_patterns`（原:53→现:51-54） | 假警报 | 防御模式.md 丢失→找茬清单空 | docstring 明文"解析不了→[]，不猜"（:48-49）；消费方是 validator._defense_checklist（#23，prompt 料）；文档是仓内静态文件，丢了=checkout 坏了 |
| 104 | _memory_core.py `_read_json`（原:82→现:70-84） | 假警报（**原判的修法已经在了**） | （原判：留底失败→原文件被无声覆写） | 现行代码已做 `.corrupt` 留底 + witness.warn(corrupt_json_backed_up)（:76-81）；残留=留底自身 OSError 时仍无痕（:82-83），窄；写路径 atomic_write_json（:87-88）。本条是 S1 族的**范本实现** |
| 105 | _memory_lifecycle.py rebuild（原:52→现:49-53） | 假警报 | 损坏 trace 跳过→重建的记忆缺角 | 手动维护操作（/api/memory/rebuild）；缺数不编数；值得补跳过计数但不至真坏 |
| 106 | model_prices.py `load_prices`（原:45→现:43-48） | 假警报（原判 B/4 降级） | 价表损坏当空→下次 set_price 覆写 | set_price 走 `atomic_write_json`（model_prices.py:83）——撕裂驱动已除；空表的下游是 price_for 返 None"未配置，绝不兜底"（:60），成本如实变"未计价"而非 $0 |
| 107 | model_registry.py `_load_custom`（原:76→现:68-77） | **真坏** | models_custom.json 损坏当 {}→下次 `_save_custom` **整写覆写，自定义模型全毁** | 与 #46 同一文件的两张脸；写侧 `_save_custom` :83 **write_text 直写**（非原子）——撕裂链活着；且 api_store.save_custom_model :638 同样直写，两个模块都会触发 |
| 108 | _benchmark.py `_judge`（原:102→现:96-103） | 假警报 | pytest 跑不了（超时/环境）→判"没通过"→基准评分偏低 | 评分进 _summarize 的 S/A+/A 档（:108-110），是 advisory 离线工具、可手动重跑；TimeoutExpired 与真失败混同值得区分，但无门禁/账吃它 |
| 109 | openai_agent.py 工具参数修复（原:537→现:556-565） | 假警报 | 坏 JSON 修复路径**无痕**（清单本条说对了一半） | 就吞异常本身（:564 except→args={}）而言无害：空 args 进工具得到自解释报错（"请指定 path/command"），模型下一轮自纠。**但**顺带发现：裸 `replace("'","\"")` 修复成功时可能把转义撇号改写成 `"`（`don\'t`→`don"t`）静默写进交付物——值得按清单建议加 warn(key=tool_args_repaired)，不过那是"无日志"问题，不是"吞异常"问题 |
| 110 | openai_agent.py 修复再败回 {}（原:543→现:564-565） | 假警报 | 空 args 硬跑工具→各工具自解释报错→模型重试 | 同 #109：write_file 空路径→_safe_path 解析到目录→write_text 抛→被 _execute_tool 外层 try 接住回错误串（:703）；不产生静默后果 |
| 111 | openai_agent.py `_check_permission`（原:668→现:684-700） | 假警报（**原判 C/4 危害已被现行代码消除**） | （原判：checker 死=静默放行） | 现行代码：except → witness.warn(perm_checker_error) + **return False 按拒绝处理**（:696-699，fail-closed），docstring 明文记录这次修复（:687-691）；配套的 `_make_permission_checker` 也已改 deny_all（#97）。清单文首"已核实的安全口子"描述的是修复前的旧代码；剩余的"没注入 checker→默认允许"（:700）是无权限模块时的**设计默认**，且上游坏掉时注入的是 deny_all 不是 None |

**批次6小结**：真坏 1（#107），假警报 10。两条最高危（_dispatch_skills:176、openai_agent:668）在现行代码里都已 fail-closed 修复——清单是在修复前生成的，这解释了为什么"已核实的安全口子"在代码里找不到。S1 族按写路径继续劈开：route_learner/model_prices（atomic）降假警报，model_registry（直写）保真坏。

---

# 我改判最多的那一族（原判和我的判断差最多的）

## 结论：fail-open 门禁族 —— 清单最红的四条，调用层全部落空

清单把「挂了就往宽松走」标成全文件最高的危险方向（🔴 五条红 + 同源组特意点名 `route_fail`），原判 C/3~C/4。我逐条读到调用层后，**四条全部不成立**：

| 条目 | 原判 | 我的判定 | 调用层证据 |
|---|---|---|---|
| `_dispatch_skills.py:176` | C/4 权限层死=工具全放行 | 假警报（**已修复**） | 现行 :176-187：except→warn+返回 `_deny_all`，注释明写"不能静默返回 None" |
| `openai_agent.py:668` | C/4 checker 死=静默放行 | 假警报（**已修复**） | 现行 :696-699：except→warn+return False 按拒绝；docstring :687-691 引用本案 |
| `router.py:159` | C/3 分类挂→gate 默认 False | 假警报 | validator.py:50 `gate_required or _gate_check_by_files(...)` 文件级兜底仍在；分类挂的窗口（LLM 不可用）里 gate/校验脚本同挂→validate 走"未知→retry/abort" fail-closed（:79-82） |
| `_observer_tools.py:135` | C/3 分类挂→gate=False | 假警报 | 同上；`gate=False` 等于"普通任务"的常态默认，不是新开的口子 |

外加同向的 `anthropic_api.py:237`（原 B/4「审查整段静默跳」→ 假警报：空集进 validator.py:45-48 被硬判"未产出任何文件"→retry/abort，方向保守只是病因被掩）。

**为什么差这么多**：清单的判据是「except 之后落在宽松默认 = 坏」（形状+方向）；我判的是「这个宽松默认**在调用层被谁吃掉**」。这个仓库在 validator 层有一整套系统性兜底（文件级 gate、未知→retry/abort、`or None`），大多数宽松默认根本到不了决策位。清单自己在 §「由此得到的结论」里写过："坏不坏取决于这个默认值在不在决策位上——是语义问题，不是语法问题"。这 111 条复核相当于把那句话做了全量验证。

## 第二大改判：S1 族必须按「写路径」劈开，原判整体给了 B/4

原判把「损坏→当空→下次覆写」一律给最高档（"下次保存就把真数据覆写没了"）。调用层显示这个族的真实危害由**写路径**决定：

- **write_text 直写的（7 处，坐实甚至升级为真坏）**：api_store:58/71、models_custom.json 的两个读者（api_store:608、model_registry:76）、agents_custom.json 的两个写者（skill_loader:323、_dispatch_crud:17）、_process_ledger:44、mcp:434、project:378。撕裂一次→损坏→下一次 load-modify-整写=真数据全毁。其中 `_dispatch_crud:17`（原 B/2）因发现 agents_custom.json 有**两个独立写者**（skill_loader 也写同一文件）而升级。
- **已换 atomic_write_json 的（3 处，降假警报）**：_model_discipline:41、model_prices:45、route_learner:166（原全 B/4）。原子写消除了自撕裂这个主要损坏源，覆写链只剩"手改改坏/磁盘坏"的外部触发。route_learner:152-156 的注释就是这次修复的存档。

## 反向升级（原判低估的）

`api_store:58/71`（B/2→真坏：损坏+**任意一次成功 API 调用**的 record_alias 即毁全配置，不是"下次人工保存"）、`_observer_answer:140`（B/4→真坏：不止无痕，还**回假成功消息**"✅ GATE1 已通过"）、`_exec.py:658`（B/3→真坏：编造 conf=0.5 覆写 validate 真分，0.2→0.5 把"立即升级"错成"同模型重试"）、`workflow:690`（B/5→真坏坐实：GATE3 入门票只认 verification_ran 标记，report 丢了门照开）、`witness:73`（B/2→真坏：全仓告警单点）。

## 总账

111 条：**真坏 20 / 假警报 91 / 判不了 0**（代码本身足以判定，无一需要补信息）。
20 条真坏里 9 条是同一形状「损坏→空→直写覆写」，可共用一个修法：`_memory_core._read_json` 的「.corrupt 留底 + warn + 拒绝带病覆写」模式（:70-84 现成范本）+ 全部写路径换 `atomic_write_json`。其余 11 条各自独立（GATE 假成功 ×2、合成回退单稿、tracker 任务消失 ×2、质量 hook 编造 0.5、witness 单点、project_cost 的 $0、安全扫描零披露、settings 回默认根、验收上下文缩水）。
