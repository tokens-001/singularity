# 审测试判据-01（ZCode 外派⑬）—— ✅ **60 份全部过完了**（开头写到 §2.5 停笔，后半由续跑的批次补齐）

> 下面是**报告原文一字未改**（08:20 起笔、18:34 停笔，85 KB / 677 行）。
> 原始件 `~/Desktop/ZCode审阅/审测试判据-01.md`。
> 任务：**审"测试判据本身写没写错"**（不是审代码）—— 硬判据只有一句：
> **「这条用例，删掉被测代码里的哪一行会红？」答不上来就是假绿候选。**
> 基线 `9d0d389`；**只读**，要变异就在副本上做。
>
> ⚠️ **这份本身是中途归档**：它跑到 §2.5（批次 A5）就停了，末尾还挂着 `<!-- 待续 -->`。
> ✅ **但整件事已经收口了 —— 别照这句去"接着做"**（2026-09-15 改这个抬头就是防这个）：
> · **续跑的批次**（A1/A2/A4/A6/A7 等）的判词在 **`审测试判据-02-20260914.md`**；
> · **覆盖到此为 60/60**（`7971299` 那条提交写着"⑬ 的 60 份到此全部过完"）；
> · 它报的洞**逐条回当前树核过、修过**（下表）；最后剩下的"未核候选"那批
>   也在 **2026-09-15** 逐条变异核完（见 02 的 §8.5）。
> ⇒ **要重开的话，先看 02，别从这份的 §2 重头扫。**

## ✅ 我的核实与落地（**它报的洞，我一条条回当前树核过、修了**）

**这一份是今晚产出最实的外派** —— 它报的每一条都能落到代码/测试上：

| 它报的 | 我怎么处理的 |
|---|---|
| 🔴 **三条真洞**：`load_for_rewrite` 那一族只修了 3 处，**mcp / agents / 自定义模型漏了**（判据是模块级/路径标记 ⇒ **第一次触碰时闸门恒假、整份重建照写**） | `f33da7a` 全修（新增 `load_toml_for_rewrite`），生产入口是增删 MCP 服务器 / agent / 自定义模型 |
| 🔴 **那三条用例是假绿**：每条都先手动读一次再调写（注释写着"置上损坏标记"）——**替被测代码把洞填上了** | 同一条提交：删掉那三行，现在钉的是"第一次触碰"；变异各自变红 |
| 两条**恒真断言**（`r1==r2` 对任何确定性实现都成立 / 换个输入恒 0.3 删钳位也不出界） | `6135f68` 改成逐条钉拦截 + 真的去顶边界 |
| 两处**只测函数不测接线**（XML 工具调用解析器钉得干净、`run()` 里那句接线删掉 14 条全绿；`cleanup_orphans` 的"遍历每棵仓库"同理） | 同一条提交：各补一条驱动真链路的用例 |
| `_format_kv` 三条**子串断言过松** | `b249449` 改成精确等值 + 钉住 str 分支独有的转义 |
| `test_fusion_v2` 三条**"输入凑巧"**（EXTRACT 里两条分歧都是 A 提的、而 A 恰好是 members[0]；复读闸那条的断言值来自轮数算术） | `83e5cfb` 改成让 B 提得更多 / 把轮数上限拉到 20 / 在整段 prompt 里找 |
| `project_id` 转发**只有签名级钉子** | `09964f8` 补接线用例（不带的话融合段用量全进 `_unknown` 桶） |
| **原子性本身全仓无测**（把 `atomic_write_json` 换回裸写，那条"是合法 JSON"照样绿） | `c139b16` 直接模拟"换名那一刻崩"，钉住"正式文件还是旧的" |
| 🟡 **三条 ruff 闸门本机从未运行**（ruff 是 dev 依赖但没装 ⇒ 静默 SKIP） | 装上并复跑（**藏/放各跑一遍**：1438+3skip → 1441 全绿），并把"dev 依赖要装"写进 `CLAUDE.md` 验证节 |

**⚠️ 它报的"双保险型"假绿（删一层另一层还兜得住）我一条都没动** ——
那些用例钉的是**行为**，行为没被破坏就不算假绿（§2.8 六条、§2.5 两条都属这类）。

---

# 审测试判据-01 —— singularity 测试判据评审（只读，未改仓库任何文件）

**审计快照**：`b3f591e`（/tmp/audit-st 工作树，钉死）。
**基线说明**：任务给的是 `9d0d389`；本会话开始时 `git rev-parse HEAD` = `944cad4`，建工作树时 HEAD 已被其他会话推进到 `b3f591e`（仓库在持续提交）。按「按实际拿到的提交做」执行，快照钉在 `b3f591e`。`9d0d389..b3f591e` 共 3 个提交：2 个纯文档 + 1 个 `validator.py` 修复（带其回归测试 `test_fixes_batch5_20260914.py`）。

## 0. 方法与环境

- **唯一硬判据**：逐条问「这条用例，删掉被测代码里的哪一行会红？」——答不上来 = 假绿候选。五种形状：①前置替被测代码填洞（§69）②断言恒真/实现快照 ③锁住错误行为（兼容层需标注）④只测函数不测接线 ⑤mock 掉被测逻辑本身。
- **环境**：主仓 `.venv` 是 strict editable 安装（finder 硬指主仓 src），直接用它跑副本会让变异实验失效 ⇒ 另建 `/tmp/audit-venv`（`--system-site-packages` + `pip install -e /tmp/audit-st`），导入确认解析到 `/tmp/audit-st/src/singularity`。全套 pytest 在副本上跑（`PYTHONDONTWRITEBYTECODE=1`、`-p no:cacheprovider`），**主仓工作区全程未 checkout、零写入**；conftest 的 autouse fixture 本身会把 `QIDIAN_DIR` 及派生目录隔离到 tmp_path。
- **基线状态**：**1389 passed, 3 skipped**。3 条 skip 全在 `test_scheduler/test_no_undefined_names.py`（ruff 未安装，环境性跳过）。
- **标注**：`[M]` = 该判词在副本上跑过变异实验证实；无标注 = 读码推证。变异实验全部在 /tmp 副本上做，做完即还原（`git status` 确认干净）。
- **收集范围警示**：`pytest.ini` 的 `norecursedirs` 含 `integration` ⇒ `tests/integration/test_step5_live_verification.py` **默认收集不到、从不随全套跑**。前端 `__rewrite_sample__/` 两文件按设计不收集（未审）。

## 1. 已知案例深审：`tests/test_scheduler/test_io_quarantine.py`（36 条用例）

事发后该文件已补「⑧ 第一次触碰」一节（不预读直接写）。逐条过完 + 变异实验的结论：

**🔴 三条假绿仍活着（§69 形状 1，且被测代码的洞是活的）：**

| # | 用例 | 判词 | 证据 |
|---|------|------|------|
| 1 | `test_mcp_读坏之后写侧拒写` | **假绿 [M]** | 用例前置 `M.load_mcp_configs()  # 置上损坏标记`。**删掉这一行实测变红**：`save_mcp_configs([])` 把坏配置整份重建。被测判据 `mcp.py:568 if _MCP_CONFIG_CORRUPT:` 是**模块级标记**，第一次触碰时为 False ⇒ 洞活。生产入口 `_api_admin.py:462,471`（增删 MCP 服务器）。「第一次触碰」补测块未覆盖 mcp 写侧。 |
| 2 | `test_读坏之后_增删_agent_要拒写` | **假绿 [M]** | 前置 `crud._load_custom_agents()  # 置上损坏标记`。**删掉实测变红**：`_save_custom_agents` 整份重建。被测判据 `_dispatch_crud.py:31 if _io.is_quarantined(p):` 标记式 ⇒ 第一次触碰洞活。生产入口 `_dispatch_crud.py` 内 6 处调用（agent 增删改）。「第一次触碰」补测块未覆盖这条写路径。 |
| 3 | `test_自定义模型表坏了_model_registry_不许整份重建` | **假绿 [M]** | 前置 `assert MR._load_custom() == {}`（兼预读）。**删掉实测变红**：`_save_custom({})` 整份重建。被测判据 `model_registry.py:89 if is_quarantined(path):` 标记式 ⇒ 第一次触碰洞活。生产入口 `model_registry.py:185,194,208`。「第一次触碰」补测块未覆盖 model_registry。 |

即：**§69 说「四个错的」里只有两个改成了「读在前」（settings.json、models_custom.json 走 api_store）；mcp_servers.toml、agents_custom.json（_dispatch_crud 侧）、model_registry 侧三处仍是标记判据**（§69 的「四个」口径见 `_io.load_for_rewrite` docstring；以本次实测为准，活洞 3 处）。它们的"写侧拒写"测试全靠预读行撑绿。

**其余 33 条判为真绿（要点）：**

- `test_文件不存在才算真的空` / `test_好好的文件原样返回` / `test_损坏返回_None_而不是空` / `test_损坏时原文件一字不动且留了证据` / `test_二次损坏不毁掉第一次的证据` / `test_顶层类型不对也算坏` / `test_toml_版同契约` —— 真绿：分别钉住 `_io.load_json_or_quarantine` 的不存在→空、解析成功→原样、损坏→None+出声、原字节备份、轮转、expect 类型校验；删对应分支各行即红。
- `test_账本损坏时_读侧降级但原文件还在` / `test_账本损坏时_写侧拒绝整份重建` / `test_账本好的时候照常记账` —— 真绿：`_process_ledger.record` 用三态返回值判 None 拒写（读在前，§69 认可的对的形状），且**没有预读行**；删拒写分支或写盘行即红。
- `test_mcp_配置坏了不许拿默认值冒名` / `test_mcp_文件不存在才用默认配置` —— 真绿：钉读侧「损坏→[]不是默认值」「缺文件→默认」两分支。
- `test_读自定义_agent_损坏时降级成空` / `test_两个写者共享同一个损坏标记` / `test_文件好的时候两个写者都照常` —— 真绿：行为钉得住（删写盘行/降级行即红）。⚠️ 备注：后者的**宣称机制已脱节**——`skill_loader.set_agent_skills` 已改读在前（`skill_loader.py:327 load_for_rewrite`），B 自己读自己判坏，A 的预读删掉它照样绿；「标记记在路径上」对 B 已不是承重墙（测试仍绿因为钉的行为真成立）。
- `test_设置文件坏了_写侧拒写并且抛` / `test_设置文件好的时候照常写且保住别的键` / `test_第一次触碰_设置文件不许整份重建` —— 真绿 [M]：老用例的预读行 `P.get_projects_root()` 已是**残留**（删掉照样绿，E4 实测）；写侧守卫删掉两条都红（E5b 实测）。场景是第二次触碰，第一次触碰由补测块覆盖。
- `test_自定义模型表坏了_api_store_不许整份重建` / `test_自定义模型表好的时候照常读写` / `test_第一次触碰_自定义模型表不许整份重建` —— 真绿：api_store 的 `save_custom_model` 走 `load_for_rewrite`（`api_store.py:666` 注释明写读在前），预读残留无害。
- `test_范围纪律表坏了_不许整份重建` / `test_范围纪律表好的时候照常记` / `test_第一次触碰_范围纪律表不许整份重建` / `test_第一次触碰的对照组_改完的文件仍保住别的键` —— 真绿：`_model_discipline.record` 走 `load_for_rewrite`（`_model_discipline.py:62`），且钉了「拒写返回 False」的返回值契约。
- `test_fusion_配置坏了降级成空表但出声` / `test_范围纪律的只读那份坏了也出声` / `test_角色覆盖坏了不能只留一行_log` —— 真绿：删降级/出声/备份各行即红。
- `test_第一次触碰_agent技能绑定不许整份重建` / `test_第一次触碰的对照组_文件好的时候四处都照常写` —— 真绿：skill_loader 读在前；对照组钉「闸门不许修成什么都不写」。
- `test_S1那族里不许裸写JSON` —— 真绿（**静态源码扫描闸门**，实现快照式但失败模式真实：S1 名单文件里加一行裸 `write_text(json.dumps` 即红；属有意的 lint 闸门，非战果）。
- `test_tmp_清扫只清陈旧的` —— 真绿：钉 `atomic_write_json` 的陈旧 tmp 清扫（删清扫分支或改成全清即红）。

**变异实验记录（全在 /tmp/audit-st，已还原）：**

| 实验 | 变异 | 结果 | 结论 |
|------|------|------|------|
| E1 | 删 `test_mcp_读坏之后写侧拒写` 的预读行 | **FAILED** | 预读行承重，mcp 第一次触碰洞活 |
| E2 | 删 `test_读坏之后_增删_agent_要拒写` 的预读行 | **FAILED** | 同上（_dispatch_crud） |
| E3 | 删 `test_自定义模型表坏了_model_registry_…` 的预读行 | **FAILED** | 同上（model_registry） |
| E4 | 删 `test_设置文件坏了_写侧拒写并且抛` 的预读行 | passed | 老预读已残留（写侧读在前兜住） |
| E5 | 删 `load_for_rewrite` 粘住层返回（`return got, True`） | passed | 删错层：被「读出 None 即拒」兜住——该函数有两层守卫 |
| E5b | 删第一层守卫（`return None, False` → `return None, True`） | **两条 settings 用例 FAILED** | settings 两条真钉住守卫 ⇒ 真绿成立 |

## 1.5 tests/ 根目录 + integration（主审自审，8 个文件）

**🔴 文件级发现：四个 `tests/` 下的文件 pytest 收集 0 条、套件绿门从不覆盖：**

| 文件 | 形态 | 判据层面结论 |
|------|------|--------------|
| `tests/test_imports.py` | 无 `test_*` 函数的手工脚本（自述跑法 `python3 tests/test_imports.py`） | 「60 模块导入完整性」防线**不在套件里**。collect 0 条 ⇒ 它绿不绿与「1389 passed」无关。 |
| `tests/test_exec_run.py` | 同上（自述不依赖 pytest） | 内容是真绿的（若手工跑）：桩只换外部依赖（dispatcher/validator/worktree/git/supervisor），被测的 `_exec.run()` 本体不被 mock；worktree 建清对称不变量、§59 用量落盘接线、§67 死线倒推预算、路径 11/12「两处都收到」的接线断言都钉得住；且把 CANCEL_DIR/PARTIAL_USAGE_DIR 显式指到 mkdtemp（吸收过 §56 事故）。⚠️ 但**「全仓最复杂的函数」的回归防线在套件之外**。 |
| `tests/test_review_gate.py` | 同上（真机验证脚本） | 内容是真绿（真 git 仓 + 真 snapshot，SNAPSHOT_DIR 指到 mkdtemp，无 mock）。它的存在理由本身就是一条判据自白：「桩测试里不会自己去 commit，所以永远是绿的」——正是形状④/时序洞的自知。套件外。 |
| `tests/smoke_test.py` | 同上（对活服务器 127.0.0.1:5050 的 E2E） | 判据层面两条弱点：①「Body过大返回413」断言 `"413" in stdout or "error" in stdout.lower()`——服务器挂了/任何错误都绿（恒真倾向）；②「SSE端点有效」`"data:" in stdout or returncode==0`——curl 正常返回即绿，不验 SSE 语义。注释里自己点过同类教训（/api/memory 的 `or True`）。写生产 `.qidian/` 但带基线比对+清理，设计如此。 |

`tests/integration/test_step5_live_verification.py`：默认不收集（`norecursedirs=integration`），手工脚本（`__main__` 守卫是 2026-09-14 才补的，注释里写明曾被显式指到时 import 即 SystemExit 拖垮整轮 pytest）。不评判据（调用真模型的手工验收，从未进套件）。

**collect 得到的 3 条用例，全真绿：**

- `tests/test_env_write.py::test_write_env` — 真绿：删 `web/app._write_env` 的「更新已存在 key 不重复/保留其他行/追加/注入 os.environ」任一行即红；写 tmp_path 副本。
- `tests/test_observer_api.py::test_observer_model_set_get` — 真绿：删 `api_store` 的 observer 键存取/落盘即红。
- `tests/test_observer_api.py::test_observer_key_survives_add` — 真绿：钉「add() 读改写不许覆盖 `_observer` 键」，删合并保留键的逻辑即红。

## 1.7 前端 vitest（主审自审：12 个默认收集文件，76 条）

**基线**：副本上 `npx vitest run` → **12 files / 76 tests 全绿**（2.4s）。`__rewrite_sample__/` 由 `vite.config.ts` 的 `exclude` 按设计排除（有注释说明），未审。

**🔴 一条假绿候选成立（形状④，变异证实）：**

- `lib/api.test.ts` 全部 8 条（4 条 `typeof api.X === 'function'` 存在性检查 + 4 条 `rejectErrorPlaceholder` 函数单测）——**只测函数，不测接线 [M]**。文件注释自己写明 2026-09-14 的真事故是「`tasks`/`models`/`apiStore` 的归一化把占位符当真值往下走」——事故点在**三处调用点的包裹**上。**实测**：把 `api.ts:44/85/96` 三处 `rejectErrorPlaceholder(...)` 包裹删掉（守卫函数本身原样保留），**前端 76 条照样全绿**。整个前端套没有任何测试用真 `api.ts` 走一遍归一化（usage/AgentsTab/SkillsTab/Alerts 全都 `vi.mock('../lib/api')`）⇒ 守卫接线脱靶，删了没测试知道。

**其余 68 条判真绿（要点，删对应实现行即红）：**

- `lib/useRun.test.tsx`（3 条）—— 钉 `toast.ts` 里 `useRun` 的返回契约（成功→响应体、无体→true、失败→false）；被测 hook 真跑，只桩了环境（ResizeObserver）。注释写明这是防「死功能无声回归」的契约测试。
- `lib/useSSE.test.tsx`（3 条）—— 钉 kind 过滤 / debounce 合并 / 无 kinds 放行；Fake EventSource 只替网络对象，useSSE 本体真跑。
- `lib/useSSE.reconnect.test.tsx`（1 条）—— 钉「CLOSED + onerror → 到点重建 EventSource」；删重连调度即红。
- `lib/useVirtualRows.test.tsx`（2 条）—— 把窗口切片算术数字钉死（start/end/padTop/padBottom），删算术行即红。
- `lib/usage.test.tsx`（14 条）—— `vi.mock` 只 mock 网络层 api 模块，页面 `Usage` 真渲染；钉「未配置价格绝不显示 $0.00」「按天历史真显示」「分组计数」「空态无 NaN/Infinity」「切时间段重取 query」等；删对应渲染/取数逻辑即红。
- `lib/antd-smoke.test.tsx`（2 条）—— 冒烟钉「App.useApp().message 真出 DOM + 主题色进 CSS-in-JS」「Bubble/Sender 脱离 useXChat 可独立渲染」；后者属第三方库集成兼容性检查，有真实失败模式（库升级即红），合规。
- `components/chat/GatePanel.test.tsx`（8 条）—— 三态渲染钉得很死（问题数/通过/无法核验/无报告/`{error}`占位→核不了/无 summary 真值→核不了/琥珀色 rgb 值/projectIssues 外露）；注释里明确写了「变异验证时'只改颜色'那一刀没被抓住」才补的颜色断言。真绿。
- `components/chat/TaskCard.test.tsx`（11 条）—— verdict 三态 + `taskStateKind` 十二状态映射 + **不认识的新档 fail-closed**（「某个以后才有的档」不许落绿）+ 渲染标签对照（「别把闸门修成什么都不动」）。真绿。
- `pages/Alerts.test.tsx`（4 条）—— 钉「常驻/事件分两栏」；注释里自曝「只查 `indexOf(k) < cut` 那种写法不变红——假绿」并改成切开两栏分别断言。vi.mock 只替 api/toast，页面真渲染。真绿。
- `components/AgentsTab.test.tsx`（10 条）—— `buildPhasePayload` 纯函数 3 条 + 页面 7 条（幽灵条目/「(已移除)」/首选标记/单请求挂不拖垮整页）。真绿。
- `components/SkillsTab.test.tsx`（10 条）—— 钉「读不到 ≠ 没有绑定」三件事 + 阶段轴派生值 + 列表级三态带重试出口（钉 wiring）；注释自曝并修掉了自己历史上的两条假绿（贴模型名的宽断言、`el.click()` 触发不了 CheckableTag 的碰巧过）。真绿。

**前端小结**：12 文件 76 条，1 条判据级缺口（api.test.ts 接线脱靶），其余真绿。整体质量高——多个文件的注释里能看出作者已自发应用「删哪一行会红」判据和变异验证。

## 2. 其余文件逐条清单

批次代理逐文件评审 + 主审变异复核后收编。标注 `[M]` = 主审在副本上变异实测；未标注 = 读码推证（代理）。**批次 A8 的 7 条假绿候选全部变异复核：6 条坐实、1 条改判真绿**（详见 §2.8 与实验记录）。

### 2.8 批次 A8（12 文件）：test_timeout_salvage / test_timeout_usage / test_token_budget / test_token_budget_paths / test_tracker / test_usage_history / test_validator / test_verdict_norm / test_witness / test_witness_alerts / test_worktree_hygiene / test_xml_tool_calls

**假绿（6 条，全部变异坐实）：**

- `test_timeout_salvage.py::TestTimeoutAlsoIndexesIntoMemory::test_index_task_failure_does_not_break_reap` — **假绿 [M]**：`_account_salvaged` 内层 except（orchestrator.py:331-335）与 `_reap_futures` 外层 try（L509-514 `_strand_guard`）双保险，任删一层仍不抛。实测把内层 except 改 `raise`（M5）→ 照样绿。且只断「不抛」，连 witness 痕都没验。
- `test_timeout_usage.py::test_read_missing_is_zero_not_crash` — **假绿 [M]**：「文件缺失→(0,"")」由 exists 短路（_exec.py:232-233）与 except OSError 兜底（L236-237）双重保障。实测删 exists 短路（M6）→ 照样绿。
- `test_usage_history.py::TestRangeSlicing::test_bad_range_raises` — **假绿 [M]**：history 层校验（_token_budget.py:540-541）与 `_range_start` 兜底 raise（L530）重复，删任一层另一处仍抛 ValueError。实测删 history 层校验（M7）→ 照样绿。
- `test_validator.py::TestPropertyValidator::test_dangerous_pattern_detection_deterministic` — **假绿（恒真断言）[M]**：`r1==r2` 对任何确定性实现成立，连「rm -rf / 该被拦」都没断。实测删掉整个危险模式拦截循环（validator.py:39-42，M1）→ 照样绿。
- `test_validator.py::TestPropertyValidator::test_confidence_in_range` — **假绿（恒真断言）[M]**：该输入（空输出+空文件）conf 恒 0.3，删钳位、删任何罚分都仍在 [0,1]。实测删 `max(0.0,min(1.0,conf))` 钳位（M8）→ 照样绿。
- `test_witness_alerts.py::test_read_alerts_empty_when_no_file` — **假绿 [M]**：exists 短路（witness.py:91-92）与 except OSError（L95-96）双保险。实测删 exists 短路（M2）→ 照样绿。

**锁住错误行为（1 条，已标注兼容层/对照组）：**

- `test_timeout_salvage.py::TestCommittedWorkIsNotInvisible::test_without_snapshot_misses_them` — 锁住错误行为（**已标注**）：无删除会红（只有把无快照路径「变聪明」才红）。docstring 明写这是对照组、把「无基准看不见已提交改动」钉成预期，产品侧有如实标注（"拿不到执行前基准"）。同文件 `test_with_snapshot_sees_committed_files` / `test_copy_type_snapshot_falls_back_and_says_so` 真绿（真 git worktree + 真基准 ref）。

**⚠️ 代理误判、主审改判的 1 条：**

- `test_validator.py::TestValidatorV2::test_crossover_review_no_files` — 代理判假绿（「删早退后 fallback 同产 verdict pass」），主审实测（M3）**删掉无文件早退（validator.py:378-379）测试变红**——断言落在 summary 文案（"no file changes" vs "empty diff…"），两路分辨得出 ⇒ **真绿 [M]**。

**两个文件级接线缺口（形状④，非单条假绿）：**

- `test_xml_tool_calls.py`（14 条全真绿，钉 `_parse_xml_tool_calls` 干净）——但解析器到执行循环的**接线**（openai_agent.py:526-540，尤其 L534 `msg["tool_calls"] = _xml_calls`）全文件不覆盖；删掉那几行，14 条全绿，全 tests/ 也没有别的文件引用 `_parse_xml_tool_calls`。文件的存在理由（「整段被当普通回答丢掉」）恰是那条接线。
- `test_worktree_hygiene.py`——`_project_repo_roots` 枚举函数本身钉住（真绿），但修复本体「cleanup_orphans 改走 `_project_repo_roots()` 遍历」（_git_worktree.py:308）那行接线删掉回退成只扫 PROJECT_ROOT，无用例红。
- `test_token_budget_paths.py` 备注：删 `record()` 里的 `_sync_dir()`（L258）不红（写盘位置跟 property 走）；「换目录重载防串账」无用例钉住。

**b3f591e 专项核查**：`test_validator.py` 内没有直接钉「"ran 0 tests" 子句」的断言——本机 Python 3.14.5 下删掉该子句 `test_run_tests_no_tests` 不红（3.12+ 由 "no tests ran" 兜住）；修复的真变异测试在 `test_fixes_batch5_20260914.py`（假 subprocess rc=0 + "Ran 0 tests"，A3 批次在审）。另一修复（exit_code 与失败数分离）由 `test_run_tests_pytest_fail` 干净钉住。

**本批其余约 118 条真绿**（逐条证据由代理给出、主审抽查校准：对照实验 M4 删 `witness._fmt_duration` 秒分支 → `test_seconds` 红，与代理判词一致）。值得留名的真绿：`test_verdict_norm` 全组（生产端归一化钉得住；备注：消费端表达式是测试里复刻的，`_review.py` 若回退归一这组不红——docstring 自认）；`test_witness_alerts.test_executor_actually_wires_the_hook`（接线测试正面样板）；`test_tracker.test_only_tracker_writes_task_files` 与 `test_no_message_written_into_heartbeat_level`（源码扫描 lint 闸门，失败模式真实）；`test_token_budget` 的不造假三态（unpriced→None 不→0）全组；`test_worktree_hygiene.test_idempotent_under_symlinked_path`（macOS /var→/private/var 真复现）。

### 2.3 批次 A3（12 文件）：test_fixes_batch4/5_20260914 / test_flow_weight / test_force_output_turn / test_fusion_usage_attribution / test_fusion_v2 / test_gate3_rollup / test_gate_confirm_kick / test_git_worktree / test_io / test_isolation / test_json_concurrency

**假绿（13 条；标 `[M]` 的变异坐实，其余同族推证）：**

- `test_fusion_v2.py::test_first_speaker_picks_most_disagreements` — **假绿 [M]**：把 `_first_speaker` 整个计数循环退化成「恒取 members[0]」，实测两条用例照样绿（N1）——测试输入里"分歧最多者"恰是 members[0]，被钉的「计数取最多」逻辑删了不红。
- `test_fusion_v2.py::test_first_speaker_tie_break_by_member_order` — **假绿 [M]**：同上，平手组与空表组在退化实现下输出同为 members[0]（N1）。
- `test_fusion_v2.py::test_repeat_round_stops_ping_pong` — **假绿 [M]**：删掉复读闸（execution_judge.py:787-788 `if cur == prev: break`）实测仍绿（N2）——rounds 从 1 起、每周期 +2、max_rounds=5，R2 仍恰好调 2 次，断言值来自轮数算术巧合；要分辨应数 R3 次数或加大 max_rounds。
- `test_fusion_v2.py::test_no_disagreements_skips_dialogue` — **假绿 [M]**：删掉 `_skip_debate` 守卫让对话照跑，实测仍绿（N3）——`kinds=[p[:20]…]` 只看 prompt 前 20 字，「陈述己方理由」在模板深处进不了切片。该行为由 `test_rulings_rounds_zero_when_no_debate` 侧钉住，本条自身断言写错切片。
- `test_fusion_v2.py::test_parse_error_returns_empty` — **假绿（双保险型，读码推证）**：垃圾候选→空稿被两层联合钉住（`d.get("parse_error")` 路径 + `if not (consensus or disagreements or gains): return ""`），单删任一层另一层兜出同值；对照 `test_all_empty_extract_returns_empty` 单层即可红。
- `test_gate3_rollup.py::test_没有文件返回_None` — **假绿（双保险型）[M]**：`_read_observer_rollup` 的 exists 短路与 except 兜底同值；实测删 exists 短路（N6）→ 照样绿。
- `test_gate3_rollup.py::test_回复里没有裁决就不写` — **假绿（双保险型，读码推证）**：`_persist` 的 `if not rollup: return`（不写半成品）与读侧 except 兜底联合兜出 None；断言只查读值、不查文件不存在，前一层删掉照样绿。
- `test_io.py::TestFormatKV::test_list_value` — **假绿（断言过松）[M]**：删掉 list 分支（TOML 数组），值落 else 引号分支输出 `files = "['a.py']"`，`"a.py"`/`"files ="` 两个子串断言照样成立（N4 实测）。
- `test_io.py::TestFormatKV::test_float_value` — **假绿（断言过松，同族推证）**：删 (int,float) 分支，3.14 落 else 输出 `score = "3.14"`，子串断言分辨不出引号；同文件 `test_int_value` 用精确等值（真绿）。
- `test_io.py::TestFormatKV::test_string_value` — **假绿（断言过松，同族推证）**：对 "hello" 删 str 分支落 else 输出逐字相同；str 分支的真实差异（\r/\t/引号转义）没被测。
- `test_io.py::TestTryParseJson::test_empty_input` — **假绿（双保险型，读码推证）**：`if not raw` 早退与末尾兜底对空串产出同值。
- `test_io.py::TestTryParseJson::test_code_fence_json` — **假绿（双保险型）[M]**：删掉 ```json 围栏分支，裸 `{…}` 分支对同一输入抠出同一候选，实测仍绿（N7）；围栏分支的行为级判据只有 `test_multiple_code_fences_uses_first_valid`（真绿）。
- `test_io.py::TestTryParseJson::test_code_fence_without_json_tag` — **假绿（双保险型，同族推证）**：同上。
- `test_io.py::TestTryParseJson::test_invalid_json_no_repair` — **假绿（读码推证）**：输入 `{invalid` 连修复逻辑也救不回来，删 `try_repair` 门让修复照跑仍同值；要用「截断可修复」输入配 try_repair=False 才钉得住。
- `test_io.py::TestApplyJsonPatch::test_invalid_patch_keeps_original` — **假绿（双保险型，读码推证）**：`if ops is None: return original` 与 `except: return original` 联合兜出原方案。
- `test_json_concurrency.py::TestRouteLearnerNoLostUpdate::test_saved_file_is_valid_json` — **假绿 [M]**：把 `atomic_write_json` 换回裸 `write_text`，实测仍绿（N5）——单线程单次写无撕裂，**被修的原子性不被分辨**；整行删掉仅因文件缺失而红，非它声称钉的属性。防撕裂（需并发/崩溃窗口）全仓无专测。
- `test_json_concurrency.py::TestCorruptJsonBackedUp::test_missing_file_returns_empty_without_backup` — **假绿（双保险型，读码推证）**：`_read_json` 的 exists 短路与 except FileNotFoundError→内层 OSError pass 联合兜出 `({}, 无备份)`。

**对照组校准 [M]**：删 `_format_kv` bool 分支 → `test_bool_true` 变红（大小写构成判据）——A3 判为真绿的同类用例抽检可信。

**真绿要点**：`test_fixes_batch4/5_20260914` 全组 12 条真绿（含 b3f591e 的 `"ran 0 tests"` 修复本体——**有专属变异判据且能红**；私钥黑名单逐行钉；observer Origin 三态含两向对照）；`test_flow_weight`/`test_safe_dispatch` 接线组真绿（⚠️ 备注：`test_auto_下_bug_fix_免调研…` 单删整支不红——空描述落兜底同值，「bug_fix 描述含触发词也免调研」的短路语义未被钉，committee 方向有分辨力）；`test_force_output_turn` 5 条钉死判据函数（⚠️ run 循环里 `_force_at` 的撤工具/continue 接线无行为级测试）；`test_fusion_v2` 其余约 26 条真绿（裁决段、重试池、采纳门槛、`_demote_bare_accept` 接线、池约束两向）；`test_git_worktree` 3 条真 git 实测；`test_isolation` 5 条钉住 conftest 隔离本体（防 §56 回退的守卫测试，两面都有红条件）；`test_json_concurrency` 两条锁用例真绿（概率型但碰撞面 20 线程足够）；`test_gate_confirm_kick` 3 条真绿。

**接线缺口（形状④，文件级）**：① `fuse_architecture_v2 → _call_model` 的 project_id 转发只有签名级钉子（`test_fusion_signature_accepts_project_id`），`_cm` 内部转发行（execution_judge.py:603）删掉本文件不红；② `openai_agent.run` 循环对 `_force_at` 的实际执行无行为级测试。

---

# 附录：批次代理逐条清单（原文收录，未经删改）

主审按判据分派批次给评审代理；正文只收编假绿/缺口/要点，这里收录每批的**全部逐条判词**（真绿也包括）。主审复核结论以正文为准（如 2.8 节 M3 对 `test_crossover_review_no_files` 的改判）。

## 附录 A8（批次 A8 原文）

判据：🔴「删掉被测代码里的哪一行会红」。凡答不出的 ⇒ 假绿；把已知缺陷钉成预期的 ⇒ 锁住错误行为（注明是否兼容层/对照组）。

### tests/test_scheduler/test_timeout_salvage.py
- test_salvage_recovers_changed_files | 真绿 | orchestrator.py:_salvage_timed_out（删 L210-212 的 `git status --porcelain` 收集循环 → changed_files 空） | monkeypatch repo_root_for/_worktrees_dir 是文件系统接缝，不是替被测代码填洞
- test_salvage_marks_usage_unknown_not_zero | 真绿 | orchestrator.py:_salvage_timed_out（删 L234 `token_count = _partial_tokens or None` → AttributeError；改成 `or 0` → 红） | 同时钉 elapsed/「超时」措辞
- test_salvage_never_raises | 真绿 | orchestrator.py:_salvage_timed_out except 分支（删 L278 `return _FallbackDisp()` → 回 None） | 钉 §59「残对象」契约，docstring 有变异说明
- TestCommittedWorkIsNotInvisible.test_with_snapshot_sees_committed_files | 真绿 | orchestrator.py:_salvage_timed_out（删 base 分支 L201-207 → 已提交改动看不见；删 commits/tail → new_commits/「未合并」断言红） | 真 git worktree + 真基准 ref，非摆拍
- TestCommittedWorkIsNotInvisible.test_without_snapshot_misses_them | 锁住错误行为 | 无删除会红——只有把无快照路径「变聪明」（新增行为）才红 | 兼容层：docstring 明确标注「对照组/旧行为正是漏掉它的原因」，把「无基准看不见已提交改动」这个已知缺陷钉成预期，且产品代码里有如实标注（"拿不到执行前基准"）
- TestCommittedWorkIsNotInvisible.test_copy_type_snapshot_falls_back_and_says_so | 真绿 | orchestrator.py:_salvage_timed_out（删 L217-218「拿不到执行前基准」标注 → raw_output 缺句） | 钉「看不全就说看不全」
- test_report_says_unknown_when_no_executor_result | 真绿 | neijinglu.py:DeliveryReport.to_dict（删 L183-186 的 `... if self.executor_result else None` → AttributeError/0） |
- TestTimeoutAlsoIndexesIntoMemory.test_timeout_calls_index_task | 真绿 | orchestrator.py:_reap_futures 超时分支（删 L512 `_account_salvaged(...)`）或 _account_salvaged 的 `_mem.index_task` 调用（L322）；`force is True` 钉 L329 | 桩掉 _salvage_timed_out/_save_trace 合理（另有专测），被测的 reap→account→index_task 接线是真的
- TestTimeoutAlsoIndexesIntoMemory.test_index_task_failure_does_not_break_reap | 假绿 | 说不出单行：_account_salvaged 内层 except（L331-335）与 _reap_futures 外层 try（L509-514）互为冗余，任删一层仍不抛；两层全删才红 | 且只断「不抛」，连 witness 痕都没验

### tests/test_scheduler/test_timeout_usage.py
- test_persist_accumulates_across_dispatches | 真绿 | _exec.py:_persist_partial_usage（删 L129 累加 → 250≠350；删 L130 model 保留 → "m-a" 变 "m-b"） |
- test_read_missing_is_zero_not_crash | 假绿 | 说不出单行：「文件缺失→(0,"")」由 exists 短路（L232-233）与 except 兜底（L236-237）双重保障，删任一层仍返回 (0,"") | 双保险冗余，测试分辨不出任何一层
- test_salvage_carries_partial_tokens | 真绿 | orchestrator.py:_salvage_timed_out（删 L172 read_partial_usage → token None；删 L226-227「下界」措辞或 L246 agent_cfg → 各自红） |
- test_salvage_still_carries_tokens_when_git_part_throws | 真绿 | orchestrator.py:_salvage_timed_out（删 except 分支 L266 → AttributeError；把「用量先读」L170-172 挪进 try → NameError 甩出） | 钉「账不跟 git 陪葬」
- test_no_usage_yet_stays_none_not_zero | 真绿 | orchestrator.py:_salvage_timed_out L234（`_partial_tokens or None`） |
- test_cleanup_removes_the_sidecar | 真绿 | _worktree.py:cleanup_task_artifacts（删 L92 `_rm(config.PARTIAL_USAGE_DIR/...)` → 读回 (10,"m")） |
- test_tool_events_survive_the_timeout | 真绿 | orchestrator.py:_salvage_timed_out（删 L177 read_partial_tool_events → NameError；删 L241 tool_events 字段 → AttributeError）+ neijinglu.batch_summary（期望值逐字段给定） |
- test_dispatches_accumulate_not_overwrite | 真绿 | _exec.py:_persist_partial_usage（删 L148-151 dispatches 追加 → KeyError；删 L139-140 started_at 回写 → 末断言红）+ _mark_dispatch_started |
- test_tool_events_absent_stays_empty_not_crash | 真绿 | orchestrator.py:_salvage_timed_out（删 L177 的 read 调用 → `_partial_events` 未定义，try/except 两处引用都炸出） |

### tests/test_scheduler/test_token_budget.py
- TestNoFabrication.test_unpriced_model_reports_none_not_zero | 真绿 | _token_budget.py:_row_cost（L50 的 None 分支删/改 0 → 红） |
- TestNoFabrication.test_daily_cost_sums_only_priced_models | 真绿 | _token_budget.py:daily_cost（删 L302-303 的 `if c is not None` 过滤 → 红） |
- TestNoFabrication.test_by_project_cost_excludes_unpriced | 真绿 | _token_budget.py:per_project_usage（删 L349-351 cost 累加） |
- TestSelfHealing.test_historical_rows_recompute_after_price_is_set | 真绿 | _token_budget.py 读时现算（daily_cost/per_model_usage；改回写时算 → 补价后仍旧值 → 红） |
- TestSelfHealing.test_clearing_price_hides_cost_again | 真绿 | model_prices.py:set_price（删 L75-76「None=删键」语义 → 价残留 → daily_cost 仍 0.14） |
- TestPoisonedHistory.test_stored_cost_est_is_ignored | 真绿 | _token_budget.py:「不读 cost_est」+ 读时算钱（改回沿用存值 → 红） |
- TestPoisonedHistory.test_stored_cost_est_is_recomputed_when_price_known | 真绿 | 同上（读时用真实单价重算） |
- TestPoisonedHistory.test_legacy_rows_with_cost_est_still_load | 真绿 | _token_budget.py:UsageRecord.cost_est 字段（L35 删 → TypeError 被 _load 吞 → _daily 空） | 兼容守卫，合法
- TestBudgetWarning.test_warning_flags_unpriced_models | 真绿 | _token_budget.py:budget_warning（删 L317-318 未配价后缀 → 红） |
- TestBudgetWarning.test_no_suffix_when_everything_priced | 真绿 | 同上反向（后缀无条件加 → 红） |
- TestSystemCallsAreCounted.test_system_call_records_with_its_level | 真绿 | _token_budget.py:record_system_tokens（删 L436 `_budget.record(...)` → 无行） | monkeypatch 模块单例是必要接缝（防写生产账本）
- TestSystemCallsAreCounted.test_every_system_path_has_a_level（parametrize×5） | 真绿 | record_system_tokens（删 level 透传 → level_breakdown 拿 ""） | 备注：名叫「every system path」但只测函数透传；各调用点传对 level 未测——覆盖缺口，非假绿
- TestSystemCallsAreCounted.test_system_call_has_no_project_or_task | 真绿 | record_system_tokens L436 前三个空串参数（改带 task/project → 红） |
- TestSystemCallsAreCounted.test_zero_tokens_is_not_recorded | 真绿 | record_system_tokens L435 `if tokens > 0` 守卫（去掉守卫 → 0 也入账） |

### tests/test_scheduler/test_token_budget_paths.py
- test_budget_path_follows_qidian_dir | 真绿 | _token_budget.py:TokenBudget._path/_history_path property（L152-158 冻回 __init__ → 红） | 这就是防污染修复本体
- test_record_writes_into_current_qidian_dir | 真绿 | _path property（冻住 → 文件落别处）+ record_tokens 的 record 调用（删 → 无文件） | 备注：删 record() 里的 `_sync_dir()`（L258）这条**不**红——写盘位置跟 property 走；`_sync_dir` 的「换目录重载防串账」全批次无用例钉住

### tests/test_scheduler/test_tracker.py
- TestAttrLanding.test_route_role_is_a_real_field | 真绿 | tracker.py:Task.route_role 字段（L72 删 → 红） | 字段存在性断言属实现快照，但那正是修复本体
- TestAttrLanding.test_route_role_round_trips | 真绿 | tracker.py:_apply_attrs（删 setattr L216 → route_role 丢）+ transition 写盘 |
- TestAttrLanding.test_old_task_file_without_route_role_loads | 真绿 | tracker.py:from_dict 的 `setdefault("route_role","")`（L103 删 → 旧文件加载炸） | 兼容守卫，合法
- TestAttrLanding.test_unknown_kwarg_warns_instead_of_vanishing | 真绿 | _apply_attrs 的 unknown-warn（删 L209-211 → warns 空） |
- TestAttrLanding.test_unknown_kwarg_on_cas_also_warns | 真绿 | tracker.cas → _apply_attrs（caller="cas"） |
- TestWritePathAtomicity.test_only_tracker_writes_task_files | 真绿 | 源码扫描 lint 闸门：包内任何 `tracker._write(` 直调即红 | 按真实失败模式判，锁 lost-update 约定

### tests/test_scheduler/test_usage_history.py
- TestMidnightRollover.test_yesterday_not_folded_into_today | 真绿 | _token_budget.py:_rollup 的按天分组（L207-211 删/改「只重算今天」→ 红） |
- TestMidnightRollover.test_rollover_across_many_days | 真绿 | 同上 |
- TestIdempotentFold.test_folding_three_times_is_stable | 真绿 | _rollup/_bump（删 _bump 的 `if not old: return new` → record 阶段 AttributeError） | 弱：只验幂等不验数值，但有可红行
- TestIdempotentFold.test_fold_does_not_lose_stored_days_when_daily_is_empty | 真绿 | _rollup L206 `_days` 拷贝起点（删 → 历史 999 丢） |
- TestBackfill.test_legacy_rows_appear_without_migration | 真绿 | _load 的 `_daily` 载入（删 → totals 0） |
- TestBackfill.test_legacy_row_still_loads | 真绿 | UsageRecord.cost_est 字段（L35 删 → TypeError 被吞 → _daily 空） | 兼容守卫
- TestBackfill.test_row_without_elapsed_key_still_loads | 真绿 | UsageRecord.elapsed_s 默认值（L40 删 → TypeError） | 兼容守卫
- TestRetention.test_keeps_newest_400_days | 真绿 | _rollup L212 `[-_MAX_DAYS:]`（删 → 500 格） |
- TestRetention.test_history_reports_earliest_for_truncated_all | 真绿 | history L546 earliest=stored[0]（删/改 → 红） |
- TestUnpricedInvariant.test_unpriced_model_cost_is_none_not_zero | 真绿 | history 的 models 组装（L604-617 删 _row_cost/unpriced 过滤 → 红） |
- TestUnpricedInvariant.test_price_filled_later_reprices_whole_history | 真绿 | history L605 读时算钱 |
- TestRangeSlicing.test_dense_ascending_days | 真绿 | _range_start（删周一起/本月 1 号分支 → 起始日错）+ 稠密补零循环 |
- TestRangeSlicing.test_all_starts_at_earliest | 真绿 | _range_start L521-522 `return earliest` |
- TestRangeSlicing.test_bad_range_raises | 假绿 | history 层校验（L540-541）与 _range_start 兜底 raise（L530）互为冗余，删任一层仍抛 ValueError | 双保险冗余（主审实测 M7 坐实）
- TestRangeSlicing.test_days_are_zero_filled | 真绿 | history 补零 while 循环（L552-562） |
- TestEmptyStore.test_no_files_at_all | 真绿 | history 空店守卫：peak 的 `if active else None`（L620 删 → max 空炸）、peak_hour 缺省、earliest 缺省 |
- TestEmptyStore.test_month_range_is_dense_even_with_no_data | 真绿 | _range_start month 分支 + 补零 |
- TestEmptyStore.test_corrupt_history_file_does_not_break | 真绿 | _load 的 history except（L184-186 删 → 构造即抛） |
- TestDayKeyIsLocal.test_local_day_boundary | 真绿 | _day_key 的 time.localtime（L68 改 UTC → 00:30 案例红） | 仅在本地时区≠UTC 的机器有判别力（本机 CST 有效）
- TestDayKeyIsLocal.test_hours_bucket_uses_local_hour | 真绿 | _bucket L85 `time.localtime(r.ts).tm_hour` | 同上
- TestStreakDefinition.test_counts_back_when_today_is_zero | 真绿 | _streak L502-503（「今天为 0 往回数」删 → current 0） |
- TestStreakDefinition.test_today_counts_when_nonzero | 真绿 | _streak 主循环 |
- TestStreakDefinition.test_gap_breaks_streak | 真绿 | _streak 断档清零 |
- TestDurationPartial.test_zero_elapsed_when_not_reported | 真绿 | history 返回结构 activity.elapsed_s/max_elapsed_s 条目（删 → KeyError） | 弱：红点在「字段必须存在」
- TestDurationPartial.test_elapsed_sums_and_max | 真绿 | _bucket 的 elapsed 累加/max + history 汇总 |
- TestDurationPartial.test_shaping_does_not_divide_by_zero | 真绿 | history 空店守卫（删任一 → 空 data 上 max()/下标炸） |
- TestConsistencyWithTodayView.test_task_and_token_counts_match | 真绿 | _bucket tasks 计数（L94 删 → 口径劈叉）+ daily_total |
- TestPersistedFile.test_record_writes_daily_file | 真绿 | record 里 `_save_history()` 调用（L265 删 → 文件不落）+ `v:1` |
- TestPersistedFile.test_saved_file_round_trips | 真绿 | _load 读 usage_daily.json（L178-186）+ _rollup 合并 |
- TestConfiguredModelsAlwaysListed.test_unused_configured_model_appears_with_zero | 真绿 | history configured 补列（L586-596，删 setdefault → 没用过的模型消失） |
- TestConfiguredModelsAlwaysListed.test_unused_model_is_not_in_unpriced_warning | 真绿 | L616 `m["used"] and m["cost"] is None`（删 used 条件 → 噪声回归） |
- TestConfiguredModelsAlwaysListed.test_reports_provider_status_not_just_available | 真绿 | models 行的 provider/provider_status 字段（L609-614 删 → KeyError/断言红） |
- TestConfiguredModelsAlwaysListed.test_unused_models_sort_after_used | 真绿 | L604 排序键 `(-x[1], x[0])` |
- TestConfiguredModelsAlwaysListed.test_broken_model_config_still_returns_history | 真绿 | L587-599 try/except（删 → 配置坏整页炸） |
- TestTodayRange.test_today_is_a_single_day | 真绿 | _range_start `return today`（删 → 落到 raise） |
- TestNoTruncationOnBusyDays.test_busy_day_is_not_truncated | 真绿 | _prune_daily 按天保留（改回 `[-500:]` → 5000≠8000）+ _bucket tasks 计数 | 钉的是「不存在截断」，对改回截断敏感
- TestNoTruncationOnBusyDays.test_old_rows_are_pruned_but_history_kept | 真绿 | daily_total 的今天过滤（L283-284 删 → 888≠111） |
- TestRouteLearnerRejectsEmptyModel.test_empty_model_is_dropped | 真绿 | route_learner.py:record 的 `if not model: return`（L71-72 删 → 脏键入账） | 放在本文件属归档错位，非判据问题
- TestRouteLearnerRejectsEmptyModel.test_named_model_still_records | 真绿 | record 的 `_stats[k] = LearnerStats(...)` |

### tests/test_scheduler/test_validator.py
- TestValidatorV2.test_run_tests_pytest_pass | 真绿 | run_project_tests 的 pytest runner 项（L180 删 → runner 变 unittest → 红） | 删 pytest 输出解析块（L215-222）不红——passed 初值即 True
- TestValidatorV2.test_run_tests_pytest_fail | 真绿 | run_project_tests L213 `result["exit_code"] = r.returncode`（删 → exit_code 0）+ 退出码措辞 | 钉 e97d25d 修复
- TestValidatorV2.test_run_tests_no_tests | 真绿 | 「没找到测试」识别（删 L199-203 条件 → pytest rc=5 被当失败；删 L232-235 措辞 → 红） | ⚠️ b3f591e 新增的 `"ran 0 tests"` 子句在本机 Python 3.14.5 下删掉不红（3.12+ 由 "no tests ran" 兜住）；真变异测试在 test_fixes_batch5
- TestValidatorV2.test_run_tests_missing_dir_says_so | 真绿 | run_project_tests L174-177 目录存在检查 |
- TestValidatorV2.test_crossover_review_needs_base_to_see_committed_work | 真绿 | crossover_review：r2 钉 base_ref 接进 git diff（删 L384 → 红）；r1 钉空 diff 早退（删 L397-402 → 红） |
- TestValidatorV2.test_crossover_review_no_files | （代理判假绿；主审 M3 实测改判**真绿**——删早退后 summary 文案不同，断言分辨得出） |
- TestValidatorV2.test_post_execution_hook | 真绿 | post_execution_hook L131（删 changed_files_count → KeyError） | conf≥0.5 断言弱
- TestPropertyValidator.test_dangerous_pattern_detection_deterministic | 假绿 | 恒真断言：r1==r2 对任何确定性实现成立；删整个危险模式拦截循环（L40-43）照样绿 | 主审 M1 坐实
- TestPropertyValidator.test_confidence_in_range | 假绿 | 本输入 conf 恒 0.3：删钳位、删任何罚分都仍在 [0,1] | 主审 M8 坐实
- TestPropertyValidator.test_empty_output_low_confidence | 真绿 | post_execution_hook L129 `conf -= 0.2`（删 → 0.5 不<0.5） |
- TestPropertyValidator.test_many_files_warning | 真绿 | post_execution_hook L132 too-many 告警 |
- TestPropertyValidator.test_safe_code_passes | 真绿 | validate 的放行分支（删 L83-84 elif → 落兜底 retry → 红） |
- TestExtractJsonObj.test_nested_issues_array | 真绿 | _extract_json_obj L332 json.loads |
- TestExtractJsonObj.test_surrounded_by_text | 真绿 | _extract_json_obj L327-328 花括号截取 |
- TestExtractJsonObj.test_no_json_returns_none | 真绿 | try/except-None 契约（删 except → JSONDecodeError 甩出） |
- TestExtractJsonObj.test_invalid_json_returns_none | 真绿 | 同上 |
- TestMultiModelReview.test_parses_nested_issues_without_nameerror | 真绿 | dispatch/收集接线（删 submit 循环或 read_text → issues 空 → IndexError 红） | ⚠️ 两个「招牌」断言无判别力：NameError 或解析删掉后，not-JSON/失败兜底同样产 critical+retry+models_used——两个修复本身都没被钉住
- TestValidatorV2.test_run_tests_pytest_pass 的解析备注 / TestMultiModelReview 见上 | — | — | —

### tests/test_scheduler/test_verdict_norm.py
- TestProducerSide.test_security_verdict_normalized（parametrize×6） | 真绿 | validator.py:security_audit_review L860 `_norm_verdict(...)`（删 → "Critical" 放行） |
- TestProducerSide.test_security_clean_stays_clean | 真绿 | _norm_verdict 的 `return s or default`（删 L351 → verdict None ≠ "clean"） | 已知 fail-open 残留（缺 verdict→默认放行）未被钉，纯未覆盖
- TestProducerSide.test_qa_verdict_normalized（parametrize×4） | 真绿 | qa_acceptance_review L789-791（删归一化或 rejected 并档 → 红） |
- TestProducerSide.test_non_json_is_fail_closed | 真绿 | security_audit_review L853-858 非 JSON fail-closed（删 → AttributeError/clean） |
- TestConsumerSide.test_norm_matches（parametrize×4） | 真绿 | _review.py:_norm 的 strip().lower()（删 → "Needs_Fix" 不匹配） |
- TestConsumerSide.test_none_and_empty_do_not_match | 真绿 | _norm 的 None→"" |
- TestEndToEnd.test_capitalized_critical_reaches_consumer | 真绿 | 生产端 L860（删 → findings 丢 → 红） | 消费端表达式是测试里复刻的，_review.py 回退归一这组不红（docstring 自认）

### tests/test_scheduler/test_witness.py
- TestFmtDuration.test_seconds | 真绿 | witness.py:_fmt_duration L288 | （主审对照 M4：删秒分支实测变红）
- TestFmtDuration.test_minutes | 真绿 | L290 |
- TestFmtDuration.test_hours | 真绿 | L291 |
- TestFmtDuration.test_boundary_60s | 真绿 | L290（删 → 60 落到 "0.02h"） |
- TestFmtDuration.test_boundary_3600s | 真绿 | L291 |
- TestFmtDuration.test_zero | 真绿 | L288 |
- TestFmtAvg.test_nonempty | 真绿 | _fmt_avg L295 计算行 | 断言用 `or` 放宽，有意的实现宽容
- TestFmtAvg.test_empty | 真绿 | L295 的 `else "--"`（删 → ZeroDivisionError） |

### tests/test_scheduler/test_witness_alerts.py
- test_warn_survives_heartbeat_cleanup | 真绿 | witness.py:warn 落 alerts.jsonl（删写盘 → 红；改回写心跳 → 被 cleanup 删光 → 红） | 核心回归
- test_read_alerts_newest_first_and_limit | 真绿 | read_alerts L106 `[-limit:][::-1]` |
- test_read_alerts_since_is_inclusive | 真绿 | read_alerts L103 since 过滤 |
- test_read_alerts_empty_when_no_file | 假绿 | exists 短路（L91-92）与 except OSError（L95-96）互为冗余 | 主审 M2 坐实
- test_review_module_can_reach_witness | 真绿 | _review.py 顶部模块级 `import witness`（删 → AttributeError） |
- test_warn_never_raises | 真绿 | warn L62 `str(msg)[:500]`（删 → None 原样入库） |
- test_note_api_error_marks_quota_exhausted | 真绿 | api_store.py:note_api_error（删 L430 门槛或 L451 set_status → 红） |
- test_note_api_error_catches_chinese_wording | 真绿 | api_store.py:_QUOTA_HINTS 的「余额不足」（删 → 400+中文不命中） |
- test_note_api_error_ignores_generic_400 | 真绿 | note_api_error L430-431 门槛（删 → 普通 400 误标） |
- test_executor_actually_wires_the_hook | 真绿 | executors/openai_agent.py:_raise_for_status L938 的 note_api_error 调用（删 → 状态不变） | 接线测试正面样板
- test_no_message_written_into_heartbeat_level | 真绿 | 源码扫描 lint 闸门 |
- TestAlertKey.test_clean_prefix_is_the_key | 真绿 | _derive_alert_key L46-50 |
- TestAlertKey.test_bare_exception_is_not_a_key | 真绿 | L50 的 Error/Exception/Warning 排除 |
- TestAlertKey.test_no_colon_is_not_a_key | 真绿 | L47-48 无冒号 → "" |
- TestAlertKey.test_explicit_key_wins | 真绿 | warn L64 `key or _derive_alert_key(...)`（删 `key or` → 显式 key 丢） |
- TestAlertSummary.test_groups_by_key_and_marks_chronic | 真绿 | alert_summary 分组/chronic（L125-143） |
- TestAlertSummary.test_two_different_exceptions_do_not_collapse | 真绿 | ident 兜底 `msg:{scope}:{msg}`（L130 删 → 裸异常被并组） |
- TestAlertSummary.test_same_key_across_scopes_merge | 真绿 | 按 key 不带 scope + scopes 收集 |
- TestAlertSummary.test_since_window_filters | 真绿 | L126 的 since 透传 |
- TestAlertSummary.test_same_key_different_details_aggregate | 真绿 | 按 key 而非整条 msg 分组 |

### tests/test_scheduler/test_worktree_hygiene.py
- TestIdempotentCreate.test_idempotent_under_symlinked_path | 真绿 | _git_worktree.py:create 的 realpath 幂等块（删 L71-76 → 二次 create rc=128） | macOS /var→/private/var 真复现
- TestIdempotentCreate.test_realpath_is_the_comparison_key | 真绿（弱） | 只有删 worktree add（L80-83）才红；删幂等比对本身不红 | 钉的是环境前提，对修复本身无判别力
- TestCleanupOrphans.test_removes_orphan_keeps_registered | 真绿 | _cleanup_worktree_dirs 的孤儿比对+rmtree（L285-291） |
- TestCleanupOrphans.test_no_worktrees_dir_is_a_noop | 真绿 | L279-280（删 → iterdir 炸） |
- TestCleanupOrphans.test_missing_repo_is_a_noop | 真绿 | L275-276 exists 检查（删 → FileNotFoundError） |
- TestProjectRepoEnumeration.test_covers_project_repos | 真绿 | _project_repo_roots 的项目枚举（删 L256-262 → 红） | ⚠️ 修复本体是 cleanup_orphans 改走 `_project_repo_roots()` 遍历（L308）——该接线删掉无用例红（接线缺口）

### tests/test_scheduler/test_xml_tool_calls.py
- TestDeepSeekDSML.test_real_shape_is_parsed | 真绿 | openai_agent.py:_parse_xml_tool_calls（DSML 正则 L77-82 + 解析循环） |
- TestDeepSeekDSML.test_multiline_content_survives | 真绿 | 正则的 re.S（删 → 跨行不匹配 → IndexError） |
- TestDeepSeekDSML.test_dsml_without_calls_wrapper_still_parsed | 真绿 | _DSML_INVOKE_RE 不要求外层包裹 |
- TestDeepSeekDSML.test_ascii_pipe_variant_also_parsed | 真绿 | _BAR `[|｜]{1,2}`（删半角 `|` → 红） |
- TestDeepSeekDSML.test_plain_text_still_returns_none | 真绿 | L101 `_looks_like_tool_markup` 闸 |
- TestDeepSeekDSML.test_mentioning_the_word_invoke_is_not_markup | 真绿 | _looks_like_tool_markup 只认 "<invoke"/DSML hint |
- TestParseXmlToolCalls.test_real_shape_is_parsed | 真绿 | _XML_INVOKE_RE/_XML_PARAM_RE 解析 |
- TestParseXmlToolCalls.test_multiple_invokes | 真绿 | finditer 多条 + 顺序保持 |
- TestParseXmlToolCalls.test_missing_format_returns_none | 真绿 | markup 闸 |
- TestParseXmlToolCalls.test_malformed_invoke_returns_empty_list | 真绿 | None/[] 语义分野 |
- TestParseXmlToolCalls.test_no_parameter_tags_gives_empty_args | 真绿 | args 组装行 |
- TestParseXmlToolCalls.test_ids_are_unique | 真绿 | id 生成 |
- TestParseXmlToolCalls.test_single_quotes_also_work | 真绿 | 正则 `["\']` |
- TestParseXmlToolCalls.test_multiline_content_survives | 真绿 | re.S |
- 【文件级】解析器→执行循环的接线（openai_agent.py:526-540，尤其 L534）全文件不覆盖，全 tests/ 无别处引用 `_parse_xml_tool_calls` —— 形状④文件级缺口

**A8 批次代理「判不了/没读」自报**：test_fixes_batch5_20260914.py 未逐条评审（A3 批次已审，全真绿）；neijinglu.batch_summary 函数体未逐行读；时区按本机非 UTC 假定；docs/防御模式.md 未读。

## 附录 A3（批次 A3 原文）

路径约定：`base.py` = src/singularity/scheduler/executors/base.py，其余同目录；行号以快照 b3f591e 为准。主审变异复核结论见正文 §2.3（N1-N7 坐实 13 条中的 7 条型）。

### tests/test_scheduler/test_fixes_batch4_20260914.py
- test_无后缀私钥那族要被拦(parametrize) | 真绿 | base.py:_BLOCKED_PATTERNS/is_blocked_path（删 `:44-45,50` 的 `"id_rsa"…"/.netrc", ".flaskenv"/".ssh/"` 任一行 → 红） | 正向钉黑名单内容
- test_正常文件别误伤(parametrize) | 真绿 | base.py:_BLOCKED_PATTERNS（红向是**加行**：把 `.crt` 等加回黑名单 → 误拦 → 红） | 负向对照，钉"证书不拦"的刻意决定
- test_模块级_run_command_要脱敏_env | 真绿 | openai_agent.py:_run_command（删 `:1155` safe_env 推导或 `:1157` `env=safe_env` → 红） | 真跑 env，行为级
- test_anthropic_每轮都要重算超时 | 真绿 | anthropic_api.py:run（删/挪出 `:97` 的 `_tmo = max(1.0, min(_tmo_base, left))` → 各轮 timeout 相等 → 红） | 只 mock httpx.post 与工具，未 mock 被测超时逻辑
- test_observer_默认绑回环不许对外 | 真绿 | observer/config.py:20 `DEFAULT_HOST` 与 bridge.py:202 host 默认值（改/删任一 → 红） | 钉默认常量，失败模式真实
- test_外部网页的_Origin_连不上_observer_ws | 真绿 | observer/server.py:ObserverServer.start（删 `:265` `origins=ws_allowed_origins()` → 红） | 真起服务真连
- test_本机_UI_的_Origin_连得上 | 真绿 | _auth.py:ws_allowed_origins/_LOCAL_ORIGIN_RE（删本机正则 → 红） | 对照防焊死
- test_不带_Origin_的客户端连得上 | 真绿 | _auth.py:256（删 `None` → 红） | 对照防误杀
- test_bridge_ws_也要校验_Origin | 真绿 | bridge.py:start_ws_server（删 `:158` → 红） | 真起 5051 线程服务
- test_预算耗尽要报_deadline_否则会被当失败换模型 | 真绿 | anthropic_api.py:run（删 `:95-96` `error_kind="deadline"`、或改写 `:132-133` 分支文本 → inspect.getsource 匹配失败 → 红） | **源码文本快照钉法**，重排版会假红，但"修法被回退即红"失败模式真实

### tests/test_scheduler/test_fixes_batch5_20260914.py
- test_老版本_unittest_的_0_测试不算通过 | 真绿 | validator.py:run_project_tests（删 `"ran 0 tests" in low` 子句 → unittest "Ran 0 tests"+rc=0 落进 rc==0 支 → 红；docstring 自带变异判据） | 钉的正是修复本身（b3f591e）
- test_有测试时不受这句话影响 | 真绿 | 同一处（红向是判据放宽误伤 "Ran 3 tests"） | 反向保护

### tests/test_scheduler/test_flow_weight.py
- test_light_两样都省 | 真绿 | project.py:resolve_flow 的 `declared == "light"` 分支 |
- test_heavy_两样都跑 | 真绿 | resolve_flow 的 `declared == "heavy"` 分支 |
- test_auto_下_bug_fix_免调研但委员会照开 | 真绿 | bug_fix 分支（committee 方向改 False → 红） | **缺口**：单删整支不红——空描述下落兜底同值；bug_fix 短路语义（描述含触发词也免调研）没被钉
- test_auto_下_模板命中走调研 | 真绿 | `_RESEARCH_TEMPLATES` 分支（删 → 落兜底 → 红） |
- test_auto_下_真实UI形态_描述短且无触发词 | 真绿 | 兜底 `return FlowDecision(False, True, "auto", …)`（fail-closed 双值） |
- test_auto_下_描述命中触发词 | 真绿 | `if hit:` 分支（删 → 红） |
- test_非法值当_auto_处理且不抛异常 | 真绿 | 负向钉，防把宽容处理改严 |
- test_老项目文件没有该键也能读 | 真绿 | project.py:73 默认值 + from_dict:153 setdefault | 兼容性契约
- test_needs_research_只是转发 | 真绿 | workflow.py:_needs_research（删转发 → None≠bool → 红） |
- test_safe_dispatch_轻量项目传_false | 真绿 | workflow.py:_safe_dispatch（删 `:203-204` 或 `:214` → seen 值变 → 红） | 接线行直接被钉
- test_safe_dispatch_重量项目传_true | 真绿 | 同上 |
- test_safe_dispatch_显式覆盖优先 | 真绿 | workflow.py:203 的 `if allow_committee is None:` 守卫 |
- test_committee_allowed_四条件 | 真绿 | _dispatch_exec.py:_committee_allowed（四断言各对应一条件） |
- test_立项_轻量_直接进架构且留下理由 | 真绿 | workflow.py:start_project_workflow（删 `:979-982` add_lineage 块 → 红） |
- test_立项_auto走调研并把判据写进轨迹 | 真绿 | 同上 |
- test_建议器_短描述无触发词才建议 | 真绿 | project.py:suggest_flow |
- test_建议器_不越权 | 真绿 | suggest_flow 四断言四条件 |
- test_建议器_不写任何状态 | 真绿 | 纯度契约的负向钉 |
- test_模板名别名_前端发的_bugfix_收敛到_bug_fix | 真绿 | project.py:570-575 `_TEMPLATE_ALIASES` |
- test_模板名别名_未知值原样放行 | 真绿 | normalize_template 负向钉 |
- test_模板校验集合从_TEMPLATES_派生 | 真绿 | project.py:valid_templates（删 `| frozenset(_TEMPLATE_ALIASES)` → 红） |
- test_前端下拉的模板都有定义 | 真绿 | project.py:TEMPLATES | 与上条两方向哨兵

### tests/test_scheduler/test_force_output_turn.py
- test_reserves_a_final_turn | 真绿 | openai_agent.py:force_output_at:279（删钳制 → 红） |
- test_real_config_case_is_fixed | 真绿 | 同上（回退旧公式 → 红） |
- test_normal_config_unchanged | 真绿 | `max_tool_turns + 2` 分量 |
- test_never_zero_or_negative | 真绿 | `max(2, …)` 下限 |
- test_always_leaves_room | 真绿 | max_turns-1 钳制 | run 循环里 `_force_at` 的接线无行为级测试（缺口）

### tests/test_scheduler/test_fusion_usage_attribution.py
- test_stream_records_under_project_when_given | 真绿 | execution_judge.py:_stream_once（删 `:108-115` → 红） |
- test_stream_falls_back_to_system_tokens | 真绿 | else 分支 `record_system_tokens`（删 → rows 空 → 红） |
- test_call_model_forwards_project_id | 真绿 | execution_judge.py:_call_model（删 `:142` 透传 → 红） |
- test_fuse_signature_accepts_project_id | 真绿 | fuse_architecture_v2:579 签名参数 | **签名级快照钉法**：fuse 内部 `_cm` 转发行(:603)删掉本文件不红——行为转发无专测（接线缺口）

### tests/test_scheduler/test_fusion_v2.py
- test_pick_writer_prefers_disciplined_model | 真绿 | _pick_writer（删 scored/min 逻辑 → 红） |
- test_pick_writer_falls_back_without_data | 真绿 | `return _first_speaker(…)` 兜底（删 → None → 红） |
- test_finalize_prompt_carries_original_plans | 真绿 | `plans=_plans_block(plans)`（删 → 红） |
- test_first_speaker_picks_most_disagreements | 假绿 | 删计数循环、退化成恒取 members[0]，输出仍 "A" → 不红 | 主审 N1 实测坐实
- test_first_speaker_tie_break_by_member_order | 假绿 | 平手组与空表组在退化实现下同为 "A" | 主审 N1 坐实
- test_insist_then_agree_gives_point_to_responder | 真绿 | 裁决段 insist+agree 分支（删 → winner 断言红） |
- test_insist_then_question_keeps_writers_view | 真绿 | `conf == "agree"` 守卫 |
- test_repeat_round_stops_ping_pong | 假绿 | 删复读闸后 R2 仍恰好调 2 次（轮数算术巧合） | 主审 N2 坐实
- test_gain_rejected_if_any_side_rejects | 真绿 | 采纳门槛（按段切分断言可分辨） |
- test_parse_error_returns_empty | 假绿（双保险型） | parse_error 路径与空 extract 守卫两层同值，断言分辨不出 |
- test_empty_extract_retries_then_gives_up | 真绿 | retry_pool 重试循环与去重 |
- test_extract_retry_recovers | 真绿 | 同上 |
- test_extract_retries_with_member_when_pool_exhausted | 真绿 | 第二档候选（删 → 红） |
- test_all_empty_extract_returns_empty | 真绿 | `if not (consensus or …): return ""`（单层即可红） | 与 test_parse_error 互补
- test_no_disagreements_skips_dialogue | 假绿 | 删守卫对话照跑仍绿：`kinds=[p[:20]…]` 切片太浅 | 主审 N3 坐实；行为由 test_rulings_rounds_zero_when_no_debate 侧钉住
- test_plans_total_cap_splits_evenly | 真绿 | `_plans_block` 均分 |
- test_single_plan_passthrough | 真绿 | `if len(plans) < 2: return plans[0][1]` |
- test_confirm_issues_triggers_one_rewrite | 真绿 | 确认循环三断言三行 |
- test_call_model_falls_back_to_reasoning | 真绿 | reasoning 兜底块 |
- test_call_model_truly_empty_still_warns | 真绿 | empty_content 告警 |
- test_confirm_empty_is_warned | 真绿 | fuse._check 告警 |
- test_demote_bare_accept | 真绿 | _demote_bare_accept 降级条件 |
- test_bare_accept_does_not_converge | 真绿 | ROUND2 收票处的接线（删 → 提前收敛 → 红） |
- test_extractor_stays_member_when_no_usable_alternative | 真绿 | `and _usable(m)` 条件 | 依赖测试环境 `_usable("A")=False` 前提
- test_bare_adopt_stance_not_counted | 真绿 | gain_votes 收票处接线 |
- test_rulings_out_param_records_the_debate | 真绿 | `rulings.update(…)`（删 → KeyError） |
- test_rulings_rounds_zero_when_no_debate | 真绿 | `rounds = 0` 预绑定（删 → NameError） | 顺带兜住 test_no_disagreements 的缺口
- test_second_confirm_issues_are_warned | 真绿 | `if attempt: witness.warn(…); break` |
- TestExtractorRespectsActivePool.test_no_swap_to_model_outside_pool | 真绿 | `and _usable(m)`（删 → 池外 → 红） |
- TestExtractorRespectsActivePool.test_swap_still_works_when_fallback_is_in_pool | 真绿 | 换兜底块（删 → 红） |

### tests/test_scheduler/test_gate3_rollup.py
- TestParseRollup.test_正常 | 真绿 | _observer_definition.py:parse_verdict_rollup |
- TestParseRollup.test_夹在自由文本里也能抠出来 | 真绿 | `re.finditer(r'\{[^{}]*\}')` 提取循环 |
- TestParseRollup.test_不合法一律_None(parametrize) | 真绿 | `:172` 枚举校验 + `:167 except: continue` | fail-closed 契约
- TestParseRollup.test_枚举是这几个值 | 真绿 | `:144-145` 两个 frozenset |
- test_落盘后读得回来 | 真绿 | workflow.py:_read_observer_rollup |
- test_没有文件返回_None | 假绿（双保险型） | exists 短路与 except 兜底同值 | 主审 N6 坐实
- test_写读闭环 | 真绿 | _persist_gate3_rollup 的 `path.write_text(…)` | 钉读写同路径接线
- test_不在_GATE3_就不写 | 真绿 | `if not project_at_gate3(project_id): return` |
- test_回复里没有裁决就不写 | 假绿（双保险型） | `if not rollup: return` 与读侧 except 联合兜出；断言不查文件不存在 |
- test_文件里是非法值也返回_None | 真绿 | 读侧二次校验（手写文件绕过写侧，非冗余） |
- test_有汇总时按汇总路由 | 真绿 | handle_gate3_reject 的采纳块 |
- test_没有汇总时行为跟改动前一致_无报告回实现层 | 真绿 | `fix_route = "impl"` + lineage no_qa 字段 | 指无汇总分支，非兼容层问题
- test_汇总能盖过无报告时的默认 | 真绿 | rollup 优先块 |

### tests/test_scheduler/test_gate_confirm_kick.py
- TestGateConfirmKick.test_gate1_approval_kicks_planning | 真绿 | _api_projects.py:project_gate_confirm 的 kick 块（删 → 红） |
- TestGateConfirmKick.test_gate2_approval_does_not_kick | 真绿 | 负向钉 |
- TestGateConfirmKick.test_gate3_approval_does_not_kick | 真绿 | 负向钉 |

### tests/test_scheduler/test_git_worktree.py
- test_reports_root_level_extensionless_conflict | 真绿 | merge_tree_probe 解析循环 `:244-249`（真 git 仓库） |
- test_does_not_leak_progress_messages_into_conflict_list | 真绿 | `if not line: break` + `[1:]` 跳首行 | 与上条互补
- test_empty_conflict_list_means_command_error | 真绿（弱钉） | 错误契约；本输入删解析循环不红（由前两条钉） |

### tests/test_scheduler/test_io.py
- TestFormatKV.test_list_value | 假绿 | list 分支删掉后落 else，子串断言分辨不出 TOML 数组与带引号字符串 | 主审 N4 坐实
- TestFormatKV.test_bool_true | 真绿 | bool 分支（大小写构成判据） | 主审对照实测变红
- TestFormatKV.test_bool_false | 真绿 | 同上 |
- TestFormatKV.test_int_value | 真绿 | (int,float) 分支（精确等值红） |
- TestFormatKV.test_float_value | 假绿 | 删 (int,float) 分支落 else 输出 `score = "3.14"`，子串断言分辨不出引号 |
- TestFormatKV.test_string_value | 假绿 | 对 "hello" 两分支逐字相同；\r/\t 转义差异没被测 |
- TestTryParseJson.test_empty_input | 假绿（双保险型） | `if not raw` 早退与末尾兜底同值 |
- TestTryParseJson.test_none_input | 真绿 | `if not raw` 早退（None 上 finditer TypeError） |
- TestTryParseJson.test_code_fence_json | 假绿（双保险型） | 围栏分支删掉裸分支兜出同一候选 | 主审 N7 坐实
- TestTryParseJson.test_bare_json_object | 真绿 | 裸分支（围栏分支对此输入匹配不上） |
- TestTryParseJson.test_trailing_comma_fix | 真绿 | `,\s*}` 修复行 |
- TestTryParseJson.test_trailing_comma_in_array | 真绿 | `,\s*]` 修复行 |
- TestTryParseJson.test_no_json_found | 真绿 | parse_error 标记 |
- TestTryParseJson.test_code_fence_without_json_tag | 假绿（双保险型） | 同 test_code_fence_json |
- TestTryParseJson.test_invalid_json_no_repair | 假绿 | 输入救不回来，删 try_repair 门仍同值 |
- TestTryParseJson.test_multiple_code_fences_uses_first_valid | 真绿 | 围栏分支唯一的行为级判据 |
- TestApplyJsonPatch.test_replace_nested_list_field | 真绿 | _apply_op/_set_path replace 路径 |
- TestApplyJsonPatch.test_replace_top_level_field | 真绿 | 同上 |
- TestApplyJsonPatch.test_add_new_field | 真绿 | add 路径 |
- TestApplyJsonPatch.test_remove_field | 真绿 | remove 分支 |
- TestApplyJsonPatch.test_patch_with_code_fence | 真绿 | _parse_patch_ops 提取 |
- TestApplyJsonPatch.test_invalid_patch_keeps_original | 假绿（双保险型） | `ops is None` 早退与 except 联合兜出 |
- TestApplyJsonPatch.test_non_json_original_kept | 真绿 | parse_error 守卫 |
- TestApplyJsonPatch.test_bad_path_falls_back_to_original | 真绿 | except 兜底（弱：只查 a.b 不变，auto-vivify 变体不红） |

### tests/test_scheduler/test_isolation.py
- test_alerts_not_written_to_production | 真绿 | conftest 隔离 + witness 读时现算（两面都有红条件） | 守卫测试
- test_tasks_not_written_to_production | 真绿 | conftest + tracker 读时现算 |
- test_import_time_paths_also_isolated | 真绿 | conftest 元组补刀行（删任一 → 红） |
- test_derived_paths_are_computed_at_call_time | 真绿 | 8 个入口的读时现算（防 §34 回退） |
- test_warn_actually_lands_somewhere | 真绿 | warn 落盘行 | 反向对照：隔离不等于禁用

### tests/test_scheduler/test_json_concurrency.py
- TestRouteLearnerNoLostUpdate.test_concurrent_record_outcome_keeps_every_update | 真绿 | record_outcome 的 `with _LOCK:`（删 → 20 线程丢更新） | 概率型判据但碰撞面足够
- TestRouteLearnerNoLostUpdate.test_saved_file_is_valid_json | 假绿 | 原子写换裸写照样绿——被修的原子性不被分辨 | 主审 N5 坐实；防撕裂全仓无专测
- TestTokenBudgetNoLostUpdate.test_concurrent_record_keeps_every_entry | 真绿 | record 的 `with _LOCK:` |
- TestCorruptJsonBackedUp.test_corrupt_file_is_renamed_not_silently_emptied | 真绿 | except 分支的 `path.replace(bak)` |
- TestCorruptJsonBackedUp.test_missing_file_returns_empty_without_backup | 假绿（双保险型） | exists 短路与 except→OSError pass 联合兜出 |

**A3 批次代理「判不了/没读」自报**：docs/防御模式.md 未读；三处环境前提无法静态证实（`_usable("A")=False`、CLAUDE_CLI_TIMEOUT>budget_s 已查实、并发红是概率型）；fuse→_cm project_id 转发与 openai_agent run 循环 `_force_at` 两处接线缺口值得主审知道；QIDIAN_SKIP_EMBED 与 observer server 的关系未深挖（判无害）。

### 2.5 批次 A5（12 文件）：test_no_silent_except / test_no_undefined_names / test_orchestrator / test_phase_dispatch_wiring / test_phase_models / test_phase_models_wiring / test_preflight / test_probe_constraints_checklist / test_process_ledger / test_project / test_project_budget / test_project_phase_advance

**假绿（2 条，均变异坐实）：**

- `test_phase_models.py::TestLoad::test_missing_file_is_empty` — **假绿（双层冗余）[M]**：`phase_models.load` 的 exists 短路与 `except Exception: return {}` 兜底同值（删 exists 短路 P1 → 照样绿）。行为契约本身成立，但守卫行无一可被此测独立钉住。⚠️ 这正是 §69 的**读侧**变体：读侧两层兜底同值是可接受的冗余，但意味着「删掉 exists 短路」这类修复没法有专属判据。
- `test_process_ledger.py::TestDigest::test_empty_ledger_gives_empty` — **假绿（双层冗余）[M]**：`digest` 的 `if not rows: return ""` 早退与「空 lines 的 join 天然为 ""」同值（删早退 P2 → 照样绿）。

**存疑（3 条，环境性）**：`test_no_undefined_names.py` 的三条 ruff 闸门（`test_no_undefined_names_in_src` / `test_allowlist_has_no_stale_entries` / `test_star_import_files_have_no_truly_undefined_names`）——本机 ruff 未装，恒 SKIP，**本机从未运行过、任何变异在本机不会红**。判据设计本身有效（真跑 ruff），但在当前环境里它们是「3 条永远不参与的绿」。

**未独立钉住的冗余半边 4 处**（判真绿但注明）：`test_missing_task_not_counted_as_done`（None-continue ≡ `_status_str` 容错）；`TestPurgeModel` 两条的 `if hit:` 守卫半边；`test_unconfigured_uses_pool_minus_writer`（未配置早退 ≡ 全不可用兜底，仅差一条未断言的 warn）；`test_restrict_without_lineup_changes_nothing`（`if lineup:` 守卫防误告警半边）。

**真绿要点**：`test_no_silent_except` 全组 6 条真绿（全仓静默 except **棘轮** ±1 双向红 + 嵌套 handler 量尺语义 + 阈值双侧断言 + 防回潮文本 lint——这是这套 lint 闸门的正面样板）；`test_phase_dispatch_wiring`/`test_phase_models_wiring` 全组真绿（mock 全落 collaborator——LLM 执行器、记账 sink、可用性探测——被测接线行存活；`is False` 区分 None/False 的判据写法抓得住漏传）；`test_orchestrator` 全组真绿（含 exists 检查被独立钉住的正面例子：tracker.read_task 的 except 只捕 JSON/Type/Value，FileNotFoundError 不在其内）；`test_process_ledger` 其余真绿（`_status_str` 取 value 回归点精确、`TestPathNotFrozen` 能分辨读时现算 vs 导入冻结）；`test_project` / `test_project_budget` / `test_project_phase_advance` 全组真绿（含「打桩即 fail」写法使『没配就不许查』真正可红、只读残骸 chmod 真造、§60 约束清单写入行、GATE3 入门票去重）。

**A5 批次代理「判不了/没读」自报**：collaborator 内部实现未逐行读（依赖基线全绿前提）；ruff SKIP 三条本机不可验证；test_process_ledger 不覆盖「账本损坏拒写」分支（归 test_io_quarantine）；探针类测试「定案后连测一起删」的承诺静态不可验证。

## 附录 A5（批次 A5 原文，逐条）

（原文全文收录，未经删改；假绿两条的主审变异坐实见正文。）

### tests/test_scheduler/test_no_silent_except.py
- test_scan_is_not_blind | 真绿 | 本文件内 scan()/_Scan（删 `counts.update(s.counts)` 等扫描逻辑 ⇒ 计数归 0，两条 >400/>300 自检红） | 自检型守卫
- test_no_new_silent_except | 真绿 | src 任意 handler（新增或修复一处静默 except ⇒ 棘轮 ±1 都红，双向） | 全仓棘轮；行号漂移不红是设计
- test_nested_handler_noise_does_not_count_for_the_outer | 真绿 | 本文件 _is_noisy（删 `child is not handler: continue` ⇒ 红） | 测 lint 量尺自身嵌套语义，非恒真
- test_baseline_keys_are_wellformed | 真绿 | silent_except_baseline.json（键格式改坏/值改 0 ⇒ 红） | 与棘轮 removed 方向冗余，实为诊断细化
- test_stalled_threshold_exceeds_one_legitimate_dispatch | 真绿 | witness.py `STALLED_AFTER_S = config.TASK_DEADLINE_S + 300.0`（双侧断言） |
- test_no_caller_hardcodes_a_tighter_stalled_threshold | 真绿 | 防回潮文本 lint（红条件是「新增」字面量） | 当前零命中，删现有行不红，属 lint 闸门合理形状

### tests/test_scheduler/test_no_undefined_names.py
- test_no_undefined_names_in_src | 存疑 | 需 ruff 在位才红 | **环境性 SKIP，本机从未运行**
- test_allowlist_has_no_stale_entries | 存疑 | 同上；ALLOWED 空 ⇒ 暂时空转 |
- test_star_import_files_have_no_truly_undefined_names | 存疑 | 同上；F821 星号盲区补丁，静态+运行时双确认 |
- test_consolidate_memory_runs_without_nameerror | 真绿 | _memory_consolidator.py:consolidate_memory 的函数级 from-import（删之 ⇒ NameError 被 except 吞成 warn ⇒ 断言红） | monkeypatch 机制已核有效
- test_from_import_targets_exist | 真绿 | src 任意 `from M import N` 且 N 不存在（不依赖 ruff） | checked>400 自检
- test_module_attributes_exist | 真绿 | src 任意 `模块.属性` 引用不存在（checked>500 自检） |

### tests/test_scheduler/test_orchestrator.py
- TestSchedulePolicy.test_empty | 真绿 | schedule_policy 删 `return sorted(...)` ⇒ None | 弱但可红
- TestSchedulePolicy.test_sort_by_priority | 真绿 | _score 删 `0.5 * t.priority` |
- TestSchedulePolicy.test_starvation_prevention | 真绿 | _score 删 `1.0 * t.starvation_score` |
- TestScheduleMonotonicity.test_priority_ordering | 真绿 | 同上（重复用例，无害） |
- TestScheduleMonotonicity.test_starvation_prevents_hunger | 真绿 | 同上 |
- TestScheduleMonotonicity.test_level_no_longer_affects_ordering | 真绿 | _score 删 priority 权重项 | 名实不符（level_bonus 复活此测仍绿）但断言有独立红线
- TestScheduleMonotonicity.test_deterministic | 真绿 | 非确定性排序/删 return ⇒ 红 |
- TestScheduleMonotonicity.test_dependency_weight | 真绿 | _score 删 `0.5 * dep_weight` |
- TestScheduleMonotonicity.test_empty_list | 真绿 | 同 test_empty |
- TestChaosResilience.test_decompose_bad_input | 真绿 | decompose 的坏输入早退 |
- TestChaosResilience.test_decompose_valid | 真绿 | decompose json 解析主体 |
- TestChaosResilience.test_tracker_read_nonexistent | 真绿 | tracker.read_task 删 exists 早退 ⇒ FileNotFoundError 不在 except 元组 ⇒ 真红 | **exists 被独立钉住的正面例子**
- TestChaosResilience.test_schedule_policy_1k_under_50ms | 真绿 | 性能断言（O(n²) ⇒ 超时红） |
- TestBenchmark.test_schedule_policy_1k_tasks | 真绿 | 同上 + len 钉 return |
- TestBenchmark.test_decompose_100_tasks | 真绿 | decompose 主体 |
- TestBenchmark.test_topo_sort_50_nodes | 真绿 | _topo_sort 主体 |

### tests/test_scheduler/test_phase_dispatch_wiring.py
- test_phase_cwd_is_project_repo_not_qidian | 真绿 | workflow.py:_phase_cwd（三重断言） |
- test_phase_cwd_never_falls_back_to_qidian_repo | 真绿 | 兜底若返回 PROJECT_ROOT ⇒ 红 | 兼容层式窄断言，已自我标注
- test_safe_dispatch_passes_cwd | 真绿 | _safe_dispatch 删 `cwd=_phase_cwd(project)` ⇒ 红 | 真·接线回归点
- test_research_disables_tools | 真绿 | `_safe_dispatch(..., no_tools=True)` 调用点删传参 ⇒ 红 | 形状4 的正确测法
- test_planning_disables_tools | 真绿 | 两处 no_tools=True（重试支路弱覆盖） |
- test_phase_usage_recorded_under_project | 真绿 | 删 `_record_phase_usage(...)` 行 ⇒ 红 |
- test_committee_usage_recorded_per_member | 真绿 | 删逐成员循环 ⇒ 合成名被拦 ⇒ rows 空 ⇒ 红 | 连带钉合成名跳过分支
- test_phase_usage_zero_tokens_not_recorded | 真绿 | 删 `if tokens <= 0: return` ⇒ 红 |
- test_executing_phases_keep_tools | 真绿 | 删 `no_tools=no_tools` 透传 ⇒ `is False` 断言红 | None/False 判据抓得住漏传

### tests/test_scheduler/test_phase_models.py
- TestLoad.test_missing_file_is_empty | 假绿（两层冗余） | exists 短路与 except 兜底同值 | 主审 P1 坐实
- TestLoad.test_broken_json_is_empty_not_raise | 真绿 | except return {}（单层，可红） |
- TestLoad.test_top_level_not_dict_is_empty | 真绿 | _clean isinstance 守卫 |
- TestLoad.test_unknown_phase_key_dropped | 真绿 | _clean key 过滤 |
- TestLoad.test_non_list_value_dropped | 真绿 | 同上另一半 |
- TestLoad.test_non_string_and_blank_entries_dropped | 真绿 | _clean 内层判据 |
- TestLoad.test_dedup_keeps_order | 真绿 | seen 去重 |
- TestLoad.test_empty_list_leaves_no_key | 真绿 | `if models:` |
- TestSave.test_roundtrip | 真绿 | save 删 atomic_write_json ⇒ 红 |
- TestSave.test_empty_list_deletes_key | 真绿 | 同 `if models:` |
- TestSave.test_save_filters_like_load | 真绿 | save 共用 _clean |
- TestSave.test_save_survives_non_dict | 真绿 | _clean isinstance 守卫 |
- TestPurgeModel.test_removes_from_all_phases | 真绿 | 删 save/remove ⇒ 红 |
- TestPurgeModel.test_no_hit_returns_false_and_leaves_file_alone | 真绿 | 删 `return hit` ⇒ 红 | `if hit:` 半边未独立钉
- TestPurgeModel.test_missing_file_is_noop | 真绿 | 删 `return hit` ⇒ 红 |
- TestSelection.test_nothing_configured_is_the_old_behaviour | 真绿 | 删末行 ⇒ TypeError | 兼容边界，锁正确约定
- TestSelection.test_phase_config_restricts | 真绿 | 删 return ⇒ 红 |
- TestSelection.test_project_lineup_wins_and_does_not_restrict | 真绿 | 删第一分支 ⇒ 红 | docstring 明写兼容边界
- TestSelection.test_project_lineup_for_another_level_is_ignored | 真绿 | 删 `.get(level)` ⇒ 红 |
- TestSelection.test_project_without_lineup_falls_through_to_phase_config | 真绿 | 删空判断 ⇒ 红 |
- TestSelection.test_project_none_is_safe | 真绿 | 删容错 ⇒ AttributeError |
- TestSelection.test_level_is_honoured | 真绿 | level 透传链 |

### tests/test_scheduler/test_phase_models_wiring.py
- TestRestrictToLineup.test_restrict_returns_only_the_lineup | 真绿 | dispatcher._collect 删 restrict return ⇒ 红 | 「席位限制不住」回归点
- TestRestrictToLineup.test_no_restrict_still_appends_the_rest | 真绿 | 删全量追加 ⇒ 红 | 兼容层，已标注
- TestRestrictToLineup.test_restrict_preserves_the_configured_order | 真绿 | 按 lineup 序遍历 |
- TestRestrictToLineup.test_restrict_skips_the_learner_reorder | 真绿 | 删 `and not restricted` ⇒ 学习器顶序 ⇒ 红 |
- TestRestrictToLineup.test_learner_reorder_still_works_without_restrict | 真绿 | 删学习器排序块 ⇒ 红 | 反向钉
- TestRestrictToLineup.test_restrict_falls_back_to_full_pool_when_nothing_resolves | 真绿 | 删 warn 或删 fail-open 回全池 ⇒ 红 | 两断言各钉一半
- TestRestrictToLineup.test_partial_resolution_still_restricts | 真绿 | `if res:` 判定 |
- TestRestrictToLineup.test_restrict_without_lineup_changes_nothing | 真绿 | 删收集循环 ⇒ 空链 ⇒ 红 | `if lineup:` 防误告警半边未独立钉
- TestPickReviewers.test_unconfigured_uses_pool_minus_writer | 真绿 | 删过滤/[:2] ⇒ 红 | 未配置早退与全不可用兜底同值冗余，未独立钉
- TestPickReviewers.test_designated_list_wins_and_is_not_truncated_to_two | 真绿 | 名单主循环 |
- TestPickReviewers.test_writer_is_dropped_and_warned | 真绿 | warn+剔除双断言 |
- TestPickReviewers.test_designated_preserves_configured_order | 真绿 | 保序 |
- TestPickReviewers.test_unavailable_designated_entries_are_skipped | 真绿 | 可用性过滤 |
- TestPickReviewers.test_all_designated_unavailable_falls_back_to_pool | 真绿 | warn 断言把两路分辨开 |
- TestExtractorModel.test_phase_config_beats_fusion_toml | 真绿 | `if designated: return designated[0]` |
- TestExtractorModel.test_fusion_toml_still_works_when_unconfigured | 真绿 | toml 路径（兼容层已标注） |
- TestExtractorModel.test_default_when_nothing_configured | 真绿 | `or _V2_EXTRACT_DEFAULT` |
- TestExtractorModel.test_first_entry_wins | 真绿 | 取首 |

### tests/test_scheduler/test_preflight.py
- TestProbeUrl.test_any_http_response_counts_as_reachable | 真绿 | _probe_url（stub 裸 object 恰能分辨「任何响应都算通」） |
- TestProbeUrl.test_connection_error_is_unreachable | 真绿 | except/return False |
- TestPreflightExternal.test_all_good_reports_nothing | 真绿 | 误报任一条 ⇒ 红 |
- TestPreflightExternal.test_reports_missing_embed_model | 真绿 | 删判空 ⇒ 红 |
- TestPreflightExternal.test_reports_unreachable_api | 真绿 | 删 probe 判断 ⇒ 红 |
- TestPreflightExternal.test_same_base_url_probed_once | 真绿 | 集合去重（次数断言分辨） |
- TestPreflightExternal.test_probe_exception_is_reported_not_raised | 真绿 | 删 except-append ⇒ 异常上抛 |

### tests/test_scheduler/test_probe_constraints_checklist.py
- test_write_probe_fires_and_round_trips | 真绿 | 删探针块/赋值行 ⇒ 红 | same_obj 快照断言有行为断言独立存在；探针自认临时代码
- test_read_probe_fires_only_when_constraints_are_really_absent | 真绿 | 删读取点探针块 ⇒ 红 |
- test_read_probe_stays_quiet_when_fallback_can_rescue | 真绿 | 删兜底 return arch ⇒ 红 |
- test_read_probe_stays_quiet_when_checklist_has_content | 真绿 | `if not constraints:` 守卫 |

### tests/test_scheduler/test_process_ledger.py
- TestRecord.test_tasks_done_counted_by_enum_value | 真绿 | _status_str 取 value 行（回归点精确） |
- TestRecord.test_missing_task_not_counted_as_done | 真绿 | 删 done 计数 ⇒ 红 | None-continue 与 _status_str 容错两层冗余半边未独立钉
- TestRecord.test_issue_kinds_tallied | 真绿 | 统计循环 |
- TestRecord.test_appends_does_not_rewrite_history | 真绿 | 删 append ⇒ 丢行 |
- TestRecord.test_writes_its_own_file | 真绿 | 删落盘 ⇒ 文件不存在 |
- TestRecord.test_failure_outcome_is_recorded_too | 真绿 | 删 row.update ⇒ 红 |
- TestRecord.test_never_raises_on_bad_input | 真绿 | 删 except ⇒ 上抛 | 未测「损坏拒写」分支（归 test_io_quarantine）
- TestDigest.test_empty_ledger_gives_empty | 假绿（两层冗余） | 早退与空 join 同值 | 主审 P2 坐实
- TestDigest.test_states_facts_not_lessons | 真绿 | bits 组装 + 负向措辞闸门 |
- TestDigest.test_mentions_failures_and_issues | 真绿 | 删 fail append ⇒ 红 |
- TestDigest.test_limits_to_recent_n | 真绿 | `[-limit:]` |
- TestPathNotFrozen.test_path_is_computed_at_call_time | 真绿 | _path 冻结 ⇒ 红 | 分辨读时现算 vs 导入冻结

### tests/test_scheduler/test_project.py
- TestProjectState.test_create_and_phases | 真绿 | create 的 phase 实参/id |
- TestProjectState.test_confirm_gate | 真绿 | _GATE_NEXT 推进 |
- TestProjectState.test_reject_gate | 真绿 | _REJECT_FALLBACK |
- TestProjectWorkflow.test_run_execution_creates_real_tasks | 真绿 | transition 参数/建任务循环（真落盘读回） |
- TestProjectWorkflow.test_run_phase_manual_stops_at_gates | 真绿 | GATE 分支非 auto 路径 |
- TestProjectWorkflow.test_run_phase_auto_chains_to_executing | 真绿 | auto 链（GATE1→PLANNING→GATE2→EXECUTING 全链断言） |
- TestProjectWorkflow.test_gate_confirm_approved_advances | 真绿 | _GATE_NEXT 表 |
- TestProjectWorkflow.test_gate_confirm_rejected_falls_back | 真绿 | GATE3 有意缺席（S7 分级路由），注释明示 |
- TestProjectWorkflow.test_architecture_redo_from_executing | 真绿 | set_phase + architecture=None |
- TestProjectDelete.test_delete_removes_json_and_dir | 真绿 | unlink/目录删除 |
- TestProjectDelete.test_delete_missing_project_is_false | 真绿（弱） | 恒 True ⇒ 红 |
- TestProjectDelete.test_delete_handles_readonly_agent_output | 真绿 | 退回裸 rmtree ⇒ PermissionError | chmod 真造只读残骸
- TestProjectDelete.test_repo_dir_does_not_resurrect_deleted_project | 真绿 | 兜底重新 mkdir ⇒ 复活 ⇒ 红 |

### tests/test_scheduler/test_project_budget.py
- TestBucketKeepsProjects.test_groups_by_project_and_model | 真绿 | _bucket 两级累计 |
- TestBucketKeepsProjects.test_bump_merges_projects_per_key_by_max | 真绿 | max 合并段 |
- TestBucketKeepsProjects.test_bump_tolerates_old_bucket_without_projects | 真绿 | 容错（兼容层已标注） |
- TestProjectSpendTotal.test_sums_across_days_and_prices_each_model | 真绿 | 跨天循环+计价（0.73 钉两维） |
- TestProjectSpendTotal.test_unknown_project_is_zero | 真绿（弱） |
- TestProjectSpendTotal.test_unpriced_model_counts_as_zero_not_crash | 真绿 | `if c is not None` |
- TestProjectBudgetState.test_thresholds（parametrize 6 档） | 真绿 | 阈值档位 + msg 配对 |
- TestProjectBudgetState.test_no_budget_means_no_check | 真绿 | 打桩即 fail ⇒「没配就不许查」真可红 |

### tests/test_scheduler/test_project_phase_advance.py
- test_all_tasks_failed_does_not_advance | 真绿 | `if not done_ids:` 拦截块（「FAILED 也算终态导致谎报交付」回归点） |
- test_issue_recorded_only_once | 真绿 | 去重守卫 |
- test_partial_success_still_advances | 真绿 | else 分支 set_phase |
- test_all_done_advances | 真绿 | 同上 |
- test_decompose_fallback_reads_project_architecture | 真绿 | `arch_json = proj.architecture or {}` |
- test_decompose_fallback_writes_constraints_checklist | 真绿 | §60 约束清单写入行 |
- test_no_decomposable_tasks_is_surfaced_not_silently_stuck | 真绿 | issue 块 + 去重（mock 不盖被测逻辑） |
- TestGate3Admission.test_no_evidence_adds_ticket | 真绿 | set_phase 的 GATE3 admission 接线 |
- TestGate3Admission.test_ran_marker_clears_the_ticket | 真绿 | evidence 早退 |
- TestGate3Admission.test_skipped_marker_also_counts_as_evidence | 真绿 | 标记元组 |
- TestGate3Admission.test_other_phases_unaffected | 真绿 | phase 限定（防扩大化） |

<!-- 待续 -->
