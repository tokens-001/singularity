# A/B：辩论后「融合」vs「直接取一份」（2026-09-09）

> ⚠️ **2026-09-10 更新：下文 2b / 2c 的「+7.8 / +11.0」已被推翻，不要再引用。**
> 那两个数用的基准是 `plans[0]`（链上第一份），而它实际是**最弱**的一份 ——
> 换成「N 份里最好的那份」后，同一批数据变成 **−3.3 ~ −16.5**。
> 再把评委的输入截断关掉，符号又会翻转。**融合 A/B 至今没有可信结论。**
> 详见文末「2026-09-10 复核」。

## 问题

架构阶段的 7 波里，融合占 2 波（stage1 五维分析 + stage2 定稿），约 30% 时间。
融合到底有没有增值？如果没有，可以砍掉省 2 波。

## 方法

`tests/integration/ab_fusion.py`：同一个 brief 跑一次真实委员会，截获辩论后的 N 份方案，然后

- **A = `fuse_architecture(方案们)`**（现状）
- **B = 方案[0]**（跳过融合）

两臂共用同一次委员会运行，差异只在最后一步 —— 隔离「融合」这一变量。
盲评用委员会之外的模型，甲/乙 顺序互换各评一次取平均（抵消位置偏好），5 维度各 0-10 分。

## 结果 ①：2 模型阵容（deepseek-v4-flash + deepseek-v4-pro）

盲评模型 `glm-5.2`，3 组：

| brief | A 融合 | B 单份 | 差 |
|---|---|---|---|
| 1 任务队列 | 38.0 | 40.0 | −2.0 |
| 2 文件同步 | 20.5 | 19.5 | +1.0 |
| 3 多租户计费 | 28.5 | 29.0 | −0.5 |
| **均值** | **29.0** | **29.5** | **−0.5** |

A 胜 1/3。**没有证据表明融合增值**，但样本 3 组、差异在噪声内。

不砍的理由：B = `plans[0]` 意味着链上第一个模型永远胜出，砍融合等于引入新偏差；
且两成员同源（flash+pro），方案高度相似，融合本来就没多少可合的。

## 结果 ②：3 家异源阵容 —— 融合明显增值

### 2a. 首次尝试（pro + glm-5.3 + kimi-k3）失败

brief 1 只有 1 个模型产出，另外 2 家静默失败，实验中止。
疑似慢/思考模型撞 240s 单次调用超时（kimi-k3 是 `slow`；glm-5.3 实测返回 `reasoning_content`，
但 `models.toml` 标 `reasoning=false`，标注错误）。换成快速阵容后三家均正常出稿。

### 2b. 快速阵容（deepseek-v4-flash + glm-5.3-flash + kimi-k2.7-code-highspeed）

**跳过辩论**（`AB_NO_DEBATE=1` 默认）—— A/B 测的是融合，辩论是无关变量。3 组：

| brief | A 融合 | B 单份 | 差 |
|---|---|---|---|
| 1 任务队列 | 39.5 | 35.5 | +4.0 |
| 2 文件同步 | 29.0 | 22.5 | +6.5 |
| 3 多租户计费 | 36.0 | 23.0 | +13.0 |
| **均值** | **34.8** | **27.0** | **+7.8** |

**A 胜 3/3。**

### 2c. 快速阵容 + **开辩论**（`AB_NO_DEBATE=0`）

只跑完 2 道（brief 2 的辩论耗时 2300s，时间不够，主动停在第 2 道后）：

| brief | A 融合 | B 单份 | 差 |
|---|---|---|---|
| 1 任务队列 | 37.0 | 26.0 | +11.0 |
| 2 文件同步 | 34.5 | 23.5 | +11.0 |
| **均值** | — | — | **+11.0** |

同题配对（brief 1）：无辩论 +4.0 vs 有辩论 +11.0。

### 三轮对比

| 阵容 / 设计 | 组数 | 均值差 | A 胜率 |
|---|---|---|---|
| 2 模型**同源** + 辩论 | 3 | −0.5 | 1/3 |
| 3 家**异源** + 无辩论 | 3 | +7.8 | 3/3 |
| 3 家**异源** + 辩论 | 2 | **+11.0** | 2/2 |

**方向坐实**：融合的价值取决于输入方案的多样性（同源 −0.5 vs 异源 +7.8~+11.0，差一个数量级）。
辩论**没有**削弱融合价值，反而更高 —— 可能因为辩论让三份方案各自演化得更不同。

### ⚠️ 数值不可信（两个系统性偏向）

1. **评分维度全是"覆盖度"**（模块/数据/API/任务/风险）—— 融合的机械作用就是取并集，
   这个评分表**天然奖励融合**。
2. **B = `plans[0]`（链上第一个模型），不是最强的那份** —— 若第一份偏弱，融合优势被放大。
   **公平对照应是「融合 vs N 份里最好的那份」**。

反证：brief 2 的融合稿 9483 字，比任何单份都短（单稿 13114/14433/11873）却高 6.5 分 →
说明不全是"越长越全越高分"，偏向存在但不是全部。

**下一步（未做）**：`tests/integration/ab_best_baseline.py`（已写好，未跑）——
一次调用给 N 份单稿 + 融合稿打同一把尺子，比较 **融合 vs max(单稿)**。委员会结果全在缓存，只需重跑盲评（约 5-7 分钟）。

### 其他混杂

- 各 2-3 组，样本小
- 评委噪声实测 ±3 分（brief 1 同材料两次评：36/33 vs 39.5/35.5）
- brief 3（3 家+辩论）未跑

### 生产隐患（本次发现）

brief 2 的辩论耗时 **2300s**（38 分钟），是 brief 1（500s）的 4.6 倍 —— 疑似模型超时后
**自动重试**（`openai_agent._api_call` 带重试，单次 240s）。会拖垮整轮调度，值得单独查。

### 诊断线索（2a 的失败）

- **根因未定论**：`_run_no_tools` 曾把异常吞掉、无日志 —— 只能靠猜。**这是本次最该修的**。
- `max_tokens=20000` 三家都接受（排除"参数被拒"假设）。
- ⚠️ **一条被排除的假设**：曾测得 `tools: []` 时 glm-5.3/kimi-k3 返回 `content: ""` —— 但那个测试同时用了
  `max_tokens=50`，思考模型 50 token 全花在 reasoning 上，**是 max_tokens 造成的，不是 `tools: []`**。

### 已修（本次）

- `_run_no_tools` 异常 / 空输出 → `witness` 告警 `no_tools_fail` / `no_tools_empty`（不再静默）
- `_dispatch_committee` 部分成员无产出 → 告警 `committee_partial:N/M 缺:xxx`

### 待办（2026-09-10 状态）

1. ~~跑「3 家 + 开辩论」消掉辩论这个混杂~~ —— 跑完 2 题，但结论已被 09-10 复核推翻，见文末
2. ~~修 `models.toml` 里 glm-5.3 的 `reasoning` 标注~~ —— 已完成（`models.toml:202` = `true`）

## 产物

- 脚本：`tests/integration/ab_fusion.py`（`[brief数]` 参数；`AB_NO_DEBATE=0` 开辩论；
  委员会结果缓存在 `.ab_cache.json`，盲评失败可复用重跑）
- 缓存：`.ab_cache_2models.json`（2 模型+辩论）、`.ab_cache_3v_nodebate.json`（3 家+无辩论）
- 实验用阵容通过 `/api/agents` 配置（不手改 `agents_custom.json`）

---

# 2026-09-10 复核：上一页的结论被推翻

## 1. 基准错了：`plans[0]` 是最弱的那份

原实验 B = `plans[0]`。用「N 份里最好的那份」当基准重算（`ab_best_baseline.py`）：

| 缓存（阵容） | 融合 − 最好单稿 | 胜率 |
|---|---|---|
| `.ab_cache_2models`（flash+pro +辩论） | +3.3 | 2/3 |
| `.ab_cache_3v_nodebate`（三家，无辩论） | **−3.3** | 1/3 |
| `.ab_cache`（三家 + 辩论） | **−16.5** | 0/2 |

明细最能说明问题 —— `第一份` 总是远低于 `最好单稿`：

```
.ab_cache  brief1: 融合 27  最好单稿 44  第一份 28
           brief2: 融合 29  最好单稿 45  第一份 30
```

所以原「+7.8 / +11.0」是**拿最弱那份当基准撑出来的**。

## 2. 关掉截断，符号又翻转 —— 测量本身不稳

脚本为防 prompt 过长把每份截到 10000 字。融合稿天然更长，**被砍得最狠**：

```
.ab_cache brief1: 单稿 8815/5218/8384 字（都没到 10000，未截断）
                  融合 16200 字 → 被砍掉 38%
```

`AB_MAX_CHARS=0` 关掉截断重跑：

| 缓存 | 截断 10000 | 不截断 |
|---|---|---|
| `.ab_cache`（三家+辩论） | −16.5 | **+5.5** |
| `.ab_cache_3v_nodebate` | −3.3 | −9.7 |

**同一个缓存、同一份稿子，只因为让不让评委看全，从 −16.5 变 +5.5。**

## 3. 还发现融合阶段自己就把方案砍了

`fuse_architecture` 里 `o[:2000]` —— 方案实际 8k~20k 字，**裁判和定稿人只看得到前 ~15%**。
已于 801f527 修复（可配 `QIDIAN_FUSION_PLAN_CHARS`，默认 20000）。

## 4. 测量缺陷清单（本轮全部踩过）

| 缺陷 | 后果 |
|---|---|
| 基准取 `plans[0]` | 结论完全反向 |
| 输入截断对长稿不利 | 符号翻转 |
| rubric 全是覆盖度 | 变成测篇幅，不是测质量 |
| 模型署名丢失（截获的是纯文本） | 事后无法归因"哪家碾压" |
| 评委可能弱于选手 | 分数不可信 |
| 单次生成方差极大（同模型两次相似度 0.03~0.17） | 单题样本无意义 |
| 模型 id 是浮动别名，无法 pin | 结论不可复现 |
| 思考模型当评委烧光 max_tokens | content 空，打分失败 |

## 5. 已加的判据工具

- `tests/integration/pairwise.py` —— 成对比较 → Bradley-Terry + bootstrap 置信区间。
  **CI 重叠 = 评委分不开这两个方案**，把"评委天花板"变成可观测信号。
- `tests/integration/ab_rank.py` —— 让评委**排序**而非打分，一次调用得到完整顺序，
  分解成 6 条成对胜负，比逐对调用省 12 倍。
- `tests/integration/ab_debate_vs_ref.py` —— 走真实生产路径跑委员会，
  读 `.qidian/.last_fusion.json` 拿**署名**，对照外部参照模型。

## 6. 代码修复（801f527）

- 融合输入截断 2000 → 可配（默认 20000）+ STAGE2 加「吸收重写」指令
- 裁判/定稿人若就是委员会成员 → `warn:fusion_self_judge` 告警
  （当时 `judge_model=deepseek-v4-flash`、`call_model=glm-5.3-flash` 恰好都是成员本人）
- 辩论所有修订都返回空补丁 → 提前停，省 2 波

## 7. 仍未解决

- **架构域没有客观判据**：跑不了测试，只能靠 LLM 评委（有天花板）
- **`_is_slow_model` 把 reasoning 模型全部踢出辩论**（生产默认 v4-pro 不参与辩论）
- 模型版本不可 pin

---

# 2026-09-10 终局：三题复现，结论是否定的

用**修复后的代码**（融合看得到全文、自适应终止）+ 干净的裁判（kimi-k3，
池子里没有 kimi 模型）+ 外部参照（glm-5.3 全血版），跑 3 题 × 2 次排序。

| brief | 融合 − glm-5.3 | 判定 |
|---|---|---|
| 1 任务队列 | −1 / −3 | 追平 |
| 2 文件同步 | **−8 / −9** | **落后** |
| 3 多租户计费 | +1 / +0 | 追平 |

**均值 −3.3 → 落后（6 次测量）。**

## 更关键的第二个问题：融合比自己最好的成员强吗

| brief | 融合 | 最好成员 | 差 |
|---|---|---|---|
| 1 | 43-45 | 39 | +4~6 |
| 2 | 38-39 | 35-38 | +0~4 |
| 3 | 32 | **44**（glm-5.2 单干） | **−10~12** |

**brief 3 融合明显输给自己的成员。** 前两题小赢、第三题大输，方向不一致。

## 前提也塌了

参照 `glm-5.3` 在同一个裁判下的得分：**brief 1 拿 45-48、brief 2 拿 46-48、
brief 3 只拿 31-32**。它自己波动 17 分 —— "高一档"这个前提在这套题上不稳，
brief 3 上它甚至不如 glm-5.2 单干（44）。

## 结论（当日）

~~**"两个模型辩论融合能追平高一档模型"—— 没有证据支持。**~~

⚠️ **这个结论当天就被推翻了，见下一节。**

---

# 2026-09-10 最终结论：融合稿被截断，之前的负面结论全部作废

## 怎么发现的

用户质疑："取长补短怎么会比原始方案低？理论上不可能低于双方原始稿。"

于是**把稿子导出来读**（而不是只看分数）—— 一眼就看到了：

```
brief 1 融合 25182 字，结尾断在:  "user_flow": "提交任务 → Worker
brief 2 融合 26901 字，结尾断在:  "risk": "会话状态丢失或不
brief 3 融合 24004 字，结尾断在:  "rule": "金额以最小货币单位整数 bigint 存储与计算；API 传输金额使用字符串，

所有单稿都是完整收尾的
```

**三题融合稿全部撞 `max_tokens=16000` 被腰斩**（融合稿实测需 24k~28k 字）。

## 决定性证据：分数与"截断到哪"完美单调

| brief | 融合稿写到哪 | 融合分 | 参照分 | 差 |
|---|---|---|---|---|
| 1 | tasks ✓ risks ✓ 写完 | 43-45 | 45-48 | −1~−3 |
| 2 | tasks ✓ risks 断在中间 | 38-39 | 46-48 | −8~−9 |
| 3 | tasks ✗ risks ✗ 都没写到 | 32 | 31-32 | +1~+0 |

brief 3 的 `tasks` 是 schema **必填**项（也是后续拆任务实现任务的依据），
评审维度"任务拆解"直接 0 分 —— **−12 分就是这么来的**。

## 只重跑融合这一步的验证

成员稿在缓存里，只需重做 `fuse_architecture` + 一次打分（3 次调用，不是重跑委员会）：

```
QIDIAN_FUSION_MAX_TOKENS=32000 → 新融合稿 27993 字，tasks ✓ risks ✓

同池打分:  glm-5.2 39 | v4-pro 33 | 新融合 46 | glm-5.3 23

新融合 46 vs 最好成员 39 → +7
新融合 46 vs 参照     23 → +23
```

**补上 tasks/risks 后，融合稿从垫底（32）变成最高分（46）。**

## 修正后的结论

1. **融合 ≥ max(输入)** —— 成立（+7）。用户的理论判断正确。
2. **融合 > 高一档参照** —— 成立（+23）。
3. **"融合无收益"是错的** —— 那个结论完全由截断造成，不是机制缺陷。

## 但机制性的膨胀仍在

融合稿 27993 字 ≈ 单稿的 1.8 倍。构成：

```
api_contracts 43% | data_model 35% | modules 16% | 其余 2%
```

三个放大因素：**取并集** × **JSON schema 的结构性冗余**（表格更紧凑）× **格式翻译**
（glm-5.2 用表格、v4-pro 用 SQL DDL，融合要统一成 JSON 字段数组）。

→ 用户设计的"定稿输入 = 对话结论"仍值得做，那是从根上控制长度。

## 教训

**只看分数不看稿子，会得出完全相反的结论。** 这次差一点把有收益的机制砍掉。
数字异常时，先读原始产物。
