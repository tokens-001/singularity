# 轮次成绩单 · 2026-09-23（**清理前冻存**）

> 为什么有这一份：2026-09-23 清掉了 `.qidian/tasks/` 那 54 个任务文件 + 6 个验证项目。
> `delivery_facts --rounds` 的「任务 N 个」和「按类」两栏**按任务文件算** —— 删了就读成
> 「任务 0 个」（09-21 清前四轮时就是这么把数字弄丢的，那次没冻，只能靠当时的成绩单）。
> 这次按规矩**先冻读数再删**。
>
> ⚠️ **这是删除前的原样输出**，不是重算的。备份包：`~/项目/项目仓库/奇点残留-备份-20260923.tar.gz`

## 冻结读数（`scripts/delivery_facts.py --rounds` 原样）

```
跨轮次对照（6 轮，按创建时刻，越靠下越新）
  `进仓` = 真躺在项目仓 HEAD 树上的产物 —— **不是**「提交在不在历史里」；行数一律连着文件名读。
  `按类` = 按任务**标题**分（实现模块 / 接线·集成 / 写测试 / 验收），过 = **任务状态 done** —— ⚠️ 和上面`进仓`不是同一把尺（只读任务无产物，进仓恒 0），别相加。

  1789833908112  gate2      任务 8 个（failed 8） · QA=—
     进仓 0/8 任务 · 0 提交 · +0 行 · 文件 （无）   |   没进仓 0 任务 · +0 行 · 两栏都在 0 个   |   无账（下界）
     按类（过=任务 done）：实现模块 0/4 · 接线/集成 0/1 · 写测试 0/2 · 验收 0/1
  1789835823874  gate2      任务 0 个 · QA=no_go
     进仓 0/0 任务 · 0 提交 · +0 行 · 文件 （无）   |   没进仓 0 任务 · +0 行 · 两栏都在 0 个   |   无账（下界）
     按类（过=任务 done）：
  1789861334423  gate2      任务 0 个 · QA=no_go
     进仓 0/0 任务 · 0 提交 · +0 行 · 文件 （无）   |   没进仓 0 任务 · +0 行 · 两栏都在 0 个   |   无账（下界）
     按类（过=任务 done）：
  1789922309317  gate2      任务 0 个 · QA=未产出
     进仓 0/0 任务 · 0 提交 · +0 行 · 文件 （无）   |   没进仓 0 任务 · +0 行 · 两栏都在 0 个   |   814,207 tokens
     按类（过=任务 done）：
  1789965398548  gate2      任务 11 个（done 1 · failed 10） · QA=no_go
     进仓 4/11 任务 · 4 提交 · +1636 行 · 文件 logstat/models.py, logstat/parser.py …+1   |   没进仓 3 任务 · +1701 行 · 两栏都在 0 个   |   1,208,506 tokens
     按类（过=任务 done）：实现模块 1/6 · 接线/集成 0/1 · 写测试 0/3 · 验收 0/1
  1790076160658  gate2      任务 8 个（done 2 · failed 6） · QA=no_go
     进仓 1/8 任务 · 1 提交 · +124 行 · 文件 pyproject.toml, src/txtstat/__init__.py …+1   |   没进仓 3 任务 · +696 行 · 两栏都在 0 个   |   628,248 tokens
     按类（过=任务 done）：实现模块 1/4 · 接线/集成 0/1 · 写测试 0/2 · 验收 1/1

  ⚠️ 「没进仓」行数是各锚相对各自父提交的合计、**含重复**（多任务改同一文件）—— 要的是量级，不是精确值。
```

## 这次删了什么

- `.qidian/tasks/*.json` —— **54 个**（50 failed · 4 done，**全是终态**）
- 6 个验证项目的 JSON 与附属文件（architecture / executable_tasks / machine-checks /
  qa-report / research / security-report 等）+ 各自的 `<id>/` 目录（history、prd.json）

## 删之前核过、确认没被牵连的

- **没有活着的锚**：`salvaged.jsonl` 那 13 条**全是更早那批已删任务的**；`refs/qidian/pending` **0 条**；
  54 个任务里**没有一个**有 `children` ⇒ 删除不产生断链
- **项目仓原样不动**：删的是 `.qidian/` 里的记录，`~/项目/项目仓库/验证-*` 那些成品仓一个没碰
- **那 3 个雷拆掉了**：`1789835823874` / `1789861334423` / `1789922309317`
  —— **0 任务 + phase=gate2**，正是 09-22 实测过的"起后端那一秒被自动拆解、8 个任务入队开烧"的形状

## 顺带记一条（本次没处理的）

- `.qidian/partial_usage/` 里那 35 个侧车（按 task_id 命名）在删任务后**一部分成了孤儿**。
  本次**没动**它 —— 那是另一件事，等单独拍。
