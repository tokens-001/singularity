# 未完成清单

> 每条一行。**做完就删掉整行**（别留 ✅）。
> **详细分析不写这儿** —— 归仓库 `docs/`（踩坑 → `防御模式.md`；没验过 → `待验证清单-20260912.md`）。
> ⚠️ **2026-09-17 第二次瘦身**：29.6KB → 11KB（当晚又修掉 7 条），**一字未丢**，全文存在
> `~/projects/singularity/docs/OPEN快照-20260917.md`（09-16 那份快照仍在 `…-20260916.md`）。
> 🔴 **加新条目只写一行。** 这个文件被 `UserPromptSubmit` hook **每轮 `cat` 一次**
> —— 多 1KB，每个会话每轮就多背 1KB（09-16 实测：一天写了 13KB，用户体感"上下文一下就满"；
> **09-17 我自己又写了 22KB**，所以又瘦了一次）。

- [奇点] 🔴 **接手指针** —— 先看这条。
  **09-17 一天跑了两轮真机**（同一个项目 `1789618855407`，同一题目：JSONL 日志统计 CLI）。
  · **凌晨那轮**：完整交付、但账难看（6 成 5 败 / 71 分钟 / **39% token 打水漂**），当晚修掉 7 条。
  · **下午·晚那轮**（**专门验那 7 条**）：**6 条验完 ✅、1 条没被走到 ❌**，**又挖出 4 条**。
  逐条判据 + "怎么验的"全在 `docs/待验证清单-20260912.md`（**别在这儿抄数**）。
  🎯 **下一步 = 再跑一轮真机**，专门验 09-17 晚修的这 3 条（**都不能靠离线测试替代**）：
   ① **打回重做这次会不会真的重新拆任务**（`7d24625`）—— 上一轮 10 秒冲过执行层、重规划白花钱；
   ② **建出来的任务描述里有没有约束/机器检查命令**（`7d24625`）—— 上一轮机器检查 0/10 的真因；
   ③ **`b1cfacd` 约束进执行器** —— 上一轮走的是另一条入口、**没被走到**，这轮该走到了。
  **开跑前先核**（09-17 血的教训，两条都写进下面「怎么跑真机」了）：
   · **后端必须新起**（`debug=False` **无热重载**；判据：`ps -o lstart= -p <pid>` 要**晚于** `git log -1`）；
   · `.qidian/projects/` 里 **5 个项目定义**；后端**跑着**。
- [奇点] 🔴 **心跳分辨不出"模型慢"和"工人死了"**（09-17 真机发现，属 #77 那一族）
  `witness.heartbeat()` 是**每个 turn 写一次**（`_exec.py:561`，在 turn 循环里），**不是定时器**
  ⇒ 心跳的新鲜度上界 = **单次模型调用的最长耗时**。于是这两种情况**长得一模一样**：
  ①模型正在写一个大文件（真的在干活，4 分钟正常）②工人早就没了。
  孤儿探测/停滞检查都建立在这个信号上 ⇒ **判据量的是"某轮开始了"，不是"有人在推进"**。
  替代判据（同 `_strand_guard` 那条修法）：`running_futures` 非空 ∧ 线程池全空闲 —— 状态矛盾。
- [奇点] 🟡 **审查阵容里混进了写手模型 ⇒ 实际只剩一家 + 告警被糊死**（09-17 真机，一轮报了 11 次）
  `.qidian/phase_models.json` 的 `reviewing` = `[glm-5.3-flash, deepseek-flash]`，
  而 `executing` 也是这两个 ⇒ 写手是 deepseek-flash ⇒ 剔掉它（`_review.py:96-97`，**剔得对**）
  ⇒ **实际审查池只剩 `glm-5.3-flash` 一家**（配的是两家）。
  ⚠️ 而且 `witness.warn('review', 'designated_reviewer_is_writer:…')` **每审一次报一次**
  —— 一轮 11 条同 key。这正是 `alert_summary` 自己注释里警惕的形状
  ("25 条告警 22 条挤在两个 key 上")：**常亮 ≠ 问题多，是判据跟配置脱节**。
  ⇒ 两件：①阵容按**角色**分开配（写手不进审查池）②这条警告要么去重要么只在变化时报。
- [奇点] 🟡 **「留痕」写成每轮一条 = 又一条糊筛子的告警**（09-17 真机，`drain_dep_blocked`）
  `merge.py:138` 那句是 `4eae901` 修复**故意加的留痕**（"静默跳过会让人以为队列空了"），
  但它**每轮 drain 都报一次**（实测 7 条/分钟，条件在就一直报）。
  ⚠️ **它和"审查阵容"那条同一个形状**：`witness.warn` 是**事件流**，
  用它表达**持续状态**就必然刷屏。⇒ 这类要么去重（只在**状态变化**时报）、
  要么走"常亮"通道（`alert_summary` 那套按 key 聚合的）而不是每轮 append。
  🔵 **对照**：同一条修复的另半边是好样的 —— 依赖失败的降级**写进了任务自身的
  `error` 字段**（`上游依赖 … 已失败 (降级运行)`），那是"状态"不是"事件"，没有刷屏问题。
- [奇点] 🔴🔴 **「打回重做」是半吊子：架构重做了，执行用的还是旧任务**（09-17 真机坐实，**最严重的一条**）
  **两条建任务的路，重规划后一条都够不着**：
  · `_workflow_phases._run_execution`（清 `task_ids` + 建新任务）—— **GATE2 批准时不跑它**
    （批准只启 planning，executing 归调度循环推 —— 这是刻意的，防两套驱动抢 phase）；
  · `orchestrator._decompose_and_create_tasks`（也清也建）—— 被 `if not proj.task_ids:` 挡住，
    而**旧任务还在**（非空）⇒ 永远不触发。
  ⇒ **新架构的 T1~T13 一个都没建出来**；项目拿着**旧的 13 个任务**（6 成 7 败、全终态）
    10 秒冲过 `executing` → 集成 → 审查。
  🔵 **同根的第二枪**：`constraints_checklist` 的唯一写入点之一就在 `_decompose_and_create_tasks`
    里，它没跑 ⇒ 清单空 ⇒ `constraints_checklist_fallback` 报警。
    ⚠️ 而那条告警的注释**早就警告过**："下次真跑若还见到这条，说明又有新的写入点分叉了"
    —— 这正是。**修法**：重规划成功时（`_run_planning` 末尾）把 `proj.task_ids` 清掉，
    让调度循环那条 guard 能触发；旧任务文件另做归档（别直接删 —— 有可打捞产物挂在上面）。
- [奇点] 🔴 **打回重做会覆盖上一版架构，旧版无处可查**（09-17 真机，用户当场想看对比）
  `_REJECT_FALLBACK[GATE2]=PLANNING` + `architecture=None` + 重跑 ⇒
  `architecture.md` 是 **`write_text` 覆盖写**，项目 json 里的 `architecture` 也被替换
  ⇒ **打回前后两版只剩一版**。而"打回"的**全部意义就是对比改进** —— 没有旧版，
  人只能凭记忆判断"这次是不是更好"，等于每次都在盲点。
  🔵 同族：调研那份也一样（`research.md` 覆盖写），但调研有 `research-raw` 兜底、架构没有。
  ⇒ 要么把旧版归档（`.qidian/projects/<id>.architecture.<ts>.md`），要么把差异打进 lineage。
- [奇点] 🔴 **「打回能说理由」验完了 —— 通道通，但**管不动**模型**（09-17 真机，**实验证伪**）
  **实验**：GATE2 打回两次，第二次把理由写死成「请逐条补上 [7][8][9] 三条没覆盖的需求
  （六条硬契约 / tests 逐条覆盖 / pyproject+console_scripts），check 的 argv 要指向真会创建的测试文件」。
  **四跳全通**（打回 → 落盘 lineage → 拼进提示词 → 我逐字核过提示词末尾）——
  **而结果是三轮 `uncovered [7,8,9]` 一字未动。**
  ⇒ **"理由进了提示词" ≠ "模型会照做"。** 打回能让方案**变样**（模块 8→6→8、约束 9→11、
     第二版还补了 2 个测试任务），但**对某条具体的 issue 无能为力**。
  根因（查实）：`_validate_architecture` 只把「**tasks 缺失/为空**」当 `arch_invalid`（会硬拦 GATE2 批准），
  其余都只是 `arch_warning` —— 注释写着有意如此（"按六字段齐全拦会把好活挡在门外"）。
  ⇒ **覆盖率不满本来就不拦**。要拦就得把它升成 `arch_invalid`（风险：挡好活），
     或在执行层把 uncovered 变成任务（改动大）。**09-17 用户拍板：暂时不动（A）** ——
     系统报到人眼前（`issues` + 观察者两次主动说）就够了，**人负责决定**。
  🔵 **副产品**：这条实验同时证明了**观察者主动汇报真的有用** —— 它两次都把这个问题
     用大白话摆到用户面前（"有 3 条需求目前没人管……重点说清那 3 条怎么办"），
     而且**两次都是准的**（我核过数）。**系统报、人决定**这条链是通的。
- [奇点] 🟡 **调研解析失败没有重试**（09-17 顺手发现）—— 规划有：`_workflow_phases.py:351-362`
  检查 `parse_error` 后带"格式错误"提示**重跑一次**；**调研没有这道**，坏了就直接进 GATE1
  （09-17 那轮就是这样，用户看到的是解不开的报告）。调研还更贵。
- [奇点] 🟡 **GATE1 打回后不清 `research_report`** —— 重跑期间界面显示的是**上一版**报告。
  没清的理由：重跑若早退（预算硬停）会变成空面板、旧报告永久丢。⇒ 是取舍不是遗漏，别再"修"。
- [奇点] ⚠️ **待查：集成失败之后没重新集成，项目却进了 GATE3**（09-17 真机，**没坐实**）
  lineage 里 `集成合并失败第1次→回实现层重试` 之后**没有第二次集成**，却 `executing→gate3`。
  已排除：`_merge_inflight` 没释放、`set_phase` 漏记。**别当结论用。** → **详情 防御模式 §77**
- [奇点] 🔴 **验证半截：依赖失败锁死调度那条已修（`4eae901`），但"合并路径"没被真机走到**
  （这轮 3890 超时了，没到合并那步）。**下次真机专门看**：依赖失败的任务，
  它的合并请求这次能不能正常走完。→ **详情 防御模式 §77.1**
- [奇点] 🔴 **「任务标成 running 却没人管」那一族** —— ❌ **09-17 真机验了：没兜住**，**要重修**
  5 处 `_strand_guard` + 孤儿探测**一条没响**。孤儿判据量的是「登记表里有没有名字」，
  而故障是「名字还在表里、人早走了」。**替代判据（精确、非超时猜法）**：
  `running_futures` 非空 ∧ 工人全空闲 —— 状态矛盾。→ **详情 防御模式 §77.3**
- [奇点] ⚠️ **待查：「审查砍掉的文件」有没有真记进报告**（09-17，**没坐实**）
  `_review.py:335` 改动 >3 个文件时只审前 3 个（有意，且同时 warn + `unverified.append`），
  但 **46 个 trace 里一条「未审查」都没有**。**没能定位是哪个任务触发的**（告警不带 task_id）。
  良性解释未排除：`_save_trace` 幂等，trace 若先落盘则追加进不去。
- [奇点] 🔴 **任务排队等的时间算进执行预算**（09-17 真机**量到了准数**）
  `deadline_at = submit + 900s`，排队每秒都从预算扣。
  **实测（项目 1789618855407，7 个任务同时提交、并发 2）**：
  `13:30:47` 提交 ⇒ 全部 deadline `13:45:47`；`917` 排队 **8 分 17 秒**、`13:39:04` 才上场
  ⇒ **上场时只剩 400s（44%）**。
  ⚠️ **但不能叫"上场即死"**（我 09-17 先前这么写，当场被真机打脸）：917 拿着这 400s
  **照样干了 94k tokens、22 次工具、写出 6430 字符的 `output.py`** ⇒ 它的症状是
  **"上场时预算被砍掉一截"**，砍掉多少 = 排了多久，**不是必然致命**。
  真要判"这任务还跑得完吗"，得看**剩余预算 vs 这类任务的平均耗时**，不能只看"被扣过"。
- [奇点] 🟡 **同族：`_budget_exhausted` 只拦 `≤0`** —— 而 `cap=6s` 是正数，
  照样发起注定空的调用。→ **详情 防御模式 §77.8**
- [奇点] 🟡 **`final_status` 会把「已交付」标成 `blocked`**（09-16 查清）
  `final_status` 是 `neijinglu.build_report` 算的**交付判词**，不是任务状态。
  **没修**：动的是报告语义，UI 和账本都在读，改了得真机验一轮 —— 不值得为一行字冒险。
- [奇点] 🟡 **「委员会换/加席位」先别做** —— 地基没验（P1 三臂实验只差跑）；席位视角已被 A/B 证伪。→ **详情 §3、§21**
- [奇点] 🟡 **「收编 Claude/Codex」** —— 真门槛是三个硬伤（禁工具/记账靠正则/权限拦不住）。
  🔵 **09-17 查到一条路（只读了公开文档，没读源码，别当定论）**：OpenHands 用 **ACP**
  当这些 CLI 的**客户端**（不是"收编"）⇒ **不碰**那三件事。但 ACP **自带权限通道**
  `session/request_permission`（agent→client、失败关闭）⇒ **"权限拦不住"有标准做法**；
  **记账它没提**（三个硬伤只对上了一个）。⇒ **待评估，不是待做。** → **详情 §5**
- [奇点] 🟡 **Google Stitch 挂进奇点** —— 卡在「它可能只认 OAuth」。下一步是**探针不是写代码**。→ **详情 §6**
- [奇点] 🔴 **「清理孤儿 pending ref」是地雷** —— 它现在不工作，而**修好它会毁掉可恢复的产物**。
  ⚠️ 同族第三次「读错仓库」。**修之前先定什么算"孤儿"**。→ **详情 §9**
  🔵 **09-17 之后好办一点了**：F3（`caca6c0`）把"哪些任务还有可打捞的产物"
  摆到了界面上（`salvageable_refs()`）⇒ **动手清之前至少能先看清有哪些**。
- [奇点] 🟡 **「有报告但 issues 没标路由」默认回架构**（清空重规划）—— 与同函数「无依据时回实现层」的原则相反。→ **详情 §10**
- [奇点] 🟡 **QA 契约「间歇性」** —— 探针响过、**只证明"不是那两个候选"**，没证明是什么。→ **详情 §11**
- [奇点] 🟡 **GATE2 两种处境同一句话 —— 已修 `6aa1539`，⚠️ 真机没验**（本轮**没触发**那条路）。
  兜底升上来时会改口 + 把 `lineage` 里那条 reason 摆出来。→ **详情 §12**
- [奇点] 🟡 **告警 ③「给关键告警一条真出口」**（邮件/桌面通知）—— 剩下的是"**哪些算关键**"。→ **详情 §16**
  ⚠️ **09-17 中午修正**：原文写"`lazy_spoke` 已归零"**是错的** —— 它在**后端启动那一刻**就会报
  （12:20:48 重启、12:43 跑测试各一条）。那个"0 条"只统计了某一轮真机的时间窗。
- [奇点] 🔴 **`lazy_spoke_import_failed` 又活了 —— 09-16 那把锁没盖住全部路径**（09-17 中午实测）
  `dispatcher.py:441` 的 `_LAZY_SPOKES_LOCK` 只护住 `__getattr__` **这一个入口**；
  别处并发导入 `_dispatch_skills` 时照样拿到半成品模块 ⇒ `_dispatch_exec:9` 那条
  模块级 `from ..._dispatch_skills import _load_skills_for_agent` 仍然炸。
  **判据**：它在 12:20:48（我正在重启后端）和 12:43（跑 pytest）各报一条 ⇒ **我改之前就在**，
  不是本轮引入的。危害：**技能/权限那条 spoke 静默失效**（导不进来就 `continue`，能力悄悄没了）。
- [奇点] 🟡 **重启把任务回收成 PENDING 后，重派撞上"worktree 已经没了"** —— **机制没坐实**，别当结论用。→ **详情 §17**
- [奇点] 🔴 **人审门的否定族还剩一个洞：「别继续了」判 approved** —— 没硬修，用**钉住现状的测试**把天花板变可见。→ **详情 §19**
- [奇点] **异常收尾的 3 个缺口：修了 2 个** —— **⚠️ 真机没验**（worker 异常不好造，等撞上顺手看）。→ **详情 §23**
- [奇点] **异常收尾的取消路径** —— 真机没验到，**改用"真落盘"验的**（严格讲不算真机验）。→ **详情 §26**
- [奇点] P1 三臂实验：方案在 `docs/P1-三臂实验方案-20260912.md`，**没跑（只差跑）**。→ **详情 §21**
- [奇点] **验收轮状态**：逐条的 ✅/❌/◐/— 全在 `docs/待验证清单-20260912.md`，**别在这儿抄数**。
  🔑 轮 4 的教训：**数"总数"要连组成一起数**。→ **详情 §20**
- [奇点] ⚠️ **`待验证清单` 的 #33 过期了**（它写"并发 1 ⇒ 暂停占死队列"，实际部署传 `concurrent=2`）。→ **详情 §24**
- [奇点] 📦 **历史存档：2026-09-14 中午版接手指针**（已做完的账，**别照它做**）。→ **详情 §18**
- [奇点] ⏸ **搁置**（09-12 用户拍板：质量 / 成本·速度 两项先不做）→ 前置的"分阶段算账"**一并搁置，别重复提**。

---

## 怎么跑真机（09-17 核过，跑之前照这个来）

- **起后端**：`cd ~/projects/singularity && nohup .venv/bin/python -m singularity.web.app > /tmp/qidian.log 2>&1 &`
- 🔴 **改完代码必须先重启后端再跑真机**（`app.py:2432` 是 `debug=False`，**没有热重载**）。
  09-17 中午实测踩到：跑着的进程是 **01:40 起的**，而 7 条修复是 **02:12–02:28 提交的**
  —— 不重启就开跑 = 整轮 71 分钟跑在旧代码上，然后得出"修复没用"的假结论。
  判据：`ps -o lstart= -p <pid>` 要**晚于** `git log -1 --format=%ad`。
- ⚠️ **`auto_mode=False` ⇒ GATE1/2/3 都要人点**：
  `POST /api/projects/<id>/gate-confirm` body `{"gate":"gate1|2|3","decision":"approved"}`；续跑 `POST /api/projects/<id>/start`
- ⚠️ **curl 一律 `--noproxy '*'`**（连本机 127.0.0.1 也会被系统代理劫走）
- ⚠️ 端口是 **5050**（不是 8000）
- **看现场常用**：`sample <pid> 2`（免 root 的 py-spy 替代）· `.qidian/partial_usage/<task>.json`（侧车：
  token/派发/工具事件）· `.qidian/heartbeats/<task>_any.json`（心跳）· `.qidian/alerts.jsonl`（告警）
- 🔴 **任务的工作树在项目仓的隐藏兄弟目录，不在 `.qidian/worktrees/`**（那个目录恒空，09-17 找它找了半天）：
  `~/项目/项目仓库/.<项目名>-worktrees/<task_id>_<level>/`
  判据：`ls ~/项目/项目仓库/` 里 **以 `.` 开头**的那些。任务在那里面写文件、提交（
  `agent changes in <task_id>_<level>`），**合并成功后才进项目仓** —— 所以"项目仓里什么都没有"
  不等于"任务没干活"。
  ⚠️ 侧车 `partial_usage` 里那个 `(27字符)` 是**消息长度不是结果**，别拿它判断写入成败
  （09-17 我据此误判过一次"写了但盘上没有"）。要看 `result_preview`。
- ⚠️ **重启会把 RUNNING 任务回收成 PENDING 重派** —— 见上面「worktree 已经没了」那条
