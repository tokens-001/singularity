# 未完成清单

> 每条一行。**做完就删掉整行**（别留 ✅）。
> **详细分析不写这儿** —— 归仓库 `docs/`（踩坑 → `防御模式.md`；没验过 → `待验证清单-20260912.md`）。
> ⚠️ **2026-09-16 瘦身**：54KB → 7KB，**一字未丢**，全文存在
> `~/projects/singularity/docs/OPEN快照-20260916.md`（下文 `§N` ＝ 那儿的第 N 条）。
> 🔴 **加新条目只写一行。** 这个文件被 `UserPromptSubmit` hook **每轮 `cat` 一次**
> —— 多 1KB，每个会话每轮就多背 1KB（09-16 实测：一天写了 13KB，用户体感"上下文一下就满"）。

- [奇点] 🔴 **机器检查 10/10 全失败 —— 判据要的测试文件名从没传到干活的人手里**（09-17 真机）
  **症状**：`machine_checks ran=10 passed=0`，十条**全是同一个原因**：
  `ERROR: file or directory not found: tests/test_contract_normal.py`（`exit=4`）。
  架构里每条 `constraints[].check.argv` 都指定了一个测试文件，**而没有一个被创建**。
  **根因**（读码）：`execution_judge.py:996` 构造执行器上下文时只传**规则的文字** ——
  ```python
  ctx_parts.append(f"约束[{c.get('type','')}]: {c.get('rule','')}")   # ← 只有 rule，没有 check
  ```
  ⇒ 干活的人看到的是「约束[reliability]: 正常路径与默认值：给定 20 行日志…」，
  **那个 `tests/test_contract_normal.py` 从头到尾没出现过** ⇒ 他不可能知道要造这个文件。
  🔴 **和 `test_cases.json` 那条是同一个形状**（「只有读的人，没有写的人」）——
  **判据要的输入，没有传到产出它的人手里。今晚第二次。**
  ⇒ 候选修法：`ctx_parts` 里把 `check.argv` 一起带上（至少带上路径），
  或者机器检查别指定具体文件、改成按目录/按名字跑。**没动手。**
- [奇点] ⚠️ **待查：集成合并失败之后，没看到重新集成的记录，项目却进了 GATE3**（09-17 真机，**没坐实**）
  **确认到的**：lineage 里 `executing→integrating`（01:56:04）→ `集成合并失败第1次→回实现层重试`
  → `machine_checks`（01:57:33，10/10 失败）→ **`executing→gate3`（01:59:18，"验收报告完毕"）**。
  QA/安全报告确实生成了（01:58–01:59）⇒ 验收步骤（`run_test_fix_loop`）**真跑了**
  ⇒ 按代码它只从 `reviewing` 分支进 ⇒ **phase 应该经过 `reviewing`，但 lineage 里没有这一条**。
  **没确认的**：为什么。**已排除**：`_merge_inflight` 没释放（`_run_integration_merge_async`
  的 `finally` 里 `discard` 了，读过了）；`set_phase` 漏记（它只在 phase 没变时 early-return）。
  ⇒ 可疑点：**集成没过，却照常验收交付**。要坐实得先给这条路上加痕（现在能查的只有 phase 变化）。
  ⚠️ 别当结论用 —— 我可能只是没找到那条合法路径。
- [奇点] 🔴🔴🔴 **根因坐实：「依赖失败」⇒ 合并队列永久 defer ⇒ 调度循环永久自旋、任务永久卡 RUNNING**
  （09-17 真机，**一晚复现两次、重启后仍复现**，现场已存）
  **一句话**：`_deps_satisfied` 要求依赖**全部 DONE**，而 **FAILED 是终态、永远到不了 DONE**
  ⇒ 只要一个任务的依赖失败了，它的合并请求就**永远满足不了依赖**。
  **完整链条**（每一环都核过）：
  ```
  3890 依赖 3889（3889=failed）
    → mq._deps_satisfied(3890 的合并请求) 恒 False
    → mq.drain() 永远 defer 它、返回 []          （merge.py:142 `if not self._deps_satisfied(req): 放回队尾`）
    → _drain_pending 只在 drain 返回结果时才 pop   （orchestrator.py:655）
    → 3890 永远留在 pending_batches
    → 循环睡觉条件是 `not running_futures AND not pending_batches`（orchestrator.py:790）
      两个工人都空闲（running_futures 空）＋ pending_batches 非空 ⇒ **条件恒 False ⇒ 永不 sleep**
    → 全速自旋（实测 **1731 条 `drain_dep_blocked` / 2 分钟**，进程吃 44 分钟 CPU）
    → 孤儿探测的 live = running_futures ∪ pending_batches **包含它** ⇒ 判"有人管" ⇒ 跳过
    → `_strand_guard` 不响（**没东西抛异常**，任务只是永远不被处理）
  ```
  🔴 **这是「修了一半」**：`merge.py:127-138` 的注释自己写着
  「**实测 1 个依赖未满足的请求就能让 `drain()` 永不返回**……卡住 = 整个调度停摆」——
  **他们知道，但只修了内层**（让 drain 别再死循环，改成 break + 报警），
  **外层循环仍把「pending_batches 非空」当成「有活干」** ⇒ 内层的死循环换成了外层的空转。
  （同族：`docs/防御模式.md` §76 就是「三个只做了一半的形状」——**这是第四个**。）
  ⇒ **修法（三处，缺一不可）**：
  ① **`_deps_satisfied` 要认「终态」不是「DONE」** —— 依赖 FAILED/ROLLED_BACK 意味着
     "别再等了、降级合并"，不是"永远等"；现在这条把「等成功」和「等不到」混成一个判据。
  ② **循环的睡觉条件不该依赖 `pending_batches` 是否为空** —— 该看"这一圈有没有任何进展"。
  ③ 孤儿判据换成**活性**（见下条）。
  ⚠️ **而且这条是"静默死锁"**：不抛异常、不报警、界面显示一切正常（任务 running、工人在池子里）
  —— **只有 CPU 在烧、只有告警在刷**。所以必须先修 ①②，否则下次还是"卡一小时没人知道"。
- [奇点] 🔴 **观察者的「停滞告警」发不到任何人手里 —— 而它本来是唯一的"主动发现者"**（09-17 真机查清）
  **背景**：`check_stalled()`（阈值 `STALLED_AFTER_S = 900+300 = 1200s`）全仓**只有观察者和一个 admin 接口**在用，
  **调度器/编排器一次都不调** ⇒ 观察者**是唯一会主动发现停滞的角色**。今晚那个任务卡了 50 分钟，**早该响**。
  🔴 **链条**：`_observer_worker` 每圈跑 `_check_anomalies()` → 产出的告警**广播给 `_pending_replies`**。
  **而 `_pending_replies` 几乎永远是空的**：
  · 唯一正经的注册入口 `register_client` **全仓零调用者**（`.py/.ts/.tsx/.js/.sh` 全搜过）；
  · 唯一写入点是聊天路径 —— **有人问问题的那一刻**才写；而 `unregister_client` **也是零调用者**
    ⇒ 问过一次就留个旧回调，进程重启才清。
  ⇒ **没人聊天时，告警广播给一个空字典，无声消失。**
  🔴 **而且一个字都不落盘**：`_check_anomalies` 只 `return` dict，没有 `witness.warn`
  ⇒ `alerts.jsonl` 里连痕迹都没有（全量搜 observer = **0 条**）。
  ⚠️ **最讽刺的一处**：桥里**有现成能用的广播通道** `bridge.broadcast_observer`
  （走 Observer Server、带频道、还返回"发给了几个客户端"）—— **`app.py:708` 的调度事件就在用它**，
  **偏偏停滞告警没走它**。
  ⇒ 候选修法：`_observer_worker.py:47-61` 改走 `broadcast_observer` ＋ **同时 `witness.warn` 落盘**；
  `register_client`/`unregister_client` 这对**死函数**要么接上要么删。**没动手。**
  🔵🔵 **但只"接上线"不够 —— 谁来监督守护者？**（用户点出来的：设观察者的初衷就是治这个）
  · **它要治的病和它得的病是同一个**：任务静默停滞没人知道 / 它静默失效没人知道。
    它在跑、在监听、循环在转 —— **外部看一切正常**；它的失效方式是"什么都不发生"，
    而"什么都不发生"与"一切正常"**长得一模一样**。
  · `check_stalled` 量的是**任务**有没有心跳，**没有任何东西量观察者自己** ⇒
    "接线断了"（`register_client` 写了没人调、整段路从没通过电）**不可能被发现**，
    因为发现它需要一个"更上一层的观察者"，而那个位置是空的。
  · ⇒ **真修法 = 给守护者本身一个可被证伪的活性痕迹**：它每转一圈落一条盘
    （醒了/查了几项/发现几条）⇒ **痕迹断了 = 它聋了**，而不是现在的"静默即失效"。
    ⚠️ **而且这条痕迹必须落在"它失效时你一定会看的地方"**（`alerts.jsonl` 或界面），
    **不能只落在它自己的日志里** —— 那等于没说给任何人听。
  · 🔵 **这一夜的形状（逐层升高，同一个病）**：任务说"在跑"其实没人管 / 循环说"在转"其实没进展 /
    告警说"出声了"其实没人听 / 观察者说"我盯着呢"其实喇叭是空的。
    **每层都在用"我的声明"证明自己，故障永远在"声明与实际之间"。**
- [奇点] 🔴🔴 **调度循环自旋 + 任务卡在 RUNNING 没人管 —— §25 那条真机验了：没兜住**（09-17 真机，现场已存）
  **症状**：整轮停摆 50 分钟，任务 `1789572663890` 状态 `running` 但**侧车 49 分钟没动、心跳 50 分钟没跳**。
  **栈（`sample` 抓的，免 root，存 `_归档-20260917-T3冲突前/卡死栈-sample.txt`）**：
  · `ThreadPoolExecutor-326_0/_1`（**两个工人**）都在 `_queue_SimpleQueue_get` **睡着** —— 池子里没有活；
  · `_loop_worker` **100% 在转 JSON 解析**（`scan_once_unicode`/`scanstring_unicode`）—— **空转**。
  ⇒ **和 09-13 那次 py-spy 栈一模一样**（`orchestrator.py` 注释原话："池子里没有工作线程、循环空转"）。
  🔴 **§25 那 5 处 `_strand_guard` 一条都没响，孤儿探测也没响**（全量告警核过）
  ⇒ **「已修未验」现在有结论：真机一跑，它没兜住这个形状。**
  ⚠️ **还没坐实的**：卡在 `_reap_futures` 的哪一步（`running_futures` 没清 ⇒ 循环走不到孤儿探测那段）。
  **两个副作用，本身就是发现**：
  · 🔴 **循环没有退避 —— 而且这不是卡死独有的，是常态**（`orchestrator.py:789`）：
    `while True:` 里**只有**「`not running_futures and not pending_batches`」那条分支
    `time.sleep(0.5)`；**只要有任务在跑就一点不睡**，全速转 `_reap_futures` / `_drain_pending` /
    `_auto_trigger_test_fix`（最后这个**每圈 glob 全部项目文件**）。
    作者自己知道它转得快：唯一被节流的是孤儿扫描（注释"每 3 秒一轮没必要"）。
    ⇒ 平时任务少、轮次短，没人注意；这次 11 个任务跑了一小时 ⇒ **2174 条** `drain_dep_blocked`、
    进程吃掉 **44 分钟 CPU**。⚠️ **重启后立刻复现**（新进程 3 分钟又刷 2292 条）⇒ 与卡死无关，是常态。
  · 🔴 **告警轮转把诊断证据冲掉了**：`witness._ALERT_KEEP = 1000`，超阈值只留最后 1000 行。
    而注释写的假设是「**告警量小(一次任务几十条)，不值得做轮转**」—— 自旋当场破了这个假设，
    而轮转做的头一件事就是**把你要用来诊断这次自旋的证据删掉**（开跑前 2714 行 → 事后 2175 行）。
  🔴🔴 **为什么孤儿探测也没响 —— 判据选错了对象**（`orchestrator.py:744`）：
  它的定义是 `live = running_futures ∪ pending_batches`，`if tid in live: continue  # 有人管`。
  **它量的是"登记表里有没有这个名字"** —— 而这次故障恰恰是**名字还在表里、人早走了**
  （3890 就挂在 `running_futures` 里）⇒ 探测一看"表里有它"就跳过了。
  ⚠️ docstring 还专门夸过这个选择（"精确的'不在表里'，**不是'多久没动静'那种超时猜法**，
  后者会被慢模型/人审等待/暂停误伤"）—— **动机对，代价是对"future 永远不会完成"完全瞎。**
  ✅ **有个既精确又不误伤的替代判据**（不是超时猜法，是**状态矛盾**）：
  **`running_futures` 非空 ∧ 池子里的工人全空闲** —— 这两件事不可能同时健康。
  现成材料：`.qidian/heartbeats/<task>_any.json` 的心跳就是干这个的。
  ⇒ 候选：① 循环加退避/让出 ② 孤儿判据换成"状态矛盾"那条 ③ 慢路径告警**按 key 去重后再落盘**
  （现在同一条 50 分钟写 2174 次）④ 轮转前把被删的另存一份。**没动手。**
  🔵 **更一般的教训（当晚挖出的问题全是这一个形状）**：判据一直在量**"声明"**，
  而故障总出在**"声明与实际的差"** —— 循环"在跑"但没进展 / 任务"有名字"但没人干 /
  `conflict_held`"在等人"但活早合完了 / `.pyc`"在仓里"但不是代码 / 告警文件"没超阈值"但证据已删。
  **每修一次只覆盖想到的那几处 ⇒ 会一直有新的一处。**
  解法不是再加第 6 处 guard，是加一层**对账**（"我声明的和实际的，对得上吗"）——
  §25 加的本来就是对账探针，方向对，只是**对账时拿登记表当实际了**。
- [奇点] ⚠️ **待查：「审查砍掉的文件」有没有真记进报告**（09-17 真机，**没坐实，别当结论用**）
  **确认到的**：`_review.py:335` 在改动 >3 个文件时**只审前 3 个**（有意为之，成本控制），
  并且**同时**做两件事：`witness.warn(review_files_truncated:N->3)` ＋
  `validation.unverified.append("M 个改动文件未审查…")`。注释写得很明白：只发告警不够，
  **"告警是给排障的人看的，报告是给用户看的"**。本仓 09-17 真机**告警响了 2 次**（`4->3`）。
  **没确认的**：**46 个 trace 里一条「未审查」都没有** ⇒ 那条追加**似乎没进报告**。
  ⚠️ 但我**没能定位是哪个任务触发的**（告警只带数量，不带任务 id），所以**不能说它丢了**。
  已知的良性解释（未排除）：`_save_trace` 是**幂等**的（"已写过的 trace 不覆盖"）⇒
  如果 trace 在审查追加之前就落过盘，那条追加自然进不去。
  ⇒ 要坐实得先给这条告警**带上 task_id**（现在带不了，所以谁也追不了），再复现一次。
  ⚠️ **顺带**：`changed` 的来源是 `exec_result.changed_files`，而**`.pyc` 混在里面**时
  「前 3 个」很可能全是字节码 —— 真源码反而被挤出审查范围（见下面 .gitignore 那条）。
- [奇点] 🔴 **任务排队等的时间**会被算进**执行预算** ⇒ 排队够久就"上场即死"（09-17 真机，真实规模第一次露）
  **机制**（读码 + 实测都对上）：`deadline_at = pool.submit 那一刻 + TASK_DEADLINE_S(900s)`
  （`orchestrator.py:147`），而 `_dispatch_budget_s = deadline_at − now − 收尾余量(90s)`
  ⇒ **submit 之后在池子里排队的每一秒，都从执行预算里扣**。
  ⚠️ **这是 2026-09-14 有意的设计**（注释在 `orchestrator.py:139`）：为了让执行器自收尾早于外面那把
  900s 的刀，而不是"又差一个排队时间"。**它没错，但它没兜住 `并发=2 + 任务多` 这个区间。**
  **实测**（`验证-日志统计-20260917`，11 个任务 / `concurrent=2`，建任务时刻全是 23:31:03）：
  排队空等 4s / 4s / 213s / 213s / 660s / 660s / **972s** / **1471s** / **1732s** ——
  **前 7 个都正常交付，卡在 972s 那个（T10）开始出问题**：零产出、末次派发只拿到 `cap=6s` ⇒ failed。
  ⚠️ **同族的**：`_budget_exhausted`（09-16 修）只在预算 **≤0** 时才拦 —— 而 `cap=6s` 是**正数**，
  照样发起了一次注定空的调用。**"太小也是没有"这个区间没人管。**
  ⇒ 候选：① 预算低于某个可用阈值（比如 <60s）就别发起 ② 排队不计入执行预算，另给排队上限
  （但会跟 09-14 那条"两把尺要对齐"打架，得一起想）③ 并发调大 / 减少同模块任务。
  **没动手**（这轮改了要重启，重启会踩 §17）。
  ⚠️ **顺带**：`conflict_held` 被 `_DEAD_END` 当"死路依赖"⇒ 下游被标「上游依赖 X 已失败(降级运行)」，
  而 X 后来解开了**这句标记不会更新**（实测 3887 已是 done，3889 还挂着这句）。
- [奇点] 🔴 **`resolve` 解完冲突后任务永远卡在 `conflict_held`**（09-17 真机撞见，**这轮被它卡住过**）：
  `MergeQueue._mark_merged`（`merge.py:194`）**只改内存里那几个字段，不 `tracker.transition`** ——
  而 `_park`（`:213`）是**会** transition 的 ⇒ **进得去、出不来**。后果：
  · `resolve(manual)` 返回 `{"status":"merged"}`、**合并在 git 里真发生了**，但任务状态还是 `conflict_held`；
  · 而 `resolve` 已经 `self._parked.pop()` + **删了盘上那份 parked 记录** ⇒ 再调只会回「无 parking 记录」
    （`req is None` 那条**同样不 transition**）⇒ **任务永久卡死**，`/api/conflicts` 还一直列着它；
  · 阶段推进的 `pending` 集合是「非 DONE/ROLLED_BACK/FAILED/DECOMPOSED」，**`conflict_held` 算 pending**
    （`orchestrator.py:844`）⇒ **项目永远推不进 `integrating`**。
  · **而且没有任何 API 能把任务从 `conflict_held` 挪出来**：`retry` 只收 FAILED/ROLLED_BACK，
    `update` 只能改 description，`cancel` 只能标 FAILED（**那是假的** —— 活明明合进去了）。
  ⚠️ 这轮**用改任务 json 的方式（status→done）解开的**（`read_task` 每次读盘、scan cache 2s TTL）。
  ⇒ 修法：`_mark_merged` 补 `tracker.transition(task_id, DONE)`；`resolve` 别在 transition 之前删 parked。
  **没动手**（这轮跑完再改，改了要重启，重启会踩 §17）。
- [奇点] 🔴 **生成的项目仓没有 `.gitignore` ⇒ `.pyc` 被提交、合并时在二进制上冲突卡死任务**（09-17 真机撞见）：
  真实规模那轮（`验证-日志统计-20260917`）一个任务 `conflict_held`，冲突是
  `['logstat/__pycache__/__init__.cpython-314.pyc', 'logstat/__pycache__/parser.cpython-314.pyc']`
  —— **冲突发生在字节码上**。根因：项目仓**压根没有 `.gitignore`**，`git ls-files` 里真躺着 3 个 `.pyc`。
  ⇒ **多个任务碰同一个模块**时，各自跑测试编译出**不同的二进制 `.pyc`** ⇒ 合并必冲突 ⇒ 任务 held、下游降级。
  ⚠️ **前十几轮没撞见是因为 FizzBuzz**：单文件时写实现和写测试碰的是**不同路径**的 pyc，不交叉；
  **这是"真实规模、多任务同模块"才会露出来的形状**（同族：[调研报告解析失败] 也是真规模第一次跑才露）。
  ⇒ 候选修法：建项目仓时写一份 `.gitignore`（至少 `__pycache__/`、`*.pyc`、`.pytest_cache/`）；或提交前剔除。
  **没动手。**
- [奇点] 🔴 **调研报告解析失败 ⇒ 界面上是个空框，一个字不提示**（09-17 凌晨真机撞见，用户原话「我怎么不能看报告」）：
  真实规模那轮（`验证-日志统计-20260917`）报告 20KB，模型吐的 JSON **字符串里带裸换行、还漏了闭引号和逗号**
  （`research.md:114`）⇒ `try_parse_json` 那几层修复（围栏 / `}?` / 尾逗号 / 截断修复）**一層都不覆盖**
  ⇒ 走兜底 `{"raw_output": raw[:5000], "parse_error": True}`（`_io.py:193`）
  ⇒ 前端 `ResearchReport` 只读 `competitive_analysis.products` / `pitfalls` / `recommendation`，兜底那份**一个都没有**
  ⇒ **渲染成空框**；而**前端压根不认识 `parse_error`**（全仓 grep 零命中）⇒ **静默**。
  ⚠️ **全文没丢**（`<id>.research.md` 20KB 完整），但 **UI 没有入口**（`/files` 跳过 `.md` 和 `.qidian/`）。
  ⚠️ 前十几轮没撞见，是因为**全是 fizzbuzz、报告小能解析** —— 「真实规模」第一次跑就露出来了。
  ⇒ 两条候选（**没动手**）：① 前端认 `parse_error`，至少显示「解析失败 ＋ 原文」② 兜底别截断到 5000。
  ⚠️ **连带**：`_run_planning` 只把 `research.md[:5000]` 喂给架构师（2 万字的报告只喂四分之一）。
- [奇点] 🟡 **`final_status` 会把「已交付」标成 `blocked`**（09-16 查清，⚠️ 我原先猜"记的是中间态"是**错的**）：
  `final_status` **不是任务状态**（那看任务 json），是 `neijinglu.build_report` 算的**交付判词**：
  `rolled_back` / `action=="abort"→blocked` / `verdict=="通过"→delivered(_unverified)` / **其余 → blocked**。
  ⇒ 真机那轮 **4 个任务全交付了**，其中一个（`[只读]`）判词是 `blocked`：它的
  `verdict=信息不足`、`action=retry`（validator 想要下一轮），任务照样被 cascade 接受、合并、交付。
  **读 trace 的人会以为它没成。** ⇒ 语义上更像 `delivered_unverified`（"交付了但有保留"）。
  **没修**：动的是**报告语义**，UI 和账本都在读，改了得真机验一轮 —— 不值得为一行字冒这个险。
- [奇点] 🔴 **接手指针** —— 先看这条。
  🎯 **下一步：跑一次真机，验"已修未验"的那六条**（清单下面标了 ⚠️ 的那几条）。
  顺带看**两个数掉没掉**（`collect_changes` / `lazy_spoke`，见下面告警那条）。
  **环境实际状态**（09-16 23:12 核的）：`.qidian/projects/` 里 **3 个项目定义**
  （`1789477040616` / `1789481895784` 两个旧的 + 今晚的 `1789563770837`），
  **盘上 7 个项目仓**；三个更早的真机项目**只删了定义**，证据在
  `~/项目/_归档-20260916-真机验证证据/`（README ＋ 三份 git bundle ＋ `产物/` 那份 40 测试全过的源码）。
  ⚠️ **账本没清** —— `usage_daily.json` / `token_usage.json` / `memory/{edges,events}.json`
  里还有旧项目 id，**见到孤儿 id 不是故障**。
  · **后端停着**，要跑：`nohup .venv/bin/python -m singularity.web.app > /tmp/qidian.log 2>&1 &`
  · ⚠️ `auto_mode=False` ⇒ **GATE1/2/3 都要人点**（`POST /api/projects/<id>/gate-confirm`
    body `{"gate":"gate1/2/3","decision":"approved"}`）；续跑 `POST /api/projects/<id>/start`；
    curl 记得 `--noproxy '*'`（本机也会被系统代理劫走）。→ **详情 §1**
- [奇点] 🟡 **交付清单 E2E 那半：代码已修（`4eb63cd`），⚠️ 真机没验** —— `reports` 那半
  09-16 真机验过（收到 4 个报告）；E2E 的根因是**输入没人写**（`test_cases.json`），已补写入端。
  ⚠️ **09-17 真机新发现：它的读取端押在一条没被强制的约定上**。`_run_integration_merge` 拿
  `test_cases.json` 里声明的 **`name` 去拼 `pytest -k`**，前提是"声明的 name 就是测试函数名"——
  而**架构 schema 里 `test_cases.name` 就是 `""`，没有任何地方要求这个格式**
  （`execution_judge.py:270`）。09-16 那轮 5/5 全中**是运气**：那个架构恰好起成
  `test_normal_path_output_n5` 这种函数名。**09-17 这轮全成了中文描述**（`parse_ts 时区归一化`、
  `CLI --level ERROR 正常路径`）⇒ `-k` 一条都选不中 ⇒ pytest 退 5 ⇒ 走「出声 + 放行」
  ⇒ **集成用例还是没真跑**（只是这次会出声）。⚠️ 行为待这轮跑到集成检查确认（机制已读码坐实）。
  ⇒ 真修法二选一：给 schema 加一条「name 必须是 pytest 函数名」并要求写测试的人照用；
  或别猜名字，改成按 `target_module` / 目录跑。**没动手。**
- [奇点] 🟡 **「委员会换/加席位」先别做** —— 地基没验（P1 三臂实验只差跑）；席位视角已被 A/B 证伪。→ **详情 §3、§21**
- [奇点] 🟡 **「收编 Claude/Codex」** —— Claude 那半**代码早做完**（`claude_cli.py`）但**没配、也没 Anthropic key**；
  Codex 没有。真门槛是三个硬伤（禁工具/记账靠正则/权限拦不住）。→ **详情 §5**
  🔵 **09-17 查到一条**如果**（**只读了 OpenHands 的公开文档 + ACP 规范摘要，没读源码，别当定论**）：
  **我们卡住的三条，是因为我们在"收编"它们；OpenHands 换了条路 —— 当它们的"客户端"。**
  · OpenHands 用 **ACP**（Agent Client Protocol，**Zed 2025-08 开源**，Apache 2.0；类比 LSP：
    LSP 让编辑器不用给每种语言写适配，ACP 让宿主不用给每个 agent 写适配；**ACP 连"宿主↔agent"，
    MCP 连"agent↔工具"**）。做法是 **Agent Server 把 CLI 拉成子进程、JSON-RPC over stdio 对话**：
    Claude Code = `npx -y @agentclientprotocol/claude-agent-acp`；Codex = `npx -y @zed-industries/codex-acp`。
    ⚠️ **两家都没有原生 ACP，是 Zed 维护的适配器桥接的**（多一层依赖，是个风险）。
  · 🔴 **关键差别**：文档原话「**The external agent manages its own LLM, tools, and execution**」
    ⇒ **OpenHands 不碰那三件事**。所以它不是**解决**了我们的三个硬伤，是**根本不碰**（换层抽象）。
  · ✅ **但有一条确实可能解我们的死结**：ACP 协议**本身有权限通道** `session/request_permission`
    （方向 **agent → client**，客户端回 `allow_once`/`allow_always`/`reject_once`/`cancelled`，
    实现一般**失败关闭**）⇒ **"权限拦不住"有标准做法**：我们当 client，它来问，我们说了算。
    ⚠️ **记账（token/usage）文档里没提** —— 这三个硬伤里它只对上了一个，别以为找到了全套答案。
  ⇒ **待评估**（不是待做）：① 奇点当 ACP client 是否可行 ② 适配器那层靠不靠得住 ③ 记账怎么办。
- [奇点] 🟡 **Google Stitch 挂进奇点** —— 卡在「它可能只认 OAuth」。下一步是**探针不是写代码**（要一把 key）。→ **详情 §6**
- [奇点] 🔴 **只差 F3 了**（`一次性调用卡 240s 零产出`那条）：F2 `93376a7` **今晚真机没触发**（没卡）；
  **F1 ✅ `532ae89`**（撞上限且零产出 ⇒ 抛，别返回"看起来正常的空回答"）。
  **F3 没做**：判失败但**产物可打捞** —— 现在靠 `refs/qidian/pending/*` 活着，界面上一个字不显示。→ **详情 §7**
- [奇点] 🔴 **「清理孤儿 pending ref」是地雷** —— 它现在不工作，而**修好它会毁掉可恢复的产物**。
  ⚠️ 同族第三次「读错仓库」，前两次修了这处没修。**修之前先定什么算"孤儿"**。→ **详情 §9**
- [奇点] 🟡 **「有报告但 issues 没标路由」默认回架构**（清空重规划）—— 与同函数「无依据时回实现层」的原则相反。
  现在够不到，留着。→ **详情 §10**
- [奇点] 🟡 **QA 契约「间歇性」** —— 探针响过、**只证明"不是那两个候选"**，没证明是什么。再查得换地方。→ **详情 §11**
- [奇点] 🟡 **GATE2 两种处境同一句话 —— 已修 `6aa1539`，⚠️ 真机没验**（前端已 `npm run build`）。
  兜底升上来时会改口 + 把 `lineage` 里那条 reason 摆出来（「为什么又问你一次」）。→ **详情 §12**
- [奇点] 🟡 **告警 ③「真出口」还不到时候** —— ① 是测量误差、② 早就建好了（见快照 §16 补记）；
  **筛子还糊着**：近 7 天 1000 条→37 个 key，**前三名占 60%**。`collect_changes`（338）✅ 已修 `cb684df`、
  `lazy_spoke`（133）✅ 已修 `8675358` —— **等下一轮真机看这两个数掉没掉**，掉了才谈 ③。→ **详情 §16**
- [奇点] 🟡 **重启把任务回收成 PENDING 后，重派撞上"worktree 已经没了"** —— **机制没坐实**，
  别当结论用。"重启一次就会踩到"本身就值得查。→ **详情 §17**
- [奇点] 🔴 **人审门的否定族还剩一个洞：「别继续了」判 approved** —— 没硬修，用**钉住现状的测试**把天花板变可见。→ **详情 §19**
- [奇点] 🔴 **聚合视图一上线抓出三处"从来没跑过"的功能** —— 已修，**⚠️ 真机没验**。→ **详情 §22**
- [奇点] 🔴 **「任务标成 running 却没人管」那一族**（5 处兜住 `e3a835d`/`70478ca`）
  —— ❌ **09-17 真机验了：没兜住**（任务卡 RUNNING、工人空闲、循环自旋，**5 处 guard + 孤儿探测一条没响**）
  ⇒ **这条从"已修未验"变成"修了但没解决问题"，要重修**。现场与栈 → 上面那条。→ **详情 §25**
- [奇点] **异常收尾的 3 个缺口：修了 2 个** —— **⚠️ 真机没验**（worker 异常不好造，等撞上顺手看）。→ **详情 §23**
- [奇点] **异常收尾的取消路径** —— 真机没验到，**改用"真落盘"验的**（严格讲不算真机验）。→ **详情 §26**
- [奇点] P1 三臂实验：方案在 `docs/P1-三臂实验方案-20260912.md`，**没跑（只差跑）**。
  跑之前对上三件（写在方案里）。→ **详情 §21**
- [奇点] **验收轮状态**：逐条的 ✅/❌/◐/— 全在 `docs/待验证清单-20260912.md`，**别在这儿抄数**。
  🔑 轮 4 的教训：**数"总数"要连组成一起数**。→ **详情 §20**
- [奇点] ⚠️ **`待验证清单` 的 #33 过期了**（它写"并发 1 ⇒ 暂停占死队列"，实际部署传 `concurrent=2`）。
  **下次动"暂停占工人"那条时先核这里。** → **详情 §24**
- [奇点] 📦 **历史存档：2026-09-14 中午版接手指针**（已做完的账，**别照它做**）。→ **详情 §18**
- [奇点] ⏸ **搁置**（09-12 用户拍板：质量 / 成本·速度 两项先不做）→ 前置的"分阶段算账"**一并搁置，别重复提**。
  ⚠️ 要做时记得：超时任务的花费补进去的是**下界**（成本仍偏低）。→ **详情 §27**
