# 文档核对-02（F / ZCode · GLM-5.3-Flash）+ 我的核实

> 原始件**就是本文件正文** —— 它按指令**直接写进了仓库 `docs/`**（不是 `~/Desktop/ZCode审阅/`）。
> 开工约 01:5x，**02:30 完成**。对象是 `docs/生产流现状.md` + `docs/项目速查.md`
> （B 那轮核的是另外两份：`防御模式.md` + `架构性质清单.md`）。

## ✅ 我核过的（抽 9 条结论性的，逐条翻当前源码）

| 它说 | 我核到 |
|---|---|
| 速查写版本 v4.4，实际是 **2.1.156** | ✅ `pyproject.toml:7` `version = "2.1.156"` |
| 「集成失败 ≤2 次回 EXECUTING」偏宽 | ✅ `project.py:55` `_INTEGRATE_MAX_RETRIES = 2`，而 `orchestrator.py:958` 判据是 `>= 2` ⇒ **第 1 次失败回 EXECUTING、第 2 次直接升 GATE2**。文档把"上限"读成了"允许次数" |
| 「issues 在 `_run_execution` 开头整体清空」位置错 | ✅ 清空在 `workflow.py:430`（`run_test_fix_loop` 里），注释白纸黑字「清空上一轮 issues 必须在验收**之前**」 |
| 辩论轮数不是「1~3 轮」 | ✅ `execution_judge.py:731` `max_rounds = max(2, int(os.environ.get("QIDIAN_FUSION_V2_ROUNDS", "5")))` ⇒ **默认上限 5、env 可调** |
| `/ws` 不是 Flask 路由，是独立 WS 服务器 | ✅ `app.py:2283` `ws_bridge.start_ws_server(host="127.0.0.1", port=5051)`（另有 Observer WS 在 8765） |
| `/api/tasks/<id>` 没有 DELETE | ✅ 删任务走 `POST /api/tasks/<task_id>/delete`（`app.py:1095`）；`<task_id>` 那个只有 GET/PUT |
| 速查的数字过期（测试收集数） | ✅ 我自己刚跑过：scheduler **1241** / 全量 **1248**，它报的数对得上 |
| 「防御模式现在 71 条」 | ✅ `grep -c '^### '` = 71。⚠️ **但 71 里有一条是我今天新加的 `### §67 补记`** ⇒ 按"编号条目"数仍是 **70**（B 那轮数 70，两边不矛盾）。**不是它数错，是那个数会跟着我的编辑漂。** |
| `test_flow_weight` 22 条不是 18 | ✅ 实测 `22 tests collected` |

## ⚠️ 一条要留意的：基线是准的，但**树在它跑的中途变了**

它自报 `HEAD = 90580a2（2026-09-14 01:54）` —— **我核了，这个自称是准的**：
`90580a2` 的提交时间正是 `01:54:15`，它 02:30 完成、头部是开工时写的。**没有谎报基线。**

⚠️ **但第二批 `4281960` 是 02:09 落的，正落在它跑的中间** ⇒ 它后半程读的树已经变了。
影响面：第二批动了 `orchestrator.py` / `validator.py` / `web/app.py` / `mcp.py` / `_api_admin.py` /
`_task_runner.py`，而它的 §一 / §二 正好覆盖 orchestrator + workflow
⇒ **它判"成立"的条目里有几条是对着改动前的树判的**。
抽核过的这 9 条**不受影响**（都不在那些改动点上），但**别把整份当成"对 `4281960` 的结论"** ——
它是对 `90580a2` 的结论。**下次派任务要把"基线提交"写进指令体、并要求它开工时钉一次。**

## 📌 它自己的纪律（值得记）

- 明说**只读那两份文档 + 仓库源码**，其他文档一律没读；`防御模式.md` **只跑了它自带的核对命令**
  `grep -c '^### '` 取条数、**没读正文** —— 而这正是上一轮（B）被污染的方式，它躲开了。
- `.qidian/` 运行时数据和外部目录没碰，**涉及运行时状态的如实标「核不了」**（5 条），没有硬判。
- **101 条：成立 77 ｜ 部分过期 19 ｜ 完全不符 0 ｜ 核不了 5**，成立也带行号。
- 末尾附了按文档位置排序的 19 条修改清单，改文档可直接照做。

---

# 文档核对-02（2026-09-14）

- **对象**：`docs/生产流现状.md`、`docs/项目速查.md`
- **方法/硬约束**：只读这两份 + 仓库源码。其他文档一律没读（`防御模式.md` 只跑了它自带的核对命令 `grep -c '^### '` 取条数；引用到的其余文档只做存在性检查）；`.qidian/` 等运行时数据、`~/知识/` 等外部目录未读 → 相关条目标「核不了」。
- **源码基线**：HEAD = `90580a2`（2026-09-14 01:54，外派评审/扫bug 第一批 6 处修复）。两份文档落款 09-11～09-14。
- **证据路径约定**：`scheduler/…`、`web/…` 均相对 `src/singularity/`；`tests/…` 相对仓库根。文档侧用「现状 L‥」「速查 L‥」表示行号。

**总计 101 条：成立 77 ｜ 部分过期 19 ｜ 完全不符 0 ｜ 核不了 5**

---

## 一、生产流现状.md（60 条）

### §一 实际流转图（L14-58）

| 位置 | 判定 | 证据（文件:行） | 现在实际是什么样 |
|---|---|---|---|
| 现状 L16 立项填 description；`_needs_research()` 决定调研 | 成立 | scheduler/workflow.py:958-968；workflow.py:283-287 | description 缺则拒启；`_needs_research` 已改为转发唯一判据 `resolve_flow`，语义不变 |
| 现状 L18 RESEARCHING 可跳过 | 成立 | scheduler/workflow.py:966-968 | 免调研时立项直接落 PLANNING |
| 现状 L19 调研员出报告 | 成立 | scheduler/_workflow_phases.py:160 | `_run_research` 在此，run_phase RESEARCHING 分支调用（workflow.py:324） |
| 现状 L21 GATE1 人审；打回→TEMPLATE | 成立 | scheduler/workflow.py:363-378；scheduler/project.py:38 | `_REJECT_FALLBACK[GATE1]=TEMPLATE`，与图一致 |
| 现状 L24-26 PLANNING 架构师出方案+任务清单；架构类→委员会 | 成立 | scheduler/_workflow_phases.py:288-306；scheduler/_dispatch_exec.py:55-69 | 架构 prompt+席位选取在；委员会闸门实际是**四**条件（还含 allow_committee、角色否决） |
| 现状 L28 GATE2 人审；打回→RESEARCHING | 成立 | scheduler/project.py:39、176-201 | confirm_gate 走 `_REJECT_FALLBACK[GATE2]=RESEARCHING` |
| 现状 L31-32 EXECUTING 拆任务→tracker→orchestrator 逐个执行 | 成立 | scheduler/_workflow_phases.py:499-530；scheduler/orchestrator.py:736-795 | `_run_execution` 建任务入 tracker，调度循环推进 |
| 现状 L34-36 INTEGRATING 非人门；失败 ≤2 次→回 EXECUTING；触顶→GATE2 | **部分过期** | scheduler/project.py:28、55；scheduler/orchestrator.py:913、956-977 | 非人门/worktree 合并成立；**「失败 ≤2 次回 EXECUTING」偏宽**：`_INTEGRATE_MAX_RETRIES=2` 且判据是 `>=2`——只有第 1 次失败回 EXECUTING，第 2 次失败即升 GATE2 |
| 现状 L38-40 REVIEWING 非人门；QA+安全审计+E2E；constraints 缺整段跳过 | 成立 | scheduler/workflow.py:561-590、643、657、662-663 | 空清单早退并记 `verification_skipped`（比文档多一步留痕） |
| 现状 L42 GATE3 人审按 fix_route 分级打回 | 成立 | scheduler/workflow.py:863-941；scheduler/project.py:34-41 | 分级路由在，枚举处注释同口径 |
| 现状 L45-47 DELIVERING 非人门；tag+收 README/打包配置+归档报告→delivery_manifest.json | 成立 | scheduler/orchestrator.py:1046-1112 | tag:1069-1071、README/DEPLOY:1085-1092、报告:1094-1099、pyproject/Dockerfile/package.json 探测:1101-1107、manifest:1110-1112 |

### GATE3 打回分级表（L51-58）

| 位置 | 判定 | 证据（文件:行） | 现在实际是什么样 |
|---|---|---|---|
| 现状 L55 impl→EXECUTING，重置 DONE 任务为 PENDING | 成立 | scheduler/workflow.py:919-933 | `transition(force=True)`，注释即「DONE 是终态，GATE3 打回是唯一合法重置」 |
| 现状 L56 design→PLANNING，清空 architecture | 成立 | scheduler/workflow.py:939-941 | set_phase(PLANNING) + `architecture=None` |
| 现状 L57 note→不回退只记录 | 成立 | scheduler/workflow.py:934-938 | 仅记 lineage，保持当前阶段 |
| 现状 L58 读不到 QA 报告→EXECUTING（不动架构） | 成立 | scheduler/workflow.py:899-904 | `if not has_qa: fix_route="impl"`，注释逐字「无依据时回实现层…不动架构」。备注：现在 observer_rollup 优先（workflow.py:907-916），文档未提 |

### §二 两套驱动（L62-86）

| 位置 | 判定 | 证据（文件:行） | 现在实际是什么样 |
|---|---|---|---|
| 现状 L69-73 run_phase 认识全部阶段；INTEGRATING/DELIVERING 走交棒分支 | 成立 | scheduler/workflow.py:337-344 | 交棒分支在，注释自述「以前落到 else 报未知 phase」 |
| 现状 L74 `_auto_trigger_test_fix` 每 tick 推 INTEGRATING/DELIVERING | 成立（表述注） | scheduler/orchestrator.py:736-828 | executing→INTEGRATING 落在 :795；delivering 分支 :813-825 是「推进处于 delivering 的项目跑交付→DONE」——全函数没有 set_phase(DELIVERING)，按此理解成立 |
| 现状 L75 project_gate_confirm 人点通过、启 planning 等 | 成立 | scheduler/_api_projects.py:195-213 | 只有 PLANNING 顺手启；executing/integrating/delivering 留给调度循环，注释写明理由 |
| 现状 L76 GATE3 打回按 fix_route 分级回 EXECUTING/PLANNING | 成立 | scheduler/_api_projects.py:220-226 | 调 `handle_gate3_reject` |
| 现状 L78-81 分工只活在 _api_projects.py:198 注释；PHASE_DRIVER+越权告警+守卫测试还没做 | 成立 | scheduler/_api_projects.py:198；全仓 grep `PHASE_DRIVER` 零命中 | 注释原话逐字「planning —— **只有 run_phase 能推**」；治法确实未做 |
| 现状 L83-86 所有流转过 set_phase 唯一入口，自动留痕 from/to/reason/ts；读 lineage `action=="phase"` | 成立 | scheduler/project.py:225-248、212-215、241-242 | add_lineage 补 ts；docstring 自述「阶段流转的唯一入口」 |
| 现状 L84 「以前 21 处散点赋值」 | 成立（历史自述） | scheduler/project.py:231 | docstring 原话「以前是 21 处 proj.phase = X 散在 4 个文件里」，两处口径一致；历史数字无法独立复验 |
| 现状 L67 test_doc_facts.py 钉住这句话，再出现会先红 | 成立 | tests/test_scheduler/test_doc_facts.py:79-83 | 断言 run_phase 派发链覆盖 Phase 全集，注释点名 INTEGRATING/DELIVERING |

### §三 多模型委员会（L92-105）

| 位置 | 判定 | 证据（文件:行） | 现在实际是什么样 |
|---|---|---|---|
| 现状 L93-95 触发两条件：`_is_architecture_task` 六词 + `len(chain)>=2` | 成立 | scheduler/execution_judge.py:969-971；scheduler/_dispatch_exec.py:66 | 六词逐字在；备注：实际闸门四条件（+:63-69 的 allow_committee、角色否决），与本文件专条/速查一致 |
| 现状 L97-98 链条：并行出方案→fuse_architecture_v2（提取→**辩论 1~3 轮**→定稿→其余确认→异议重写 1 次） | **部分过期** | scheduler/execution_judge.py:258、356、401、250-253；scheduler/_dispatch_exec.py:440-449 | 各环节都在；**辩论轮数已非 1~3**：`max_rounds = max(2, env QIDIAN_FUSION_V2_ROUNDS 默认 5)`（execution_judge.py:731） |
| 现状 L100 席位从候选链里取，**最多 4** | **部分过期** | scheduler/_workflow_phases.py:295-298；scheduler/phase_models.py:87-92；scheduler/execution_judge.py:465 | 席位实际=该阶段配置的名单（restrict 生效时 chain 即 lineup）；全仓无「最多 4」席位上限——4 只是并行线程池 `max_workers=min(n,4)`。速查 L59「席位=该阶段配的名单」才与现状一致 |
| 现状 L101-102 去重按 `api_store.canonical()`；别名由 openai_agent 响应路径对账写入 | 成立 | scheduler/dispatcher.py:362-378；scheduler/api_store.py:343；scheduler/executors/openai_agent.py:926 | dispatcher 注释逐字「映射由 openai_agent 在响应路径上对账写入」；`record_alias` 在响应路径 |
| 现状 L104 实测 2 席/621 秒/17158 字符 | 核不了 | —（历史实测数字） | 机制链路存在（见上两行）；数字无法从源码复核 |

### §四 窟窿表（L109-118）

| 位置 | 判定 | 证据（文件:行） | 现在实际是什么样 |
|---|---|---|---|
| 现状 L116 #2 重入语义没定义，只立了 GATE2 一例 | 成立 | scheduler/project.py:184-187；grep「重入」仅命中无关处（orchestrator.py:44、tracker.py:18-19） | GATE2 arch_invalid 不放行是唯一成文的实例；全仓无统一重入声明 |
| 现状 L117 #3 提取员默认 glm-5.3-flash；fusion.toml 覆盖 deepseek-flash 但它同时是委员（自己评自己） | **部分过期** | scheduler/execution_judge.py:439、457；scheduler/fusion.toml 末行+2026-09-12 注释块；scheduler/phase_models.py:11；scheduler/execution_judge.py:231-234 | 代码默认与 fusion.toml 覆盖行都在；**但取值优先级=阶段配置>fusion.toml>代码默认**，extract 现配 glm-5.2 且不在委员会 ⇒「提取员当委员」的降级已消（fusion.toml 注释自述）；自评告警代码仍在 |

### 专条：流程重量分级（L120-155）

| 位置 | 判定 | 证据（文件:行） | 现在实际是什么样 |
|---|---|---|---|
| 现状 L126-127 两开关：`_needs_research`(workflow.py) / `_is_architecture_task`(execution_judge.py) | 成立 | scheduler/workflow.py:283；scheduler/execution_judge.py:961-971；scheduler/project.py:592-596 | project.py 注释同口径「两个独立开关」 |
| 现状 L133-134 唯一入口 resolve_flow→FlowDecision；新增 flow_weight 字段 | 成立 | scheduler/project.py:607-620、73 | FlowDecision(research/committee/source/reason)+weight 派生属性都在 |
| 现状 L136-142 auto 永不否决委员会；light 两样都省；heavy 都跑；存量无键→auto 零迁移 | 成立 | scheduler/project.py:620-644、153 | light=(False,False)、heavy=(True,True)、auto 分支 committee 恒 True；`setdefault("flow_weight","auto")` |
| 现状 L144-146 suggest_flow（≤100 字且无触发词）只建议不写状态；PUT flow-weight 人点了才生效 | 成立 | scheduler/project.py:602-604、646-663；scheduler/_api_projects.py:82-103、53-60 | `_SUGGEST_MAX_DESC=100`；setter 校验 auto/light/heavy 并落 lineage |
| 现状 L148-150 立项写 flow_weight lineage（weight/source/research/committee/reason）；不放 issues 因其会被清空 | **部分过期** | scheduler/workflow.py:969-974（五键逐字）；scheduler/workflow.py:418、430；scheduler/_workflow_phases.py:499 | lineage 本身成立；**「issues 在 _run_execution 开头被整体清空」不准**：整体清空唯一在 workflow.py:430，位于 run_test_fix_loop 内、验收之前；`_run_execution`（_workflow_phases.py:499）只建任务、不清 issues |
| 现状 L152-153 没做的：不自动判轻量；不碰三套对不上的模板表（后端 4/API 收 8/前端给 5） | **部分过期** | scheduler/project.py:695-712；web/app.py:152-153、1227-1228；web/frontend/src/pages/Projects.tsx（4 个 id） | 「不自动判轻量」成立；**模板表错位已修**：后端 4 个，API 改为从 TEMPLATES 派生（注释自述「抄的那份恰恰漂移过」），前端也是 4 个——「4/8/5」已对上 |
| 现状 L155 test_flow_weight.py 18 条（纯函数无 LLM） | **部分过期** | tests/test_scheduler/test_flow_weight.py（grep -c 'def test' = 22） | 文件在、仍无 LLM；条数现为 **22** |

### 专条：审查层（L157-190）

| 位置 | 判定 | 证据（文件:行） | 现在实际是什么样 |
|---|---|---|---|
| 现状 L165 QA 报告 qa-report.md→_run_verification | 成立 | scheduler/workflow.py:561、643 | — |
| 现状 L166 安全审计师报告 security-report.md，并行 | 成立 | scheduler/workflow.py:657；workflow.py:421 区 docstring | docstring「QA+安全审计师并行出报告，人工在 GATE3 审核」 |
| 现状 L167 结构化 QA 报告→validator.build_qa_report | 成立 | scheduler/validator.py:677 | 签名 passed/issues/verdict/verdict_reason；fix_route 挂 issues 条目，GATE3 读它（workflow.py:889-897） |
| 现状 L168 需求符合性核验 supervisor.check_requirement_conformance | 成立 | scheduler/supervisor.py:404、162 | — |
| 现状 L169 约束清单验收 `_review.qa_acceptance_review` | **部分过期（位置漂移）** | scheduler/validator.py:699（定义）；scheduler/_review.py:492（调用） | 函数已搬到 **validator.py**，不再是 `_review.qa_acceptance_review` |
| 现状 L170 每任务机械检查 `_exec.py:426`→run_post_exec_checks | **部分过期（位置漂移）** | scheduler/_review.py:171（定义）；scheduler/_exec.py:665（调用） | `_exec.py:426` 处现在是 model_prices 相关代码；函数实名在 _review.py:171 |
| 现状 L171 E2E 对照 test_cases.json | 成立 | scheduler/workflow.py:662-663 | — |
| 现状 L173-180 自动修闭环被 d3c117e 删 | 成立 | git show d3c117e（2026-06-27「砍AI互审」：删 conductor/judge_monitor/_judge_and_profile/_run_fusion/_do_review） | 与文档叙述一致 |
| 现状 L184-186 前置条件整段跳过：constraints 空→_run_verification 直接 return | 成立 | scheduler/workflow.py:572-590 | 早退且记 verification_skipped + lineage 探针（比文档描述多留痕） |
| 现状 L186-187 软信号注释「只写软信号+warning（机械关键词，先不设 hard gate）」 | 成立 | scheduler/_review.py:533 | 注释逐字命中 |

### 2026-09-11 已修表（L192-198）

| 位置 | 判定 | 证据（文件:行） | 现在实际是什么样 |
|---|---|---|---|
| 现状 L196 #1 run_phase 交棒给调度循环 | 成立 | scheduler/workflow.py:337-344；a41f590 在案 | 同 §二第 1 行 |
| 现状 L197 #2 FIXING 全仓无人赋值→删；REVIEWING 留（UI 显示审查中） | 成立 | scheduler/workflow.py:364-380；scheduler/orchestrator.py:827；scheduler/project.py:139 | 全仓无 FIXING 枚举/赋值（仅注释提及）；REVIEWING 分支注释即「不是死代码——UI 靠它显示审查中」；「9 处引用」为历史数字无法复验 |
| 现状 L198 #3 架构校验分两档：tasks 缺失才拦；拦在「人点通过」一瞬 | 成立 | scheduler/_workflow_phases.py:367-388；scheduler/project.py:184-187 | fatal=含 tasks 的 blocker，其余 arch_warning；confirm_gate 在批准瞬间拦 |

### §五 实测基线（L202-215）

| 位置 | 判定 | 证据（文件:行） | 现在实际是什么样 |
|---|---|---|---|
| 现状 L206 后端 1087／全量 1094；前端 28（未复核） | **部分过期** | `pytest --collect-only`（2026-09-14）：tests/test_scheduler/ = **1241**，tests/ = **1248** | 数字已漂（其间新增多批测试）；前端 28 核不了（未跑） |
| 现状 L207 候选链 deepseek-v4-pro、deepseek-flash、glm-4.7 | 核不了 | scheduler/agents.toml:1-2（自述仅占位，实际配置 agents_custom.json 由 Web UI 管理） | 激活池是运行时数据（.qidian/，按约束未读） |
| 现状 L208 委员会能跑通（2 席/621s/17158 字符） | 核不了 | 同 §三实测行 | 机制在，数字是历史实测 |
| 现状 L209 已配 11 个模型；未配如实显示「未配置价格」 | 核不了（个数）＋成立（机制） | scheduler/model_prices.py:30（价表在 .qidian/model_prices.json）、:16；web/frontend/src/components/ModelsTab.tsx:73 | 11 个是运行时数据核不了；「查不到返回 None→如实显示未配置价格」成立 |
| 现状 L210 交付空清单正常，不编造 | 成立 | scheduler/orchestrator.py:1085-1107 | 全部 if exists 探测 |
| 现状 L212-214 token_spent 已删；花费看 /cost 的 cost（用量按 project_id 汇总） | 成立 | scheduler/_api_projects.py:326-329、345-346 | cost 取自 usage stats by_project；token_spent 仅剩删除注释；全仓无赋值点 |

### §六（L218-225）

| 位置 | 判定 | 证据（文件:行） | 现在实际是什么样 |
|---|---|---|---|
| 现状 L220 防御模式.md **58 条**（含 §52b） | **部分过期** | `grep -c '^### ' docs/防御模式.md` = **71**（文档自带核对命令） | 条数已涨到 71；§52b 是否仍在未核（按约束没读内容） |
| 现状 L221-222 演化史.md / CLAUDE.md | 成立（存在性） | docs/演化史.md、CLAUDE.md 均在 | 内容未核 |
| 现状 L223 验证命令（1087 条） | **部分过期** | 同 §五测试行 | 现收集 1241 |
| 现状 L225 09-12 晚 5 笔只有桩测试绿过 | 核不了 | docs/待验证清单-20260912.md 存在（仅存在性） | 过程性断言，无法从源码复核 |

---

## 二、项目速查.md（41 条）

| 位置 | 判定 | 证据（文件:行） | 现在实际是什么样 |
|---|---|---|---|
| 速查 L8 pip install -e . | 成立 | pyproject.toml 在 | — |
| 速查 L10 `python3 -m singularity.web.app` → 127.0.0.1:5050 | **部分过期** | web/app.py:2306（port=5050）；本文件 L16-18 自述 | 端口对；但系统 python3 起不来（editable 指向死路径，见下行）——实际必须 `.venv/bin/python`，本代码块与下方警告自相矛盾 |
| 速查 L11 docker compose up -d --build | 成立 | docker-compose.yml、Dockerfile 均在 | — |
| 速查 L14 测试命令 + tests/test_exec_run.py | 成立 | tests/test_exec_run.py 在 | — |
| 速查 L16-18 venv 警告：homebrew editable 指向已不存在的 ~/项目/奇点/src | 成立 | /opt/homebrew/lib/python3*/site-packages/__editable___singularity_2_1_156_finder.py（MAPPING→/Users/jingzhe/项目/奇点/src/singularity）；该目录 ls 不存在 | 警告完全属实 |
| 速查 L22-25 项目约束（先读现状+详解、扫防御模式、危改 /checkpoint） | 成立（部分核） | 三份文档均在（仅存在性） | /checkpoint 是会话机制，仓库内核不了 |
| 速查 L29-34 指针 | 成立（存在性） | 各文件在 | ~/知识/ 未核 |
| 速查 L36-37 09-11 清理：四份自认过时文档已删 | 成立 | 仓库根无 ARCHITECTURE.md/production-flow.md/现状结构图.md/角色契约.md（ls 证实；scheduler/ARCHITECTURE.md 是另一文件） | git 可取回未验 |
| 速查 L44 当前版本 **v4.4**；**814** 测试绿（~10s）；全量 **823** | **部分过期** | pyproject.toml:7 version = **"2.1.156"**；collect-only：1241/1248 | 源码唯一版本号是 2.1.156（「v4.4」疑为演化史口径，按约束没读）；测试数过期 |
| 速查 L50-56 架构概览：Router→Orchestrator→Dispatcher→流水线 | 成立 | scheduler/router.py、orchestrator.py、dispatcher.py；scheduler/project.py:20-31 | 流水线与 Phase 枚举逐一对应 |
| 速查 L54 10 角色名单 | 成立 | scheduler/roles.toml（恰 10 个 name，名单逐字对上） | — |
| 速查 L56 能力推荐制 rating(SSS+~A)+recommended_for | 成立 | scheduler/models.toml:3-9、16；scheduler/api_store.py:542 | 评级表 SSS+ > SSS > … > A |
| 速查 L58-59 委员会触发：强短语+候选链≥2，席位=该阶段配的名单 | 成立 | scheduler/_dispatch_exec.py:55-69；scheduler/execution_judge.py:969-971；scheduler/_workflow_phases.py:295-298 | 与现状 §三「最多 4」冲突处，速查这版是对的 |
| 速查 L68-77 核心模块表（Router/Orchestrator/Dispatcher壳/Validator/Memory壳/Observer/Executors/Web/Skills 5 个/Neijinglu） | 成立 | 各文件在：_dispatch_crud/_dispatch_exec/_dispatch_skills、_memory_*.py、_observer_*.py 8 个+observer/、executors/、skills/{archify,code-review,codegraph,creative-brainstorm,ddd}/SKILL.md、neijinglu.py | — |
| 速查 L71 Workflow「**6 阶段**流水线」壳→_workflow_phases.py | **部分过期** | scheduler/project.py:20-31；_workflow_phases.py 在 | 壳/拆分成立；「6 阶段」过时——Phase 枚举现为 **11 态**（template→done） |
| 速查 L82 文件持久化 .qidian/ | 成立 | scheduler/config.py:31；.gitignore:15 | — |
| 速查 L83 Worktree 隔离 | 成立 | scheduler/_worktree.py、_git_worktree.py；orchestrator.py:21 | — |
| 速查 L84 GATE 人审不走 _check_paused+PAUSE_DIR；暂停标记只服务「逐步确认」+Tasks 手动暂停；前端无 auto_mode | 成立 | scheduler/config.py:31（注释逐字「暂停标记：逐步确认模式 / 手动暂停按钮 (GATE 不走这里)」）；scheduler/_exec.py:275、537；scheduler/_api_tasks.py:285-316；前端 grep auto_mode 零命中 | — |
| 速查 L85-87 多模型碰撞只在架构阶段；09-11 加角色否决 | 成立 | scheduler/_dispatch_exec.py:63-69、160-172 | 注释即「planner 拆出的子任务天然继承架构词，故加角色否决」 |
| 速查 L93 Router._llm_classify | 成立 | scheduler/router.py:90 | — |
| 速查 L94 写 .qidian/tasks/ | 成立 | scheduler/tracker.py:117 | — |
| 速查 L96-97 Validator 裁决 pass/retry/abort | 成立 | scheduler/validator.py:27-53 | — |
| 速查 L98 Memory.index_task | 成立 | scheduler/_memory_core.py:336 | — |
| 速查 L99 Observer WebSocket 推送 | 成立 | web/app.py:2281-2287 | — |
| 速查 L107-109 /api/tasks GET/POST；/api/tasks/<id> GET/PUT/**DELETE**；pause\|resume POST | **部分过期** | web/app.py 路由表（pause:1076、resume:1082） | GET/PUT/pause/resume 对；**DELETE 不在 /api/tasks/<id> 上**——实际是 POST /api/tasks/<task_id>/delete |
| 速查 L110-111 /api/projects GET/POST；/run-phase POST 后台线程 | 成立 | web/app.py 路由；scheduler/_api_projects.py:236 起（_start_background） | — |
| 速查 L112 /api/phase-roles GET/PUT | 成立 | web/app.py 路由表 | — |
| 速查 L113 /api/phase-models GET/PUT（五项：调研/架构/实现/审查/融合提取） | 成立 | web/app.py:1899-1900、1935 | `_PHASE_MODEL_LABELS = 四阶段 + [("extract","融合提取")]` |
| 速查 L114 /api/agents GET/POST/DELETE | 成立（备注） | web/app.py 路由表 | DELETE/PUT 实际落在 /api/agents/<level>/<model> 子路径 |
| 速查 L115-116 /api/agent-skills/<level>；/api/agents/<level>/<model>/skills | 成立 | web/app.py 路由表 | 两条均在，GET/PUT |
| 速查 L117 /api/models GET/POST | 成立（备注） | web/app.py 路由表 | 另有 PUT/DELETE/import/benchmark/tier |
| 速查 L118 /api/memory GET（`?action=query&q=…`） | **部分过期** | web/app.py:1190-1201 | 路由在、q 参数在；**没有 action=query 参数**——handler 只读 q/files/beam/hops/depth |
| 速查 L119-120 /api/approvals GET；/api/tasks/<id>/approval POST | 成立 | web/app.py:2041、1106 | — |
| 速查 L121 `/ws` WebSocket Observer 实时连接 | **部分过期** | web/app.py:2281-2287 | 不是 Flask 的 /ws 路由（全仓无此 route）；是**独立 WS 服务器 ws://127.0.0.1:5051** |
| 速查 L123-125 四个路径不存在（/api/config/roles、/api/config/models、/api/admin/skills、/api/memory/search） | 成立 | 四条 grep `@app.route` 全 0 | 仍不存在；补充：09-14 起 /api/memory/chain/<task_id> 已修通（90580a2 第 6 条） |
| 速查 L131-134 前端页面 /、/tasks、/config（ModelsTab/AgentsTab/SkillsTab）、/projects | 成立（备注） | web/frontend/src/pages/（Chat/Tasks/Config/Projects.tsx）；App.tsx:27-32；components/ 三 Tab | 另有 /usage、/alerts 两页未列（表未声称穷尽） |
| 速查 L138 .env 不入 git | 成立 | .gitignore:3 | — |
| 速查 L139 .qidian/ gitignore | 成立 | .gitignore:15 | — |
| 速查 L140 scheduler/config.py 路径常量+脚本配置 | 成立 | scheduler/config.py（如 :31 PAUSE_DIR） | — |
| 速查 L141 roles.py+roles.toml 10 角色（name/persona/prompt） | 成立（备注） | scheduler/roles.toml:8-9 等 | 10 条目对；第三键实名 **system_prompt**，非 prompt |
| 速查 L142 models.toml（provider/model_id/api_base） | **部分过期** | scheduler/models.toml:12-16；grep api_base = 0 | 实际键是 **id/provider/display/recommended_for**；无 model_id 写法、无 api_base（接入信息不在 models.toml） |

---

## 三、重点：19 条「部分过期」清单（改文档时照此改）

1. **现状 L34-36**：集成失败「≤2 次回 EXECUTING」→ 实际只有第 1 次失败回 EXECUTING，第 2 次即升 GATE2（`_INTEGRATE_MAX_RETRIES=2`，判据 `>=2`）。
2. **现状 L97-98**：辩论「1~3 轮」→ 现在 `max_rounds = max(2, env 默认 5)`。
3. **现状 L100**：席位「从候选链取最多 4」→ 席位=阶段配置名单，无 4 席上限（4 只是线程池并发）；速查的写法才对。
4. **现状 L117（#3）**：「提取员同时是委员」已消——阶段配置（extract=glm-5.2，不在委员会）压过 fusion.toml 覆盖；代码默认与覆盖行仍在。
5. **现状 L148-150**：「issues 在 _run_execution 开头清空」→ 实际在 `run_test_fix_loop`（workflow.py:430）内、验收之前；`_run_execution` 不清 issues。
6. **现状 L152-153**：「三套模板表 4/8/5 对不上」已修——API 改为从 TEMPLATES 派生，现三处都是 4。
7. **现状 L155**：test_flow_weight 18 条 → 现 22 条。
8. **现状 L169**：`_review.qa_acceptance_review` → 现定义在 validator.py:699（调用在 _review.py:492）。
9. **现状 L170**：`_exec.py:426` → run_post_exec_checks 现定义在 _review.py:171，调用在 _exec.py:665。
10. **现状 L206/L223**：1087/1094 → 现收集 1241/1248。
11. **现状 L220**：防御模式 58 条 → 现 71 条。
12. **速查 L10**：`python3 -m singularity.web.app` 与本文件 L16-18 的 venv 警告自相矛盾——系统 python3 起不来。
13. **速查 L44**：v4.4/814/823 → pyproject 是 2.1.156；测试现 1241/1248。
14. **速查 L71**：「6 阶段流水线」→ Phase 枚举 11 态。
15. **速查 L107-109**：`/api/tasks/<id>` 无 DELETE 方法——实际是 POST `/api/tasks/<id>/delete`。
16. **速查 L118**：`/api/memory` 无 `action=query` 参数（只有 q/files/beam/hops/depth）。
17. **速查 L121**：`/ws` 不是 Flask 路由——独立 WS 服务器 ws://127.0.0.1:5051。
18. **速查 L142**：models.toml 键是 id/provider/display/recommended_for，无 model_id/api_base。
19. **速查 L141（轻）**：roles.toml 第三键实名 system_prompt（name/persona/system_prompt）。

## 四、「核不了」5 条及原因

| 条目 | 原因 |
|---|---|
| 现状 L104 / L208（委员会实测 2 席/621s/17158 字符） | 历史实测数字，源码无法复核（机制链路已核在） |
| 现状 L207（候选链三模型） | 激活池是运行时数据（agents.toml 仅占位；.qidian/ 按约束未读） |
| 现状 L209（已配 11 个模型） | .qidian/model_prices.json 运行时数据未读（「未配置价格」机制已核成立） |
| 现状 L225（09-12 五笔只有桩测试绿） | 过程性断言；待验证清单文件存在（仅存在性） |

## 五、备注

- HEAD `90580a2`（09-14）晚于两文档落款，改了 GATE 人审回复判定（`_is_gate_reply`）、`/api/memory/chain`（原恒 500）等 6 处——两份文档均未描述这些细节，故不构成任何一条的「不符」，但速查 API 表可补 `/api/memory/chain/<task_id>`。
- 「成立」条目均给出直接行号；个别历史性数字（21 处散点赋值、FIXING 9 处引用）源码注释与文档口径一致，但本身无法独立复验，已在行内注明。
