# 架构级静默失败 —— 外派材料（**设计题**，不是补丁题）

> **给谁看**：外部模型。整份读完就能答，不需要访问代码库。
> **上一份外派（12 案版）是"帮我设计治理方案"**，产出了 6 条增量。
> **这一份是另一层的问题**：*要不要动架构、动哪一层、我们列的方案可行性如何、顺序怎么排。*
>
> ⚠️ **请务必先读第六节「我们已知错过的方向」**。在这个系统里，
> **"看起来最像的原因"经常是错的** —— 我们为此浪费过整晚，也误导过上一轮外派的模型。

---

## 一、这个系统是什么（30 秒版）

一个 **AI Agent 调度平台**：多个大模型按角色协作完成软件开发任务。

- **编排**：一个调度循环（后台线程）+ 任务状态机 + 门禁（GATE）
- **执行**：把任务派给执行器（有的走 HTTP 调模型 API，有的起子进程）
- **并发**：`ThreadPoolExecutor` + 一堆裸 future；Web 层是 Flask（同步）
- **持久化**：**零数据库**，全是 JSON 文件（`.qidian/` 下），设计上是为了能 `grep` 排查
- **规模**：Python 单体，`scheduler/` 下 **76 个** `.py`、`src/singularity` 全仓 **96 个**，
  全仓 **561 个 `except` 子句**
- **模型输出是不可信的**：所有产出都是模型生成的文本，要解析、要校验

**一句话**：这是一个**手写的、用文件和线程池拼出来的工作流引擎**。

---

## 二、问题陈述

**静默失败**的定义（我们用的）：**事情没发生，但没人知道。** 不是"报错了"，是"连报错都没有"。

它在我们这里的表现是三类：
1. 该跑的东西**从来没跑过**，而系统显示正常
2. 跑了但**结果丢了**，没人发现
3. 状态写着"进行中"，但**没有任何人在推进它**

**规模**：我们自己的事故库（66 条"症状 → 根因 → 规则"）里，**20 条（约 30%）属于这个形状**。
**每三条教训里就有一条是它。**

---

## 三、数据（2026-09-13 实测，都是实数）

### 3.1 事故库的构成

- 事故条目总数：**66**
- 含"静默 / 无声 / 吞掉 / 看不见 / 恒为 / 从没跑过"这类措辞的：**20 条 = 30%**

### 3.2 异常吞噬（今天量的）

数法：对每个 `except` 子句，看它**后面 5 行内**有没有 `warn` / 日志 / `raise`。
⚠️ **这是估算，不是精确值**（有的可能更靠后才记，也可能有遗漏），但量级清楚：

| 指标 | 数值 |
|---|---|
| `except` 子句总数 | **561** |
| 其中**既不出声、也不往上抛** | **约 420（74%）** |
| 分布在 | **67 个文件**（363 个在 `scheduler/`） |
| 最集中的文件 | `app.py` 41 · `orchestrator.py` 37 · `openai_agent.py` 27 · `validator.py` 16 · `_exec.py` 16 |

**注意最后一行**：连**告警模块自己**（`witness.py`，15 处）和**记忆整合器**（`_memory_consolidator.py`，15 处）也在里面。

### 3.3 告警侧的现状（重要，决定了方案可行性）

我们**已经**做过告警聚合。做它的理由是一段实测：

> 26 分钟的窗口里 **25 条告警，22 条（88%）挤在两个 key 上**。
> 常亮**不是**"最近问题多"，是**判据跟配置脱节** —— 它该出现在配置问题栏，不该淹在事故流里把真事故盖住。

⇒ **任何"多加点告警"的方案，都必须先回答"会不会把筛子糊死"。**

### 3.4 三个真实案例（都在过去 24 小时内）

**案例 A（今天发现）**：三个内部函数**压根没被 import**，一调就抛 `NameError`，
而它被 `except` 接住、变成一条普通告警。后果：**内存维护、洞察提取、因果边写入 —— 三件事从来没跑过**，系统一直显示正常。
**发现它的不是人，是一台新做的"聚合视图"**：251 条告警按 key 归并后，6 条 `name 'X' is not defined` 才浮出来。

**案例 B（今天发现）**：一份"架构约束清单"**恒为空**。我们原先以为是"被别的写点覆盖了"，
**错的** —— 真相是**有两条创建任务的路径，跑得最多的那条从来不写这个清单**。

**案例 C（今天发现）**：一个任务被标成"运行中"，之后**一个字节都没跑**：
0 日志 / 无心跳 / 无取消标记 / 无痕迹。过了 1100 秒连超时收割都没触发。
根因：**先写状态、后登记 future** —— 中间断了，任务就永远挂着，而**循环看不见它**。
更值得记的是它**为什么查不出来**：兜底逻辑只往 SSE 推事件，**不落日志、不进告警文件** ⇒ **事故连持久痕迹都没有**。

---

## 四、我们的诊断（**请质疑这一段**）

我们把静默失败分成两族：

| | 是什么 | 例子 |
|---|---|---|
| **A 族 · 机制性** | 东西丢了，但没人知道 | 派发了没接上 · 写了没落盘 · 建任务的路没写清单 |
| **B 族 · 判据性** | **判据本身就是假的** | 节流计数器挂在"重启就归零"的模块变量上（永远到不了阈值）· 字段空着（机制好的、内容空的） |

**我们由此得出的结论**（**请重点攻击这一条**）：

> 成熟方案（持久化执行 / 水位触发 / 结构化并发）**只能治 A 族**。
> B 族治不了 —— 因为 B 不是"信息丢了"，是"**判据从一开始就不成立，而没有人验过它**"。
> ⇒ 换个引擎会让人以为问题解决了，**而 B 族会原样留下来**。

---

## 五、我们列的方案 + 我们自己的可行性判断（**请逐条质疑**）

顺着上面的诊断，我们列了 7 条，并排了可行性。**这些判断是我们的，不是结论。**

| # | 方案 | 我们评的可行性 | 我们的理由 |
|---|---|---|---|
| 1 | **对账循环**：定期从盘上的状态重算"现在应该发生什么"，跟实际发生的对一遍（事件只负责叫醒，判断永远从状态重算 —— K8s controller 那种水位触发） | **高** | 已经有雏形（一条"走到没活干那一刻不该有运行中任务"的探测）；是增量改动，不碰结构。**前提：第一版只能"报"，不能"改"** —— 因为它可能其实在跑，只是没写痕迹 |
| 2 | **静默 except → 静态守卫 + 白名单**：写个静态检查扫出"既不出声也不上抛"的 except，现有的进白名单，**新加的会被拦** | **高** | 一天以内。收益不是"改干净"，是**这个形状不再新增** + 立刻拿到一份**可按文件排序**的清单 |
| 3 | **全量改 420 处**（挨个加告警） | ⚠️ 中 | **会淹告警**（见 §3.3）。得先分类，而分类要判断力，不是机械活 |
| 4 | **线程池包一层**：负责"提交 + 必须 join + 必须逐个检查每个 future 的结果" | ✅ 中高 | 效果接近"结构化并发"（孤儿不可能存在），但**不碰 async** |
| 5 | **换 async / 结构化并发**（Trio 那种） | ❌ 低 | 我们是同步链 + 线程池；换 async = 重写调用链 |
| 6 | **换持久化驱动**（Temporal 那类：每步先落事件再往下走） | ❌ 低 | 编排层重写；**而且它治不到我们最疼的两块**（模型输出不可信、判据本身是假的） |
| 7 | **架构性质清单**（把 66 条事故**反过来读**，得到"架构必须满足的性质"） | ✅ 已做完 | 见**第九节**附件。理由：事故记录绑在代码上、换个实现就带不走；**性质带得走** |

**我们的整体判断**：高可行性的两条**都不是"改代码"，是"加一台仪器"** —— 一台静态守卫、一台对账循环。

---

## 六、⚠️ 我们已知错过的方向（**这一节最重要**）

**在这个系统里，"看起来最像的原因"经常是错的。** 以下是**已经证伪**的：

| 我们（或上一轮外派）曾经认定的原因 | 真相 | 教训 |
|---|---|---|
| 约束清单为空 = **"被别的写点覆盖了"**（时序竞态） | 是"**跑得最多的那条路压根不写它**" | 假前提长出整条错的分析链 |
| 上一轮外派建议：**"在承诺字段的 5 个写点各加两行探针"** | 那个字段**生产上只有一个写点**；真因在别处 | **材料里的假前提，会把对方带偏到错误的位置** |
| 任务没有 token 记录 = **"从没发起过模型调用"** | 那是**上报口径的缺失**（被砍时恒为 0）；它**发起了** | **拿上报口径的缺失当事实，会得出相反的根因** |
| 超时有两种完全不同的形状 | **只有一种**（执行器一直干到被杀），差别只在杀之前落了多少盘 | 同一份数据，两种解释都成立时，别急着下结论 |
| 静态扫描报"0 引用" = 这个函数没人用 | 动态引用 / 延迟导入它看不见 | **"没人引用"≠"没人用"** |

**共同点**：这些都违反了同一句话 —— **"缺证据"不等于"没发生"。**

**所以请你**：
- 优先质疑**前提**，而不是顺着前提给方案。
- 如果你要从我们给的事实里推出一个结论，**请标明那是推断**，并说出"如果这个前提错了，结论会怎么变"。
- **我们材料里没写的，就是没有证据支持的** —— 别替我们补。

---

## 七、希望你产出什么（**要方案，不要判断题**）

**先说清要什么**：我们已经有了一堆判断（第五节、第九节）。**缺的不是第二意见，是能落地的方案。**
第 1–4 题是要你**给方案**；第 5–7 题是让你**检验我们的判断**。

### 必须给的（第 1–4 题）

**1. 挑 2–3 个你认为最值得做的，给出可落地的方案。** 每个方案要说清：
   - **改哪里**（**第八节**有文件路径、行号和现场代码，直接引）
   - **大致怎么改**（伪代码或关键几行的写法即可，不用整段实现）
   - **怎么验证它生效**（**必须能回答"这个改动要是失效了，谁会知道？"** —— 见第九节第 10 条）
   - **它可能引入什么新的静默失败**（拿第九节那 18 条自查一遍你提的方案）

**2. 逐条打我们的可行性判断**（第 5 节那 7 条）。哪些你认为是**错的**？
   特别是我们最看好的第 1、2 条 —— **有什么我们没想到的坑？**

**3. 如果只能做一件，做哪件？** 给出排序和理由。

**4. 有没有第四条路，我们完全没提到？** 特别是**不改运行代码**就能治的手段（静态分析、测试策略、流程约束、现成工具……）。

### 检验我们的（第 5–7 题）

**5. 我们最大的误判可能是什么？**（不是"还可能有什么问题"，是"**我们已经写下来的判断里，哪一条最可能是错的**"。）

**6. "默认怀疑"怎么落地？** 我们的核心主张是把默认从"没报错就是没事"翻成"拿不出证据就是有问题"。
   但一个已经有 561 个 `except`、96 个模块的系统，**这个翻转具体怎么走**才不会退化成"到处加告警、把告警淹掉"？（见 §3.3）

**7. 第九节那份"架构性质清单"（我们自己写的 18 条）**：请**直接批评它** —— 哪几条是废话？哪几条互相矛盾？漏了什么？

### 不要什么

- ❌ **不要"要注意 X、Y、Z"这种**（我们自己的事故库就是这么攒的，不解决问题）
- ❌ **不要"建议引入框架/中间件"这种** —— 除非你说清"为什么这个仓库自己写不了"，见第五节第 5、6 条
- ❌ **不要顺着我们给的前提往下推**（上一轮外派就是这么栽的，见第六节）

✅ 要的是：**方案 + 可判断的结论 + 说清哪句是推断、哪句是事实 + 如果那个前提错了，结论会怎么变**。

---

## 八、源码位置与现场代码（**要改就是改这里**）

> 第 4、5、6 节每条判断，下面都给出**具体 `文件:行号` + 现场代码**。
> 行号是 2026-09-13 的；代码一改行号就漂，**以函数名为准**。
> ⚠️ 这个仓库**零数据库**，所有状态都是 `.qidian/` 下的 JSON 文件（设计上就是为了能 `grep` 排查）。

### A1 静默吞噬的典型现场 —— 连"记忆整合"自己也是这么被吞的

`src/singularity/web/app.py:532-539`（调度循环处理完一波任务之后）：

```python
# MAGMA 慢通道整合
try:
    from singularity.scheduler.memory import consolidate_memory
    added = consolidate_memory()
    if added:
        _push_event("memory", f"慢通道: +{added} 条隐含因果边")
except Exception:
    pass
```

**问题**：这里抛任何异常都**没有任何痕迹**（连 SSE 都没有）。
⇒ 如果 `consolidate_memory()` 因为任何原因坏了，**你唯一能观察到的现象就是"什么都没发生"**。

### A2 案例 A 的现场 —— 三个函数没导入，被 `except` 吞成告警

`src/singularity/scheduler/_memory_consolidator.py:84-95`：

```python
# F821 守卫本该拦住，但本文件开头有**星号 import** ⇒ ruff 解析不了命名空间、直接不报
from singularity.scheduler._memory_lifecycle import auto_maintain, system2_extract
try:
    lc = auto_maintain()
    ...
except Exception as e: witness.warn('memory', f'consolidate:{e}')
try:
    s2 = system2_extract()
    ...
```

（`add_inferred_causal_edge` 同形状，在 `:107-135`；`_llm_judge_causal` 在主循环 `:121`。）

**修之前的样子**：这三处**都没有 import** ⇒ 必抛 `NameError` ⇒ 被上面的 `except` 吞成一条**普通告警**。
**后果**：内存维护 / 洞察提取 / 潜因果边写入 **从来没跑过**，而系统一直显示正常。

### A3 案例 C 的现场 —— 先改状态、后登记

`src/singularity/scheduler/orchestrator.py:106-116`：

```python
snap = snap_mod.take(t.id, repo_root=repo_root_for(t))
tracker.transition(t.id, TaskStatus.RUNNING, snapshot_id=snap.id)
# ⚠️ **从这一行往下，任务已经是 RUNNING 了。** 后面任何一步抛，都会留下一个
# **没人管的 RUNNING 任务**：没有 future ⇒ 循环看不见它 ⇒
# `ready_tasks()` 也不返回它（它是 RUNNING 不是 PENDING）⇒ 900s 收割永远够不着。
try:
    ...
```

**问题**：状态已经写盘 = 对外可见，但"谁来推进它"**还没登记**。
⇒ 中间断了，就产生一个**永远没人管的"运行中"任务**。按这个形状全仓扫过，**共 5 处**。

### A4 已有的水位判断雏形 —— 要提"对账循环"就从这个函数起

`orchestrator.py:609-635` `_warn_orphan_running()`：

```python
def _warn_orphan_running() -> None:
    """**只报不改**：走到"没 future、没 pending、没就绪任务"这一刻，**不该有 RUNNING 任务**。
    ⚠️ **只报警、不改状态** —— 自动"纠正"会把真问题抹平成假的一致。
    ⚠️ **每个任务只报一次**：这个检查每次空转都会走到，不去重会刷屏。
    """
```

**这就是第 5 节方案 1 的雏形** —— 它已经是"**从盘上重算现状**"而不是"信事件"。
差别只在于：它只在**空转那一刻**看一眼，**不是定期**看。
⇒ 你要提对账循环，**请以这个函数为起点**说怎么扩。⚠️ 里面"**只报不改**"那条是**有意的设计**，别推翻。

### A5 案例 B 的现场 —— 两条建任务的路，只有一条写清单

**写清单的那条**（`_workflow_phases.py:569`，在 `_run_execution` 里）：

```python
project.fix_round = 0
project.constraints_checklist = constraints
project.set_phase(Phase.EXECUTING, "架构确认 → 建任务进执行")
```

**真正跑的那条**（`orchestrator.py:783` `_decompose_and_create_tasks`）—— 它的 docstring 自己写着答案：

```
⚠️ **它不是"兜底"，是 GATE2 批准路径上的正常路径。** 这里原来写着"正常路径用不到它"
**这个前提是假的**（真机实测）：批准 GATE2 时只顺手启 planning，不启 executing
⇒ `_run_execution` **一次都不跑** ⇒ 建任务的是这条 ⇒ 而它**只建任务、不写清单** ⇒ 清单恒空。
```

⇒ **同一件事两个入口，必然有一个忘了做全套，而且忘了的通常是跑得最多的那个**（第九节第 14 条）。

### A6 告警入口 —— 加任何新痕迹前先看这里

- `witness.py:52` `warn(scope, msg, key="")` —— `key` 是**聚合键**，"常驻条件"按它归并
- `witness.py:98` `alert_summary()` —— 就是那台"筛子"

**加任何新痕迹前先想清楚它的 `key` 是什么**，否则你会制造一个新的常驻噪声源（§3.3）。

### A7 执行器的 turn 循环 —— "超时"为什么曾经是无声的

`_exec.py:499` `for turn in range(1, level_max + 1):` —— 这个循环**一圈表都不看**，
唯一的总时限来自外面一句硬编码的 `deadline = 900`，到点**无声收割**。
（2026-09-13 已修：循环内看表 + 单次调用封顶。**但"出事时没有痕迹"这个形状还在别处。**）

### A8 静态守卫的现成样板 —— 要提"加守卫"就评它

`tests/test_scheduler/test_no_undefined_names.py` —— 一个"**扫全仓 + 白名单 + 白名单过期检测**"的守卫：

```python
def test_no_undefined_names_in_src(): ...
def test_allowlist_has_no_stale_entries():
    assert not stale, f"这些豁免已经不再命中 F821 了，从 ALLOWED 里删掉: {stale}"
```

⚠️ **它自己有过一次盲区教训**（文件里一句 `import *` ⇒ ruff 对**整个文件**不报 F821）。
第 5 节方案 2（静默 except 的静态守卫）想复用的就是这套机制 —— **请直接评它能不能照搬，以及会不会重蹈那个盲区。**

### A9 静默 except 最集中的地方（要动手就从这几个文件起）

| 文件 | 处数（估） |
|---|---|
| `web/app.py` | 41 |
| `orchestrator.py` | 37 |
| `openai_agent.py` | 27 |
| `validator.py` / `_exec.py` | 各 16 |
| `witness.py` / `_memory_consolidator.py` | 各 15 |

⚠️ 注意 `witness.py`（**告警模块自己**）和 `_memory_consolidator.py`（**就是案例 A 那个文件**）在列。

---

## 九、附件：我们自己写的「架构性质清单」（18 条，供批判）

> 做法：把 66 条事故**反过来读**。事故记录绑在代码上、换个实现就带不走；性质带得走。
> **下面每条的括号里是它从哪些事故来的**。

**一、状态与推进**

1. **写盘的状态，必须有人负责推进它** —— 任何被持久化成"进行中"的东西，要么有明确推进者，要么有兜底回收者。查法：搜出所有"写入进行中状态"的地方，逐个问"下一跳是谁"，答不出来就是孤儿。〔案例 C〕
2. **终态必须不可逆** —— 判定完成/失败后不许被后面改回去；终态在状态机里不该有出边。
3. **顺序必须由机器保证，不靠调用方自觉** —— "A 必须在 B 之前"要写成结构约束；**注释不是约束**。
4. **「发出」和「完成」必须能分辨** —— 不许把"我发了信号"当成"事情做完了"。

**二、失败必须留痕**

5. **任何失败路径，必须留下可聚合的痕迹** —— 接住异常要么出声、要么上抛，不许静默；而且痕迹要**能被聚合看见**（孤零零一条日志等于没有）。〔案例 A〕
6. **「没有数据」和「数据说没问题」必须能分辨** —— 三态：有值 / 缺值 / 压根没发起。
7. **一个地方坏，不许连坐** —— 一个回调、一个成员抛异常，不能把无关的活儿一起饿死。
8. **「不是异常的失败」也要算失败** —— 挂起、重试预算耗尽、超时后线程还活着，这些 `except` 拦不住。
9. **告警必须带时间** —— 读的人要能分辨"已修的"和"现存的"。

**三、判据本身必须可检验**

10. **每条判据都要答得出"它什么时候会失效"** —— 答不出 = 没写完；守卫测试还要**变异验证**（把 bug 放回去，它真的会红）。
11. **守卫自己也要有"盲区检查"** —— 我们有过一条静态检查，因为文件里有 `import *` 而对**整个文件**闭嘴。"检查跑过了"≠"检查在那儿有效"。
12. **判据的输入必须真的会被写** —— 字段有值 ≠ 有人写它；契约漂了、只有一半路径在写、写进去的名字查不到，判据都会**恒假且看起来正常**。
13. **不许有单向棘轮** —— 只增不减的计数器 / 永不归零的状态，能把系统永久锁死。

**四、单源**

14. **每件事只能有一个定义处、一个入口** —— 同一份契约两份拷贝必然漂；多条路做同一件事，**必然有一条忘了做全套**，而且那条通常是**跑得最多的那条**。"没人引用"≠"没人用"。

**五、边界**

15. **不可信的东西不许进 shell / 路径 / 环境**。
16. **每个执行都要在"它该在的目录"里跑** —— 漏传工作目录 = 在最不该跑的地方跑。
17. **拿不准就往保守缩**（fail-closed）。
18. **资源要么用完清、要么有主；隔离要按进程算** —— "确定性路径 + 线程锁"不等于进程安全。

**这份清单末尾那句**（我们也想请你评）：

> **它只对"默认怀疑"的架构有效。** 如果新架构的默认还是"没报错就是没事"，
> 再照这张表检查，也只是把 66 条事故**换个代码形状重新犯一遍**。
