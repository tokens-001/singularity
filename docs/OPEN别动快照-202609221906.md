# 别动清单（已决定不做 / 没坐实 / 指针）

> **这个文件不进 hook**（`UserPromptSubmit` 只 `cat` `~/OPEN.md`）—— 所以它**不会每轮念给你听**，
> 这是有意的：里面的东西**不需要每轮看**。
>
> 🔴 **但它有一个必须遵守的用法**：**提任何"该做"的事之前，先 grep 这个文件。**
> 里面的条目全是"已经拍板不做 / 只是观察没坐实 / 只是去哪看"的 ——
> 不加这一步就会**重复提**，而重复提正是它们当初被记下来的原因。
> ```
> grep -n '<关键词>' ~/OPEN-别动.md
> ```
>
> **它是 2026-09-20 从 `OPEN.md` 拆出来的**（那天清单 41 条看着吓人，其实只有 ~17 条是要动手的活，
> 剩下 24 条是三类"不是活"的东西混在里面）。拆完 `OPEN.md` 只留**要动手的**。
>
> ⚠️ **别往这里塞真待办** —— 塞进来就等于它不会被每轮念到，也就等于**它会沉底**。
> 判断标准：**"要不要动手做点什么"** —— 要，进 `OPEN.md`；不要（不做 / 不知道真假 / 只是个索引），进这儿。

---

## 一、已拍板不做（**留着只为防我重复提**）

- [奇点] 🔴 **「任务排队等的时间算进执行预算」—— 核过了，这是有意修的，别再当 bug**
  （原条目 09-17 记的，09-20 核穿后从 `OPEN.md` 移来）
  **症状是真的**：实测 `13:30:47` 提交 ⇒ 全部 deadline `13:45:47`；`917` 排队 **8 分 17 秒**
  ⇒ 上场只剩 400s（44%）。**但根因不是"忘了扣排队时间"**：
  · **收割刀**（`orchestrator._reap_futures`）：`running_futures[fut] = (…, time.time())` 记的是
    **submit 时刻**，`+ config.TASK_DEADLINE_S` 判死；
  · **执行器**（`_exec._dispatch_budget_s`）= `ctx.deadline_at − now − TASK_WRAPUP_MARGIN_S`，
    也是从**同一把尺**倒推。
  **两把尺必须对齐，而执行器不能自己起表** —— `_dispatch_exec._run_executor` 每次 dispatch
  新建，用 `start` 起算等于每轮把预算清零 ⇒ 任务跑过 ≥2 次 dispatch 后自收尾**永远不触发**，
  人早被外面那把 900s 无声砍了（`docs/防御模式.md` §67，09-13 查明；
  `orchestrator.py` 里「死线必须在 submit 之前定」那段注释写着同一条，09-14 核外派）。
  ⇒ 若改成从"起跑"算，执行器比收割刀晚 `排队时长` 收尾，**排队超过收尾余量（90s）就被硬砍、无账**。
  🔵 **真因是并发 1**（单任务可跑 810s ⇒ 排队几分钟是常态）。要治是**提并发**（= 同时烧更多钱）
  或改总时长上限 —— 那是另一个决定，**不是改那两行**。

- [奇点] 🟡 **验收会因为上游少一个字段整段不发生**（09-18 讨论所得）
  `workflow.py` 里那句「验收跳过: 架构没产出约束清单」—— 标准里「没输入」该算**验收失败**，
  不是**验收没发生**。⚠️ 不是静默：它写了 `verification_skipped`，还当了 GATE3 的入门票。
  🔵 **09-19 拍板：先不动** —— 改它要动**门的准入语义**；按仓里「人负责决定」的规矩够用。
  窟窿表在 `docs/生产流现状.md` §四（`#4 审查不闭环`）。**别再当遗漏提。**
- [奇点] 🟡 **删项目：停任务已进代码，但两处仍是"有意不动"**（09-19）
  ✅ `4316fb5c`：`DELETE /api/projects/<id>` 现在**先取消它的非终态任务**（治的正是
  "废弃项目 ≠ 停任务 ⇒ 重启捞回来接着烧钱"那个 🔴），并把留下的任务数**报出来**
  —— ✅ **09-19 21:59 真机验到了**：删 round b 的项目 ⇒ 11 个非终态任务全转 `failed`
  （5 个 PENDING 直接转 · 6 个 RUNNING 先收标记、30 秒后在 turn 边界转），
  `alerts.jsonl` 多一条 `project_deleted_left_tasks:13`，13 个任务 JSON 一个没少。
  ✅ **任务 JSON 一个不删**（09-19 你拍的板）：它是锚的唯一线索，删了产物就变孤儿 ref。
  ✅ 项目根那 8 个 0B `.<名字>-worktrees` 空目录**已清**（`rmdir`，非空会自己拒绝）。
  ⚠️ **两处仍是有意不动**，别再当遗漏：① **项目仓不删**（那是成品本身）；
  ② **孤儿任务仍在盘上**（只是现在会被报出来，不再无声攒）。
  🔵 09-19 清的那 10 个的备份在 `~/项目/项目仓库/残留任务-备份-20260919-000002.tar.gz`（含任务↔ref 对照表）。
  ⚠️ **`conflict_held` 那条没动**：cancel 会把它转 failed，但 parked 的合并请求还在、
  `drain` 仍可能把它捞回去 —— 见下面那条。
- [奇点] 🟡 **心跳那条：原判据已作废，真缺口改成「调度器从不看停滞」**（09-19 量穿）
  ❌ **原写的替代判据（`running_futures` 非空 ∧ 线程池全空闲）实测站不住，别再照它做** ——
  真的 `ThreadPoolExecutor` 上量过：未完成 future 数 > 工人数**是正常排队**；
  "全空闲 ∧ 有排队项"只存在于 `submit()` 到工人醒来之间的**几微秒**（赌竞态）；
  工人被 `sys.exit` 带走时 future 照样被标 done。全文更正已写进 `docs/防御模式.md` §77.3。
  ⚠️ **同条 item 的前提也是错的**：它写"新鲜度上界 = 单次模型调用的 240s"，
  而心跳在 `for turn` 循环里、**一个 turn = 一整次 dispatch** ⇒ 上界是 **810s**，
  而 `STALLED_AFTER_S` = **1200s** 本来就在上面 ⇒ **"心跳陈旧"这条判据本身站得住**。
  🔵 **真缺口只剩一个**：`check_stalled()` 全仓**只有观察者调**，**调度器/编排器一次都不调**
  ⇒ 观察者聋了就没人在看。🔵 **09-19 拍板：先不动** —— 观察者自己**有活性痕迹**
  （`observer_state.json` + `/api/observer/status` 能看出它聋了），再加一个调用点
  只会多一份重复告警。**等真撞上"观察者死了没人发现"再说。**
  ✅ 告警"只报事实不报怎么办"那半边 **09-19 已修 `ded50bd0`**。
- [奇点] 🟡 **审查阵容只有一家 —— ①已做（写手摘掉了），"只剩一家"是现状不是残留**（09-19）
  ✅ 走 API 改的：`reviewing` 从 `[glm-5.3-flash, deepseek-flash]` → **`[glm-5.3-flash]`**
  （写手 `deepseek-flash` 摘掉，读盘核对过；备份 `/tmp/phase_models.bak-20260919-125430.json`）。
  ⇒ 名单不再说谎，`designated_reviewer_is_writer` 那条警告也不再每审一次报一次。
  🔴 **但审查**确实**只剩一家** —— 这是事实，不是修好了：**要多人审只能启用第三个模型**。
  🔵 **别再试"扩池"补人**：那条路已死（闸门 = 激活池 = 阵容，恒返回空，见 `_review.py` docstring）。
  ⚠️ **残留洞**：写手若换成 `glm-5.3-flash`（cascade 落到它时就会），
  名单里又只剩写手自己 ⇒ 那条警告会再响、审查池退回"全池排除写手"。
- [奇点] 🟡 **`final_status` 会把「已交付」标成 `blocked`**（09-16 查清）
  它是 `neijinglu.build_report` 算的**交付判词**，不是任务状态。**没修**：动的是报告语义，
  UI 和账本都在读，改了得真机验一轮 —— 不值得为一行字冒险。
- [奇点] 🟡 **「委员会换/加席位」先别做** —— 地基没验（P1 三臂实验只差跑）；席位视角已被 A/B 证伪。→ **详情 §3、§21**
- [奇点] 🟡 **「收编 Claude/Codex」** —— 真门槛是三个硬伤（禁工具/记账靠正则/权限拦不住）。
  🔵 09-17 查到一条路（**只读了公开文档，别当定论**）：OpenHands 用 **ACP** 当这些 CLI 的**客户端**，
  且 ACP **自带权限通道** `session/request_permission` ⇒ "权限拦不住"有标准做法；**记账它没提**。
  ⇒ **待评估，不是待做。** → **详情 §5**
- [奇点] 🟡 **Google Stitch 挂进奇点** —— 卡在「它可能只认 OAuth」。下一步是**探针不是写代码**。→ **详情 §6**
- [奇点] 🔴 **人审门的否定族还剩一个洞：「别继续了」判 approved**（**拍板：搁置**）
  🔵 **09-19 拍板：先不动** —— 代码注释和那条钉住天花板的测试里都写着同一句：
  **真正的解法是让门用「显式确认动作」（点按钮），不是把自然语言继续塞进正则**；
  往正则上加词只是往一个已知会漏的方向加。谁哪天真改好了，
  `test_已知的漏网_别继续了_仍然是批准的` 会红 —— 那时把断言改成 `rejected` 并删掉这条。
- [奇点] ⏸ **搁置**（09-12 用户拍板：质量 / 成本·速度 两项先不做）→ 前置的"分阶段算账"**一并搁置，别重复提**。
- [奇点] 🟡 **前端样式调优整体推迟**（09-20 用户拍板）—— 架构图的盒宽/标题截断长度这些
  **先不动**，后期统一优化前端时一起做。⚠️ **别再提"把图调好看点"**（那是被推迟过的）。
- [奇点] 🔴 **项目级「自动返工」—— 09-21 用户拍板：关着，代码不删**
  **用户原话**：「那就关了」。`48b8920a` 已把默认翻成关
  （`QIDIAN_AUTO_REWORK` 要显式设 `1` 才开；判据是 `!= "1"` ⇒ **任何"不是 1"的值都落到关**）。
  **为什么不删代码**（我给的建议，用户采纳）：它是**可逆的**，而"等失败原因能进提示词
  之后再打开"这条路还在 —— 那才是它本该的样子。
  🔵 **已核没有旁路**：自动那条（`orchestrator` 里那处）和兜底那条（`workflow` 里那处）
  **都走 `_auto_rework_allowed`**；`_api_projects` 里那条是**人工打回**，不受开关管
  （也不该管 —— 人按的就得执行）。开关**每次调用现读**，不存在冻住的坑。
  ⚠️ **判据没真机验**：它要跑到"审查触顶"那一步才看得出。**别把"改成关了"说成"验过了"。**
  ⚠️ **别再提"要不要把自动返工删掉"** —— 拍过板了（关着不删）。
  ⚠️ **也别拿"重跑不带信息"去否任务级的重试**：那条只对**项目级回炉**成立，
  任务级 cascade 重试经 `_dispatch_exec` 把 feedback 拼进了提示词，**是带信息的**。

- [奇点] 🔴 **B5「只重做有问题的那几个 task」（精准重置）—— 09-21 用户拍板：现在不做**
  **用户原话**：「任务失败后一直在修 bug，就没重试过，每次都是失败一大片，**没必要**」。
  我核过，**他的理由比我原来的那条硬**：失败总是一大片 ⇒ **精准 ≈ 全量**，边开不开一个样。
  🔵 数据印证：c 轮 11 个任务 **8 个失败** ⇒ 精准重试会重置 8/11，与全量无实质差别。
  🔵 这也**解释了那条边为什么一直攒不到数据**（`precise_reset_would_be` 两次都是 `[]`）——
  **不是样本退化，是现状下它必然为空**。
  ⏳ **什么时候再回来看**：失败率降到"一次死两三个"的时候 —— 那时精准重试才刚需。
  而**降失败率的钥匙不在重试策略上**，在「别一次死一片」：
  `5ae8cdb6`（没轮到 ⇒ 回 PENDING 重排，**真机没验**）正是干这个的。
  ⚠️ **别再提"要不要改成精准重试"** —— 那是被拍板推迟过的。
  ⚠️ 那条"只报不改"的记录（`precise_reset_would_be`）**没删**：成本≈0，且它空着本身
  就是"现在还轮不到它"的证据。

- [奇点] 🟡 **补 LICENSE —— 09-22 用户拍板：不做，条目已从 `OPEN.md` 删掉**
  （原条目说的是 `tokens-001/singularity` 已经是 public 仓、但根目录没有 LICENSE ⇒ 严格说只是
  「公开可见」不是「开源」。用户 09-22 原话「这个删了」。）
  ⚠️ **别再提"补个 LICENSE/开源"** —— 拍过板了。

---

## 二、没坐实（**只是观察，别当结论用**）

- [奇点] 🟡 **删掉的项目，它的 conflict_held 任务仍然解不开**（`repo_dir` 那条修完剩下的半边，09-19）
  ✅ mkdir 那个成因已修 `7bdeb34`（`_worktrees_dir()` 不再建目录）⇒ `repo_root_for()` 不再被喂出一个刚建出来的假路径。
  ⚠️ **残留**：`repo_dir()` 兜底仍返回一个**不存在**的路径，`merge.resolve_merge` 拿它仍会抛 `FileNotFoundError`。
  ⇒ 只对「父项目被删过、任务还卡着」的现场成立 —— 而 09-19 已清掉 10 个孤儿任务，**盘上现在没有这种现场了**。🔵 是「没证据」不是「已修」，**下次撞上再说**。
- [奇点] 🟡 **`cancels/` 里 6 个标记没人消费 —— 而"谁把任务转的 FAILED"对不上**（09-19 21:59 现场）
  6 个标记 mtime 21:59:24-25（`task_cancel` 的 body 格式），对应任务 21:59:54 转 FAILED、理由 `用户手动取消`。
  能写这句的只有两处：`_task_runner.finalize`（**会消费标记**，文件该没了）· `_api_tasks.task_cancel`
  的 else 分支（只在状态**非** RUNNING 时走）。两条都对不上"标记还躺在盘上"，`alerts.jsonl` 里
  也没有 `cancel_marker_unlink_failed` / `cancel_marker_unreadable`。⚠️ **没解释出来，别当结论。**
  🔵 已核**不是**定时炸弹：`task_retry` 开头 `cleanup_task_artifacts` 会删它
  （`_worktree.py` 里 `_rm(config.CANCEL_DIR / ...)`，`orchestrator` 610-615 那段注属实）。
  🔵 顺带：8 个标记**一个带 `by` 的都没有** ⇒ `68664faa` 那条路修完后**还没被走到**（不是没修对）。
  ✅ **09-20 更正（这句已过期）**：当天 **23:26:31** 就走到了一次 —— `cancels/1789829637704.json`
  = `{"by": "timeout", ...}`，任务终态 `failed`、理由 `执行超时(>900.0s)`。
  ⇒ **21:59 那个"一个都没有"只是当时的快照**，别当"这条路没通"用。
- [奇点] ⚠️ **待查：集成失败之后没重新集成，项目却进了 GATE3**（09-17，**没坐实**）
  已排除 `_merge_inflight` 没释放、`set_phase` 漏记。**别当结论用。** → **详情 防御模式 §77**
- [奇点] ⚠️ **待查：「审查砍掉的文件」有没有真记进报告**（09-17，**没坐实**）
  `_review.py` 里那句 `len(changed) > 3`（有意，同时 warn + `unverified.append`），
  但 **46 个 trace 里一条「未审查」都没有**。**没能定位是哪个任务触发的**（告警不带 task_id）。
  良性解释未排除：`_save_trace` 幂等，trace 若先落盘则追加进不去。
  ✅ **09-19 已把"没法定位"这半边去掉**：那条告警现在带 `task.id`
  （聚合键不变）。⇒ **下次谁触发过，`alerts.jsonl` 里直接能查到哪个任务**，
  再拿它去对 trace 就能判"到底记没记进报告"。
  ✅ **09-20 00:13 真机第一次抓到「带 task_id」的那条**：老轮 `1789829637702`（23:42）·
  新轮 `1789834349419`（00:13）⇒ **"没法定位"这半边验完了**。
  ⚠️ **"记没记进报告"这半边仍没坐实，而且两个样本都用不上**（09-20 12:5x 复核）：
  · 老轮 `702` 终态 `blocked`、`changed_files=[]`（**它压根没走到交付报告**）；
  · 新轮 `…419` **早跑完了，也是 `failed` + `changed_files=[]` + `unverified` 空**
    —— 同样走不到"改了 > 3 个文件"那句。
  ⇒ 要拿到样本得**等一个真改了 4 个以上文件的任务**（`f"{len(changed)-3} 个改动文件未审查
  （只审了前 3 个）"` 在 `_review.py`，落进 `validation.unverified`）。
  ⚠️ 顺带记一个口径疑点：**老轮 702 改了 0 个文件却报 `5->3`** —— 那句数字可能不是从
  `changed_files` 来的，拿到样本时**先核它数的是哪个集合**，别直接当结论。
- [奇点] 🟡 **QA 契约「间歇性」** —— 探针响过、**只证明"不是那两个候选"**，没证明是什么。→ **详情 §11**
- [奇点] 🟡 **重启把任务回收成 PENDING 后，重派撞上"worktree 已经没了"** —— **机制没坐实**，别当结论用。→ **详情 §17**

---

## 三、指针（**只是"去哪看"，不是活**）

- [奇点] **验收轮状态**：逐条的 ✅/❌/◐/— 全在 `docs/待验证清单-20260912.md`，**别在这儿抄数**。
  🔑 轮 4 的教训：**数"总数"要连组成一起数**。→ **详情 §20**

- [判例] 🔵 **微信小程序：个人主体做不了 —— 用户 09-20 拍板「先放着」**（原 🔴）
  原因：**只要跑大模型，审核就要「深度合成-AI问答」类目，而该类目个人主体拿不到** ⇒ 报什么类目都没用。

  上午我只查到"法律咨询三类目"那一层 —— **说轻了**。真正卡死的是：**只要用了大模型，
  审核就会要求补「深度合成-AI问答」类目，而该类目个人主体拿不到**：
  · 腾讯云开发文档：「目前，**个人主体小程序尚未开放深度合成相关服务类目**，建议申请企业主体类型小程序」
  · 微信云开发 CloudBase：「深度合成类目**仅向非个人主体（即企业或组织）开放**。个人主体小程序无法申请此类目」
  ⇒ **判例助手报什么类目都没用** —— 模型一跑，驳回理由就是"请补充选择：深度合成-AI问答类目"。
  ✅ **可行的替代**：① **个体工商户**（被视为非个人主体，可申请深度合成类目；线上可办执照）
  —— ⚠️ 但**还要同时过"法律类目"那关**（上次查的那条仍在），两关都要过，不确定第二关怎么判；
  ② **抖音小程序**（个人主体有「AI工具」类目，**但只能调第三方已备案算法** + 提交备案截图 + 合作协议）；
  ③ **H5 + 公众号** —— **微信官方自己给个人开发者的建议**（文档原话就是"建议改用 H5、公众号…"），
     正好是现状那条路；④ 找律所/企业主体挂靠（最正规，最贵）。
  🔵 **DeepSeek 有算法备案**（网信算备110108970550101240011号，2024-04-12，深度合成服务算法备案-服务技术支持者；
  大模型备案 Beijing-DeepseekChat-202404280016）—— ⚠️ **这个编号是从二手转载拿的，没去网信办公示页核**，
  真要提交材料前必须自己上官网核一遍。
  ⏳ **时间成本**：小程序备案约 2–22 个工作日；算法备案通常 2 个月以上（用第三方技术是否更快**未核实**）。
  ⚠️ **先别写小程序代码** —— 写码是最便宜的部分，卡死的是备案+类目+主体资格。
