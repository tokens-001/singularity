# 全量审计报告（2026-09-11 凌晨）

> **口径**：`src/` 94 个 py 文件 / 23,873 行 + 前端 `web/frontend/src`（2,781 行）。
> **方法**：9 路并行只读审计（按模块簇分工）+ 1 路独立死代码扫描 + 本人对高优先项的二次复验。
> **本次未改任何代码。**
>
> **复验标记**（读法很重要）：
> - ✅ **已复验** = 我本人读过完整调用链 / 跑过，机制确定
> - ⬜ **待复验** = 子审计员报告，我**没有**独立复验，采信前需自查
>
> 子审计员自己也犯了 4 类误判（见 §6），所以 ⬜ 不等于假，只是**未经第二双眼睛**。

---

## 一、P0 —— 已复验，机制确定

### P0-1 ✅ 审查/门禁看到的是**空 diff** —— 5 道门禁形同虚设

**哪里坏了**：`_exec.py:378` 调 `_process_planner_or_merge`，该函数在 `_exec.py:182`（v3）
/ `:190`（v2）**已经把 worktree 的改动 commit 了**；而 `validate()`（`:406`）和
`run_post_exec_checks()`（`:426`）在这**之后**才跑。

**为什么是空的**：审查取 diff 用的全是**裸 `git diff`**（无基准 ref）：
`_review.py:82` / `:376` / `:425` 是 `git diff <file>`，`validator.py:229` 是
`git diff HEAD -- <file>`，`validator.py:268/272` 是 `git diff [--stat]`。
commit 之后这些**一律 0 行**。

**影响**（按 `_review.py:84` 的判据 `line_count < 50`）：
- 单文件任务 → `_is_trivial_change` 判 trivial → **项目测试 + 多模型审查 + QA 约束验收
  + 需求符合性 + 安全审计，5 道全跳**，报告照样写 `delivered`
- 多文件任务 → `multi_model_review(diff_only=True)` 拿到空 diff → **一个模型都不调**就返回
  `{"issues":[],"models_used":[]}`；QA/安全审计拿到 `(无 diff)`
- `_hard_diff_rules` 的"移除了 N 处鉴权"检测依赖 `git diff HEAD` → 永不为真

**唯一还硬的**是 `supervisor.supervise`（机械检查，不吃 diff）。

**为什么测试没抓到**：`tests/test_scheduler/test_review_gate.py:229` 把 `subprocess.run`
monkeypatch 成返回**假 diff** —— 单测结构上不可能发现这个 bug。

**未做的验证**：我没跑端到端流水线去**观察**一道门禁在空 diff 上放行。
机制由读码确定（顺序 + 命令 + commit 三件事都单独核过），端到端**未观测**。

---

### P0-2 ✅ `witness` 幽灵名 —— **错误处理路径本身崩溃**（16 处调用 / 0 处定义）

`witness` 全仓**没有任何定义**（`grep -rE "^(def|class) witness"` = 0），却被 3 个模块
调用 16 次。危害不是"少了一条告警"，而是**本该优雅降级的 except 分支自己抛异常**，
把降级变成崩溃，**并掩盖原始错误**。

逐函数核实结果（顶层无 import、是否有函数内 import、**行序**）：

| 位置 | 函数 | 局部 import | 结论 |
|---|---|---|---|
| `_worktree.py:44` | `_anchor_ref` | 有(L43) | ✅ 正常 |
| `_worktree.py:80/103/111` | `cleanup_task_artifacts` | 有(L69/110) | ✅ 正常（`_rm` L80 是嵌套闭包，外层能捕获） |
| `_worktree.py:131` | `_maybe_create_worktree` | 有(L130) | ✅ 正常 |
| **`_worktree.py:136`** | `_maybe_create_worktree` | L130 在 try **体内** | ❌ **`UnboundLocalError`** —— 异常若发生在 L122-124（早于 L130），名字从未绑定 |
| **`_worktree.py:150`** | `_cleanup_wt` | **无** | ❌ **`NameError`**。它在 `_exec.run()` 的 `finally`（`_exec.py:507`）里调用 → worktree 留在磁盘（泄漏）+ 任务被记成"worker 异常" |
| **`pre_search.py:104`** | `pre_search` | L127 才 import | ❌ **`UnboundLocalError`** —— 函数内有 import 就使名字**整个函数作用域**变局部，L104 早于 L127 |
| `pre_search.py:128/130/177/179` | `pre_search` | 有 | ✅ 正常 |
| **`_dispatch_skills.py:85/107/152`** | 三个函数 | **无** | ❌ **`NameError`**。L85/107 把"降级 `return [],"",{}`"变成抛异常；L152 让"需审批"通知永远发不出去 |

**这条的教训**：`_worktree.py` / `pre_search.py` 里那些"有局部 import"的写法是**对的**，
说明有人知道要 import —— 只是**漏抄了几处**。依赖"错误路径"才能触发的代码，
测试覆盖率天然是 0。

---

### P0-3 ✅ 集成合并与交付操作的是**奇点自己的仓库**，不是项目仓库

`orchestrator.py:502`（`_run_integration_merge`）与 `:558`（`_run_delivery`）都写
`root = str(config.PROJECT_ROOT)`。而 `config.PROJECT_ROOT = Path(__file__).parents[3]`
= **本仓库 `projects/singularity`**。同一个文件里其他地方取的都是项目仓库
（`repo_root_for(t)`，见 `:105/:144/:183…`）。

**症状**：
- 集成阶段 `git status --porcelain` 检的是奇点的工作区 → 你正在改奇点自己时，
  项目被无理由打回 `EXECUTING`，两次后升 GATE2
- 交付阶段给**奇点仓库**打 `release/<项目id>-<时间>` 标签，把奇点的 `pyproject.toml`
  当项目产物（永远报 "python-package"）

**旁证**：`_run_delivery` 唯一出口是 `return True`（`orchestrator.py:607`），失败分支
（`:360-364` "交付失败, 需人工处理"）**是死代码** → 用户永远看到"交付完成!"。

---

### P0-4 ✅ 调用**不存在的函数** —— 3 个入口必炸

| 入口 | 证据 |
|---|---|
| `POST /api/projects/<id>/auto` | `_api_projects.py:243` 调 `proj_mod.advance_phase` —— 全仓 **0 定义**（上一轮删掉的 `advance_to` 没留替代）。子审计员实跑复现 → 500 + HTML 报错页 |
| `POST /api/conflicts/<id>/resolve` | `_api_memory.py:63` `from .merge import resolve_conflict` —— 该名全仓 **0 定义**；真实现是 `MergeQueue.resolve()`（`merge.py:207`），而它自己 **src+tests 零调用** |
| `PUT /api/token-budget` | `_api_monitor.py:58` `from ._token_budget import set_budget` —— `set_budget` 只是 `TokenBudget` 的**方法**，不是模块级函数 |

三处全仓 grep 定义数均为 **0**，机制确定。

---

### P0-5 ✅ CLI 三处 `NameError`

| 命令 | 位置 | 缺的名字 |
|---|---|---|
| `scheduler rollback <id>` | `_cli_tasks.py:130/135` | `snap_mod`（在 `__main__` 命名空间，函数取不到） |
| `scheduler memory query/traverse` | `_cli_memory.py:55/72` | `_parse_concurrent`（只在 `__main__.py` 定义） |
| `python _cli_projects.py` | `_cli_projects.py:236` | `main`（本模块**没定义**） |

`rollback` 那条只在**快照真实存在**时才走到，即"最需要它的时候"炸。

---

## 二、P0/P1 —— ⬜ 待复验（子审计员报告，我未独立复验）

### 门禁 fail-open 族（与 P0-1 同源，修了 P0-1 才轮得到这些）

| # | 位置 | 症状 |
|---|---|---|
| ⬜ | `validator.py:720` / `:649` ↔ `_review.py:431` / `:382` | **verdict 大小写归一化漏网**：只归一精确小写 `critical`/`rejected`，默认值直接是 `clean`/`accepted`。模型吐 `"Critical"` → 外层透传 → `findings=[]` → **真漏洞随代码合并**。09-10 修过 severity 大小写，这是**换了字段的同一个病** |
| ⬜ | `validator.py:148-178` | `run_project_tests`：三个 runner（pytest/unittest/npm）全超时或不存在 → 循环走完 → **返回 `passed=True`** |
| ⬜ | `_review.py:307-312` | 拿 `== "needs_fix"` 比，而 prompt 要求的枚举是 `pass|retry|abort` → **分支永不成立**，`abort`/`retry` 结论无后果 |
| ⬜ | `_review.py:184-199` / `:263-275`、`validator.py:453-459` | "超时包装"不成立：`with ThreadPoolExecutor` 退出时 `shutdown(wait=True)` 继续 join → 600s 只是"延迟判定"。子审计员实跑复现（1s timeout 却 3s 才退出） |
| ⬜ | `_review.py:374-380` / `:423-429` | diff 取不到（异常/空）→ `except: pass` → 模型看到 `(无 diff)` → 多半答 `clean` |
| ⬜ | `supervisor.py:430` / `:400-406` | 机械兜底 `has_files = any(...) if covered_by else True` → 没写 `covered_by_tasks` 的条目**直接算过**；读表失败一律 `passed=True` |
| ⬜ | `_review.py:77-87` | trivial 跳过**不写 unverified**（归档 #10 立的规矩没覆盖到这条） |

### 静默失败 / 通道断头

| # | 位置 | 症状 |
|---|---|---|
| ⬜ | `_review.py` 多处 → `_exec.py:580-599` → `neijinglu.py:97-130` | 审查/QA/安全/需求符合性的结论**只进内存 `quality`，不进 trace、不进交付报告**。修档 #10 只补了 `review_files_truncated` 一条，同类的规矩没推广 |
| ⬜ | `neijinglu.py:112-113` | `to_dict()` 对 `executor_result` 无保护解引用 → **worker 异常/超时这两条路径根本没写出 trace**（最该看现场时什么都没有）。同一函数 `:127-129` 却写了 `if`，是漏改 |
| ⬜ | `_observer_answer.py:97-113` | 观察者聊天里确认 GATE → 回"✅ 已通过"但**磁盘没动**（`load` → `confirm_gate` → 忘了 `save`）→ 假成功 |
| ⬜ | `_worktree.py:137-140` | `wt_create` 抛异常 → `except Exception: return None`，**连一条 warn 都没有** → 任务在主仓库**无沙箱裸跑**。触发路径已复现：残留 worktree 目录 → `git worktree add` rc=128 |
| ⬜ | `_git_worktree.py:252-292` | 启动清理**只扫奇点仓库**；项目 worktree 在 `<projects_root>/.<项目名>-worktrees`，**永不被清** → 攒到 50 个后该 repo 每个任务都静默无沙箱。且 `/api/cleanup` 扫的 `QIDIAN_DIR/worktrees` **恒空** → 等于没有任何清理入口 |
| ⬜ | `snapshot.py:96-111` | 回滚时 `git stash push` 的**返回码不检查** → stash 失败仍继续 `checkout -- .` + `clean -fd` → **用户未跟踪文件永久删除**。已在 /tmp 仓库实跑复现 |
| ⬜ | `snapshot.py:91-99` | 回滚把用户改动静默挪进 stash，**既不恢复也不提示**，接口返回 200「已回滚」 |
| ⬜ | `_memory_lifecycle.py:189-194` | `if not isinstance(ev, dict): continue`，但 `_load_events()` 返回的是 `EventNode` **对象** → 每条都被跳过 → DCPM System 2 **产出永远是 0**。实跑复现（64 条真实事件 → insights 恒空） |
| ⬜ | `_api_projects.py:217-222` | `project_lineage` 调 `task_templates.get(project_id)`，而 `get()` 的 `DEFAULT_TEMPLATE` **恒为真** → 永远返回通用模板，下面列任务的正确分支是死代码 |
| ⬜ | `_api_projects.py:243` | 见 P0-4 |

### 并发 / 数据

| # | 位置 | 症状 |
|---|---|---|
| ⬜ | `project.py:319-329` + `:285` | `project._LOCK` 只保护 `_next_id`，`save()` 裸奔（`tracker.py` 同类场景已加锁）。且 `tmp = p.with_suffix(".tmp")` 是**确定性路径**，两线程同时 save → 共用同一 tmp |
| ⬜ | `route_learner.py:141-156`、`_token_budget.py:31-55` | 非原子写 + 读改写无锁（`api_store` 用了 `atomic_write_json`，这两处没用）。撕裂一次 → 下次 save 把整份历史覆盖成一条 |
| ⬜ | `_memory_graph.py` / `_memory_core.py` / `_memory_lifecycle.py` | 记忆 JSON 被**调度线程 + Flask 请求线程**同时读改写，全模块 `grep Lock` = 0。`atomic_write_json` 防写坏但不防**丢更新** |
| ⬜ | `_memory_core.py:49-55` | 损坏 JSON 静默当空 → 下次写覆盖全库，无备份、无告警 |
| ⬜ | `mcp.py:303-319` | `load_configs` 清空重建注册表时无锁 → 执行中的任务撞空表得"未知 MCP 工具" |
| ⬜ | `api_store.py:249-270` | 欠费/限流标记**有去无回**：`probe()` 第一行就 `if entry.status != "active": continue` → 永远观测不到恢复。线上实例：`zhipu | quota_exhausted | notes=…http429` —— **一次 429 把整个智谱永久摘掉** |

### 其余 ⬜

| # | 位置 | 症状 |
|---|---|---|
| ⬜ | `router.py:68-77` | 路由分类**完全失效**（`task_type` 恒 `default`）。因 `load_agents()` 拿到的 `entry`/`api_key_env` 是空串（`_build_agent_from_registry` 才补得出来）。**三层叠加**：即便修好 key，下一行 URL 拼接也会 404。子审计员实跑复现 |
| ⬜ | `_dispatch_exec.py:265-280` | `_FusionResult.token_count = 0` 硬编码 → **架构阶段（委员会+融合）一条用量都不记**。归档里"第一次记下自己的用量"来自 minimal_probe，不含架构阶段 |
| ⬜ | `execution_judge.py:616-632` | 第二轮确认调用失败（空返回）→ `cur_c = {}` → 被当成"已收敛"、**所有分歧判给发言方**，且无告警 |
| ⬜ | `execution_judge.py:652-658` | "全体 adopt 才采纳"实际是"所有**表过态**者 adopt" → 与规格文档 §9.1.2 矛盾，门槛比文档松 |
| ⬜ | `execution_judge.py:44-56` | 融合路径**不查 `api_store.is_available`** → 每次先撞两次死 provider（默认提取员和第一备选都是智谱，正欠费） |
| ⬜ | `model_registry.py:103-117` | `models_custom.json` 覆盖时 `speed/reasoning/max_turns/recommended_for/cost` 被**整体替换**。实测：跑过基准的模型 `strengths` 全变成同一份标签、`recommended_for` 变 `['any']`。注：`reasoning` 目前**已无行为消费者**，属显示问题 |
| ⬜ | `_workflow_phases.py:296` | 重规划后 `task_ids` **只增不清** → GATE3 打回时把上一版已废弃的 DONE 任务**一起重置重跑** |
| ⬜ | `orchestrator.py:382-425` | 兜底建任务路径**丢掉 `depends_on`** → 架构里的 DAG 被压平，本该串行的任务同时开跑 |
| ⬜ | `web/app.py:1144-1205` | 项目文件面板**忽略 `project_id`**，列的是奇点自己的源码树；且 `git ls-files` 未关 `core.quotePath` → 中文名全是八进制转义 → 面板乱码、点开 404 |
| ⬜ | `_auth.py:18/102-149` | 鉴权一旦开启就**永久锁死**：明文 token **从不完整输出**（只有 `[:8]`）、TTL 30 天无续期入口、现有 admin 已过期 **83.2 天**。开 = 谁都进不来，关 = 完全无鉴权 |
| ⬜ | `__main__.py:29-68` | `scheduler --help` / 打错命令 → **兜底进 `_cmd_run`** → 建任务并真跑完整流水线（真烧钱）。`rollback`/`add`/`merge`/`apply` 漏参数同理 |
| ⬜ | `app.py:1117-1141/1257-1260` | 分钟级 workflow（`run_phase`/`start_project_workflow`/`handle_gate3_reject`）**同步跑在 Flask 请求线程**里 |
| ⬜ | `_io.py:77-89` + `app.py:777-795` | `save_toml` 不转义引号/换行 → TOML 写坏；GET 侧 `except: return {}` → **配置静默变空** |
| ⬜ | `_api_projects.py:135`、`app.py:965`、`_api_tasks.py:514` | 非法 `gate` 参数 → 未捕获 `ValueError` → 500；JSON 数组当 body → `AttributeError`/`TypeError` → 500（多处 handler 假设 dict） |
| ⬜ | `execution_judge.py:506-513` | 分歧票按原始 `id` 索引，模型回显 `"1"` 而提取员给 `1` → **静默丢票**（机制成立，**未观测到实例**，置信度低） |

---

## 三、死代码（✅ 已独立复验）

独立扫描器（AST + 真实引用口径，**排除**注释/字符串）在**删除前代码树**上校准：
标记 36 个 = 上一轮 36 个复验候选、**0 误报**；"仅测试引用"9 个也完全吻合。

### 真死（可删）

| 符号 | 位置 | 规模 | 复核 |
|---|---|---|---|
| `_collect_ready_pending` | `tracker.py:281` | ~14 行 | 全仓仅 2 处命中：定义行 + `tracker.py:139` **注释**。逻辑已被 `ready_tasks()` 完全覆盖 |
| `start_all` + 孤儿 `_all_started` | `bridge.py:315` / `:33` | ~35 行 | 3 处自引用全是**函数体内的日志字符串**。`web/app.py:1945/1951` 是分别直调 `start_ws_server`/`start_observer_server`，绕开了它。上一轮删掉了它的对偶 `stop_all` |
| `probe` | `api_store.py:497` | ~43 行 | Python 内 **0 引用**。裸词 grep 的 234 处命中**全是 vendored 前端资产里的 `route-probe`**（`-` 是词边界）—— 这正是"判据会撒谎"的活例 |

**边界声明**：判据是"**仓库内** 0 引用"，不等于"没人用"。`start_all`/`probe` 是公共命名，
最可能被仓外脚本按名调用。删前按 `docs/死代码清单-20260909.md` 第一节留好 revert 点。

### 仅测试引用（9 个，与上一轮**完全相同**，属"未使用的 API 面"，非死代码）
`guess_template` / `_reorder_agents_by_rank` / `fallback_for_tier` / `rank_candidates` /
`models_for_provider` / `sample_current_state` / `architecture_redo` / `get_success_rate` / `get_stats`

### 待业务确认（勿照单删）
- **`blog/` 子包整体** → 判定**废弃**：实跑 `import singularity.blog` 双断裂
  （第 7 行缺 `flask_sqlalchemy`；第 31 行 `from singularity.blog import models` 而
  `blog/` 下根本没有 `models.py`）。`.flaskenv` 声明它是 `FLASK_APP`，但真实入口是
  `python -m singularity.web.app`
- **`observer/mcp_server.py`** → **不是死代码，勿删**：外部 MCP 客户端经 stdio 拉起，
  `observer_agent.py:17` 专门为它再导出两个下划线名
- **`_memory_experience` / `_memory_lifecycle` 的统计函数** → public facade（`__all__` +
  `memory.py` 的 `import *`），`_cli_memory.py:22` 真调用

### 其他死分支
`_run_delivery` 的失败分支（`orchestrator.py:607` 唯一出口是 `True`）、
`_ESCALATION = {}` 空表（`dispatcher.py:21`）→ `escalate()` 恒返回 None，
`:365`/`:491` 的 `fallback: A→B` 告警**在撒谎**（B 从没被试过）、
`_task_runner.py:250` 的"重试耗尽→自动拆分"（`retry_count` 全仓只被 `recover()` 写）、
`_cache.py` 整块 TTLStore（0 调用）、`codegraph.py:125` 的 `call_graph`（`visit_Call` 是 `pass`）、
`router.py` 的 `_CLASSIFY_CACHE`（提前 return 跳过写缓存）。

---

## 四、明确判定"**不是**问题"的（避免下轮重复查）

- `merge_tree_probe` 的冲突解析：**已修对了** —— 实跑 git 2.50.1 确认根目录无扩展名文件
  （Makefile/Dockerfile）正确进列表
- `drain()` 死循环：逐 case 走查（含全 defer、交错 defer/成功）均终止
- `_do_merge`：脏工作区 fail-closed、冲突/非零 rc 都 `merge --abort`
- **中文路径**：实测 `git worktree list --porcelain` 在 `core.quotePath=true` 下也**不转义**
  中文 → `cleanup_orphans` 的判断对中文路径成立
- `witness.heartbeat` 参数错位：修复归档 #10 那 4 处**已清干净**
- `_model_breaker`：**没有**永久熔断/永不恢复
- **前端 XSS：无**。全量 grep `dangerouslySetInnerHTML|innerHTML|eval(|new Function` = 0 命中
- **前端硬编码地址：无**。`BASE=''` 全走同源相对路径
- `observer/` **没有采样线程**，也没有采样循环 → "线程泄漏"这一项不存在
- 鉴权绕过：静态/SPA/SSE/`_api.py` 聚合路由**都没找到绕过路径**
- 路径穿越：项目名/task id/文件名三个入口在 Flask 转换器 + 校验组合下**堵住了**

---

## 五、前端（⬜ 全部待复验）

| # | 位置 | 症状 |
|---|---|---|
| ⬜ | `lib/api.ts:77` ⟷ `web/app.py:1714` | **技能勾选实际把该模型技能清空**：前端发 `{skills}`，后端读 `skill_names` → 永远取到 `[]` → 覆盖成空数组。标签高亮但刷新即失效 |
| ⬜ | `pages/Chat.tsx:138-146` + `:37-40` | 未选项目时发的第一条消息**立刻从屏幕消失**；通用对话每次进页被清空 |
| ⬜ | `components/FilePanel.tsx:80-90` | 读文件失败（403/404）显示成「**(空文件)**」—— 后端把错误也放在合法 JSON 里 |
| ⬜ | `components/FilePanel.tsx:26-35` | 列表/diff 出错静默显示「暂无文件」「无变更记录」，`error` 字段**全代码零引用** |
| ⬜ | `pages/Tasks.tsx:28-40` | 任务运行期间 SSE 每次触发都 `setLoading(true)` → 列表**反复整表闪骨架屏** |
| ⬜ | `pages/Chat.tsx:78-88` | traceCache 写占位后失败不清理 → 一次瞬时失败 = 该任务**终生不再拉取**产物 |
| ⬜ | `lib/useSSE.ts:14-41` | 单例连接进入 CLOSED 后**永不重建** → 静默退化成 10s 轮询，用户只觉得"变慢了" |
| ⬜ | 多处 | 竞态：快速切项目旧响应覆盖新列表（`Chat.tsx:67-91`）、展开错配（`Projects.tsx:60-64`） |
| ⬜ | `lib/api.ts` 15 个封装 | 零调用；其中 `startLoop`/`stopLoop` 零引用，但侧边栏在显示"调度运行中/已停" —— **有状态显示，没有启停入口** |

---

## 六、方法教训（本轮最值钱的部分）

1. **我自己的检查器第一版就错了**：用 `ast.walk` 把函数内局部绑定当成模块级，
   于是 `pre_search.py:witness`（局部 import）和 `_api_memory.py:resolve_conflict`
   （那行坏 import 本身）全被误判成 ✅。改成"只下潜模块顶层"+ 自检已知名后才发现。
   —— **"自己写的工具也要独立重算"，这个仓库第 4 次踩同一个坑。**
2. **裸词 grep 对公共命名完全不可信**：`probe` 裸词 234 处命中，
   真实引用 **0** —— 全是 vendored JS 里的 `route-probe`（`-` 是词边界）。
3. **`except` 分支里的名字最容易死**：`witness` 那 6 处全在错误路径。
   函数内 `import` 会让名字**整个作用域**变局部 —— 所以"漏抄一处 import"的后果
   不是 NameError 而是 `UnboundLocalError`，且只在异常路径暴露。
4. **单测 monkeypatch 掉的东西，正是 bug 藏身处**：`test_review_gate.py:229`
   伪造了 `subprocess.run` 的 diff 返回值 —— P0-1 在那里结构上不可能被发现。
5. **同一份文档里的事实会互相矛盾**：`修复归档-20260910.md` 未决 #4 仍写
   "reviewer 扩池没真机跑过"，而 #14 已划掉标"已验证"（同一条）；
   §"下次从这里接" item 3 说未决 9/10 没查根因，而正文 #9/#10 都标了已修。

---

## 七、建议的处理顺序（**推测，未实测效果**）

1. **P0-1 空 diff** —— 一个改动决定 5 道门禁是真的还是装饰。修法要先有测量：
   把 diff 基准改成 `snap.ref`（快照 ref）而不是裸 `git diff`
2. **P0-2 witness 幽灵名** —— 6 处改成统一顶层 import，风险低、收益立刻（错误路径不再炸）
3. **P0-3 集成/交付错仓库** —— 把 `config.PROJECT_ROOT` 换成 `repo_dir(proj.id)`
4. **P0-4 三个 500 接口** —— 要么接回实现，要么下掉入口
5. 死代码 3 个 + `blog/`（**`mcp_server.py` 别动**）
6. ⬜ 那批统一收口：verdict 归一化（照抄 09-10 severity 的修法）、`quality` 进 trace
