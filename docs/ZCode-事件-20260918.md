# ZCode 事件（2026-09-18 曝光 / 09-19 卸载收尾）

> **来历**：这一段原来是 `~/OPEN.md` 上的一条待办。**2026-09-19 用户拍板「key 不管了」**
> ⇒ 待办作废、整行从清单删掉，内容原样搬到这里存档（清单那条规矩：详细分析不写清单里）。
> 🔴 **搬的是记录，不是结论** —— 下面每一条的「已核实 / 查不到」边界都照原样保留，
> 尤其「查不出传没传过」那句**仍然是当前状态**，别当成"已经排除了"。

## 一句话

ZCode（智谱的桌面 AI 编程工具）默认开启「代码库索引 / Repo Wiki」，**UI 开关关不掉、
隐私政策没披露**，包里含**完整 git 历史 / LFS / reflog / 全局配置**（`.git` 占 86.6%）。
智谱 09-18 已道歉。**本机影响面**：它的工具调用里有 `Edit`×60 / `Write`×18，路径是
`/Users/jingzhe/projects/singularity/...` ⇒ **它真在这个仓库里读写过**（核实过，不是猜的）。

## 09-19 卸载收尾（已做完）

✅ **已卸干净**（8 项 / 约 1.5G）：`/Applications/ZCode.app` · `~/.zcode`(344M，含它自己的
session/artifacts/**Computer Use app**) · `~/Library/{Application Support/ZCode, Caches/dev.zcode.app,
Caches/@zcodedesktop-updater(445M), HTTPStorages/dev.zcode.app, Preferences/dev.zcode.app.plist}` ·
`~/ZCodeProject`。复查过：无进程、无 LaunchAgent、shell 配置无残留。

删前核过两条**不丢东西**：ZCode 产出的报告在 `~/Desktop/ZCode审阅/`(30 份)**和**仓库
`docs/*-20260914.md`（已进 git）两处都有；`mano-cua` 是 Homebrew 装的（`mano-cua 1.1.4`），
与 ZCode 的 computer-use 无关。`~/Desktop/ZCode审阅/` **保留**（那是用户的成果，不是残留）。

## 🔴 日志证据：没查到，而且**查不了了**

卸载前扫过 `v2/logs` + `cli/log`：`upload` **0 命中**、`snapshot`/`oss` 命中都无关、
`checkpoint` 只有 `[rpc:register] channel "git-checkpoint"`（机制武装着，没记传了什么）。
`checkpoints/` 里**只有一个工作区**，且是**别的**目录（`~/ZCodeProject`，空、`failureCount:1`）。

⚠️ **但这不能当"没传"的证据**：包上传成功后本地删；打包按工作区做，而我**没弄清
"工作区"和"它改过哪些文件"怎么对应**；关键词扫不到 ≠ 没有（`docs/防御模式.md` §77.3
刚栽过同形状）。

⚠️ **教训（AI 的错）**：删 `~/.zcode` 前核了"内容是否别处也有"，**没核"它是不是某个
还没结的问题的证据"** —— 用户正在问这件事，却把材料清了，且无 TM/快照可捞。

## 风险面（当时核过的）

- ✅ **git 历史是干净的**（本机核实）：全 ref 搜 `DEEPSEEK_API_KEY=` / `sk-` **零命中**，
  `.env` **从未被提交过**（`git log --all --name-only -- .env` 空）；仓库也没有 `.git/lfs`。
- ⇒ **风险只在"打包工作区"这条路上**，敏感面 = 磁盘上这几样（都被 gitignore，
  **会不会进包没有依据**）：`.env`(4K, 5 个 key) · `.qidian/`(6.2M 项目数据) · `.venv/`(1.4G)。
- 🔵 `~/.zcode/v2/credentials.json` 是 ZCode 自己的凭据（**已随 `~/.zcode` 删掉**）。

## 09-19 拍板

**key 不换**（用户定的：卸载只是止损，不再产生新包；传出去的换不回来，换与不换由他定）。
🔵 「要不要继续用 ZCode」也一并作废（工具已卸载，且它已暴露"能读文件就会传走"这条）。
