# 修复归档（2026-09-10 深夜轮）

> 一轮集中的 bug 清扫 + v2 转正。**本文的重点不是"修了什么"，而是"哪些还没验"** ——
> 这一轮最大的教训就是"读到了 ≠ 生效了""没测过 ≠ 没事"。
>
> 起点：查「提取员议程设置权」→ 顺着静默失败模式一路扫到 merge / worktree / tracker。
> 测试数 420 → **487**。第二段（09-11 凌晨）见下方「补记」。

## 补记（2026-09-11 凌晨）—— 一条真流水线探针带来的三件事

跑通了 `tests/integration/pipeline_probe.py`（复杂多文件任务：3 文件的 todo CLI，auto 模式）：
**终态 `done`，2301 秒，7 个子任务全部失败**。

失败原因各不相同，而且**是该失败的** —— 门禁把垃圾挡在了主仓库外面：

| × | 原因 |
|---|---|
| 2 | 网络错误 `[Errno 61] Connection refused` → 模型空输出 |
| 2 | QA 拦下：`[laziness]` 偷懒信号 + 测试没过 |
| 2 | 输出 < 80 字符（escalation 耗尽） |
| 1 | QA 拦下：`[completeness]` 无文件改动 |

⚠️ 探针跑的是**旧代码**（探针 23:30 启动，相关修复 23:55 才提交），所以它只描述旧版本行为。

### 顺带揪出的三个真 bug（都已修，测试实测在旧代码上红）

| # | bug | 影响 |
|---|---|---|
| 16 | **`route.level` 字段根本不存在**，4 处读它 | 两档制后 level 已主动废弃（见 `pre_search.apply_escalation` 注释），`RouteResult` 没有这个字段是对的。每任务必抛 AttributeError 被 except 吞掉 —— `_task_runner` 那两处在 try 块**开头**，异常把同一个 try 里的 `record_tokens` / `learner.record` 一并跳过 → **用量统计 / 路由学习 / 经验归档三个子系统长期静默失效**；`_exec` 那处让 `mem.update_attrs` 从没执行 |
| 17 | **`validation=None` → 失败任务写不出 trace** | `BatchOutput.validation` 默认 None（`_types.py:39`），orchestrator 在 worker 异常 / 超时路径确实传 None；`build_report` 读 `validation.action` 抛 AttributeError 被吞 → **任务失败时反而没有 trace**，最需要看现场的时候什么都没有 |
| 18 | **测试往生产 `.qidian/alerts.jsonl` 写数据** | 整套测试追加 12 行；该文件 **869 条里 802 条（92%）是测试噪声** —— 而它正是拿来查故障的 |

#18 的修法值得记一笔：**不是加清理，是断源。** `tests/conftest.py` 加 autouse 夹具把
`config.QIDIAN_DIR` 指到临时目录（绝大多数模块是**调用时取属性**，能被覆盖），
并删掉 `tests/test_scheduler/conftest.py` —— 那个「事后清理」夹具在此之前已被证明**完全空转**。
**治症状的代码，在病因修掉之后应该一起删掉。**

新增 8 条守卫测试（`test_isolation.py` / `test_route_level.py` / `test_neijinglu.py`），
全部实测**在旧代码上红、且红因正确**；#17 那条的报错跟生产告警一字不差。

### 文档漂移修正（只改核实过的）

| 文件 | 改了什么 | 核实方式 |
|---|---|---|
| `项目速查.md` | `cd ~/奇点` → 真实路径 | `ls ~/奇点` 不存在 |
| `项目速查.md` | 276 测试 → 473（全量 487） | 实跑 pytest |
| `项目速查.md` | 16 角色 → 10 角色 + 真名 | roles.toml 实际内容 |
| `现状结构图.md` | 加过期横幅（**不重写**） | 那是 09-07 快照，当时确实 16 个 |

**没动的**：`6阶段`、`4角色多模型碰撞`、`6个技能` —— 没核到依据，不编。

## 一、改了什么默认 / 契约（不是纯 bug 修）

| 改动 | 性质 | 验证状态 |
|---|---|---|
| **v2 融合转正**（默认开，`QIDIAN_FUSION_V2=0` 回退） | 架构阶段的融合机制换默认 | ✅ 能跑通（真项目实测）；❌ **没和旧两阶段比过** |
| **委员会产物交接：全局文件 → 内存** | 去掉 `QIDIAN_DIR/.last_fusion.json` 全局可变文件，改挂 `DispatchResult.executor_result.fusion_meta` | ✅ 单测锁住契约 |
| **reviewer 扩池** | 审查者来源从「启用的 agent」扩到**模型注册表**——会调用未启用的模型（花钱） | ✅ 单测 + 变异验证；❌ 没真机跑过 |
| **Task 新增 `route_role` 字段** | **激活了一个从诞生就休眠的功能**（角色提示词注入） | ✅ 单测锁住；❌ 没跑过真任务看效果 |
| **tracker kwarg 契约**：未知键从静默丢弃 → `warn` | 契约变更，以后写错字段会被看见 | ✅ 单测 |

## 二、修的 bug（按严重度）

### P0 / 高

| # | bug | 影响 | 怎么发现的 |
|---|---|---|---|
| 1 | **`MergeQueue.drain()` 死循环** | 挂在**调度主循环第⑥步**——卡住 = 整个调度停摆 | 读码 + 线程守护实测复现 |
| 2 | **审查门禁 severity 大小写 fail-open** | 模型吐 `"Critical"` → 匹配不上 → 不拦 → **真漏洞随代码合并** | 端到端复现（旧代码上带 `"Critical"` 确实放行） |
| 3 | **`route_role` 字段根本不存在** | 写入被 `hasattr` 静默丢弃、读取永远拿到 `""` → **角色提示词注入从 2026-06-27 提交起就是死的**（约两个半月） | `git log -S route_role -- tracker.py` = 0 次 |
| 4 | **`_observer_tools` 锁外 read-modify-write** | 把调度刚 CAS 出的 `ROUTED` 覆盖回 `pending` → **任务可能被重复派发** | 确定性复现 |
| 5 | **`merge_tree_probe` 解析冲突文件名** | ① 根目录无扩展名的冲突文件（Makefile/Dockerfile）被漏 → 若是唯一冲突则任务判 **failed 终态**（不是 parking）② `"Auto-merging xxx.md"` 被当成文件名 | 真 git 仓库实测 |

### 中

| # | bug | 影响 |
|---|---|---|
| 6 | **`concurrent.futures.TimeoutExpired` 不存在**（Python 3.14） | 超时分支是**死代码**：求值异常类抛 AttributeError 被外层吞成 `test_error`，分支里的 `return`（超时后不再跑更贵的步骤）失效 |
| 7 | **`_debate` 空转**（N=2 且一个慢模型） | 辩论照跑照烧钱，产出为零、零告警 |
| 8 | **`.last_fusion.json` 并发串项目** | 两个架构任务互相覆盖 → A 项目读到 B 的委员会产物（**未观测到实例，靠可达性推导**） |
| 9 | **辩论/融合降级 3 处 `except: pass`** | 失败静默降级，外面只看得到"跑完了" |
| 10 | **`witness.heartbeat` 第二参数塞消息**（4 处漏网） | 消息被存成伪 level、status 仍 running，任务终态时文件又被清理逻辑删 = 信息丢失；`_worktree` 那处掩盖了**"worktree 达上限 → 任务无沙箱跑"** |
| 11 | **QA `status` 大小写** | `"Fail"` 匹配不上 → 约束不达标却放行 |
| 12 | **`_check_artifact` 拿空路径调 ruff** | `ruff check --select=E,F` 不带路径 = 扫整个仓库 → 别人代码的违规把本任务判硬失败（**本机/生产都没装 ruff，此路径当前是死的**） |
| 13 | **`transition`/`cas` 未知 kwarg 静默丢弃** | 系统性问题 —— #3 就是它藏出来的 |
| 14 | 存委员会产物失败静默 / `project.delete` 清关联文件失败静默 | 前端空白/磁盘留孤儿，都查不出原因 |
| 15 | **`_is_slow_model` 的 `or e.reasoning`** | 字段语义是"响应怎么解析"不是"跑得慢"；删它零行为变化，拆掉一个地雷 |

## 三、测试卫生

- **测试在往生产数据目录写**：`test_project.py` 没隔离 `QIDIAN_DIR`，每次跑都真建项目写进
  `.qidian/projects/`，而 teardown 只 unlink 状态文件不删 sidecar → 攒了 **199 组孤儿**。
  已隔离。自查手法：`find .qidian -type f | sort` 快照 → 跑测试 → `comm -13` 看新增。
- **两条"假绿"测试**（我自己写的，自查时抓到）：只断言证据文本的话，本机没装 ruff 时旧代码
  走 `FileNotFoundError` 分支恰好也含 "skipped" → 测试在旧代码上照样绿。
  同理 `test_merge.py` 的死循环测试原本因 `M.witness` 不存在而红，**不是因死循环**。

## 四、未决清单（**别把"没测"当"没事"**）

1. **v2 vs 旧两阶段没跑过头对头** ← 转正带来的最大缺口。转正换掉的是现役默认，
   风险方向是"v2 成功了但更差"，自动回退救不了。
   **要答它**：同一个 brief 切 `QIDIAN_FUSION_V2=0/1` 各跑一次 + 双向配对判分（`pairwise_run.py`）。
2. **v2 架构阶段耗时 878 秒**（3 模型，真项目实测）。对比 2 模型 561s。**每次走架构都付这个成本。**
   不管 v2 留不留都得付 —— 比第 1 条更容易验证也更容易改善，建议先查这个。
3. **提取员召回约 90%**（3 题 29 条真分歧，默认提取员 26/29）。约 10% 的真分歧永远进不了讨论。
   **决定不加第二个提取员** —— 理由是"漏了会伤产物"没证据，且查到反证
   （`_V2_FINALIZE` 本来就带成员原稿全文，定稿人看得见原稿）。
4. **reviewer 扩池没真机跑过** —— 只过了单测。
5. **`route_role` 激活后的实际效果没测** —— 角色提示词从没注入过，现在会注入了，
   行为变了但没看过输出差异。
6. **199 组孤儿文件没删**（逻辑上不可达，但是用户数据）。清理命令见记忆。
7. **发现但没改**：`qa_engineer` 与 `security_auditor` 共用 `judge` 人格，而该人格的
   style_prompt 是「四维检查(全部机械执行，**不调 LLM**)」—— 那是 supervisor 的机械检查描述，
   被拼到两个**本来就是 LLM 角色**的提示词前面。
   **没改的理由**：提示词内容质量问题，改法不唯一，且没有任何测量支撑哪个更好。
8. **文档漂移**：`项目速查.md` 已修；`现状速写.md` **还没修**（仍写「16 角色」「276 测试」），
   `现状结构图.md` 加了过期横幅但正文未重写。现 `roles.toml` 是 **10 个角色**、测试 **487**。
9. **项目 phase 到 `done`，但 7 个任务全失败**（09-11 探针实测）—— 项目级终态没反映任务级失败，
   用户看到 "done" 会以为成功了。**没查明根因**，别瞎猜着改。
10. **`review_files_truncated:4->3`**（审查者只看到 4 个文件里的 3 个，探针里 7 次）——
    又一处静默降级。**没追**。
11. `_memory_core._MEMORY_DIR` / `config.SNAPSHOT_DIR` 等是**导入时**固定路径，
    测试的 `QIDIAN_DIR` 隔离覆盖不到它们。本套测试目前没走到，走到了就是"测试写真实目录"。

## 五、方法教训（这轮最值钱的部分）

- **读到了 ≠ 生效了**。`route_role` 那条：写侧、读侧代码都"正常"，字段却不存在；
  09-07 那份诊断图甚至写着"route_role 只塞 prompt"——它读到了那个动作，没验证是否生效。
- **判据会撒谎，要看原始产物**。v2 探针第一轮报"疑似截断"，是我判据写成 `endswith("}")`，
  而 v2 产出带 ``` 围栏。**同一课在这个仓库已经踩过至少三次。**
- **测试必须在旧代码上因"这个 bug"变红**，不是因别的原因（AttributeError / 环境差异）。
- **自己写的工具也要独立重算**。本轮新写的 import 图生成器**错了三次**（包解析成 `__init__`、
  `executors` 路径判断、`from pkg.mod import sym` 从退回退），每次都是靠手工重算抓出来的。
- **静默失败仍是这个仓库的主要 bug 模式**。本轮又抓到 6 处，其中 4 处是 09-10 那轮
  "修过"的漏网（`route_role` 那条甚至只补了 warn 没修根因 —— 治了症状没治病）。

## 六、本轮新增的工具

| 文件 | 用途 |
|---|---|
| `tests/integration/extract_recall.py` | 提取员召回率实测（多模型同一批稿子 + 裁判对齐） |
| `tests/integration/v2_default_probe.py` | v2 转正真机验证（默认路径是否真走 v2） |
| `tests/integration/pipeline_probe.py` | 流水线真机复现（复杂多文件任务会不会不收敛） |
| `tests/integration/arch_graph.py` + `arch_svg.py` | 从 import 关系生成模块耦合矩阵（边来自 ast，分组是人工规则） |
| `docs/archify/singularity-flow.html` | 九步流程说明图（给人看的，非技术） |
| `docs/archify/singularity-imports.html` | 模块耦合矩阵 |

## 下次从这里接

1. **查 v2 架构阶段 878 秒**（比"v2 是否更好"更容易验证，且无论如何都要付）
   —— 计时埋点已加好：`v2_fusion_run.py` 的 spy 现在记**每次调用的耗时**，
   跑一次 `AB_SKIP_SCORE=1 .venv/bin/python tests/integration/v2_fusion_run.py 3` 就有分步明细。
   已知：单次提取调用实测 **> 6 分钟**（大 prompt + 思考模型）—— 慢在单次调用本身。
2. **v2 vs 旧两阶段配对比较**（切 `QIDIAN_FUSION_V2` 各跑一臂）
3. 未决清单 **9 / 10** 两条（探针新发现，都没查根因）
4. 清 `tests/integration/.extract_recall_cache.json` 之外的杂项；`poc/` 是探针调研阶段的产物，可删
