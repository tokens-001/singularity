# Qoder CN 外派审查 —— 方案与第一步（2026-09-20）

> 来历：`~/OPEN.md` 那条。**这是一份可执行的方案，不是待办** ——
> 待办在清单里只占一行，只指到这儿。

## 是什么

免费 Qwen3.8-Flash 的外部审查，**9/30 23:59 截止**。
**只审不改**（外派的那一方不动代码）· 报告落 `docs/` 进 git ·
**和 `docs/*-20260914.md`（ZCode 那批）配对比对** —— **两家都报的 ≈ 真问题**。

## 已经准备的

- 已建 `.tongyiignore`，排除 `.git/` `docs/` `.qidian/`
- ⚠️ **没实测它认不认这个文件** —— 这类工具"支持某配置文件"经常只是文档上说

## 🔴 第一步是「标定」，不是「跑一遍看它报什么」

**拿 ZCode 已经找到的缺陷当答案**（那批的结论在 `docs/*-20260914.md`），
看 Qwen 能不能**独立复现**出来。

**为什么必须先标定**：量不出它的能力，就**不知道该不该在 10/1 之后继续用它** ——
而"它报了一堆东西"本身不说明任何事（报得多不等于报得准，也不等于报得全）。
这正是本仓那条：**先造尺子，再量东西**。

## 与别的事的关系

- 它和 [[outsourcing-to-a-chat-model]] 那条是同一族：**外派给任何能读工作区的对话式模型**，
  硬约束得靠文件摆，自报的独立性不能信
- ⚠️ **和 ZCode 的事件无关**：ZCode 是**已卸载**的那个（静默打包上传工作区）；
  Qoder CN 是本地 IDE，但**同样要问"它会不会把工作区传走"** ——
  `.tongyiignore` 就是为这个建的，而它**还没被实测过**
