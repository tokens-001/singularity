# Issue Tracker

- **Type:** Local markdown
- **Location:** `.scratch/<feature-name>/issues.md`
- **Workflow:** Issues are markdown files under `.scratch/`. Each feature or workstream gets its own directory with an `issues.md` tracking open/closed items.
- **PRs as request surface:** No (solo repo, no external PRs).
