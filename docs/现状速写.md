# 奇点现状速写（2026-09）

> 这是当前权威的现状说明，替代已过时的 `ARCHITECTURE.md`（6-20 写成，仍讲「三层模型 / 55 文件 / 19 技能」）。
> 历史演进见 `演化史.md`；架构蓝图细节见 `专家团队架构.md`。

## 一句话

AI Agent 调度平台：多模型按角色协作，把自然语言需求走完六阶段软件开发流水线，交付可运行的成品项目。**自部署、出厂零模型、数据落本地 JSON。**

## 规模

- 88 个 py 文件，约 2.2 万行
- 276 测试全绿（`pytest tests/test_scheduler/ -q`）
- 前端 Flask + React，知识引擎 sentence-transformers + chromadb

## 两大引擎

| 引擎 | 位置 | 职责 |
|------|------|------|
| 调度引擎 | `src/singularity/scheduler/`（60+ 文件） | 六阶段流水线 + 16 角色 + 能力评级推荐制 + GATE 审查 + worktree 隔离 |
| 知识引擎 | `data/knowledge/` | 句向量 + ChromaDB 语义搜索，任务进来先 pre_search 查可借鉴方案 |

## 六阶段流水线

```
定义(G1) → 架构(G2) → 实现 → 集成 → 审查(G3) → 验收 → 交付
```

每道 GATE 有门禁审查，不通过打回重做。

## 角色（16）+ 人格（7）+ 技能（6）

**角色**：researcher 研究员 · system_architect 系统架构师 · ai_architect AI架构师 · frontend_architect 前端架构师 · implementer 执行者 · builder 构建者 · frontend_engineer 前端工程师 · backend_engineer 后端工程师 · data_engineer 数据工程师 · ai_engineer AI工程师 · devops_engineer DevOps工程师 · debugger 调试者 · supervisor 监督者(机械质检) · reviewer 审查者(主观) · qa_engineer QA工程师 · security_auditor 安全审计师

**人格**：scholar / strategist / engineer / senior_engineer / detective / judge / inspector

**技能**（方法型，可叠加）：code-review / ddd / ponytail / codegraph / creative-brainstorm / observer

## 模型能力快照 + 推荐制

- `models.toml` 是能力快照：每个模型带 rating（SSS+~A 八档评级）+ recommended_for（推荐用途）+ strengths + cost + speed
- 调度按 recommended_for **权威推荐 + 用户自定义，不强制分层路由**（代码原文）
- 4 个关键角色（系统架构 / AI架构 / 安全 / 研究员）多模型碰撞（3 模型委员会）

## 核心数据流

```
用户 → Observer 对话 → 路由分类 → 六阶段流水线
     → dispatcher 分发 → executor 调模型 → validator GATE 审查
     → merge 合并 → 成品存 ~/qidian-projects/<项目名>
```

## 关键设计决策（现状）

1. **文件持久化零数据库**：`.qidian/` 下全 JSON，可 grep 排查。
2. **worktree 隔离**：每任务独立 git worktree，类 Docker 效果，git 原生零依赖。
3. **出厂零模型**：`models.toml`（能力快照，供调度器）vs `models_custom.json`（模型库，前端显示）。用户选厂家 → 输 key → 扫描导入。
4. **自部署**：Dockerfile + docker-compose，key 明文存项目 `.env`。
5. **执行器**：openai_agent / anthropic_api / claude_cli / zhipu_api 四个 runtime。

## 技术债（诚实标注）

- **文档过时**：ARCHITECTURE.md 仍写「三层模型 / 55 文件 / 19 技能」；README 的「19 个角色技能包」也是旧的。
- **体积偏胖**：88 文件 2.2 万行对个人项目偏大，scheduler 单目录 60+ 文件，有边做边堆的成分。
- **未实战验证**：276 测试是自测基线，真实用户 / 真实复杂任务未经过验证。

## 恢复运行

```bash
pip install -e .
python3 -m singularity.web.app      # 或 docker compose up -d --build
```
