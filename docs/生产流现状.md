# 生产流 · 现状

> **这份写的是代码实际在干什么**，不是设计意图。
> 设计意图看 `production-flow.md`（2026-06-24 / v3，**已落后三个大版本，只当历史读**）。
> 时间：2026-09-11 核对源码。

---

## 一、实际流转图

```
TEMPLATE
  │  立项：填 description；_needs_research() 决定要不要调研
  ▼
RESEARCHING ──(可跳过)──┐
  │  调研员出报告        │
  ▼                     │
GATE1 ◄─────────────────┘      人审        打回 → TEMPLATE（重写需求）
  │
  ▼
PLANNING
  │  架构师出方案 + 任务清单
  │  ★ 架构类任务 → 多模型委员会（见 §三）
  ▼
GATE2                          人审        打回 → RESEARCHING（回调研）
  │
  ▼
EXECUTING
  │  拆任务 → tracker → orchestrator 逐个执行
  ▼
INTEGRATING                    非人门
  │  多路 worktree 合并 + 集成测试
  │  失败 ≤2 次 → 回 EXECUTING；触顶 → GATE2
  ▼
REVIEWING                      非人门
  │  跑验收：QA 报告 + 安全审计 + E2E（★ 只报不改，见 §四专条）
  │  ⚠ 架构没产出 constraints 时整段跳过
  ▼
GATE3                          人审        按 fix_route 分级打回，见下
  │
  ▼
DELIVERING                     非人门
  │  打 tag + 收 README/打包配置 + 归档报告 → 写 delivery_manifest.json
  ▼
DONE
```

**GATE3 打回是分级的**（`handle_gate3_reject`）：

| fix_route | 回哪 | 说明 |
|---|---|---|
| `impl` | EXECUTING | 重置 DONE 任务为 PENDING 重做 |
| `design` | PLANNING | 清空 `architecture` 重新规划 |
| `note` | 不回退 | 只记录，不阻断 |
| **读不到 QA 报告** | **EXECUTING** | 无依据时回实现层（**不动架构**），见 §四 |

---

## 二、两套驱动（这是当前最硬的一条债）

同一个 `phase` 被**两个入口**写，各自认识**不同的阶段子集**：

| 驱动 | 什么时候跑 | 认识的阶段 |
|---|---|---|
| `workflow.run_phase()` | 人手点"下一步" / CLI | TEMPLATE / RESEARCHING / PLANNING / EXECUTING / GATE1~3 / REVIEWING / FIXING / DONE |
| `orchestrator._auto_trigger_test_fix()` | 调度循环每一 tick | 还会推 **INTEGRATING / DELIVERING** |

**`run_phase` 不认识 `INTEGRATING` 和 `DELIVERING`** —— 走到就报 `未知 phase`。

**2026-09-11 起**：所有流转都过 `ProjectState.set_phase(phase, reason)` 这一个口，
自动留痕（from / to / reason / ts）。以前 21 处散点赋值，出事时落盘里只有最终
phase、**没有任何轨迹** —— "点通过永远弹回 GATE2"那种死循环，翻遍文件也看不出
是谁把它推回去的。现在直接读 `project.lineage` 里 `action == "phase"` 的条目。

---

## 三、多模型委员会（唯一验证过有价值的能力）

**触发条件**（两个都满足）：
1. `_is_architecture_task(task)` —— 任务文本含 `模块划分 / 数据模型 / API契约 / 技术栈 / 架构方案 / 系统架构`
   （架构阶段的 prompt 天然全含；普通用户任务一般不含）
2. `len(chain) >= 2` —— 候选链至少 2 个模型

**链条**：并行出方案 → `fuse_architecture_v2`
（提取共识/分歧 → 辩论 1~3 轮 → 定稿 → 其余委员确认 → 有异议重写 1 次）

**席位**：从候选链里取，最多 4。
**去重**：按 `api_store.canonical()` 归一 —— 两个 id 指向同一实际模型时只留一个
（别名由 `openai_agent` 在响应路径上对账写入，`/v1/models` 看不出来）。

**实测**：委员 2 席、621 秒、融合产物 17158 字符。
⚠ 慢主要慢在**提取员**那一步（见 §四），不在委员本身。

---

## 四、已知的窟窿

### 还开着的

| # | 什么 | 位置 / 症状 |
|---|---|---|
| 1 | **流程重量不分级** | `_needs_research` 只看 template + 关键词。单文件工具和多模块系统走同一套 6 维度调研 + 3 方案文档 |
| 2 | **重入语义没定义** | 重进一个阶段时哪些痕迹该清、哪些计数归零，没有声明，各写入点自行其是（防御模式 #44/#45 是它的两个投影）。**只立了 GATE2 那一个实例的规矩** |
| 3 | **提取员默认是硬编码** | `execution_judge._V2_EXTRACT_DEFAULT = "glm-5.3-flash"`；已在 `fusion.toml` 显式覆盖为 `deepseek-flash`，但**它同时是委员会成员**（自己评自己，代码有告警） |
| 4 | **审查「只报不改」** | 见下方专条 —— **不是空壳**，是闭环缺了一环，且有两处会让它整段不跑 |

### 专条：审查层到底有什么、缺什么

> 2026-09-11 更正：本文件初版写成「审查层是空的」。**说过头了** —— 它在跑，只是不闭环。

**有的**（都在执行路径上）：

| 检查 | 位置 |
|---|---|
| QA 工程师报告（`qa-report.md`） | `_run_verification`（调 LLM） |
| 安全审计师报告（`security-report.md`） | 同上，并行 |
| 结构化 QA 报告（passed/issues/verdict/fix_route） | `validator.build_qa_report` |
| 需求符合性核验 | `supervisor.check_requirement_conformance`（LLM 主 + 机械兜底） |
| 约束清单验收 | `_review.qa_acceptance_review` |
| 每个任务后的机械检查 | `_exec.py:426` → `run_post_exec_checks`（**软信号**） |
| E2E 用例执行 | 对照 `test_cases.json` |

**缺的**（被 `d3c117e` 删掉的那一环）：

```
发现问题的 AI  →  ✂️  自己改  →  再验
                 ↑ 这一环没了
```

所以责任链是 **AI 报告 → 人判 → 人决定改不改**。"发现"能力还在，"自动修"没了。

**两处会让它不跑 / 不拦**：

1. **前置条件能整段跳过**：架构没产出 `constraints` → `constraints_checklist` 为空 →
   `_run_verification` 直接 `return`，上面那张表**一行都不执行**
2. **一部分是软信号**：需求符合性对账的注释自己写着「只写软信号 + warning
   （机械关键词，**先不设 hard gate**）」—— 发现了也不拦

**为什么它仍然是理论边界的约束**：「能自动验证到多大，才能自动推进到多大」——
现在验证有了、闭环没有，所以推进仍然要人接手。

### 2026-09-11 已修（本文件初版列的 6 条里，三条已清）

| 原 # | 什么 | 怎么修的 |
|---|---|---|
| 1 | `run_phase` 不认识 INTEGRATING / DELIVERING | 照 EXECUTING 的做法交棒给调度循环，不再报"未知 phase" |
| 2 | REVIEWING / FIXING 是死代码 | `FIXING` **全仓无人赋值** → 删枚举 + 9 处引用。`REVIEWING` 留着（真实中间态，UI 靠它显示"审查中"） |
| 3 | 架构校验只警告不拦 | 落盘 + 分两档：**tasks 缺失才拦**（下一步没法干），缺 data_model 之类只记。拦在"人点通过"那一瞬，不引入自动重试循环 |

---

## 五、当前实测基线（拿来对账用）

| 项 | 值 |
|---|---|
| 测试 | 后端 **703** 通过（`tests/test_scheduler/`，~7s）／前端 28 |
| 候选链 | `deepseek-v4-pro`、`deepseek-flash`、`glm-4.7` |
| 委员会 | 能跑通，产出非空融合产物（实测 2 席 / 621 秒 / 17158 字符） |
| 单价 | 已配 11 个模型（`model_prices.json`，USD/百万 token **混合价**）。未配的如实显示"未配置价格"，**不显示 $0.00** |
| 交付 | 空清单是**正常的**：仓库里没有 README / pyproject / Dockerfile 时不编造 |

> `token_spent` 恒为 0 那个字段 2026-09-11 已删 —— 它和旁边算出来的真 `cost`
> 并排摆着，看着像"这个项目花了 0 元"。项目花费看 `/api/projects/<id>/cost`
> 的 `cost` 字段（从用量记录按 project_id 汇总）。

---

## 六、改流程前先读

- `docs/防御模式.md`（46 条）—— 动状态机 / 门禁 / worktree / 子进程 / 模型调用前**必须扫**
- `docs/演化史.md` —— 历轮为什么这么改
- `CLAUDE.md` —— 模块地图 + 验证命令
- 验证命令：`pytest tests/test_scheduler/ -q`（682 条，~14s）
